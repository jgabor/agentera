# Plan: Align Profile Path References with Decision 28

<!-- Level: light | Created: 2026-04-23 | Status: complete -->

## What

Replace all 19 manual occurrences of the legacy path `~/.claude/profile/PROFILE.md` across SPEC.md, 9 consumer SKILL.md files, DOCS.md, DOCS template, README.md, and the opencode adapter doc with profilera's platform-aware pattern (`$PROFILERA_PROFILE_DIR/PROFILE.md` with platform-appropriate defaults). Regenerate 12 auto-generated contract.md files from the corrected SPEC.md. File ISS-40 follow-up for building the actual `<!-- platform: profile-path -->` runtime substitution mechanism.

## Why

Decision 28 (firm) moved PROFILE.md to platform-appropriate data dirs. Profilera's Python scripts already write there. But every consumer SKILL.md still instructs agents to read from the legacy path. The `<!-- platform: profile-path -->` annotations were designed for runtime substitution but no runtime performs it — text is taken literally. When hej reads its own instructions and goes to `~/.claude/profile/PROFILE.md`, the file isn't there (unless auto-migration already ran). This is ISS-40 in TODO.md.

## Constraints

- profilera/SKILL.md is already correct — don't touch it
- inspektera/SKILL.md and planera/SKILL.md reference profile only via script invocation — no hardcoded path to fix
- dokumentera/SKILL.md has no profile path references — skip
- Contract.md files (12) are auto-generated — fix SPEC.md then regenerate via `scripts/generate_contracts.py`
- Archived plans are historical — skip
- Retain all `<!-- platform: profile-path -->` annotations unchanged
- Replacement text must use profilera's pattern: `$PROFILERA_PROFILE_DIR/PROFILE.md` with platform-appropriate defaults, not another hardcoded single path

## Scope

**In**: SPEC.md (5 locs), hej (2), realisera (2), optimera (1), resonera (1), orkestrera (1), inspirera (1), visionera (1), visualisera (1), DOCS.md (1), DOCS template (1), README.md (1), opencode adapter doc (2), contract.md regeneration, validate_spec.py run
**Out**: profilera/SKILL.md (already correct), inspektera/planera/dokumentera SKILL.md (no legacy path), archived plans, building the actual runtime substitution mechanism (separate follow-up plan)

## Replacement Pattern

Authoritative definition (SPEC.md Section 4, line 138 — used once):
`$PROFILERA_PROFILE_DIR/PROFILE.md` (defaulting to `$XDG_DATA_HOME/agentera/PROFILE.md` on Linux, `~/Library/Application Support/agentera/PROFILE.md` on macOS, `%APPDATA%/agentera/PROFILE.md` on Windows)

Consumer references (SKILL.md files, DOCS.md, README.md):
`$PROFILERA_PROFILE_DIR/PROFILE.md` (default: `$XDG_DATA_HOME/agentera/PROFILE.md`) <!-- platform: profile-path -->

SPEC.md capabilities table (Section 20, line 949):
`$PROFILERA_PROFILE_DIR/PROFILE.md` — profilera determines platform-appropriate data dir; override via `PROFILERA_PROFILE_DIR` env var

## Acceptance Criteria

▸ GIVEN `rg '~/.claude/profile/PROFILE.md' . --glob '!.agentera/archive/**'` WHEN run from repo root THEN 0 matches
▸ GIVEN SPEC.md Section 4 global PROFILE.md definition WHEN read THEN it references `$PROFILERA_PROFILE_DIR` with full platform matrix, not any single hardcoded path
▸ GIVEN SPEC.md Section 20 capabilities table profile-path row WHEN read THEN the implementation column references profilera's env var override pattern, not the legacy Claude dir
▸ GIVEN any consumer SKILL.md with a direct profile-read instruction WHEN an agent follows the instruction THEN the text points to `$PROFILERA_PROFILE_DIR/PROFILE.md` with the Linux default, not `~/.claude/profile/PROFILE.md`
▸ GIVEN SPEC.md updated WHEN running `python3 scripts/generate_contracts.py` THEN all 12 contract.md files regenerate without error and `rg '~/.claude/profile/PROFILE.md' skills/*/references/contract.md` returns 0 matches
▸ GIVEN all edits complete WHEN running `python3 scripts/validate_spec.py` THEN all spec alignment checks pass
