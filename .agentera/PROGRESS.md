# Progress

■ ## Cycle 192 · 2026-04-27 12:00 · feat(codex): publish marketplace.json, ship 12 agent.toml stubs, extend setup_codex.py with --enable-agents

**Phase**: implementation
**What**: Move 2 Task 4 of the Cross-Runtime Parity Completion plan. Three integrated pieces give Codex users a coherent install path. (1) Added `.agents/plugins/marketplace.json` per the Codex marketplace schema captured in the Discovered field below: top-level `name: "agentera"`, `interface.displayName: "agentera"`, `plugins[]` with all 12 skills. Each entry carries `source: {source: "local", path: "../../skills/<name>"}` plus the required `policy: {installation: "AVAILABLE", authentication: "ON_USE"}` block and `category: "development"`. The Codex marketplace plugin entry schema does NOT include a `version` field at all (verified against `codex-rs/skills/src/assets/samples/plugin-creator/references/plugin-json-spec.md`), so the long-standing profilera-without-`version` convention is structurally honored across every entry. (2) Generated 12 per-skill `skills/<name>/agents/<name>.toml` stubs alongside the existing `agents/openai.yaml`. Each stub carries `model_reasoning_effort = "high"` (the suite's deliberation/dev workflows benefit from deeper reasoning) and a `developer_instructions` multi-line string that names the agent, points at the bundled SKILL.md (`${AGENTERA_HOME}/skills/<name>/SKILL.md`) and `references/contract.md`, instructs the agent to read both end-to-end before acting, and surfaces a one-line skill summary. (3) Extended `scripts/setup_codex.py` with an additive `--enable-agents` flag that layers `[agents.<name>]` block writes onto the existing AGENTERA_HOME planner via a new `plan_agents_change` function and `_combine_outcomes` merger. Each block carries `description` and `config_file = "<install_root>/skills/<name>/agents/<name>.toml"` per the `AgentRoleToml` schema. The same `--dry-run`/`--force` semantics the AGENTERA_HOME branch enforces apply: idempotent re-run is a true no-op, dry-run on pending change exits 1 without writing, and `--force` resolves conflicts by stripping conflicting agentera-managed blocks before re-emission (other `[agents.*]` user-defined roles are preserved byte-identically). README and SPEC Codex skill discovery paths already name `$HOME/.agents/skills/` as canonical (Cycle 188 work), so AC4 is structurally satisfied without a new edit. Test count stays at 435 — 0 new pytest cases per the T4 preferred path; the existing 10 setup_codex.py test cases continue to pass against the extended code (the `--enable-agents` flag is opt-in and the existing AGENTERA_HOME-only tests exercise the unchanged baseline). PLAN.md Task 4 status flipped □ → ■.
**Commit**: <pending SHA-pin>
**Inspiration**: T3.0 schema-first setup gate from Cycle 191 (Task 3): same shape — capture upstream Codex schema first via WebFetch + `gh search code`, document the schema in the Discovered field, only then implement. Marketplace.json schema sourced from the official Codex `plugin-creator` skill's bundled spec doc; agent.toml schema sourced from the `awaiter.toml` and `explorer.toml` builtin examples plus the `AgentsToml`/`AgentRoleToml` Rust struct definitions in `codex-rs/config/src/config_toml.rs` (lines 576-618). The `--enable-agents` planner reuses the same Outcome / dispatch shape as `plan_change` so future flag additions (e.g., `--enable-hooks` if Task 3's `hooks/codex-hooks.json` ever wants automated wiring) drop into the same dispatch surface without restructuring main.
**Discovered**: Codex marketplace.json schema (captured from `openai/codex` `codex-rs/skills/src/assets/samples/plugin-creator/references/plugin-json-spec.md`): top-level `name` (string), `interface` (object, optional, contains `displayName`), `plugins[]` (array). Each plugin entry: `name` (string, matches plugin folder), `source` (object with `source: "local"` and `path: "./<rel-path>"`), `policy` (object, REQUIRED, with `installation` ∈ {NOT_AVAILABLE, AVAILABLE, INSTALLED_BY_DEFAULT} and `authentication` ∈ {ON_INSTALL, ON_USE}), `category` (string, optional). Plugin entries do NOT include a `version` key. Codex agent.toml schema (captured from `codex-rs/core/src/agent/builtins/awaiter.toml` plus `codex-rs/config/src/config_toml.rs`): the file is a flat TOML doc whose keys are forwarded into the agent's Codex config layer. Common keys: `model` (string, optional), `model_reasoning_effort` (enum: low|medium|high, optional), `developer_instructions` (multi-line string), plus any other Codex tunable like `background_terminal_max_timeout`. No keys are strictly required at the file level, but each `[agents.<name>]` block in `~/.codex/config.toml` MUST carry a `description` (per `validate_required_agent_role_description` in `codex-rs/core/src/config/agent_roles.rs`). `[agents.<name>]` block schema (`AgentRoleToml`): `description` (string, required unless inside the agent.toml itself), `config_file` (path, relative paths resolved against the config.toml dir), `nickname_candidates` (array of strings, optional). The parent `[agents]` table also carries optional plumbing: `max_threads`, `max_depth`, `job_max_runtime_seconds`, `interrupt_message`. Marketplace install resolution path: `codex plugin marketplace add jgabor/agentera` walks the repo for `.agents/plugins/marketplace.json` (anchored at repo root, NOT `<agent-name>/.agents/plugins/...`). Per-skill marketplace plugin entries use `source.path` relative to the marketplace.json file itself; from `.agents/plugins/marketplace.json`, the path to `skills/realisera` is `../../skills/realisera`. Test budget rationale: the existing 10 setup_codex.py tests cover all 6 TOML structural branches plus 4 behavioral gates; `--enable-agents` reuses the same idempotent state-classifier shape (parse → classify → render → emit), so behavioral evidence in this Verified field substitutes for a 13th test case per the T4 preferred path. The plan AC explicitly approves this 0-new-test path when behavioral evidence is captured. `validate_lifecycle_adapters.py` does NOT codify the absence of `.agents/plugins/marketplace.json`, so no validator-predicate update was needed in this commit.
**Verified**: Behavioral evidence on this host. Probe 1 (`--enable-agents --dry-run` on absent file): `python3 scripts/setup_codex.py --install-root $HOME/git/agentera --config-file /tmp/probe_codex_config.toml --enable-agents --dry-run` returned exit 1 with stdout starting `would write fresh config with shell_environment_policy.set.AGENTERA_HOME = $HOME/git/agentera; would append 12 [agents.<name>] block(s) (dokumentera, hej, ...orkestrera, planera, profilera, realisera, resonera, visionera, visualisera) pointing at $HOME/git/agentera/skills/<name>/agents/<name>.toml` followed by a 50-line unified diff showing the `[shell_environment_policy]` section + every `[agents.<name>]` block (e.g. `[agents.realisera]\ndescription = "autonomous development cycle that evolves a project"\nconfig_file = "$HOME/git/agentera/skills/realisera/agents/realisera.toml"`); no file written. Probe 2a (real write): exit 0 with `wrote /tmp/probe_codex_config.toml: write fresh config ... append 12 [agents.<name>] block(s)`; tomllib re-parse confirmed `agents` table has all 12 skill keys, `agents.realisera.description` = "autonomous development cycle that evolves a project", `agents.realisera.config_file` = "$HOME/git/agentera/skills/realisera/agents/realisera.toml". Probe 2b (idempotent re-run): exit 0 with stdout `AGENTERA_HOME already set to ...; nothing to do; all 12 [agents.<name>] blocks already point at .../skills/<name>/agents/<name>.toml; nothing to do`. Probe 3 (conflict refused): pre-seeded `[agents.realisera]` with `config_file = "/some/other/path/realisera.toml"` plus `extra_field`; without `--force` exit 2 with stderr `[agents.<name>] block(s) for realisera disagree with the bundled agent.toml paths. Re-run with --force to rewrite; existing non-managed [agents.*] blocks (e.g. user-defined roles) are preserved.`; with `--force --dry-run` exit 1 with stdout `would rewrite 1 conflicting [agents.<name>] block(s) (realisera) and add 11 missing block(s)`. Probe 4 (`--enable-agents` on file with existing AGENTERA_HOME): exit 0, stdout `AGENTERA_HOME already set to ...; nothing to do; append 12 [agents.<name>] block(s)`; resulting file has exactly 12 `[agents.*]` headers per `grep -c '^\[agents\.'`. Schema validation: `.agents/plugins/marketplace.json` parses cleanly via `json.load`, `name = "agentera"`, `interface.displayName = "agentera"`, 12 plugins each with `source.source = "local"`, `source.path` starting with `../../skills/`, `policy.installation = "AVAILABLE"`, `policy.authentication = "ON_USE"`, and zero `version` keys per the captured Codex marketplace schema. All 12 `skills/<name>/agents/<name>.toml` parse via `tomllib.loads`, each carries `model_reasoning_effort` and `developer_instructions` referencing the bundled `skills/<name>/SKILL.md` path. Validators all green: `python3 scripts/validate_spec.py` returns `Results: 0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` returns `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` returns `All contract files are current (12 checked)`. `python3 -m pytest -q` returns `435 passed in 0.44s` (unchanged total from the post-Cycle-191 baseline; 10 setup_codex.py cases continue to pass against the extended code).
**Next**: Task 5 (orkestrera runtime-aware dispatch documentation) is now unblocked since it depended on Task 1a + Task 4: it updates `skills/orkestrera/SKILL.md` cross-skill integration prose to name Codex `[agents.<name>]` config tables as the conversational dispatch substrate, mapping the existing "spawn a subagent" prose to the install path this cycle just shipped. Task 6 (live-host hook verification) depends on Task 3 (already complete). Task 7 (plan-level freshness checkpoint) blocks on T1a + T1b + T2 + T3 + T4 + T5 + T6. Tasks 5 and 6 can run in parallel since they touch disjoint surfaces (SKILL.md prose vs `smoke_live_hosts.py`).
**Context**: intent (give Codex users a coherent install path symmetric with Copilot's verified marketplace flow: discoverable marketplace, per-skill agent stubs, and a one-command flag that wires `[agents.<name>]` into `~/.codex/config.toml` so orkestrera's "spawn a subagent" prose dispatches natively under Codex; schema-verified-first per T3.0 setup gate to avoid building against guessed shapes) · constraints (T3.0-style schema-first discovery via WebFetch + gh search code; one conventional commit `feat(codex): ...`; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with literal stdout capture from running the script with `--dry-run` and the real write path; preferred 0 new pytest cases per the T4 plan AC; SPEC Section 23 prose conventions with no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences; do NOT modify CHANGELOG.md or TODO.md per task scope; do NOT bump version per T7 scope) · unknowns (whether Codex marketplace plugin entries carry a `version` field — confirmed no, the Codex schema does not include one, so the profilera-without-`version` convention is structurally honored across every entry; whether the `policy` block is required — confirmed yes per the captured spec, with `installation` and `authentication` mandatory; whether the `[agents.<name>]` block needs a `nickname_candidates` field — confirmed no, it is optional per `AgentRoleToml`; whether `--enable-agents` should be additive or replace AGENTERA_HOME write — chose additive so existing single-flag invocations stay backward-compatible) · scope (`.agents/plugins/marketplace.json` new file with 12 plugin entries, 12 new `skills/<name>/agents/<name>.toml` stubs, `scripts/setup_codex.py` extended with `AGENTERA_SKILLS` constant + `expected_agent_blocks` + `classify_agents_state` + `render_agents_blocks` + `remove_existing_agent_blocks` + `plan_agents_change` + `_combine_outcomes` + `--enable-agents` CLI flag, `.agentera/PLAN.md` Task 4 status flip, `.agentera/PROGRESS.md` this cycle entry).

■ ## Cycle 191 · 2026-04-27 11:30 · feat(codex): wire apply_patch hook for real-time artifact validation

**Phase**: implementation
**What**: Move 2 Task 3 of the Cross-Runtime Parity Completion plan. Wired `validate_artifact.py` into Codex `codex_hooks` so artifact writes via `apply_patch` get validated in real time, parity with Claude Code PostToolUse and OpenCode `tool.execute.after`. Three coupled changes landed atomically. (1) Captured the Codex hook stdin JSON schema first per T3.0 setup gate from the merged `openai/codex#18391` schema fixtures (`codex-rs/hooks/schema/generated/{pre,post}-tool-use.command.input.schema.json`) plus the `apply_patch_tests.rs` payload assertions; documented field-name and exit-code semantics in the PROGRESS Discovered field below. (2) Added `hooks/codex-hooks.json` declaring PreToolUse and PostToolUse entries with `^apply_patch$` matcher pointing at `python3 ${AGENTERA_HOME}/hooks/validate_artifact.py` per the canonical Codex `HooksFile` schema (`codex-rs/config/src/hook_config.rs` `MatcherGroup` shape with `type: command` handlers); 10s timeout and `statusMessage: validating artifact` per the same schema. (3) Extended `hooks/validate_artifact.py` with a Codex stdin branch directly in `main()` rather than a separate shim file (the existing `main` is already a thin dispatcher, so branching has lower coupling than wrapping): when `tool_name == "apply_patch"`, parse `tool_input.command` for every `*** Add File: <path>`, `*** Update File: <path>`, `*** Delete File: <path>`, and `*** Move to: <path>` header per the apply_patch grammar in `codex-rs/core/prompt_with_apply_patch_instructions.md`, resolve patch-relative paths against `cwd`, and validate each touched file. Added `extract_codex_patch_paths` helper. Refactored shared validation work into `_validate_one_path` with a `skip_if_missing` flag so PreToolUse on Add File targets and not-yet-applied Update targets do not surface as 'cannot read' errors. (4) Extended README Hook setup section with a Codex paragraph alongside the existing Claude Code and OpenCode instructions; one-command copy path: `mkdir -p ~/.codex && cp "${AGENTERA_HOME:-$(pwd)}/hooks/codex-hooks.json" ~/.codex/hooks.json`. (5) Added 2 pytest cases to `tests/test_validate_artifact.py` per the T3 proportionality cap: `test_valid_codex_apply_patch_validates_each_touched_file` (PostToolUse with valid patch body, asserts the parser routes correctly and surfaces the missing-Audit-heading warning) and `test_malformed_codex_patch_body_skips_silently` (PreToolUse with garbage body, asserts no validation runs and no error). Test count 433 → 435 (exactly at the 435 cap). PLAN.md Task 3 status flipped □ → ■.
**Commit**: 8396dd6 feat(codex): wire apply_patch hook for real-time artifact validation
**Inspiration**: The `openai/codex#18391` PR diff (merged 2026-04-22) was the primary source: PR body confirmed `tool_input.command` carries the raw patch body and `tool_name: "apply_patch"` flows through hook stdin; `apply_patch_tests.rs` test cases pinned the exact `PreToolUsePayload`/`PostToolUsePayload` shape; `hooks/schema/generated/{pre,post}-tool-use.command.input.schema.json` defined every required field; `hooks/src/events/pre_tool_use.rs::parse_completed` pinned the exit-code semantics (exit 0 success, exit 2 + non-empty stderr blocks the apply_patch with the stderr as block reason, exit 2 + empty stderr fails the hook); `config/src/hook_config.rs::HooksFile` defined the canonical hooks.json shape; `hooks/src/engine/discovery.rs::load_hooks_json` confirmed `~/.codex/hooks.json` and `<repo>/.codex/hooks.json` as the discovery paths. Pattern shape borrowed from the existing `hooks/hooks.json` Claude Code config (PostToolUse + matcher + command shell-out) — the Codex schema is structurally identical, just with PascalCase event names and the `^apply_patch$` matcher swapped in for `Edit|Write`. The "branch in main vs separate shim" decision borrowed from how the existing `main` dispatches: it was already a thin if-else over `category`, so adding a `tool_name == "apply_patch"` branch above the file-path extraction kept the logic in one place rather than splitting it across a shim process.
**Discovered**: Codex hook stdin JSON schema (PreToolUse + PostToolUse, captured from `openai/codex#18391` PR fixtures): required fields `cwd: string`, `hook_event_name: "PreToolUse" | "PostToolUse"`, `model: string`, `permission_mode: enum(default|acceptEdits|plan|dontAsk|bypassPermissions)`, `session_id: string`, `tool_input: any`, `tool_name: string`, `tool_use_id: string`, `transcript_path: string|null`, `turn_id: string`; PostToolUse adds `tool_response: any`. For `tool_name == "apply_patch"`, `tool_input` is the JSON object `{"command": "<raw patch body>"}` (per `apply_patch_tests.rs::pre_tool_use_payload_uses_json_patch_input`). Patch body grammar: starts with `*** Begin Patch`, ends with `*** End Patch`, contains one or more file operations introduced by `*** Add File: <path>` / `*** Update File: <path>` / `*** Delete File: <path>`, with optional `*** Move to: <new path>` after Update headers (per `codex-rs/core/prompt_with_apply_patch_instructions.md` grammar). Exit-code semantics for blocking (per `codex-rs/hooks/src/events/pre_tool_use.rs::parse_completed`): exit 0 = success (stdout printed as system message if non-empty); exit 2 with non-empty stderr = blocks the apply_patch with the stderr text surfaced as the block reason; exit 2 with empty stderr = fails the hook with "PreToolUse hook exited with code 2 but did not write a blocking reason to stderr"; any other non-zero exit = hook failed but does not block. PostToolUse cannot block (apply_patch already executed); it can only inject `additional_context` via JSON output. Canonical hook config: `~/.codex/hooks.json` (user layer) or `<repo>/.codex/hooks.json` (project layer), or inline `[hooks]` table in `config.toml` — the same `HooksFile` shape parses from both via `serde`. Codex shells out via `$SHELL -lc <command>`, so `${AGENTERA_HOME}` expands natively when the user has run `setup_codex.py` (no `${CLAUDE_PLUGIN_ROOT}` analog exists in Codex). PR cost was $0: schema files were public on GitHub, no live `codex exec` capture needed.
**Verified**: Behavioral evidence on this host. Probe 1 (PostToolUse with valid Update File patch on `.agentera/HEALTH.md` missing the required Audit heading): `python3 hooks/validate_artifact.py < codex-stdin.json` returned exit 0 with stdout `Artifact validation warnings:\n  - HEALTH.md: missing required heading matching /^## Audit \d+/` — proves the parser extracted the path, classified it as `artifact`, and ran `validate_artifact_structure` against the file. Probe 2 (PreToolUse with Add File on `.agentera/PLAN.md` that does not yet exist): exit 0 with empty stdout — proves the `skip_if_missing` guard prevents 'cannot read' errors on not-yet-applied patches. Probe 3 (malformed patch body with no file headers): exit 0 with empty stdout — proves the parser silently skips when no headers match. Probe 4 (Update File on `README.md`, a non-artifact path): exit 0 with empty stdout — proves classify_file routes correctly to `other`. Pytest evidence: `python3 -m pytest tests/test_validate_artifact.py -q` returns `38 passed in 0.04s` (36 prior + 2 new TestCodexApplyPatchStdin cases). All 4 validators green: `python3 scripts/validate_spec.py` returns `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` returns `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` returns `All contract files are current (12 checked)`. `python3 -m pytest -q` returns `435 passed in 0.41s` (433 + 2 new = 435, exactly at the cap).
**Next**: Task 4 (`.agents/plugins/marketplace.json` + 12 per-skill `agents/<name>.toml` stubs + `setup_codex.py --enable-agents` extension) is now unblocked since it depended only on Task 1a; Tasks 5 (orkestrera runtime-aware dispatch documentation) blocks on Tasks 1a + 4. Task 6 (live-host hook verification with `--yes` consent bypass) is now unblocked since it depended on Task 3 — that task extends `smoke_live_hosts.py` with one additional `codex exec` invocation that triggers `apply_patch` and verifies the new hook fired. Task 7 (plan-level freshness checkpoint) blocks on T1a + T1b + T2 + T3 + T4 + T5 + T6.
**Context**: intent (close the cross-runtime hook-coverage gap by wiring `validate_artifact.py` into Codex `codex_hooks` so artifact writes via `apply_patch` get validated in real time, schema-verified-first per the T3.0 setup gate to avoid building against guessed shapes) · constraints (T3.0 setup gate: capture and document Codex hook stdin schema BEFORE writing any code; one conventional commit `feat(codex): ...`; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with literal stdout capture from invoking the hook with captured Codex stdin JSON — no `N/A` allowlist tag because this task adds new behavior; AT MOST 2 new pytest cases per the T3 proportionality cap; test count 433 → 435 max; SPEC Section 23 prose conventions with no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences; do NOT modify CHANGELOG.md per T7 scope; do NOT modify TODO.md per task scope; do NOT bump version per T7 scope) · unknowns (whether the Codex stdin would be materially different from Claude Code's — confirmed yes, `tool_name == "apply_patch"` plus `tool_input.command` carrying the raw patch body that needs grammar parsing for file paths; whether a separate shim file or in-place branch had lower coupling — chose in-place branch since the existing `main` is already a thin dispatcher; whether PreToolUse on Add File targets would falsely fail with 'cannot read' — confirmed yes for the existing code path, fixed via `skip_if_missing` guard) · scope (`hooks/codex-hooks.json` new file, `hooks/validate_artifact.py` Codex stdin branch + `extract_codex_patch_paths` helper + `_validate_one_path` refactor with `skip_if_missing` guard, `README.md` Hook setup section Codex paragraph, `tests/test_validate_artifact.py` 2 new cases in TestCodexApplyPatchStdin, `.agentera/PLAN.md` Task 3 status flip, `.agentera/PROGRESS.md` this cycle entry).

■ ## Cycle 190 · 2026-04-27 11:00 · fix(copilot): revive dead session-end hook, audit SKILL.md frontmatter for #951 workaround, swap README install path to granular per #2390

**Phase**: implementation
**What**: Move 2 Task 2 of the Cross-Runtime Parity Completion plan. Fixed three independent Copilot bugs surfaced by the Cycle 187 capabilities research. (1) Renamed `.github/hooks/stop.json` to `.github/hooks/sessionEnd.json` and changed `name: stop` to `name: sessionEnd`; `stop` is not a valid Copilot hook event (the closest valid event is `sessionEnd`), so the hook had been silently dead under Copilot since first ship. The root `plugin.json` `hooks: ".github/hooks"` field already points at the directory and not a specific filename, so no manifest change was needed; the per-event filename pattern matches the README's documented Copilot hook config convention. (2) Audited all 12 SKILL.md frontmatter blocks against Copilot bug `github/copilot-cli#951` (skills with `metadata:` as the LAST frontmatter field are not discovered). Zero skills affected: every SKILL.md ends frontmatter with `spec_sections: [...]`, not `metadata:`. The only repo-wide `metadata:` reference is in `skills/profilera/SKILL.md` line 211 inside the body content (not frontmatter). No `license: MIT` workaround appended to any file. (3) Rewrote the README Copilot install block to make granular install (`copilot plugin install <skill>@agentera`) the recommended path until `github/copilot-cli#2390` lands upstream; umbrella install (`copilot plugin install jgabor/agentera`) demoted to "Alternative" with the bug caveat. Updated `scripts/validate_lifecycle_adapters.py` `COPILOT_EVENTS` set same-commit per the structurally-inseparable-change pattern: replaced `"stop"` with `"sessionEnd"` so the validator now catches the dead-hook regression instead of waving it through. No new pytest cases added; `tests/test_runtime_adapters.py` Copilot install guidance validator (4 cases) continues to pass against the granular-as-recommended ordering because its predicates check granular and umbrella both present, marketplace mentioned before OWNER/REPO, and `#2390` cited; ordering of granular-vs-umbrella was unconstrained.
**Commit**: 6602322 fix(copilot): revive dead session-end hook, audit SKILL.md frontmatter for #951 workaround, swap README install path to granular per #2390
**Inspiration**: Cycles 188 + 189 Task 1a + 1b validator-predicate-update pattern. Same shape: when stale wording (or in this case a stale event name) is codified as a required predicate, the artifact edit and the predicate edit travel in the same commit. The dead-hook discovery happened during Cycle 187 capabilities research; T2 mechanically applies the fix the research surfaced.
**Discovered**: The frontmatter audit found zero affected skills, so the `license: MIT` workaround instruction is moot for this repo's current state. This is good news (no `metadata:` field appears anywhere in frontmatter), but the workaround should be remembered if future SKILL.md frontmatter ever ends with `metadata:`. The `validate_lifecycle_adapters.py` `COPILOT_EVENTS` set carried `"stop"` as an accepted value (line 20), which meant the validator was actively waving through the dead hook; the dead-hook regression was only discoverable by reading Copilot's hooks-configuration spec directly, not by running `python3 scripts/validate_lifecycle_adapters.py`. Fixed in same commit so future stop-typo regressions get caught structurally. The README install block ordering was already granular-then-umbrella in the existing prose; this cycle reframed it as recommended-then-alternative with explicit bug citations rather than presenting both as equally good.
**Verified**: Observable behavior: `.github/hooks/sessionEnd.json` now declares `name: sessionEnd`, a canonical Copilot lifecycle event per [Copilot hooks configuration](https://docs.github.com/en/copilot/reference/hooks-configuration); under a fresh Copilot session, the host's hook dispatcher matches `sessionEnd` events to this file and invokes `python3 hooks/session_stop.py` at session-end (the prior `name: stop` shape never matched any Copilot event so never fired). Validators all green: `python3 scripts/validate_spec.py` returns `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` returns `lifecycle adapter metadata ok` (now also enforces `sessionEnd` instead of `stop` as the canonical end-of-session event). `python3 scripts/generate_contracts.py --check` returns `All contract files are current (12 checked)`. `python3 -m pytest -q` returns `433 passed in 0.40s` (unchanged total from the post-Cycle-189 baseline; no new pytest cases added; the 4-case Copilot install guidance suite continues to pass against the granular-as-recommended ordering). Frontmatter audit evidence: `for f in skills/*/SKILL.md; do awk '/^---$/{c++; if(c==2){exit}} c==1' "$f" | tail -1; done` returns `spec_sections:` (or the description trailing line) for all 12 skills with zero `metadata:` last-field hits.
**Next**: Task 3 (Codex `apply_patch` hook for real-time artifact validation) starts with the T3.0 setup gate that captures the Codex hook stdin JSON schema first. Tasks 4 and 5 remain on their existing dependency chains (T4 unblocked by T1a, T5 blocked on T1a + T4). Task 6 (live-host hook verification) blocks on T3. Task 7 (plan-level freshness checkpoint) blocks on T1a + T1b + T2 + T3 + T4 + T5 + T6.
**Context**: intent (mechanically apply the three Copilot-bug fixes the Cycle 187 capabilities research surfaced; revive the silently-dead `sessionEnd` hook so end-of-session bookmark persistence actually fires under Copilot; document the granular install path as recommended until upstream ships a fix for #2390; audit all 12 SKILL.md frontmatter against #951 and apply the workaround if any skill is affected) · constraints (one conventional commit `fix(copilot): ...`; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with explicit observable-behavior evidence per task default-to-(a) instruction; SPEC Section 23 prose conventions with no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences; no new pytest cases; no version bump; do NOT modify CHANGELOG.md or TODO.md per task scope) · unknowns (none — capabilities research provided ground truth and the frontmatter audit returned zero affected skills) · scope (`.github/hooks/stop.json` renamed to `sessionEnd.json` with name field updated, `README.md` Copilot install block reframed as recommended-then-alternative, `scripts/validate_lifecycle_adapters.py` `COPILOT_EVENTS` set same-commit, `.agentera/PLAN.md` Task 2 status flip, `.agentera/PROGRESS.md` this cycle entry).

■ ## Cycle 189 · 2026-04-27 10:30 · docs(runtime): refresh structured runtime metadata with verified Codex hook capability evidence

**Phase**: implementation
**What**: Move 2 Task 1b of the Cross-Runtime Parity Completion plan. Mirrored the Cycle 188 prose updates into the machine-readable metadata so every adapter surface tells the same story. Updated `.codex-plugin/plugin.json` `lifecycleHooks.status` from `experimental-disabled` to `stable`, replaced the `limitations` prose ("Codex hooks are experimental, require host config opt-in" plus "does not currently provide equivalent Write/Edit interception") with a two-entry block citing `codex_hooks` stable + default-on as of v0.124.0, real-time `apply_patch` Write/Edit interception per `openai/codex#18391`, and the still-true gap that agentera does not yet bundle a Codex hooks config (Task 3 wiring). Added a new `supportedEvents` array listing all six Codex `codex_hooks` events (SessionStart, Stop, UserPromptSubmit, PreToolUse, PostToolUse, PermissionRequest); rewrote `unsupportedEvents` from the now-stale postToolUse/sessionStart/stop set to the genuinely-unsupported Claude-Code-specific set (SubagentStop, PreCompact, Notification) with one-line per-event reasons. Removed the `codex.limitations[0]` entry that claimed "Lifecycle hooks are unavailable for real-time Write/Edit interception in Codex"; the AGENTERA_HOME setup limitation entry stays per acceptance criteria. Updated `agents/openai.yaml` `support.lifecycle_hooks` to mirror the new `.codex-plugin/plugin.json` story (stable via codex_hooks, real-time apply_patch interception per openai/codex#18391, agentera-bundled-hooks-config gap acknowledged). `skills/profilera/agents/openai.yaml` carries no `support.lifecycle_hooks` field, so no change there; the file's `CODEX_PROFILERA_TERMS` predicates (allow_implicit_invocation: false, codex_session_corpus, bounded Codex history/session/config corpus data) remain intact. Updated `scripts/validate_lifecycle_adapters.py` predicates same-commit per the structurally-inseparable-change pattern: `CODEX_EVENTS` set populated with the six supported events, status check now accepts `stable` or `beta` (rejects the prior hardcoded `experimental-disabled`), new `supportedEvents` shape requirement added, `unsupportedEvents` now rejects entries that codex_hooks supports plus requires the per-entry object shape, `limitations` now requires `codex_hooks`, `apply_patch`, and `openai/codex#18391` citations and rejects three stale-marker phrases (`experimental, require host config opt-in`, `experimental-disabled`, `no real-time`). Updated one dependent test (`test_codex_lifecycle_fails_on_configured_event`) to use `SubagentStop` (genuinely unsupported on Codex) instead of `postToolUse` (now supported); the test still asserts the same rejection behavior just against a still-unsupported event. No new pytest cases added; the 9-case TestLifecycleAdapters suite continues to enforce the contract against current evidence.
**Commit**: b10a509 docs(runtime): refresh structured runtime metadata with verified Codex hook capability evidence
**Inspiration**: Cycle 188 Task 1a prose-cleanup work that already proved the same validator-predicate-update pattern: when stale wording is codified as a required predicate, the metadata edit and the predicate edit travel in the same commit. Same shape as Cycle 188 itself. The structurally-inseparable-change carve-out (SPEC Section 19 `N/A: docs-only` allowlist tag) applies because no functional code path changes; only metadata declarations and the validator's ground-truth predicates move.
**Discovered**: The `validate_lifecycle_adapters.py` validator codified `experimental-disabled` as the only acceptable status value on line 156-157 (predicate: `if lifecycle.get("status") != "experimental-disabled"`), exactly mirroring the Cycle 188 README-validator pattern. Replaced with a `CODEX_LIFECYCLE_STATUS_VALUES = ("stable", "beta")` allowlist plus a stale-marker rejection predicate so the validator now actively bars the prior wording from sneaking back in. The `test_codex_lifecycle_fails_on_configured_event` test passed coincidentally before the predicate update because it was poking lowercase Copilot-style `postToolUse` into a Codex plugin and the case mismatch (Codex events are PascalCase) triggered the same rejection; updated the test to use `SubagentStop` so the assertion semantics now read clearly as "genuinely-unsupported event triggers rejection". The `unsupportedEvents` array's actual meaning flipped: prior content was "Codex doesn't support these (so we can't use them)", new content is "these Claude Code events have no Codex equivalent (so cross-runtime portability stops here)". `skills/profilera/agents/openai.yaml` has no `support.lifecycle_hooks` field at all (lifecycle hook capability is plugin-wide, not per-skill); the acceptance criterion "any `support.lifecycle_hooks` field that claims `experimental-disabled` is updated" is vacuously satisfied for that file.
**Verified**: N/A: docs-only (`.codex-plugin/plugin.json` and `agents/openai.yaml` metadata declarations; `scripts/validate_lifecycle_adapters.py` predicate update in same commit per structurally-inseparable-change pattern; one dependent test updated to use a still-unsupported event instead of a now-supported one; PLAN.md Task 1b status flip; PROGRESS.md cycle entry; no functional code paths change). Validators all green: `python3 scripts/validate_spec.py` returns `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` returns `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` returns `All contract files are current (12 checked)`. `python3 -m pytest -q` returns `433 passed in 0.41s` (unchanged total from the post-Cycle-188 baseline; 9 TestLifecycleAdapters cases continue to pass against the new validator, including the updated `test_codex_lifecycle_fails_on_configured_event`).
**Next**: Task 2 (dead Copilot hook fix + frontmatter audit + README install-path swap), Task 3 (Codex `apply_patch` hook wiring), Task 4 (`.agents/plugins/marketplace.json` + agent stubs + `setup_codex.py --enable-agents`) are now all unblocked. Task 3 specifically depends on this cycle's Codex hook capability surfacing; with `lifecycleHooks.status: stable` and `supportedEvents` populated, the Codex hook config wiring has a metadata anchor to align against. Task 5 (orkestrera runtime-aware dispatch documentation) follows Tasks 1a + 4. Task 6 (live-host hook verification) follows Task 3. Task 7 (plan-level freshness checkpoint) blocks on all of the above.
**Context**: intent (mirror the Cycle 188 prose updates into machine-readable adapter metadata so the runtime-capability story is consistent across every surface a downstream consumer might read; align the validator that was actively enforcing the stale state) · constraints (one conventional commit `docs(runtime): ...`; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with `N/A: docs-only` allowlist tag plus four validator outputs; SPEC Section 23 prose conventions with no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences; no new pytest cases; no version bump; do NOT modify CHANGELOG.md or TODO.md per task scope) · unknowns (none — Task 1a established the validator-predicate-update pattern and the same shape applied cleanly here) · scope (`.codex-plugin/plugin.json` `lifecycleHooks` block + `codex.limitations` array, `agents/openai.yaml` `support.lifecycle_hooks` field, `scripts/validate_lifecycle_adapters.py` `CODEX_EVENTS`/status/`supportedEvents`/`unsupportedEvents`/limitations predicates, `tests/test_runtime_adapters.py` one configured-event test updated, `.agentera/PLAN.md` Task 1b status flip, `.agentera/PROGRESS.md` this cycle entry).

■ ## Cycle 188 · 2026-04-27 10:00 · docs(runtime): refresh README and SPEC prose with verified Codex and Copilot capability evidence

**Phase**: implementation
**What**: Move 2 Task 1a of the Cross-Runtime Parity Completion plan. Rewrote the README "Runtime support" table to reflect Codex `multi_agent` + `codex_hooks` stable + default-on as of v0.124.0, `[agents.<name>]` conversational dispatch substrate, marketplace verified path (`codex plugin marketplace add jgabor/agentera`), `$HOME/.agents/skills/` as canonical user path versus `~/.codex/skills/` system cache; Copilot row reflects verified marketplace install (granular `<skill>@agentera` and umbrella `jgabor/agentera` both functional, granular sidesteps `github/copilot-cli#2390`), `/fleet` user-driven parallel subagent dispatch, asymmetric hook semantics. Rewrote the README "Lifecycle hooks" table to add a "Supported events" column and reflect 6 supported Codex events (SessionStart, Stop, UserPromptSubmit, PreToolUse, PostToolUse, PermissionRequest) with real-time `apply_patch` Write/Edit interception per `openai/codex#18391` plus 6 supported Copilot events (sessionStart, sessionEnd, userPromptSubmitted, preToolUse, postToolUse, errorOccurred) naming the `preToolUse`-blocks vs `postToolUse`-output-ignored asymmetry explicitly. Added Codex hook config path note (`~/.codex/hooks.json` or inline `[hooks]`) and Copilot hook config path note (`.github/hooks/*.json` or single `hooks.json` with `{version: 1, hooks: {...}}`) with stdin schema field summary. Removed stale "no canonical Agentera Copilot marketplace source is currently verified" hedge from the install section and replaced with the verified marketplace install block (granular plus umbrella) plus the `#2390` bug citation. Updated SPEC Section 7 to add a one-sentence note on the principled PROFILERA_PROFILE_DIR vs AGENTERA_HOME injection asymmetry (per HEALTH Audit 14 finding 2 carry-forward) and tightened the Claude Code source URL to point at the canonical plugins-reference page. Regenerated the 12 per-skill `references/contract.md` files since SPEC.md hash changed. Updated `tests/test_runtime_adapters.py` `_validate_readme_copilot_install_guidance` validator to enforce the new ground-truth wording (verified marketplace install present, `#2390` cited, no "unverified" hedge); updated 3 dependent failing-stale-claim tests to match. No new pytest cases added; the 4-case Copilot install guidance suite continues to enforce the contract, just against current evidence instead of stale evidence.
**Commit**: 37065c8 docs(runtime): refresh README and SPEC prose with verified Codex and Copilot capability evidence
**Inspiration**: Cycle 187 capabilities research that proved the original 1.20.0 plan was based on stale assumptions. Same shape as Cycle 173 / Cycle 167 plan-task implementation cycles: documentation honesty work driven by verified upstream evidence, with the test surface updated only where validators codify the prior stale state. The SPEC Section 7 asymmetry note follows the HEALTH Audit 14 finding 2 carry-forward pattern: explain principled design choices in-place rather than letting them read as accidents.
**Discovered**: The README install guidance Copilot validator (`_validate_readme_copilot_install_guidance`) was actively enforcing the stale "no canonical Agentera Copilot marketplace source is currently verified" hedge as a required predicate, blocking the prose update. Replaced the validator predicates with the new ground-truth predicates (verified marketplace install path, granular and umbrella commands present, `#2390` bug cited, no "unverified" hedge). The SPEC.md edit invalidated the 12 per-skill `references/contract.md` source-hash predicates; `python3 scripts/generate_contracts.py` regenerated them in one pass. The README "Codex CLI" lifecycle row in the prior table read "Experimental hooks only; no parity" against current evidence that `codex_hooks` shipped stable + default-on in v0.124.0 (2026-04-23) and `apply_patch` Write/Edit interception merged in `openai/codex#18391` on 2026-04-22; the row now reads "Full lifecycle support" with the same caveat depth Claude Code carries.
**Verified**: N/A: docs-only (README + SPEC prose; PLAN.md status flip; PROGRESS.md cycle entry; per-skill `references/contract.md` regeneration via deterministic script; test validator wording update with no new pytest cases). Validators all green: `python3 scripts/validate_spec.py` returns `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` returns `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` returns `All contract files are current (12 checked)`. `python3 -m pytest -q` returns `433 passed in 0.39s` (unchanged total from the post-Cycle-187 baseline; 4 Copilot-install-guidance test cases continue to pass against the new validator). Em-dash audit clean: `grep -nP "—|–"` against README.md and SPEC.md returns only pre-existing template hits in SPEC sections 4, 14, 15, and 23 (all unmodified by this cycle).
**Next**: Task 1b (structured runtime metadata): update `.codex-plugin/plugin.json` `lifecycleHooks.status`, `unsupportedEvents`, `limitations` prose, and `codex.limitations` array to reflect current Codex hook state; update `agents/openai.yaml` and `skills/profilera/agents/openai.yaml` `support.lifecycle_hooks` fields. Task 1b depends on Task 1a (this cycle) and unblocks Task 3 (Codex `apply_patch` hook wiring). Task 2 (dead Copilot hook fix + frontmatter audit + README install-path swap) and Task 4 (`.agents/plugins/marketplace.json` + agent stubs + `setup_codex.py --enable-agents`) are unblocked in parallel by this cycle.
**Context**: intent (close stale-claim cleanup so every README and SPEC capability claim about Codex and Copilot is verifiable against current upstream evidence; align test fixtures that were enforcing stale state) · constraints (one conventional commit `docs(runtime): ...`; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with `N/A: docs-only` allowlist tag plus four validator outputs; SPEC Section 23 prose conventions with no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences; no new pytest cases; no version bump; do NOT modify CHANGELOG.md or TODO.md per task scope) · unknowns (none — capabilities research provided ground truth and the test failure surfaced predictably) · scope (`README.md` runtime support table + lifecycle hooks table + Copilot install section + Codex install section, `SPEC.md` Section 7 prose + per-runtime mechanism table, `tests/test_runtime_adapters.py` validator and 3 dependent test cases, 12 `skills/*/references/contract.md` regenerated, `.agentera/PLAN.md` Task 1a status flip, `.agentera/PROGRESS.md` this cycle entry).

■ ## Cycle 187 · 2026-04-27 09:00 · chore(release): renumber and consolidate three local pre-push releases into a single 1.20.0

**Phase**: release
**What**: Move 1 of the user-directed Option B parity-completion path. Consolidated three pre-push minor bumps (1.20.0 cross-runtime portability, 1.21.0 setup helpers, 1.22.0 live-host verification) into one coherent 1.20.0 release on this local branch. Renumbered version_files from 1.22.0 to 1.20.0 across `plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js` AGENTERA_VERSION, `registry.json` 12 entries, `.claude-plugin/marketplace.json` 12 versions (profilera entry preserved without a `version` key per long-standing convention), and all 12 `skills/*/.claude-plugin/plugin.json` files. Promoted CHANGELOG: replaced the three separate `## [1.22.0] · 2026-04-26`, `## [1.21.0] · 2026-04-26`, and `## [1.20.0] · 2026-04-26` blocks with a single `## [1.20.0] · 2026-04-27` block whose Added section carries every artifact the three releases shipped (AGENTERA_HOME contract, OpenCode bootstrap, setup_codex.py, setup_copilot.py, smoke_setup_helpers.py, smoke_live_hosts.py, README install + verify-by-hand subsections, README Scripts row enumeration, spec validator lint rule), Changed section carries the cross-skill compaction-form refactor, the SPEC 7-23 to 8-24 renumber, the Codex requiredCapabilities reconciliation, the lifecycle adapter back-compat, the AGENTERA_VERSION drift-test refactor, and the DOCS.md Index expansion, and Note section preserves the unrelated claude-code/conversation_turn duplicate-source_id callout. `## [Unreleased]` left empty at the top so the upcoming Move 2 parity-completion plan has a clean accumulation surface. PROGRESS.md cycles 175-186 stay as-is because they accurately record when the underlying work happened. PLAN.md (Live-Host Verification, all 6 tasks complete) is unchanged here; archival happens with the next plan bootstrap.
**Commit**: 12e434a chore(release): renumber three local pre-push releases into one 1.20.0
**Inspiration**: User feedback that the original 1.20.0 release was supposed to be a coherent "agentera works as intended on all four runtimes" release, plus parallel research that proved both Codex and Copilot capabilities had been documented based on stale assumptions. Nothing has been pushed (`git status: 55 ahead of origin/main, no v1.20/v1.21/v1.22 tags exist`), so renumbering is fully reversible. The consolidation pattern follows the same shape as Cycle 179 / 167 / 124 / 116 / 150 release-cycle bumps but in reverse: instead of promoting one Unreleased block to a new release, it merges three already-promoted blocks back into one.
**Discovered**: Codex `multi_agent` and `codex_hooks` are stable and default-on per local `codex features list` output (was documented as "experimental disabled" in the README). Codex `apply_patch` matchers for Write/Edit hooks shipped 2026-04-22 in PR `openai/codex#18391` (three days before this branch's 1.20.0 plan started, so the original "no real-time Write/Edit interception" claim was already obsolete at plan time). Copilot's marketplace install path is verified working (the user's `~/.copilot/config.json` shows agentera installed via both granular `<skill>@agentera` and umbrella `copilot plugin install jgabor/agentera` paths, contradicting the README's "no canonical Agentera marketplace source is currently verified" claim). Two viable third-party Codex marketplace aggregators exist: `hashgraph-online/awesome-codex-plugins` (mirrored installable marketplace) and `RoggeOhta/awesome-codex-cli` (curated). agentera's `.github/hooks/stop.json` declares `name: stop` but `stop` is not a valid Copilot hook event; the closest valid event is `sessionEnd`, so this hook has been silently dead under Copilot. These deltas are out of scope for Move 1; Move 2 (Cross-Runtime Parity Completion plan) addresses them.
**Verified**: N/A: chore-build-config (release renumber + CHANGELOG consolidation; no functional code changes). `grep -rn '"version": "1.22.0"' plugin.json .github/plugin/plugin.json .codex-plugin/plugin.json registry.json .claude-plugin/marketplace.json skills/*/.claude-plugin/plugin.json` returns 0 matches. `grep -c '"version": "1.20.0"\|AGENTERA_VERSION = "1.20.0"'` across the same set plus `.opencode/plugins/agentera.js` returns 40 occurrences (1 each in three standalone `plugin.json` files, 12 in `registry.json`, 12 in `.claude-plugin/marketplace.json` preserving profilera key-less, 1 in each of 12 `skills/*/.claude-plugin/plugin.json`, 1 in `agentera.js`). marketplace.json profilera entry confirmed without a `version` key. Validators all green: `python3 scripts/validate_spec.py` returns `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` returns `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` returns `All contract files are current (12 checked)`. `python3 -m pytest -q` returns `433 passed in 0.41s` (unchanged from the post-Cycle-186 baseline because Move 1 changes no test surface).
**Next**: Move 2 starts a new "Cross-Runtime Parity Completion" plan that closes the actual gaps the capabilities research surfaced. Tasks are roughly: (1) stale-claim cleanup across README and SPEC for both Codex and Copilot; (2) fix the dead `stop.json` Copilot hook; (3) port `validate_artifact.py` to a Codex `apply_patch` hook so real-time artifact validation works on Codex; (4) add `.agents/plugins/marketplace.json` for Codex marketplace consumption; (5) wire orkestrera runtime-aware dispatch with Codex `[agents.*]` config and a documented Copilot gap; (6) audit all 12 SKILL.md frontmatter against Copilot bug `github/copilot-cli#951`; (7) extend `smoke_live_hosts.py` to verify Codex hooks fire end-to-end; (8) submit aggregator PRs to `github/awesome-copilot` and `hashgraph-online/awesome-codex-plugins` as a follow-up. Move 3 (push and tag 1.20.0) follows after Move 2 lands and inspektera audits clean.
**Context**: intent (collapse three local pre-push minor bumps into one coherent 1.20.0 release that matches the user's original cross-runtime expectation, preserve every substantive artifact the three releases shipped, date the consolidated release for today since the consolidation lands today and the original three same-day bumps no longer fit the release model) · constraints (no push; no tag; one conventional commit; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with `N/A: chore-build-config` allowlist tag plus validator outputs and grep counts as supporting evidence; preserve `.claude-plugin/marketplace.json` profilera entry without a `version` key; do NOT alter the DOCS.md `version_files` list; do NOT edit PROGRESS.md cycles 175-186 since they accurately record when work happened; do NOT touch PLAN.md or HEALTH.md since archival and audit are owned by Move 2 / Move 3) · unknowns (whether to date the consolidated release for today (consolidation date) or for 2026-04-26 (when most underlying work landed): chose today because the release decision lands now and the cycle-level entries preserve when the underlying work happened) · scope (`plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js`, `registry.json`, `.claude-plugin/marketplace.json`, all 12 `skills/*/.claude-plugin/plugin.json`, `CHANGELOG.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 186 · 2026-04-26 21:00 · chore(release): bump suite to 1.22.0

**Phase**: release
**What**: Plan-level freshness checkpoint for the Live-Host Verification plan plus the suite version bump from 1.21.0 to 1.22.0 per DOCS.md `feat = minor` policy because the plan delivered `scripts/smoke_live_hosts.py` (Tasks 2-4) as a new repo-level utility. The plan closed the live-host AGENTERA_HOME-inheritance and SKILL.md compaction-execution verification gap that Audit 15 finding 2 had narrowed (conf 50): T1 audited profilera Codex collection end-to-end and reconciled `.codex-plugin/plugin.json` `requiredCapabilities[codex_session_corpus].status` from `degraded` to `ok` (252 history_prompt + 1 project_config_signal records confirmed in corpus); T2 shipped the harness scaffold with default-mode profilera audit + delegated `smoke_setup_helpers.py` plus `--live`-mode probe, cost gate, consent prompt, and SnapshotRegistry framework with orphan-snapshot auto-recovery; T3 wired the live `codex exec` invocation (one combined prompt exercising AGENTERA_HOME echo via marker brackets AND `compact_artifact.py` against a 14-cycle PROGRESS fixture, with byte-identity SHA256 round-trip on the real `~/.codex/config.toml`); T4 wired the symmetric `bash -c 'export AGENTERA_HOME=...; copilot -p ... --allow-all-tools'` invocation with byte-identity round-trip on all four shell rc files (`~/.bashrc`, `~/.bash_profile`, `~/.profile`, `~/.zshrc`); T5 documented the manual verification protocol in README "Verify ... by hand" subsections under both Codex and Copilot setup blocks plus the new harness row in DOCS.md Index and README Scripts. T6 (this cycle) bumped 19 version surfaces (Copilot root `plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js` AGENTERA_VERSION, `registry.json` 12 entries, `.claude-plugin/marketplace.json` 12 versions preserving the profilera entry without a `version` key per the long-standing convention, all 12 `skills/*/.claude-plugin/plugin.json`), promoted CHANGELOG `## [Unreleased]` to `## [1.22.0] · 2026-04-26` with Added (smoke harness, README "Verify by hand" subsections, README Scripts row) and Changed (`requiredCapabilities[codex_session_corpus].status` reconciliation, lifecycle validator back-compat) entries, fixed the Cycle 183 duplicate `**Commit**` line as plan-level hygiene (per realisera template, exactly ONE `**Commit**` per cycle), filed `[live-host-smoke-mjs-doc-row]` as a Normal TODO follow-up for the missing `smoke_opencode_bootstrap.mjs` Index row that T5 flagged but did not fix, and flipped PLAN.md Task 6 status □ → ■.
**Commit**: 503d22a chore(release): bump suite to 1.22.0
**Commits**: This plan produced 10 commits across 6 cycles: Cycle 181 T1 audit `3b9af17` (chore(audit): profilera Codex collection verification and metadata reconciliation) + SHA-pin `a7fd89a`; Cycle 182 T2 scaffold `513356e` (feat(smoke): add scripts/smoke_live_hosts.py scaffold for codex+copilot verification) + SHA-pin `c6de24d`; Cycle 183 T3 codex live `90f820f` (feat(smoke): wire codex exec AGENTERA_HOME + compaction live verification) + SHA-pin `f45fb69`; Cycle 184 T4 copilot live `32ae296` (feat(smoke): wire copilot -p AGENTERA_HOME + compaction live verification) + SHA-pin `541baa6`; Cycle 185 T5 docs `2c02499` (docs(verify): document manual AGENTERA_HOME verification and surface smoke_live_hosts.py) + SHA-pin `5b7742c`; Cycle 186 T6 release this commit + follow-up SHA-pin.
**Inspiration**: Cycle 179 (commit `cc1eacc`) plan-level freshness checkpoint pattern for Suite Usage Analytics: same shape — promote `## [Unreleased]` to a dated release block with Added/Changed/Fixed entries summarizing each task's user-visible impact (one short line per task, not commit messages verbatim), bump every file in DOCS.md `version_files` in one atomic commit, fix any plan-level cleanup deferred from earlier cycles (Cycle 183's duplicate `**Commit**` line) in the same commit because chore-build-config scope absorbs hygiene cleanly, and add a single plan-summary cycle entry whose **What** field summarizes the plan at the plan level (not per-task restatement) plus a **Commits** field listing every commit the plan produced. The decision to keep `## [Unreleased]` as a fresh empty block at the top after promotion follows the same Cycle 179 / Cycle 173 / Cycle 167 release-cycle convention so the next plan has a clean accumulation surface.
**Discovered**: Resolves HEALTH.md Audit 15 finding 2 (live-host AGENTERA_HOME inheritance still untested, conf 50): Tasks 3-4 verified end-to-end against real `codex exec` and `bash -c '...copilot -p...'` invocations on this host with byte-identity proof on user config files; Task 5 documented the manual verification protocol for users without `--live` access. Next inspektera audit can mark the finding resolved with reference to commits 90f820f, 32ae296, and 2c02499. Independently observed: Cycle 183 had a duplicate `**Commit**` line at the bottom of the entry — both lines named `90f820f`, but exactly one `**Commit**` per cycle is the realisera template invariant. Removed the second occurrence as plan-level hygiene in this same commit (chore(release) scope absorbs the cleanup cleanly, no separate commit needed). The `[live-host-smoke-mjs-doc-row]` TODO entry tracks the unrelated DOCS Index gap that T5 flagged in its Discovered field but explicitly deferred per Task 5 scope.
**Verified**: N/A: chore-build-config (release version bump + CHANGELOG promotion + plan-level freshness checkpoint + duplicate-Commit-line cleanup; no functional code changes). Bump landing evidence — `grep -c '"version": "1.21.0"'` returns 0 across all version_files (`plugin.json` 0, `.github/plugin/plugin.json` 0, `.codex-plugin/plugin.json` 0, `.opencode/plugins/agentera.js` 0 for both `"version":` and `AGENTERA_VERSION =` patterns, `registry.json` 0, `.claude-plugin/marketplace.json` 0, all 12 `skills/*/.claude-plugin/plugin.json` 0). `grep -c '"version": "1.22.0"'` returns expected counts (`plugin.json` 1, `.github/plugin/plugin.json` 1, `.codex-plugin/plugin.json` 1, `.opencode/plugins/agentera.js` 0 for `"version":` and 1 for `AGENTERA_VERSION = "1.22.0"`, `registry.json` 12, `.claude-plugin/marketplace.json` 12 — 1 top-level + 11 inner with profilera deliberately preserved without a `version` key per long-standing convention, each `skills/*/.claude-plugin/plugin.json` 1 across all 12 skills). Validators all green at the post-T5 baseline: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.41s` (unchanged from post-T5 baseline; T6 adds zero pytest cases per the chore-build-config scope).
**Next**: Live-Host Verification plan is complete; PLAN.md will be archived or replaced by the next active plan when one is bootstrapped. The next inspektera audit should mark Audit 15 finding 2 resolved with reference to commits 90f820f, 32ae296, and 2c02499 plus the manual verification protocol now in README. Two open follow-ups remain in TODO Normal: `[claude-code-extract-duplicate-source-ids]` (filed Cycle 181, blocks `~/.local/share/agentera/intermediate/corpus.json` writes on hosts with substantial Claude Code conversation history; fix path documented inline in the entry) and `[live-host-smoke-mjs-doc-row]` (filed this cycle, low-impact docs gap for `smoke_opencode_bootstrap.mjs` row in DOCS Index and README Scripts). The `[copilot-marketplace]` and `[opencode-session-events]` follow-ups carried from prior plans remain Normal-tier and unchanged.
**Context**: intent (close Live-Host Verification plan with the `feat = minor` release bump per DOCS.md policy, promote CHANGELOG with one short Added/Changed entry per user-visible impact, fix the Cycle 183 duplicate `**Commit**` line as plan-level hygiene in the same commit, file the missing `smoke_opencode_bootstrap.mjs` row as a Normal TODO follow-up since T5 explicitly deferred it, and resolve HEALTH.md Audit 15 finding 2 in this entry's Discovered field per AC6 because no new audit has run since the plan started) · constraints (one conventional commit `chore(release): bump suite to 1.22.0` plus a SHA-pin follow-up commit; no push; bump every file in DOCS.md `version_files` and add no new entries; preserve the marketplace.json profilera entry without a `version` key per the long-standing convention; mandatory `**Verified**` field per SPEC Section 19 with `N/A: chore-build-config` allowlist tag plus validator outputs and grep-bump-landing evidence; EXACTLY ONE `**Commit**` line in this entry to avoid the Cycle 183 defect; PLAN.md Task 6 status flipped □ → ■ in the same commit; SPEC Section 23 prose conventions: no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences) · unknowns (none — task acceptance criteria were fully concrete and the existing Cycle 179 / Cycle 173 release-cycle pattern provided the template) · scope (`plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js`, `registry.json`, `.claude-plugin/marketplace.json`, all 12 `skills/*/.claude-plugin/plugin.json`, `CHANGELOG.md`, `TODO.md`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 185 · 2026-04-26 20:30 · docs(verify): document manual AGENTERA_HOME verification and surface smoke_live_hosts.py

**Phase**: docs
**What**: Task 5 of the Live-Host Verification plan. Three coupled doc surfaces landed atomically. README Copilot setup section gained a "Verify Copilot AGENTERA_HOME by hand" subsection placed after the manual `echo 'export AGENTERA_HOME=...' >> ~/.bashrc` block; it carries one bash one-liner the user pastes into a real `copilot` session — `bash -c 'echo "AGENTERA_HOME=$AGENTERA_HOME"; python3 ${AGENTERA_HOME:-$CLAUDE_PLUGIN_ROOT}/scripts/compact_artifact.py'` — that exercises both the AGENTERA_HOME echo (read-side inheritance per SPEC.md Section 7) AND the bash-fallback compaction-script resolution in one paste, with prose naming the two expected outcomes (echo prints the install root; `compact_artifact.py` prints its `usage:` line proving the bash-fallback resolved). README Codex setup section gained the symmetric "Verify Codex AGENTERA_HOME by hand" subsection placed after the manual TOML snippet, same one-liner shape, same two-outcomes rubric, with the `codex exec` non-interactive verification scope explicitly named so the user knows the protocol covers both interactive and non-interactive surfaces via `[shell_environment_policy].set` semantics. The chosen one-liner uses `compact_artifact.py` with no arguments (which exits 2 with a usage line) rather than `--help` (which the script also treats as an unknown spec name and exits 2 with the same usage line) so neither path mutates a real artifact. Both subsections explicitly cite when to use the manual protocol: no auth, no API budget, behind a firewall, or any host where invoking the model is undesirable. README Scripts section gained a `scripts/smoke_live_hosts.py` row alongside the existing `smoke_setup_helpers.py` row; the row enumerates default mode (no flags, runs profilera Codex audit + delegates to `smoke_setup_helpers.py`, no live CLI invoked, $0 cost), `--live` mode (prints `Estimated cost: $0.20-1.00 across two model calls` and a one-line consent prompt before any subprocess invocation, exactly one `codex exec` and one `bash -c '...copilot -p...'` invocation each carrying a combined prompt that exercises both AGENTERA_HOME echo AND `compact_artifact.py` execution, snapshot/restore + SHA256 round-trip on `~/.codex/config.toml` and shell rc files), and the gap it closes (Audit 15 finding 2: "Codex and Copilot live-host AGENTERA_HOME inheritance still untested"). DOCS.md Index gained a `Live-host smoke runner | scripts/smoke_live_hosts.py | 2026-04-26 | ■ current` row positioned alongside the existing `Setup helper smoke runner` row. DOCS.md Audit Log gained a `### 2026-04-26 (Live-Host Verification Task 5)` block listing four (fixed)-status findings: Index missing the `smoke_live_hosts.py`row, README Codex section lacking a manual verification protocol, README Copilot section lacking the same, and README Scripts section not enumerating the new harness with default-mode/`--live`-mode/Audit-15-finding-2 callouts. DOCS.md`Last audit:` comment bumped from `2026-04-26 (Codex+Copilot Setup Helpers Task 4)` to `2026-04-26 (Live-Host Verification Task 5)`. PLAN.md Task 5 status flipped □ → ■.
**Commit**: 2c02499 docs(verify): document manual AGENTERA_HOME verification and surface smoke_live_hosts.py
**Inspiration**: Cycle 178 (commit`8e57ef4`) docs-cycle pattern for surfacing new helpers in README Scripts plus DOCS.md Index plus DOCS.md Audit Log in a single coherent commit with`(fixed)` status entries grouped by `2026-04-26 (<plan name> <task>)` header. The "Verify ... by hand" subsection naming mirrors the existing `Verify local plugin metadata with: claude plugin validate .` framing already in the README's Alternative install methods block (action verb + scope + concrete command). The choice of `compact_artifact.py` with no arguments (exit 2 + usage line) rather than `--help` came from inspection of the script's manual argparse: both paths print the same usage line with the same exit code, so the no-argument form is the cleanest spec name to teach because a user can extend it to `... compact_artifact.py progress <path>`for a real compaction without re-reading docs. The marker-bracketed prompt design from Cycle 183 (`===AGENTERA_HOME_ECHO_BEGIN===`) is what the harness uses internally; the manual one-liner uses simple`echo "AGENTERA_HOME=$AGENTERA_HOME"` because the user is reading the output by eye, not parsing markers programmatically.
**Discovered**: `compact_artifact.py --help` exits 2 with `usage: compact_artifact.py <spec-name> <path>` because the script does manual arg parsing rather than argparse, so `--help` is treated as an unknown spec name. This is fine for the manual protocol — the user sees the usage line and knows the script resolved through the bash-fallback. The same exit code (2) and output happen for the no-argument invocation, so the docs use the simpler no-argument form. `smoke_opencode_bootstrap.mjs` is referenced in the Task 5 acceptance criterion ("alongside `smoke_setup_helpers.py` and `smoke_opencode_bootstrap.mjs`") but is not yet in DOCS.md Index nor README Scripts; per Task 5 scope (add the new harness row, not rectify pre-existing missing rows) the new row sits next to`smoke_setup_helpers.py` only. The mjs row is a separate gap that future docs work can address. Cycle 183's duplicate `**Commit**` line (PROGRESS.md lines 18 and 23 both `Commit: 90f820f`) remains unfixed per task constraint — Task 6 owns plan-level cleanup.
**Verified**: N/A: docs-only. Changes are README.md (two new "Verify ... by hand" subsections under Copilot and Codex setup blocks plus one new Scripts table row),`.agentera/DOCS.md` (one new Index row, one new Audit Log entry block, one Last-audit comment bump), `.agentera/PLAN.md` (Task 5 status flip), and this PROGRESS.md cycle entry. No scripts, hooks, SKILL.md files, version files, or CHANGELOG touched. Validators all green at the post-T4 baseline: `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills`.`python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok`.`python3 scripts/generate_contracts.py --check` -> `All contract files are current (12 checked)`.`python3 -m pytest -q` -> `433 passed in 0.42s` (unchanged from post-T4 Cycle 184 baseline; Task 5 is documentation only and adds zero pytest cases per AC5).
**Next**: Task 6 (plan-level freshness checkpoint plus 1.21.0 → 1.22.0 release bump per `feat = minor` policy because the smoke_live_hosts.py harness is a Tasks 2-4 feat) is now unblocked; T1, T2, T3, T4, and T5 are all complete. Task 6 will promote CHANGELOG `## [Unreleased]` to `## [1.22.0] · 2026-04-26` listing the live-host smoke harness as Added and any profilera metadata reconciliation under Changed, bump every file in DOCS.md `version_files`, fix the Cycle 183 duplicate`**Commit**` line as part of plan-level cleanup, and resolve HEALTH.md Audit 15 finding 2 in the next audit or in this plan's PROGRESS cycle entry's Discovered field.
**Context**: intent (give users without `--live` access — no auth, no API budget, behind firewall — a documented manual path to verify AGENTERA_HOME inheritance and SKILL.md compaction-script resolution under both `codex` and `copilot`; surface the new`scripts/smoke_live_hosts.py` harness in the README Scripts catalog and DOCS.md Index so it is discoverable from the same entry points as `validate_spec.py`,`eval_skills.py`,`usage_stats.py`, and the setup helpers; resolve the four (fixed)-status DOCS.md findings via one Audit Log block) · constraints (documentation only — NO scripts, NO hooks, NO SKILL.md, NO version files, NO CHANGELOG per Task 6 scope; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory`**Verified**` field with `N/A: docs-only` allowlist tag plus validator outputs as supporting evidence per SPEC Section 19; no new pytest cases per AC5; EXACTLY ONE `**Commit**`line in this entry to avoid the Cycle 183 duplicate-Commit-line defect; PLAN.md Task 5 status flipped □ → ■ in the same commit; SPEC Section 23 prose conventions: no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences) · unknowns (none — task acceptance criteria were fully concrete and the existing Cycle 178 pattern provided the template) · scope (`README.md`,`.agentera/DOCS.md`,`.agentera/PLAN.md`,`.agentera/PROGRESS.md`).

■ ## Cycle 184 · 2026-04-26 20:00 · feat(smoke): wire copilot -p AGENTERA_HOME + compaction live verification

**Phase**: build
**What**: Task 4 of the Live-Host Verification plan. Replaced the Task 2 stub `run_copilot_live_section` with the end-to-end live-host wiring: PATH probe (`shutil.which("copilot")`), `copilot --version` sanity probe (10s), AC5 auth probe via `bash -c 'copilot -p "reply with the literal text OK and nothing else" --allow-all-tools'` with a 30s timeout (timeout / non-zero / missing-OK all SKIP with guidance pointing the operator at GitHub Copilot CLI's interactive auth flow), snapshot every existing shell rc file in `(.bashrc, .bash_profile, .profile, .zshrc)` and SHA256-hash each BEFORE any mutation (defense-in-depth mirroring the Codex section's `~/.codex/config.toml` round-trip even though `bash -c 'export ...'` does not touch any rc file), build a tmp install root via `tempfile.TemporaryDirectory(prefix="agentera-smoke-copilot-home-")` (using a fresh tmp dir as the AGENTERA_HOME value cleanly proves the bash-exported value is what propagates to copilot — there is no way for the harness's parent process to have set it to this fresh tmp path), seed a tmp `PROGRESS.md` fixture with 14 mock cycles via the existing `_build_progress_fixture` helper (above the 10-keep threshold so compaction MUST move 4 entries into `## Archived Cycles`), compose the same marker-bracketed combined prompt shape as Task 3 (`===AGENTERA_HOME_ECHO_BEGIN===` / `===COMPACTION_OUTPUT_BEGIN===`) instructing the agent to run `echo "AGENTERA_HOME=$AGENTERA_HOME"` then `python3 <repo>/scripts/compact_artifact.py progress <fixture>`, escape the prompt for embedding inside a `bash -c '...'` outer single-quoted script (backslash-escape `\\`, `"`, `$`, and backtick so bash does not terminate the inner `copilot -p "..."` quoting early), and issue exactly ONE invocation of the AC1 shape: `subprocess.run(["bash", "-c", f"export AGENTERA_HOME={tmp_install_root}; copilot -p \"{escaped_prompt}\" --allow-all-tools"])` with a 300s timeout (mirroring the Codex section's wall-clock cap). Parser pulls the AGENTERA_HOME value via the existing `_extract_between` substring slicing helper and asserts it contains `AGENTERA_HOME=<tmp_install_root>`; fixture mtime + line-count delta + `## Archived Cycles` heading presence are checked against the pre-seed snapshot. Post-run SHA256 of every snapshotted rc file is compared against the pre-run hash via a new `_assert_rc_unchanged` helper that hard-fails on any mismatch and logs every after-hash for operator audit even on the success path. The auth-failure branch (non-zero exit with "auth"/"login" in stderr) routes through `_assert_rc_unchanged` before returning so the SKIP path still proves byte-identity. PLAN.md Task 4 status flipped □ → ■.
**Commit**: 32ae296 feat(smoke): wire copilot -p AGENTERA_HOME + compaction live verification
**Inspiration**: Task 3's marker-bracketed prompt design (`===AGENTERA_HOME_ECHO_BEGIN===` / `===COMPACTION_OUTPUT_BEGIN===`) plus the `_extract_between` helper from Cycle 183 (commit `90f820f`) is the direct twin: same prompt shape, same parser, same `_build_progress_fixture` 14-cycle seed, same compaction-effect assertions (mtime advanced + line-count reduced + archive heading present). The "use a tmp dir as the AGENTERA_HOME value rather than REPO_ROOT" choice borrows from the same isolation pattern Task 3 uses for `CODEX_HOME` (point env at a tmp surface so the test cannot accidentally rely on the parent process happening to have the right value baked in). The shell-quote-escape logic (backslash-escape `\\`, `"`, `$`, backtick to embed an arbitrary string inside `bash -c '...'`'s outer single quotes wrapping a `copilot -p "..."` inner double-quote) is borrowed from how `subprocess.list2cmdline` handles quoting on Windows — same problem (literal-pass an arbitrary string through one shell layer), different solution (we control the wrapping shape so we only need to escape the four metacharacters that matter inside double quotes). The `_assert_rc_unchanged` helper extracts the SHA256-round-trip pattern from Task 3's inline check so multiple rc files can be verified uniformly without duplicating the assert+log pair four times.
**Discovered**: `bash -c 'copilot -p "..." --allow-all-tools'` ran in 25.2s end-to-end on this host, well under the 300s ceiling. Compaction effect was unambiguous: the seeded 14-cycle fixture (lines=127) shrank to 98 lines with `## Archived Cycles` heading present and 4 cycles converted to one-line entries (compaction stdout: `compacted: <fixture> (14->10 full, 0->4 oneline, 0 dropped)` — identical to the Codex section's output, which proves both runtimes execute the same script through their respective shell-tool surfaces). AGENTERA_HOME echo came back exactly as the tmp install root value (`/tmp/agentera-smoke-copilot-home-08hqnv55`), proving copilot inherits the env var from the parent bash shell that exported it — the read side of the inheritance promise. All four snapshotted rc files (`.bashrc`, `.bash_profile`, `.profile`, `.zshrc`) had byte-identical SHA256 before and after (`6d7414d0...` for bashrc, `9c8e62d7...` for bash_profile, `e3b0c442...` for the empty profile/zshrc), confirming the bash-export form does not touch any rc file. Auth probe shape: `bash -c 'copilot -p "reply OK" --allow-all-tools'` returned 0 with output containing "OK" within ~5s on this authed host; on a fresh unauthed host the call would block on GitHub's device-flow login prompt and the 30s timeout would fire, producing the AC5 SKIP with the `copilot` interactive-auth guidance. Copilot's stdout is more verbose than codex's `--output-last-message` (it echoes tool-call shells like `● Echo AGENTERA_HOME env var (shell)` between the markers), but the marker brackets isolate the values so `_extract_between` returns them verbatim regardless. No surprises affecting plan scope. Note: chose a tmp install root (not REPO_ROOT) because AC1 says `<tmp install root>` literally — this also strengthens the proof, since a fresh tmp path could not be in the parent process's environment by accident.
**Verified**: Live-host smoke run on this host. `echo y | python3 scripts/smoke_live_hosts.py --live` produced (Copilot section excerpt verbatim): `--- copilot live section ---` / `probe: copilot resolved to /bin/copilot` / `probe: copilot GitHub Copilot CLI 1.0.36.` / `probe: copilot auth probe healthy (output contains 'OK')` / `snapshot: $HOME/.bashrc -> /tmp/agentera-smoke-live-.bashrc-20260426-171650-7fea445a47b0.bak` / `copilot-rc sha256 (before) $HOME/.bashrc: 6d7414d07b61aab0490bdbe8883fc44f303df5ea2e902dff03a6bc186cf96a49` / `snapshot: $HOME/.bash_profile -> /tmp/agentera-smoke-live-.bash_profile-...bak` / `copilot-rc sha256 (before) $HOME/.bash_profile: 9c8e62d73277fb2eda68762473a3dcf15e73bb2e224e6859a1a09cfb811410b6` / `snapshot: $HOME/.profile -> /tmp/agentera-smoke-live-.profile-...bak` / `copilot-rc sha256 (before) $HOME/.profile: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `snapshot: $HOME/.zshrc -> /tmp/agentera-smoke-live-.zshrc-...bak` / `copilot-rc sha256 (before) $HOME/.zshrc: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `copilot: tmp AGENTERA_HOME=/tmp/agentera-smoke-copilot-home-08hqnv55` / `copilot: seeded fixture .../PROGRESS.md (lines=127, mtime=1777216610)` / `copilot: invoking bash -c 'export AGENTERA_HOME=/tmp/agentera-smoke-copilot-home-08hqnv55; copilot -p "..." --allow-all-tools'` / `copilot: bash -c 'copilot -p ...' returned exit=0 in 25.2s` / agent output between markers: `===AGENTERA_HOME_ECHO_BEGIN=== AGENTERA_HOME=/tmp/agentera-smoke-copilot-home-08hqnv55 ===AGENTERA_HOME_ECHO_END===` and `===COMPACTION_OUTPUT_BEGIN=== compacted: /tmp/agentera-smoke-copilot-fixture-5ypp38pn/PROGRESS.md (14->10 full, 0->4 oneline, 0 dropped) ===COMPACTION_OUTPUT_END===` / `copilot: AGENTERA_HOME echo verified: AGENTERA_HOME=/tmp/agentera-smoke-copilot-home-08hqnv55` / `copilot: fixture compaction verified (lines 127 -> 98, archived heading: present)` / `copilot-rc sha256 (after) $HOME/.bashrc: 6d7414d07b61aab0490bdbe8883fc44f303df5ea2e902dff03a6bc186cf96a49` / `copilot-rc sha256 (after) $HOME/.bash_profile: 9c8e62d73277fb2eda68762473a3dcf15e73bb2e224e6859a1a09cfb811410b6` / `copilot-rc sha256 (after) $HOME/.profile: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `copilot-rc sha256 (after) $HOME/.zshrc: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `copilot: verified under bash -c 'export AGENTERA_HOME=…; copilot -p … --allow-all-tools'; user shell rc files byte-identical (SHA256 round-trip)` / `PASS: all smoke checks passed` (exit 0). AC1 satisfied (exactly ONE `bash -c 'export AGENTERA_HOME=<tmp>; copilot -p "..." --allow-all-tools'` invocation issued; subprocess.run shape is `["bash", "-c", "export AGENTERA_HOME=...; copilot -p \"...\" --allow-all-tools"]`; combined prompt drove both shell tool calls). AC2 satisfied (AGENTERA_HOME echo `/tmp/agentera-smoke-copilot-home-08hqnv55` matches the export verbatim; fixture mtime advanced 1777216610 → post-run, lines reduced 127 → 98, archive heading present). AC3 satisfied (consent prompt unchanged from Cycle 182: `Proceed with live CLI invocations (codex exec + copilot -p --allow-all-tools)? [y/N]:` — `--allow-all-tools` named explicitly). AC4 satisfied (all 4 rc files byte-identical via SHA256 round-trip: bashrc `6d7414d0...` before == after, bash_profile `9c8e62d7...` before == after, profile/zshrc `e3b0c442...` (empty file hash) before == after). AC5 satisfied (auth probe uses `bash -c 'copilot -p "reply with the literal text OK and nothing else" --allow-all-tools'` with `AUTH_PROBE_TIMEOUT_SECONDS=30`; timeout / non-zero / missing-OK all route through `skip()` with guidance pointing at GitHub Copilot CLI's interactive auth flow — verified by reading the three skip branches in `run_copilot_live_section` steps 2 and 3). Validators all green at post-T3 baseline: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.41s` (unchanged from post-T3 baseline; T4 adds zero pytest cases per T2 AC6). Pre-commit hooks ran clean.
**Next**: Task 5 (manual verification protocol in README + DOCS surface) is now unblocked since it depends on Tasks 1-4 and Task 4 was the last execution-side task. Task 6 (1.21.0 → 1.22.0 release bump + CHANGELOG) follows once Task 5 lands. The Copilot section's stdout is more verbose than Codex's (it echoes shell-tool wrappers like `● Echo AGENTERA_HOME env var (shell)`), but the marker-bracketed parsing isolates the values cleanly — Task 5 docs should mention the verbose Copilot output as expected behavior so a user reading the harness output knows what to look for. Cycle 183 has a duplicate `**Commit**` line (lines 7 and 12 both `Commit: 90f820f`); Task 6 owns plan-level cleanup, so flagged here for attention without fixing in this cycle.
**Context**: intent (close the live-host AGENTERA_HOME-inheritance + compaction-execution verification gap from Audit 15 finding 2 against the real `copilot` CLI by issuing exactly one `bash -c 'export AGENTERA_HOME=<tmp>; copilot -p "..." --allow-all-tools'` invocation that exercises both the env-var propagation read side AND the compaction script the SKILL.md prose cites, with byte-identity proof on every shell rc file the harness touches and a skip-with-guidance path on auth failure) · constraints (stdlib only per CLAUDE.md scripts convention; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field per SPEC Section 19 with literal `bash -c 'copilot -p ...'` stdout capture proving AGENTERA_HOME echo + compaction effect AND SHA256 round-trip on all snapshotted rc files — no `N/A` allowlist tag because this is functional code; do NOT touch the Codex section per Task 3 scope, do NOT modify SnapshotRegistry/orphan-recovery framework per Task 3 scope (they work), do NOT modify README/DOCS per Task 5 scope, do NOT bump version per Task 6 scope, do NOT modify CHANGELOG per Task 6 scope; tests/ stays untouched per T2 AC6 — the harness IS the test surface; single `copilot -p` invocation per AC1 — do not retry on failure mid-prompt, let it fail visibly so the bug is observable) · unknowns (whether the bash-quote-escape logic would survive copilot's prompt-parsing — confirmed by 25s end-to-end run with both shell tool calls observed and both markers parsed verbatim; whether copilot's verbose stdout would defeat the marker-extraction — confirmed by `_extract_between` substring slicing returning the values cleanly even with shell-tool wrappers and prose between markers; whether using a tmp dir vs REPO_ROOT for the AGENTERA_HOME value would change the assertion shape — chose tmp per AC1's literal `<tmp install root>` text and confirmed the parser works against the tmp value verbatim) · scope (`scripts/smoke_live_hosts.py`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 183 · 2026-04-26 19:30 · feat(smoke): wire codex exec AGENTERA_HOME + compaction live verification

**Phase**: build
**What**: Task 3 of the Live-Host Verification plan. Replaced the Task 2 stub `run_codex_live_section` with the end-to-end live-host wiring: PATH probe (`shutil.which`), `codex --version` sanity probe, snapshot + SHA256 of the real `~/.codex/config.toml` BEFORE any tmp surface is created, build a tmp `CODEX_HOME` directory (`tempfile.TemporaryDirectory(prefix="agentera-smoke-codex-home-")`), copy the user's real `auth.json` into it so codex is authenticated against the tmp config, invoke `setup_codex.py --config-file <tmp>/config.toml --install-root <repo>` to write the AGENTERA_HOME entry under `[shell_environment_policy].set` in the tmp config (real config never touched), seed a tmp `PROGRESS.md` fixture with 14 mock cycles (above the 10-keep threshold so compaction MUST move 4 entries into `## Archived Cycles`), then issue exactly ONE `codex exec` invocation with `CODEX_HOME=<tmp>` exported via env, `--cd <tmp_workdir> --skip-git-repo-check --dangerously-bypass-approvals-and-sandbox --output-last-message <tmp>` (the user pre-authorized live spend so the bypass-approvals path is acceptable for one invocation), and a combined prompt instructing the agent to run two bash tool calls in sequence — `echo "AGENTERA_HOME=$AGENTERA_HOME"` and `python3 <repo>/scripts/compact_artifact.py progress <fixture>` — then print the captured outputs between distinct `===AGENTERA_HOME_ECHO_BEGIN===` / `===COMPACTION_OUTPUT_BEGIN===` markers. Parser pulls the AGENTERA_HOME value via `_extract_between` substring slicing (no regex) and asserts it contains `AGENTERA_HOME=<install_root>`; fixture mtime + line-count delta + `## Archived Cycles` heading presence are checked against the pre-seed snapshot. Post-run SHA256 of the real config is compared against the pre-run hash and a hard fail is raised on mismatch (AC4 defense-in-depth — even though the tmp `CODEX_HOME` redirect means the real config is never written, the SHA256 round-trip would catch a regression that accidentally targeted the real path). On success the harness prints `verified under codex exec; interactive mode inferred via shell_environment_policy semantics` per AC3. Added orphan-snapshot detection at startup (AC5): each `SnapshotRegistry.snapshot` write now drops a sidecar `<backup>.meta` file containing the original absolute path; `recover_orphan_snapshots()` runs once at the top of `main()` before any other section, scans `/tmp/agentera-smoke-live-*.bak`, restores each backup over the path the sidecar names, and unlinks both the backup and the sidecar so the next run sees a clean tmp surface. `restore_all` was extended to clean up the sidecar alongside its backup so a normal-exit run leaves no orphans for the next invocation. PLAN.md Task 3 status flipped □ → ■.
**Commit**: 90f820f feat(smoke): wire codex exec AGENTERA_HOME + compaction live verification
**Inspiration**: Task 2's `SnapshotRegistry` + cost-gate + probe scaffold from Cycle 182 (commit `513356e`) is the substrate this cycle plugs into; the section structure (probe → snapshot → mutate → assert → finally-restore) and the `PASS:` / `SKIP:` / `FAIL:` protocol are unchanged. The "build a tmp `CODEX_HOME` and copy `auth.json`" pattern mirrors the `setup_codex.py --config-file <PATH>` flag's purpose (cited verbatim in the task hint as "use this to point at a TMP path so the user's real `~/.codex/config.toml` is never touched") combined with codex's documented `CODEX_HOME` env var that overrides the config dir lookup. The marker-bracketed prompt design (`===AGENTERA_HOME_ECHO_BEGIN===` / `===COMPACTION_OUTPUT_BEGIN===`) is borrowed from how `scripts/eval_skills.py` parses agent output — distinct unique markers around the values let the harness extract them without depending on prose surrounding the values. The orphan-recovery sidecar pattern (write `<backup>.meta` next to `<backup>.bak` so a later process can find the original path) is structurally similar to how `git rebase --autosquash` uses `.bak` + a fixup script — pair a state file with a metadata file so recovery is deterministic.
**Discovered**: codex exec ran in 17.0s end-to-end, well under the 300s timeout cap chosen as a safe ceiling (the Bash tool's 10-minute hard cap is the real ceiling, and a hung run would orphan the snapshot pair which the next invocation auto-restores). Compaction effect was unambiguous: the seeded 14-cycle fixture (lines=127) shrank to 98 lines with `## Archived Cycles` heading present and 4 cycles converted to one-line entries (output: `compacted: <fixture> (14->10 full, 0->4 oneline, 0 dropped)`). AGENTERA_HOME echo came back exactly as `$HOME/git/agentera` — the value `setup_codex.py` wrote to the tmp config under `[shell_environment_policy].set.AGENTERA_HOME` was correctly propagated by codex into the bash tool's subprocess env, confirming the read side of the inheritance promise that Audit 15 finding 2 flagged as untested. Real `~/.codex/config.toml` SHA256 was `071e5bbf951a8ef1b7a2161e451db3e2ea93bdacd941b0e02e93375879f39486` BEFORE and AFTER the run — byte-identical, AC4 satisfied. Orphan-recovery validated end-to-end via a synthetic test: created a fake `/tmp/agentera-smoke-live-fakefile-19700101-000000-deadbeef.bak` + sidecar pointing at a tmp original, mutated the original, ran the harness, observed the orphan was detected (`orphan-snapshots: detected 1 tmp backup(s) from a prior run`), restored, and the original's SHA256 matched its pre-mutation value. No surprises affecting plan scope. Note: chose `--dangerously-bypass-approvals-and-sandbox` for the one codex exec call rather than `-s danger-full-access` alone because the latter still triggers approval prompts for shell tool invocations under codex 0.125.0; the bypass flag is the documented non-interactive shortcut and the user pre-authorized live spend.
**Verified**: Live-host smoke run on this host. `echo y | python3 scripts/smoke_live_hosts.py --live` produced (verbatim, with the codex exec stdout captured between markers): `--- codex live section ---` / `probe: codex resolved to /bin/codex` / `probe: codex codex-cli 0.125.0` / `snapshot: $HOME/.codex/config.toml -> /tmp/agentera-smoke-live-config.toml-20260426-165644-7f7b6d978f90.bak` / `codex-config sha256 (before): 071e5bbf951a8ef1b7a2161e451db3e2ea93bdacd941b0e02e93375879f39486` / `codex: tmp CODEX_HOME=/tmp/agentera-smoke-codex-home-1gp0yedb` / `codex: copied auth.json into tmp CODEX_HOME (authed)` / `codex: setup_codex.py wrote /tmp/.../config.toml: wrote ...config.toml: write fresh config with shell_environment_policy.set.AGENTERA_HOME = $HOME/git/agentera` / `codex: seeded fixture .../PROGRESS.md (lines=127, mtime=1777215405)` / `codex: invoking codex exec (CODEX_HOME=/tmp/agentera-smoke-codex-home-1gp0yedb)` / `codex: codex exec returned exit=0 in 17.0s` / agent output between markers: `===AGENTERA_HOME_ECHO_BEGIN=== AGENTERA_HOME=$HOME/git/agentera ===AGENTERA_HOME_ECHO_END===` and `===COMPACTION_OUTPUT_BEGIN=== compacted: .../PROGRESS.md (14->10 full, 0->4 oneline, 0 dropped) ===COMPACTION_OUTPUT_END===` / `codex: AGENTERA_HOME echo verified: AGENTERA_HOME=$HOME/git/agentera` / `codex: fixture compaction verified (lines 127 -> 98, archived heading: present)` / `codex-config sha256 (after): 071e5bbf951a8ef1b7a2161e451db3e2ea93bdacd941b0e02e93375879f39486` / `codex: verified under codex exec; interactive mode inferred via shell_environment_policy semantics` / `PASS: all smoke checks passed` (exit 0). AC1 satisfied (exactly one `codex exec` invocation; combined prompt drove both bash tool calls). AC2 satisfied (AGENTERA_HOME echo matches install root; fixture mtime advanced 1777215405 → post-run, lines reduced 127 → 98, archive heading present). AC3 satisfied (literal "verified under codex exec; interactive mode inferred via shell_environment_policy semantics" line printed). AC4 satisfied (real config SHA256 byte-identical: `071e5bbf...` before == after; independently confirmed via `sha256sum ~/.codex/config.toml`). AC5 satisfied via synthetic orphan injection: created `/tmp/agentera-smoke-live-fakefile-19700101-000000-deadbeef.bak` + `.meta` sidecar pointing at a separate tmp original, mutated the original, ran the harness, observed `orphan-snapshots: detected 1 tmp backup(s) from a prior run; attempting auto-restore` / `orphan-snapshots: restored /tmp/agentera-orphan-test-sr1I from /tmp/agentera-smoke-live-fakefile-19700101-000000-deadbeef.bak (crashed prior run)` and the restored file's SHA256 matched the pre-mutation hash exactly. Validators all green at post-T2 baseline: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.41s` (unchanged from post-T2 baseline; T3 adds zero pytest cases per T2 AC6 — the harness IS the test surface). Pre-commit hooks ran clean.
**Next**: Task 4 (Copilot live-host AGENTERA_HOME + compaction smoke via `bash -c 'export AGENTERA_HOME=...; copilot -p ... --allow-all-tools'`) is unblocked since it depends only on Task 2 and Task 3 left the harness skeleton intact for Task 4 to plug into via `run_copilot_live_section`. Task 5 (README + DOCS surface) and Task 6 (1.21.0 → 1.22.0 bump) follow once Task 4 lands. The orphan-snapshot recovery framework added here also benefits T4: any sidecar-tagged backup left by a crashed Copilot section will be auto-restored on the next invocation, regardless of which section produced it.
**Context**: intent (close the live-host AGENTERA_HOME-inheritance + compaction-execution verification gap from Audit 15 finding 2 against the real `codex` CLI by issuing exactly one `codex exec` invocation that exercises both the env-var propagation read side AND the compaction script the SKILL.md prose cites, with byte-identity proof on the real `~/.codex/config.toml` and orphan-snapshot auto-recovery so a crashed run cannot strand the user's config) · constraints (stdlib only per CLAUDE.md scripts convention; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field per SPEC Section 20 with literal codex exec output capture proving AGENTERA_HOME echo + compaction effect — no `N/A` allowlist tag because this is functional code; do NOT touch the Copilot section per Task 4 scope, do NOT modify README/DOCS per Task 5 scope, do NOT bump version per Task 6 scope; tests/ stays untouched per T2 AC6) · unknowns (whether codex exec under `CODEX_HOME=<tmp>` would still recognize the user's auth — addressed by copying real `auth.json` into the tmp dir before invocation; whether the bypass-approvals flag would land the bash tool calls without prompts — confirmed by 17s end-to-end run with both tool calls observed in output; whether the marker-extraction would survive prose surrounding the values — confirmed by `_extract_between` substring slicing returning the values verbatim) · scope (`scripts/smoke_live_hosts.py`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 182 · 2026-04-26 19:00 · feat(smoke): add scripts/smoke_live_hosts.py scaffold for codex+copilot verification

**Phase**: build
**What**: Task 2 of the Live-Host Verification plan. Shipped `scripts/smoke_live_hosts.py`, a stdlib-only harness that mirrors the Cycle 177 `smoke_setup_helpers.py` shape (sequential numbered sections, `PASS:` / `FAIL:` / `SKIP:` to stdout, top-level `finally` restore, fail-fast). Two modes. Default mode (no flags) runs the profilera Codex collection audit by importing `extract_all` from the profilera scripts directory and calling `build_corpus()` in process — this is the Task 1 path and avoids the unrelated claude-code duplicate-source_id failure that blocks the extractor's CLI from writing corpus.json. Records are grouped by their top-level `runtime` key (not `provenance.runtime`) and per-runtime counts plus `metadata.runtime_status` are reported on stdout. After the audit, the harness delegates to `scripts/smoke_setup_helpers.py` as a subprocess so the existing 11-case offline gate continues to run; the delegated stdout is indented two spaces under this harness's output for visual grouping. `--live` mode prints `Estimated cost: $0.20-1.00 across two model calls (one per runtime; --live mode only)` and a one-line consent prompt naming `codex exec + copilot -p --allow-all-tools` so the operator knows what permission they grant; `y` or `yes` (case-insensitive) proceeds, anything else aborts. Each per-runtime probe distinguishes "not on PATH" (`shutil.which` returns `None` → no subprocess invoked) from "binary present but auth probe timed out" (timeout caught at 10s for `--version` or 30s for the auth probe → recorded as `not-authed` skip with the timeout reason in the message). Both skip types accumulate into a `skips` list and emit a one-line summary at the end before the trailing `PASS:` so a host with no live CLIs sees a clean exit-0 with the skipped sections enumerated. The `SnapshotRegistry` class registers `(original_path, tmp_backup_path)` pairs BEFORE any mutation: existing files are copied to `/tmp/agentera-smoke-live-<basename>-<timestamp>-<id>.bak` and the tmp path is logged; absent files record an absence marker so the restore path can unlink any file created during the run. `restore_all()` runs in the top-level `finally` so it executes on success, hard fail, and crash. The Task 2 cut wires the snapshot framework but registers snapshots only after a successful probe; Tasks 3 and 4 plug in the actual `codex exec` AGENTERA_HOME + `compact_artifact.py` invocation and the `bash -c 'export AGENTERA_HOME=...; copilot -p ...'` invocation under this guard. PLAN.md Task 2 status flipped □ → ■.
**Commit**: 513356e feat(smoke): add scripts/smoke_live_hosts.py scaffold for codex+copilot verification
**Inspiration**: `scripts/smoke_setup_helpers.py` (Cycle 177, commit 1a558a7) is the direct shape twin — same `PASS:` / `FAIL:` to stdout, same try/finally cleanup, same fail-fast contract, same single-file standalone runner. The probe-then-snapshot ordering (snapshot only after the runtime is confirmed healthy) borrows from `setup_codex.py` / `setup_copilot.py` (Cycles 175 / 176): both write only after the install root is verified against canonical entries, so the same "validate first, mutate second" discipline carries through. The `<absent>` snapshot semantics (record absence at snapshot time, unlink any file created during the run) borrows from the `tempfile.NamedTemporaryFile(delete=False)` + manual cleanup pattern that `smoke_setup_helpers.py` uses for its per-case tmp HOME dirs. The cost-gate-then-consent UX is unprecedented in this repo (no prior live-CLI-gated harness existed); the one-line consent shape was chosen so a piped/non-interactive run (CI) sees `EOFError` on `input()` and treats it as a decline, preventing accidental billing.
**Discovered**: Initial cut grouped records by `provenance.runtime` (the `runtime_status` shape uses runtime as a top-level metadata key), but `extract_all.py` records carry `runtime` as a top-level record key (lines 802, 824, 855, 878, 901, 920, 1164, 1183, 1210, 1237, 1256, 1552, 1615, 1641, 1671); first run reported `<unknown>=2151` instead of the per-runtime breakdown. Fixed by reading `record.get("runtime", "<unknown>")` directly. Counts now match the Cycle 181 audit exactly: `claude-code=1897, codex-cli=253, copilot-cli=1`, `253 codex-cli record(s)` carried to corpus, `21 validation error(s) from build_corpus(); 0 mention codex` — the unrelated `[claude-code-extract-duplicate-source-ids]` failure is reported at info level and does NOT block the harness per the AC1 directive. AC3 distinct-skip-message verified by monkeypatching `shutil.which` to return `None` for both binaries (got `SKIP: codex (not on PATH)` / `SKIP: copilot (not on PATH)`) and separately by monkeypatching `subprocess.run` to raise `TimeoutExpired` (got `SKIP: codex (binary present but`codex --version`timed out)`); both paths return `not-on-path` / `not-authed` cleanly without invoking the auth probe. Snapshot/restore framework verified end-to-end against a `tempfile.NamedTemporaryFile` (mutate → restore left the file byte-identical) and against an absent path (create during run → restore removed it). Both `codex` and `copilot` ARE on this host's PATH (`/bin/codex`, `/bin/copilot`), so the AC5 "no live CLI available" path could not be exercised end-to-end from real probes without invoking the model — the simulated `shutil.which → None` path covers the AC5 contract via the same code path. No surprises affecting plan scope.
**Verified**: Default mode (no flags): `python3 scripts/smoke_live_hosts.py` → `--- profilera Codex collection audit ---` / `audit: total records=2151 across runtimes=claude-code=1897, codex-cli=253, copilot-cli=1` / `audit: codex-cli available; corpus carries 253 codex-cli record(s)` / `audit: 21 validation error(s) from build_corpus(); 0 mention codex` / `--- setup helpers smoke (delegated) ---` / `PASS: all smoke checks passed` / `PASS: all smoke checks passed`, exit 0 (AC1 satisfied). `--live` mode with declined consent: `echo n | python3 scripts/smoke_live_hosts.py --live` runs the offline sections then prints `--- live mode: cost gate ---` / `Estimated cost: $0.20-1.00 across two model calls (one per runtime; --live mode only)` / `Proceed with live CLI invocations (codex exec + copilot -p --allow-all-tools)? [y/N]:` / `aborted: consent declined; no live CLI invoked`, exit 1 with no subprocess invocation against `codex` or `copilot` (AC2 satisfied; cost line and one-line consent prompt printed BEFORE any CLI subprocess). AC3 verified via in-process simulation: `shutil.which → None` produces `SKIP: codex (not on PATH)` and `SKIP: copilot (not on PATH)` and returns `not-on-path` from `probe_runtime` without subprocess invocation; `subprocess.run` raising `TimeoutExpired` produces `SKIP: codex (binary present but`codex --version`timed out)` and returns `not-authed` — distinct messages for distinct failure modes. AC4 verified: `SnapshotRegistry.snapshot()` of an existing tmpfile produced `/tmp/agentera-smoke-live-tmpq1a1t8wu.toml-20260426-161520-7f01fdb94820.bak`, `restore_all()` after mutation restored byte-identical original content; `snapshot()` of an absent path recorded the absence and `restore_all()` after creation removed the file. AC5 path verified via the same simulation as AC3: with both `which` calls returning `None`, the per-runtime sections add to `skips` and never invoke a subprocess; the main loop's `if skips:` branch prints the summary and returns 0 — the harness exits cleanly with both sections enumerated. AC6 satisfied: `tests/` directory unchanged, no new pytest cases for the harness body. Validators all green at post-T1 baseline: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.40s` (unchanged; T2 adds zero pytest cases per AC6 — the harness IS the test surface). Pre-commit hooks ran clean: contracts (0.03s), docs (0.11s), spec-lint (0.09s).
**Next**: Task 3 (Codex live `codex exec` AGENTERA_HOME + `compact_artifact.py` combined-prompt exercise) and Task 4 (Copilot `bash -c 'export AGENTERA_HOME=...; copilot -p ... --allow-all-tools'` exercise) are unblocked since both depend only on Task 2. Both will plug into the snapshot guard wired here: Task 3 snapshots `~/.codex/config.toml` BEFORE invoking `setup_codex.py --config-file` against a tmp install root, runs the `codex exec` invocation, parses the AGENTERA_HOME echo and the fixture compaction outcome, then the top-level `finally` restores the original config from the tmp backup. Task 4 mirrors the same shape against `~/.bashrc` (or whichever rc file the harness's shell-detection logic picks). Task 5 (README + DOCS surface) and Task 6 (1.21.0 → 1.22.0 bump) follow once Tasks 3-4 land. The `[claude-code-extract-duplicate-source-ids]` TODO from Cycle 181 remains independent and the harness's `0 mention codex` line proves the failure is not a Codex regression.
**Context**: intent (build the live-host harness scaffold so Tasks 3-4 can plug in their CLI invocations without re-shaping the framework, while exercising the offline gates — Codex collection audit + delegated setup helpers smoke — under a single operator-facing entrypoint) · constraints (stdlib only per CLAUDE.md scripts convention; no live CLI invocations in this task — Task 2 must wire the probe/cost-gate/snapshot UX without spending model budget; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field per SPEC Section 20 with end-to-end harness output across both default and `--live`-with-declined-consent modes plus simulated AC3/AC4/AC5 paths and validator outputs as supporting evidence — no N/A allowlist tag because the harness is functional code; do NOT introduce any actual `codex exec` or `copilot -p` invocations per Task 3/4 scope, do NOT touch README/DOCS per Task 5 scope, do NOT bump version per Task 6 scope; tests/ stays untouched per AC6 — the harness IS the test surface) · unknowns (whether the in-process `build_corpus()` call would surface a different runtime-key shape than `extract_all.py`'s extractors expect — ruled out by reading the runtime-key sites in extract_all.py and matching the top-level `record.runtime` field directly; whether a piped non-interactive `--live` invocation would hang on `input()` — ruled out by catching `EOFError` and treating it as a decline, which lets CI dry-probe the cost gate without a TTY) · scope (`scripts/smoke_live_hosts.py`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

## Archived Cycles

- Cycle 181 (2026-04-26): chore(audit): profilera Codex collection verification and metadata reconciliation
- Cycle 180 (2026-04-26): chore(plan): freshness checkpoint for Codex+Copilot Completion
- Cycle 179 (2026-04-26): chore(release): bump suite to 1.21.0
- Cycle 178 (2026-04-26): docs(install): surface setup helpers in README and refresh DOCS.md Index
- Cycle 177 (2026-04-26): test(smoke): add scripts/smoke_setup_helpers.py for codex and copilot helpers
- Cycle 176 (2026-04-26): feat(setup): add scripts/setup_copilot.py for AGENTERA_HOME shell-rc injection
- Cycle 175 (2026-04-26): feat(setup): add scripts/setup_codex.py for AGENTERA_HOME injection
- Cycle 174 (2026-04-26): chore(plan): freshness checkpoint for Cross-Runtime Portability
- Cycle 173 (2026-04-26): chore(release): bump suite to 1.20.0
- Cycle 172 (2026-04-26): refactor(skills): adopt AGENTERA_HOME bash fallback and shift Section refs after SPEC renumber
- Cycle 171 (2026-04-26): docs(install): codex and copilot AGENTERA_HOME setup steps
- Cycle 170 (2026-04-26): feat(opencode): bootstrap at init and inject AGENTERA_HOME via shell.env
- Cycle 169 (2026-04-26): feat(spec): standardize AGENTERA_HOME contract for cross-runtime helper paths
- Cycle 168 (2026-04-26): chore(plan): freshness checkpoint for Suite Usage Analytics
- Cycle 167 (2026-04-26): chore(release): bump suite to 1.19.0
- Cycle 166 (2026-04-26): docs(usage): document scripts/usage_stats.py across README, DOCS.md, AGENTS.md
- Cycle 165 (2026-04-26): feat(usage): emit USAGE.md, stdout summary, and JSON
- Cycle 164 (2026-04-26): feat(usage): classify trigger phrasing and scope by project
- Cycle 163 (2026-04-26): feat(usage): detect skill invocations and pair with exit signals
- Cycle 162 (2026-04-26): feat(validator): accept arbitrary SKILL.md paths for third-party authoring
- Cycle 161 (2026-04-25): chore(plan): checkpoint copilot marketplace freshness
- Cycle 160 (2026-04-25): docs(release): apply copilot release convention
- Cycle 159 (2026-04-25): docs(copilot): update user guidance
- Cycle 158 (2026-04-25): chore(runtime): verify copilot host state
- Cycle 157 (2026-04-25): test(install): guard copilot marketplace claims
- Cycle 156 (2026-04-25): docs(install): clarify copilot marketplace placeholder
- Cycle 155 (2026-04-25): docs(plan): record copilot marketplace evidence
- Cycle 154 (2026-04-25): docs(copilot): prefer marketplace plugin installs
- Cycle 153 (2026-04-25): fix(copilot): load skills from checkout plugin root
- Cycle 152 (2026-04-25): chore(runtime): smoke live Copilot and Codex hosts
- Cycle 151 (2026-04-25): chore(plan): checkpoint Audit 11 freshness
- Cycle 150 (2026-04-25): chore(release): bump suite to 1.18.1
- Cycle 149 (2026-04-25): test(profilera): deepen corpus validation fixtures
- Cycle 148 (2026-04-25): refactor(profilera): localize corpus runtime orchestration
- Cycle 147 (2026-04-25): fix(adapters): repair OpenCode path and hook drift
- Cycle 146 (2026-04-25): fix(adapters): catch Codex invocation hint drift
- Cycle 145 (2026-04-25): fix(adapters): tighten runtime metadata drift guards
- Cycle 144 (2026-04-25): fix(profilera): redact Copilot config secrets
- Cycle 143 (2026-04-25): fix(profilera): align Section 21 corpus record envelope
- Cycle 142 (2026-04-24): chore(plan): checkpoint runtime portability freshness
