# Progress

■ ## Cycle 188 · 2026-04-27 10:00 · docs(runtime): refresh README and SPEC prose with verified Codex and Copilot capability evidence

**Phase**: implementation
**What**: Move 2 Task 1a of the Cross-Runtime Parity Completion plan. Rewrote the README "Runtime support" table to reflect Codex `multi_agent` + `codex_hooks` stable + default-on as of v0.124.0, `[agents.<name>]` conversational dispatch substrate, marketplace verified path (`codex plugin marketplace add jgabor/agentera`), `$HOME/.agents/skills/` as canonical user path versus `~/.codex/skills/` system cache; Copilot row reflects verified marketplace install (granular `<skill>@agentera` and umbrella `jgabor/agentera` both functional, granular sidesteps `github/copilot-cli#2390`), `/fleet` user-driven parallel subagent dispatch, asymmetric hook semantics. Rewrote the README "Lifecycle hooks" table to add a "Supported events" column and reflect 6 supported Codex events (SessionStart, Stop, UserPromptSubmit, PreToolUse, PostToolUse, PermissionRequest) with real-time `apply_patch` Write/Edit interception per `openai/codex#18391` plus 6 supported Copilot events (sessionStart, sessionEnd, userPromptSubmitted, preToolUse, postToolUse, errorOccurred) naming the `preToolUse`-blocks vs `postToolUse`-output-ignored asymmetry explicitly. Added Codex hook config path note (`~/.codex/hooks.json` or inline `[hooks]`) and Copilot hook config path note (`.github/hooks/*.json` or single `hooks.json` with `{version: 1, hooks: {...}}`) with stdin schema field summary. Removed stale "no canonical Agentera Copilot marketplace source is currently verified" hedge from the install section and replaced with the verified marketplace install block (granular plus umbrella) plus the `#2390` bug citation. Updated SPEC Section 7 to add a one-sentence note on the principled PROFILERA_PROFILE_DIR vs AGENTERA_HOME injection asymmetry (per HEALTH Audit 14 finding 2 carry-forward) and tightened the Claude Code source URL to point at the canonical plugins-reference page. Regenerated the 12 per-skill `references/contract.md` files since SPEC.md hash changed. Updated `tests/test_runtime_adapters.py` `_validate_readme_copilot_install_guidance` validator to enforce the new ground-truth wording (verified marketplace install present, `#2390` cited, no "unverified" hedge); updated 3 dependent failing-stale-claim tests to match. No new pytest cases added; the 4-case Copilot install guidance suite continues to enforce the contract, just against current evidence instead of stale evidence.
**Commit**: <pending — pinned by next SHA-pin commit>
**Inspiration**: Cycle 187 capabilities research that proved the original 1.20.0 plan was based on stale assumptions. Same shape as Cycle 173 / Cycle 167 plan-task implementation cycles: documentation honesty work driven by verified upstream evidence, with the test surface updated only where validators codify the prior stale state. The SPEC Section 7 asymmetry note follows the HEALTH Audit 14 finding 2 carry-forward pattern: explain principled design choices in-place rather than letting them read as accidents.
**Discovered**: The README install guidance Copilot validator (`_validate_readme_copilot_install_guidance`) was actively enforcing the stale "no canonical Agentera Copilot marketplace source is currently verified" hedge as a required predicate, blocking the prose update. Replaced the validator predicates with the new ground-truth predicates (verified marketplace install path, granular and umbrella commands present, `#2390` bug cited, no "unverified" hedge). The SPEC.md edit invalidated the 12 per-skill `references/contract.md` source-hash predicates; `python3 scripts/generate_contracts.py` regenerated them in one pass. The README "Codex CLI" lifecycle row in the prior table read "Experimental hooks only; no parity" against current evidence that `codex_hooks` shipped stable + default-on in v0.124.0 (2026-04-23) and `apply_patch` Write/Edit interception merged in `openai/codex#18391` on 2026-04-22; the row now reads "Full lifecycle support" with the same caveat depth Claude Code carries.
**Verified**: N/A: docs-only (README + SPEC prose; PLAN.md status flip; PROGRESS.md cycle entry; per-skill `references/contract.md` regeneration via deterministic script; test validator wording update with no new pytest cases). Validators all green: `python3 scripts/validate_spec.py` returns `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` returns `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` returns `All contract files are current (12 checked)`. `python3 -m pytest -q` returns `433 passed in 0.39s` (unchanged total from the post-Cycle-187 baseline; 4 Copilot-install-guidance test cases continue to pass against the new validator). Em-dash audit clean: `grep -nP "—|–"` against README.md and SPEC.md returns only pre-existing template hits in SPEC sections 4, 14, 15, and 23 (all unmodified by this cycle).
**Next**: Task 1b (structured runtime metadata): update `.codex-plugin/plugin.json` `lifecycleHooks.status`, `unsupportedEvents`, `limitations` prose, and `codex.limitations` array to reflect current Codex hook state; update `agents/openai.yaml` and `skills/profilera/agents/openai.yaml` `support.lifecycle_hooks` fields. Task 1b depends on Task 1a (this cycle) and unblocks Task 3 (Codex `apply_patch` hook wiring). Task 2 (dead Copilot hook fix + frontmatter audit + README install-path swap) and Task 4 (`.agents/plugins/marketplace.json` + agent stubs + `setup_codex.py --enable-agents`) are unblocked in parallel by this cycle.
**Context**: intent (close stale-claim cleanup so every README and SPEC capability claim about Codex and Copilot is verifiable against current upstream evidence; align test fixtures that were enforcing stale state) · constraints (one conventional commit `docs(runtime): ...`; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with `N/A: docs-only` allowlist tag plus four validator outputs; SPEC Section 23 prose conventions with no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences; no new pytest cases; no version bump; do NOT modify CHANGELOG.md or TODO.md per task scope) · unknowns (none — capabilities research provided ground truth and the test failure surfaced predictably) · scope (`README.md` runtime support table + lifecycle hooks table + Copilot install section + Codex install section, `SPEC.md` Section 7 prose + per-runtime mechanism table, `tests/test_runtime_adapters.py` validator and 3 dependent test cases, 12 `skills/*/references/contract.md` regenerated, `.agentera/PLAN.md` Task 1a status flip, `.agentera/PROGRESS.md` this cycle entry).

■ ## Cycle 187 · 2026-04-27 09:00 · chore(release): renumber and consolidate three local pre-push releases into a single 1.20.0

**Phase**: release
**What**: Move 1 of the user-directed Option B parity-completion path. Consolidated three pre-push minor bumps (1.20.0 cross-runtime portability, 1.21.0 setup helpers, 1.22.0 live-host verification) into one coherent 1.20.0 release on this local branch. Renumbered version_files from 1.22.0 to 1.20.0 across `plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js` AGENTERA_VERSION, `registry.json` 12 entries, `.claude-plugin/marketplace.json` 12 versions (profilera entry preserved without a `version` key per long-standing convention), and all 12 `skills/*/.claude-plugin/plugin.json` files. Promoted CHANGELOG: replaced the three separate `## [1.22.0] · 2026-04-26`, `## [1.21.0] · 2026-04-26`, and `## [1.20.0] · 2026-04-26` blocks with a single `## [1.20.0] · 2026-04-27` block whose Added section carries every artifact the three releases shipped (AGENTERA_HOME contract, OpenCode bootstrap, setup_codex.py, setup_copilot.py, smoke_setup_helpers.py, smoke_live_hosts.py, README install + verify-by-hand subsections, README Scripts row enumeration, spec validator lint rule), Changed section carries the cross-skill compaction-form refactor, the SPEC 7-23 to 8-24 renumber, the Codex requiredCapabilities reconciliation, the lifecycle adapter back-compat, the AGENTERA_VERSION drift-test refactor, and the DOCS.md Index expansion, and Note section preserves the unrelated claude-code/conversation_turn duplicate-source_id callout. `## [Unreleased]` left empty at the top so the upcoming Move 2 parity-completion plan has a clean accumulation surface. PROGRESS.md cycles 175-186 stay as-is because they accurately record when the underlying work happened. PLAN.md (Live-Host Verification, all 6 tasks complete) is unchanged here; archival happens with the next plan bootstrap.
**Commit**: 12e434a chore(release): renumber three local pre-push releases into one 1.20.0
**Inspiration**: User feedback that the original 1.20.0 release was supposed to be a coherent "agentera works as intended on all four runtimes" release, plus parallel research that proved both Codex and Copilot capabilities had been documented based on stale assumptions. Nothing has been pushed (`git status: 55 ahead of origin/main, no v1.20/v1.21/v1.22 tags exist`), so renumbering is fully reversible. The consolidation pattern follows the same shape as Cycle 179 / 167 / 124 / 116 / 150 release-cycle bumps but in reverse: instead of promoting one Unreleased block to a new release, it merges three already-promoted blocks back into one.
**Discovered**: Codex `multi_agent` and `codex_hooks` are stable and default-on per local `codex features list` output (was documented as "experimental disabled" in the README). Codex `apply_patch` matchers for Write/Edit hooks shipped 2026-04-22 in PR `openai/codex#18391` (three days before this branch's 1.20.0 plan started, so the original "no real-time Write/Edit interception" claim was already obsolete at plan time). Copilot's marketplace install path is verified working (the user's `~/.copilot/config.json` shows agentera installed via both granular `<skill>@agentera` and umbrella `copilot plugin install jgabor/agentera` paths, contradicting the README's "no canonical Agentera marketplace source is currently verified" claim). Two viable third-party Codex marketplace aggregators exist: `hashgraph-online/awesome-codex-plugins` (mirrored installable marketplace) and `RoggeOhta/awesome-codex-cli` (curated). agentera's `.github/hooks/stop.json` declares `name: stop` but `stop` is not a valid Copilot hook event; the closest valid event is `sessionEnd`, so this hook has been silently dead under Copilot. These deltas are out of scope for Move 1; Move 2 (Cross-Runtime Parity Completion plan) addresses them.
**Verified**: N/A: chore-build-config (release renumber + CHANGELOG consolidation; no functional code changes). `grep -rn '"version": "1.22.0"' plugin.json .github/plugin/plugin.json .codex-plugin/plugin.json registry.json .claude-plugin/marketplace.json skills/*/.claude-plugin/plugin.json` returns 0 matches. `grep -c '"version": "1.20.0"\|AGENTERA_VERSION = "1.20.0"'` across the same set plus `.opencode/plugins/agentera.js` returns 40 occurrences (1 each in three standalone `plugin.json` files, 12 in `registry.json`, 12 in `.claude-plugin/marketplace.json` preserving profilera key-less, 1 in each of 12 `skills/*/.claude-plugin/plugin.json`, 1 in `agentera.js`). marketplace.json profilera entry confirmed without a `version` key. Validators all green: `python3 scripts/validate_spec.py` returns `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` returns `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` returns `All contract files are current (12 checked)`. `python3 -m pytest -q` returns `433 passed in 0.41s` (unchanged from the post-Cycle-186 baseline because Move 1 changes no test surface).
**Next**: Move 2 starts a new "Cross-Runtime Parity Completion" plan that closes the actual gaps the capabilities research surfaced. Tasks are roughly: (1) stale-claim cleanup across README and SPEC for both Codex and Copilot; (2) fix the dead `stop.json` Copilot hook; (3) port `validate_artifact.py` to a Codex `apply_patch` hook so real-time artifact validation works on Codex; (4) add `.agents/plugins/marketplace.json` for Codex marketplace consumption; (5) wire orkestrera runtime-aware dispatch with Codex `[agents.*]` config and a documented Copilot gap; (6) audit all 12 SKILL.md frontmatter against Copilot bug `github/copilot-cli#951`; (7) extend `smoke_live_hosts.py` to verify Codex hooks fire end-to-end; (8) submit aggregator PRs to `github/awesome-copilot` and `hashgraph-online/awesome-codex-plugins` as a follow-up. Move 3 (push and tag 1.20.0) follows after Move 2 lands and inspektera audits clean.
**Context**: intent (collapse three local pre-push minor bumps into one coherent 1.20.0 release that matches the user's original cross-runtime expectation, preserve every substantive artifact the three releases shipped, date the consolidated release for today since the consolidation lands today and the original three same-day bumps no longer fit the release model) · constraints (no push; no tag; one conventional commit; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with `N/A: chore-build-config` allowlist tag plus validator outputs and grep counts as supporting evidence; preserve `.claude-plugin/marketplace.json` profilera entry without a `version` key; do NOT alter the DOCS.md `version_files` list; do NOT edit PROGRESS.md cycles 175-186 since they accurately record when work happened; do NOT touch PLAN.md or HEALTH.md since archival and audit are owned by Move 2 / Move 3) · unknowns (whether to date the consolidated release for today (consolidation date) or for 2026-04-26 (when most underlying work landed): chose today because the release decision lands now and the cycle-level entries preserve when the underlying work happened) · scope (`plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js`, `registry.json`, `.claude-plugin/marketplace.json`, all 12 `skills/*/.claude-plugin/plugin.json`, `CHANGELOG.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 186 · 2026-04-26 21:00 · chore(release): bump suite to 1.22.0

**Phase**: release
**What**: Plan-level freshness checkpoint for the Live-Host Verification plan plus the suite version bump from 1.21.0 to 1.22.0 per DOCS.md `feat = minor` policy because the plan delivered `scripts/smoke_live_hosts.py` (Tasks 2-4) as a new repo-level utility. The plan closed the live-host AGENTERA_HOME-inheritance and SKILL.md compaction-execution verification gap that Audit 15 finding 2 had narrowed (conf 50): T1 audited profilera Codex collection end-to-end and reconciled `.codex-plugin/plugin.json` `requiredCapabilities[codex_session_corpus].status` from `degraded` to `ok` (252 history_prompt + 1 project_config_signal records confirmed in corpus); T2 shipped the harness scaffold with default-mode profilera audit + delegated `smoke_setup_helpers.py` plus `--live`-mode probe, cost gate, consent prompt, and SnapshotRegistry framework with orphan-snapshot auto-recovery; T3 wired the live `codex exec` invocation (one combined prompt exercising AGENTERA_HOME echo via marker brackets AND `compact_artifact.py` against a 14-cycle PROGRESS fixture, with byte-identity SHA256 round-trip on the real `~/.codex/config.toml`); T4 wired the symmetric `bash -c 'export AGENTERA_HOME=...; copilot -p ... --allow-all-tools'` invocation with byte-identity round-trip on all four shell rc files (`~/.bashrc`, `~/.bash_profile`, `~/.profile`, `~/.zshrc`); T5 documented the manual verification protocol in README "Verify ... by hand" subsections under both Codex and Copilot setup blocks plus the new harness row in DOCS.md Index and README Scripts. T6 (this cycle) bumped 19 version surfaces (Copilot root `plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js` AGENTERA_VERSION, `registry.json` 12 entries, `.claude-plugin/marketplace.json` 12 versions preserving the profilera entry without a `version` key per the long-standing convention, all 12 `skills/*/.claude-plugin/plugin.json`), promoted CHANGELOG `## [Unreleased]` to `## [1.22.0] · 2026-04-26` with Added (smoke harness, README "Verify by hand" subsections, README Scripts row) and Changed (`requiredCapabilities[codex_session_corpus].status` reconciliation, lifecycle validator back-compat) entries, fixed the Cycle 183 duplicate `**Commit**` line as plan-level hygiene (per realisera template, exactly ONE `**Commit**` per cycle), filed `[live-host-smoke-mjs-doc-row]` as a Normal TODO follow-up for the missing `smoke_opencode_bootstrap.mjs` Index row that T5 flagged but did not fix, and flipped PLAN.md Task 6 status □ → ■.
**Commit**: 503d22a chore(release): bump suite to 1.22.0
**Commits**: This plan produced 10 commits across 6 cycles: Cycle 181 T1 audit `3b9af17` (chore(audit): profilera Codex collection verification and metadata reconciliation) + SHA-pin `a7fd89a`; Cycle 182 T2 scaffold `513356e` (feat(smoke): add scripts/smoke_live_hosts.py scaffold for codex+copilot verification) + SHA-pin `c6de24d`; Cycle 183 T3 codex live `90f820f` (feat(smoke): wire codex exec AGENTERA_HOME + compaction live verification) + SHA-pin `f45fb69`; Cycle 184 T4 copilot live `32ae296` (feat(smoke): wire copilot -p AGENTERA_HOME + compaction live verification) + SHA-pin `541baa6`; Cycle 185 T5 docs `2c02499` (docs(verify): document manual AGENTERA_HOME verification and surface smoke_live_hosts.py) + SHA-pin `5b7742c`; Cycle 186 T6 release this commit + follow-up SHA-pin.
**Inspiration**: Cycle 179 (commit `cc1eacc`) plan-level freshness checkpoint pattern for Suite Usage Analytics: same shape — promote `## [Unreleased]` to a dated release block with Added/Changed/Fixed entries summarizing each task's user-visible impact (one short line per task, not commit messages verbatim), bump every file in DOCS.md `version_files` in one atomic commit, fix any plan-level cleanup deferred from earlier cycles (Cycle 183's duplicate `**Commit**` line) in the same commit because chore-build-config scope absorbs hygiene cleanly, and add a single plan-summary cycle entry whose **What** field summarizes the plan at the plan level (not per-task restatement) plus a **Commits** field listing every commit the plan produced. The decision to keep `## [Unreleased]` as a fresh empty block at the top after promotion follows the same Cycle 179 / Cycle 173 / Cycle 167 release-cycle convention so the next plan has a clean accumulation surface.
**Discovered**: Resolves HEALTH.md Audit 15 finding 2 (live-host AGENTERA_HOME inheritance still untested, conf 50): Tasks 3-4 verified end-to-end against real `codex exec` and `bash -c '...copilot -p...'` invocations on this host with byte-identity proof on user config files; Task 5 documented the manual verification protocol for users without `--live` access. Next inspektera audit can mark the finding resolved with reference to commits 90f820f, 32ae296, and 2c02499. Independently observed: Cycle 183 had a duplicate `**Commit**` line at the bottom of the entry — both lines named `90f820f`, but exactly one `**Commit**` per cycle is the realisera template invariant. Removed the second occurrence as plan-level hygiene in this same commit (chore(release) scope absorbs the cleanup cleanly, no separate commit needed). The `[live-host-smoke-mjs-doc-row]` TODO entry tracks the unrelated DOCS Index gap that T5 flagged in its Discovered field but explicitly deferred per Task 5 scope.
**Verified**: N/A: chore-build-config (release version bump + CHANGELOG promotion + plan-level freshness checkpoint + duplicate-Commit-line cleanup; no functional code changes). Bump landing evidence — `grep -c '"version": "1.21.0"'` returns 0 across all version_files (`plugin.json` 0, `.github/plugin/plugin.json` 0, `.codex-plugin/plugin.json` 0, `.opencode/plugins/agentera.js` 0 for both `"version":` and `AGENTERA_VERSION =` patterns, `registry.json` 0, `.claude-plugin/marketplace.json` 0, all 12 `skills/*/.claude-plugin/plugin.json` 0). `grep -c '"version": "1.22.0"'` returns expected counts (`plugin.json` 1, `.github/plugin/plugin.json` 1, `.codex-plugin/plugin.json` 1, `.opencode/plugins/agentera.js` 0 for `"version":` and 1 for `AGENTERA_VERSION = "1.22.0"`, `registry.json` 12, `.claude-plugin/marketplace.json` 12 — 1 top-level + 11 inner with profilera deliberately preserved without a `version` key per long-standing convention, each `skills/*/.claude-plugin/plugin.json` 1 across all 12 skills). Validators all green at the post-T5 baseline: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.41s` (unchanged from post-T5 baseline; T6 adds zero pytest cases per the chore-build-config scope).
**Next**: Live-Host Verification plan is complete; PLAN.md will be archived or replaced by the next active plan when one is bootstrapped. The next inspektera audit should mark Audit 15 finding 2 resolved with reference to commits 90f820f, 32ae296, and 2c02499 plus the manual verification protocol now in README. Two open follow-ups remain in TODO Normal: `[claude-code-extract-duplicate-source-ids]` (filed Cycle 181, blocks `~/.local/share/agentera/intermediate/corpus.json` writes on hosts with substantial Claude Code conversation history; fix path documented inline in the entry) and `[live-host-smoke-mjs-doc-row]` (filed this cycle, low-impact docs gap for `smoke_opencode_bootstrap.mjs` row in DOCS Index and README Scripts). The `[copilot-marketplace]` and `[opencode-session-events]` follow-ups carried from prior plans remain Normal-tier and unchanged.
**Context**: intent (close Live-Host Verification plan with the `feat = minor` release bump per DOCS.md policy, promote CHANGELOG with one short Added/Changed entry per user-visible impact, fix the Cycle 183 duplicate `**Commit**` line as plan-level hygiene in the same commit, file the missing `smoke_opencode_bootstrap.mjs` row as a Normal TODO follow-up since T5 explicitly deferred it, and resolve HEALTH.md Audit 15 finding 2 in this entry's Discovered field per AC6 because no new audit has run since the plan started) · constraints (one conventional commit `chore(release): bump suite to 1.22.0` plus a SHA-pin follow-up commit; no push; bump every file in DOCS.md `version_files` and add no new entries; preserve the marketplace.json profilera entry without a `version` key per the long-standing convention; mandatory `**Verified**` field per SPEC Section 19 with `N/A: chore-build-config` allowlist tag plus validator outputs and grep-bump-landing evidence; EXACTLY ONE `**Commit**` line in this entry to avoid the Cycle 183 defect; PLAN.md Task 6 status flipped □ → ■ in the same commit; SPEC Section 23 prose conventions: no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences) · unknowns (none — task acceptance criteria were fully concrete and the existing Cycle 179 / Cycle 173 release-cycle pattern provided the template) · scope (`plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js`, `registry.json`, `.claude-plugin/marketplace.json`, all 12 `skills/*/.claude-plugin/plugin.json`, `CHANGELOG.md`, `TODO.md`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 185 · 2026-04-26 20:30 · docs(verify): document manual AGENTERA_HOME verification and surface smoke_live_hosts.py

**Phase**: docs
**What**: Task 5 of the Live-Host Verification plan. Three coupled doc surfaces landed atomically. README Copilot setup section gained a "Verify Copilot AGENTERA_HOME by hand" subsection placed after the manual `echo 'export AGENTERA_HOME=...' >> ~/.bashrc` block; it carries one bash one-liner the user pastes into a real `copilot` session — `bash -c 'echo "AGENTERA_HOME=$AGENTERA_HOME"; python3 ${AGENTERA_HOME:-$CLAUDE_PLUGIN_ROOT}/scripts/compact_artifact.py'` — that exercises both the AGENTERA_HOME echo (read-side inheritance per SPEC.md Section 7) AND the bash-fallback compaction-script resolution in one paste, with prose naming the two expected outcomes (echo prints the install root; `compact_artifact.py` prints its `usage:` line proving the bash-fallback resolved). README Codex setup section gained the symmetric "Verify Codex AGENTERA_HOME by hand" subsection placed after the manual TOML snippet, same one-liner shape, same two-outcomes rubric, with the `codex exec` non-interactive verification scope explicitly named so the user knows the protocol covers both interactive and non-interactive surfaces via `[shell_environment_policy].set` semantics. The chosen one-liner uses `compact_artifact.py` with no arguments (which exits 2 with a usage line) rather than `--help` (which the script also treats as an unknown spec name and exits 2 with the same usage line) so neither path mutates a real artifact. Both subsections explicitly cite when to use the manual protocol: no auth, no API budget, behind a firewall, or any host where invoking the model is undesirable. README Scripts section gained a `scripts/smoke_live_hosts.py` row alongside the existing `smoke_setup_helpers.py` row; the row enumerates default mode (no flags, runs profilera Codex audit + delegates to `smoke_setup_helpers.py`, no live CLI invoked, $0 cost), `--live` mode (prints `Estimated cost: $0.20-1.00 across two model calls` and a one-line consent prompt before any subprocess invocation, exactly one `codex exec` and one `bash -c '...copilot -p...'` invocation each carrying a combined prompt that exercises both AGENTERA_HOME echo AND `compact_artifact.py` execution, snapshot/restore + SHA256 round-trip on `~/.codex/config.toml` and shell rc files), and the gap it closes (Audit 15 finding 2: "Codex and Copilot live-host AGENTERA_HOME inheritance still untested"). DOCS.md Index gained a `Live-host smoke runner | scripts/smoke_live_hosts.py | 2026-04-26 | ■ current` row positioned alongside the existing `Setup helper smoke runner` row. DOCS.md Audit Log gained a `### 2026-04-26 (Live-Host Verification Task 5)` block listing four (fixed)-status findings: Index missing the `smoke_live_hosts.py`row, README Codex section lacking a manual verification protocol, README Copilot section lacking the same, and README Scripts section not enumerating the new harness with default-mode/`--live`-mode/Audit-15-finding-2 callouts. DOCS.md`Last audit:` comment bumped from `2026-04-26 (Codex+Copilot Setup Helpers Task 4)` to `2026-04-26 (Live-Host Verification Task 5)`. PLAN.md Task 5 status flipped □ → ■.
**Commit**: 2c02499 docs(verify): document manual AGENTERA_HOME verification and surface smoke_live_hosts.py
**Inspiration**: Cycle 178 (commit`8e57ef4`) docs-cycle pattern for surfacing new helpers in README Scripts plus DOCS.md Index plus DOCS.md Audit Log in a single coherent commit with`(fixed)` status entries grouped by `2026-04-26 (<plan name> <task>)` header. The "Verify ... by hand" subsection naming mirrors the existing `Verify local plugin metadata with: claude plugin validate .` framing already in the README's Alternative install methods block (action verb + scope + concrete command). The choice of `compact_artifact.py` with no arguments (exit 2 + usage line) rather than `--help` came from inspection of the script's manual argparse: both paths print the same usage line with the same exit code, so the no-argument form is the cleanest spec name to teach because a user can extend it to `... compact_artifact.py progress <path>`for a real compaction without re-reading docs. The marker-bracketed prompt design from Cycle 183 (`===AGENTERA_HOME_ECHO_BEGIN===`) is what the harness uses internally; the manual one-liner uses simple`echo "AGENTERA_HOME=$AGENTERA_HOME"` because the user is reading the output by eye, not parsing markers programmatically.
**Discovered**: `compact_artifact.py --help` exits 2 with `usage: compact_artifact.py <spec-name> <path>` because the script does manual arg parsing rather than argparse, so `--help` is treated as an unknown spec name. This is fine for the manual protocol — the user sees the usage line and knows the script resolved through the bash-fallback. The same exit code (2) and output happen for the no-argument invocation, so the docs use the simpler no-argument form. `smoke_opencode_bootstrap.mjs` is referenced in the Task 5 acceptance criterion ("alongside `smoke_setup_helpers.py` and `smoke_opencode_bootstrap.mjs`") but is not yet in DOCS.md Index nor README Scripts; per Task 5 scope (add the new harness row, not rectify pre-existing missing rows) the new row sits next to`smoke_setup_helpers.py` only. The mjs row is a separate gap that future docs work can address. Cycle 183's duplicate `**Commit**` line (PROGRESS.md lines 18 and 23 both `Commit: 90f820f`) remains unfixed per task constraint — Task 6 owns plan-level cleanup.
**Verified**: N/A: docs-only. Changes are README.md (two new "Verify ... by hand" subsections under Copilot and Codex setup blocks plus one new Scripts table row),`.agentera/DOCS.md` (one new Index row, one new Audit Log entry block, one Last-audit comment bump), `.agentera/PLAN.md` (Task 5 status flip), and this PROGRESS.md cycle entry. No scripts, hooks, SKILL.md files, version files, or CHANGELOG touched. Validators all green at the post-T4 baseline: `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills`.`python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok`.`python3 scripts/generate_contracts.py --check` -> `All contract files are current (12 checked)`.`python3 -m pytest -q` -> `433 passed in 0.42s` (unchanged from post-T4 Cycle 184 baseline; Task 5 is documentation only and adds zero pytest cases per AC5).
**Next**: Task 6 (plan-level freshness checkpoint plus 1.21.0 → 1.22.0 release bump per `feat = minor` policy because the smoke_live_hosts.py harness is a Tasks 2-4 feat) is now unblocked; T1, T2, T3, T4, and T5 are all complete. Task 6 will promote CHANGELOG `## [Unreleased]` to `## [1.22.0] · 2026-04-26` listing the live-host smoke harness as Added and any profilera metadata reconciliation under Changed, bump every file in DOCS.md `version_files`, fix the Cycle 183 duplicate`**Commit**` line as part of plan-level cleanup, and resolve HEALTH.md Audit 15 finding 2 in the next audit or in this plan's PROGRESS cycle entry's Discovered field.
**Context**: intent (give users without `--live` access — no auth, no API budget, behind firewall — a documented manual path to verify AGENTERA_HOME inheritance and SKILL.md compaction-script resolution under both `codex` and `copilot`; surface the new`scripts/smoke_live_hosts.py` harness in the README Scripts catalog and DOCS.md Index so it is discoverable from the same entry points as `validate_spec.py`,`eval_skills.py`,`usage_stats.py`, and the setup helpers; resolve the four (fixed)-status DOCS.md findings via one Audit Log block) · constraints (documentation only — NO scripts, NO hooks, NO SKILL.md, NO version files, NO CHANGELOG per Task 6 scope; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory`**Verified**` field with `N/A: docs-only` allowlist tag plus validator outputs as supporting evidence per SPEC Section 19; no new pytest cases per AC5; EXACTLY ONE `**Commit**`line in this entry to avoid the Cycle 183 duplicate-Commit-line defect; PLAN.md Task 5 status flipped □ → ■ in the same commit; SPEC Section 23 prose conventions: no em-dashes, colons for labels, middle dot · for separators, ≤25 word sentences) · unknowns (none — task acceptance criteria were fully concrete and the existing Cycle 178 pattern provided the template) · scope (`README.md`,`.agentera/DOCS.md`,`.agentera/PLAN.md`,`.agentera/PROGRESS.md`).

■ ## Cycle 184 · 2026-04-26 20:00 · feat(smoke): wire copilot -p AGENTERA_HOME + compaction live verification

**Phase**: build
**What**: Task 4 of the Live-Host Verification plan. Replaced the Task 2 stub `run_copilot_live_section` with the end-to-end live-host wiring: PATH probe (`shutil.which("copilot")`), `copilot --version` sanity probe (10s), AC5 auth probe via `bash -c 'copilot -p "reply with the literal text OK and nothing else" --allow-all-tools'` with a 30s timeout (timeout / non-zero / missing-OK all SKIP with guidance pointing the operator at GitHub Copilot CLI's interactive auth flow), snapshot every existing shell rc file in `(.bashrc, .bash_profile, .profile, .zshrc)` and SHA256-hash each BEFORE any mutation (defense-in-depth mirroring the Codex section's `~/.codex/config.toml` round-trip even though `bash -c 'export ...'` does not touch any rc file), build a tmp install root via `tempfile.TemporaryDirectory(prefix="agentera-smoke-copilot-home-")` (using a fresh tmp dir as the AGENTERA_HOME value cleanly proves the bash-exported value is what propagates to copilot — there is no way for the harness's parent process to have set it to this fresh tmp path), seed a tmp `PROGRESS.md` fixture with 14 mock cycles via the existing `_build_progress_fixture` helper (above the 10-keep threshold so compaction MUST move 4 entries into `## Archived Cycles`), compose the same marker-bracketed combined prompt shape as Task 3 (`===AGENTERA_HOME_ECHO_BEGIN===` / `===COMPACTION_OUTPUT_BEGIN===`) instructing the agent to run `echo "AGENTERA_HOME=$AGENTERA_HOME"` then `python3 <repo>/scripts/compact_artifact.py progress <fixture>`, escape the prompt for embedding inside a `bash -c '...'` outer single-quoted script (backslash-escape `\\`, `"`, `$`, and backtick so bash does not terminate the inner `copilot -p "..."` quoting early), and issue exactly ONE invocation of the AC1 shape: `subprocess.run(["bash", "-c", f"export AGENTERA_HOME={tmp_install_root}; copilot -p \"{escaped_prompt}\" --allow-all-tools"])` with a 300s timeout (mirroring the Codex section's wall-clock cap). Parser pulls the AGENTERA_HOME value via the existing `_extract_between` substring slicing helper and asserts it contains `AGENTERA_HOME=<tmp_install_root>`; fixture mtime + line-count delta + `## Archived Cycles` heading presence are checked against the pre-seed snapshot. Post-run SHA256 of every snapshotted rc file is compared against the pre-run hash via a new `_assert_rc_unchanged` helper that hard-fails on any mismatch and logs every after-hash for operator audit even on the success path. The auth-failure branch (non-zero exit with "auth"/"login" in stderr) routes through `_assert_rc_unchanged` before returning so the SKIP path still proves byte-identity. PLAN.md Task 4 status flipped □ → ■.
**Commit**: 32ae296 feat(smoke): wire copilot -p AGENTERA_HOME + compaction live verification
**Inspiration**: Task 3's marker-bracketed prompt design (`===AGENTERA_HOME_ECHO_BEGIN===` / `===COMPACTION_OUTPUT_BEGIN===`) plus the `_extract_between` helper from Cycle 183 (commit `90f820f`) is the direct twin: same prompt shape, same parser, same `_build_progress_fixture` 14-cycle seed, same compaction-effect assertions (mtime advanced + line-count reduced + archive heading present). The "use a tmp dir as the AGENTERA_HOME value rather than REPO_ROOT" choice borrows from the same isolation pattern Task 3 uses for `CODEX_HOME` (point env at a tmp surface so the test cannot accidentally rely on the parent process happening to have the right value baked in). The shell-quote-escape logic (backslash-escape `\\`, `"`, `$`, backtick to embed an arbitrary string inside `bash -c '...'`'s outer single quotes wrapping a `copilot -p "..."` inner double-quote) is borrowed from how `subprocess.list2cmdline` handles quoting on Windows — same problem (literal-pass an arbitrary string through one shell layer), different solution (we control the wrapping shape so we only need to escape the four metacharacters that matter inside double quotes). The `_assert_rc_unchanged` helper extracts the SHA256-round-trip pattern from Task 3's inline check so multiple rc files can be verified uniformly without duplicating the assert+log pair four times.
**Discovered**: `bash -c 'copilot -p "..." --allow-all-tools'` ran in 25.2s end-to-end on this host, well under the 300s ceiling. Compaction effect was unambiguous: the seeded 14-cycle fixture (lines=127) shrank to 98 lines with `## Archived Cycles` heading present and 4 cycles converted to one-line entries (compaction stdout: `compacted: <fixture> (14->10 full, 0->4 oneline, 0 dropped)` — identical to the Codex section's output, which proves both runtimes execute the same script through their respective shell-tool surfaces). AGENTERA_HOME echo came back exactly as the tmp install root value (`/tmp/agentera-smoke-copilot-home-08hqnv55`), proving copilot inherits the env var from the parent bash shell that exported it — the read side of the inheritance promise. All four snapshotted rc files (`.bashrc`, `.bash_profile`, `.profile`, `.zshrc`) had byte-identical SHA256 before and after (`6d7414d0...` for bashrc, `9c8e62d7...` for bash_profile, `e3b0c442...` for the empty profile/zshrc), confirming the bash-export form does not touch any rc file. Auth probe shape: `bash -c 'copilot -p "reply OK" --allow-all-tools'` returned 0 with output containing "OK" within ~5s on this authed host; on a fresh unauthed host the call would block on GitHub's device-flow login prompt and the 30s timeout would fire, producing the AC5 SKIP with the `copilot` interactive-auth guidance. Copilot's stdout is more verbose than codex's `--output-last-message` (it echoes tool-call shells like `● Echo AGENTERA_HOME env var (shell)` between the markers), but the marker brackets isolate the values so `_extract_between` returns them verbatim regardless. No surprises affecting plan scope. Note: chose a tmp install root (not REPO_ROOT) because AC1 says `<tmp install root>` literally — this also strengthens the proof, since a fresh tmp path could not be in the parent process's environment by accident.
**Verified**: Live-host smoke run on this host. `echo y | python3 scripts/smoke_live_hosts.py --live` produced (Copilot section excerpt verbatim): `--- copilot live section ---` / `probe: copilot resolved to /bin/copilot` / `probe: copilot GitHub Copilot CLI 1.0.36.` / `probe: copilot auth probe healthy (output contains 'OK')` / `snapshot: $HOME/.bashrc -> /tmp/agentera-smoke-live-.bashrc-20260426-171650-7fea445a47b0.bak` / `copilot-rc sha256 (before) $HOME/.bashrc: 6d7414d07b61aab0490bdbe8883fc44f303df5ea2e902dff03a6bc186cf96a49` / `snapshot: $HOME/.bash_profile -> /tmp/agentera-smoke-live-.bash_profile-...bak` / `copilot-rc sha256 (before) $HOME/.bash_profile: 9c8e62d73277fb2eda68762473a3dcf15e73bb2e224e6859a1a09cfb811410b6` / `snapshot: $HOME/.profile -> /tmp/agentera-smoke-live-.profile-...bak` / `copilot-rc sha256 (before) $HOME/.profile: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `snapshot: $HOME/.zshrc -> /tmp/agentera-smoke-live-.zshrc-...bak` / `copilot-rc sha256 (before) $HOME/.zshrc: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `copilot: tmp AGENTERA_HOME=/tmp/agentera-smoke-copilot-home-08hqnv55` / `copilot: seeded fixture .../PROGRESS.md (lines=127, mtime=1777216610)` / `copilot: invoking bash -c 'export AGENTERA_HOME=/tmp/agentera-smoke-copilot-home-08hqnv55; copilot -p "..." --allow-all-tools'` / `copilot: bash -c 'copilot -p ...' returned exit=0 in 25.2s` / agent output between markers: `===AGENTERA_HOME_ECHO_BEGIN=== AGENTERA_HOME=/tmp/agentera-smoke-copilot-home-08hqnv55 ===AGENTERA_HOME_ECHO_END===` and `===COMPACTION_OUTPUT_BEGIN=== compacted: /tmp/agentera-smoke-copilot-fixture-5ypp38pn/PROGRESS.md (14->10 full, 0->4 oneline, 0 dropped) ===COMPACTION_OUTPUT_END===` / `copilot: AGENTERA_HOME echo verified: AGENTERA_HOME=/tmp/agentera-smoke-copilot-home-08hqnv55` / `copilot: fixture compaction verified (lines 127 -> 98, archived heading: present)` / `copilot-rc sha256 (after) $HOME/.bashrc: 6d7414d07b61aab0490bdbe8883fc44f303df5ea2e902dff03a6bc186cf96a49` / `copilot-rc sha256 (after) $HOME/.bash_profile: 9c8e62d73277fb2eda68762473a3dcf15e73bb2e224e6859a1a09cfb811410b6` / `copilot-rc sha256 (after) $HOME/.profile: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `copilot-rc sha256 (after) $HOME/.zshrc: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` / `copilot: verified under bash -c 'export AGENTERA_HOME=…; copilot -p … --allow-all-tools'; user shell rc files byte-identical (SHA256 round-trip)` / `PASS: all smoke checks passed` (exit 0). AC1 satisfied (exactly ONE `bash -c 'export AGENTERA_HOME=<tmp>; copilot -p "..." --allow-all-tools'` invocation issued; subprocess.run shape is `["bash", "-c", "export AGENTERA_HOME=...; copilot -p \"...\" --allow-all-tools"]`; combined prompt drove both shell tool calls). AC2 satisfied (AGENTERA_HOME echo `/tmp/agentera-smoke-copilot-home-08hqnv55` matches the export verbatim; fixture mtime advanced 1777216610 → post-run, lines reduced 127 → 98, archive heading present). AC3 satisfied (consent prompt unchanged from Cycle 182: `Proceed with live CLI invocations (codex exec + copilot -p --allow-all-tools)? [y/N]:` — `--allow-all-tools` named explicitly). AC4 satisfied (all 4 rc files byte-identical via SHA256 round-trip: bashrc `6d7414d0...` before == after, bash_profile `9c8e62d7...` before == after, profile/zshrc `e3b0c442...` (empty file hash) before == after). AC5 satisfied (auth probe uses `bash -c 'copilot -p "reply with the literal text OK and nothing else" --allow-all-tools'` with `AUTH_PROBE_TIMEOUT_SECONDS=30`; timeout / non-zero / missing-OK all route through `skip()` with guidance pointing at GitHub Copilot CLI's interactive auth flow — verified by reading the three skip branches in `run_copilot_live_section` steps 2 and 3). Validators all green at post-T3 baseline: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.41s` (unchanged from post-T3 baseline; T4 adds zero pytest cases per T2 AC6). Pre-commit hooks ran clean.
**Next**: Task 5 (manual verification protocol in README + DOCS surface) is now unblocked since it depends on Tasks 1-4 and Task 4 was the last execution-side task. Task 6 (1.21.0 → 1.22.0 release bump + CHANGELOG) follows once Task 5 lands. The Copilot section's stdout is more verbose than Codex's (it echoes shell-tool wrappers like `● Echo AGENTERA_HOME env var (shell)`), but the marker-bracketed parsing isolates the values cleanly — Task 5 docs should mention the verbose Copilot output as expected behavior so a user reading the harness output knows what to look for. Cycle 183 has a duplicate `**Commit**` line (lines 7 and 12 both `Commit: 90f820f`); Task 6 owns plan-level cleanup, so flagged here for attention without fixing in this cycle.
**Context**: intent (close the live-host AGENTERA_HOME-inheritance + compaction-execution verification gap from Audit 15 finding 2 against the real `copilot` CLI by issuing exactly one `bash -c 'export AGENTERA_HOME=<tmp>; copilot -p "..." --allow-all-tools'` invocation that exercises both the env-var propagation read side AND the compaction script the SKILL.md prose cites, with byte-identity proof on every shell rc file the harness touches and a skip-with-guidance path on auth failure) · constraints (stdlib only per CLAUDE.md scripts convention; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field per SPEC Section 19 with literal `bash -c 'copilot -p ...'` stdout capture proving AGENTERA_HOME echo + compaction effect AND SHA256 round-trip on all snapshotted rc files — no `N/A` allowlist tag because this is functional code; do NOT touch the Codex section per Task 3 scope, do NOT modify SnapshotRegistry/orphan-recovery framework per Task 3 scope (they work), do NOT modify README/DOCS per Task 5 scope, do NOT bump version per Task 6 scope, do NOT modify CHANGELOG per Task 6 scope; tests/ stays untouched per T2 AC6 — the harness IS the test surface; single `copilot -p` invocation per AC1 — do not retry on failure mid-prompt, let it fail visibly so the bug is observable) · unknowns (whether the bash-quote-escape logic would survive copilot's prompt-parsing — confirmed by 25s end-to-end run with both shell tool calls observed and both markers parsed verbatim; whether copilot's verbose stdout would defeat the marker-extraction — confirmed by `_extract_between` substring slicing returning the values cleanly even with shell-tool wrappers and prose between markers; whether using a tmp dir vs REPO_ROOT for the AGENTERA_HOME value would change the assertion shape — chose tmp per AC1's literal `<tmp install root>` text and confirmed the parser works against the tmp value verbatim) · scope (`scripts/smoke_live_hosts.py`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 183 · 2026-04-26 19:30 · feat(smoke): wire codex exec AGENTERA_HOME + compaction live verification

**Phase**: build
**What**: Task 3 of the Live-Host Verification plan. Replaced the Task 2 stub `run_codex_live_section` with the end-to-end live-host wiring: PATH probe (`shutil.which`), `codex --version` sanity probe, snapshot + SHA256 of the real `~/.codex/config.toml` BEFORE any tmp surface is created, build a tmp `CODEX_HOME` directory (`tempfile.TemporaryDirectory(prefix="agentera-smoke-codex-home-")`), copy the user's real `auth.json` into it so codex is authenticated against the tmp config, invoke `setup_codex.py --config-file <tmp>/config.toml --install-root <repo>` to write the AGENTERA_HOME entry under `[shell_environment_policy].set` in the tmp config (real config never touched), seed a tmp `PROGRESS.md` fixture with 14 mock cycles (above the 10-keep threshold so compaction MUST move 4 entries into `## Archived Cycles`), then issue exactly ONE `codex exec` invocation with `CODEX_HOME=<tmp>` exported via env, `--cd <tmp_workdir> --skip-git-repo-check --dangerously-bypass-approvals-and-sandbox --output-last-message <tmp>` (the user pre-authorized live spend so the bypass-approvals path is acceptable for one invocation), and a combined prompt instructing the agent to run two bash tool calls in sequence — `echo "AGENTERA_HOME=$AGENTERA_HOME"` and `python3 <repo>/scripts/compact_artifact.py progress <fixture>` — then print the captured outputs between distinct `===AGENTERA_HOME_ECHO_BEGIN===` / `===COMPACTION_OUTPUT_BEGIN===` markers. Parser pulls the AGENTERA_HOME value via `_extract_between` substring slicing (no regex) and asserts it contains `AGENTERA_HOME=<install_root>`; fixture mtime + line-count delta + `## Archived Cycles` heading presence are checked against the pre-seed snapshot. Post-run SHA256 of the real config is compared against the pre-run hash and a hard fail is raised on mismatch (AC4 defense-in-depth — even though the tmp `CODEX_HOME` redirect means the real config is never written, the SHA256 round-trip would catch a regression that accidentally targeted the real path). On success the harness prints `verified under codex exec; interactive mode inferred via shell_environment_policy semantics` per AC3. Added orphan-snapshot detection at startup (AC5): each `SnapshotRegistry.snapshot` write now drops a sidecar `<backup>.meta` file containing the original absolute path; `recover_orphan_snapshots()` runs once at the top of `main()` before any other section, scans `/tmp/agentera-smoke-live-*.bak`, restores each backup over the path the sidecar names, and unlinks both the backup and the sidecar so the next run sees a clean tmp surface. `restore_all` was extended to clean up the sidecar alongside its backup so a normal-exit run leaves no orphans for the next invocation. PLAN.md Task 3 status flipped □ → ■.
**Commit**: 90f820f feat(smoke): wire codex exec AGENTERA_HOME + compaction live verification
**Inspiration**: Task 2's `SnapshotRegistry` + cost-gate + probe scaffold from Cycle 182 (commit `513356e`) is the substrate this cycle plugs into; the section structure (probe → snapshot → mutate → assert → finally-restore) and the `PASS:` / `SKIP:` / `FAIL:` protocol are unchanged. The "build a tmp `CODEX_HOME` and copy `auth.json`" pattern mirrors the `setup_codex.py --config-file <PATH>` flag's purpose (cited verbatim in the task hint as "use this to point at a TMP path so the user's real `~/.codex/config.toml` is never touched") combined with codex's documented `CODEX_HOME` env var that overrides the config dir lookup. The marker-bracketed prompt design (`===AGENTERA_HOME_ECHO_BEGIN===` / `===COMPACTION_OUTPUT_BEGIN===`) is borrowed from how `scripts/eval_skills.py` parses agent output — distinct unique markers around the values let the harness extract them without depending on prose surrounding the values. The orphan-recovery sidecar pattern (write `<backup>.meta` next to `<backup>.bak` so a later process can find the original path) is structurally similar to how `git rebase --autosquash` uses `.bak` + a fixup script — pair a state file with a metadata file so recovery is deterministic.
**Discovered**: codex exec ran in 17.0s end-to-end, well under the 300s timeout cap chosen as a safe ceiling (the Bash tool's 10-minute hard cap is the real ceiling, and a hung run would orphan the snapshot pair which the next invocation auto-restores). Compaction effect was unambiguous: the seeded 14-cycle fixture (lines=127) shrank to 98 lines with `## Archived Cycles` heading present and 4 cycles converted to one-line entries (output: `compacted: <fixture> (14->10 full, 0->4 oneline, 0 dropped)`). AGENTERA_HOME echo came back exactly as `$HOME/git/agentera` — the value `setup_codex.py` wrote to the tmp config under `[shell_environment_policy].set.AGENTERA_HOME` was correctly propagated by codex into the bash tool's subprocess env, confirming the read side of the inheritance promise that Audit 15 finding 2 flagged as untested. Real `~/.codex/config.toml` SHA256 was `071e5bbf951a8ef1b7a2161e451db3e2ea93bdacd941b0e02e93375879f39486` BEFORE and AFTER the run — byte-identical, AC4 satisfied. Orphan-recovery validated end-to-end via a synthetic test: created a fake `/tmp/agentera-smoke-live-fakefile-19700101-000000-deadbeef.bak` + sidecar pointing at a tmp original, mutated the original, ran the harness, observed the orphan was detected (`orphan-snapshots: detected 1 tmp backup(s) from a prior run`), restored, and the original's SHA256 matched its pre-mutation value. No surprises affecting plan scope. Note: chose `--dangerously-bypass-approvals-and-sandbox` for the one codex exec call rather than `-s danger-full-access` alone because the latter still triggers approval prompts for shell tool invocations under codex 0.125.0; the bypass flag is the documented non-interactive shortcut and the user pre-authorized live spend.
**Verified**: Live-host smoke run on this host. `echo y | python3 scripts/smoke_live_hosts.py --live` produced (verbatim, with the codex exec stdout captured between markers): `--- codex live section ---` / `probe: codex resolved to /bin/codex` / `probe: codex codex-cli 0.125.0` / `snapshot: $HOME/.codex/config.toml -> /tmp/agentera-smoke-live-config.toml-20260426-165644-7f7b6d978f90.bak` / `codex-config sha256 (before): 071e5bbf951a8ef1b7a2161e451db3e2ea93bdacd941b0e02e93375879f39486` / `codex: tmp CODEX_HOME=/tmp/agentera-smoke-codex-home-1gp0yedb` / `codex: copied auth.json into tmp CODEX_HOME (authed)` / `codex: setup_codex.py wrote /tmp/.../config.toml: wrote ...config.toml: write fresh config with shell_environment_policy.set.AGENTERA_HOME = $HOME/git/agentera` / `codex: seeded fixture .../PROGRESS.md (lines=127, mtime=1777215405)` / `codex: invoking codex exec (CODEX_HOME=/tmp/agentera-smoke-codex-home-1gp0yedb)` / `codex: codex exec returned exit=0 in 17.0s` / agent output between markers: `===AGENTERA_HOME_ECHO_BEGIN=== AGENTERA_HOME=$HOME/git/agentera ===AGENTERA_HOME_ECHO_END===` and `===COMPACTION_OUTPUT_BEGIN=== compacted: .../PROGRESS.md (14->10 full, 0->4 oneline, 0 dropped) ===COMPACTION_OUTPUT_END===` / `codex: AGENTERA_HOME echo verified: AGENTERA_HOME=$HOME/git/agentera` / `codex: fixture compaction verified (lines 127 -> 98, archived heading: present)` / `codex-config sha256 (after): 071e5bbf951a8ef1b7a2161e451db3e2ea93bdacd941b0e02e93375879f39486` / `codex: verified under codex exec; interactive mode inferred via shell_environment_policy semantics` / `PASS: all smoke checks passed` (exit 0). AC1 satisfied (exactly one `codex exec` invocation; combined prompt drove both bash tool calls). AC2 satisfied (AGENTERA_HOME echo matches install root; fixture mtime advanced 1777215405 → post-run, lines reduced 127 → 98, archive heading present). AC3 satisfied (literal "verified under codex exec; interactive mode inferred via shell_environment_policy semantics" line printed). AC4 satisfied (real config SHA256 byte-identical: `071e5bbf...` before == after; independently confirmed via `sha256sum ~/.codex/config.toml`). AC5 satisfied via synthetic orphan injection: created `/tmp/agentera-smoke-live-fakefile-19700101-000000-deadbeef.bak` + `.meta` sidecar pointing at a separate tmp original, mutated the original, ran the harness, observed `orphan-snapshots: detected 1 tmp backup(s) from a prior run; attempting auto-restore` / `orphan-snapshots: restored /tmp/agentera-orphan-test-sr1I from /tmp/agentera-smoke-live-fakefile-19700101-000000-deadbeef.bak (crashed prior run)` and the restored file's SHA256 matched the pre-mutation hash exactly. Validators all green at post-T2 baseline: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.41s` (unchanged from post-T2 baseline; T3 adds zero pytest cases per T2 AC6 — the harness IS the test surface). Pre-commit hooks ran clean.
**Next**: Task 4 (Copilot live-host AGENTERA_HOME + compaction smoke via `bash -c 'export AGENTERA_HOME=...; copilot -p ... --allow-all-tools'`) is unblocked since it depends only on Task 2 and Task 3 left the harness skeleton intact for Task 4 to plug into via `run_copilot_live_section`. Task 5 (README + DOCS surface) and Task 6 (1.21.0 → 1.22.0 bump) follow once Task 4 lands. The orphan-snapshot recovery framework added here also benefits T4: any sidecar-tagged backup left by a crashed Copilot section will be auto-restored on the next invocation, regardless of which section produced it.
**Context**: intent (close the live-host AGENTERA_HOME-inheritance + compaction-execution verification gap from Audit 15 finding 2 against the real `codex` CLI by issuing exactly one `codex exec` invocation that exercises both the env-var propagation read side AND the compaction script the SKILL.md prose cites, with byte-identity proof on the real `~/.codex/config.toml` and orphan-snapshot auto-recovery so a crashed run cannot strand the user's config) · constraints (stdlib only per CLAUDE.md scripts convention; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field per SPEC Section 20 with literal codex exec output capture proving AGENTERA_HOME echo + compaction effect — no `N/A` allowlist tag because this is functional code; do NOT touch the Copilot section per Task 4 scope, do NOT modify README/DOCS per Task 5 scope, do NOT bump version per Task 6 scope; tests/ stays untouched per T2 AC6) · unknowns (whether codex exec under `CODEX_HOME=<tmp>` would still recognize the user's auth — addressed by copying real `auth.json` into the tmp dir before invocation; whether the bypass-approvals flag would land the bash tool calls without prompts — confirmed by 17s end-to-end run with both tool calls observed in output; whether the marker-extraction would survive prose surrounding the values — confirmed by `_extract_between` substring slicing returning the values verbatim) · scope (`scripts/smoke_live_hosts.py`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 182 · 2026-04-26 19:00 · feat(smoke): add scripts/smoke_live_hosts.py scaffold for codex+copilot verification

**Phase**: build
**What**: Task 2 of the Live-Host Verification plan. Shipped `scripts/smoke_live_hosts.py`, a stdlib-only harness that mirrors the Cycle 177 `smoke_setup_helpers.py` shape (sequential numbered sections, `PASS:` / `FAIL:` / `SKIP:` to stdout, top-level `finally` restore, fail-fast). Two modes. Default mode (no flags) runs the profilera Codex collection audit by importing `extract_all` from the profilera scripts directory and calling `build_corpus()` in process — this is the Task 1 path and avoids the unrelated claude-code duplicate-source_id failure that blocks the extractor's CLI from writing corpus.json. Records are grouped by their top-level `runtime` key (not `provenance.runtime`) and per-runtime counts plus `metadata.runtime_status` are reported on stdout. After the audit, the harness delegates to `scripts/smoke_setup_helpers.py` as a subprocess so the existing 11-case offline gate continues to run; the delegated stdout is indented two spaces under this harness's output for visual grouping. `--live` mode prints `Estimated cost: $0.20-1.00 across two model calls (one per runtime; --live mode only)` and a one-line consent prompt naming `codex exec + copilot -p --allow-all-tools` so the operator knows what permission they grant; `y` or `yes` (case-insensitive) proceeds, anything else aborts. Each per-runtime probe distinguishes "not on PATH" (`shutil.which` returns `None` → no subprocess invoked) from "binary present but auth probe timed out" (timeout caught at 10s for `--version` or 30s for the auth probe → recorded as `not-authed` skip with the timeout reason in the message). Both skip types accumulate into a `skips` list and emit a one-line summary at the end before the trailing `PASS:` so a host with no live CLIs sees a clean exit-0 with the skipped sections enumerated. The `SnapshotRegistry` class registers `(original_path, tmp_backup_path)` pairs BEFORE any mutation: existing files are copied to `/tmp/agentera-smoke-live-<basename>-<timestamp>-<id>.bak` and the tmp path is logged; absent files record an absence marker so the restore path can unlink any file created during the run. `restore_all()` runs in the top-level `finally` so it executes on success, hard fail, and crash. The Task 2 cut wires the snapshot framework but registers snapshots only after a successful probe; Tasks 3 and 4 plug in the actual `codex exec` AGENTERA_HOME + `compact_artifact.py` invocation and the `bash -c 'export AGENTERA_HOME=...; copilot -p ...'` invocation under this guard. PLAN.md Task 2 status flipped □ → ■.
**Commit**: 513356e feat(smoke): add scripts/smoke_live_hosts.py scaffold for codex+copilot verification
**Inspiration**: `scripts/smoke_setup_helpers.py` (Cycle 177, commit 1a558a7) is the direct shape twin — same `PASS:` / `FAIL:` to stdout, same try/finally cleanup, same fail-fast contract, same single-file standalone runner. The probe-then-snapshot ordering (snapshot only after the runtime is confirmed healthy) borrows from `setup_codex.py` / `setup_copilot.py` (Cycles 175 / 176): both write only after the install root is verified against canonical entries, so the same "validate first, mutate second" discipline carries through. The `<absent>` snapshot semantics (record absence at snapshot time, unlink any file created during the run) borrows from the `tempfile.NamedTemporaryFile(delete=False)` + manual cleanup pattern that `smoke_setup_helpers.py` uses for its per-case tmp HOME dirs. The cost-gate-then-consent UX is unprecedented in this repo (no prior live-CLI-gated harness existed); the one-line consent shape was chosen so a piped/non-interactive run (CI) sees `EOFError` on `input()` and treats it as a decline, preventing accidental billing.
**Discovered**: Initial cut grouped records by `provenance.runtime` (the `runtime_status` shape uses runtime as a top-level metadata key), but `extract_all.py` records carry `runtime` as a top-level record key (lines 802, 824, 855, 878, 901, 920, 1164, 1183, 1210, 1237, 1256, 1552, 1615, 1641, 1671); first run reported `<unknown>=2151` instead of the per-runtime breakdown. Fixed by reading `record.get("runtime", "<unknown>")` directly. Counts now match the Cycle 181 audit exactly: `claude-code=1897, codex-cli=253, copilot-cli=1`, `253 codex-cli record(s)` carried to corpus, `21 validation error(s) from build_corpus(); 0 mention codex` — the unrelated `[claude-code-extract-duplicate-source-ids]` failure is reported at info level and does NOT block the harness per the AC1 directive. AC3 distinct-skip-message verified by monkeypatching `shutil.which` to return `None` for both binaries (got `SKIP: codex (not on PATH)` / `SKIP: copilot (not on PATH)`) and separately by monkeypatching `subprocess.run` to raise `TimeoutExpired` (got `SKIP: codex (binary present but`codex --version`timed out)`); both paths return `not-on-path` / `not-authed` cleanly without invoking the auth probe. Snapshot/restore framework verified end-to-end against a `tempfile.NamedTemporaryFile` (mutate → restore left the file byte-identical) and against an absent path (create during run → restore removed it). Both `codex` and `copilot` ARE on this host's PATH (`/bin/codex`, `/bin/copilot`), so the AC5 "no live CLI available" path could not be exercised end-to-end from real probes without invoking the model — the simulated `shutil.which → None` path covers the AC5 contract via the same code path. No surprises affecting plan scope.
**Verified**: Default mode (no flags): `python3 scripts/smoke_live_hosts.py` → `--- profilera Codex collection audit ---` / `audit: total records=2151 across runtimes=claude-code=1897, codex-cli=253, copilot-cli=1` / `audit: codex-cli available; corpus carries 253 codex-cli record(s)` / `audit: 21 validation error(s) from build_corpus(); 0 mention codex` / `--- setup helpers smoke (delegated) ---` / `PASS: all smoke checks passed` / `PASS: all smoke checks passed`, exit 0 (AC1 satisfied). `--live` mode with declined consent: `echo n | python3 scripts/smoke_live_hosts.py --live` runs the offline sections then prints `--- live mode: cost gate ---` / `Estimated cost: $0.20-1.00 across two model calls (one per runtime; --live mode only)` / `Proceed with live CLI invocations (codex exec + copilot -p --allow-all-tools)? [y/N]:` / `aborted: consent declined; no live CLI invoked`, exit 1 with no subprocess invocation against `codex` or `copilot` (AC2 satisfied; cost line and one-line consent prompt printed BEFORE any CLI subprocess). AC3 verified via in-process simulation: `shutil.which → None` produces `SKIP: codex (not on PATH)` and `SKIP: copilot (not on PATH)` and returns `not-on-path` from `probe_runtime` without subprocess invocation; `subprocess.run` raising `TimeoutExpired` produces `SKIP: codex (binary present but`codex --version`timed out)` and returns `not-authed` — distinct messages for distinct failure modes. AC4 verified: `SnapshotRegistry.snapshot()` of an existing tmpfile produced `/tmp/agentera-smoke-live-tmpq1a1t8wu.toml-20260426-161520-7f01fdb94820.bak`, `restore_all()` after mutation restored byte-identical original content; `snapshot()` of an absent path recorded the absence and `restore_all()` after creation removed the file. AC5 path verified via the same simulation as AC3: with both `which` calls returning `None`, the per-runtime sections add to `skips` and never invoke a subprocess; the main loop's `if skips:` branch prints the summary and returns 0 — the harness exits cleanly with both sections enumerated. AC6 satisfied: `tests/` directory unchanged, no new pytest cases for the harness body. Validators all green at post-T1 baseline: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.40s` (unchanged; T2 adds zero pytest cases per AC6 — the harness IS the test surface). Pre-commit hooks ran clean: contracts (0.03s), docs (0.11s), spec-lint (0.09s).
**Next**: Task 3 (Codex live `codex exec` AGENTERA_HOME + `compact_artifact.py` combined-prompt exercise) and Task 4 (Copilot `bash -c 'export AGENTERA_HOME=...; copilot -p ... --allow-all-tools'` exercise) are unblocked since both depend only on Task 2. Both will plug into the snapshot guard wired here: Task 3 snapshots `~/.codex/config.toml` BEFORE invoking `setup_codex.py --config-file` against a tmp install root, runs the `codex exec` invocation, parses the AGENTERA_HOME echo and the fixture compaction outcome, then the top-level `finally` restores the original config from the tmp backup. Task 4 mirrors the same shape against `~/.bashrc` (or whichever rc file the harness's shell-detection logic picks). Task 5 (README + DOCS surface) and Task 6 (1.21.0 → 1.22.0 bump) follow once Tasks 3-4 land. The `[claude-code-extract-duplicate-source-ids]` TODO from Cycle 181 remains independent and the harness's `0 mention codex` line proves the failure is not a Codex regression.
**Context**: intent (build the live-host harness scaffold so Tasks 3-4 can plug in their CLI invocations without re-shaping the framework, while exercising the offline gates — Codex collection audit + delegated setup helpers smoke — under a single operator-facing entrypoint) · constraints (stdlib only per CLAUDE.md scripts convention; no live CLI invocations in this task — Task 2 must wire the probe/cost-gate/snapshot UX without spending model budget; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field per SPEC Section 20 with end-to-end harness output across both default and `--live`-with-declined-consent modes plus simulated AC3/AC4/AC5 paths and validator outputs as supporting evidence — no N/A allowlist tag because the harness is functional code; do NOT introduce any actual `codex exec` or `copilot -p` invocations per Task 3/4 scope, do NOT touch README/DOCS per Task 5 scope, do NOT bump version per Task 6 scope; tests/ stays untouched per AC6 — the harness IS the test surface) · unknowns (whether the in-process `build_corpus()` call would surface a different runtime-key shape than `extract_all.py`'s extractors expect — ruled out by reading the runtime-key sites in extract_all.py and matching the top-level `record.runtime` field directly; whether a piped non-interactive `--live` invocation would hang on `input()` — ruled out by catching `EOFError` and treating it as a decline, which lets CI dry-probe the cost gate without a TTY) · scope (`scripts/smoke_live_hosts.py`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 181 · 2026-04-26 18:30 · chore(audit): profilera Codex collection verification and metadata reconciliation

**Phase**: audit
**What**: Task 1 of the Live-Host Verification plan. Verified profilera's Codex collection works end-to-end against the live `~/.codex/history.jsonl` (252 entries) and `~/.codex/config.toml`, then reconciled `.codex-plugin/plugin.json` `skillMetadata[profilera].requiredCapabilities[codex_session_corpus].status` from `degraded` to `ok`. The audit ran `extract_all.build_corpus()` directly and inspected the in-memory corpus: codex-cli contributes 253 records (252 history_prompt + 1 project_config_signal), codex-cli appears in `metadata.runtimes` alongside claude-code and copilot-cli, the two unsupported families (`instruction_document` with documented "no surface" reason, `conversation_turn` against an empty `~/.codex/sessions`) are correctly reported as `missing` in `runtime_status`, and no codex-cli record contributes to the duplicate-source_id failure. Mirrored the status flip in `agents/openai.yaml` and `skills/profilera/agents/openai.yaml`. Updated companion limitation prose so "degrades by reporting missing source families" / "reported as degraded metadata" become "reports any absent source families" / "reported in corpus metadata" — the original "report missing families in metadata" promise is fulfilled by the verified extractor behavior, so the language now matches reality without reusing the word "degraded" as a status. Relaxed `scripts/validate_lifecycle_adapters.py` to accept `status` of either `ok` or `degraded` (back-compat preserved) and to require an explicit `status:` declaration in the YAML surfaces rather than the literal `status: degraded` substring. Mirrored the same loosened check in `tests/test_runtime_adapters.py` for both the `.codex-plugin/plugin.json` capability and the `agents/openai.yaml` UI metadata. Filed `[claude-code-extract-duplicate-source-ids]` as a Normal TODO with the named root cause (the source_id key for `_records_from_conversations` omits the source jsonl path, so resumed conversations across multiple project files collide). PLAN.md Task 1 status flipped □ → ■; Task 1 Surprise logged with the runtime-by-runtime grouping evidence.
**Commit**: 3b9af17 chore(audit): profilera Codex collection verification and metadata reconciliation
**Inspiration**: Live-Host Verification PLAN.md Task 1 acceptance criteria. The audit-by-grouping pattern (run extractor → group failing records by `(runtime, source_kind)` → name the contributor) mirrors the inspektera evidence-based-finding shape: a confidence-bearing claim must point at a specific record set rather than a generic "extractor fails" observation. The `degraded → ok` decision frame (audit confirms extraction works → flip status; preserved limitation prose for missing surfaces stays accurate) follows AC2 literal wording.
**Discovered**: All 21 duplicate-source_id failures are from `claude-code/conversation_turn` (39 records contributing to 18 duplicate groups, including three triple-duplicates at source_ids `6a2a74ca34ef0cd9`, `d1c9ea106ef41cbb`, and `c6932dfa2e6d7a23`); zero codex-cli records contribute. Root cause in `_records_from_conversations` (extract_all.py line 885): the source_id key is `(runtime, source_kind, str(timestamp), session_id, response_hash)` — when the same `(timestamp, session_id, user_response)` tuple appears in multiple `~/.claude/projects/*.jsonl` files (resumed conversations or session export overlap), the same source_id is generated. Fix path: thread the source jsonl path through `_process_conversation` and `extract_conversations` into the entry dict so `_records_from_conversations` can include `str(jsonl_path)` in the key parts. Out of scope per Task 1 constraints (claude-code is not Codex); filed as TODO. Independent of Task 1: the `.codex-plugin/plugin.json` `codex.limitations` text "profilera collects bounded Codex session corpus data when present and degrades by reporting missing source families in corpus metadata" used the word "degrades" which now would conflict with the `ok` status; rephrased to "reports any absent source families" so the overall metadata story stays internally consistent.
**Verified**: Extractor output before metadata change reproduced 21 ERROR lines (`duplicate source_id` for source_ids enumerated in the discovered field above) and `Aborted in 3.6s. corpus.json NOT written due to 21 validation error(s).` Direct `build_corpus()` inspection: total records 2151 across runtimes claude-code (1897), codex-cli (253), copilot-cli (1); duplicate-source_id groups all map to (claude-code, conversation_turn) with 39 contributing records; codex-cli families resolve to history_prompt count 252 status ok, project_config_signal count 1 status ok, instruction_document count 0 status missing reason "no documented Codex CLI local instruction source family surface", conversation_turn count 0 status missing checked_surface `~/.codex/sessions`. After metadata change: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.41s` (unchanged from post-Audit-15 baseline; AC5 satisfied — verification-only audit added zero new pytest cases, only loosened existing lifecycle assertions). Pre-commit hooks ran clean: configs (0.07s), contracts (0.03s), docs (0.12s), spec-lint (0.09s).
**Next**: Task 2 of the Live-Host Verification plan (`scripts/smoke_live_hosts.py` scaffold with default-mode delegation to `scripts/smoke_setup_helpers.py` plus the Task 1 audit path, and `--live` mode probe + cost gate + snapshot/restore framework with no actual CLI invocations) is now unblocked since Task 1 has no dependents inside the plan and Task 2 has `Depends on: none`. Tasks 3 (Codex live `codex exec` exercise) and 4 (Copilot `bash -c 'export ...'` exercise) depend on Task 2. Task 5 (docs surface for the harness + manual verification one-liners) depends on T1-T4. Task 6 (version bump 1.21.0 → 1.22.0) depends on the rest. The new `[claude-code-extract-duplicate-source-ids]` TODO entry tracks the unrelated extractor fix that should land before any user actually depends on `~/.local/share/agentera/intermediate/corpus.json` being writable on a host with substantial Claude Code conversation history.
**Context**: intent (verify whether the live Codex collection actually produces records that would land in corpus.json end-to-end and reconcile the `degraded` status placeholder against observed reality, while honestly handling the duplicate-source_id failure that blocks the actual file write — including refusing to use it as cover for keeping `degraded` if the cause turns out to be elsewhere) · constraints (no extract_all.py logic changes per Task 1 constraint; only metadata claims and one targeted lifecycle assertion may evolve; if Codex turned out to be the duplicate cause a small dedup patch was permitted, but root-cause analysis showed claude-code as the contributor so the patch path was forbidden and a TODO follow-up filed instead; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field per SPEC Section 20 with extractor failure reproduction, build_corpus() runtime-by-family grouping, and post-change validator outputs as supporting evidence — no N/A allowlist tag because the changes touch functional metadata that downstream agents consume; do NOT introduce any smoke-harness changes per Task 2 scope, do NOT invoke live Codex/Copilot CLIs per Task 3/4 scope, do NOT write README/DOCS surface per Task 5 scope, do NOT bump version per Task 6 scope) · unknowns (whether the action-text rephrase "Codex history, session, and config surfaces land in the Section 22 corpus when present" is too forward-looking given that absent-surface families would still report missing in `runtime_status` — judged acceptable because the rephrased text uses "land ... when present" which precisely matches the observed behavior and the absent-surface reporting promise is preserved verbatim in the trailing clause) · scope (`.codex-plugin/plugin.json`, `agents/openai.yaml`, `skills/profilera/agents/openai.yaml`, `scripts/validate_lifecycle_adapters.py`, `tests/test_runtime_adapters.py`, `TODO.md`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 180 · 2026-04-26 18:00 · chore(plan): freshness checkpoint for Codex+Copilot Completion

**Phase**: checkpoint
**What**: Plan-level summary cycle closing the Codex+Copilot Completion plan. Cycles 175-179 carry the per-task detail (T1 `scripts/setup_codex.py` at f82a776; T2 `scripts/setup_copilot.py` at 058ebc6; T3 `scripts/smoke_setup_helpers.py` at 1a558a7; T4 README install lead-in plus DOCS.md Index/Coverage refresh resolving Audit 14 finding 1 at 8e57ef4; T5 1.20.0 -> 1.21.0 release at 9773f35); this entry summarizes the plan-level outcome without restating that detail. The five cycles trace the same one-command-setup story end-to-end: Cycle 171's manual `[shell_environment_policy]` and `echo 'export AGENTERA_HOME=...' >> ~/.bashrc` snippets became `python3 scripts/setup_codex.py` and `python3 scripts/setup_copilot.py`, both stdlib-only, both idempotent, both dry-runnable, both verifying the install root against four canonical sibling entries before any write; a single `python3 scripts/smoke_setup_helpers.py` exercises every realistic state of both helpers across 11 sequential black-box cases without requiring live Codex or Copilot CLIs; README leads each runtime's install section with the helper invocation while the manual snippet stays as the alternative; DOCS.md Index gains rows for all three scripts and the Audit 14 staleness rows are reset to actual most-recent commit dates and current test counts; the suite shipped at 1.21.0 with two Added entries (Codex helper, Copilot helper) plus the smoke harness and one Changed entry (README install lead-in + DOCS Index/Coverage refresh) per `feat = minor` policy. PLAN.md Task 6 marked ■ complete; TODO.md `[codex-setup-helper]` and `[copilot-setup-helper]` items moved to Resolved citing this plan and its commits. Plan is ready for orkestrera to archive.
**Commit**: faf9c04 chore(plan): freshness checkpoint for Codex+Copilot Completion
**Inspiration**: SPEC Section 20 N/A allowlist (`docs-only`) and the Cycle 174 / 168 / 161 / 151 / 142 / 134 / 118 plan-level checkpoint pattern. No external pattern needed.
**Discovered**: CHANGELOG 1.21.0 release block was already coherent post-T5: three `Added` entries (`scripts/setup_codex.py`, `scripts/setup_copilot.py`, `scripts/smoke_setup_helpers.py`) and two `Changed` entries (README Codex/Copilot install lead-ins, DOCS.md Index dates and Coverage refresh) trace the plan's two helper feats, the smoke-harness test surface, and the Task 4 docs surface without duplication, validating Cycle 179's promotion choice. Audit 14's Freshness warning (DOCS.md Index dates pre-2026-04-26 plus the stale "359 tests across 13 files" Coverage line) was structurally resolved by T4 / commit 8e57ef4 — the Audit Log entry under "2026-04-26 (Codex+Copilot Setup Helpers Task 4)" records both staleness rows as `(fixed)`, so the next inspektera audit will see the resolution without a separate carry-forward note. PLAN.md Surprises is empty for this plan (no logged divergences across Cycles 175-179).
**Verified**: N/A: docs-only. Changes are PLAN.md Task 6 status flip (□ → ■), two TODO.md `[topic]`-prefixed Normal items moved to Resolved with citations, and this PROGRESS.md plan-level entry. No code, scripts, tests, or version metadata touched. Validators stay green at the post-T5 Cycle 179 baseline: `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` -> `All contract files are current (12 checked)`. `python3 -m pytest -q` -> `433 passed in 0.40s` (no test count change; T6 touched no code).
**Next**: Orkestrera (or the next session) archives PLAN.md to `.agentera/archive/PLAN-2026-04-26-codex-copilot-completion.md` and resumes vision-driven work selection. Open follow-ups land in TODO.md Normal: `[copilot-marketplace]` (deferred publication of a canonical Agentera Copilot marketplace source) and `[opencode-session-events]` (SESSION.md bookmark replacement plus session-start context preload via OpenCode's real `event` mechanism). Audit 14's two info findings (PROFILERA_PROFILE_DIR vs AGENTERA_HOME injection asymmetry; Codex/Copilot live-host coverage gap) remain explicit caveats and surface candidates for the next audit. The marketplace.json profilera-without-version-key convention noted across multiple bumps remains an open normalization question worth deciding before the next minor bump.
**Context**: intent (close Codex+Copilot Completion plan with one aggregate cycle entry summarizing the one-command-setup story end-to-end, not per-cycle restatements; move the two deferred TODO follow-ups to Resolved with citations to this plan and its commits; confirm CHANGELOG 1.21.0 represents the plan coherently and HEALTH.md Audit 14 finding 1 is resolved) · constraints (no script/test/version changes; CHANGELOG already coherent so leave it untouched; one conventional commit; no push; do not archive PLAN.md — orkestrera handles archival; mandatory `**Verified**` field with `N/A: docs-only` allowlist tag plus validator outputs as supporting evidence; T6 must not change validator/lifecycle/contract/pytest counts from the post-T5 baseline) · unknowns (none) · scope (`.agentera/PLAN.md`, `TODO.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 179 · 2026-04-26 17:30 · chore(release): bump suite to 1.21.0

**Phase**: release
**What**: Task 5 of the Codex+Copilot Completion plan. Bumped suite metadata from 1.20.0 to 1.21.0 across every file in DOCS.md `version_files` (Copilot root `plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js` `AGENTERA_VERSION` constant, `registry.json` 12 entries, `.claude-plugin/marketplace.json` 12 versions, and 12 `skills/*/.claude-plugin/plugin.json` files; profilera entry in `.claude-plugin/marketplace.json` deliberately preserved without a `version` key per the long-standing convention). Promoted CHANGELOG `## [Unreleased]` to `## [1.21.0] · 2026-04-26` (empty Unreleased block left at top); the release block lists the Codex setup helper feat (T1 `scripts/setup_codex.py`), the Copilot setup helper feat (T2 `scripts/setup_copilot.py`), and the smoke harness (T3 `scripts/smoke_setup_helpers.py`) under Added, plus the README install-instruction lead-in change and the DOCS.md Index/Coverage refresh (T4) under Changed. PLAN.md Task 5 status flipped □ → ■. Per `feat = minor` policy, two unreleased feats trigger one minor bump. The Cycle 173 test refactor (`_read_opencode_agentera_version` reads AGENTERA_VERSION from agentera.js at test time) means no test-fixture edits were required; the version-drift test continues to read the live constant.
**Commit**: 9773f35 chore(release): bump suite to 1.21.0
**Inspiration**: DOCS.md `versioning.semver_policy` (`feat = minor`) and the Cycle 173 / 167 / 124 / 116 / 150 version-bump pattern. Cycle 173's structural close of the hardcoded-version-literal Surprise (commit ed1b516) means this bump required zero test changes — the AGENTERA_VERSION reader path now picks up the live constant, validating the refactor's purpose.
**Discovered**: The marketplace.json profilera entry remained without a version key as expected; the bump count surfaces lined up exactly (1 each in three standalone plugin.json files, 12 in registry.json, 12 in .claude-plugin/marketplace.json with profilera preserved key-less, 1 in each of 12 skills/*/.claude-plugin/plugin.json, 1 in .opencode/plugins/agentera.js). The Cycle 173 prediction held: future bumps will not require any test-file edits because `_read_opencode_agentera_version` reads the live constant from agentera.js. No new surprises this cycle.
**Verified**: N/A: chore-build-config. `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` -> `All contract files are current (12 checked)`. `python3 -m pytest -q` -> `433 passed in 0.39s` (unchanged from post-T4 Cycle 178 baseline; Task 5 is chore-build-config and adds zero tests). `grep -c '"version": "1.20.0"'` across all version_files returns 0 in every file (plugin.json, .github/plugin/plugin.json, .codex-plugin/plugin.json, .claude-plugin/marketplace.json, registry.json, .opencode/plugins/agentera.js, all 12 skills/*/.claude-plugin/plugin.json). Counts at 1.21.0 confirm the bump landed in every listed file: 1 in each standalone plugin.json (3 files), 12 in registry.json, 12 in .claude-plugin/marketplace.json (1 top-level + 11 inner; profilera entry preserved with no version key), 1 in each skills/*/.claude-plugin/plugin.json (12 files), 1 in .opencode/plugins/agentera.js (`AGENTERA_VERSION = "1.21.0"`).
**Next**: Task 6 (plan-level freshness checkpoint) closes the Codex+Copilot Completion plan with one aggregate cycle summary, marks Tasks 1-5 ■ complete in PLAN.md, files the `[codex-setup-helper]` and `[copilot-setup-helper]` items as Resolved entries in TODO.md citing this plan and its commits, and confirms HEALTH.md Audit 14 Freshness warning is resolved or carried forward in the next audit. The plan's Overall Acceptance criteria can then be verified end-to-end.
**Context**: intent (apply DOCS.md feat=minor policy for two unreleased feats from Tasks 1 and 2 in one consistent bump and ship the 1.21.0 release that surfaces the Codex and Copilot setup helpers) · constraints (every file in version_files, no others; CHANGELOG promoted without inventing entries beyond what Tasks 1-4 shipped; PLAN Task 5 marked ■ complete after substantive commit; validators stay green at the 433-test post-T4 baseline; one conventional commit; no push; SHA-pin via PROGRESS follow-up; `**Verified**` field with `N/A: chore-build-config` allowlist tag plus grep counts and validator outputs as supporting evidence; do not introduce a `version` key on the marketplace.json profilera entry — preserve the long-standing convention) · unknowns (none) · scope (`plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.opencode/plugins/agentera.js`, `registry.json`, `skills/*/.claude-plugin/plugin.json`, `CHANGELOG.md`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 178 · 2026-04-26 17:00 · docs(install): surface setup helpers in README and refresh DOCS.md Index

**Phase**: docs
**What**: Task 4 of the Codex+Copilot Completion plan. Three coupled doc surfaces landed atomically. README Codex setup section now leads with `python3 scripts/setup_codex.py` as the recommended path (with one short paragraph explaining what the helper does — stdlib `tomllib` read, byte-identical preservation of unrelated tables, no-op when correct, refusal on conflicting siblings without `--force`, dry-run preview, install-root verification against the four canonical entries) and keeps the `[shell_environment_policy]` TOML snippet behind an "if you prefer a manual edit" header as the alternative. README Copilot setup section mirrors the same shape: `python3 scripts/setup_copilot.py` recommended path with shell-detection, marker-block, dry-run, and `--rc-file PATH` notes; the manual `echo 'export AGENTERA_HOME=...' >> ~/.bashrc` snippet retained as the alternative. README Scripts section gained three new rows (`scripts/setup_codex.py`, `scripts/setup_copilot.py`, `scripts/smoke_setup_helpers.py`) alongside the existing `validate_spec.py`, `eval_skills.py`, and `usage_stats.py` entries; each new row carries flags and exit-code semantics matching the helper's argparse output. DOCS.md Index gained three rows for the same scripts at 2026-04-26. Audit 14 finding 1 resolved: Index dates for Progress, TODO, Changelog, Health, Plan, and DOCS bumped from their pre-2026-04-26 values to the actual most-recent commit dates (all 2026-04-26 confirmed via `git log -1 --format=%aI -- <path>`); Coverage line corrected from "359 tests across 13 files" to "433 tests across 17 files" (412 post-1.20.0 baseline + 10 Codex helper tests + 11 Copilot helper tests, verified via `pytest --collect-only -q | tail` and `ls tests/*.py | wc -l`). DOCS.md Audit Log entry added naming all five resolutions with severity and (fixed) status. PLAN.md Task 4 status flipped □ → ■.
**Commit**: 8e57ef4 docs(install): surface setup helpers in README and refresh DOCS.md Index
**Inspiration**: SPEC Section 12 visual identity tokens (■ current status marker reused for the new Index rows). The "recommended path / if you prefer a manual edit" framing is borrowed from the existing README OpenCode hook setup block which leads with the curl one-liner and treats hand-installation as the fallback. The Audit Log entry shape mirrors the prior 2026-04-26 (Cross-Runtime Portability Task 3) and (Suite Usage Analytics Task 4) entries: `· severity (status)` suffix per finding, grouped by audit cycle.
**Discovered**: The Audit 14 brief specified test count 412 across 14, but the post-T2 baseline is now 433 across 17 (412 + 10 from `tests/test_setup_codex.py` + 11 from `tests/test_setup_copilot.py`; T3 added zero pytest cases per the proportionality note "the harness IS the test"). The brief's "433" hint matched reality. Test file count: `find tests -maxdepth 1 -name '*.py'` returns 17 files. The Cycle 177 PROGRESS entry already noted DOCS.md was last touched 2026-04-26 by T1 (Plan row date bump in commit f82a776); this cycle's edit is the second same-day change to DOCS.md, so the Index "DOCS" row date stays at 2026-04-26. No surprises affecting plan scope.
**Verified**: N/A: docs-only. `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` -> `All contract files are current (12 checked)`. `python3 -m pytest -q` -> `433 passed in 0.40s` (unchanged from post-T3 Cycle 177 baseline; Task 4 is documentation only). Pre-commit hooks ran clean: contracts (0.03s), docs (0.17s), spec-lint (0.09s). SPEC.md byte-identical: `git diff --stat SPEC.md` shows no change (acceptance criterion 7 satisfied).
**Next**: Task 5 (1.20.0 -> 1.21.0 bump across every file in DOCS.md `version_files` plus CHANGELOG promotion with one Added entry per helper) is now unblocked; T1, T2, T3, and T4 are all complete. Task 6 closes the plan with a freshness checkpoint, the `[codex-setup-helper]` and `[copilot-setup-helper]` TODO Resolved entries, and the HEALTH.md Audit 14 resolution note.
**Context**: intent (lead the Codex and Copilot install sections with the new helpers as the recommended path so users on those runtimes get a one-command setup matching the OpenCode UX bar; resolve Audit 14 finding 1 in the same task so the Index dates and Coverage line stop carrying stale values; surface the helpers in the Scripts table so they are discoverable from the repo-level utilities catalog) · constraints (documentation + DOCS metadata only — NO scripts, NO hooks, NO version files, NO SKILL.md changes, NO version bump per Task 5 scope, NO freshness checkpoint per Task 6 scope; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with `N/A: docs-only` allowlist tag plus validator outputs as supporting evidence; SPEC.md byte-identical per acceptance criterion 7; Index dates derived from `git log -1 --format=%aI -- <path>` per task brief) · unknowns (none — Audit 14's freshness rows had explicit guidance and the helpers' descriptions trace to their argparse output) · scope (`README.md`, `.agentera/DOCS.md`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 177 · 2026-04-26 16:30 · test(smoke): add scripts/smoke_setup_helpers.py for codex and copilot helpers

**Phase**: build
**What**: Task 3 of the Codex+Copilot Completion plan. Shipped `scripts/smoke_setup_helpers.py`, a Python smoke harness mirroring the `scripts/smoke_opencode_bootstrap.mjs` shape (temp dir setup, env-var snapshot/restore in try/finally, sequential numbered cases with fail-fast, `PASS:` / `FAIL:` output protocol, exit 0 / exit 1) but in Python because both helpers under test (`setup_codex.py`, `setup_copilot.py`) are Python. The harness exercises both helpers as black boxes via `subprocess` rather than importing internals — this matches what a real user observes (stdout/stderr text, exit codes, file contents, byte-identity of unrelated lines) and survives helper refactors. 11 sequential cases total, within the 10-12 AC6 budget: Codex helper (5: fresh-write, idempotent re-run with byte-identity check, sibling-section preservation under existing `[shell_environment_policy]` header without `set`, conflict refusal without `--force`, dry-run on absent config exits 1 without creating file); Copilot helper (4: bash → `~/.bashrc` with `export` syntax, zsh → `~/.zshrc`, fish → `~/.config/fish/config.fish` with `set -x` syntax including parent-dir auto-creation, unsupported csh shell exits 2 with `--rc-file` escape-hatch guidance); Cross-cutting (2: both helpers reject a bogus `--install-root` with named missing canonical entries from the shared T1/T2 contract; both helpers' `--dry-run` on an idempotent target exits 0 — verifies the validate_spec.py-style "no change → exit 0, change pending → exit 1" gate respects no-op). Each case constructs a fresh tmp `HOME` subdirectory and passes a minimal env (PATH + HOME + optional SHELL) to the subprocess via `env=`, deliberately omitting `AGENTERA_HOME` and `CLAUDE_PLUGIN_ROOT` so the helper's auto-detect walk-up resolves from the script location (live repo root) rather than from a leaked env var. The top-level `finally` restores the four env vars the harness might mutate (`HOME`, `SHELL`, `AGENTERA_HOME`, `CLAUDE_PLUGIN_ROOT`) and removes the smoke tempdir. PLAN.md Task 3 status flipped □ → ■.
**Commit**: 1a558a7 test(smoke): add scripts/smoke_setup_helpers.py for codex and copilot helpers
**Inspiration**: `scripts/smoke_opencode_bootstrap.mjs` (Cycle 170, commit 7299775) is the direct shape twin — same try/finally env restore pattern, same sequential numbered cases with fail-fast, same `PASS:` / `FAIL:` output protocol, same single-file standalone runner. The Python language choice is dictated by the helpers under test (both are stdlib-only Python per Cycles 175 and 176); using subprocess to invoke the helpers as black boxes mirrors the smoke_opencode_bootstrap.mjs approach of importing the plugin and exercising its public surface (the helpers' "public surface" is their CLI, hence subprocess). The 11-case budget (5 + 4 + 2) tracks AC6's 10-12 cap and the implicit per-helper coverage breakdown (4 Codex acceptance scenarios + 4 Copilot acceptance scenarios from AC1-2, with one Codex extra for the conflict-refusal branch and two cross-cutting cases shared by both helpers). Output protocol anchored on stdout (not stderr like the .mjs sends FAIL to stderr) per AC4's literal "stdout ends with FAIL" wording.
**Discovered**: The Codex helper's auto-detect walk-up correctly resolves from the live agentera repo when invoked from any path under it; this means the harness gets the right install root without needing to pass `--install-root` explicitly in most cases, which kept the case bodies short. The conflict-refusal case in Test 4 became the natural fifth Codex case because it covers the one acceptance scenario (sibling keys without AGENTERA_HOME) that has no analog in the Copilot helper (Copilot's marker comment isolates ownership). The "both helpers' dry-run on idempotent target exits 0" cross-cutting case (Test 11) exercises a subtle gate: validate_spec.py's convention is "exit 0 when no change is needed, exit 1 when a change would occur"; verifying this on a no-op target prevents a regression where dry-run might unconditionally exit 1. No surprises affecting plan scope; the harness is itself a test surface so it carries no separate pytest unit tests (the harness IS the test, per the task brief's note on test proportionality). PLAN.md Surprises unchanged.
**Verified**: End-to-end smoke run: `python3 scripts/smoke_setup_helpers.py` → `PASS: all smoke checks passed`, `EXIT=0` (all 11 sequential cases pass; tempdir cleaned up; env vars restored). FAIL-path sanity: invoked `s.fail('synthetic test reason')` directly → printed `FAIL: synthetic test reason` to stdout and `SystemExit(1)`, confirming AC4 protocol holds (stdout ends with FAIL, exit 1). Validators all green: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.39s` (unchanged from post-T2 Cycle 176 baseline; Task 3 adds no pytest cases per the proportionality note — the harness IS the test surface). Pre-commit hooks ran clean: contracts (0.03s), docs (0.12s), spec-lint (0.09s).
**Next**: Task 4 (README + DOCS surface for both helpers + the smoke runner; Audit 14 Freshness fix to refresh DOCS Index dates and the Coverage line) is now unblocked since T1, T2, T3 are all complete. Task 5 bumps suite to 1.21.0 across every file in DOCS.md `version_files`. Task 6 closes the plan with a freshness checkpoint, TODO follow-ups for any deferred items, and confirms HEALTH.md Audit 14 resolution.
**Context**: intent (provide a single-command `python3 scripts/smoke_setup_helpers.py` that exercises every realistic state of both setup helpers without requiring live Codex or Copilot CLIs, mirroring the `smoke_opencode_bootstrap.mjs` UX so users have a uniform "is this thing wired correctly?" check across all three helper-bearing runtimes) · constraints (stdlib only per CLAUDE.md scripts convention; subprocess-based black-box exercise rather than importing helper internals so the harness survives refactors; temp dirs and env-var restore in finally per AC5; 10-12 case budget per AC6 with explicit case enumeration in the docstring; one conventional commit; no push; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with smoke output, FAIL-path sanity, validator outputs, and pytest output as supporting evidence — no N/A allowlist tag because test infra is functional code per the task brief; no pytest unit tests for the harness itself per the proportionality note "the harness IS the test") · unknowns (whether the FAIL output protocol should send the line to stdout or stderr — `smoke_opencode_bootstrap.mjs` sends FAIL to stderr while AC4 anchors on stdout; harness sends to stdout per the literal AC wording so the protocol is observable via stdout-redirect alone) · scope (`scripts/smoke_setup_helpers.py`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 176 · 2026-04-26 16:00 · feat(setup): add scripts/setup_copilot.py for AGENTERA_HOME shell-rc injection

**Phase**: build
**What**: Task 2 of the Codex+Copilot Completion plan. Shipped `scripts/setup_copilot.py`, a stdlib-only helper that idempotently writes `export AGENTERA_HOME=<install root>` (or fish's `set -x AGENTERA_HOME ...`) into the user's shell rc so Copilot CLI inherits the install root from the surrounding shell. Mirrors the Cycle 175 `setup_codex.py` shape: install-root verification against the four canonical sibling entries (`scripts/validate_spec.py`, `hooks/`, `skills/`, `SPEC.md`), `--install-root PATH` override, `--rc-file PATH` override, `--dry-run`, env-var fallbacks (`AGENTERA_HOME`, `CLAUDE_PLUGIN_ROOT`), explicit exit codes (0 success/no-op, 1 dry-run-with-pending-change, 2 error). Three real shell branches drive the rc-target resolution: bash (`$SHELL` ends in `bash`) → `~/.bashrc`; zsh → `~/.zshrc`; fish → `~/.config/fish/config.fish` with `set -x` syntax. Fourth branch handles unsupported shells (csh, tcsh, etc.) by printing the detected shell name plus a bash one-liner the user can adapt and pointing at `--rc-file` as the escape hatch; exits 2. Idempotency anchors on a literal marker comment (`# agentera: AGENTERA_HOME (managed)`) followed by the export line: re-runs find the marker, compare the next line, no-op on match, in-place rewrite on mismatch with every other rc line preserved byte-identically. Bare user-written `AGENTERA_HOME` mentions (no marker comment) trigger a notice telling the user we left their hand-written line alone and our managed block was appended below it. The resolved rc target prints to stdout *before* any write so the operator can Ctrl-C if detection went sideways (critic-driven design tightening per PLAN.md). `--rc-file PATH` infers syntax from path: `.fish` extension or any `fish/` segment → fish syntax, otherwise export. Added `tests/test_setup_copilot.py` with 11 cases (under the 12-case AC10 budget): four shell-detection branches (bash/zsh/fish/unsupported), three rc-state branches (marker-at-desired no-op, marker-at-different-value rewrites in place, bare-export-without-marker triggers notice), `--rc-file` syntax inference (fish vs export), `--dry-run` on pending exits 1, `--dry-run` on no-op exits 0, and install-root validation (shared T1 contract). Tests use a `fake_home` fixture that monkeypatches `$HOME` to `tmp_path` so no real `~/.bashrc` is ever touched. PLAN.md Task 2 status flipped □ → ■.
**Commit**: 058ebc6 feat(setup): add scripts/setup_copilot.py for AGENTERA_HOME shell-rc injection
**Inspiration**: SPEC Section 7 AGENTERA_HOME contract (Cycle 169 / commit f1867bf) defined the install-root primitive and Copilot's row in the per-runtime mechanism table (shell rc export, since Copilot has no plugin-level env-injection API). Cycle 175's `setup_codex.py` (commit f82a776) is the direct shape twin — same install-root verification, same auto-detect walk-up, same env-var fallbacks, same `--dry-run` exit-1-on-change convention from `validate_spec.py` and `generate_contracts.py --check`. The marker-comment idempotency anchor pattern is borrowed from common shell-config managers (fzf install, conda init, asdf hook) which all use a literal comment line as the find-and-rewrite anchor rather than substring-grepping the export line itself.
**Discovered**: The "bare export without marker" edge case (AC7) needed careful framing: the helper must *notice* a user-written `AGENTERA_HOME` line so it can warn ("we left your line untouched") but must never *modify* it. Resolved via permissive substring match (`MANAGED_KEY in line`) over lines outside the managed two-line block, paired with a no-modification path that simply appends the managed marker block below. The user owns their bare line; we own ours; both coexist. Path-based syntax inference for `--rc-file` was an interpretation call: the AC8 contract reads "syntax matching extension/path convention (`.fish` → fish syntax, otherwise export)". I implemented this as suffix-match on `.fish` plus a `fish/` parts check (so `~/.config/fish/config.fish` works even if a user typo'd the extension). The fish-syntax test asserts both halves of the inference rule (a `.fish` path with bash `$SHELL` produces fish; a `.rc` path with zsh `$SHELL` produces export) within a single test case to stay under the 12-case budget. No surprises affecting plan scope; logging via PLAN.md left to the plan checkpoint at Task 6.
**Verified**: End-to-end smoke against `mktemp -d` rc targets across all four shell branches plus three rc-state branches plus the dry-run gate: Smoke 1 (bash, fresh) wrote `# agentera: AGENTERA_HOME (managed)\nexport AGENTERA_HOME="$HOME/git/agentera"\n` to `<tmp>/.bashrc` and exited 0 with target-resolution print. Smoke 2 (bash, idempotent re-run) printed `AGENTERA_HOME marker block already at $HOME/git/agentera; nothing to do` and exited 0. Smoke 3 (zsh, fresh) wrote the same block to `<tmp>/.zshrc` (bashrc untouched in same tmp dir). Smoke 4 (fish, fresh) wrote `set -x AGENTERA_HOME "$HOME/git/agentera"` to `<tmp>/.config/fish/config.fish` (parent dir auto-created). Smoke 5 (csh, unsupported) exited 2 with `unsupported shell: csh`, the bash one-liner template, and the `--rc-file` escape-hatch pointer. Smoke 6 (bare export without marker) appended the managed block below the user's `export AGENTERA_HOME="/old/manual/path"` line, exited 0, printed `notice: an existing AGENTERA_HOME mention was left untouched (no managed marker comment); your hand-written line is preserved and the new managed block was appended below it`, and `cat` confirmed both lines coexist. Smoke 7 (marker at different value with surrounding user lines) rewrote the managed export line in place, preserved `# user line A`, `alias ll="ls -la"`, `# user line B` byte-identically, kept the marker comment singleton (no duplication), and exited 0. Smoke 8 (dry-run on absent rc) exited 1 with the unified diff printed and the file never created. Validators all green: `python3 scripts/validate_spec.py` → `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` → `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` → `All contract files are current (12 checked)`. `python3 -m pytest -q` → `433 passed in 0.39s` (was 422 baseline at Cycle 175, +11 new from `tests/test_setup_copilot.py`).
**Next**: Task 3 (`scripts/smoke_setup_helpers.py` cross-helper smoke runner mirroring the `smoke_opencode_bootstrap.mjs` shape) is now unblocked since both Task 1 and Task 2 are complete. Task 4 (README + DOCS surface + Audit 14 Freshness fix) follows. Task 5 bumps 1.20.0 → 1.21.0. Task 6 closes the plan with a freshness checkpoint.
**Context**: intent (convert Cycle 171's manual `echo 'export AGENTERA_HOME=...' >> ~/.bashrc` snippet into a single idempotent command that handles every realistic shell + rc-state combination without sudo, third-party deps, or the user re-pasting on every install-root change) · constraints (stdlib only per CLAUDE.md scripts convention; idempotent and dry-runnable; install-root verified against canonical entries before any write; one conventional commit; no push; SHA-pin via PROGRESS follow-up; test budget capped at 12 cases per PLAN Task 2 AC10 with edge expansion bounded to 4 shell-detection branches plus 3 rc-state branches; mandatory `**Verified**` field per SPEC Section 20 with end-to-end smoke output across each branch plus pytest and validator outputs as supporting evidence — no N/A allowlist tag because the helper is functional code; resolved rc target must print before any write per the plan's critic-driven design tightening) · unknowns (whether real-user rc files in the wild ever carry the marker comment with a hand-edited export line that we then rewrite — the spec is "rewrite the line immediately following the marker", so even a hand-edited managed line gets re-pointed at the canonical install root, which matches the "managed" semantics the marker telegraphs) · scope (`scripts/setup_copilot.py`, `tests/test_setup_copilot.py`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 175 · 2026-04-26 15:30 · feat(setup): add scripts/setup_codex.py for AGENTERA_HOME injection

**Phase**: build
**What**: Task 1 of the Codex+Copilot Completion plan. Shipped `scripts/setup_codex.py`, a stdlib-only helper that idempotently writes `[shell_environment_policy].set.AGENTERA_HOME` into `~/.codex/config.toml` so Codex propagates the install root into every shell-tool subprocess. Replaces the manual TOML snippet that landed in Cycle 171 (commit b71da5a). Three structural write paths cover the realistic file states: fresh-write when the config is absent (or empty), a section-bounded line insert when `[shell_environment_policy]` exists without `set`, and a rewrite of the `set = { ... }` line under `--force` when sibling keys exist alongside (or in place of) AGENTERA_HOME. AGENTERA_HOME already at the desired value exits 0 byte-identical (idempotency); a value drift with no sibling keys is a safe in-place update without `--force`; sibling keys without AGENTERA_HOME refuse non-zero with a printed diff and write nothing. `--dry-run` exits 1 on a pending change, 0 on no-op (mirrors validate_spec.py's drift convention). `--install-root PATH` overrides auto-detection; auto-detection walks up from the script's own location checking for the four canonical entries (`scripts/validate_spec.py`, `hooks/`, `skills/`, `SPEC.md`), then falls back to `AGENTERA_HOME` and `CLAUDE_PLUGIN_ROOT` env vars before erroring with explicit guidance. Stdlib only: `tomllib` for read-side parsing plus a hand-rolled minimal TOML emitter for the one inline-table shape we produce (basic-string escaping per TOML 1.0). Added `tests/test_setup_codex.py` with 10 cases (under the 12-case AC8 budget): six TOML branches (fresh, section-absent append, section-without-set insert, noop byte-identical, conflict refuses, --force merges) plus four behavioral gates (install-root verification rejects bad paths with named missing entries, auto-detect failure exits 2 with `--install-root` guidance, dry-run on pending exits 1, dry-run on noop exits 0). PLAN.md Task 1 status flipped □ → ■; DOCS.md Plan row date refreshed to 2026-04-26.
**Commit**: f82a776 feat(setup): add scripts/setup_codex.py for AGENTERA_HOME injection
**Inspiration**: SPEC Section 7 AGENTERA_HOME contract (Cycle 169 / commit f1867bf) defined the install-root primitive; Cycle 171 (commit b71da5a) shipped the manual snippet this helper now automates. Verified-source authority for the TOML shape: `ShellEnvironmentPolicyToml` schema (`https://github.com/openai/codex/blob/main/codex-rs/core/config.schema.json`) and Codex config reference (`https://developers.openai.com/codex/config-reference`). Script shape grounded in the `scripts/compact_artifact.py` (Cycle 165) and `scripts/usage_stats.py` (Cycle 159) patterns: stdlib-only, clean argparse, idempotent, explicit exit codes (0 success/no-op, 1 dry-run-with-pending-change, 2 error). `--dry-run` exit-1-on-change convention copied from `scripts/validate_spec.py` and `scripts/generate_contracts.py --check`.
**Discovered**: TOML 1.0 has no stdlib emitter (tomllib is read-only as of 3.11), so the helper hand-rolls a minimal basic-string emitter for the single inline-table shape it produces. The decision to refuse multi-line `set` tables under `--force` (rather than try to parse and reformat them) keeps the line-based mutation safe at the cost of one extra error path; users with that uncommon shape get a "edit manually" message rather than a corrupted config. The "AGENTERA_HOME present at a different value with no sibling keys" sub-case turned out to be naturally safe to update without `--force` because the user already owns the section completely; this is treated as a value update (action="insert") rather than a conflict, which keeps the no-op-on-correct-value semantics consistent. The auto-detection walk-up correctly finds the live agentera repo when running from a normal clone, so the auto-detect-failure test stubs `auto_detect_install_root` to None to deterministically hit the "no valid root" branch (a real walk-up from any path under the repo would otherwise resolve to the repo root).
**Verified**: End-to-end smoke against a `mktemp -d` config target across all six TOML branches plus three gates: Branch 1 (absent file) wrote `[shell_environment_policy]\nset = { AGENTERA_HOME = "$HOME/git/agentera" }\n` and exited 0; idempotent re-run printed `AGENTERA_HOME already set ...; nothing to do` and `diff -q` confirmed byte-identical. Branch 2 (file with `[unrelated]` table) appended the section, preserving `[unrelated]\nname = "keep me"` verbatim. Branch 3 (`[shell_environment_policy]` with `inherit = "core"` and a separate `[other]` table) inserted `set = {...}` immediately after the section header with both `inherit` and `[other]` byte-identical. Branch 4 (AGENTERA_HOME at desired value) returned the no-op message and exited 0. Branch 5 (sibling keys `LANG`, `PATH` without AGENTERA_HOME) printed `[shell_environment_policy].set has sibling keys (LANG, PATH). Re-run with --force ...` plus the `current:` / `proposed:` inline-table diff and exited 2; `diff -q` confirmed the file was untouched. Branch 6 (same conflict + `--force`) merged AGENTERA_HOME alongside LANG and PATH, preserving sibling key order (LANG, PATH, AGENTERA_HOME) and exited 0; tomllib re-parse confirmed all three keys parse to the expected string values. Dry-run on absent config printed the unified diff and exited 1 with no file created. Dry-run on the noop config exited 0. Bad install root (`--install-root /tmp/.../bogus`) exited 2 with `missing canonical entries: scripts/validate_spec.py, hooks, skills, SPEC.md`. Validators all green: `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` -> `All contract files are current (12 checked)`. `python3 -m pytest -q` -> `422 passed in 0.39s` (was 412 baseline at Cycle 174, +10 new from `tests/test_setup_codex.py`).
**Next**: Task 2 (`scripts/setup_copilot.py` shell-rc helper covering bash/zsh/fish + unsupported-shell branches with marker-comment idempotency) is now unblocked; runs in parallel with Task 1 per `Depends on: none`. Task 3 (`scripts/smoke_setup_helpers.py` cross-helper smoke runner mirroring the `smoke_opencode_bootstrap.mjs` shape) requires both Task 1 and Task 2. Task 4 (README + DOCS surface + Audit 14 Freshness fix) follows. Task 5 bumps 1.20.0 → 1.21.0. Task 6 closes the plan with a freshness checkpoint.
**Context**: intent (convert Cycle 171's manual `[shell_environment_policy]` snippet into a single idempotent command that handles every realistic state of `~/.codex/config.toml` without sudo, third-party deps, or the operator hand-editing TOML on every install-root change) · constraints (stdlib only per CLAUDE.md scripts convention; idempotent and dry-runnable; install-root verified against canonical entries before any write; one conventional commit; no push; SHA-pin via PROGRESS follow-up; test budget capped at 12 cases per PLAN Task 1 AC8 with edge expansion bounded to the six TOML structural branches; mandatory `**Verified**` field per SPEC Section 20 with end-to-end smoke output across each branch plus pytest and validator outputs as supporting evidence — no N/A allowlist tag because the helper is functional code) · unknowns (whether real-user `~/.codex/config.toml` files in the wild ever carry multi-line `set` tables; the current cut refuses those under `--force` with an "edit manually" message rather than risk a corrupted rewrite) · scope (`scripts/setup_codex.py`, `tests/test_setup_codex.py`, `.agentera/PLAN.md`, `.agentera/DOCS.md`).

■ ## Cycle 174 · 2026-04-26 15:00 · chore(plan): freshness checkpoint for Cross-Runtime Portability

**Phase**: checkpoint
**What**: Plan-level summary cycle closing the Cross-Runtime Portability plan. Cycles 169-173 carry the per-task detail (T1 SPEC.md AGENTERA_HOME contract at f1867bf; T2 OpenCode bootstrap-at-init plus shell.env injection at 7299775; T3 Codex and Copilot setup docs at b71da5a; T4 SKILL.md compaction-invocation cleanup, lint rule, and SPEC renumbering drift remediation at ec4235b; T5 1.19.0 -> 1.20.0 release and test version-literal refactor at ed1b516); this entry summarizes the plan-level outcome without restating that detail. The four cycles trace the same AGENTERA_HOME story end-to-end: contract formalized in SPEC.md Section 7, OpenCode adapter wired through the real `shell.env` plugin hook, Codex and Copilot adapters documented through their official user-config mechanisms, and the 5 producer SKILL.md compaction invocations adopted via the `${AGENTERA_HOME:-$CLAUDE_PLUGIN_ROOT}` bash fallback so Claude Code keeps working without any Claude-side hook change. Net outcome: the OpenCode adapter that previously surfaced zero agentera slash commands now bootstraps the 12-command set at plugin init and injects AGENTERA_HOME into every shell-tool subprocess; Codex and Copilot users have copy-pasteable setup snippets backed by each runtime's authoritative source; the canonical 12 SKILL.md files no longer carry bare `${CLAUDE_PLUGIN_ROOT}` references in skill prose; the spec validator gained a deterministic lint rule against future regressions; and the suite shipped at 1.20.0 with CHANGELOG entries for three feats (T1, T2, T3+lint), one docs surface (T3 README install steps), and one refactor (T4 SKILL.md cleanup) plus the T5 release-chore mechanics. PLAN.md Task 6 marked ■ complete; TODO.md gained three Normal follow-ups for the deferred items (`[opencode-session-events]` for SESSION.md bookmark replacement plus session-start context preload via OpenCode's real `event` mechanism, `[codex-setup-helper]` and `[copilot-setup-helper]` for optional idempotent setup helpers per the plan's Out-of-scope language). Plan is ready for orkestrera to archive.
**Commit**: 2592fff chore(plan): freshness checkpoint for Cross-Runtime Portability
**Inspiration**: SPEC Section 20 N/A allowlist (`docs-only`) and the Cycle 168 / 161 / 151 / 142 / 134 / 118 plan-level checkpoint pattern. No external pattern needed.
**Discovered**: CHANGELOG 1.20.0 release block was already coherent post-T5: four `Added` entries (AGENTERA_HOME contract, OpenCode bootstrap-at-init plus shell.env, Codex+Copilot install docs, lint rule) and three `Changed` entries (5 SKILL.md compaction-invocation rewrites, SPEC renumber and follow-on shifts, and the test version-literal refactor) trace the plan's three feats, one docs entry, and one refactor without duplication, validating Cycle 173's promotion choice. T3's Discovered note about the missing DOCS Index row for `.github/plugin/plugin.json` was self-resolved in the same cycle and does not warrant a Surprise entry. T5's Cycle 173 confirmed no new surprises. PLAN.md Surprises is therefore complete with the three logged divergences (T1 renumbering drift map, T2 phantom-key dead-code observation plus single-fire harness measurement, T4 six-additional-surfaces enumeration).
**Verified**: N/A: docs-only. Changes are PLAN.md Task 6 status flip (□ → ■), three TODO.md `[topic]`-prefixed Normal follow-ups, and this PROGRESS.md plan-level entry. No code, scripts, tests, or version metadata touched. Validators stay green at the post-T5 Cycle 173 baseline: `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` -> `All contract files are current (12 checked)`. `python3 -m pytest -q` -> `412 passed in 0.37s` (no test count change; T6 touched no code).
**Next**: Orkestrera (or the next session) archives PLAN.md to `.agentera/archive/PLAN-2026-04-26-cross-runtime-portability.md` and resumes vision-driven work selection. Open follow-ups land in TODO.md Normal: the three new Cross-Runtime Portability items (`[opencode-session-events]`, `[codex-setup-helper]`, `[copilot-setup-helper]`) plus the pre-existing `[copilot-marketplace]` deferral. The marketplace.json profilera-without-version-key convention noted across multiple bumps remains an open normalization question worth deciding before the next minor bump.
**Context**: intent (close Cross-Runtime Portability plan with one aggregate cycle entry summarizing the AGENTERA_HOME story end-to-end, not per-cycle restatements; file the three deferred Out-of-scope items as Normal TODO follow-ups; confirm CHANGELOG 1.20.0 represents the plan coherently) · constraints (no script/test/version changes; CHANGELOG already coherent so leave it untouched; one conventional commit; no push; do not archive PLAN.md — orkestrera handles archival; mandatory `**Verified**` field with `N/A: docs-only` allowlist tag plus validator outputs as supporting evidence; T6 must not change validator/lifecycle/contract/pytest counts from the post-T5 baseline) · unknowns (none) · scope (`.agentera/PLAN.md`, `TODO.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 173 · 2026-04-26 14:30 · chore(release): bump suite to 1.20.0

**Phase**: release
**What**: Task 5 of the Cross-Runtime Portability plan. Bumped suite metadata from 1.19.0 to 1.20.0 across every file in DOCS.md `version_files` (Copilot root `plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.opencode/plugins/agentera.js` `AGENTERA_VERSION` constant, `registry.json` 12 entries, `.claude-plugin/marketplace.json` 12 versions, and 12 `skills/*/.claude-plugin/plugin.json` files; profilera entry in marketplace.json deliberately preserved without a version key per the long-standing convention). Promoted CHANGELOG `## [Unreleased]` to `## [1.20.0] · 2026-04-26` (empty Unreleased block left at top); the release block lists the AGENTERA_HOME contract feat (T1), the OpenCode bootstrap-at-init plus shell-env injection feat (T2), the Codex and Copilot setup documentation feat (T3) plus the spec validator lint rule, and the 5 SKILL.md compaction-invocation rewrites, SPEC.md 7-23 -> 8-24 renumber, and test version-literal refactor under Changed. Refactored `tests/test_runtime_adapters.py::test_opencode_package_fails_on_version_drift` to read AGENTERA_VERSION from `.opencode/plugins/agentera.js` at test time via a new `_read_opencode_agentera_version` helper, eliminating the hardcoded literal that previously required manual sync on every bump (Cycle 167 Surprise). Added one-pass `test_opencode_agentera_version_reader_extracts_current_literal` verifying the reader matches the registry suite version. PLAN.md Task 5 marked ■ complete. Per `feat = minor` policy, three unreleased feats trigger one minor bump.
**Commit**: ed1b516 chore(release): bump suite to 1.20.0
**Inspiration**: DOCS.md `versioning.semver_policy` (`feat = minor`) and the Cycle 167 / 124 / 116 / 150 version-bump pattern. Cycle 167's Discovered field explicitly surfaced the hardcoded version literal as a Surprise that should be refactored away on the next bump; this cycle resolves that follow-up by routing version discovery through a helper that reads agentera.js at test time.
**Discovered**: The marketplace.json profilera entry remained without a version key as expected; the bump count surfaces (12 entries in marketplace, 12 in registry, 11 inner non-profilera + 1 top-level = 12 in marketplace) lined up exactly. The Cycle 167 hardcoded-literal Surprise is now closed structurally: future bumps will not require any test-file edits because `_read_opencode_agentera_version` reads the live constant from agentera.js and the drift test substitutes a guaranteed-different sentinel (`0.0.0-stale-fixture`) rather than carrying a static prior version. No new surprises this cycle.
**Verified**: N/A: chore-build-config. `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills`. `python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok`. `python3 scripts/generate_contracts.py --check` -> `All contract files are current (12 checked)`. `python3 -m pytest -q` -> `412 passed in 0.37s` (was 411 baseline at Cycle 172, +1 net new from `test_opencode_agentera_version_reader_extracts_current_literal`). `grep -c '"version": "1.19.0"'` across all version_files returns 0 in every file (plugin.json, .github/plugin/plugin.json, .codex-plugin/plugin.json, .claude-plugin/marketplace.json, registry.json, .opencode/plugins/agentera.js, all 12 skills/*/.claude-plugin/plugin.json). Counts at 1.20.0 confirm the bump landed in every listed file: 1 in each standalone plugin.json (3 files), 12 in registry.json, 12 in .claude-plugin/marketplace.json (1 top-level + 11 inner; profilera entry preserved with no version key), 1 in each skills/*/.claude-plugin/plugin.json (12 files), 1 in .opencode/plugins/agentera.js (`AGENTERA_VERSION = "1.20.0"`).
**Next**: Task 6 (plan-level freshness checkpoint) closes the Cross-Runtime Portability plan with one aggregate cycle summary, marks Tasks 1-5 ■ complete in PLAN.md, files the SESSION.md OpenCode bookmark replacement (`event` hook), optional Codex setup helper, and optional Copilot setup helper as TODO follow-ups, and confirms CHANGELOG 1.20.0 represents the release coherently. The plan's Overall Acceptance criteria can then be verified end-to-end.
**Context**: intent (apply DOCS.md feat=minor policy for three unreleased feats in one consistent bump and structurally close the Cycle 167 hardcoded-version-literal Surprise so future bumps are mechanically simpler) · constraints (every file in version_files, no others; CHANGELOG promoted without inventing entries; test refactor adds 1 pass case only per task brief; PLAN Task 5 marked ■ complete after substantive commit; validators stay green; one conventional commit; no push; SHA-pin via PROGRESS follow-up; `**Verified**` field with `N/A: chore-build-config` allowlist tag plus grep counts and validator outputs as supporting evidence; do not introduce a `version` key on the marketplace.json profilera entry — preserve the long-standing convention) · unknowns (none) · scope (`plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.opencode/plugins/agentera.js`, `registry.json`, `skills/*/.claude-plugin/plugin.json`, `CHANGELOG.md`, `tests/test_runtime_adapters.py`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 172 · 2026-04-26 14:00 · refactor(skills): adopt AGENTERA_HOME bash fallback and shift Section refs after SPEC renumber

**Phase**: build
**What**: Task 4 of the Cross-Runtime Portability plan. Two coupled scopes landed atomically. Scope A: rewrote 5 SKILL.md compaction-script invocations (realisera todo-resolved + progress, resonera decisions, optimera experiments, inspektera health) from bare `${CLAUDE_PLUGIN_ROOT}` to `${AGENTERA_HOME:-$CLAUDE_PLUGIN_ROOT}` so the install root resolves identically across Claude Code, OpenCode, Codex, and Copilot; activated a deterministic spec-validator lint rule (`check_no_bare_claude_plugin_root`, Check 24) that warns on bare `${CLAUDE_PLUGIN_ROOT}` in skill prose with the suggested replacement, and added one pass + one fail test for the rule. Scope B: remediated the SPEC renumbering drift from Cycle 169. Updated `spec_sections` frontmatter in 9 SKILL.md files (realisera 19,22 → 20,23; optimera 22 → 23; orkestrera 11,18,19 → 12,19,20; planera 16 → 17; inspektera 16,17,18 → 17,18,19; inspirera 12 → 13; visionera 12 → 13; visualisera 12 → 13; hej 12,18 → 13,19). Shifted every "Section N" prose reference in SKILL.md files (Section 19 → 20 Reality Verification Gate; Section 22 → 23 Pre-dispatch Commit Gate; Section 23 → 24 Artifact Writing Conventions). Shifted hardcoded Section literals in `scripts/validate_spec.py` (`REALITY_VERIFICATION_ENFORCERS` docstring, `_has_section_22_ref` → `_has_section_23_ref` plus its patterns, gate indicator messages, `check_pre_dispatch_commit_gate`, `check_commit_message_hygiene`, `check_artifact_writing_conventions`, `check_banned_vocabulary`, and the recognized-capabilities docstring referring to Section 21). Shifted `tests/test_validate_spec.py` fixtures (Section 19 → 20, Section 22 → 23) and renamed three test methods accordingly. Shifted the tightly coupled `tests/test_runtime_adapters.py` literal + `scripts/validate_lifecycle_adapters.py` `CODEX_PROFILERA_INVOCATION_TERMS` + `.codex-plugin/plugin.json` invocationHint (Section 21 → 22 Session Corpus Contract). Updated documentation surfaces (`agents/openai.yaml`, `skills/profilera/agents/openai.yaml`, `references/adapters/opencode.md`, plus comments/docstrings in `skills/profilera/scripts/extract_all.py` and `scripts/usage_stats.py`) for the same Section 21 → 22 shift. Regenerated all 12 per-skill `references/contract.md` files via `scripts/generate_contracts.py` so the generated headers carry the correct section numbers.
**Commit**: ec4235b refactor(skills): adopt AGENTERA_HOME bash fallback and shift Section refs after SPEC renumber
**Inspiration**: SPEC Section 7 (Cycle 169 / commit f1867bf) defined AGENTERA_HOME as the cross-runtime primitive and explicitly named bash-fallback as Claude Code's row in the per-runtime mechanism table. PLAN.md Cycle 169 Surprise documented the exact mapping for the renumbering drift; this task executes that mapping plus the lint rule the contract anticipated. Lint rule shape grounded in the existing `check_artifact_writing_conventions` pattern (regex over prose with code-block exclusion, deterministic warn).
**Discovered**: The Cycle 169 Surprise scope was the documented mapping for SKILL.md prose, validator code, and two test files. Six additional surfaces also carried hardcoded Section 21 references to the Session Corpus Contract that the original Surprise didn't enumerate (`.codex-plugin/plugin.json`, `scripts/validate_lifecycle_adapters.py`, `agents/openai.yaml`, `skills/profilera/agents/openai.yaml`, `skills/profilera/scripts/extract_all.py`, `scripts/usage_stats.py`, `references/adapters/opencode.md`). The plugin.json + validator + test triplet was tightly coupled (the test asserts the validator's error string which mentions the section number that flows from the plugin metadata it checks) so all three had to move together; the four documentation surfaces moved with them for consistency. No new test budget needed beyond the lint-rule pair — existing lifecycle-adapter, spec-validator, and contract-freshness coverage held green throughout. Logged as Task 4 Surprise in PLAN.md.
**Verified**: `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills` including the new `no-bare-claude-plugin-root` check passing for every canonical skill. `python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok` (validates the synced Section 22 invocationHint vs the synced `CODEX_PROFILERA_INVOCATION_TERMS`). `python3 scripts/generate_contracts.py --check` -> `All contract files are current (12 checked)` confirming the regenerated contract.md headers match the new spec_sections frontmatter. `python3 -m pytest -q` -> `411 passed in 0.43s` (was 409 baseline, +2 new lint-rule tests: `test_fallback_form_passes` and `test_bare_form_warns`). Lint-rule smoke against a synthetic third-party SKILL.md: bare-form variant produced `WARN  smoke_skill  no-bare-claude-plugin-root  smoke_skill/SKILL.md line 8: bare '${CLAUDE_PLUGIN_ROOT}' in skill prose; use '${AGENTERA_HOME:-$CLAUDE_PLUGIN_ROOT}' (spec Section 7 Install Root)`; fallback-form variant produced `PASS  smoke_skill  no-bare-claude-plugin-root` confirming both directions. Bash-fallback resolution smoke: with `AGENTERA_HOME=$HOME/git/agentera`, `bash -c 'python3 ${AGENTERA_HOME:-$CLAUDE_PLUGIN_ROOT}/scripts/compact_artifact.py progress /tmp/smoke_PROGRESS.md'` exited 0 with `no change: ... (3 full, 0 oneline, under threshold)`; with `AGENTERA_HOME` unset and `CLAUDE_PLUGIN_ROOT=$HOME/git/agentera`, the same invocation exited 0 with the same observed output, confirming the fallback path resolves identically (no Claude Code regression).
**Next**: Task 5 (1.19.0 -> 1.20.0 bump + the `tests/test_runtime_adapters.py` AGENTERA_VERSION literal refactor to read from `agentera.js` so future bumps don't drift) is now unblocked; Tasks 1-4 complete. Task 6 closes the plan with a freshness checkpoint and TODO follow-ups for SESSION.md bookmark replacement (OpenCode `event` hook) and optional Codex/Copilot setup helpers.
**Context**: intent (land the SKILL.md compaction-invocation cleanup, the matching lint rule, and the Cycle 169 SPEC renumbering drift in one atomic commit so the canonical 12 skills hit 0 errors / 0 warnings the moment the rule is enabled and so no surface is left pointing at the wrong section number) · constraints (no version bump per Task 5 scope, no Codex/Copilot doc changes per Task 3 scope, no OpenCode plugin changes per Task 2 scope; one conventional commit; no push; SHA-pin via PROGRESS follow-up; test proportionality 1 pass + 1 fail for the new lint rule per SPEC Section 17; mandatory `**Verified**` field with validator outputs, lint-rule pass/fail demonstrations, and bash-fallback smoke under both AGENTERA_HOME-set and AGENTERA_HOME-unset conditions) · unknowns (whether further drift exists in archived plans/decisions/health snapshots — those are historical and intentionally untouched per the docs-vs-archive convention) · scope (`scripts/validate_spec.py`, `scripts/validate_lifecycle_adapters.py`, `scripts/usage_stats.py`, `tests/test_validate_spec.py`, `tests/test_runtime_adapters.py`, `skills/*/SKILL.md` x 12, `skills/*/references/contract.md` x 12, `skills/profilera/agents/openai.yaml`, `skills/profilera/scripts/extract_all.py`, `agents/openai.yaml`, `.codex-plugin/plugin.json`, `references/adapters/opencode.md`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

■ ## Cycle 171 · 2026-04-26 13:35 · docs(install): codex and copilot AGENTERA_HOME setup steps

**Phase**: docs
**What**: Task 3 of the Cross-Runtime Portability plan. Documented the user-side AGENTERA_HOME setup for the two runtimes that have no plugin-level env-injection API. README.md alternative-install section gained a Codex setup block (`~/.codex/config.toml` `[shell_environment_policy] set = { AGENTERA_HOME = "<install root>" }` snippet, citing the Codex config reference and the `ShellEnvironmentPolicyToml` schema) and a Copilot setup block (`echo 'export AGENTERA_HOME=<install root>' >> ~/.bashrc` snippet, citing the Copilot CLI plugin and hooks references that confirm no plugin-level env-injection API). `.codex-plugin/plugin.json` `codex.limitations` array gained an entry naming the AGENTERA_HOME setup requirement and pointing at README. Root `plugin.json` and `.github/plugin/plugin.json` Copilot descriptions each gained one sentence flagging the requirement and pointing at README. DOCS.md Index dates bumped for the four touched files (`README.md` was already today; `.codex-plugin/plugin.json`, `plugin.json`, and a newly added `.github/plugin/plugin.json` row now show 2026-04-26) and an audit-log entry records the three doc gaps fixed. PLAN.md Task 3 marked complete.
**Commit**: b71da5a docs(install): codex and copilot AGENTERA_HOME setup steps
**Inspiration**: SPEC.md Section 7 per-runtime mechanism table (Cycle 169 / commit f1867bf) defined the contract; this task surfaces the user-facing setup instructions for the Codex and Copilot rows. Verified-source authority for the Codex snippet: `ShellEnvironmentPolicyToml` schema (`https://github.com/openai/codex/blob/main/codex-rs/core/config.schema.json`) and Codex config reference (`https://developers.openai.com/codex/config-reference`). Verified-source authority for the Copilot guidance: Copilot CLI plugin reference (no `env` field in plugin.json schema) and Copilot hooks reference (hook stdout marked `Ignored`).
**Discovered**: DOCS.md Index had no row for `.github/plugin/plugin.json` even though the file is checked into the repo as a Copilot manifest; added the row alongside the date bumps. The existing `Copilot plugin manifest | plugin.json` row covered only the root manifest. No other surprises; the task was a straightforward surface for the SPEC.md contract Cycle 169 already formalized.
**Verified**: N/A: docs-only. `python3 scripts/validate_spec.py` -> `0 error(s), 0 warning(s) across 12 skills` (matches Cycle 170 baseline). `python3 scripts/validate_lifecycle_adapters.py` -> `lifecycle adapter metadata ok`. `python3 -m pytest -q` -> `409 passed in 0.38s` (no test count change from Cycle 170; this task touched no code, no hooks, no SKILL.md, no version files). JSON parse check confirmed all three modified manifest files load cleanly with the new strings present.
**Next**: Task 4 (SKILL.md compaction-invocation cleanup at 5 sites across 4 producer skills + spec validator lint rule for bare `${CLAUDE_PLUGIN_ROOT}` in SKILL.md prose) is now unblocked; both T2 and T3 dependencies are satisfied. The Cycle 169 SKILL.md `spec_sections` and Section N renumbering drift logged in PLAN.md Surprises must still land before or alongside Task 4 ships. Task 5 (1.19.0 -> 1.20.0 bump + the `tests/test_runtime_adapters.py` AGENTERA_VERSION literal refactor to read from `agentera.js`) follows Task 4. Task 6 closes the plan with a freshness checkpoint and TODO follow-ups for SESSION.md bookmark replacement and optional Codex/Copilot setup helpers.
**Context**: intent (give Codex and Copilot users an explicit, copy-pasteable setup step that wires AGENTERA_HOME into each runtime's shell-tool environment using its official documented mechanism) · constraints (docs and metadata only — no SKILL.md changes per Task 4 scope, no code or hook changes, no version bump per Task 5 scope; one conventional commit; SHA-pin via PROGRESS follow-up; mandatory `**Verified**` field with `N/A: docs-only` allowlist tag plus validator output as supporting evidence; do not invent setup mechanisms not in the verified sources) · unknowns (whether real Copilot/Codex install paths converge on a single conventional location worth hardcoding in the snippets — left as `<plugin install root>` placeholder per task brief; optional setup helpers are deferred to Task 6 follow-ups) · scope (`README.md`, `.codex-plugin/plugin.json`, `plugin.json`, `.github/plugin/plugin.json`, `.agentera/DOCS.md`, `.agentera/PLAN.md`, `.agentera/PROGRESS.md`).

## Archived Cycles

- Cycle 170 (2026-04-26): feat(opencode): bootstrap at init and inject AGENTERA_HOME via shell.env
- Cycle 169 (2026-04-26): feat(spec): standardize AGENTERA_HOME contract for cross-runtime helper paths
- Cycle 168 (2026-04-26): chore(plan): freshness checkpoint for Suite Usage Analytics
- Cycle 167 (2026-04-26): chore(release): bump suite to 1.19.0
- Cycle 166 (2026-04-26): docs(usage): document scripts/usage_stats.py across README, DOCS.md, AGENTS.md
- Cycle 165 (2026-04-26): feat(usage): emit USAGE.md, stdout summary, and JSON
- Cycle 164 (2026-04-26): feat(usage): classify trigger phrasing and scope by project
- Cycle 163 (2026-04-26): feat(usage): detect skill invocations and pair with exit signals
- Cycle 162 (2026-04-26): feat(validator): accept arbitrary SKILL.md paths for third-party authoring
- Cycle 161 (2026-04-25): chore(plan): checkpoint copilot marketplace freshness
- Cycle 160 (2026-04-25): docs(release): apply copilot release convention
- Cycle 159 (2026-04-25): docs(copilot): update user guidance
- Cycle 158 (2026-04-25): chore(runtime): verify copilot host state
- Cycle 157 (2026-04-25): test(install): guard copilot marketplace claims
- Cycle 156 (2026-04-25): docs(install): clarify copilot marketplace placeholder
- Cycle 155 (2026-04-25): docs(plan): record copilot marketplace evidence
- Cycle 154 (2026-04-25): docs(copilot): prefer marketplace plugin installs
- Cycle 153 (2026-04-25): fix(copilot): load skills from checkout plugin root
- Cycle 152 (2026-04-25): chore(runtime): smoke live Copilot and Codex hosts
- Cycle 151 (2026-04-25): chore(plan): checkpoint Audit 11 freshness
- Cycle 150 (2026-04-25): chore(release): bump suite to 1.18.1
- Cycle 149 (2026-04-25): test(profilera): deepen corpus validation fixtures
- Cycle 148 (2026-04-25): refactor(profilera): localize corpus runtime orchestration
- Cycle 147 (2026-04-25): fix(adapters): repair OpenCode path and hook drift
- Cycle 146 (2026-04-25): fix(adapters): catch Codex invocation hint drift
- Cycle 145 (2026-04-25): fix(adapters): tighten runtime metadata drift guards
- Cycle 144 (2026-04-25): fix(profilera): redact Copilot config secrets
- Cycle 143 (2026-04-25): fix(profilera): align Section 21 corpus record envelope
- Cycle 142 (2026-04-24): chore(plan): checkpoint runtime portability freshness
- Cycle 141 (2026-04-24): chore(release): bump suite to 1.18.0
- Cycle 140 (2026-04-24): test(profilera): integrate multi-runtime status validation
- Cycle 139 (2026-04-24): evidence(profilera): verify Codex extraction entrypoint
- Cycle 138 (2026-04-24): feat(profilera): collect Codex corpus records
- Cycle 137 (2026-04-24): feat(profilera): collect Copilot corpus records
- Cycle 136 (2026-04-24): fix(adapters): repair Codex and OpenCode metadata
- Cycle 135 (2026-04-24): fix(adapters): repair Claude and Copilot metadata
- Cycle 134 (2026-04-23): chore(plan): checkpoint Copilot and Codex native loading freshness
- Cycle 133 (2026-04-23): chore(release): verify 1.17.0 version bump evidence
- Cycle 132 (2026-04-23): test(adapters): cover runtime adapter metadata
- Cycle 131 (2026-04-23): feat(hooks): add lifecycle adapter strategy
