id: izkgsrdezq
artifact: progress
record:
  timestamp: 2026-07-20 06:17
  type: fix
  phase: build
  what: Secured registry-driven package bundle entries for ipayvkggov. PackageRegistry now enforces one directories/files namespace with globally unique IDs and paths plus normalized relative POSIX paths. copy-bundle imports the emitted loader, preflights contained real source and destination plans before mutation, rejects source symlink escapes and destination collisions, and no longer carries parallel shape/path validation or writes parity outputs outside bundle.
  context:
    intent: Close the reproduced wqeazunluo-audit-2 bundle traversal and duplicate-identity defects while preserving the complete package-retirement candidate and blocked audit history.
    constraints: No task/evaluation/TODO/decision status mutation, commit, push, publish, tag, release, version change, changelog edit, packed-upgrade work, broad documentation change, or modification of the nine deletions and migration/history inputs.
    unknowns: none
    scope: packages/cli/src/registries/packageRegistry.ts; packages/cli/scripts/copy-bundle.mjs; packages/cli/package.json bundle:data build seam; references/adapters/package-manifest-interface-model.yaml; focused registry and prepack exploit tests; typed progress.
  inspiration: Active successor task ipayvkggov and reproduced wqeazunluo-audit-2 exploit classes.
  discovered: The registry previously deduplicated IDs within each list only and used host path normalization that accepted cross-list collisions, backslashes, leading dot segments, and traversal. The bundler independently parsed YAML, removed bundle before source validation, and could resolve declared sources or destinations outside their roots. Registry validation is now the sole syntax/identity seam; bundling adds only filesystem containment planning.
  verified: Focused registry/copy/prepack/release candidate tests passed 83/83 across 5 files; full CLI passed 2377/2377 across 200 files. Direct temporary-fixture probes made the emitted loader and bundler reject duplicate cross-list IDs, duplicate cross-list paths, empty paths, ../outside, ./README.md, POSIX absolute, Windows drive/backslash forms, trailing separators, and noncanonical repeated separators. All bundler probes exited nonzero with the loader's actionable ID/path correction, and preexisting bundle plus outside sentinels remained unchanged. Separate probes rejected an external source symlink and recursive/explicit duplicate destination before mutation. Build, typecheck, and direct bundle:data passed; each staged the registry-owned 8 surfaces through the emitted loader. npm dry-run inventory remained 672 files with required CLI/shared-skill/schema/reference/registry paths present, all nine retired paths absent, and no native bundle paths; extracted help exited 0. State validation passed 1587 entities with 0 issues before this append; capability contract passed 2/2; compact reported 1 artifact with 0 over-limit/errors/changes; git diff --check passed. Package remained 3.0.0-dev.25, suite/skill/registry remained 3.0.0, and CHANGELOG.md remained unchanged. Shim/web files were not changed by ipayvkggov and were not rerun.
  next: Leave ipayvkggov in_progress for independent audit and do not record evaluation. Preserve blocked wqeazunluo with both failed audits; packed upgrade and later plan tasks remain out of scope.
