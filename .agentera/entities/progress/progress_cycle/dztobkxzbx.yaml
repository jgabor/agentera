id: dztobkxzbx
artifact: progress
record:
  timestamp: 2026-07-13 17:15
  type: feat
  phase: build
  what: Implemented Task 12 read-only durability diagnostics under agentera check durability. The authority now publishes the exact syntax, bounded selectors, complete/degraded/unavailable status values, local versus Git evidence, read-only and no-remote guarantees. The diagnostic validates local numbered archives, captures HEAD before and after inspection, detects dirty archives, shallow history, rewritten history, changed HEAD, missing commits, non-Git projects, and missing committed bytes without importing writer or projection code. Added deterministic Git fixture coverage and CLI contract coverage.
  inspiration: Active lossless archive-backed state storage plan Task 12 and references/artifacts/state-storage-authority.yaml.
  discovered: Local validation remains authoritative when Git is absent or degraded. A shallow repository can expose a reachable current archive while still reporting degraded historical durability. Rewritten history is detected only when retained local bytes match a prior reflog head that is no longer an ancestor; no remote or custom ref is needed. The full suite retains exactly five npm 12 npm pack --dry-run --json parsing failures in test/packaging/prepack.test.ts.
  verified: "Focused: pnpm -C packages/cli exec vp test run test/state/durability.test.ts test/state/archiveAuthority.test.ts, 2 files and 19 tests passed. Adjacent: pnpm -C packages/cli exec vp test run test/state/durability.test.ts test/state/archiveAuthority.test.ts test/state/archiveRecovery.test.ts test/state/archiveDiscovery.test.ts test/state/archivePublication.test.ts test/state/directRetrieval.test.ts test/state/decisionOverlay.test.ts test/state/projectionPolicy.test.ts test/cli/state.test.ts test/cli/compactParity.test.ts, 10 files and 135 tests passed. Non-packaging: pnpm -C packages/cli exec vp test run --exclude test/packaging/prepack.test.ts, 170 files and 1692 tests passed. Full: pnpm -C packages/cli test, 171 files and 1696 of 1701 tests passed; exactly 5 pre-existing npm 12 npm pack --dry-run --json parsing failures remain in test/packaging/prepack.test.ts. pnpm -C packages/cli run typecheck passed; pnpm -C packages/cli run build passed; pnpm -C packages/cli lint passed with 102 warnings and 0 errors. node packages/cli/dist/bin/agentera.js check validate capability-contract --format json passed; node packages/cli/dist/bin/agentera.js check validate artifact --artifact progress --format json passed; node packages/cli/dist/bin/agentera.js check compact --format json passed with artifact_count=9, over_limit_count=0, protected_overflow_count=0, error_count=0, changed_count=0."
  next: Task 12 remains pending for review and must not be marked complete. Preserve the blocked Task 9 metadata and degraded TODO entry. Do not fix Task 9 or perform migration, prime consumer, health, vision, profile, decisions, version, publish, push, tag, or remote work.
  context:
    intent: Execute active Agentera plan Task 12 without coupling local archive writes to Git or marking the task complete.
    constraints: "Git is optional evidence only: no commits, commit fields in archive records, custom refs, remote reads, fetch, push, or mutation. The diagnostic is read-only and local writes remain independent. The five npm 12 packaging failures are pre-existing."
    scope: packages/cli/src/state/durability.ts; packages/cli/src/state/archiveDiscovery.ts; packages/cli/src/cli/commands/durability.ts; packages/cli/src/cli/dispatch/check.ts; packages/cli/src/cli/dispatch/index.ts; packages/cli/src/cli/help.ts; packages/cli/test/state/durability.test.ts; packages/cli/test/state/archiveAuthority.test.ts; references/artifacts/state-storage-authority.yaml; .agentera/plan.yaml; TODO.md
