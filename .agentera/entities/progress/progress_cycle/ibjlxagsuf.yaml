id: ibjlxagsuf
artifact: progress
record:
  timestamp: 2026-07-20 02:33
  type: fix
  phase: build
  what: "Supersedes progress mgrwtgqcpp for task nwaesildcl: corrected audit nwaesildcl-audit-1 by making retained lifecycle references internally migration-only, restoring plan lifecycle coverage, and adding direct shared-skill diagnosis coverage."
  context:
    intent: Preserve the repository-native retirement candidate while correcting every concrete audit finding without entering Task 4 package/distribution scope or changing task status.
    constraints: Preserved mgrwtgqcpp and the recorded failed audit; no task/evaluation/TODO/decision status, version, release, package/distribution contract beyond the existing minimal bridge, retained descriptor, remote, host configuration, user-project configuration, migration fixture/reader/history, commit, push, publish, or tag change.
    scope: references/adapters/runtime-lifecycle-authority.yaml; references/adapters/runtime-lifecycle-adapters.yaml; references/cli/prime-consumer-compatibility.yaml; lifecycle migration-only loaders/factory; schema shared-skill path source; active protocol, restored plan lifecycle, shared-skill diagnosis, and explicit cleanup tests.
  discovered: The explicit Claude cleanup route still traversed the lifecycle adapter factory; deriving adapter instances from the now-empty authority inventory preserves that cleanup route without recreating current runtime adapters. The restored plan lifecycle scanner remained valid after removing its obsolete expectation that the retired OpenCode plugin be a live candidate.
  verified: "Finding 1: runtime-lifecycle-authority now declares migration_only_authority with active_runtimes empty; runtime-lifecycle-adapters declares migration_only_contract with no shared or managed resources and no adapters, and its validator enforces those migration-only status labels. prime-consumer-compatibility contains no deleted OpenCode plugin/agent, Cursor agent, or opencode_compaction_context consumer references. activeProtocolSurface directly parses and rejects internally active lifecycle contracts and deleted source paths. Finding 2: planLifecycleContract was restored from HEAD and retains bounded recursive inventory, symlink/cache/exclusion, classification, schema, and fixture coverage with only the retired plugin expectation removed. Finding 3: direct tests prove a valid canonical skill passes, a missing skill warns with install and direct-CLI actions, and unrelated directories or symlinks remain byte/identity-preserved with manual-review guidance. Original criteria remain covered by the retirement inventory, five direct CLI hooks, install-root precedence, v2 migration fixtures/readers/history, inert OpenCode descriptors, explicit Claude cleanup, and temporary-home non-mutation tests. Focused suites passed 6 files/41 tests, prime compatibility plus active protocol passed 2/39, and active lifecycle plus authority passed 2/7. Final full CLI retry passed 199 files/2342 tests; one preceding run hit the existing prime heap-budget variance before the clean retry. Typecheck passed; build staged 15 data surfaces; state validation passed with 1574 entities and 0 issues before this append; capability-contract passed 2/2; compact passed with 0 over-limit and 0 errors; git diff --check passed."
  next: Final independent audit should evaluate nwaesildcl while leaving it in progress. Task wqeazunluo retains ownership of package/distribution contracts and descriptor removal.
