id: tonwbfwngw
artifact: progress
record:
  timestamp: 2026-08-08 11:55
  type: fix
  phase: build
  what: Replaced broad bare-multiword colon admission with an authority-declared bounded grammar, added adjacent direct-reference binding, and preserved the dirty explicit-mining candidate without closing evaluator-owned state.
  context:
    intent: Resolve replacement task guhuzevlez after two failed evaluations of milybqttmo by closing only the accepted colon, adjacency, formatting, and regression gaps.
    constraints: Keep guhuzevlez pending, milybqttmo blocked, TODO psteyugpzv open, and all prior progress and evaluation evidence intact. Do not run Audit, commit, publish, change recurring behavior, or mutate protected state.
    unknowns: Independent evaluation has not rerun against this correction.
    scope: Explicit cue parsing and types, personal glossary contract types and authority validation, paired explicit regressions, and the existing retired-runtime scalar census fixture.
  discovered: The failed parser treated any bare multiword text before a colon as safe, so ordinary labels remained eligible. It also anchored colon cues to the start of the physical line and required literal term repetition in adjacent exclusions. That combination both missed direct references and let unrelated qualifiers contaminate the cue sentence. Formatting then exposed the 1000-line source gate, so behavior-neutral type extractions were required.
  verified: "Colon approvals each produced exactly 1 cue: backtick-delimited ship shape, quoted ship shape, API with matching Application Programming Interface initials, Definition marker, Term marker, and bulleted Definition marker. Colon rejections each produced 0 cues: status, name, Notes, RFC 2119, build status, last updated, release notes, Meeting at 12:30, Deploy at 09:45, cache policy, unsupported Note marker, bare 12:30, URL, heading, acronym heading, citation [1], RFC citation, and API with mismatched Application Binary Interface initials; the 13-record mined rejection subset emitted unsafe_syntax for every record. Adjacent direct-reference question, example, future, and hypothetical prefixes each produced 0 cues with indirect_question, example_context, future_definition, and hypothetical_definition respectively; an after-cue this-definition example also produced 0. Paired unrelated question, example, future, and conditional sentences each left the following definition eligible with exactly 1 cue. Prior matrix remained intact: complete user provenance produced a valid capsule while incomplete, agent, and injected provenance abstained; project scope before and after remained excluded and the project-glossary trap stayed byte-equal; same-meaning anchors kept 1 candidate while conflicting anchors kept 0 and emitted 2 conflict abstentions; same-term retraction produced 0 while unrelated retraction produced 1; UTF-8 term and meaning slices remained exact; deterministic replay and ordering remained byte-identical; admission and recurring mining regressions passed. Final focused explicit, admission, recurrence, authority, and candidate-contract run passed 5 files and 197 tests. Full source passed 233 files, 3606 tests, and 1 skip after correcting the first run's monolith and census failures; the isolated upgrade timeout also passed 25 tests. Package passed 3 files and 34 tests. Typecheck passed. Build passed. Compact reported 1 artifact, 0 over limit, 0 formatting, 0 errors, and 0 changes. Repository format check passed all 9 changed explicit-mining source and test files; unrelated retired-policy formatter churn was reverted, leaving only its required one-line census update. git diff --check passed."
  next: Keep guhuzevlez pending, milybqttmo blocked, TODO psteyugpzv open, and evaluator state unchanged for independent evaluation. Do not run Audit or create a commit.
  publication_order: 115
