id: fhfzplrlcj
artifact: todo
record:
  severity: normal
  status: resolved
  description: "[fix:3.0.0] Legacy Python parity checks gated behind opt-in `--legacy-python-parity` flag. `lifecycleAdapters.ts:95-96` — default `validate lifecycle-adapters`..."
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[fix:3.0.0] Legacy Python parity checks gated behind opt-in `--legacy-python-parity` flag. `lifecycleAdapters.ts:95-96` — default `validate lifecycle-adapters`..."
      status: resolved
      order: 65
      severity: normal
