id: auwlbuobdw
artifact: todo
record:
  severity: critical
  status: open
  description: "[fix:3.0.0] Align `packages/cli/src/state/write/operations.ts` dependency discovery with entity-mode bare task IDs. `agentera state plan explain --verb append` reports `integer_list` and task numbers, but runtime rejects `3` and requires a ten-letter task ID. Make discovery, parsing, examples, and command regressions agree."
  readiness:
    capability: build
    reason: The typed writer discovery contract must describe the accepted task-identity vocabulary.
    dependencies:
      []
    blocked: null
    gate: null
    queue_rank: 50
    order_reason: Bare task IDs provide a working correction, so this does not block the glossary producer plan.
