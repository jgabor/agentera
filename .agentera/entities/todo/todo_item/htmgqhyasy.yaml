id: htmgqhyasy
artifact: todo
record:
  severity: critical
  status: open
  description: "[fix:3.0.0] Establish one declarative state mutation grammar in `references/artifacts/state-storage-authority.yaml`. Classify every public verb as a record payload or simple transition; require `--input <path|->` for record payloads; enumerate permitted selectors, preconditions, CLI-owned fields, recovery, examples, and bounds; and retire unsupported `health repair`. Derive operations, explain, schema, and help from that projection while TypeScript code remains the behavior owner. Add a generated parity matrix and reject record-content flags before effects."
  readiness:
    capability: build
    reason: Decision imumkmffwh requires one structured-input mutation grammar without letting schema, help, or tests create parallel behavioral authority.
    dependencies:
      []
    blocked: null
    gate: null
    queue_rank: 30
    order_reason: Land the mutation grammar before parallel family conversions and before prime drops embedded writer contracts.
