id: kzthftsrys
artifact: todo
record:
  kind: test
  target_version: 3.0.0
  title: Reduce batched runtime integration median below baseline
  requirements:
    - Retain two runtime batches and all 16 real dispatcher and spawn-boundary observations.
    - Confirm the batching result with the user-approved two-run local timing check without weakening protected-root, hostile-path, isolation, restoration, timeout, or authority contracts.
  acceptance:
    - Two consecutive runtime integration runs under the same pinned local conditions pass.
    - Each observed wall time is at least 20 percent below the recorded 129.769-second baseline.
    - The runtime integration, package parity, policy, typecheck, and build checks pass.
  release_blocker: false
  severity: normal
  status: resolved
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[test:3.0.0] Reduce batched runtime integration median below baseline"
      status: resolved
      order: 117
      severity: normal
  lifecycle:
    operation: resolve
    reason: Two consecutive runtime integration runs passed more than 20 percent below the recorded baseline.
    date: 2026-08-23
