id: kzthftsrys
artifact: todo
record:
  kind: test
  target_version: 3.0.0
  title: Reduce batched runtime integration median below baseline
  requirements:
    - Retain two runtime batches and all 16 real dispatcher and spawn-boundary observations.
    - Identify and remove the measured runtime overhead without weakening protected-root, hostile-path, isolation, restoration, timeout, or authority contracts.
  acceptance:
    - Under the recorded equivalent three-run protocol, the corrected median is lower than the 129.769-second baseline.
    - All runtime integration, package parity, policy, typecheck, and build checks pass.
  release_blocker: false
  severity: normal
  status: open
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[test:3.0.0] Reduce batched runtime integration median below baseline"
      severity: normal
      status: open
      order: 32
