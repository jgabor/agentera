id: deoopdvnll
artifact: todo
record:
  severity: normal
  status: resolved
  description: "[fix:3.0.0] Add `--version` / `version` command to the CLI so consumers can check the installed..."
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[fix:3.0.0] Add `--version` / `version` command to the CLI so consumers can check the installed..."
      status: resolved
      order: 71
      severity: normal
