id: psteyugpzv
artifact: todo
record:
  kind: fix
  target_version: 3.0.0
  title: Replan explicit-definition parser segmentation
  requirements:
    - Replace the one-colon-per-line parser with a bounded sentence or segment model that can represent multiple same-line definitions and directional adjacent references.
    - Reject YAML block-scalar headers and nested marker/config structures even with inline comments, blank lines, or intervening prose.
    - Preserve complete provenance, project isolation, conflicts, retractions, exact UTF-8 spans, replay, traps, recurring mining, and formatter conformance.
  acceptance:
    - YAML block headers and non-immediate nested marker/config variants emit no explicit candidate.
    - Two- and three-definition same-line records preserve all non-target cues and directional exclusions.
    - The complete explicit cue and exclusion matrix passes repository formatting and verification gates.
    - A replanned replacement task receives persisted PASS before this TODO resolves.
  release_blocker: false
  severity: degraded
  status: open
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[fix:3.0.0] Replan explicit-definition parser segmentation"
      severity: degraded
      status: open
      order: 3
  readiness:
    capability: plan
    reason: Replacement task guhuzevlez exhausted two evaluations; parser segmentation must be replanned before dependent candidate selection.
    dependencies:
      []
    blocked: null
    gate: null
    queue_rank: 1
    order_reason: This terminal parser blocker prevents explicit mining completion and every downstream candidate task.
