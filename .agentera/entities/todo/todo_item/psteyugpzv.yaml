id: psteyugpzv
artifact: todo
record:
  kind: fix
  target_version: 3.0.0
  title: Replan explicit-definition parser segmentation
  requirements:
    - Preserve the finite authority-driven parser work while separating a same-line direct qualifier sentence from the preceding eligible meaning and binding the qualifier independently.
    - Emit the loaded structural_fragment abstention for structural entry, persistence, and reject-fragment-then-exit prose before permitting a valid post-exit definition.
    - Preserve complete provenance, project isolation, conflicts, retractions, exact UTF-8 spans, replay, all ten cues, recurring mining, and formatter conformance.
  acceptance:
    - The exact same-line qualifier reproducer retains only the preceding definition meaning, excludes only the authority-bound adjacent target, and preserves unrelated cues.
    - Structural entry, persistence, and prose exit each emit structural_fragment; no config fragment leaks and a valid definition after exit still mines.
    - Adjacent and nonadjacent direction, continuation, duplicate, conflict, structural, cue, and exclusion matrices pass repository formatting and source verification.
    - A replanned replacement task receives persisted independent PASS before this TODO resolves.
  release_blocker: false
  severity: degraded
  status: resolved
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[fix:3.0.0] Replan explicit-definition parser segmentation"
      status: resolved
      order: 97
      severity: degraded
  readiness:
    capability: plan
    reason: Replacement task nipgzvgjzm exhausted two evaluations; same-line qualifier segmentation and structural prose-exit outcomes require replanning before dependent candidate selection.
    dependencies:
      []
    blocked: null
    gate: null
    queue_rank: 1
    order_reason: This terminal parser blocker prevents explicit mining completion and every downstream candidate task.
  lifecycle:
    operation: resolve
    reason: Finite explicit parser replacement passed independent evaluation.
    date: 2026-08-09
