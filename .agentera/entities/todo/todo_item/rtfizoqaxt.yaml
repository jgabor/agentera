id: rtfizoqaxt
artifact: todo
record:
  kind: chore
  target_version: 3.0.0
  title: Provide a fail-closed Vite+ CI bootstrap
  requirements:
    - Select a setup path whose executable installer bytes are integrity-verified before execution.
    - Fail closed rather than falling back to mutable release, website, CDN, or main-branch installer content.
    - Preserve explicit frozen installs, lifecycle-script restrictions, cache prohibition, and the setup-free OIDC publisher.
  acceptance:
    - One exact Vite+ and setup action pair passes the retained toolchain baseline verifier.
    - Plan seltbrpsin can be replanned without weakening its publication trust boundary.
  release_blocker: false
  severity: normal
  status: open
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[chore:3.0.0] Provide a fail-closed Vite+ CI bootstrap"
      severity: normal
      status: open
      order: 30
