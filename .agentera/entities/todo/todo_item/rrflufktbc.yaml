id: rrflufktbc
artifact: todo
record:
  kind: fix
  target_version: 3.0.0
  title: Make explicit TODO cutover preserve one authoritative public row per item.
  requirements:
    - During `agentera upgrade --yes`, activate only after proving a complete one-to-one match between current TODO checkbox rows and canonical entities, including rows under legacy or project-specific headings; refuse entity-derived unmatched rows instead of appending a second projection.
    - Treat current `TODO.md` text, severity, checkbox status, and order as public authority; stale pre-created entity values must not reopen resolved work or override Markdown during cutover.
    - Publish the activation marker, visible IDs, public baselines, and canonical Markdown in one recoverable journal; keep previews read-only, reject ambiguity before effects, and preserve IDs across retries.
  acceptance:
    - An OpenEval-shaped fixture with a hierarchical TODO, 161 pre-created entities, and stale open entity statuses never gains a second 161-row Normal section and never reopens completed Markdown items.
    - A complete exact fixture activates with one ID-bearing public row per entity while preserving unrelated prose; an unmatched, stale, or ambiguous fixture exits before changing any bytes.
    - Interrupted or repeated upgrade converges without reallocating IDs, and ordinary reads remain mutation-free.
  release_blocker: true
  severity: normal
  readiness:
    capability: build
    reason: The explicit v3 cutover must leave TODO public and operational state fully connected before reporting success.
    dependencies:
      []
    blocked: null
    gate: null
    queue_rank: 37
    order_reason: Complete automatic migration before relying on manual recovery or downstream status reporting.
  status: resolved
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[fix:3.0.0] Make explicit TODO cutover preserve one authoritative public row per item."
      status: resolved
      order: 95
      severity: normal
  lifecycle:
    operation: resolve
    reason: Replacement plan task jstgyilsyk passed independent Audit for safe automatic cutover and recovery convergence.
    date: 2026-08-07
