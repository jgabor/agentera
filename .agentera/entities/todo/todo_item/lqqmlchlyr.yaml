id: lqqmlchlyr
artifact: todo
record:
  kind: fix
  target_version: 3.0.0
  title: Prepare one verified TypeScript stable-cutover candidate after approval.
  requirements:
    - Require explicit stable-cutover approval before changing channel authority, bootstrap resolution, Python delivery, compatibility files, or native runtime hooks.
    - Apply the approved bootstrap, Python-deprecation, and compatibility deletions from the completed cutover inventory.
    - Build and verify one immutable TypeScript candidate without publishing it or mutating npm state.
    - Preserve the exact candidate identity and evidence for the final publication transaction.
  acceptance:
    - The approved source removes the retained v2 and native-runtime surfaces named by the cutover inventory.
    - Bare agentera bootstrap, channel authority, and the Python deprecation response agree in source, docs, tests, and package output.
    - Release gates pass against one retained immutable TypeScript candidate.
    - No npm package or dist-tag changes during candidate preparation.
  release_blocker: true
  severity: critical
  readiness:
    capability: build
    reason: Cutover source changes must occur only after explicit approval and after the stable TypeScript publication path is ready.
    dependencies:
      - artifact: todo
        id: pqpzmhdpsj
      - artifact: todo
        id: odiytubocn
    blocked: null
    gate:
      state: pending
      reason: Bootstrap changes and compatibility deletion require explicit stable-cutover approval.
      recovery: Obtain explicit stable-cutover approval after the stable TypeScript publication contract is ready.
    queue_rank: 190
    order_reason: Prepare the exact candidate after approval and before the irreversible @latest publication transaction.
  status: open
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[fix:3.0.0] Prepare one verified TypeScript stable-cutover candidate after approval."
      severity: critical
      status: open
      order: 3
