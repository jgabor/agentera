id: orvesqszkf
artifact: todo
record:
  severity: degraded
  status: resolved
  description: "[fix:3.0.0] Normalize npm registry JSON before publication convergence checks. Accept scalar, object, and singleton-array shapes from supported npm versions for exact-version integrity and dist-tags; retain string-safe prerelease ordering; add npm 12 fixtures; and prove matching exact package and tag state replays as non-publishing success rather than an integrity conflict."
  readiness:
    capability: build
    reason: The observed npm 12 singleton-array shape produces a false integrity conflict after successful publication; parser parity is concrete and bounded before stable release.
    dependencies:
      []
    blocked: null
    gate: null
    queue_rank: 10
    order_reason: Repair publication convergence before broader contract-gate work; critical bootstrap and startup fixes remain severity-first.
