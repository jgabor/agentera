id: vptlelnadp
artifact: todo
record:
  severity: critical
  status: open
  description: "[fix:3.0.0] Make every documented and machine-returned v3 bootstrap one safe, channel-correct call. Until stable promotion, `agentera route receipt` provenance, `prime` startup and fallback commands, shared-skill examples, and package smokes must use `npx -y agentera@next`, not authorize bare `agentera`. Verify clean, v2, partial, and v3 projects end with `prime` and `doctor` passing without redirecting `@latest`. The matrix must assert the unified startup outcome owned by TODO `rsplcfvosn` and prove healthy projects require no second dashboard or fallback call."
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[fix:3.0.0] Make every documented and machine-returned v3 bootstrap one safe, channel-correct call. Until stable promotion, `agentera route receipt` provenance, `prime` startup and fallback commands, shared-skill examples, and package smokes must use `npx -y agentera@next`, not authorize bare `agentera`. Verify clean, v2, partial, and v3 projects end with `prime` and `doctor` passing without redirecting `@latest`. The matrix must assert the unified startup outcome owned by TODO `rsplcfvosn` and prove healthy projects require no second dashboard or fallback call."
      severity: critical
      status: open
      order: 1
  readiness:
    capability: build
    reason: Retry budget is exhausted after aibyumkzxr-audit-1 and aibyumkzxr-audit-2 both failed bundled-authority scanner acceptance.
    dependencies:
      []
    blocked:
      reason: The stable-v2 exemption lacks exact ordered command equality; the scanner also misses shell prompts, numbered or prose commands, and YAML list mappings.
      recovery: Enforce exact [dry-run, yes] sequence and fail-closed command detection for common Markdown/YAML forms with adversarial coverage.
    gate: null
    queue_rank: 1
    order_reason: Keep this critical bootstrap authority gap visible but ineligible until both scanner defects are corrected under a replacement task.
