id: vptlelnadp
artifact: todo
record:
  severity: critical
  status: open
  description: "[fix:3.0.0] Make every documented and machine-returned v3 bootstrap one safe, channel-correct call. Until stable promotion, `agentera route receipt` provenance, `prime` startup and fallback commands, shared-skill examples, and package smokes must use `npx -y agentera@next`, not authorize bare `agentera`. Verify clean, v2, partial, and v3 projects end with `prime` and `doctor` passing without redirecting `@latest`. The matrix must assert the unified startup outcome owned by TODO `rsplcfvosn` and prove healthy projects require no second dashboard or fallback call."
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[fix:3.0.0] Make every documented and machine-returned v3 bootstrap one safe, channel-correct call. Until stable promotion, `agentera route receipt` provenance, `prime` startup and fallback commands, shared-skill examples, and package smokes must use `npx -y agentera@next`, not authorize bare `agentera`. Verify clean, v2, partial, and v3 projects end with `prime` and `doctor` passing without redirecting `@latest`. The matrix must assert the unified startup outcome owned by TODO `rsplcfvosn` and prove healthy projects require no second dashboard or fallback call."
      severity: critical
      status: open
      order: 1
  readiness:
    capability: build
    reason: Task A vzazfyoobl exhausted both independent Audit attempts; dependent Task B iezyzgirjb remains pending and blocked.
    dependencies:
      []
    blocked:
      reason: The scanner must recognize every Agentera subcommand and every command in a scalar, including nested quoted commands; @latest outside the exact stable pair must reject independently of subcommand or wrapper; producer closure must conservatively cover all constructor import and re-export consumers; generated parity must compare id, path, format, classification, and reason; unused exemptions must reject.
      recovery: Implement all five exact closures, then rerun the audit-2 adversarial probes from ses_032261e52ffekfdnDxqjSnGeE4 for non-whitelisted subcommands, multiple and nested quoted scalar commands, wrapped or split @latest, constructor import/re-export consumers, generated metadata mismatches, and unused exemptions before any Task B execution.
    gate: null
    queue_rank: 1
    order_reason: Keep this critical bootstrap blocker first; Task B cannot run because its sole dependency exhausted evaluation without PASS.
