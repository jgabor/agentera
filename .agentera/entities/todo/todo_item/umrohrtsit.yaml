id: umrohrtsit
artifact: todo
record:
  severity: critical
  status: open
  description: "[fix:3.0.0] Make `agentera doctor --format json` diagnose every retired Agentera-native resource. It currently reports `up_to_date` while stale OpenCode and Codex integrations remain. Emit a bounded degraded or action-required signal with exact resource IDs and targeted preview commands, path or fingerprint evidence only, and no resource contents. Use isolated temporary homes for automated acceptance and keep the real stale installation unchanged as final external evidence until repaired CLI cleanup is explicitly approved and succeeds."
  readiness:
    capability: build
    reason: Doctor must expose the decision-owned retirement boundary without mutating or leaking the stale installation retained as acceptance evidence.
    dependencies:
      - artifact: todo
        id: wwojgufooz
    blocked: null
    gate: null
    queue_rank: 15
    order_reason: Complete resource inventory first, then make doctor report every canonical cleanup target before any real cleanup approval.
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[fix:3.0.0] Make `agentera doctor --format json` diagnose every retired Agentera-native resource. It currently reports `up_to_date` while stale OpenCode and Codex integrations remain. Emit a bounded degraded or action-required signal with exact resource IDs and targeted preview commands, path or fingerprint evidence only, and no resource contents. Use isolated temporary homes for automated acceptance and keep the real stale installation unchanged as final external evidence until repaired CLI cleanup is explicitly approved and succeeds."
      severity: critical
      status: open
      order: 7
