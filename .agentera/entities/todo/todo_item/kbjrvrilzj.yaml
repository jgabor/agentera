id: kbjrvrilzj
artifact: todo
record:
  severity: degraded
  status: open
  description: "[feat:3.0.0] Add general transactional TODO batch apply after the decision-required reconciliation transaction. `agentera state todo apply --input <path|->` must compose create, patch, severity changes, supersede, resolve, reopen, and dependency edits atomically, including local symbolic references between new items. Reuse the canonical snapshot, CAS, idempotency, receipt, and recovery primitive; prove one preview, one apply, and one validation replace serial CRUD without partial publication. Partial for Decision `kpybrsfywb`: this improves arbitrary batch workflows but does not own Markdown reconciliation or release correctness."
  readiness:
    capability: build
    reason: General batch composition is optional workflow efficiency and must reuse, not own, the decision-required reconciliation transaction.
    dependencies:
      - artifact: todo
        id: frpdshynmd
    blocked: null
    gate: null
    queue_rank: 10
    order_reason: Add arbitrary batch composition after the critical reconciliation transaction is complete; it no longer blocks Decision kpybrsfywb.
