id: banfdsinug
artifact: plan
record:
  name: Validation coverage and guidance alignment
  depends_on:
    - gfpdwrtlws
  status: complete
  acceptance:
    - GIVEN the validate namespace behavior exists WHEN focused tests run THEN they cover successful capability validation, successful artifact validation, invalid target feedback, JSON output, and preserved direct validator compatibility.
    - GIVEN help, docs, fixtures, or capability guidance are touched WHEN they are reviewed THEN they make the validation namespace discoverable while preserving existing artifact and capability vocabulary.
    - GIVEN affected capability or artifact guidance changes WHEN validation runs THEN affected capability validation, artifact validation, package or CLI tests, and diff checks pass.
  evidence:
    - Added focused regression coverage in `tests/test_validate_namespace.py` for successful `agentera validate capability hej` text validation matching `scripts/validate_capability.py`, successful capability JSON output, successful `agentera validate artifact --artifact PROGRESS.md` JSON output matching `hooks/validate_artifact.py`, invalid capability target feedback, and validate help discoverability while preserving canonical capability names and artifact labels.
    - "Reviewed the Task 1/2 validate guidance surfaces and made the minimal alignment in `references/cli/agent-ready-state-contract.yaml`: the `validate_namespace` status now reflects implemented behavior, the purpose describes the supported namespace, and evidence sources state that `agentera --help` exposes validate. No capability prose, artifact labels, capability names, schema groups, fixtures, or direct validator vocabulary were renamed."
    - "Focused verification passed: `uv run pytest tests/test_validate_namespace.py -q`; `uv run pytest tests/test_explicit_cli_surfaces.py -q`; `uv run scripts/agentera validate capability hej`; `uv run scripts/agentera validate capability hej --format json`; `uv run scripts/validate_capability.py skills/agentera/capabilities/hej`; `uv run scripts/validate_capability.py --self-validate`; `uv run scripts/agentera validate artifact --artifact PROGRESS.md --file .agentera/progress.yaml --format json`; `uv run hooks/validate_artifact.py --artifact PROGRESS.md --file .agentera/progress.yaml --format json`; `uv run hooks/validate_artifact.py --artifact PLAN.md --file .agentera/plan.yaml --format json`; `uv run hooks/validate_artifact.py --artifact DOCS.md --file .agentera/docs.yaml --format json`; and `git diff --check`."
  plan: nqybsfimjn
