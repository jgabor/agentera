id: nmewuhappg
artifact: plan
record:
  name: Deliver confirmation-gated project persistence
  depends_on:
    - xlrtdarzcs
  status: superseded
  acceptance:
    - GIVEN Task 4 is selected WHEN execution begins THEN an authoritative writer, external approval record, and atomic mutation contract are all identified; otherwise the task blocks and triggers replanning.
    - GIVEN a proposal lacks approval or is rejected WHEN persistence is requested THEN no glossary or documentation state changes, and audit and discuss executions remain mutation-free.
    - GIVEN an approved proposal WHEN the authorized writer persists it THEN each entry contains only shared fields and project-file provenance; approval remains in its owning record outside the entry.
    - GIVEN the same approved mutation repeats WHEN applied in a disposable project THEN it is atomic and idempotent and preserves unrelated confirmed entries.
    - GIVEN invalid approval, provenance, or entry state WHEN mutation is attempted THEN it fails before side effects with a working correction.
  plan: antsflnznb
  superseded_by:
    - dydjhciwcn
  superseded_reason: The completed replacement implements the firm build-owned approval and atomic publication authority established after the original task blocked.
