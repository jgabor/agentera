id: qesbsuazwq
artifact: plan
record:
  name: Retire residual normal-state routing and correction vocabulary
  depends_on:
    - igibjjvytl
  status: blocked
  acceptance:
    - GIVEN marker-active startup and state commands WHEN dispatched THEN they use entity authority without per-handler legacy normal-operation branches.
    - GIVEN marker-absent state WHEN classified THEN upgrade owns classification and recognized v2 aggregate readers remain read-only migration inputs.
    - GIVEN an unsupported removed command WHEN dispatched THEN its correction wins before unrelated migration diagnostics.
    - GIVEN affected command surfaces WHEN exercised THEN arguments, streams, exits, diagnostics, and side effects agree.
    - GIVEN specialized selectors WHEN exercised THEN identity, relationships, cursors, filters, omissions, byte bounds, and direct decision-list behavior remain unchanged.
  test_proportionality: Active and marker-absent dispatch, correction precedence, migration discovery, and focused specialized selectors.
  plan: ntfgalrhbm
  evaluation:
    attempt_count: 2
    failure_count: 2
    last_verdict: fail
    last_failure_evidence: Hook binding, canonical decision contexts, active continuations, Unicode/byte bounds, writer coverage, correction precedence, and migration routes passed. Archived plan-task continuation omits plan identity, reselects active plan, and fails cursor_snapshot_unavailable; a stale --plan-id diagnostic remains. currentAuthorityConsumers scans legacy calls globally but direct aggregate paths only in hand-selected files and misses path.join(root,'.agentera','decisions.yaml') variants.
    provenance:
      attempt_id: qesbsuazwq-audit-2
      source: independent review ses_081de8de0ffeBVajsm02N8qYxT
      recorded_at: 2026-07-20 08:15
      writer_command: agentera state plan record-evaluation
  evidence: qesbsuazwq-audit-2 confirmed all other routing/correction/entity-authority behavior. Preserve both audits; finish cursor-bound archived plan identity and closed aggregate-path scanning in a successor task.
  blocked_reason: "Blocked after 2/2 audits: archived plan-task continuation cannot recover plan identity from its cursor, and aggregate-path consumer scanning is not closed over production TypeScript."
