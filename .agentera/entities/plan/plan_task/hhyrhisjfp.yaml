id: hhyrhisjfp
artifact: plan
record:
  name: Remove redundant correctness work
  depends_on:
    - ddsmbqcrhb
  status: in_progress
  acceptance:
    - GIVEN bounded state-list fixtures WHEN cursor behavior is exercised THEN one canonical traversal still proves exact order, uniqueness, termination, and final-page behavior.
    - GIVEN each additional output representation WHEN one continuation page is rendered and read THEN its cursor and payload remain compatible with the canonical traversal.
    - GIVEN glossary acquisition limits WHEN direct fixtures are read THEN 65,536 bytes and 100 entries pass, 101 entries and invalid UTF-8 fail closed, and one writer-reader compatibility case remains.
    - GIVEN the duplicate Prime guidance and compact structured checks WHEN they are removed THEN their exact guidance, newline, alias deprecation, canonical envelope, and routing assertions remain in the canonical parity suites.
    - GIVEN each simplified testable unit WHEN coverage is counted THEN it defaults to one passing and one failing case except for the named boundary matrices above.
  plan: bhhshalaqm
