id: onlyyjethm
artifact: plan
record:
  name: Qualify source integration
  depends_on:
    - qalythqtiq
  status: pending
  acceptance:
    - GIVEN source behavior is complete WHEN source, type, build, compact, profile, and contract gates run THEN all pass from the local artifact.
    - GIVEN a gate fails WHEN reported THEN its discovery, loading, execution, or integration owner is explicit and earlier evidence is not end-to-end success.
    - GIVEN generated authorities and projections WHEN qualification runs THEN help, schema, commands, contracts, and capability instructions agree.
    - GIVEN each integration gate WHEN regressions run THEN one pass and one fail prove it.
  plan: bcpkchatfu
  evidence:
    - "Root cause: redundant package verification work and serial cold-process startup exhausted generated-overlap. Preserved source/package/build and cold-artifact checks: package 40/40 in 300.636s; overlap 351.496s with 3,918 source tests, 40 package tests, build, 42 activation checks, zero identity/surface failures, and the unchanged 410s window."
    - Abortable scheduler regression proved deterministic ordering at concurrency four, queued cancellation, active-child termination, and close/timer settlement before root removal; independent Audit PASS. Canonical source receipt remains pending on the exact committed retry.
    - "Routing compactness: bounded docs projection reduced real bare Prime to 11,238/12,000 UTF-8 bytes, a 762-byte margin (at least the 562-byte audited margin). Explicit docs stayed rich at 20 returned of 94 and status context stayed valid at 21,514 bytes; unchanged 12,000 budget; Audit PASS."
    - "Activation compactness: package-owner evidence is 229,531/262,144 bytes after replacing the duplicate second manifest with count plus canonical digest bound to complete retained extracted-tree evidence. All 42 activation checks and package 44/44 pass; unchanged evidence bound; Audit PASS."
    - "Lifecycle inventory: the Git-index scanner and ignored Unix-socket regression pass without recursive filesystem scanning. Canonical source receipt remains pending until the implementation and state are committed and the exact source qualification retry runs."
    - Generated-overlap evaluated source identity commit 3b41be521651a78c26be0a0e64d47631c8ff1324, tree df176ac694d213a05f915dfe7268d4f5a2b445f9, working-tree SHA-256 19be0e960bf5a377c6952b24784f9750ed52d033288bf75b7845f768e2bd5ecd, and identity SHA-256 334c8663fc85b290e3bd78de57dcc5ddd3299d5f57be06dc4d03d8850b44165b with canonical Git modes 100644/100755/120000. Tracked and untracked mode-only regressions changed both digests and stale generation selection rejected; non-executable and core.filemode=false noise stayed stable.
    - "Package Prime route deduplication kept each progress and health list/get route exactly once in overlap-shaped degraded output with a 685-byte independent margin under the unchanged 12,000-byte budget. Generated overlap PASS: source 240 files/3,981 tests, package 4 files/44 tests, build PASS, 42 activation checks, zero identity or surface failures; Audit PASS. Canonical source receipt remains pending after commit and exact qualification."
    - Validator-owned nonforgeable assembly snapshots and deep-freezes observations, assembles and cross-validates without caller evidence overrides; ordinary validation and unchanged Barrier B independently re-observe artifacts. Canonical no-receipt qualification passed 10/10 in 397.361s with 22.639s margin; package 44/44, focused source 54/54, typecheck, and Audit PASS. Budgets and receipt order are unchanged; no receipt, candidate, registry, activation, or publication effects occurred. Clean source receipt remains pending after commit.
    - Shared packages/cli/scripts/git-source-tree.mjs identity now hashes deterministic paths, stage-zero modes, and blob or target bytes for generated output and source receipts. core.symlinks=false clean regular-file surrogates for index 120000 canonicalize as symlinks; true/default regular replacements mismatch. Committed executable, symlink, and surrogate-target changes invalidate receipts; irrelevant chmod and core.filemode=false remain stable. Digest-valid legacy and stale receipts reject. Focused release/generated checks passed 91 tests with one platform skip; typecheck and Audit PASS. No receipt, candidate, registry, activation, or publication effect occurred. Final clean source receipt must be reissued after commit.
    - Second rebase onto feat/v3 dev.72 required source-evidence assembly, structured schema parity, and current-generation review fixture corrections; focused release/runtime 247, runtime matrix 3, source/package parity 25, typecheck, build, compact, and capability checks passed; independent Audit PASS. Final clean source/candidate qualification is intentionally deferred to the feat/v3 release workflow per user instruction.
