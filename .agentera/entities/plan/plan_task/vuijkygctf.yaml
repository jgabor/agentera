id: vuijkygctf
artifact: plan
record:
  name: Separate deterministic and probabilistic lock evidence
  depends_on:
    - qifoditbva
    - fbgcvyeexr
  status: complete
  acceptance:
    - GIVEN simultaneous publishers and stale locks WHEN the source owner runs THEN deterministic one-winner, explicit-loser, and cleanup behavior remains proven.
    - GIVEN the stress owner runs WHEN 100 stale-lock races execute THEN every repetition preserves atomicity and cleanup.
    - GIVEN a stress repetition fails WHEN diagnostics are emitted THEN the repetition and violated invariant are named.
    - GIVEN merge and scheduled policies run WHEN lock evidence is selected THEN they follow the risk decision recorded in Task 1.
  test_proportionality: Use a small deterministic source sample and preserve all 100 probabilistic repetitions in stress.
  plan: rmtdgltdgm
  evaluation:
    attempt_count: 2
    failure_count: 1
    last_verdict: pass
    last_failure_evidence: The source stale-lock test gates worker start but does not deterministically hold both reclaimers at the stale-lock contention point, so one worker may complete before the other enters reclamation. The stress loop's postcondition failures are bounded and named, but worker spawn/exit, readiness timeout, JSON parse, and hung-worker failures bypass repetition/invariant formatting and may leave sibling work unsettled. Add controlled stale-reclaim overlap and bounded lifecycle diagnostics for every repetition failure path without weakening the 100-race stress or policy split.
    provenance:
      attempt_id: vuijkygctf-audit-2
      source: independent orchestrate audit attempt 2
      recorded_at: 2026-07-21 01:04
      writer_command: agentera state plan record-evaluation
