id: udueghnaov
artifact: plan
record:
  name: Characterize the fixed migration and reproduce the recovery loop
  depends_on:
    []
  status: complete
  acceptance:
    - GIVEN a recognized marker-absent v2 project under npm or source v3 execution WHEN preview runs THEN entity cutover is required without mutation.
    - GIVEN the recovery command emitted by prime WHEN it runs THEN retrying prime succeeds or recovery fails nonzero with a more specific handoff.
    - GIVEN v1 or unknown source WHEN previewed THEN it is rejected with manual instructions.
    - GIVEN a packed v3 CLI and representative v2 installation WHEN exercised THEN the full forward boundary is observable.
  test_proportionality: One packed fixture, npm/source variants, one recovery-loop case, and one fault per durable boundary.
  plan: psmmjdmrwd
