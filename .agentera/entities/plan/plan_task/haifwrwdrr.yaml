id: haifwrwdrr
artifact: plan
record:
  plan: bhhshalaqm
  name: Confirm runtime batching duration
  status: pending
  depends_on:
    - ktnrvrhoqs
  acceptance:
    - GIVEN the same pinned local conditions WHEN two consecutive current runtime integration runs complete THEN both pass.
    - GIVEN the recorded 129.769s pre-batch baseline WHEN each current shell wall time is compared THEN each is at least 20% below the baseline.
    - GIVEN those two runs WHEN their runtime contract is inspected THEN they preserve the existing 240000ms timeout and authority digest.
