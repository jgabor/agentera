id: jpmwftenzl
artifact: plan
record:
  plan: bhhshalaqm
  name: Correct stale-lock duplicate detection
  status: in_progress
  depends_on:
    - haifwrwdrr
    - hhyrhisjfp
    - izsczmiboc
  acceptance:
    - GIVEN 100 governed same-ID stale-lock races WHEN both writers finish THEN every repetition has exactly one publisher and one canonical duplicate-ID loser with no writer-lock timeout.
    - GIVEN the contention correction WHEN production contracts are inspected THEN the two-second writer-lock deadline and canonical duplicate-ID error remain unchanged.
    - GIVEN the focused repair WHEN verification runs THEN the targeted stress owner, relevant lock tests, typecheck, and build pass without weakened assertions or budgets.
