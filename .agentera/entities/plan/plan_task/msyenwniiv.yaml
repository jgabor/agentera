id: msyenwniiv
artifact: plan
record:
  name: Synchronize release contracts and project state
  depends_on:
    - otmtspkvqk
    - qaabpascat
    - ujelwalvtv
    - lyllpovmgr
    - leakspniav
  status: in_progress
  acceptance:
    - GIVEN verified checked-in version publication WHEN release authorities are synchronized THEN maintainer guidance, workflow documentation, package metadata contracts, and changelog agree without run-number allocation or first-dev.83 assumptions.
    - GIVEN the development authority changes WHEN package registry and suite contracts are validated THEN the registry remains the 3.0.0 suite authority and no ADR-only authority migration is implied.
    - GIVEN implementation completion WHEN typed state is synchronized THEN this plan and required progress records report verified behavior without unrelated TODO, vision, profile, or satisfaction mutation.
    - GIVEN source, typecheck, build, release, workflow, package, compact, and dry-run gates WHEN final verification runs THEN they pass against the same checked-in version contract.
    - GIVEN closeout and deferred scope WHEN artifacts are reviewed THEN they claim no push, npm mutation, stable adoption, credential change, tag, or release beyond preparation of 3.0.0-dev.84.
  plan: pbvgdsqunx
  evidence:
    - Active release guidance, the package-publication authority, verification guidance, and CHANGELOG now use the checked-in development package version; stale run-number allocation and first-dev.83 assumptions remain only in historical typed records or negative tests.
    - The package registry still declares registry.json skills[0].version as persisted suite authority, requires an explicit ADR for any future change, and validates package suite metadata at 3.0.0 while the publishable manifest remains 3.0.0-dev.84.
    - Focused stale-surface, workflow, release-contract, metadata, and registry tests passed; full source passed 4148 tests with 2 platform skips, and typecheck and build passed.
    - "Generated overlap passed, and the full release conjunction passed all 11 gates: source, stress, performance, capacity, package, generated-overlap, typecheck, build, compact, capability-contract, and activation-conjunction."
    - Capability contract, compact, package verification 20/20, package dry-run, Markdown lint, and JSON formatting passed against 3.0.0-dev.84. No push, npm mutation, stable adoption, credential change, tag, or publication occurred.
