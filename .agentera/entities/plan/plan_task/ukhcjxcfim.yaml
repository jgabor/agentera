id: ukhcjxcfim
artifact: plan
record:
  plan: rmtdgltdgm
  name: Close performance owner selection escapes
  status: pending
  depends_on:
    - qifoditbva
    - fbgcvyeexr
  acceptance:
    - GIVEN a performance positional subset is requested WHEN it omits the sole evidence producer THEN the owner rejects it before spawning Vitest with a bounded working correction.
    - GIVEN forwarded arguments contain one or more -- delimiters WHEN owner arguments are normalized THEN only validated options and performance-owned filters reach the runner and delimiter injection cannot broaden selection.
    - GIVEN source, stress, package, unowned, traversal, glob, selector, configuration, output, unknown, or producer-omitting routes are attempted WHEN validation runs THEN every route exits before runner invocation and sentinel evidence proves zero spawns.
    - GIVEN the direct and integration performance owners run WHEN evidence is observed THEN each executes one real 25-sample matrix, emits exactly one bounded schema record, and ordinary source emits none.
    - GIVEN prior progress underclaimed counts or overstated forwarding safety WHEN repair completes THEN a new immutable progress record reports final exact counts and corrected evidence.
