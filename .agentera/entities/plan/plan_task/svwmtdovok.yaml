id: svwmtdovok
artifact: plan
record:
  name: Retire residual normal-state routing and migration vocabulary duplication
  depends_on:
    []
  status: pending
  acceptance:
    - GIVEN normal startup and state operations WHEN dispatched THEN they require active entity authority without per-command legacy behavior.
    - GIVEN non-active project state WHEN classified THEN upgrade alone owns classification while declared migration discovery remains read-only.
    - GIVEN startup, state, upgrade, help, schema, and correction surfaces affected by legacy removal WHEN queried or invoked THEN arguments, streams, exits, diagnostics, and side-effect boundaries agree; unrelated namespaces remain unchanged.
    - GIVEN specialized entity selectors WHEN routed THEN existing identity, relationship, cursor, filter, omission, byte-bound, and direct decision-list behavior remains unchanged.
  test_proportionality: Entity-mode and migration-gate matrices, affected command-surface matrix, and focused specialized-selector cases.
  plan: xqctyflnqu
