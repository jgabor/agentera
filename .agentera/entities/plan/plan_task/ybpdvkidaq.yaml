id: ybpdvkidaq
artifact: plan
record:
  name: M3 · Make bounded summary limits predictable (hykqpkcsix)
  depends_on:
    - ewkfnxucxq
  status: superseded
  acceptance:
    - GIVEN N filtered candidates WHEN limit N is requested THEN N summary rows return even when optional detail exceeds the byte budget.
    - GIVEN IDs-only, selected fields, or continuation WHEN read THEN order, queue rank, counts, omissions, cursors, and exact-get recovery are deterministic.
    - GIVEN full detail cannot fit WHEN summaries succeed THEN the outcome reports success with degradation rather than dropping rows.
    - GIVEN selector and byte-pressure classes WHEN tested THEN each has one success and one bounded degradation or rejection case.
  plan: frkpjqmpnj
  evaluation:
    attempt_count: 3
    failure_count: 2
    last_verdict: fail
    last_failure_evidence: TODO cardinality now passes at 40/60/100, but a cursor created with --limit 10 resumes successfully with --limit 5 even though retrieval authority binds continuation to limit. Encode effective limit and return structured cursor_invalid on mismatch. See packages/cli/src/state/todoDocsEntities.ts:909-911 and state-storage-authority.yaml:285,291.
    provenance:
      attempt_id: ybpdvkidaq-audit-3
      source: Audit ses_03e67c4cbffewdCHwyvmZqIsTi
      recorded_at: 2026-08-02 10:56
      writer_command: agentera state plan record-evaluation
  superseded_by:
    - dnbytwkmiu
  superseded_reason: Replacement preserves 100-row cardinality and closes all cursor request bindings
