id: ybpdvkidaq
artifact: plan
record:
  name: M3 · Make bounded summary limits predictable (hykqpkcsix)
  depends_on:
    - ewkfnxucxq
  status: complete
  acceptance:
    - GIVEN N filtered candidates WHEN limit N is requested THEN N summary rows return even when optional detail exceeds the byte budget.
    - GIVEN IDs-only, selected fields, or continuation WHEN read THEN order, queue rank, counts, omissions, cursors, and exact-get recovery are deterministic.
    - GIVEN full detail cannot fit WHEN summaries succeed THEN the outcome reports success with degradation rather than dropping rows.
    - GIVEN selector and byte-pressure classes WHEN tested THEN each has one success and one bounded degradation or rejection case.
  plan: frkpjqmpnj
  evaluation:
    attempt_count: 2
    failure_count: 1
    last_verdict: pass
    last_failure_evidence: Filtered decision continuations omit --topic and filtered health continuations do not shell-quote --dimension, so valid pages cannot advance deterministically; selector parsing also accepts --fieldsx as --fields. See packages/cli/src/state/decisionEntities.ts:324, packages/cli/src/state/healthEntities.ts:272, and packages/cli/src/cli/commands/state/list.ts:100-122.
    provenance:
      attempt_id: ybpdvkidaq-audit-2
      source: Audit ses_0400e3da4ffeJT375g86agmeVk bounded re-review
      recorded_at: 2026-08-02 03:23
      writer_command: agentera state plan record-evaluation
