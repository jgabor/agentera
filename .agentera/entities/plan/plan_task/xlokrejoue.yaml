id: xlokrejoue
artifact: plan
record:
  name: Eliminate generated-output races
  depends_on:
    - fbgcvyeexr
  status: complete
  acceptance:
    - GIVEN a clean checkout WHEN source verification runs THEN no prior generated checkout output is required.
    - GIVEN source, build, and package commands overlap WHEN outputs are consumed THEN no command observes a partial generated surface.
    - GIVEN package verification runs WHEN package behavior is checked THEN it still uses an extracted installation.
    - GIVEN generation is interrupted WHEN state is inspected THEN the previous complete result remains or failure recovery is actionable.
    - GIVEN generated surfaces need recovery WHEN guidance is consulted THEN ownership and correction are documented once.
  test_proportionality: Cover clean checkout, overlapping readers and writers, interruption, and extracted-package independence.
  plan: rmtdgltdgm
  evaluation:
    attempt_count: 1
    failure_count: 1
    last_verdict: fail
    last_failure_evidence: Clean source verification still has upgrade tests that read checkout dist/capabilities, and clean CI later invokes checkout dist without a checkout build. Generated publication serializes writers but swaps dist and bundle separately, so lock-free readers can observe missing or cross-generation surfaces; its overlap test checks only final writer state. Recovery also deletes the journal when a hadPrevious backup is missing, allowing a partial target to become unrecoverable, with an owner-record crash gap. Remove checkout-output dependencies, design reader-safe generation selection or participation, and make every degraded recovery state explicit/idempotent with deterministic tests.
    provenance:
      attempt_id: xlokrejoue-audit-1
      source: independent orchestrate audit attempt 1
      recorded_at: 2026-07-21 01:31
      writer_command: agentera state plan record-evaluation
