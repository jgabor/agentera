id: xlokrejoue
artifact: plan
record:
  name: Eliminate generated-output races
  depends_on:
    - fbgcvyeexr
  status: blocked
  acceptance:
    - GIVEN a clean checkout WHEN source verification runs THEN no prior generated checkout output is required.
    - GIVEN source, build, and package commands overlap WHEN outputs are consumed THEN no command observes a partial generated surface.
    - GIVEN package verification runs WHEN package behavior is checked THEN it still uses an extracted installation.
    - GIVEN generation is interrupted WHEN state is inspected THEN the previous complete result remains or failure recovery is actionable.
    - GIVEN generated surfaces need recovery WHEN guidance is consulted THEN ownership and correction are documented once.
  test_proportionality: Cover clean checkout, overlapping readers and writers, interruption, and extracted-package independence.
  plan: rmtdgltdgm
  evaluation:
    attempt_count: 2
    failure_count: 2
    last_verdict: fail
    last_failure_evidence: The immutable-generation design fixes clean source and ordinary atomic selection, but current can resolve outside the generations root and be imported; overlap proof runs only 4 focused source files rather than the full owner; a directory at current causes repeated EISDIR and .current-* residue; every .selected generation is retained indefinitely without safe bounded cleanup; and live packaging documentation still contradicts the governed direct-pack behavior. Constrain selected paths, prove full-owner overlap, recover corrupt pointer states, define safe bounded retention, and reconcile the single documentation authority.
    provenance:
      attempt_id: xlokrejoue-audit-2
      source: independent orchestrate audit attempt 2
      recorded_at: 2026-07-21 02:11
      writer_command: agentera state plan record-evaluation
