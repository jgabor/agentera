id: lzhjiqlqsu
artifact: plan
record:
  name: Evaluate and harden the portable cascade
  depends_on:
    - lghihphurk
    - tcgzbnkhtd
  status: complete
  acceptance:
    - GIVEN the frozen synthetic corpus WHEN evaluated THEN every result and failure is attributable to a deterministic or receipt-validation tier.
    - GIVEN offline cascade conformance WHEN measured THEN harmful deterministic and protocol misroutes remain at zero without tuning against hidden or vendor-host data.
    - GIVEN latency measurements WHEN reported THEN deterministic and receipt-validation boundaries remain distinct, while semantic model quality and latency are explicitly host-dependent and unmeasured.
    - GIVEN checkout and packed execution WHEN verified THEN both use the same protocol, phrase authority, shared skill guidance, and synthetic corpus.
    - GIVEN the portable host-owned architecture WHEN evaluation closes THEN no OpenAI credential, ZDR approval, sealed holdout, or provider-specific live benchmark is required for implementation acceptance.
  plan: pxyvjauajx
  evaluation:
    attempt_count: 2
    failure_count: 1
    last_verdict: pass
    last_failure_evidence: A failing or harmful synthetic corpus report still exits zero, causing check verify to report pass; route help also omits evaluate. Propagate report failure through both CLI gates and add failing-corpus plus help regressions.
    provenance:
      attempt_id: audit-lzhjiqlqsu-2
      source: "follow-up audit correction: failed report exit propagation, verify parity, and route help regression gates"
      recorded_at: 2026-07-23 08:25
      writer_command: agentera state plan record-evaluation
