id: ersqcjcbfs
artifact: plan
record:
  name: Unify state mutation under one crash-consistent transaction
  depends_on:
    - dqwsaeloke
  status: complete
  acceptance:
    - GIVEN writer append, automatic projection, or explicit projection repair WHEN state mutates THEN one lock and publication protocol governs every changed file.
    - GIVEN failure after any staged write, archive publication, projection publication, or directory sync WHEN retry runs THEN state converges without summary-only loss or duplicate identity.
    - GIVEN concurrent mutation attempts WHEN they overlap THEN one succeeds or callers receive a retryable conflict without partial publication.
    - GIVEN injected failures at each boundary WHEN tests run THEN recovery and cleanup behavior is proven.
  plan: snzwzclohe
