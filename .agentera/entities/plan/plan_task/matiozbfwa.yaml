id: matiozbfwa
artifact: plan
record:
  name: Prove the fixed packed upgrade path
  depends_on:
    - wohtrrdvrw
  status: pending
  acceptance:
    - GIVEN a representative Git-clean managed v2 fixture WHEN the extracted package performs the sole upgrade THEN expected records and relationships survive, migration sources remain unchanged, valid entity authority activates, and prime succeeds.
    - GIVEN required managed cross-major rewiring WHEN the packed upgrade completes THEN required changes converge while optional lifecycle resources remain unchanged without a selector.
    - GIVEN the same full command runs again WHEN the project is already active THEN it converges without duplicate entities or repeated resource writes.
    - GIVEN ownership-blocked or unsupported state WHEN tested by its owning source lane THEN package integration does not duplicate a separate recovery matrix.
  test_proportionality: One representative extracted-package success fixture, one idempotent rerun, and evidence that failure matrices remain source-lane owned.
  plan: jyritjjnyd
