id: scrtlfqljn
artifact: plan
record:
  plan: rmtdgltdgm
  name: Require every overlap suite to execute
  status: blocked
  depends_on:
    - fbgcvyeexr
  acceptance:
    - GIVEN an owner reporter includes a pending, skipped, todo, failed, or otherwise non-passing suite result WHEN overlap validates coverage THEN the gate fails even if assertionResults is empty or aggregate counts balance.
    - GIVEN exact owner inventory is expected WHEN structured results are checked THEN every inventory file contributes executed assertion coverage, except the one explicitly permitted Darwin assertion-level skip within an otherwise executed file.
    - GIVEN Linux and Darwin pending contracts run WHEN the known conditional assertion is reported THEN Linux accepts exactly that assertion skip, Darwin executes it, package has no pending coverage, and all other suite/assertion omissions fail.
    - GIVEN full source, build, and extracted-package overlap runs WHEN validation completes THEN all owners pass with one complete generation, zero identity or surface failures, and no hidden pending suite coverage.
  evaluation:
    attempt_count: 2
    failure_count: 2
    last_verdict: fail
    last_failure_evidence: All original overlap runtime criteria pass, but reporter projection overwrites contradictory raw nonzero failed/pending suite aggregates before validation. Conditional uniqueness scanning can count comments or strings as executable declarations, malformed policy values can expand or redefine the fixed exception, and adversarial getters/malformed array elements can throw secondary unbounded or non-actionable errors. Preserve contradictory raw evidence, validate a fixed policy schema, use executable syntax-aware uniqueness, and make normalization/diagnostics total over hostile reporter data.
    provenance:
      attempt_id: scrtlfqljn-audit-2
      source: independent orchestrate audit attempt 2
      recorded_at: 2026-07-21 10:11
      writer_command: agentera state plan record-evaluation
