id: ypdtaatnyq
artifact: plan
record:
  name: Consolidate repeated startup and capability work
  depends_on:
    - opphhvqohs
  status: in_progress
  acceptance:
    - GIVEN the dashboard budget scenarios WHEN they run THEN each byte, adversarial-detail, and reconciliation claim remains covered without repeated equivalent state acquisition.
    - GIVEN every capability WHEN canonical entity context is built THEN each receives the expected plan projection and passes semantic checks.
    - GIVEN public startup and compact boundaries WHEN representative checks run THEN hostile aggregates remain ignored and the project remains unchanged.
    - GIVEN each changed testable unit WHEN coverage is reviewed THEN it retains one passing and one failing case unless one side is not meaningful and the task records why.
  plan: uqqfojmzbv
