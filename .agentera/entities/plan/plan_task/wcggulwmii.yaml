id: wcggulwmii
artifact: plan
record:
  name: Ship the one-command contract
  depends_on:
    - xhxchnzrhy
    - uwfilxpkrh
    - pffccgtcro
    - yknigjocnc
  status: pending
  acceptance:
    - GIVEN only the packed development package WHEN a v2 Git fixture runs the public upgrade --yes command THEN entity authority activates and prime succeeds.
    - GIVEN active user guidance WHEN the v2-to-v3 path is followed THEN it shows one apply invocation plus optional dry-run, not internal migration inputs.
    - GIVEN release metadata WHEN synchronized THEN the npm package advances one development build, suite versions remain 3.0.0, and bundle/source references are current.
    - GIVEN release and integration gates WHEN they run THEN full tests, validation, build, package dry-run, extracted-package execution, and proportional safety cases pass.
  plan: zqwisfegxj
