id: xatnspdqch
artifact: plan
record:
  name: Harden reviewed lifecycle and authority gaps
  depends_on:
    - rgyeduwclh
  status: complete
  acceptance:
    - GIVEN `scripts/agentera_upgrade.py` emits canonical `lifecycleStatus` and compatibility `status` metadata WHEN focused tests run THEN build and apply upgrade paths cover `pending`, `noop`, `applied`, `blocked`, `failed`, `skipped`, and unknown workflow-status mapping boundaries as appropriate.
    - GIVEN Realisera/Orkestrera/profile caveats are inspected WHEN wording is corrected THEN a stale-profile caveat no longer implies approval to update app files.
    - GIVEN all machine-readable vocabulary authorities are inspected WHEN schema consistency is enforced THEN `references/cli/app-lifecycle-vocabulary.yaml` has docs-delegation metadata or equivalent focused coverage consistent with the other authority files.
  plan: rphgtlxiqf
