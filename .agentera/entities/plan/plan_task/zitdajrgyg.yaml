id: zitdajrgyg
artifact: plan
record:
  name: Registries & validate subsystems — alias elimination + cast narrowing
  depends_on:
    - jjfklpiget
    - mpnxqpqwfn
  status: complete
  acceptance:
    - GIVEN registries and validators WHEN the `unknown` and `any` local variants are folded to the canonical type THEN typecheck passes and the existing validate/registry fixtures still pass.
  evidence:
    - Local aliases removed in 8 files (4 registries, 3 validate, 1 validate test); unknown/any variants folded to canonical JsonObject.
    - lifecycleAdapters/shared.ts kept as sanctioned `Dict = JsonObject` passthrough (task 6 deletes).
    - "Exception: PackageRegistry.get() return annotated `Record<string, any>` with // cast comment for the 2-level upgrade/appModel chain (documented escape hatch); revisit in task 5/6."
    - typecheck 0 errors; 1145/1145 tests green; 0 fixture drift.
  plan: fuerermwkr
