id: jiupbftubj
artifact: plan
record:
  name: Separate cold performance evidence
  depends_on:
    - qifoditbva
    - fbgcvyeexr
  status: superseded
  acceptance:
    - GIVEN source verification runs WHEN startup and retrieval are checked THEN a bounded functional smoke remains.
    - GIVEN the performance owner runs WHEN authority measurements execute THEN declared scales, cold processes, five repetitions, latency, heap, and output limits are preserved.
    - GIVEN performance evidence completes WHEN results are emitted THEN runner conditions and samples are machine-readable.
    - GIVEN inspector sampling changes WHEN maxima are compared THEN equivalent measurement behavior is proven before reducing overhead.
  test_proportionality: Keep one source smoke and the full authority-required five-repetition performance matrix.
  plan: rmtdgltdgm
  evaluation:
    attempt_count: 2
    failure_count: 2
    last_verdict: fail
    last_failure_evidence: The production performance owner now emits valid bounded JSON, but its command regression does not exercise that real owner path. It sets AGENTERA_PERFORMANCE_OUTPUT_FIXTURE and forwards an unowned .fixture.ts file excluded from inventory, relying on an environment-gated include bypass. Replace the synthetic path with a regression against owned performance evidence, validate forwarded filters cannot bypass ownership, and correct progress claims/counts.
    provenance:
      attempt_id: jiupbftubj-audit-2
      source: independent orchestrate audit attempt 2
      recorded_at: 2026-07-20 23:39
      writer_command: agentera state plan record-evaluation
  superseded_by:
    - lkyflumkrj
    - qgkxggjber
    - trmxcygkoe
  superseded_reason: Completed worker-policy, fixture, and focused runtime evidence cover the blocked performance-owner validation.
