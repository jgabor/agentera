id: jiupbftubj
artifact: plan
record:
  name: Separate cold performance evidence
  depends_on:
    - qifoditbva
    - fbgcvyeexr
  status: complete
  acceptance:
    - GIVEN source verification runs WHEN startup and retrieval are checked THEN a bounded functional smoke remains.
    - GIVEN the performance owner runs WHEN authority measurements execute THEN declared scales, cold processes, five repetitions, latency, heap, and output limits are preserved.
    - GIVEN performance evidence completes WHEN results are emitted THEN runner conditions and samples are machine-readable.
    - GIVEN inspector sampling changes WHEN maxima are compared THEN equivalent measurement behavior is proven before reducing overhead.
  test_proportionality: Keep one source smoke and the full authority-required five-repetition performance matrix.
  plan: rmtdgltdgm
  evaluation:
    attempt_count: 1
    failure_count: 1
    last_verdict: fail
    last_failure_evidence: The performance test constructs complete machine-readable JSON, but the supported test:performance owner command suppresses console output through default test interception. Evidence appears only with an undocumented disable-console-intercept flag, so normal owner output does not satisfy the machine-readable evidence criterion. Expose bounded performance JSON by default through the owner command and test observable stdout; keep source smoke free of performance-only execution cost.
    provenance:
      attempt_id: jiupbftubj-audit-1
      source: independent orchestrate audit attempt 1
      recorded_at: 2026-07-20 23:21
      writer_command: agentera state plan record-evaluation
