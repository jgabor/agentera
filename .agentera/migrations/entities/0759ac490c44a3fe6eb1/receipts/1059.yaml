id: eqetvotbtx
artifact: plan
record:
  name: Make protocol scan exceptions and bundle inventory exact
  status: complete
  depends_on:
    - gqoilsmayw
  acceptance:
    - GIVEN retired-token findings and approved exceptions WHEN the active protocol scan runs THEN findings and exact canonical contexts are consumed one-to-one and duplicate, added, moved, or changed occurrences fail.
    - GIVEN registry-owned and separately generated bundle outputs WHEN inventory is checked THEN every shipped file is classified exactly once and any generated-only addition fails until explicitly classified.
    - GIVEN source and bundled active surfaces WHEN scanned THEN no active retired identity or selector is hidden by an exception and source/generated occurrence parity remains exact.
  plan: pohgeeghnl
