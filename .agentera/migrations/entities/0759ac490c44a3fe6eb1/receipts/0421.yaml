id: tquzntqpjc
artifact: plan
record:
  name: Evidence context output contract
  depends_on:
    - ncwuqasnuu
  status: complete
  acceptance:
    - GIVEN `agentera hej --format json --capability-context inspektera` runs with plan, progress, docs, health, and TODO state available WHEN structured output is returned THEN it includes `evidence_context` with evaluation target, plan criteria, progress verification, docs state, health state, protected-state checks, version checks, residual risks, fallback commands, raw-read policy, and truthful completeness metadata.
    - GIVEN one required evidence state family is unavailable or incomplete WHEN evidence context is built THEN `complete_for_evidence_context` is false or caveated, missing required state is named, existing fallback commands are listed, and `raw_artifact_reads_required` remains false for normal behavior.
    - GIVEN command guidance and tests are scanned WHEN this task completes THEN no new `agentera inspektera`, top-level command, subcommand, route-alias command, or other unsupported capability-name CLI command is introduced.
  evidence:
    - "`scripts/agentera` now emits `evidence_context` only for `agentera hej --format json --capability-context inspektera`; the normal output includes evaluation target, plan criteria, progress verification, docs state, health state, TODO state, protected-state and version placeholders, residual risks, fallback commands, raw-read policy, and completeness metadata."
    - "The context selects targets from CLI-visible plan state in Task 1 order and marks `complete_for_evidence_context` false when a required family such as progress verification is unavailable, while preserving existing fallback commands and `raw_artifact_reads_required: false`."
    - "`references/cli/agent-ready-state-contract.yaml` and Decision 45 contract tests now include `evidence_context` in the structured hej envelope and field-selection contract, with explicit `--capability-context inspektera` semantics and no `agentera inspektera` command."
    - Focused tests cover complete/caveated evidence context, missing required progress state, structured hej field exposure, and Decision 45 contract semantics that forbid `agentera inspektera`. No new top-level command, subcommand, route-alias command, installed app refresh, profile refresh, vision edit, objective-state edit, publication, push, tag, or commit occurred.
  plan: tzdfruwtjr
