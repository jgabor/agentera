id: zyvhcuxogj
artifact: plan
record:
  name: Focused evidence-context regression coverage
  depends_on:
    - tquzntqpjc
    - syzfmsqssv
    - uhbovddpnb
  status: complete
  acceptance:
    - GIVEN a complete evidence-context fixture is available WHEN focused CLI tests run THEN they assert the stable top-level context keys, complete source contract, no unsupported command text, and raw-read-last-resort policy.
    - GIVEN missing plan criteria, missing progress verification, stale docs or health state, unknown protected-state evidence, unsupported capability command usage, and residual-risk preservation are tested WHEN focused tests run THEN each case has a deterministic complete, incomplete, or caveated outcome with exact fallback and caveat assertions.
    - GIVEN Decision 45 contract and field-selection tests run WHEN implementation is complete THEN `evidence_context` is represented in the structured hej envelope and unsupported fields still fail clearly.
    - GIVEN testable state families are covered WHEN this task completes THEN coverage includes at least one available path and one missing or caveated path for plan criteria, progress verification, docs/health state, protected/version state, and residual risks.
  evidence:
    - "`tests/test_query_cli.py` now seeds a complete Inspektera evidence-context fixture and asserts stable top-level keys, exact source-provenance fields for plan/progress/docs/health/TODO/version state, source-contract metadata, raw-read-last-resort policy, fallback commands, and absence of unsupported `agentera inspektera` guidance."
    - Focused cases now cover missing plan criteria, missing progress verification, stale docs and health evidence, protected-state evidence reported as not checked by design, unsupported `agentera inspektera` command usage, and attributed residual-risk preservation with deterministic fallbacks and caveats.
    - Field-selection coverage now asserts sparse hej `evidence_context` output via `--capability-context inspektera`, unsupported hej fields failing without partial stdout, and Decision 45 field-selection contract coverage for `evidence_context`.
    - "`scripts/agentera` now exposes deterministic docs/health current-state caveats and clears the obsolete Task 4 deferred source-contract entry. Focused verification passed with `uv run pytest tests/test_query_cli.py::TestHej tests/test_query_cli.py::TestRoutineStructuredOutput tests/test_decision45_cli_contract.py` (98 tests) and `python -m py_compile scripts/agentera`. No unsupported CLI command was added, and no installed app refresh, profile refresh, vision edit, objective-state edit, publication, push, tag, or commit occurred."
  plan: tzdfruwtjr
