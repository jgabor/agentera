id: ymrgipathn
artifact: plan
record:
  name: Validate namespace contract definition
  depends_on:
    []
  status: complete
  acceptance:
    - GIVEN an agent needs to validate Agentera capability or artifact state WHEN the intended `agentera validate` namespace is specified THEN the supported target families, required inputs, output formats, and non-goals are clear without relying on unsupported capability-name commands.
    - GIVEN current direct validation commands already exist WHEN compatibility expectations are defined THEN their continued availability and observable behavior are preserved as acceptance constraints.
    - GIVEN invalid validation input is possible WHEN the command contract is reviewed THEN valid values, correct syntax, and concrete examples are included in the expected user-facing behavior.
  evidence:
    - Defined the design-only external contract in `references/cli/agent-ready-state-contract.yaml` under `validate_namespace` with supported target families `capability` and `artifact`, required inputs, `text` and `json` output formats, examples, non-goals, and explicit prohibition on unsupported capability-name CLI commands.
    - "Recorded direct-validator compatibility constraints: keep `scripts/validate_capability.py skills/agentera/capabilities/<name>` and `scripts/validate_capability.py --self-validate` available with existing behavior; keep `hooks/validate_artifact.py --artifact <ARTIFACT> --file <PATH> --format text|json` available with existing docs.yaml mapping, JSON envelope, violation wording, and hook behavior."
    - "Substantiated invalid-input expectations from current behavior: `hooks/validate_artifact.py --artifact NOTREAL.md --file .agentera/plan.yaml --format json` returned a JSON failure listing valid artifact labels; invalid capability directory validation reported missing `prose.md` and `schemas/`; the contract now requires valid values, correct syntax, and concrete examples for invalid family, capability, artifact, and format input."
  plan: iojgvxazbl
