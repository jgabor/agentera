id: ncwuqasnuu
artifact: plan
record:
  name: Evidence context inventory and evaluation target contract
  depends_on:
    []
  status: complete
  acceptance:
    - GIVEN existing CLI state outputs, the 2.3.10 TODO, Inspektera guidance, and prior context contracts are inventoried CLI-first WHEN inventory completes THEN implementation evidence records included state, missing state, last-resort raw fallback needs, and the required versus optional evidence matrix.
    - GIVEN plan and progress state can describe more than one possible unit of work WHEN the evidence target contract is specified THEN it defines the selection order, selection reason, no-target behavior, and caveats without requiring raw plan or progress reads for normal evaluation startup.
    - "GIVEN the active plan references the archived 2.3.9 plan WHEN inventory starts THEN the archive exists with `header.status: complete`, and implementation scope treats archival as already handled by Planera activation."
    - GIVEN installed app or profile state is stale WHEN planning and validation proceed THEN stale state is recorded as a caveat and no upgrade, repair, installed app refresh, or profile refresh is run.
  evidence:
    - CLI-first inventory used `agentera hej --format json --capability-context orkestrera`, `agentera plan/progress/docs/health/todo --format json`, and `agentera decisions --format json`; included state covers plan, progress, docs, health, and TODO, while startup context still misses decisions, vision, and profile and decisions expose 40 compacted historical entries with missing fields/caveats.
    - "`references/cli/agent-ready-state-contract.yaml` now records the Task 1 `evidence_context_target_contract`: normal target selection uses CLI state only, chooses in-progress task, first dependency-ready pending task, latest completed task with evidence, then repository-level no-target, and records the selection reason and caveats without raw plan/progress reads."
    - The same contract records the required evidence matrix for evaluation target, plan criteria, progress verification, docs state, health state, TODO state, and source contract; optional/caveated evidence includes decisions, vision, profile, protected-state checks, version checks, and publication/remote state.
    - "The previous plan archive exists at `.agentera/archive/PLAN-2026-05-15-2-3-9-dokumentera-closeout-context-source-contract.yaml` with `header.status: complete`; Task 1 treats archival as already handled by Planera activation."
    - Stale profile-derived state is preserved as a caveat, installed app stale/update-needed state is defined as a caveat if present, and no upgrade, repair, installed app refresh, profile refresh, publication, push, tag, vision edit, objective-state edit, or unsupported `agentera inspektera` command was run.
  plan: tzdfruwtjr
