id: dskktdnyyc
artifact: plan
record:
  name: Implement minimal doctor rendering change
  depends_on:
    - uoaiafiiex
  status: complete
  acceptance:
    - GIVEN text output changes WHEN code is edited THEN the change is limited to doctor rendering and helper wording or compatibility mapping needed to classify up-to-date, outdated, repair-needed, migration-needed, manual-review, optional, update, repair, and blocked next actions.
    - GIVEN JSON consumers depend on doctor fields WHEN implementation is complete THEN the plan either preserves existing values as compatibility aliases or deliberately migrates them with tests and documented compatibility boundaries; do not add an unexamined internal/public vocabulary split.
    - GIVEN dry-run/apply commands are rendered WHEN they appear in text or JSON THEN command strings include correct spacing before flags such as `--dry-run`.
  evidence:
    - "Updated `scripts/agentera_upgrade.py` so doctor app metadata emits canonical statuses: `up_to_date`, `outdated`, `repair_needed`, `migration_needed`, and `manual_review_needed`. Signal statuses now use the same canonical vocabulary, while diagnostic `kind` values remain specific causes except the migration kind, which is now `migration_needed`."
    - Updated doctor rendering so `up_to_date` text says app files are up to date and omits the required-looking `Next:` repair sequence; `dryRunCommand` and `applyCommand` are null for `up_to_date` and manual-review states, while outdated/repair/migration states retain preview/apply/retry safety guidance.
    - Updated `scripts/agentera` hej app-home handling so `up_to_date` produces no app attention item and non-current app statuses render canonical, readable attention labels.
    - Updated focused doctor/hej tests for the deliberate JSON migration instead of retaining compatibility aliases. `uv run pytest tests/test_bundle_freshness.py tests/test_install_root_characterization.py tests/test_query_cli.py -q` passed with 217 tests.
    - "Local live probes now show `agentera doctor` text as `status: up to date` plus `No action needed`, and `agentera doctor --json` reports `status=up_to_date`, `signals=[]`, `dryRunCommand=null`, and `applyCommand=null` with correct command formatting preserved for actionable states by existing tests."
  plan: zlfqmzwpfh
