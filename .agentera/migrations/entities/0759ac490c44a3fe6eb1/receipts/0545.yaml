id: xycqigfhbr
artifact: plan
record:
  name: Inventory vocabulary occurrences and define classifications
  depends_on:
    []
  status: complete
  acceptance:
    - GIVEN the focused vocabulary scan runs WHEN it encounters surviving `bundle` or `SKILL.md` occurrences THEN each in-scope occurrence is classified as canonical, compatibility-preserved, historical, fixture-only, or generic.
    - GIVEN an occurrence does not fit an allowed classification WHEN the focused test runs THEN it fails with enough path/context detail to decide whether to reword, classify, or intentionally fixture the occurrence.
  evidence:
    - Inventory captured focused occurrences across AGENTS.md, README.md, docs/vocabulary.md, references/adapters/opencode.md, scripts/measure_token_payload.py, skills/agentera/SKILL.md, selected capability files, and focused tests in references/cli/bundle-skill-vocabulary.yaml focused_scan.
    - "`uv run pytest tests/test_bundle_skill_vocabulary_authority.py -q` classified all focused `bundle` and `SKILL.md` matches with actionable path, line, term, and text failure output."
  plan: uwuahelugw
