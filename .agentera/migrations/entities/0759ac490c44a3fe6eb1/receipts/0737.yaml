id: phgmjcsyem
artifact: plan
record:
  name: GitHub Copilot adapter repair
  depends_on:
    - mqkobtxxis
    - mrwhnvxtnm
  status: complete
  acceptance:
    - GIVEN supported GitHub Copilot fixture schemas are extracted WHEN corpus building runs THEN normalized records include runtime label, timestamp, conversation key, and tool or command events needed by the startup analyzer.
    - GIVEN malformed Copilot items are encountered WHEN extraction runs THEN degraded counts are bounded and raw payloads are not emitted.
    - GIVEN Copilot records do not match the benchmark window WHEN benchmark windowing runs THEN the status remains distinct from `schema_divergent`.
  evidence: '`scripts/extract_corpus.py` now extracts GitHub Copilot SQLite `turns` tool payloads from JSON data/content and explicit tool columns into normalized `tool_call` records with runtime labels, timestamps, conversation keys, tool names, and arguments needed by `scripts/startup_analysis_contract.py`. Focused tests in `tests/test_startup_analysis_contract.py` verify supported Copilot fixtures produce startup analyzer records and state sequences; malformed Copilot tool JSON produces bounded degraded `schema_divergent` counts without raw payloads; and Copilot records filtered out by benchmark windowing remain `ok / records_extracted` with `record_count: 0`, distinct from `schema_divergent`. Verification: `uv run pytest tests/test_startup_analysis_contract.py -k "copilot or schema_divergent or zero_window"` passed with 6 selected tests; `uv run pytest tests/test_extract_corpus.py -k "copilot or cross_runtime"` passed with 5 selected tests; and the startup contract matrix test passed.'
  plan: oaethqalav
