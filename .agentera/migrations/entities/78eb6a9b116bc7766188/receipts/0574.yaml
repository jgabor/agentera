id: piveinibhn
artifact: plan
record:
  name: "Update tests: remove route engine tests, add ranked-hint orientation tests"
  depends_on:
    - vgmqfqkihv
    - rnpdpreidr
    - wpmebtvrco
  status: complete
  acceptance:
    - GIVEN the test suite is run THEN no test file imports from routing/routeEngine or routeOutput AND routeEngine.test.ts and primeRoute.test.ts are deleted
    - GIVEN selectStatusReadiness is tested with a mock project state having a pending plan THEN the test asserts recommended.capability equals 'orchestrate' AND alternatives is non-empty
    - GIVEN selectStatusReadiness is tested with a mock project state having no plan AND no health issues AND no TODOs THEN the test asserts recommended is non-null (never returns an empty ranked list)
  plan: fidstglvlh
