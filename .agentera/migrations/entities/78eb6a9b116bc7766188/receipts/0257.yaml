id: tsqmskrqsk
artifact: plan
record:
  name: Version and local state sync
  depends_on:
    - lrpdivjlhv
    - gfpdwrtlws
    - banfdsinug
  status: complete
  acceptance:
    - GIVEN the change type is `feat` WHEN release metadata is reviewed THEN version-bearing surfaces are updated for the documented minor-level policy unless the docs policy proves no version-bearing surface applies.
    - GIVEN verification passes WHEN project state is synced THEN TODO, PROGRESS, PLAN, and conditional CHANGELOG/DOCS records include the feature scope and exact verification evidence.
    - GIVEN closeout completes WHEN scope is audited THEN no publication, push, tag, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, or unsupported capability-name CLI command occurred.
  evidence:
    - "Reviewed `.agentera/docs.yaml` semver policy and version file conventions: `feat` maps to a minor bump, current version-bearing surfaces were at 2.3.15, and the documented feature target is 2.4.0. Aligned `pyproject.toml`, `uv.lock`, `plugin.json`, `.github/plugin/plugin.json`, `.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.opencode/plugins/agentera.js`, `skills/agentera/SKILL.md`, `skills/hej/SKILL.md`, and `registry.json` to 2.4.0."
    - Synchronized local state for the D48 validate namespace feature in TODO, CHANGELOG, DOCS audit metadata, PROGRESS cycle 520, and this PLAN Task 4 evidence. The D48 validate TODO was resolved with the feature scope and verification evidence while the D48 smoke/verify and corpus/usage TODOs remain open.
    - "Focused verification passed: `uv run pytest tests/test_validate_namespace.py -q`; `uv run pytest tests/test_explicit_cli_surfaces.py -q`; `uv run pytest tests/test_package_registry.py tests/test_packaged_cli.py -q`; `uv run scripts/agentera validate capability hej`; `uv run scripts/agentera validate capability hej --format json`; `uv run scripts/agentera validate artifact --artifact PROGRESS.md --file .agentera/progress.yaml --format json`; `uv run scripts/validate_capability.py skills/agentera/capabilities/hej`; `uv run scripts/validate_capability.py --self-validate`; direct artifact validators for PROGRESS.md, PLAN.md, TODO.md, CHANGELOG.md, and DOCS.md; and `git diff --check`."
    - "Scope audit: no publication, push, tag, commit, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, package-manager update, unsupported capability-name CLI command, or D48 smoke/verify/corpus/usage namespace work occurred during Task 4 closeout."
  plan: nqybsfimjn
