id: bnvgfubtll
artifact: plan
record:
  name: Usage namespace contract definition
  depends_on:
    []
  status: complete
  acceptance:
    - GIVEN the D48 analytics namespace is reviewed WHEN public command names are compared THEN `agentera usage` is the selected command and `agentera corpus` is recorded as rejected or deferred because this plan reports analytics rather than extracting corpus data.
    - GIVEN the command contract is reviewed WHEN supported options are listed THEN `--format text|json`, `--corpus PATH`, `--project VALUE`, default path precedence, no-write JSON behavior, and unmatched-project empty-report behavior are explicit.
    - GIVEN direct analytics users already call `scripts/usage_stats.py` WHEN the namespace contract is recorded THEN that script's observable CLI behavior is preserved as a compatibility requirement.
    - GIVEN invalid usage namespace input is possible WHEN diagnostics are specified THEN unsupported formats or malformed facade requests return valid values, syntax, examples, and nonzero exits without listing private corpus-derived project IDs.
  evidence:
    - Added `usage_namespace` to `references/cli/agent-ready-state-contract.yaml` with public command `agentera usage`, rejected/deferred command `agentera corpus`, rationale that this plan reports usage analytics rather than extracting corpus data, supported flags `--format text|json`, `--corpus PATH`, and `--project VALUE`, path precedence, JSON no-write behavior, unmatched-project empty-report behavior, missing-corpus guidance, compatibility constraints, and non-goals.
    - Preserved direct `scripts/usage_stats.py` as the compatibility engine and explicitly recorded that JSON output remains the existing top-level analytics payload rather than a wrapper envelope.
  plan: hzpbdwvadx
