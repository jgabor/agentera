id: divdnkhyqn
artifact: plan
record:
  name: Benchmark and trigger boundary
  depends_on:
    []
  status: complete
  acceptance:
    - GIVEN the TODO cites `capability_prose_read_counts.planera=5` WHEN benchmark evidence is inspected THEN the exact retained report, command, fixture, or trace that emitted the count is named.
    - GIVEN each Planera prose read source is classified WHEN the boundary is recorded THEN reads are labeled as runtime routing, test harness setup, fixture replay, validation, legitimate capability-development reads, or avoidable normal invocation reads.
    - GIVEN optimization scope is selected WHEN implementation starts THEN only avoidable normal invocation reads are targeted and valid prose-read exceptions remain explicit.
  evidence:
    - Retained report `/home/jgabor/.local/share/agentera/benchmarks/startup-state/latest-report.json` before implementation emitted `capability_prose_read_counts.planera=5` in a `since_previous_benchmark` window generated by `mage bench:startupState`; the aggregate report is privacy-safe and does not retain raw transcript text or full local paths.
    - The classifier treats read/grep/glob events touching `skills/agentera/capabilities/`, `skills/agentera/SKILL.md`, or `skills/agentera/protocol.yaml` as `capability_prose_read`, so the measured pressure is capability prose/startup guidance access rather than persisted artifact reads.
    - Scope targets avoidable normal Planera invocation reads only; prose reads remain valid for editing Planera, resolving ambiguity, validating behavior outside the compact contract, or investigating benchmark/read-trigger evidence.
  plan: dmdkskyqxj
