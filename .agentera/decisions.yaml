decisions:
  - number: 79
    date: 2026-07-06
    question: How should the status capability prose be designed to maximize
      effectiveness and token efficiency under the v3
      source_contract.capability_startup contract, and which v2-era residue
      should be cut from the most frequently invoked capability surface?
    context: Status is invoked on every /agentera session — first impression and the
      most frequently called capability. Per D57 (firm) the instructions.ts
      module is canonical; per D65 (firm) the runtime serves it via agentera
      prime --context <name>. Per D71 (firm but unreflected in status prose) RFC
      2119 modal vocabulary is the convention enforced in
      capability_schema_contract.yaml#modal_vocabulary. Per D72 (firm but
      straggler in status prose) the bundle to app rename landed in prime output
      but status prose still said bundle.status. The
      source_contract.capability_startup field declares
      complete_for_capability_startup=true and
      raw_artifact_reads_required=false, providing a machine-readable startup
      contract; the prose nonetheless triple-stated the rule (use prime, do not
      read raw .yaml), restated forbidden-command lists (drifted — prose forbade
      agentera plan while v3 uses agentera state plan), re-enumerated all
      protocol token IDs (drift risk versus protocol.yaml), and inlined ~3K
      chars of install-recovery procedure (AGENTERA_HOME broken-copy, 1.x
      managed marker cleanup, npx skills update cross-runtime leak,
      bundle.status inspection gate) that belongs to the upgrade capability and
      agentera doctor command. SKILL.md and instructions.ts both defined the
      dashboard rendering contract with drifted shapes (SKILL.md said
      health/todo/plan/objective/profile; instructions.ts said
      health/todo/plan/optim/profile — different field orders; SKILL.md showed
      all four severities on todo summary; instructions.ts drops normal and
      routes it through attention). Discuss ran a five-question deliberation
      over philosophy, ownership, scope, structure, and synthesis mode.
    alternatives:
      - name: Direct contract — lean on source_contract; status owns dashboard
        status: chosen
        tradeoffs: Prose only states what the contract cannot encode (dashboard
          template, routing priority tree, exit markers, safety rails,
          conventions). Source_contract.capability_startup mechanizes what the
          prose previously re-encoded (complete_for_capability_startup,
          raw_artifact_reads_required, cli_fallback list). Cuts ~7K chars of
          redundant rules (-42% total). Status prose owns the dashboard
          contract; SKILL.md delegates. Trust that the runtime serves the prose
          via prime --context (D65) and that the source_contract fields are
          machine-readable by capable models.
        win_condition: Prose drops to ~10K chars; v2-era field and command names gone
          (no bundle.status, no agentera plan, no --install-root, no
          AGENTERA_HOME resolver); SKILL.md dashboard block replaced with a
          pointer to status prose; routing priority tree and exit markers
          preserved verbatim; D71 modal vocab applied throughout; D72
          terminology applied throughout.
      - name: Self-contained operator manual — defense-in-depth
        status: rejected
        tradeoffs: Preserve every rule in prose. Apply D71 + D72.
        reason_rejected: Duplication persists and drift will recur on the next contract
          change; proven drift in this audit (forbidden-command list said
          agentera plan while v3 uses agentera state plan; prose gated on
          bundle.status while prime emits app.status). Defense-in-depth is the
          source_contract's job — the contract field is the canonical,
          machine-readable declaration; re-stating it in prose waters down the
          contract as the source of truth. D71 modal vocab is the weak-model
          mitigation (RFC 2119 keywords), not prose duplication.
      - name: Hybrid — contract spine + relocated recovery
        status: rejected
        tradeoffs: Lean on contract for startup; keep AGENTERA_HOME recovery flow with a
          pointer to references/cli/upgrade-repair-wording.md.
        reason_rejected: Keeps the procedural content somewhere — adds a pointer to
          maintain. Status's actual rendering obligation for upgrade state is
          one paragraph (render project_integration.message; preview dry-run;
          ask before apply); the procedural recovery content is upgrade/doctor
          capability territory, not status. A pointer is unnecessary; the
          rendering rule is what stays.
    choice: Direct contract — lean on source_contract.capability_startup; status
      prose owns the dashboard rendering contract (SKILL.md delegates); cut all
      procedural install-recovery content (status renders project_integration
      fields when present and routes to upgrade/doctor for procedure);
      three-phase structure (Dashboard rendering then Briefing then Routing
      suggestion); apply D71 RFC 2119 modal vocabulary throughout; apply D72
      bundle-to-app terminology throughout.
    reasoning: The source_contract.capability_startup field exists to be the
      machine-readable contract between the CLI and the host; re-encoding it in
      prose creates a second source of truth that drifts, as proven by the audit
      findings (forbidden-command list drift, bundle.status straggler). Status's
      actual obligations are (1) render the dashboard per a canonical template,
      (2) suggest a concrete next action with a named artifact item, (3) emit a
      mandatory exit marker, (4) stay read-only. Procedural install recovery
      belongs to the upgrade capability and agentera doctor command, not status.
      Owning the dashboard template in status prose (where the only job is
      briefing and routing) aligns ownership with responsibility and eliminates
      the SKILL.md and instructions.ts drift class. D71 modal vocab addresses
      the weak-model failure mode (instructions read literally as user commands)
      without duplicating contract-encoded rules.
    confidence: firm
    feeds_into: packages/cli/src/capabilities/status/instructions.ts,
      skills/agentera/SKILL.md,
      skills/agentera/capabilities/status/schemas/triggers.yaml,
      skills/agentera/capabilities/status/schemas/validation.yaml,
      references/cli/routing-model.md, capability_schema_contract.yaml, TODO.md
    outcome: "Status prose rewritten from 18,035 to 10,389 chars (-42%). Three
      phases (Dashboard rendering then Briefing then Routing suggestion). D71
      RFC 2119 modal vocab applied (5 MUST, 5 MUST NOT). D72 terminology applied
      (bundle fields replaced with app fields). v2-era residue cut:
      AGENTERA_HOME broken-copy recovery (~1.5K), 1.x managed marker paragraph,
      npx skills update cross-runtime leak, bundle.status gate procedure, Step
      0.5 CLI-owned checks (~1K), triple-stated do-not-read-raw-yaml rules
      (~800), duplicate forbidden-command list (~400, also v2-era agentera plan
      names), contract-values token enumeration (~400, replaced with one-line
      reference to protocol.yaml), artifact-path-resolution subsection (~700,
      delegated to SKILL.md), agentera query reference (unported — actual v3 is
      agentera state query), PROFILE.md global path detail (prime profile.path
      surfaces), PROGRESS.md recency heuristic mention (prime health.stale
      surfaces). Generic greeting wording rephrased to conversational smalltalk
      — drops greeting terminology entirely. SKILL.md dashboard rendering block
      (22 lines) replaced with 7-line pointer delegating to status prose.
      triggers.yaml T2 entry dropped hello/hi greeting patterns and Greetings
      description (relics from when status was named hej). routing-model.md
      Layer 1 hej mention and Layer 5 greetings mention cleaned up. 1271/1271
      vitest green; typecheck 0 errors; capability validation PASS;
      capability-contract self-validation PASS; protocol internally consistent;
      artifact compaction within budget. Preserved verbatim (routing priority
      tree with 8-bullet decision logic plus anti-regression rule, ASCII
      dashboard template, ≤120-word output constraint, exit marker rule with
      EX1-EX4, 6 safety rails with D71 modal cleanup, cross-capability
      integration paragraph). TODO.md status-audit task and
      prose-to-instructions rename task marked resolved in this commit.
      Satisfaction deferred to the user per discuss rules."
  - number: 80
    date: 2026-07-06
    question: How should capability instruction structure be standardized across all
      12 capabilities, using status (D79) as the reference implementation while
      retaining each capability's unique workflow shape?
    context: "D79 (firm) rewrote the status capability prose using direct-contract
      philosophy, three-phase structure, and D71/D72 modal vocabulary and
      terminology cleanup. The 11 remaining capability audits (TODO.md lines
      17-27) need a shared design principle to audit against. D71 (firm) RFC
      2119 modal vocabulary is applicable to all 12. D57/D65 (firm) make
      instructions.ts the canonical module served via agentera prime --context.
      A survey of all 12 capability instructions.ts files found five universal
      sections already present across all 12 (Purpose/Visual identity, State
      artifacts, Safety rails, Exit signals, Cross-capability integration) plus
      a sixth (Workflow phases) with four-plus genuinely different shapes:
      linear (status, research, audit), conversational loop (discuss),
      cycle-based (build, optimize), mode-split (vision, document, profile,
      design, plan), and multi-surface orchestration (orchestrate). Three
      capabilities also have domain-specific content sections that do not fit
      the spine (design: Colors/Typography; profile: PROFILE.md template;
      research: deliverable template). Discuss ran a deliberation over
      abstraction level, spine sections, mandatory vs per-capability scope,
      shape-constraint risk, output artifacts, and audit cadence."
    alternatives:
      - name: Six-section spine, five constrained plus Workflow free-form
        status: chosen
        tradeoffs: "A common skeleton (Purpose, State artifacts, Workflow phases, Safety
          rails, Exit signals, Cross-capability integration) that all 12 share.
          Five low-risk sections are shape-constrained. Workflow phases is
          mandatory-as-section but free-form in content. Domain content sections
          (design: Colors/Typography; profile: PROFILE.md template; research:
          deliverable template) are per-capability optional flex sections.
          Getting started use-case examples fold into Cross-capability
          integration as the inverse of feeds-into/triggers/informed-by.
          Personality sections relocate to vision artifact or SOUL.md (tracked
          as TODO). Status (D79) is the reference implementation.
          references/cli/capability-instruction-structure.md captures the spine
          spec, five workflow shapes as named taxonomy, and a per-capability
          deviation table filled incrementally as each audit completes."
      - name: Single template, all 12 conform
        status: rejected
        tradeoffs: One structure all capabilities inherit. Maximally consistent.
        reason_rejected: "Forces capabilities into a shape that does not fit — discuss
          scratchpad loop into a linear phase model, build/optimize cycles into
          step-numbered phases, orchestrate three surface templates into a
          single workflow. Proven risk: the four-plus genuinely different
          workflow shapes resist a single template without flattening the
          distinctions that make each capability effective."
      - name: Minimal shared rules, per-capability self-determination
        status: rejected
        tradeoffs: Only cross-cutting rules (D71 modal vocab, direct contract, mandatory
          exit marker, output budget). Each capability owns its own phase
          structure, section ordering, and procedural depth. No spine.
        reason_rejected: Too loose — no spine to audit against. The 11 remaining audits
          would each discover the same structural principles independently or
          miss them entirely. D79 direct-contract philosophy would not propagate
          without a capturing decision.
    choice: "Six-section spine (Purpose, State artifacts, Workflow phases, Safety
      rails, Exit signals, Cross-capability integration). Five low-risk sections
      shape-constrained (Purpose: glyph plus one-paragraph purpose; State
      artifacts: reads/writes/consumes plus path resolution with per-capability
      artifact-schema subsections allowed; Safety rails: critical block with
      MUST NOT rules; Exit signals: EX1-EX4 with glyph marker; Cross-capability
      integration: feeds-into, triggers, informed-by, plus getting-started
      use-case examples folded in as the inverse of feeds-into/triggers).
      Workflow phases is mandatory-as-section but free-form in content. Domain
      content sections are per-capability optional flex sections. Five workflow
      shapes named as taxonomy: linear (status, research, audit), loop
      (discuss), cycle (build, optimize), mode-split (vision, document, profile,
      design, plan), multi-surface (orchestrate). Status (D79) is the reference
      implementation. D80 plus
      references/cli/capability-instruction-structure.md are the output
      artifacts. Each future capability audit runs as a discuss session against
      D80 plus the reference doc; the reference doc deviation table is filled
      incrementally."
    reasoning: The five low-risk sections are already universal across all 12
      capabilities and are either protocol-bound (Safety rails, Exit signals) or
      structurally consistent (Purpose, State artifacts, Cross-capability
      integration). Shape-constraining them is safe and eliminates drift.
      Workflow phases has four-plus genuinely different shapes that resist a
      single template — constraining them would flatten discuss conversational
      loop into fake linear steps, force cycle-based capabilities into
      step-numbered phases that do not capture the iterative nature, or squeeze
      orchestrate multi-surface model into a single workflow. The five-shape
      taxonomy names the variation without constraining it. The reference doc
      deviation table captures where each capability diverges from the spine,
      filled incrementally during each audit. Getting started folds into
      Cross-capability integration because invocation guidance is the inverse of
      feeds-into/triggers — same relationship, consumer perspective. Personality
      sections relocate to vision artifact or SOUL.md because conversational
      voice rules belong in identity, not per- capability prose. Status serves
      as the concrete reference implementation because it is the only capability
      already rewritten under direct-contract philosophy with D71/D72 applied.
    confidence: firm
    feeds_into: packages/cli/src/capabilities/<name>/instructions.ts,
      references/cli/capability-instruction-structure.md,
      capability_schema_contract.yaml, TODO.md
    outcome: Decision captured. references/cli/capability-instruction-structure.md
      carries the spine spec, five workflow shapes taxonomy, per-capability
      deviation table skeleton (11 rows marked not-yet-audited, status marked
      complete via D79), and domain content flex section guidance. TODO.md
      receives Personality-relocation entry. The 11 remaining capability TODOs
      (lines 17-27) now run against D80 plus the reference doc.
  - number: 81
    date: 2026-07-06
    question: How should the vision capability instructions be audited and rewritten
      to align with D80's six-section spine, retire v2-era residue, and adopt
      the D79 direct-contract philosophy while preserving vision's
      Socratic-conversation character?
    context: "D80 (firm) sets the six-section spine; D79 (firm) set status as the
      reference implementation with direct-contract delegation, D71 modal vocab,
      D72 bundle-to-app terminology, and v2-era residue retired. TODO.md line 16
      lists the vision capability audit as a v3.0.0 docs task. A pre-flight
      survey of vision's existing 17,076-char instructions found 25 drift
      findings: v2-era command references (agentera describe, agentera lint,
      agentera decisions, agentera query), v2-era env var resolver flow
      ($AGENTERA_PROFILE_DIR / $XDG_DATA_HOME), v2-era canonical filenames
      (CLAUDE.md, PROGRESS.md as direct reads when prime already serves them via
      vision_startup_context), hand-enumerated protocol token ID block
      duplicates protocol.yaml, host-bootstrap Artifact path resolution
      subsection duplicates SKILL.md lines 143-156, ghost contract Section 24
      reference (contract.md ends at Section 23 in v3), triple-stated
      exit-marker obligations, duplicated Step 4 pre-write self-audit block
      between Create and Refine modes, six-arc Socratic conversation (effective)
      wrapped in narration-voice riffs (drift), Getting started section placed
      outside D80 §6 Cross-capability integration, validation V6
      principle_brevity rule declared but linter does not enforce and live
      vision.yaml intentionally violates. Ground-up workflow review surfaced six
      substantive decision points: Step 0 handshake with prime --context vision,
      CLI-driven mode detection via source.exists, Replace mode reads archived
      vision as corrective context, profile-stale handling, principle brevity
      rule fate, and whether to add a 7th Socratic arc for anti-vision.
      Personality/SOUL.md TODO entry (line 48) flagged a conflict: Step 3's
      hardcoded 'sharp colleague' personality duplicating the vision artifact's
      identity.voice field."
    alternatives:
      - name: D79-pure direct-contract adoption with personality-neutral Step 3,
          mode-split shape preserved
        status: chosen
        tradeoffs: "Adopts the D79 direct-contract clause as the opening paragraph
          (vision MUST source state from agentera prime --context vision; trust
          source_contract.capability_startup_complete; cli_fallback only as last
          resort). Step 1 sources bundled state from vision_startup_context
          (progress, health, todo, profile path) and runs cli_fallback only for
          fallback_only families (decisions, vision self, design). Filesystem
          reads preserved for items prime does not surface: directory structure,
          dependency manifests, README.md, AGENTS.md, source files, DESIGN.md at
          project root, git log. Step 3 adopts vision artifact identity.voice
          when available; before a vision exists (Create greenfield), runs with
          neutral operational stance (Socratic, push past safe answers) and lets
          personality emerge from arc 5 (identity) captured into the new
          artifact — this prevents the capability from pre-empting the user's
          articulation and preserves the per-project identity principle. Step 0
          handshake made explicit; mode detection CLI-driven via agentera state
          query vision source.exists; Replace mode reads archived
          vision-{date}.yaml as corrective context; Pre-write self-audit hoisted
          (DRY) and referenced from both Create and Refine Step 4; Visual
          identity stub folded into Purpose (no §1 sub-sections per D80);
          Getting started folded into §6 as Invocation guidance per D80 §6. v2
          residue retired: 5 command forms corrected, env-var resolver flow
          removed, contract Section 24 ghost cut, triple-stated exit-marker
          decoration collapsed to Exit-signals section only, narration-voice
          riffs cut to one Step 3 operational-stance line, host-bootstrap
          Artifact path resolution subsection delegated to SKILL.md,
          Contract-values token enumeration block removed (protocol.yaml is the
          authority). Principle brevity rule cut from prose Output constraint
          and validation V6 with ID gap preserved (linter never enforced;
          abstraction and filler checks cover principle quality holistically).
          Validation V7 (exit_marker_required) unchanged. Anti-vision question
          folded into Step 3 arc 4 (The Direction) as a sub-prompt rather than
          expanded into a 7th arc. Profile-stale handling added: carry caveat
          forward; never invoke profile to refresh during a vision cycle (added
          as MUST NOT safety rail). Result: 17,076 → 16,667 chars (-2.4% after
          subagent-driven prose-tightening pass; below original). Modal vocab 3
          MUST + 9 MUST NOT + 4 SHOULD, higher than status because vision writes
          artifacts and has more protocol-force rules). Prose-tightening pass
          (subagent-driven) trimmed redundant restatements, speculative
          commentary, and duplicate mode-header boilerplate while preserving all
          substantive audit decisions: 17,678 → 16,667 (-5.7% from initial
          rewrite; below original 17,076 — vision's Socratic 6-arc narrative and
          vision.yaml shape block justify a longer floor than status's 10,389;
          modal vocab 3 MUST + 9 MUST NOT + 4 SHOULD, higher than status because
          vision writes artifacts and has more protocol-force rules). New
          D80-rule: capability instructions MUST NOT hardcode personality when
          an owning artifact (vision.yaml identity) defines it; reference the
          artifact, do not re-assert its content."
      - name: Aggressive cut to match status length (~10,000 chars)
        status: rejected
        tradeoffs: Match status's 10,389-char floor by cutting the Socratic 6-arc
          conversation or the cross-capability integration surface.
        reason_rejected: "Vision's effectiveness depends on the Socratic conversation:
          the 6 arcs map 1:1 to the vision.yaml sections (north_star, personas,
          principles, direction, identity, tension). Cutting arcs would break
          the artifact-shape contract. Cross-capability integration has 7 named
          relationships (feeds build, profile, research, audit, design,
          document, plan) plus 4 invocation-guidance bullets per D80 §6 — all
          load-bearing. A 10K target would shred vision's substance; 17,678 is
          the honest floor for this capability."
      - name: Keep ≤20-words-per-principle rule, rewrite live artifact to comply
        status: rejected
        tradeoffs: Preserves the stated rule and enforces via linter; rewrites
          .agentera/vision.yaml principles to ≤20 words each.
        reason_rejected: Live vision artifact's principles deliberately carry 30–60-word
          rationales (e.g., 'One agent, not a team' explains the
          multiplicity-under-the-hood model, 'The CLI is the runtime' grounds
          package boundaries). Truncating to ≤20 words would strip the rationale
          that makes principles actionable for autonomous development. The
          MUST-state-tradeoff / SHOULD-name-prevention /
          MUST-NOT-name-implementations rules already cover principle shape; the
          holistic verbosity/abstraction/filler linter checks enforce brevity
          without an artificial word ceiling. Rule was YAGNI from inception
          (declared but unenforced since v2); cut rather than retrofit.
    choice: "D79-pure direct-contract adoption with personality-neutral Step 3,
      mode-split shape preserved. New D80-rule: capability instructions MUST NOT
      hardcode personality when an owning artifact defines it; the instructions
      reference the artifact (vision.yaml identity.voice) without re-asserting
      its content. Applies to all 10 remaining audits."
    reasoning: "The direct-contract delegation mirrors status (D79) and stops vision
      from re-encoding state rules that source_contract.capability_startup
      already mechanizes. The personality-neutral Step 3 follows the per-project
      identity principle (vision.yaml line 3): if vision hardcodes 'sharp
      colleague' before the user articulates their identity, the capability
      pre-empts Step 3 arc 5 (identity) — the very question designed to surface
      the user's voice. The 7th Socratic arc (anti-vision) was rejected because
      The Tension arc already surfaces scope boundaries; folding anti-vision
      into The Direction as a sub-prompt captures the intent without expanding
      surface. The principle brevity cut aligns
      prose↔validation↔linter↔artifact, removing drift rather than papering over
      it. CLI-driven mode detection via source.exists removes one user prompt;
      Replace reads archived vision because the corrective context is valuable
      and otherwise lost. Profile-stale handling prevents state mutation outside
      vision's contract."
    confidence: firm
    feeds_into: packages/cli/src/capabilities/vision/instructions.ts,
      skills/agentera/capabilities/vision/schemas/validation.yaml,
      references/cli/capability-instruction-structure.md, TODO.md,
      .agentera/decisions.yaml
    outcome: "Decision captured. Vision instructions rewritten: 17,076 → 16,667
      chars (-2.4% from original — subagent-driven prose-tightening pass landed
      vision below its starting length despite structural additions); D71 modal
      vocab compliant (3 MUST / 9 MUST NOT / 4 SHOULD); D72 app terminology; D79
      direct-contract delegation in opening paragraph; D80 six-section spine
      (Purpose glyph-inline, State artifacts with vision.yaml shape subsection,
      Workflow phases mode-split with shared pre-write self-audit hoist, Safety
      rails with profile-refresh MUST NOT added, Exit signals, Cross-capability
      integration with Invocation guidance folded). Validation V6
      (principle_brevity) removed with ID gap preserved; V7 unchanged.
      references/cli/capability-instruction-structure.md deviation table vision
      row updated to Complete (D81) with notes on vision.yaml shape subsection
      and shared pre-write self-audit hoist. TODO.md line 16 vision audit marked
      resolved. New D80-rule (capability instructions MUST NOT hardcode
      personality when an owning artifact defines it) propagates to all 10
      remaining audits. The 10 remaining capability audits (discuss, research,
      plan, build, optimize, audit, document, profile, design, orchestrate) now
      run against D80, D81, and the reference doc."
  - number: 82
    date: 2026-07-09
    question: How should the discuss capability instructions be audited and
      rewritten to align with D80 six-section spine, D79 direct-contract
      philosophy, D81 voice delegation, and CLI-first artifact access, while
      adopting deliberation-craft improvements inspired by the Wayfinder skill?
    context: 'Third capability audit (D79 status, D81 vision done). Discuss is the
      only conversational-loop shape and is entry-point-adjacent:
      user-impression impact is high. Pre-flight grounding found v2-era ghosts:
      `agentera describe` and `agentera decisions` commands (v3 uses `agentera
      schema` and `agentera state decisions`); `$AGENTERA_PROFILE_DIR` raw reads
      (v3 serves `deliberation_context.profile.path`); contract Section 24
      reference (contract.md tops at Section 23); hardcoded "sharp colleague"
      personality (D81 violation — vision references `identity.voice` instead);
      re-encoded source_contract truth tables (the real
      `complete_for_normal_deliberation_context` and `raw_artifact_read_policy`
      fields are valid but the prose duplicated them); canonical-filename reads
      (DECISIONS.md and VISION.md and OBJECTIVE.md and TODO.md) when
      `canonical_artifact_label` is the artifact id. Verified-real v3 surface
      retained: `agentera check lint --artifact decisions --text` pre-write
      self-audit, runtime-native question tool names, per-decision
      `context_complete` / `missing_fields` / `compacted` / `caveats` /
      `satisfaction.review_needed` fields.'
    alternatives:
      - name: Full D79 delegation with Wayfinder-inspired deliberation-craft additions
        description: "One lean delegation to `deliberation_context` and
          `raw_artifact_read_policy`; cut re-encoded truth tables and manual
          schema lookups. Keep discuss-specific judgment (satisfaction
          authority, do-not-reconstruct). Adopt four compressed Wayfinder
          adaptations: destination-fixes-scope startup, Ruled-out scratchpad
          field, fog-aware readiness check at Done (name remaining fog; flag
          firm-while-foggy as tension without blocking exit), and reframing the
          skip-to-implementation rail as an edge signal. Add steering rules
          (breadth-before-depth, ask-user-to-sketch not produce)."
        status: chosen
      - name: Status-quo reformat against D80 spine only
        description: Apply the six-section spine and cut v2 residue but leave the
          deliberation process unchanged — no fog or readiness model, no
          steering rules.
        status: rejected
    choice: Full D79 delegation with Wayfinder-inspired additions. Content and
      process challenged, not just reformatted.
    reasoning: "The fog-aware readiness check fills discuss's only real gap:
      termination had no quality signal, so confidence was whatever the user
      declared with no remaining-fog warning. Wayfinder's fog-vs-sharp test
      (\"can you phrase the question now\") gives exactly the missing signal,
      compressed to one Done-step sentence. The other adaptations are
      one-line-each and replace weaker or missing content rather than piling on.
      Reify-as-produce was rejected to avoid design and plan role overlap;
      ask-user-to-sketch keeps discuss verbal-first."
    confidence: firm
    feeds_into: packages/cli/src/capabilities/discuss/instructions.ts,
      skills/agentera/capabilities/discuss/schemas/artifacts.yaml,
      skills/agentera/capabilities/discuss/schemas/validation.yaml,
      skills/agentera/capabilities/discuss/schemas/exit.yaml,
      references/cli/capability-instruction-structure.md, TODO.md,
      .agentera/decisions.yaml
    outcome: 'Decision captured. Discuss instructions rewritten: 13,816 to 12,045
      chars (-13%). D80 six-section spine; conversational-loop shape named "The
      deliberation loop" (section 3). D79 direct-contract:
      source_contract.capability_startup delegation replaces re-encoded truth
      tables and manual schema lookups. D81 voice delegation: personality
      section cut, references identity.voice plus neutral operational stance in
      Create mode. CLI-first artifact access: all canonical-filename reads
      (DECISIONS.md, VISION.md, OBJECTIVE.md, TODO.md) replaced with artifact-id
      references and CLI mediation; the feeds_into example models the [vision]
      artifact-id convention (canonical_artifact_label confirms the id form). v2
      ghosts cut: `agentera describe`, `agentera decisions`,
      `$AGENTERA_PROFILE_DIR`, contract Section 24, contract.md pointer,
      Artifact path resolution and Contract values token enumeration (delegated
      to SKILL.md and protocol.yaml), standalone Getting-started (folded into
      section 6 as When-to-invoke). Verified-real v3 retained (check lint
      --artifact, runtime tool names, satisfaction per-decision fields).
      Wayfinder-inspired additions: destination-fixes-scope startup, Ruled-out
      scratchpad field, fog-aware readiness check at Done, breadth-before-depth
      steering, ask-user-to-sketch, edge-signal reframing of the implementation
      rail. New D82 rule propagating to all 9 remaining audits: schema validator
      files (triggers, artifacts, validation, exit .yaml) should use artifact-id
      labels not canonical filenames — going one step beyond D81, which scrubbed
      instructions prose only; canonical_artifact_label establishes the
      convention. references/cli/capability-instruction-structure.md discuss row
      Complete (D82). TODO.md discuss audit (line 15) marked resolved. vitest
      1278/1278 green; typecheck 0 errors; capability and contract validation
      PASS; check compact within limits.'
  - number: 83
    date: 2026-07-09
    question: How should the research capability instructions be audited and
      rewritten to align with D80 six-section spine, D79 direct-contract
      philosophy, D81 voice delegation, and runtime-agnostic tool references,
      while adopting design improvements from the discuss audit (D82)?
    context: 'Fourth capability audit (D79 status, D81 vision, D82 discuss done).
      Research is a linear-shape capability frequently called for external
      pattern analysis. Pre-flight grounding found v2-era ghosts: separate
      "Visual identity" section and "Skill introduction" line (D79/D82 integrate
      glyph into Purpose); "Contract values" section with hand-enumerated VT IDs
      (D79 pattern: one-line reference to protocol.yaml); contract.md pointer
      (D79 removed from status/vision); `$AGENTERA_PROFILE_DIR` and
      `$XDG_DATA_HOME` resolver flows (v3 serves `research_context.profile` via
      prime context); full artifact-path-resolution paragraph (SKILL.md owns per
      D79); standalone Getting-started section (D80 spine: fold into section 6);
      "Notes on depth" section (not in spine); "sharp colleague" personality
      hardcoding (D81 violation); artifacts.yaml "effective profile script"
      (v2-era); research.toml `prose` field (D57/D65: field is `instructions`).
      Substance design decisions: (S1) Step 1 now extracts user motivation
      (adoption evaluation, idea extraction, comparison, curiosity) in addition
      to Source/Target — mirrors discuss fog-aware readiness check. (S2)
      Transferable concepts checkpoint elevated from buried Step 2 instruction
      to named boundary between source reading and target exploration. (S3)
      Applicability Matrix reframed toward adaptation/augmentation rather than
      code-line replacement — research is about getting inspired and adapting
      ideas, not replacing existing conventions. (S4) Source-type broadened from
      URL-only to accept local file paths, concept names, and verbal
      descriptions. Runtime-agnostic design: "GitHub MCP integration" replaced
      with "available remote-access tools"; "context7" with "documentation
      databases"; "Glob"/"Grep" with generic directory-mapping and
      content-search descriptions. Safety rails converted from NEVER to MUST NOT
      (D71). Step markers simplified from N/5 to N (no-target path skips steps).
      New length 12,484 chars.
      references/cli/capability-instruction-structure.md research row Complete
      (D83). TODO.md research audit marked resolved. typecheck 0 errors;
      capability and contract validation PASS; check compact within limits.'
    alternatives:
      - name: Rewrite with substance improvements, drift cleanup, runtime-agnostic tools
        status: chosen
        reason_chosen: >
          All four substance findings (motivation intake, checkpoint elevation,
          adaptation framing, source-type broadening) address real capability
          design gaps. Drift cleanup aligns with D79/D81/D82 precedent.
          Runtime-agnostic tool references ensure the instructions work across
          all harnesses the user might run.
      - name: Drift-only cleanup without substance changes
        status: rejected
        reason_rejected: >
          Would leave the motivation gap, buried checkpoint, and rigid matrix in
          place — a capability that produces generic analysis instead of
          targeted, problem-focused analysis.
    choice: Rewrite with substance improvements, drift cleanup, runtime-agnostic tools
    reasoning: >
      The discuss audit (D82) established that substance findings are where the
      highest-leverage changes live. Research motivation gap and buried
      checkpoint are the same class of finding. The adaptation framing reflects
      user feedback that research is about inspiration and augmentation, not
      code replacement.
    confidence: firm
    feeds_into: references/cli/capability-instruction-structure.md,
      packages/cli/src/capabilities/research/, TODO.md
  - number: 89
    date: 2026-07-10
    question: How should the profile capability instructions be audited, improved,
      and rewritten for the D80 six-section spine, D79 direct-contract
      delegation, D71 modal vocabulary, D81 voice delegation, and v3 reality?
    context: "Ninth capability audit. Profile is a mode-split shape — persona
      reconstruction with Full and Validate modes. Pre-flight grounding found
      v2-era ghosts: crystallized.json (does not exist in v3); effective profile
      script (4x in cross-capability; v3 capabilities consume status.profile);
      ~/.claude/profile/ migration claim (no v3 code); agentera decisions (v3:
      agentera state decisions); CLAUDE.md (v3: AGENTS.md); Python is
      unavailable in EX3 (v3 is TypeScript); $AGENTERA_PROFILE_DIR hardcoded 6x
      (v3 serves profile_context.profile.path); $XDG_DATA_HOME resolver flow;
      DECISIONS.md canonical filename (D82: artifact-id); D65 comment header
      marker. Spine violations: standalone Visual identity, Contract values,
      Artifact path resolution, contract.md pointer, Getting started, Notes on
      depth, Step 0 numbering. Personality leaks D81: sharp colleague, narration
      voice, colleague voice. NEVER in safety rails (D71: MUST NOT). npx -y
      agentera (audited capabilities use bare agentera). Substance design
      decisions: (S1) coverage audit + extraction merged into single step (6→4
      Full steps, preserving multi-select and EX2 gating substance per user
      pushback); (S2) prediction validation folded into Generate step with low
      accuracy <3/5 triggering EX2 flagged (D85-D88 self-audit fold pattern);
      (S3) Validate V3+V4 merged (4→3 steps). New length 19567 chars (was 24219,
      19% reduction). references/cli/capability-instruction-structure.md profile
      row Complete (D89). D78 compacted to archive for D89 entry."
    alternatives:
      - name: Rewrite with substance improvements and drift cleanup
        status: chosen
        reason_chosen: All three substance findings (extraction step compression,
          prediction validation fold + EX2, Validate V3+V4 merge) address real
          capability design gaps. Drift cleanup aligns with prior audit
          precedent (D79-D88). Extraction step compression respects user
          pushback to value each step on actual importance. Prediction
          validation EX2 closes the quality signal loop. Validate V3+V4 fold
          follows the D85-D88 self-audit fold pattern.
      - name: Drift-only cleanup without substance changes
        status: rejected
        reason_rejected: Would leave extraction mechanics dominating the capability
          identity (3 of 6 steps are infrastructure), prediction validation as a
          standalone step that doesn't close the loop (low accuracy noted but
          not acted on), and Validate V4 as thin I/O masquerading as a step; a
          capability that follows the spine but asks the wrong questions still
          fails the audit.
    choice: Rewrite with substance improvements and drift cleanup
    reasoning: "Prior audits (D82 discuss, D83 research, D84 plan, D85 build, D86
      optimize, D87 audit, D88 document) established substance findings as the
      highest-leverage changes. Profile substance findings follow the same
      pattern: extraction step compression, prediction validation fold + EX2,
      and Validate V3+V4 merge are the same class of finding. Prediction
      validation EX2 closes the quality signal loop — the capability asks how it
      knows the profile is good and now acts on the answer. Extraction
      compression respects the user pushback to value each step by merging only
      the tightly-coupled coverage+extraction flow, not all three extraction
      steps."
    confidence: firm
    feeds_into: references/cli/capability-instruction-structure.md,
      packages/cli/src/capabilities/profile/, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "Profile capability rewritten per six-section spine, direct-contract,
        voice delegation. User approved each cadence step: substance decisions
        S1-S3 individually confirmed; structural findings batch approved;
        outline confirmed before drafting; draft approved before applying to
        instructions.ts. Sibling files updated: exit.yaml Python ghost removed;
        deviation table profile row Complete (D89); D78 compacted to archive.
        typecheck 0 errors; build clean; capability validation PASS; contract
        validation PASS (0 violations); check compact within limits."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-07-10
    outcome: Rewrite with substance improvements and drift cleanup
    downstream_consequence_references:
      - source_field: feeds_into
        reference: references/cli/capability-instruction-structure.md
      - source_field: feeds_into
        reference: packages/cli/src/capabilities/profile/
      - source_field: feeds_into
        reference: TODO.md
    context_complete: true
    missing_fields: []
    compacted: false
    caveats: []
  - number: 90
    date: 2026-07-10
    question: How should the design capability instructions be audited and rewritten
      to align with the six-section spine, direct-contract philosophy, and voice
      delegation, while adopting substance design improvements (self-audit fold
      into Write/Update steps, validation via agentera check lint, Step 0 fold
      into opening) from prior audits?
    context: "Eleventh capability audit. Design is a mode-split shape — the visual
      steward of DESIGN.md with three modes: create, refine, audit. Pre-flight
      grounding found v2-era ghosts: `agentera describe` (v3: `agentera
      schema`); `agentera lint --artifact` (v3: `agentera check lint
      --artifact`); ghost contract Section 24 cited twice (contract ends at
      Section 23); `$AGENTERA_PROFILE_DIR` / `$XDG_DATA_HOME` resolver flow (v3
      serves `design_context.profile.path`); `CLAUDE.md` reference (v3:
      `AGENTS.md`); canonical filenames (`VISION.md`, `PROGRESS.md`) as direct
      reads (v3 uses artifact-id + CLI state); \"One file in `.agentera/`\"
      (DESIGN.md is at project root); `references/contract.md` pointer (D79/D81
      removed from status and vision); Contract values token enumeration
      (one-line ref to protocol.yaml); artifact-path-resolution subsection
      (SKILL.md owns); standalone \"Visual identity\" section (fold into
      Purpose); `AskUserQuestion` hardcoded tool name (v3: runtime's question
      tool); \"the sharp colleague\" personality (D81 violation); \"Narration
      voice (riff, don't script)\" personality leak; NEVER in safety rails (D71:
      MUST NOT); `// Source: D65` comment header tracking marker; standalone
      Getting started section (fold into §6); v2 references (\"Agentera v2 does
      not ship a standalone design validator\", \"no separate Agentera
      enforcement reference ships in v2\"); \"validation script\" in exit.yaml
      descriptions. Substance design decisions: (S1) pre-write self-audit and
      post-write validation folded into Write/Update steps -- Create 7->5 steps,
      Refine 4->3 steps; Audit Step 1 uses `agentera check lint --artifact
      design` for structural validation instead of manual inspection. New length
      16,383 chars (was 20,596 -- 20% reduction).
      references/cli/capability-instruction-structure.md design row Complete
      (D90)."
    alternatives:
      - name: Rewrite with substance improvements and drift cleanup
        status: chosen
        reason_chosen: The self-audit fold follows the D85-D89 established pattern. The
          validation step using `agentera check lint` instead of manual
          inspection is a genuine process improvement. Drift cleanup aligns with
          prior audit precedent across nine capabilities.
      - name: Drift-only cleanup without substance changes
        status: rejected
        reason_rejected: Would leave the pre-write self-audit as a standalone step
          duplicated between Create and Refine modes, and the validation step
          claiming v2 limitations when v3 has the lint tool -- a capability that
          follows the spine but asks the wrong questions still fails the audit.
    choice: Rewrite with substance improvements and drift cleanup
    reasoning: "Prior audits (D82 discuss through D89 profile) established substance
      findings as the highest-leverage changes. Design substance findings follow
      the same pattern: self-audit fold and validation via lint tool are the
      same class of finding as D85 self-audit fold, D86 self-audit fold, D87
      self-audit fold into Report, D88 self-audit folded into write steps, D89
      coverage+extraction merge. The validation step using `agentera check lint`
      is a process improvement unique to design."
    confidence: firm
    feeds_into: references/cli/capability-instruction-structure.md,
      packages/cli/src/capabilities/design/, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: 'Design capability rewritten per six-section spine, direct-contract,
        voice delegation. User approved each cadence step: substance decisions
        S1 individually confirmed (fold both self-audit and validation);
        structural findings batch approved; outline confirmed before drafting;
        draft approved before applying to instructions.ts. Sibling files
        updated: exit.yaml "validation script"->lint, "VISION.md
        Identity"->"vision Identity"; validation.yaml V2 "VISION.md
        Identity"->"vision Identity"; deviation table design row Complete (D90).
        typecheck 0 errors; build clean; capability validation PASS; contract
        validation PASS (0 violations); check compact within limits.'
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-07-10
    outcome: Rewrite with substance improvements and drift cleanup
    downstream_consequence_references:
      - source_field: feeds_into
        reference: references/cli/capability-instruction-structure.md
      - source_field: feeds_into
        reference: packages/cli/src/capabilities/design/
      - source_field: feeds_into
        reference: TODO.md
    context_complete: true
    missing_fields: []
    compacted: false
    caveats: []
  - number: 91
    date: 2026-07-10
    question: How should the orchestrate capability instructions be audited and
      rewritten to align with the six-section spine, direct-contract philosophy,
      and voice delegation, while adopting substance design improvements (WARN
      elimination from evaluation, test-suppression directive removal from
      delegation prompt, self-contained staleness check) from prior audits?
    context: "Twelfth and final capability audit. Orchestrate is a multi-surface
      shape -- the meta-orchestrator that delegates capabilities as subagents,
      evaluates each task with audit, and loops through plans. Pre-flight
      grounding found v2-era ghosts: `agentera decisions` (v3: `agentera state
      decisions`); `agentera progress` (v3: `agentera state progress`);
      `agentera describe` (v3: `agentera schema`); `agentera query` (v3:
      `agentera state query`); canonical filenames throughout (`PLAN.md`,
      `PROGRESS.md`, `HEALTH.md`, `DECISIONS.md`, `VISION.md`, `DOCS.md` -- v3
      uses artifact-id + CLI state); `references/contract.md` pointer (D79
      removed from all audited capabilities); Contract values token enumeration
      (one-line ref to protocol.yaml); artifact-path-resolution subsection
      (SKILL.md owns); standalone \"Visual identity\" section (fold into
      Purpose); \"The sharp colleague\" personality section (D81 violation);
      \"Narration voice (riff, don't script)\" personality leaks (3x); skill
      introduction line; standalone Getting started section (fold into section
      6); \"Keeping the orchestrator lean\" does/does-NOT table (unique
      constraints fold into Safety rails); runtime subagent mechanisms table
      (compress to brief pointer, D85 propagation); NEVER in safety rails (D71:
      MUST NOT); `// Source: D65` comment header tracking marker; \"Agentera v2
      never writes\" v2 reference in Codex CLI row; \"/loop\" reference
      (explicit user request); `complete_for_normal_deliberation_context` v2-era
      field name (v3: `complete_for_orchestration_context`); source_contract
      9-field enumeration + 4-step process (compress to 2-sentence delegation).
      Substance design decisions: (S1) WARN eliminated from evaluation -- audit
      reports PASS or FAIL per criterion only, removing the ambiguous middle
      state that left the overall verdict undefined when WARN criteria were
      present; verify_command requirement moves from WARN-only to FAIL rows with
      file:line citations. (S2) test-suppression directive removed from
      delegation prompt -- \"do not write tests unless AC requires them\"
      conflicted with build's own verification workflow; scope constraints are
      sufficient. (S3) staleness check made self-contained (3 steps, not 6) --
      no longer references contract.md Section 19 (slated for retirement);
      states the principle directly. New length 18,497 chars (was 28,544 -- 35%
      reduction). references/cli/capability-instruction-structure.md orchestrate
      row Complete (D91)."
    alternatives:
      - name: Rewrite with substance improvements and drift cleanup
        status: chosen
        reason_chosen: WARN elimination follows the deterministic state machine
          principle -- the instructions say "the orchestrator follows a
          deterministic state machine" but WARN left the overall verdict to host
          interpretation. Test-suppression removal aligns with
          delegated-capability autonomy. Staleness check compression follows D79
          direct-contract philosophy and removes the last contract.md
          dependency. Drift cleanup aligns with prior audit precedent across all
          twelve capabilities.
      - name: Drift-only cleanup without substance changes
        status: rejected
        reason_rejected: Would leave the WARN ambiguity in evaluation (the state machine
          claims determinism but delegates WARN handling to host judgment), the
          test-suppression directive overriding build's own workflow, and the
          staleness check pointing to a document slated for retirement -- a
          capability that follows the spine but asks the wrong questions still
          fails the audit.
    choice: Rewrite with substance improvements and drift cleanup
    reasoning: "Prior audits (D82 discuss through D90 design) established substance
      findings as the highest-leverage changes. Orchestrate substance findings:
      WARN elimination simplifies the state machine and aligns with the
      evaluator_handoff machine contract; test-suppression removal respects
      delegated capability autonomy; staleness check compression removes the
      last contract.md pointer across all 12 capabilities. This completes the
      D80 deviation table -- all 12 capabilities now audited."
    confidence: firm
    feeds_into: references/cli/capability-instruction-structure.md,
      packages/cli/src/capabilities/orchestrate/, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: 'Orchestrate capability rewritten per six-section spine,
        direct-contract, voice delegation. User approved each cadence step:
        substance decisions S1 (WARN elimination), S2 (test-suppression
        removal), S3 (staleness check self-contained) individually confirmed; 34
        structural findings batch approved; outline confirmed before drafting;
        self-audit pass applied to outline (3 redundancies identified and
        resolved); draft approved before applying to instructions.ts. Sibling
        files updated: validation.yaml "prose"->"instructions", canonical
        filenames->artifact-ids,
        "inspektera_citation_required"->"audit_citation_required", WARN->FAIL in
        V10; exit.yaml canonical filenames->artifact-ids; deviation table
        orchestrate row Complete (D91). typecheck 0 errors; build clean;
        capability validation PASS; contract validation PASS (0 violations);
        check compact within limits; vitest 1278/1278 green.'
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-07-10
    outcome: Rewrite with substance improvements and drift cleanup
    downstream_consequence_references:
      - source_field: feeds_into
        reference: references/cli/capability-instruction-structure.md
      - source_field: feeds_into
        reference: packages/cli/src/capabilities/orchestrate/
      - source_field: feeds_into
        reference: TODO.md
    context_complete: true
    missing_fields: []
    compacted: false
    caveats: []
  - number: 92
    date: 2026-07-11
    question: What lifecycle authority, runtime identity, support floor, and Claude
      retirement policy governs Agentera 3.0.0?
    context: Agentera must manage lifecycle convergence across OpenCode, Codex,
      Cursor, and Copilot from the canonical shared skill while preserving
      runtime trust boundaries. Cursor IDE and cursor-agent require one
      identity, Cursor CLI is the prioritized surface, and Claude Code
      active-runtime support is being retired without discarding useful
      historical transcript import.
    alternatives:
      - name: Adopt one conservative cross-runtime lifecycle contract with explicit
          ownership, read-only diagnosis, idempotent Agentera-owned writes,
          Cursor CLI as the required Cursor surface, optional Cursor IDE
          integration, and opt-in Claude historical import.
        status: chosen
      - name: Treat Cursor IDE and cursor-agent as separate runtimes or require both
          surfaces for readiness.
        status: rejected
      - name: Delegate native runtime package installation, authentication, enablement,
          or trust approval to Agentera.
        status: rejected
      - name: Continue Claude Code as an active runtime or include Claude transcripts in
          default active-runtime analytics.
        status: rejected
    choice: Active runtime IDs are exactly opencode, codex, cursor, and copilot;
      Cursor CLI is required while Cursor IDE is conditional. The canonical
      skill is ~/.agents/skills/agentera. Managed writes require a declared
      destination and matching Agentera ownership manifest or ledger. Lifecycle
      reports host_present, installed, enabled, trusted, unknown, denied, and
      not_applicable without inferring trust. Probes are bounded and read-only;
      native install, update, authentication, and trust actions remain
      user-owned and are surfaced as exact remediation commands. Preview is
      strictly read-only. Apply outcomes are applied, noop, failed,
      blocked_unowned, skipped_dependency, or action_required; independent
      operations continue and retries converge. Claude Code is removed as an
      active runtime; its transcript parser remains only as an explicit
      consent-gated historical importer excluded from active-runtime analytics.
    reasoning: A shared skill root makes common discovery portable, but hooks,
      plugins, trust, and precedence remain runtime-specific. Explicit ownership
      prevents destructive adoption of user state, read-only probing preserves
      host authority, and a complete diagnosis floor prevents unsupported
      unknowns from shipping. Cursor CLI priority matches current product intent
      without making the optional IDE a release blocker. Separating Claude
      historical data from runtime support preserves user value without implying
      lifecycle parity.
    confidence: firm
    feeds_into: Runtime lifecycle contract; shared operation engine; OpenCode,
      Codex, Cursor, and Copilot adapters; prime, doctor, and upgrade
      convergence; Claude Code retirement and historical importer; 3.0.0 release
      gates.
  - number: 93
    date: 2026-07-12
    question: What runtime integration boundary should Agentera use across Codex and
      other agent runtimes?
    context: Agentera currently combines the canonical shared skill with native
      runtime plugins, hooks, agents, and marketplace surfaces, creating
      duplicate discovery paths and version skew.
    alternatives:
      - name: Use one portable shared skill and the Agentera CLI; retire optional native
          runtime adapters.
        status: chosen
    choice: Make ~/.agents/skills/agentera the sole canonical skill for every
      compatible runtime. Make the agentera CLI the sole behavioral, state,
      validation, installation, and lifecycle authority. Retire Agentera-native
      plugins, hooks, agent descriptors, commands, marketplace integrations, and
      other runtime-specific adapters. Define runtime compatibility by the
      ability to read the shared skill and execute the CLI.
    reasoning: A single portable discovery surface and CLI authority minimizes
      moving parts, prevents duplicate registrations and runtime-specific drift,
      and provides one consistent installation, upgrade, behavior, and support
      contract. Runtime-native conveniences do not justify parallel lifecycle
      and correctness paths; invariants belong in CLI mutation and validation
      flows so they apply equally everywhere.
    confidence: firm
    feeds_into: Runtime lifecycle model; package and plugin manifests; shared skill
      contract; CLI installation, doctor, and upgrade; runtime support
      documentation; hooks and native agent retirement; release packaging and
      tests.
  - number: 94
    date: 2026-07-16
    question: How should Agentera state support ordinary Git merges across parallel
      branches and worktrees?
    context: Worktree-local .agentera/.writer.lock files do not coordinate branches.
      packages/cli/src/state/write/assign.ts allocates max(number)+1, so
      branches can claim the same identity and path such as
      .agentera/archive/progress/857.yaml; aggregate projections also make
      unrelated writes conflict. Agentera 3.0 is unreleased, Decision 58
      requires one final public vocabulary, and a one-off resumable project
      migration is acceptable.
    alternatives:
      - name: Writer-owned entity files with bare project-wide Nano IDs
        status: chosen
      - name: Numbered aggregate files with semantic merge tooling
        status: rejected
      - name: Type-prefixed IDs, writer shards, or full event sourcing
        status: rejected
    choice: Store each independently mutable entity in one canonical writer-owned
      file. Assign a project-wide ID of exactly ten lowercase letters from
      abcdefghijklmnopqrstuvwxyz. Persist public metadata as id and artifact;
      remove stable_id, artifact_id, entry_number, number aliases, and type
      prefixes. Render aggregate views through agentera state commands rather
      than treating projections as authority. Preserve intentional conflicts for
      concurrent edits to the same entity or singleton editorial artifact.
    reasoning: Independent files let ordinary Git preserve unrelated writes without
      shared allocators or custom merge drivers. Random project-wide IDs remove
      branch-local sequence collisions, while artifact supplies classification
      without duplicating type information in the ID. Writer-only mutation
      prevents projection drift. The migration must inventory and validate
      source state, persist its old-to-new mapping, rewrite live relationships,
      stage and validate entity files, retain recovery evidence, and resume
      idempotently. Historical prose may retain old labels, but canonical state
      and selectors do not.
    confidence: firm
    feeds_into: references/artifacts/state-storage-authority.yaml, artifact
      registry, state writer, retrieval contracts, migration infrastructure, and
      implementation planning
archive:
  - summary: "Staging D3+D4 (2026-06-05, agentera-mobile): Mobile capability names
      use docs alias table only (English UX, internal -era IDs unchanged);
      mobile never supports extensions, plugins, MCP, or user skill loading.
      **Confidence**: provisional/firm. Merged during D89 compaction."
  - summary: "D76 (2026-06-30, routing): **Question**: How should Agentera route
      requests to capabilities? **Choice**: LLM-primary routing. Retire prime
      --route; evolve next_action into ranked state-readiness hint. Dissolve D75
      request-vs-state mutual exclusivity. **Confidence**: firm.
      **Satisfaction**: user_confirmed_satisfied 2026-07-10."
  - summary: "D77 (2026-06-30, doctor): **Question**: Should the probeCli flag and
      in-process CLI probe in buildDoctorStatus be retired? **Choice**: Retire
      the probeCli flag, cliProbe.ts, and the probe concept. File/version
      presence and install-root classification are sufficient for app install
      health in v3. **Confidence**: firm. **Satisfaction**:
      user_confirmed_satisfied 2026-07-10. Compacted during D88 audit closeout."
  - summary: "D78 (2026-07-06, routing): **Question**: How should Agentera close the
      opencode skill-path topology mismatch where prime reports up_to_date
      against a clean symlink opencode never loads while the host runs a
      diverged real directory? **Choice**: Detect + repair via topology
      reconciliation — add ~/.agents/skills/agentera to bundleStatus candidates;
      upgrade maintains that path as a symlink to the canonical source; retire
      the ~/.config/opencode/skills/agentera symlink per the opencode doc's
      cross-location uniqueness rule. Standardize on ~/.agents/skills as the
      canonical agent-compatible root. **Confidence**: firm. **Satisfaction**:
      user_confirmed_satisfied 2026-07-10. Compacted during D89 audit closeout."
  - summary: "Decision 43 (2026-05-07): Keep Swedish `-era` names canonical and add
      one primary plain `/agentera <alias>` per capability."
  - summary: "Decision 45 (2026-05-08): Build one phased agent-ready state interface
      plan for pending 2.2.0: keep routine state command names..."
    number: 45
    date: 2026-05-08
    choice: "Build one phased agent-ready state interface plan for pending 2.2.0:
      keep routine state command names stable, improve human text output where
      useful, add structured routine output, add runtime describe/schema
      introspection, add field selection, harden artifact/path inputs, and
      hard-rename `agentera bundle-status` to `agentera doctor` with no
      compatibility alias."
    feeds_into: PLAN.md, scripts/agentera, README.md, references/cli/vocabulary.md, tests
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-15
    outcome: "Build one phased agent-ready state interface plan for pending 2.2.0:
      keep routine state command names stable, improve human text output where
      useful, add structured routine output, add runtime describe/schema
      introspection, add field selection, harden artifact/path inputs, and
      hard-rename `agentera bundle-status` to `agentera doctor` with no
      compatibility alias."
  - summary: "Decision 46 (2026-05-08): Include a narrow handoff-polish item in
      pending 2.2.0: capability prose should recommend downstream capabilities
      as..."
    number: 46
    date: 2026-05-08
    choice: "Include a narrow handoff-polish item in pending 2.2.0: capability prose
      should recommend downstream capabilities as glyph plus canonical name
      labels such as `⧉ build`, reserve `/agentera <alias>` for explicit
      slash-route documentation, use the runtime's native question tool for
      clear bounded choices, put the recommended option first, include `Done`,
      and ask for confirmation before invoking a downstream capability."
    feeds_into: SKILL.md, capability prose, tests, CHANGELOG.md,
      references/cli/vocabulary.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "D46 handoff enforcement now has runtime-facing regression evidence:
        semantic fixture defaults and documentation require glyph plus canonical
        capability names such as `⧉ build`, status routing fixtures forbid
        standalone slash-capability labels such as `/build`, focused
        semantic/dispatch tests passed 49 tests on 2026-05-15, and semantic
        fixture evals passed 4 fixtures. User confirmation is still required
        before marking this decision user-confirmed satisfied."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-23
    outcome: "Include a narrow handoff-polish item in pending 2.2.0: capability
      prose should recommend downstream capabilities as glyph plus canonical
      name labels such as `⧉ build`, reserve `/agentera <alias>` for explicit
      slash-route documentation, use the runtime's native question tool for
      clear bounded choices, put the recommended option first, include `Done`,
      and ask for confirmation before invoking a downstream capability."
  - summary: "Decision 47 (2026-05-08): Replace bundle terminology with app
      terminology across live internals, redefine default `AGENTERA_HOME` as the
      platform..."
    number: 47
    date: 2026-05-08
    choice: Replace bundle terminology with app terminology across live internals,
      redefine default `AGENTERA_HOME` as the platform data home, place the
      managed Agentera app under `AGENTERA_HOME/app`, and keep `PROFILE.md`,
      `USAGE.md`, history, and intermediate corpus data under `AGENTERA_HOME`.
    feeds_into: TODO.md, PLAN.md, README.md, UPGRADE.md, scripts/install_root.py,
      scripts/agentera_upgrade.py, scripts/agentera, package/runtime registries,
      tests
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "App-home implementation and tests exist: scripts/agentera_upgrade.py
        enforces managed app root planning/action behavior, scripts/agentera
        reports app_home, managedAppRoot, and userDataRoot,
        scripts/validate_app_home_contract.py checks public surfaces, and the
        focused app-home/upgrade/legacy-default test set passed 9 tests on
        2026-05-15."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-23
    outcome: Replace bundle terminology with app terminology across live internals,
      redefine default `AGENTERA_HOME` as the platform data home, place the
      managed Agentera app under `AGENTERA_HOME/app`, and keep `PROFILE.md`,
      `USAGE.md`, history, and intermediate corpus data under `AGENTERA_HOME`.
  - summary: "Decision 48 (2026-05-08): Expose `scripts/self_audit.py` through
      `agentera lint` first. Keep `agentera doctor` scoped to
      install/app/runtime diagnostics. Promote other..."
    number: 48
    date: 2026-05-08
    choice: "Expose `scripts/self_audit.py` through `agentera lint` first. Keep
      `agentera doctor` scoped to install/app/runtime diagnostics. Promote other
      maintainer scripts behind cohesive non-deferred namespaces: `agentera
      validate` for validators, `agentera verify` or `agentera smoke` for eval
      and smoke gates, and `agentera corpus`/`agentera usage` for profile
      analytics. Keep registry/model modules private."
    feeds_into: TODO.md, scripts/agentera, README.md, capability prose, tests
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-15
    outcome: "Expose `scripts/self_audit.py` through `agentera lint` first. Keep
      `agentera doctor` scoped to install/app/runtime diagnostics. Promote other
      maintainer scripts behind cohesive non-deferred namespaces: `agentera
      validate` for validators, `agentera verify` or `agentera smoke` for eval
      and smoke gates, and `agentera corpus`/`agentera usage` for profile
      analytics. Keep registry/model modules private."
  - summary: "Decision 50 (2026-05-12): Make 2.3.0 a focused reliability release.
      Include Decision 47 app-home migration as a hard requirement;..."
    number: 50
    date: 2026-05-12
    choice: Make 2.3.0 a focused reliability release. Include Decision 47 app-home
      migration as a hard requirement; implement Decision 48 first through
      `agentera lint`; resolve the degraded helper exposure/removal, schema
      discovery, and split-root doctor checks. Defer Profile corpus parity and
      broad script namespace promotion beyond `lint`.
    feeds_into: TODO.md, .agentera/plan.yaml, scripts/install_root.py,
      scripts/agentera_upgrade.py, scripts/agentera, README.md, UPGRADE.md,
      capability prose, tests
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-15
    outcome: Make 2.3.0 a focused reliability release. Include Decision 47 app-home
      migration as a hard requirement; implement Decision 48 first through
      `agentera lint`; resolve the degraded helper exposure/removal, schema
      discovery, and split-root doctor checks. Defer Profile corpus parity and
      broad script namespace promotion beyond `lint`.
  - summary: "Decision 51 (2026-05-13): Replace the old startup-window metric with a
      state-access metric: after a capability-related user turn, anchor..."
    number: 51
    date: 2026-05-13
    choice: "Replace the old startup-window metric with a state-access metric: after
      a capability-related user turn, anchor on the first Agentera CLI state
      call, stop at the first implementation boundary or next user turn, and
      count subsequent raw artifact read/grep/glob access, especially when the
      raw artifact overlaps the CLI state family already fetched."
    feeds_into: startup state-access analysis, scripts/agentera,
      scripts/extract_corpus.py, capability prose, tests, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-15
    outcome: "Replace the old startup-window metric with a state-access metric:
      after a capability-related user turn, anchor on the first Agentera CLI
      state call, stop at the first implementation boundary or next user turn,
      and count subsequent raw artifact read/grep/glob access, especially when
      the raw artifact overlaps the CLI state family already fetched."
  - summary: "Decision 52 (2026-05-13): Add a manual Mage benchmark suite starting
      with the Decision 51 startup state-access metric. Persist..."
    number: 52
    date: 2026-05-13
    choice: Add a manual Mage benchmark suite starting with the Decision 51 startup
      state-access metric. Persist aggregate `runs.jsonl` history under the user
      Agentera data home, not the repository, and retain only the latest
      redacted detailed report beside it.
    feeds_into: magefile.go, startup state-access benchmark, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-15
    outcome: Add a manual Mage benchmark suite starting with the Decision 51 startup
      state-access metric. Persist aggregate `runs.jsonl` history under the user
      Agentera data home, not the repository, and retain only the latest
      redacted detailed report beside it.
  - summary: "Decision 53 (2026-05-15): Store decision satisfaction state on each
      decision entry. Agents may mark provisional satisfaction with
      evidence,..."
    number: 53
    date: 2026-05-15
    choice: Store decision satisfaction state on each decision entry. Agents may
      mark provisional satisfaction with evidence, but only the user may confirm
      satisfaction. Open or provisionally satisfied decisions must not be
      compacted; old protected decisions trigger review before compaction,
      during health audits, and at release closeout.
    feeds_into: TODO.md, .agentera/decisions.yaml, scripts/agentera,
      hooks/compaction.py, tests, capability prose
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-15
    outcome: Store decision satisfaction state on each decision entry. Agents may
      mark provisional satisfaction with evidence, but only the user may confirm
      satisfaction. Open or provisionally satisfied decisions must not be
      compacted; old protected decisions trigger review before compaction,
      during health audits, and at release closeout.
  - summary: "Decision 54 (2026-05-16): Hard-rename app metadata toward one
      canonical vocabulary. Use standard app terms such as `up_to_date`,
      `outdated`,..."
    number: 54
    date: 2026-05-16
    choice: Hard-rename app metadata toward one canonical vocabulary. Use standard
      app terms such as `up_to_date`, `outdated`, `repair_needed`,
      `migration_needed`, `manual_review_needed`, `ready_to_apply`, `applied`,
      and `no_changes_needed` as the starting contract. Avoid `fresh`, `stale`,
      `refresh`, and `fixed` as canonical status words. Preserve compatibility
      aliases only when a concrete existing consumer requires them, and document
      any such alias as transitional rather than as a second source of truth. Do
      not rename capability names, artifact names or paths, exit signals, task
      statuses, health grades, or general prose as part of the doctor
      app-metadata implementation. Those broader targets require a thorough
      inventory, consumer review, and a separate Discuss decision on whether a
      dedicated `vocabulary.yaml` should replace `references/cli/vocabulary.md`
      as the structured vocabulary authority.
    feeds_into: .agentera/plan.yaml, TODO.md, references/cli/vocabulary.md,
      vocabulary.yaml, scripts/agentera_upgrade.py, scripts/agentera,
      references/cli/agent-ready-state-contract.yaml, tests, CHANGELOG.md,
      README.md, UPGRADE.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "D54 app-metadata hard-rename stands with D58 governing the 3.0 live
        boundary: any transitional compatibility aliases from D54 are migration
        and upgrade-detection inputs only, not supported live protocol aliases
        after 3.0. Reconciled in discuss satisfaction review on 2026-05-23."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-23
    outcome: Hard-rename app metadata toward one canonical vocabulary. Use standard
      app terms such as `up_to_date`, `outdated`, `repair_needed`,
      `migration_needed`, `manual_review_needed`, `ready_to_apply`, `applied`,
      and `no_changes_needed` as the starting contract. Avoid `fresh`, `stale`,
      `refresh`, and `fixed` as canonical status words. Preserve compatibility
      aliases only when a concrete existing consumer requires them, and document
      any such alias as transitional rather than as a second source of truth. Do
      not rename capability names, artifact names or paths, exit signals, task
      statuses, health grades, or general prose as part of the doctor
      app-metadata implementation. Those broader targets require a thorough
      inventory, consumer review, and a separate Discuss decision on whether a
      dedicated `vocabulary.yaml` should replace `references/cli/vocabulary.md`
      as the structured vocabulary authority.
  - summary: "Decision 55 (2026-05-17): Add `agentera stats` as the user-facing
      analytics workflow and do not expose a top-level `agentera..."
    number: 55
    date: 2026-05-17
    choice: Add `agentera stats` as the user-facing analytics workflow and do not
      expose a top-level `agentera corpus` command. Plain `agentera stats`
      reports from existing fresh data or shows the refresh command when data is
      missing or stale; it must not read local runtime history. `agentera stats
      refresh --dry-run` previews candidate runtimes, paths, and expected
      actions without extraction or writes. `agentera stats refresh --consent
      local-history` reads bounded local runtime history, writes the internal
      corpus at `$PROFILERA_PROFILE_DIR/intermediate/corpus.json`, and reports
      stats. Preserve `agentera usage` as a compatibility surface or alias.
    feeds_into: TODO.md, scripts/agentera, scripts/extract_corpus.py,
      scripts/usage_stats.py, tests, README.md, capability prose
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "D55 stats/corpus privacy boundary stands with D58 governing the 3.0
        live boundary: `agentera usage` as a compatibility surface or alias is
        migration and upgrade-detection input only, not a supported live
        protocol alias after 3.0. Reconciled in discuss satisfaction review on
        2026-05-23."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-23
    outcome: Add `agentera stats` as the user-facing analytics workflow and do not
      expose a top-level `agentera corpus` command. Plain `agentera stats`
      reports from existing fresh data or shows the refresh command when data is
      missing or stale; it must not read local runtime history. `agentera stats
      refresh --dry-run` previews candidate runtimes, paths, and expected
      actions without extraction or writes. `agentera stats refresh --consent
      local-history` reads bounded local runtime history, writes the internal
      corpus at `$PROFILERA_PROFILE_DIR/intermediate/corpus.json`, and reports
      stats. Preserve `agentera usage` as a compatibility surface or alias.
  - summary: "Decision 56 (2026-05-17): Treat a pure TypeScript/Bun rewrite of the
      whole Agentera CLI core as the 3.0 direction,..."
    number: 56
    date: 2026-05-17
    choice: Treat a pure TypeScript/Bun rewrite of the whole Agentera CLI core as
      the 3.0 direction, gated by contract-first parity. The Python
      implementation remains the behavior oracle until the TypeScript CLI
      preserves supported command JSON output shapes, exit behavior, artifact
      validation, compaction, doctor/upgrade safety, verify/usage/stats
      semantics, and runtime adapter hooks. Bun/npm/single-binary distribution
      is the payoff, but not a reason to weaken existing Agentera contracts.
    feeds_into: TODO.md, scripts/agentera, pyproject.toml, package manifests,
      runtime adapters, tests, README.md, UPGRADE.md, release planning
    satisfaction:
      state: user_confirmed_satisfied
      evidence: User confirmed satisfaction during discuss review on 2026-05-31.
        Contract-first TypeScript CLI rewrite remains the 3.0 direction; feat/v3
        packages/cli work is downstream implementation.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-31
    outcome: Treat a pure TypeScript/Bun rewrite of the whole Agentera CLI core as
      the 3.0 direction, gated by contract-first parity. The Python
      implementation remains the behavior oracle until the TypeScript CLI
      preserves supported command JSON output shapes, exit behavior, artifact
      validation, compaction, doctor/upgrade safety, verify/usage/stats
      semantics, and runtime adapter hooks. Bun/npm/single-binary distribution
      is the payoff, but not a reason to weaken existing Agentera contracts.
  - summary: "Decision 57 (2026-05-18): Rename capability `prose.md` files to
      `instructions.md`, add `first_invocation_read` metadata with values such
      as `full`, `compact_startup`,..."
    number: 57
    date: 2026-05-18
    choice: Rename capability `prose.md` files to `instructions.md`, add
      `first_invocation_read` metadata with values such as `full`,
      `compact_startup`, or `skip`, surface the required reads through `agentera
      status --format json --capability-context <name>`, and update
      `skills/agentera/SKILL.md`,
      `skills/agentera/capability_schema_contract.yaml`,
      `scripts/validate_capability.py`, runtime descriptors, tests, `README.md`,
      `AGENTS.md`, and `references/cli/vocabulary.md` so first capability
      invocation reads `instructions.md` in full unless an explicit
      compact-startup exception is declared.
    feeds_into: TODO.md, skills/agentera/SKILL.md,
      skills/agentera/capability_schema_contract.yaml, scripts/agentera,
      scripts/validate_capability.py, runtime descriptors, tests, README.md,
      AGENTS.md, references/cli/vocabulary.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "D57 instructions.md rename and first_invocation_read contract stand
        with D59 governing the capability startup context seam: required reads
        are surfaced through `agentera prime --context <capability>` rather than
        `status --capability-context`. Reconciled in discuss satisfaction review
        on 2026-05-23."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-23
    outcome: Rename capability `prose.md` files to `instructions.md`, add
      `first_invocation_read` metadata with values such as `full`,
      `compact_startup`, or `skip`, surface the required reads through `agentera
      status --format json --capability-context <name>`, and update
      `skills/agentera/SKILL.md`,
      `skills/agentera/capability_schema_contract.yaml`,
      `scripts/validate_capability.py`, runtime descriptors, tests, `README.md`,
      `AGENTS.md`, and `references/cli/vocabulary.md` so first capability
      invocation reads `instructions.md` in full unless an explicit
      compact-startup exception is declared.
  - summary: "Decision 58 (2026-05-18): Agentera 3.0 must eliminate all
      backwards-compatible and multiple naming surfaces. Zero artifacts,
      concepts, primitives, commands,..."
    number: 58
    date: 2026-05-18
    choice: Agentera 3.0 must eliminate all backwards-compatible and multiple naming
      surfaces. Zero artifacts, concepts, primitives, commands, schema fields,
      metadata fields, runtime adapter surfaces, or protocol identities may
      retain legacy aliases, parallel display names, compatibility names,
      deprecated names, or storage/protocol name splits after the 3.0 boundary.
      Every retained surface must have one name, and old names must be removed
      or migrated rather than supported alongside it. This applies to routes,
      artifact IDs, paths, schema fields, runtime metadata, adapters, package
      manifests, public docs, and command surfaces.
    feeds_into: TODO.md,
      references/artifacts/artifact-registry-interface-model.yaml,
      skills/agentera/schemas/artifacts, scripts/agentera, hooks, runtime
      adapters, tests, README.md, UPGRADE.md, references/cli/vocabulary.md
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-23
    outcome: Agentera 3.0 must eliminate all backwards-compatible and multiple
      naming surfaces. Zero artifacts, concepts, primitives, commands, schema
      fields, metadata fields, runtime adapter surfaces, or protocol identities
      may retain legacy aliases, parallel display names, compatibility names,
      deprecated names, or storage/protocol name splits after the 3.0 boundary.
      Every retained surface must have one name, and old names must be removed
      or migrated rather than supported alongside it. This applies to routes,
      artifact IDs, paths, schema fields, runtime metadata, adapters, package
      manifests, public docs, and command surfaces.
  - summary: "Decision 59 (2026-05-22): Agentera 3.0 should remove
      `--context-profile full` as a supported capability startup compatibility
      path, make the..."
    number: 59
    date: 2026-05-22
    choice: Agentera 3.0 should remove `--context-profile full` as a supported
      capability startup compatibility path, make the slim `capability_context`
      capsule the only supported capability startup JSON shape, and consolidate
      central non-capability startup behavior under `prime`. Top-level commands
      should directly map to capabilities, with `prime` as the intentional
      non-capability exception. Bare `prime` should emit static Agentera usage
      for initial session priming. A dashboard/orientation mode under `prime`
      should replace separate `status` dashboard ownership, with the final
      display label and exact flag spelling still provisional. A context mode
      under `prime` should replace `status --capability-context` for preparing
      capability startup payloads. The implementation must require a D47-style
      inventory of all public JSON output surfaces, measure bytes and GPT-5
      tokens for every supported `--format json` or `--json` public CLI output
      surface, and enforce budgets so bloated JSON fails tests.
    feeds_into: TODO.md, PLAN.md, scripts/agentera,
      scripts/measure_capability_context_payloads.py,
      references/cli/agent-ready-state-contract.yaml, tests, README.md,
      UPGRADE.md, .agentera/archive/d47-app-home-vocabulary-inventory.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: D59 slim capability_context and unified prime control-plane direction
        confirmed as 3.0 policy during discuss satisfaction review on
        2026-05-23. Implementation of dashboard mode, JSON surface inventory,
        and byte/token budgets remains downstream work.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-23
    outcome: Agentera 3.0 should remove `--context-profile full` as a supported
      capability startup compatibility path, make the slim `capability_context`
      capsule the only supported capability startup JSON shape, and consolidate
      central non-capability startup behavior under `prime`. Top-level commands
      should directly map to capabilities, with `prime` as the intentional
      non-capability exception. Bare `prime` should emit static Agentera usage
      for initial session priming. A dashboard/orientation mode under `prime`
      should replace separate `status` dashboard ownership, with the final
      display label and exact flag spelling still provisional. A context mode
      under `prime` should replace `status --capability-context` for preparing
      capability startup payloads. The implementation must require a D47-style
      inventory of all public JSON output surfaces, measure bytes and GPT-5
      tokens for every supported `--format json` or `--json` public CLI output
      surface, and enforce budgets so bloated JSON fails tests.
  - summary: "Decision 60 (2026-05-23): Extend audit with a diff-scoped review mode
      adopting a two-axis Standards-and-Spec pattern. Review output is..."
    number: 60
    date: 2026-05-23
    choice: Extend audit with a diff-scoped review mode adopting a two-axis
      Standards-and-Spec pattern. Review output is ephemeral — side-by-side
      Standards and Spec reports, optional TODO filing, and no health.yaml
      writes — so longitudinal health audits stay separate from change-scoped
      review. Plan acceptance criteria from `agentera plan --format json` are
      the primary Spec source; when no active plan exists, run Standards-only
      and note that Spec was skipped. Support both orchestrate post-build spawn
      and manual pre-merge entry; exact invocation surface is deferred. Add a
      new capability only if audit's identity collapses under the combined audit
      and review responsibilities.
    feeds_into: skills/agentera/capabilities/audit/instructions.md,
      skills/agentera/SKILL.md,
      skills/agentera/capabilities/orchestrate/instructions.md,
      scripts/agentera, tests, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-23
      evidence: User confirmed satisfaction during discuss review on 2026-05-23.
    outcome: Extend audit with a diff-scoped review mode adopting a two-axis
      Standards-and-Spec pattern. Review output is ephemeral — side-by-side
      Standards and Spec reports, optional TODO filing, and no health.yaml
      writes — so longitudinal health audits stay separate from change-scoped
      review. Plan acceptance criteria from `agentera plan --format json` are
      the primary Spec source; when no active plan exists, run Standards-only
      and note that Spec was skipped. Support both orchestrate post-build spawn
      and manual pre-merge entry; exact invocation surface is deferred. Add a
      new capability only if audit's identity collapses under the combined audit
      and review responsibilities.
  - summary: "Decision 61 (2026-05-23): Keep split ownership, but implement first in
      audit. Research imports sources; audit names evidence-backed audit..."
    number: 61
    date: 2026-05-23
    choice: Keep split ownership, but implement first in audit. Research imports
      sources; audit names evidence-backed audit candidates; discuss decides
      disputed boundaries; plan and build execute concrete candidates.
    feeds_into: references/cli/vocabulary.md, audit, research, discuss, PLAN.md, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-23
      evidence: User confirmed satisfaction during discuss review on 2026-05-23.
    outcome: Keep split ownership, but implement first in audit. Research imports
      sources; audit names evidence-backed audit candidates; discuss decides
      disputed boundaries; plan and build execute concrete candidates.
  - summary: "Decision 62 (2026-05-24): Adopt Phase 1 prose governance for design;
      formalize prime --context design plus DESIGN.md read as Step 0 gate, add
      brand-vs-product output registers and suite-vs-project design
      authorities."
    number: 62
    date: 2026-05-24
    choice: Adopt Phase 1 prose governance for design in one refine pass. Formalize
      `agentera prime --context design` plus DESIGN.md read as a hard Step 0
      gate in design instructions. Add brand-vs-product output registers and
      suite-vs-project design authorities to DESIGN.md (alongside the
      terminal/mobile surface register on feat/v3). Expand DESIGN.md constraints
      with agent-output anti-patterns and a slop test. Defer deterministic
      design-output linting until evidence or 3.0 CLI work justifies it.
    feeds_into: DESIGN.md, packages/cli/src/capabilities/design/instructions.ts,
      skills/agentera/capabilities/status/schemas/artifacts.yaml, design
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-06-09
  - summary: "Decision 63 (2026-05-24): Add Cursor as a first-class runtime in one
      v1 slice. Ship repo-native .cursor/ and agentera..."
    number: 63
    date: 2026-05-24
    choice: Add Cursor as a first-class runtime in one v1 slice. Ship repo-native
      .cursor/ and agentera upgrade --runtime cursor for any project. Register
      cursor for IDE and cursor-agent for CLI as separate RuntimeAdapter
      records. Reuse shared hooks with Cursor-shaped sessionStart wrappers;
      claim conditional hard-gate parity for reconstructable Write/Edit
      candidates only after live preToolUse smoke passes, and block release
      tagging until that smoke succeeds. Wire AGENTERA_HOME through sessionStart
      env first; add setup_cursor.py only if shell-tool smoke shows env does not
      propagate. Include .cursor-plugin/plugin.json, marketplace submission,
      managed .cursor/agents for all twelve capabilities, and eval_skills
      --runtime cursor-agent. Document cloud agents as unsupported in v1. Accept
      metadata-only bare status routing like Claude/Copilot/Codex.
    feeds_into: references/adapters/runtime-adapter-registry.yaml,
      references/adapters/runtime-feature-parity.md,
      references/adapters/cursor.md, .cursor/, .cursor-plugin/,
      hooks/validate_artifact.py, scripts/agentera_upgrade.py,
      scripts/eval_skills.py, scripts/smoke_live_hosts.py, AGENTS.md, tests,
      package-registry.yaml
    satisfaction:
      state: user_confirmed_satisfied
      evidence: Cursor v1 slice shipped with cursor and cursor-agent registry records,
        repo-native hooks and managed agents, upgrade/doctor/eval paths, and
        parity docs. Live preToolUse Write smoke passed 2026-05-24.
        Cross-platform app-home resolution is implemented in the TypeScript CLI
        (`packages/cli/src/state/installRoot.ts`). User confirmed satisfaction
        in discuss on 2026-06-09 after verifying macOS app-home resolves to
        `~/Library/Application Support/agentera`.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-06-09
    outcome: Add Cursor as a first-class runtime in one v1 slice. Ship repo-native
      .cursor/ and agentera upgrade --runtime cursor for any project. Register
      cursor for IDE and cursor-agent for CLI as separate RuntimeAdapter
      records. Reuse shared hooks with Cursor-shaped sessionStart wrappers;
      claim conditional hard-gate parity for reconstructable Write/Edit
      candidates only after live preToolUse smoke passes, and block release
      tagging until that smoke succeeds. Wire AGENTERA_HOME through sessionStart
      env first; add setup_cursor.py only if shell-tool smoke shows env does not
      propagate. Include .cursor-plugin/plugin.json, marketplace submission,
      managed .cursor/agents for all twelve capabilities, and eval_skills
      --runtime cursor-agent. Document cloud agents as unsupported in v1. Accept
      metadata-only bare status routing like Claude/Copilot/Codex.
  - summary: "Decision 64 (2026-05-26): Apply three focused refinements to
      vision.yaml: (1) add JSON output is protocol paragraph to Direction,..."
    number: 64
    date: 2026-05-26
    choice: "Apply three focused refinements to vision.yaml: (1) add JSON output is
      protocol paragraph to Direction, (2) shift personality from \"ambitious
      workshop\" to \"the sharp colleague''s workshop\", (3) add autonomy
      dimension to the emotional register. Defer runtime landscape and TS/3.0
      direction updates to a future refinement when those directions are more
      concrete."
    feeds_into: VISION.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: Three vision refinements applied in `.agentera/vision.yaml` —
        JSON-as-protocol paragraph in direction, sharp colleague's workshop
        personality, autonomy principle and emotional register. User confirmed
        satisfaction during discuss review on 2026-05-31.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-05-31
    outcome: "Apply three focused refinements to vision.yaml: (1) add JSON output is
      protocol paragraph to Direction, (2) shift personality from \"ambitious
      workshop\" to \"the sharp colleague''s workshop\", (3) add autonomy
      dimension to the emotional register. Defer runtime landscape and TS/3.0
      direction updates to a future refinement when those directions are more
      concrete."
  - summary: "Decision 65 (2026-06-02): Adopt the SKILL-md-as-discoverability +
      `instructions.ts`-as-source-of-truth model. (1) `/agentera` SKILL.md is
      the runtime discoverability surface; frontmatter..."
    number: 65
    date: 2026-06-02
    choice: Adopt the SKILL-md-as-discoverability +
      `instructions.ts`-as-source-of-truth model. (1) `/agentera` SKILL.md is
      the runtime discoverability surface; frontmatter becomes a tight `name` +
      `description` index, body becomes a routing stub that hands off to
      `agentera prime` for state and to the CLI for capability execution. (2)
      The 12 per-capability `instructions.md` files leave the runtime skill
      install and relocate to `packages/cli/src/capabilities/*/instructions.ts`
      as TypeScript modules exporting the capability prose as a string constant.
      (3) The CLI serves the prose as both the `capability_context` JSON capsule
      (D59) and as human-readable Markdown for the docs site / GitHub. (4) D57
      first_invocation_read = full is explicitly retired — the runtime shells
      out to `agentera prime --context <name>` instead of reading on-disk
      `instructions.md`. (5) The `npx skills` ecosystem surface (SKILL.md only)
      is unchanged.
    feeds_into: TODO.md, skills/agentera/SKILL.md,
      packages/cli/src/capabilities/*/instructions.ts (new),
      packages/cli/scripts/copy-bundle.mjs,
      references/cli/capability-instruction-contract.yaml,
      skills/agentera/capability_schema_contract.yaml,
      scripts/validate_capability.py, runtime descriptors, tests, README.md,
      AGENTS.md, references/cli/vocabulary.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: Deliberation closed in resonara session on 2026-06-02 with three of
        three evidence-backed challenges landing. skills.sh public surface
        empirically verified to use SKILL.md only. CLI-over-MCP trend documented
        via jannikreinhard.com and justin.poehnelt.com references provided
        during the session. User confirmed D64-style capture and TODO filing.
        User confirmed satisfaction 2026-06-02 after D65 was written to
        `.agentera/decisions.yaml` with the schema and self_audit budget bumps
        in place.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-06-02
    outcome: Adopt the SKILL-md-as-discoverability +
      `instructions.ts`-as-source-of-truth model. (1) `/agentera` SKILL.md is
      the runtime discoverability surface; frontmatter becomes a tight `name` +
      `description` index, body becomes a routing stub that hands off to
      `agentera prime` for state and to the CLI for capability execution. (2)
      The 12 per-capability `instructions.md` files leave the runtime skill
      install and relocate to `packages/cli/src/capabilities/*/instructions.ts`
      as TypeScript modules exporting the capability prose as a string constant.
      (3) The CLI serves the prose as both the `capability_context` JSON capsule
      (D59) and as human-readable Markdown for the docs site / GitHub. (4) D57
      first_invocation_read = full is explicitly retired — the runtime shells
      out to `agentera prime --context <name>` instead of reading on-disk
      `instructions.md`. (5) The `npx skills` ecosystem surface (SKILL.md only)
      is unchanged.
  - summary: "Decision 66 (2026-06-02): Remove the per-cycle `commit` field and
      delete backfill/rewrite/ancestry machinery. Do not retain `commit` as
      optional—D58..."
    number: 66
    date: 2026-06-02
    choice: Remove the per-cycle `commit` field and delete backfill/rewrite/ancestry
      machinery. Do not retain `commit` as optional—D58 rejects parallel linkage
      sources. Follow-up build plan implements removal; this decision is
      design-only.
    feeds_into: TODO.md, skills/agentera/schemas/artifacts/progress.yaml,
      packages/cli/src/state/progressCommit.ts,
      packages/cli/src/cli/commands/backfill.ts,
      packages/cli/src/hooks/validateArtifact.ts,
      packages/cli/src/cli/dispatch.ts, AGENTS.md,
      skills/agentera/capabilities/build/instructions.md, UPGRADE.md,
      .agentera/progress.yaml
    satisfaction:
      state: user_confirmed_satisfied
      evidence: Discuss evaluation and orchestrate plan task 1 (2026-06-02) recorded
        remove with 21-file ripple inventory, seven-step migration outline, and
        six follow-up build acceptance criteria. User confirmed satisfaction
        2026-06-02; TODO.md chore entry rewritten for implementation per D66.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-06-02
    outcome: Remove the per-cycle `commit` field and delete
      backfill/rewrite/ancestry machinery. Do not retain `commit` as
      optional—D58 rejects parallel linkage sources. Follow-up build plan
      implements removal; this decision is design-only.
  - summary: "Decision 67 (2026-06-05): Adopt single-brand product positioning.
      Merge mobile vision into root .agentera/vision.yaml. Restructure root
      README as Pi-style..."
    number: 67
    date: 2026-06-05
    choice: Adopt single-brand product positioning. Merge mobile vision into root
      .agentera/vision.yaml. Restructure root README as Pi-style package index
      with @agentera/mobile as flagship. Demote editor runtime install matrices
      to README Internals and packages/cli/README.md. Document capability
      English aliases in packages/mobile/README.md without renaming schema IDs
      yet. Add DESIGN.md surface register (terminal vs mobile).
    feeds_into: README.md, .agentera/vision.yaml, AGENTS.md, DESIGN.md,
      packages/mobile/, references/cli/vocabulary.md, .agentera/docs.yaml,
      docs/consolidation/monorepo-plan.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: Decision 68 (2026-06-05) corrected D67 skills-as-internals ceiling to
        a delivery surface, removed SaaS/PaaS framing, and reaffirmed
        multi-surface product boundary. User confirmed D67 satisfaction
        2026-06-05 in current discuss deliberation. D67 feeds_into list is
        satisfied.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-06-05
    outcome: Adopt single-brand product positioning. Merge mobile vision into root
      .agentera/vision.yaml. Restructure root README as Pi-style package index
      with @agentera/mobile as flagship. Demote editor runtime install matrices
      to README Internals and packages/cli/README.md. Document capability
      English aliases in packages/mobile/README.md without renaming schema IDs
      yet. Add DESIGN.md surface register (terminal vs mobile).
  - summary: "Decision 68 (2026-06-05): Adopt multi-surface product architecture
      with a single fixed workflow. Surface map — packages/cli (state access..."
    number: 68
    date: 2026-06-05
    choice: Adopt multi-surface product architecture with a single fixed workflow.
      Surface map — packages/cli (state access + agent runtime), packages/mobile
      (flagship app), packages/web (marketing + docs), editor skill/plugin
      surfaces (Cursor, Claude, OpenCode, Codex, Copilot — same workflow as
      today), future packages/desktop, packages/tui, and optional future cloud
      surface. No SaaS or PaaS framing. No user extensions, plugins, MCP
      servers, or third-party skill loading on any surface. D67 docs pass
      remains valid for monorepo consolidation; D68 corrects the "skills as
      internals only" ceiling and replaces any SaaS/PaaS or extensibility split
      with surface parity under one product boundary.
    feeds_into: .agentera/vision.yaml, docs/consolidation/monorepo-plan.md,
      README.md, AGENTS.md, packages/mobile/README.md,
      references/cli/vocabulary.md, skills/agentera/SKILL.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: User refined discuss draft 2026-06-05 — drop SaaS/PaaS, keep
        no-extensions, skills/plugins are surfaces not extensibility.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-06-05
    outcome: Adopt multi-surface product architecture with a single fixed workflow.
      Surface map — packages/cli (state access + agent runtime), packages/mobile
      (flagship app), packages/web (marketing + docs), editor skill/plugin
      surfaces (Cursor, Claude, OpenCode, Codex, Copilot — same workflow as
      today), future packages/desktop, packages/tui, and optional future cloud
      surface. No SaaS or PaaS framing. No user extensions, plugins, MCP
      servers, or third-party skill loading on any surface. D67 docs pass
      remains valid for monorepo consolidation; D68 corrects the "skills as
      internals only" ceiling and replaces any SaaS/PaaS or extensibility split
      with surface parity under one product boundary.
  - summary: "Decision 69 (2026-06-07): Adopt `packages/cli` as the narrow waist.
      Layer A (contract) — twelve-capability schema, `.agentera/` artifact
      shapes,..."
    number: 69
    date: 2026-06-07
    choice: >
      Adopt `packages/cli` as the narrow waist. Layer A (contract) —
      twelve-capability schema, `.agentera/` artifact shapes, validation, and
      `agentera prime` / `agentera state *` JSON contracts are authoritative in
      CLI. Layer B (execution routing) — all agent execution routes through the
      CLI engine (direct or `agentera serve`). Provider runtimes (Cursor SDK
      first; future OpenCode SDK, Vercel AI) are Layer B adapters behind CLI.
      Surfaces are clients — mobile (presentation-only tiering), web
      (docs/inspector), editor-runtime composite (SKILL.md + hooks + CLI
      binary), future desktop/TUI. D68 means behavioral identity (interpretation
      A).
    feeds_into: docs/consolidation/monorepo-plan.md, docs/consolidation/d69/,
      docs/consolidation/mobile-open-decisions.md, packages/cli/,
      packages/mobile/, AGENTS.md, skills/agentera/SKILL.md,
      references/adapters/, hooks/, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: Discuss deliberation 2026-06-07 — D68 interpretation A, Layer A+B,
        presentation-only mobile tiering, editor-runtime composite surface.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-06-07
    outcome: >
      Adopt `packages/cli` as the narrow waist. Layer A (contract) —
      twelve-capability schema, `.agentera/` artifact shapes, validation, and
      `agentera prime` / `agentera state *` JSON contracts are authoritative in
      CLI. Layer B (execution routing) — all agent execution routes through the
      CLI engine (direct or `agentera serve`). Provider runtimes (Cursor SDK
      first; future OpenCode SDK, Vercel AI) are Layer B adapters behind CLI.
      Surfaces are clients — mobile (presentation-only tiering), web
      (docs/inspector), editor-runtime composite (SKILL.md + hooks + CLI
      binary), future desktop/TUI. D68 means behavioral identity (interpretation
      A).
  - summary: "Decision 70 (2026-06-08): Promote the Decision 43 English aliases to
      canonical in v3. Retire the Swedish `-era` capability..."
    number: 70
    date: 2026-06-08
    choice: >
      Promote the Decision 43 English aliases to canonical in v3. Retire the
      Swedish `-era` capability names in v3 surface only. v2 stable (`@latest`)
      and its capability surface remain Swedish. Mobile UX retires the `brief`
      UI label and aligns with the CLI's `status` canonical name. Editor skills
      and web docs use the same English names as the CLI. The Decision 43 alias
      set is the binding name set for v3 capability additions; future renames
      require a new decision. Existing references to Swedish IDs in v3 surface
      are demoted via mechanical cleanup (file path renames in
      `packages/cli/src/capabilities/` and `skills/agentera/capabilities/`,
      schema content updates, runtime descriptor updates, SKILL.md dispatch
      rewrite, vocabulary-index.yaml and vocabulary.md updates). D58 is
      reaffirmed: zero legacy aliases in v3. v2 stable is explicitly out of
      scope for this rename. The coexistence probe surfaces per-distribution
      naming divergence.
    feeds_into: skills/agentera/SKILL.md, skills/agentera/capabilities/*,
      packages/cli/src/capabilities/*, packages/mobile/, packages/web/,
      references/cli/vocabulary.md, references/cli/vocabulary-index.yaml,
      skills/agentera/protocol.yaml, .agentera/decisions.yaml, TODO.md,
      CHANGELOG.md, hooks/
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-06-09
      evidence: Discuss deliberation 2026-06-08 — 10 stresses on Option 2; closeout
        deliberation 2026-06-09 — 8 implementation pins stressed and resolved;
        user accepted closeout package. Compaction deferred (D60 provisional).
        Mechanical rename deferred to implementation PR.
    outcome: >
      Promote the Decision 43 English aliases to canonical in v3. Retire the
      Swedish `-era` capability names in v3 surface only. v2 stable (`@latest`)
      and its capability surface remain Swedish. Mobile UX retires the `brief`
      UI label and aligns with the CLI's `status` canonical name. Editor skills
      and web docs use the same English names as the CLI. The Decision 43 alias
      set is the binding name set for v3 capability additions; future renames
      require a new decision. Existing references to Swedish IDs in v3 surface
      are demoted via mechanical cleanup (file path renames in
      `packages/cli/src/capabilities/` and `skills/agentera/capabilities/`,
      schema content updates, runtime descriptor updates, SKILL.md dispatch
      rewrite, vocabulary-index.yaml and vocabulary.md updates). D58 is
      reaffirmed: zero legacy aliases in v3. v2 stable is explicitly out of
      scope for this rename. The coexistence probe surfaces per-distribution
      naming divergence.
  - summary: "Decision 71 (2026-06-23): Preventive prose convention using RFC 2119
      modal vocabulary (MUST, MUST NOT, SHOULD, SHOULD NOT, MAY)...."
    number: 71
    date: 2026-06-23
    choice: >
      Preventive prose convention using RFC 2119 modal vocabulary (MUST, MUST
      NOT, SHOULD, SHOULD NOT, MAY). Lowercase modal verbs remain ordinary
      English; capitalized keywords carry protocol force. Tighten the three
      flagged section rewrites as exemplars. Drop the descriptive/decisional
      lint-extension TODO entirely. The convention lives in
      capability_schema_contract.yaml.
    feeds_into: capabilities/research, capabilities/vision, capabilities/plan,
      capability_schema_contract.yaml, VISION.md, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: D71 modal vocabulary convention adopted and applied across audited
        capabilities (D79 status, D81 vision, D82 discuss, D83 research).
        Capitalized MUST/MUST NOT/SHOULD/MAY keywords carry protocol force;
        lowercase modal verbs remain ordinary English. Convention lives in
        capability_schema_contract.yaml. 8 remaining unaudited capabilities will
        adopt during their respective audits.
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-07-09
    outcome: >
      Preventive prose convention using RFC 2119 modal vocabulary (MUST, MUST
      NOT, SHOULD, SHOULD NOT, MAY). Lowercase modal verbs remain ordinary
      English; capitalized keywords carry protocol force. Tighten the three
      flagged section rewrites as exemplars. Drop the descriptive/decisional
      lint-extension TODO entirely. The convention lives in
      capability_schema_contract.yaml.
  - summary: "Decision 72 (2026-06-24): **Question**: How should Agentera
      consolidate three parallel install-state concepts so users never guess
      which v2/v3 path they are on? **Choice**: Promote the existing internal
      InstallKind to a public install_track field in prime and doctor output.
      Rename values to plain English (v2, v3, source, unknown). Retire
      rootStatus from public output; keep Classification.kind internal for
      doctor classifier logic. Keep app.status (renamed from bundle.status) as
      an orthogonal health field. Rename bundle to app in public output (D47
      straggler). Add coexistence warning (v2 app + v3 hooks) as an attention
      item in prime via existing attention mechanism. No new composite field; no
      new concept. **Confidence**: firm (compacted during D84 audit closeout;
      satisfaction not user-reviewed before archive)."
  - summary: "Decision 73 (2026-06-26): Single primary agent replaces 12
      per-capability subagents. Agent named Agentera loads skill for routing,
      fetches prose via prime --context, dispatches to general subagent.
      Cross-runtime: OpenCode/Cursor managed, Claude/Codex/Copilot skill-only.
      Compacted during D86 audit closeout."
  - number: 74
    date: 2026-06-28
    summary: 'Decision 74 (2026-06-28): Adopt the crystallized vision: "One agent,
      one CLI, many capabilities." Twelve commitments, locked across three
      rounds o'
  - summary: "Decision 75 (2026-06-29): Trigger-schema enrichment contract for Layer
      3-4 request-derived capability routing — optional triggers.yaml fields
      (confidence_threshold, disambiguates_against, patterns_regex,
      borderline_band) and weighted-average/max substring/regex scoring.
      Superseded by Decision 76 (2026-06-30): LLM owns routing; --route retired;
      next_action evolved into a ranked state-readiness hint; trigger schema
      repositioned as LLM-readable intent documentation."
  - summary: "Decision 84 (2026-07-09): Audit and rewrite plan capability
      instructions. Six-section spine, direct-contract, voice delegation. Added
      unknowns/rejected fields, fog check at handoff, qualitative replan
      trigger. Cut scope leak (How build reads PLAN.md), effort-bias check.
      Compacted during D85 audit closeout."
    number: 84
    date: 2026-07-09
    satisfaction:
      state: user_confirmed_satisfied
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-07-10
  - summary: "Decision 85 (2026-07-10): Rewrite with substance improvements, drift
      cleanup, spawning compression"
    number: 85
    date: 2026-07-10
    choice: Rewrite with substance improvements, drift cleanup, spawning compression
    feeds_into: references/cli/capability-instruction-structure.md,
      packages/cli/src/capabilities/build/, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "Build capability rewritten per six-section spine, direct-contract,
        voice delegation. User approved each cadence step — substance decisions
        S1-S5 individually confirmed (S2 rejected). structural findings batch
        approved. outline confirmed before drafting. draft approved before
        applying to instructions.ts. Validation: typecheck 0 errors; build
        clean; capability validation PASS; contract validation PASS (0
        violations); check compact within limits; vitest 1278/1278 green.
        Sibling files updated: deviation table build row Complete (D85);
        validation/exit/artifacts schema files updated; TODO resolved; D84
        compacted to archive."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-07-10
    outcome: Rewrite with substance improvements, drift cleanup, spawning compression
  - summary: "Decision 86 (2026-07-10): Rewrite with substance improvements, drift
      cleanup, spawning compression"
    number: 86
    date: 2026-07-10
    choice: Rewrite with substance improvements, drift cleanup, spawning compression
    feeds_into: references/cli/capability-instruction-structure.md,
      packages/cli/src/capabilities/optimize/, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "Optimize capability rewritten per six-section spine, direct-contract,
        voice delegation. User approved each cadence step — substance decisions
        S1-S5 individually confirmed. structural findings batch approved.
        outline confirmed before drafting. draft approved before applying to
        instructions.ts. Validation: typecheck 0 errors; build clean; capability
        validation PASS; contract validation PASS (0 violations); check compact
        within limits; vitest green. Sibling files updated: deviation table
        optimize row Complete (D86); artifacts/validation/exit schema files
        updated; TODO resolved; D73 compacted to archive."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-07-10
    outcome: Rewrite with substance improvements, drift cleanup, spawning compression
  - summary: "Decision 87 (2026-07-10): Compress per-dimension methodology (S1),
      fold self-audit into Report (S2), compress subagent spawning to brief
      pointer..."
    number: 87
    date: 2026-07-10
    choice: Compress per-dimension methodology (S1), fold self-audit into Report
      (S2), compress subagent spawning to brief pointer with citation standard
      relocated (S3), cut ghost quick checklist (S4), apply all structural fixes
      (D80 spine, D79 direct-contract, D71 modal vocab, D81 voice delegation, v3
      reality command/file references). 31,313 → 18,401 chars (-41%). Six
      workflow steps (orient, select, assess, distill, report, connect).
    feeds_into: packages/cli/src/capabilities/audit/instructions.ts,
      references/cli/capability-instruction-structure.md,
      skills/agentera/capabilities/audit/schemas/validation.yaml,
      skills/agentera/capabilities/audit/schemas/exit.yaml,
      .agentera/decisions.yaml, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "S1-S4 substance findings confirmed individually. Structural findings
        A-G approved as batch. Outline confirmed before drafting. Draft approved
        before applying to instructions.ts. Validation: typecheck 0 errors;
        build clean; capability validation PASS; drift verification confirms
        zero v2-era residue (NEVER=absent, CLAUDE.md=absent, agentera
        describe=absent, agentera lint=absent,
        HEALTH.md/DECISIONS.md/PROGRESS.md/PLAN.md/VISION.md/DOCS.md as
        standalone filenames=absent, Visual identity section=absent, Skill
        introduction=absent, personality leaks=absent, audit-commands.md
        ghost=absent, contract.md pointer=absent). Sibling files:
        validation.yaml V3 DECISIONS.md→.agentera/decisions.yaml; exit.yaml E1
        HEALTH.md→health artifact; deviation table row Complete (D87). D76
        compacted to archive to make room."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-07-10
    outcome: Compress per-dimension methodology (S1), fold self-audit into Report
      (S2), compress subagent spawning to brief pointer with citation standard
      relocated (S3), cut ghost quick checklist (S4), apply all structural fixes
      (D80 spine, D79 direct-contract, D71 modal vocab, D81 voice delegation, v3
      reality command/file references). 31,313 → 18,401 chars (-41%). Six
      workflow steps (orient, select, assess, distill, report, connect).
  - summary: "Decision 88 (2026-07-10): Rewrite with substance improvements and
      drift cleanup"
    number: 88
    date: 2026-07-10
    choice: Rewrite with substance improvements and drift cleanup
    feeds_into: references/cli/capability-instruction-structure.md,
      packages/cli/src/capabilities/document/, TODO.md
    satisfaction:
      state: user_confirmed_satisfied
      evidence: "Document capability rewritten per six-section spine, direct-contract,
        voice delegation. User approved each cadence step: substance decisions
        S1-S5 individually confirmed; structural findings batch approved;
        outline confirmed before drafting; draft approved before applying to
        instructions.ts. Sibling files updated: triggers.yaml CLAUDE.md to
        AGENTS.md; deviation table document row Complete (D88); D77 compacted to
        archive. typecheck 0 errors; build clean; capability validation PASS;
        contract validation PASS (0 violations); check compact within limits;
        vitest 144/144 files, 1278/1278 tests green."
      user_confirmation:
        confirmed_by: user
        confirmed_at: 2026-07-10
    outcome: Rewrite with substance improvements and drift cleanup
