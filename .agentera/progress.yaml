cycles:
  - number: 657
    timestamp: 2026-06-10 00:21
    type: chore
    phase: audit
    what: "Plan T7 — state sync and audit TODO closeout: archived
      PLAN-2026-06-09-v3-cli-structural-debt.yaml; cleared active plan slot;
      thermo-nuclear audit TODO items folded to Resolved with resolved
      2026-06-10; Decision 70, mobile vite hook, and repo-wide Dict elimination
      remain open in Normal."
    inspiration: Thermo-nuclear audit T7 — close the seven-task structural-debt plan
      after T1–T6 land without a standalone bookkeeping commit.
    discovered: TODO.md thermo-nuclear block was already in Resolved from T1–T6
      work; T7 is archive, progress cycle, and verification pass only.
    verified: typecheck and build pass; vitest 901/915 pass (14 pre-existing
      failures — vocabularyAuthority YAML, npxBundle realpath, appModel darwin
      platform paths, plus coexistenceProbe, codex path, sessionStop,
      compaction).
    next: No active plan; open 3.0.0 items are Decision 70 rename, mobile
      scaffold/vite hook, and follow-on Dict alias cleanup.
    context:
      intent: Plan closeout per T7 acceptance; no commit; no vision/objective edits.
      constraints: Archive to .agentera/archive/; delete active plan slot per repo
        convention (6b8af92, 606579a).
      unknowns: vocabularyAuthority YAML and darwin test flakes are out of plan scope.
      scope: .agentera/archive/PLAN-2026-06-09-v3-cli-structural-debt.yaml,
        .agentera/plan.yaml (deleted), .agentera/progress.yaml, TODO.md verify
  - number: 656
    timestamp: 2026-06-10 00:17
    type: refactor
    phase: build
    what: "Plan T6 — prime module decomposition: extracted v1 migration, bundle
      status, orientation field assembly, and output helpers into
      `cli/commands/prime/`; shared `cli/todoSeverity.ts` for TODO band
      ordering; `prime.ts` thin cmdPrime dispatch."
    inspiration: Thermo-nuclear audit T6 — final prime split after T4–T5 extractions
      so prime is not split twice.
    discovered: prime.ts was already under the monolith gate (667 lines); split
      lands focused modules without changing JSON shapes or exit codes.
    verified: typecheck and build pass; monolithLint green; prime.test (19/19),
      orientation.test (14/14), primeAppWording (2/2) green.
    next: Plan T7 — state sync and audit TODO closeout.
    context:
      intent: Behavior-preserving prime decomposition per T6 acceptance; no commit.
      constraints: monolith gate green; shared TODO severity ordering aligned with
        state/todo.ts keys.
      unknowns: T7 folds remaining thermo-nuclear TODO items and plan closeout.
      scope: packages/cli/src/cli/commands/prime/*, cli/todoSeverity.ts,
        cli/orientation.ts, cli/commands/state/todo.ts
  - number: 655
    timestamp: 2026-06-10 00:16
    type: refactor
    phase: build
    what: "Plan T5 — integration decision table and unified prime attention:
      `upgrade/projectIntegrationDecision.ts` classifies artifacts-only,
      runtime-only, cross-major, and app-upgrade scenarios;
      `summarizeProjectIntegration` delegates to the decision table;
      `collectOrientationState` uses `projectIntegrationAttention` as the sole
      upgrade-attention source."
    inspiration: Thermo-nuclear audit T5 — single canonical upgrade narrative per
      scenario before prime.ts decomposition (T6).
    discovered: Bundle-level `appStatusAttention` duplicated integration messaging
      for outdated/repair paths; removal keeps cross-major and app-lifecycle
      copy aligned with `project_integration.message`.
    verified: typecheck and build pass; monolithLint green;
      projectIntegrationWording (5/5), primeAppWording (2/2),
      primeProjectIntegration (2/2), primeChannels (3/3) green.
    next: Plan T6 — prime module decomposition.
    context:
      intent: Behavior-preserving decision-table refactor and attention unification
        per T5 acceptance; Decision 54 upgrade/repair wording preserved.
      constraints: No commit; monolith gate green; no command-shape changes.
      unknowns: T6 prime split should import shared TODO severity ordering.
      scope: packages/cli/src/upgrade/projectIntegrationDecision.ts,
        projectIntegration.ts, cli/commands/prime.ts,
        test/upgrade/projectIntegrationWording.test.ts,
        test/cli/primeAppWording.test.ts
  - number: 654
    timestamp: 2026-06-10 00:12
    type: refactor
    phase: build
    what: "Plan T4 — doctor classification and npx platform status: extracted typed
      `ROOT_CLASSIFIERS` map in `upgrade/doctorClassifier.ts`; shared
      `resolveNpxPlatformStatus` in `upgrade/npxPlatformStatus.ts` for prime
      hejBundleStatus and projectIntegration resolveIntegrationTargets."
    inspiration: Thermo-nuclear audit T4 — shrink doctor nested ladder and unify npx
      platform app-home resolution before integration decision-table work.
    discovered: doctor.test.ts source-pin for missing_marker wording moved to
      doctorClassifier.ts with the classifier extraction.
    verified: typecheck and build pass; doctor (9/9), installRoot (9/9), cli/doctor
      (8/8), doctorUpgradeParity (16/16), primeProjectIntegration (2/2),
      projectIntegrationWording (2/2) green.
    next: Plan T5 — integration decision table and unified prime attention.
    context:
      intent: Behavior-preserving classifier map and shared npx resolver per T4
        acceptance.
      constraints: No commits; preserve Decision 54 upgrade/repair wording; monolith
        gate green; T1 BundleStatus contracts intact.
      unknowns: T5 decision-table refactor builds on this shared resolver.
      scope: packages/cli/src/upgrade/doctorClassifier.ts, npxPlatformStatus.ts,
        doctor.ts, prime.ts, projectIntegration.ts, test/upgrade/doctor.test.ts
  - number: 653
    timestamp: 2026-06-10 00:30
    type: refactor
    phase: build
    what: "Plan T3 — lifecycle adapters node default, legacy Python gated: split
      lifecycleAdapters into shared.ts, nodeFormChecks.ts, and
      legacyPythonParity.ts; default validate path skips packaged-Python-script
      and uv-binary probes; maintainer seam via --legacy-python-parity or
      AGENTERA_LEGACY_PYTHON_PARITY."
    inspiration: Thermo-nuclear audit T3 — gate Python oracle probes off npm v3
      default before main promotion.
    discovered: validateUvRuntime was already opt-in via checkUvRuntime but
      validatePackagedPythonScripts ran unconditionally; both now share the
      unified legacyPythonParity gate.
    verified: typecheck and build pass; lifecycleAdapters (7/7),
      runtimeAdapterHooksParity (12/12), and validate lifecycle-adapters
      dispatch cases green; unrelated vocabularyAuthority YAML parse failure
      pre-exists.
    next: Plan T4 — doctor classification and npx platform status.
    context:
      intent: Behavior-preserving split and default-path gating per T3 acceptance.
      constraints: No commits; preserve Decision 54 upgrade/repair wording; monolith
        gate green.
      unknowns: Full legacy Python removal deferred post-T3 per plan.
      scope: packages/cli/src/validate/lifecycleAdapters*, cli/commands/validate.ts,
        cli/dispatch/check.ts, test/validate/lifecycleAdapters.test.ts
  - number: 652
    timestamp: 2026-06-10 00:04
    type: refactor
    phase: build
    what: "Plan T2 — dispatch hygiene and mechanical deduplication (3 parallel
      realisera workstreams merged): shared `cli/dispatch/argvParser.ts`;
      upgrade flags `--runtime`, `--opencode-config-dir`, `--update-packages`
      reject with explicit guidance; prime imports `detectV1ArtifactPairs`;
      `defaultAppHome` delegates to platform helpers; `isNpxBundleRoot` replaces
      `appContext.isNpxBundle`; `upgrade/cliProbe.ts` wires `inProcessProbe` as
      default; JSON helpers consolidated in `core/pyjson.ts`."
    inspiration: Orkestrera T2 parallelization — independent dedup streams before
      lifecycle adapter split.
    discovered: macOS `/var` vs `/private/var` realpath flake in npxBundle.test.ts
      is pre-existing and unrelated to isNpxBundle consolidation.
    verified: typecheck and build pass; 88/89 targeted vitest (argvParser,
      installRoot, prime, doctor, doctorUpgradeParity, upgradeOrchestrator, hook
      upgrade-flag tests); npxBundle realpath assertion is environmental.
    next: Plan T3 — lifecycle adapters node default, legacy Python gated.
    context:
      intent: Behavior-preserving mechanical dedup per T2 acceptance.
      constraints: No commits; monolith gate green.
      unknowns: npx platform status resolver deferred to T4.
      scope: packages/cli/src/cli/dispatch/*, installRoot.ts, appContext.ts,
        cliProbe.ts, core/pyjson.ts, prime.ts, doctor.ts
  - number: 651
    timestamp: 2026-06-09 23:50
    type: refactor
    phase: build
    what: "Plan T1 — canonical bundle and orientation contracts: added
      `core/jsonValue.ts`, `cli/contracts/bundleStatus.ts`, and
      `cli/contracts/orientationState.ts`; typed `buildDoctorStatus`,
      `publicDoctorStatus`, `collectOrientationState`, and orientation summary
      exports; removed local `Dict` aliases from prime, doctor, and orientation
      modules."
    inspiration: Thermo-nuclear audit chore pass plan T1 — foundations before
      dispatch dedup and prime decomposition.
    discovered: "`JsonObject` index signatures conflict with optional-field
      interfaces; plain named interfaces and `Record<string, unknown>` at JSON
      emit boundaries keep typecheck clean without changing fixture field
      names."
    verified: pnpm -C packages/cli typecheck pass; build pass; 55/55 targeted
      prime/doctor/orientation vitest cases green.
    next: Plan T2 — dispatch hygiene and mechanical deduplication.
    context:
      intent: Behavior-preserving typing at prime/doctor/orientation export boundaries
        per plan acceptance.
      constraints: No command redesign; preserve Decision 54 upgrade/repair wording;
        no commits this session.
      unknowns: Repo-wide Dict alias elimination remains deferred post-T1.
      scope: packages/cli/src/core/jsonValue.ts, packages/cli/src/cli/contracts/*,
        packages/cli/src/upgrade/doctor.ts,
        packages/cli/src/cli/commands/{prime,doctor}.ts,
        packages/cli/src/cli/orientation.ts
  - number: 650
    timestamp: 2026-06-07 12:00
    type: chore
    phase: audit
    what: Mobile monorepo closeout — archived agentera-mobile staging artifacts
      (D1–D5 decisions, re-visioning plan, LICENSE), synced D68 delivery-surface
      docs, extended docs.yaml index; packages/mobile stub landed with lefthook
      wired; mobile docs pass complete.
    inspiration: Monorepo consolidation plan closeout sequence; Decision 68 surface
      parity propagation.
    discovered: Staging repo decisions D1–D5 superseded by monorepo D67/D68 but
      preserved as compact archive entries; D3 provisional confidence on alias
      table retained in archive summary.
    verified: Artifact paths present; docs.yaml index entries for packages/mobile
      AGENTS, TODO, CHANGELOG, LICENSE; monorepo-plan and vision direction
      aligned with D68; mobile-open-decisions.md indexed (branch cleanup pass).
    next: SvelteKit scaffold with Cursor SDK integration (packages/mobile/TODO.md).
    context:
      intent: Close the docs/artifacts mobile monorepo integration pass without
        scaffolding application code or deciding CLI↔mobile integration.
      constraints: No SvelteKit scaffold; no CLI↔mobile integration decision.
      unknowns: CLI ↔ mobile state integration strategy remains open.
      scope: packages/mobile/, .agentera/*, README.md, docs/consolidation/,
        references/cli/vocabulary.md, lefthook
  - number: 649
    timestamp: 2026-06-05 14:06
    type: chore
    phase: audit
    what: v2→v3 TS rewrite completion plan closeout (T11). Archived the active plan
      to `.agentera/archive/plan-2026-06-04-v2-v3-ts-rewrite-completion.yaml`
      and cleared the plan slot after `3.0.0-dev.6` republication on `@next`.
    inspiration: Plan T11 acceptance — fold plan/progress metadata into the release
      commit; stable `@latest` 3.0.0 graduation deferred per plan surprises.
    discovered: |
      Full 3.0.0 npm publish was withdrawn; dev.6 realigns package version,
      T8 migration docs, and the unannounced stable successor gate.
    verified: pnpm -C packages/cli test (880/881); release-metadata validate pass.
    next: Stable 3.0.0 cutover when ready; monorepo/mobile docs pass remains WIP.
    context:
      intent: Close the v2→v3 rewrite completion track on the development channel.
      constraints: No separate bookkeeping commit; plan archive + progress cycle
        folded into release amend.
      unknowns: none
      scope: .agentera/plan.yaml, .agentera/archive/, .agentera/progress.yaml
  - number: 648
    timestamp: 2026-06-04 11:00
    type: feat
    phase: build
    what: T5 v2 handoff manifest reader (plan T5, TODO L24). Authored
      `references/cli/v3-handoff-manifest.schema.yaml`; added
      `packages/cli/src/migrate/v2HandoffManifest.ts` and wired migration
      cleanup preflight to read `v3-handoff.json` in <100ms with directory-scan
      fallback. Regression in
      `packages/cli/test/migrate/v2HandoffManifest.test.ts`. Python v2 writer
      backport on `main-t5-handoff-manifest`.
    inspiration: Plan T5 both-branch sequencing — v3 reader first, v2 writer on
      main, round-trip via shared schema.
    discovered: Freshness compares `written_at` and bundle marker mtime at second
      precision so sub-second marker mtimes do not false-stale manifests.
    verified: pnpm -C packages/cli test (866/866); typecheck; build;
      capability-contract validate; v2HandoffManifest.test.ts.
    next: Push `main-t5-handoff-manifest` writer commit; cross-branch schema diff;
      continue plan T1+.
    context:
      intent: O(1) v3 migration preflight over v2 user-data inventory via handoff
        manifest.
      constraints: Contract-first; additive reader; scan fallback when manifest
        absent; TODO L24 stays open until both branches agree.
      unknowns: none
      scope: references/cli/, packages/cli/src/migrate/, packages/cli/test/migrate/
archive:
  - summary: "Cycle 647 (2026-06-04): T7 cursor agent runtime surface regression
      (plan T7, TODO fix:3.0.0). On v3 projects (all twelve..."
  - summary: "Cycle 646 (2026-06-04): Remove v1 legacy cruft after the 3.0
      compatibility boundary (plan T4 / TODO L21). Deleted..."
  - summary: "Cycle 645 (2026-06-04): Decision 58 single-name protocol boundary
      (plan T3 / TODO L20). Added `artifactProtocolIds.ts` and
      `references/cli/single-name-protocol.yaml`; migrated..."
  - summary: "Cycle 644 (2026-06-04): npm `@next` version-divergence gate (TODO
      L21). Added `packages/cli/src/release/releaseMetadata.ts` that reads
      `registry.json` `skills[0].version` as the authoritative..."
  - summary: "Cycle 643 (2026-06-04): D56 parity closeout — ported the six remaining
      CLI surface families to TypeScript and proved..."
  - summary: "Cycle 642 (2026-06-04): Plan: Decision 56 parity closeout — drafted
      the first plan in the v3-rewrite sequence targeting..."
  - summary: "Cycle 641 (2026-06-03): D65 per-capability content surface — relocated
      12 capability prose files from
      skills/agentera/capabilities/<name>/instructions.md to
      packages/cli/src/capabilities/<name>/instructions.ts as..."
  - summary: "Cycle 640 (2026-06-03): Orkestrera task_queue dependency lookup —
      planTaskRefKeys normalization in orchestrationContext and
      dependencyReadyTasks; regression tests in
      packages/cli/test/cli/prime.test.ts."
  - summary: "Cycle 639 (2026-06-03): Durable pre-commit hooks —
      citation-anchor-todo.md fixture, stronger verifyWarnCitationAtLine,
      scripts/precommit-vitest.sh and precommit-pytest.sh, lefthook wiring,
      AGENTS.md emergency-bypass..."
  - summary: "Cycle 638 (2026-06-03): TODO.md GitHub checkbox parsing — shared
      parseTodoMarkdownListItem in packages/cli/src/cli/todoMarkdown.ts;
      loadTodoItems and queryTodo markdown paths classify..."
  - summary: "Cycle 635 (2026-06-03): Next-major doctor section — extended
      references/cli/update-channels.yaml with per-channel next_major on stable,
      added packages/cli/src/upgrade/nextMajorDoctor.ts and head-of-output..."
  - summary: "Cycle 634 (2026-06-03): Final state sync and plan archive. Archived
      the 2.7.7 plan to .agentera/archive/plan-2026-06-03.yaml, cleared the
      active..."
  - summary: "Cycle 633 (2026-06-03): 2.7.7 version bump and CHANGELOG update —
      bumped version_files to 2.7.7, promoted D66/WARN-citation/compaction
      bullets into CHANGELOG [2.7.7]; marked plan task 4 complete."
  - summary: "Cycle 632 (2026-06-03): 2.7.7 version bump and CHANGELOG update;
      marked plan task 4 complete."
  - summary: "Cycle 631 (2026-06-03): Inspektera WARN/FAIL file:line citation
      enforcement — added evaluator_handoff schema to
      capability-instruction-contract.yaml, evaluatorHandoffContract.ts registry
      with report..."
  - summary: "Cycle 630 (2026-06-03): D66 drop progress.yaml per-cycle commit field
      — removed PR6/PV2 commit requirements from the progress schema;..."
  - summary: "Cycle 629 (2026-06-02): Freeze source_contract internals — new oracle
      fixture (source-contract.json) and new parity test
      (sourceContractOracles.test.ts) that pin..."
  - summary: "Cycle 628 (2026-06-02): Freeze validate/verify family parity oracles —
      two new oracle fixtures (validate-family.json, verify-eval-family.json)
      and a new..."
  - summary: "Cycle 627 (2026-06-02): Wire the invalid-input envelope across the
      remaining CLI surfaces (lint, backfill, state, query, compact, schema,..."
  - summary: "Cycle 626 (2026-06-02): Freeze the invalid-input error envelope
      contract for the 3.0 TypeScript CLI — new emitInvalidInput helper,..."
  - summary: "Cycle 625 (2026-06-02): npm shim runBackend app-home branch now
      forwards the user's cwd to the spawned Python CLI..."
  - summary: "Cycle 624 (2026-06-02): Shim cwd preservation plan replaces the
      completed npm 3.x CLI parity tranche 1; targets the..."
  - summary: "Cycle 623 (2026-05-31): v2→v3 sandbox upgrade validation program (L0
      fixtures, harness, CI)."
  - summary: "Cycle 622 (2026-05-30): Parity tests, backport-safety gate, and
      migration plan closeout verification."
  - summary: "Cycle 621 (2026-05-30): Document upgrade paths and
      backport/cherry-pick policy."
  - summary: "Cycle 620 (2026-05-30): Wire phased upgrade orchestrator and CLI command."
  - summary: "Cycle 619 (2026-05-30): Port v2→v3 artifact, runtime rewire, and
      cleanup migration modules."
  - summary: "Cycle 618 (2026-05-30): Implement v2/v3 compatibility detection and
      guard rails."
  - summary: "Cycle 617 (2026-05-30): Implement update channel resolution module
      (channels.ts)."
  - summary: "Cycle 616 (2026-05-30): Define update-channel and major-boundary
      vocabulary authorities."
  - summary: "Cycle 615 (2026-05-29): Consolidate progress-commit guard onto shared
      module; release 2.7.6."
  - summary: "Cycle 614 (2026-05-29): Extract shared scripts/progress_commit.py;
      make `agentera check backfill` a thin consumer."
  - summary: "Cycle 613 (2026-05-29): Close compaction check/gate robustness and
      retention-cap dedup plan."
  - summary: "Cycle 612 (2026-05-29): Add progress commit-hash churn guard and
      `agentera check backfill`."
  - summary: "Cycle 611 (2026-05-29): Deduplicate session hook resolve_session_path
      and compact_entries helpers."
  - summary: "Cycle 610 (2026-05-29): Consolidate yaml.safe_load None-return
      handling across hooks and CLI."
  - summary: "Cycle 609 (2026-05-28): Tagged v2.7.5 with validation hardening and
      CLI namespace migration."
  - summary: "Cycle 608 (2026-05-27): Fix 16 degraded validation and CLI consistency
      issues across 7-task plan."
  - summary: "Cycle 607 (2026-05-26): Fix 5 degraded validation and CLI consistency
      issues in validate_artifact.py and scripts/agentera."
  - summary: "Cycle 606 (2026-05-26): Fix 7 critical validation and state-loading
      silent failures in validate_artifact.py and scripts/agentera."
