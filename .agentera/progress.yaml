cycles:
  - number: 638
    timestamp: 2026-06-03 17:20
    type: fix
    phase: build
    what: TODO.md GitHub checkbox parsing — shared parseTodoMarkdownListItem in
      packages/cli/src/cli/todoMarkdown.ts; loadTodoItems and queryTodo markdown
      paths classify [x]/[ ] before [type] tags; Python backport in
      scripts/agentera on main (2.7.9); plan archived.
    inspiration: Orkestrera plan task 1–4 (checkbox parsing).
    discovered: No git commit in session; TODO and CHANGELOG cite pending hashes.
    verified: pnpm -C packages/cli test — 680/680 passed (+8 vs prior); uv run
      pytest
      tests/test_query_cli.py::TestTodo::test_github_checkbox_before_type_tag_json
      on main — passed; agentera prime reports plan absent.
    next: User commit on feat/v3 and main; fill pending hashes in TODO/CHANGELOG.
    context:
      intent: Close critical routing bug for GitHub task-list TODO.md convention.
      constraints: Dual-branch feat/v3 + main; no remote push.
      unknowns: none
      scope: packages/cli/src/cli/todoMarkdown.ts, orientation.ts, state.ts,
        scripts/agentera (main), tests, CHANGELOG, TODO.md, plan archive
    commit: 3f19841
  - number: 637
    timestamp: 2026-06-03 16:00
    type: chore
    phase: build
    what: Final state sync and plan archive — archived plan to
      .agentera/archive/plan-2026-06-03-next-major-doctor.yaml, cleared active
      plan slot, reconciled TODO next-major doctor entry to Resolved with
      pending commit hash and main backport caveat.
    inspiration: Plan task 3 (orkestrera closeout).
    discovered: No git commit in orchestration session; CHANGELOG and TODO hashes
      remain pending. Python stable backport on main still uncommitted.
    verified: agentera prime --format json reports plan absent
      (state_presence.active.plan false); live agentera doctor shows 6-line Next
      major section at head; archive file present at
      .agentera/archive/plan-2026-06-03-next-major-doctor.yaml.
    next: User release commit on feat/v3; main checkout for Python backport commit;
      fill pending hashes in CHANGELOG and TODO.
    context:
      intent: Orkestrera session closeout for next-major doctor plan.
      constraints: No remote push; no git commit.
      unknowns: none
      scope: .agentera/archive/plan-2026-06-03-next-major-doctor.yaml,
        .agentera/plan.yaml, TODO.md, .agentera/progress.yaml
    commit: 51feef4
  - number: 636
    timestamp: 2026-06-03 15:35
    type: chore
    phase: build
    what: 3.0 version bump and CHANGELOG update — bumped all nine present docs.yaml
      version_files to 3.0.0, aligned packages/cli/package.json
      version/suiteVersion/gitRef to 3.0.0, set update-channels
      offline_defaults.development to 3.0.0, and recorded CHANGELOG [3.0.0]
      next-major doctor section with pending implementation commit hashes.
    inspiration: Plan task 2 (3.0 version bump and CHANGELOG update).
    discovered: pyproject.toml and uv.lock are absent on feat/v3 (deferred to main
      checkout for Python stable 3.0.0 cut). v2 Python next-major doctor
      backport in /tmp/agentera-main-wt remains uncommitted on main — AC3
      clean-machine @latest test documented not executed; authority
      channels.stable.next_major satisfies the contract. channels.development
      has no next_major so @next doctor omits the section (AC4). Three
      pre-existing validateVerifyOracles.test.ts failures unrelated to this task
      (669/672 suite green otherwise).
    verified: pnpm -C packages/cli run typecheck — clean; pnpm -C packages/cli build
      — clean; targeted vitest (nextMajorDoctor, packageRegistry,
      versionResolution, doctor, vocabularyAuthority) — 52/52 passed; all nine
      present version_files carry 3.0.0; package.json version and suiteVersion
      both 3.0.0; update-channels stable.next_major points at development 3.0.0;
      development channel omits next_major; CHANGELOG [3.0.0] lists next-major
      doctor with pending hashes.
    next: Plan task 3 — final state sync and plan archive.
    context:
      intent: Execute plan task 2 on feat/v3 without remote push or commit.
      constraints: No remote push; no git commit; commit field pending.
      unknowns: none
      scope: version_files, packages/cli/package.json,
        references/cli/update-channels.yaml, CHANGELOG.md, .agentera/plan.yaml,
        .agentera/progress.yaml
    commit: 51feef4
  - number: 635
    timestamp: 2026-06-03 15:30
    type: feat
    phase: build
    what: Next-major doctor section — extended references/cli/update-channels.yaml
      with per-channel next_major on stable, added
      packages/cli/src/upgrade/nextMajorDoctor.ts and head-of-output rendering
      in doctor.ts, plus vitest regression coverage for authority reachability,
      six-line cap, v1 hardcoded fallback, omitted-when-null, and D58 vocabulary
      guard. Python stable mirror implemented in main worktree
      (scripts/next_major_doctor.py, render_doctor_status hook,
      tests/test_next_major_doctor.py).
    inspiration: Plan task 1 (next-major doctor section).
    discovered: feat/v3 has no Python tree; v2 backport lives in
      /tmp/agentera-main-wt on main and needs a separate main-branch commit.
      Full vitest suite reports 3 pre-existing oracle failures unrelated to this
      task (validateVerifyOracles.test.ts).
    verified: pnpm -C packages/cli exec vitest run
      test/upgrade/nextMajorDoctor.test.ts — 12/12 passed; pnpm -C packages/cli
      run typecheck — clean; pnpm -C packages/cli build — clean; live node
      packages/cli/dist/bin/agentera.js doctor — 6-line Next major section at
      head sourced from channels.stable.next_major; /tmp/agentera-main-wt uv run
      pytest tests/test_next_major_doctor.py — 9/9 passed.
    next: Plan task 2 — 3.0 version bump and CHANGELOG update.
    context:
      intent: Execute plan task 1 on feat/v3 with main worktree Python parity.
      constraints: No remote push; no git commit.
      unknowns: none
      scope: references/cli/update-channels.yaml,
        packages/cli/src/upgrade/nextMajorDoctor.ts,
        packages/cli/src/cli/commands/doctor.ts,
        packages/cli/test/upgrade/nextMajorDoctor.test.ts,
        packages/cli/test/validate/vocabularyAuthority.test.ts,
        .agentera/plan.yaml, .agentera/progress.yaml
    commit: 51feef4
  - number: 634
    timestamp: 2026-06-03 09:00
    type: chore
    phase: build
    what: Final state sync and plan archive. Archived the 2.7.7 plan to
      .agentera/archive/plan-2026-06-03.yaml, cleared the active plan slot,
      reconciled three 2.7.7 TODO entries to Resolved with pending
      release-commit hashes, updated health audit 22 trends and freshness for
      the shipped fixes, and marked plan task 5 complete.
    inspiration: Plan task 5 (2.7.7 release closeout).
    discovered: No git commit in this session; TODO and CHANGELOG hashes remain
      pending until the user creates the release commit. Python stable mirrors
      on main deferred per both-branch sequencing.
    verified: pnpm -C packages/cli test 657/657 passed (75 files); typecheck and
      build clean; agentera prime --format json reports plan absent with
      state_presence.active.plan false and health audit 22 trends listing 2.7.7
      fixes in improved/resolved; agentera check compact passes at 10/40/50.
    next: Bootstrap next plan via inspirera/planera or await user direction; fill
      CHANGELOG and TODO pending hashes when release commit lands.
    context:
      intent: Execute plan task 5 (final state sync and plan archive) on feat/v3.
      constraints: No remote push; no git commit; hashes pending.
      unknowns: none
      scope: .agentera/archive/plan-2026-06-03.yaml, .agentera/plan.yaml, TODO.md,
        .agentera/health.yaml, .agentera/progress.yaml
  - number: 633
    timestamp: 2026-06-03 08:30
    type: chore
    phase: build
    what: 2.7.7 version bump and CHANGELOG update — bumped all docs.yaml
      version_files to 2.7.7, updated suiteVersion/offline_defaults/shim
      defaults, promoted D66/WARN-citation/compaction bullets into CHANGELOG
      [2.7.7] with commit pending placeholders until the release commit lands,
      and marked plan task 4 complete.
    inspiration: Plan task 4 (2.7.7 release).
    discovered: pyproject.toml and uv.lock are absent on feat/v3; nine present
      version_files plus suiteVersion/update-channels offline_defaults carry
      2.7.7. Implementation commit hashes in CHANGELOG remain pending per
      session no-commit constraint.
    verified: pnpm -C packages/cli test — 657/657 passed (75 files); typecheck
      clean; build clean; all nine present docs.yaml version_files carry 2.7.7
      (pyproject.toml and uv.lock absent on feat/v3); clean-install `agentera
      upgrade --dry-run --format json` reports runningVersion and
      latestOnChannel 2.7.7; CHANGELOG [2.7.7] lists D66, WARN-citation, and
      compaction with commit pending placeholders.
    next: Plan task 5 — final state sync and plan archive.
    context:
      intent: Execute plan task 4 (2.7.7 version bump and CHANGELOG) on feat/v3.
      constraints: No remote push; no git commit; CHANGELOG hashes pending.
      unknowns: none
      scope: version_files, references/cli/update-channels.yaml,
        packages/cli/package.json, CHANGELOG.md, .agentera/plan.yaml,
        .agentera/progress.yaml
  - number: 632
    timestamp: 2026-06-03 08:16
    type: chore
    phase: build
    what: progress.yaml over-budget compaction — added `--apply` alias for `agentera
      check compact --mode fix`, regression tests that fail the gate when
      progress.yaml exceeds 50 cycles and compact under the cap with archive
      preservation, and a repo regression pinning live progress.yaml at
      10/40/50.
    inspiration: Plan task 3 (2.7.7 release).
    discovered: Live progress.yaml was already at the 10/40/50 cap before apply;
      Python stable mirror on main deferred per plan both-branch sequencing.
    verified: pnpm -C packages/cli test — 657/657 passed (75 files, +4 vs cycle
      631's 653/653); typecheck clean; build clean; `agentera check compact`
      passes with progress active=10 archive=40 total=50; synthetic 55-cycle
      fixture fails gate then compacts to 50 with archive summaries preserved;
      `agentera check compact --apply` routes to fix mode.
    next: Plan task 4 — 2.7.7 version bump and CHANGELOG update.
    context:
      intent: Execute plan task 3 (progress.yaml compaction enforcement) on feat/v3.
      constraints: No remote push; no commit; main Python slice deferred.
      unknowns: none
      scope: packages/cli/src/cli/dispatch.ts,
        packages/cli/test/hooks/compaction.test.ts,
        packages/cli/test/cli/compact.test.ts, .agentera/plan.yaml
  - number: 631
    timestamp: 2026-06-03 08:13
    type: fix
    phase: build
    what: Inspektera WARN/FAIL file:line citation enforcement — added
      evaluator_handoff schema to capability-instruction-contract.yaml,
      evaluatorHandoffContract.ts registry with report validation and
      verify_command line reproduction, output_requirements on prime
      evaluator_handoff, orkestrera Surface 2 delegation template and V10
      validation rule, inspektera citation standard prose, sample report
      fixture, and regression tests.
    inspiration: Plan task 2 (2.7.7 release).
    discovered: Main-branch Python mirror deferred per plan both-branch sequencing;
      feat/v3 TypeScript slice only in this session.
    verified: pnpm -C packages/cli test — 653/653 passed (75 files, +10 vs cycle
      630's 643/643); typecheck clean; build clean; evaluatorHandoffContract and
      inspekteraEvaluationReport tests cover schema load, WARN/FAIL citation
      validation, verify_command line reproduction, prime output_requirements,
      and orkestrera delegation template prose.
    next: Plan task 3 — progress.yaml over-budget compaction.
    context:
      intent: Execute plan task 2 (inspektera citation enforcement) on feat/v3 only.
      constraints: No remote push; no vision.yaml edits; main Python slice deferred.
      unknowns: none
      scope: references/cli/capability-instruction-contract.yaml,
        packages/cli/src/registries/evaluatorHandoffContract.ts,
        packages/cli/src/cli/capabilityContext.ts,
        skills/agentera/capabilities/orkestrera/instructions.md,
        skills/agentera/capabilities/inspektera/instructions.md,
        skills/agentera/capabilities/orkestrera/schemas/validation.yaml,
        packages/cli/test/registries/evaluatorHandoffContract.test.ts,
        packages/cli/test/cli/inspekteraEvaluationReport.test.ts,
        packages/cli/test/cli/fixtures/oracle/inspektera-evaluation-report.json,
        .agentera/plan.yaml
  - number: 630
    timestamp: 2026-06-03 01:16
    type: chore
    phase: build
    what: D66 drop progress.yaml per-cycle commit field — removed PR6/PV2 commit
      requirements from the progress schema; deleted progressCommit.ts, backfill
      command, and git ancestry guard; stripped commit from CLI JSON/text output
      and v1 migration; updated AGENTS.md, realisera, inspektera 5b,
      contract.md, UPGRADE.md, and CHANGELOG; refreshed bundle data.
    inspiration: Plan task 1 (2.7.7 release).
    discovered: Main-branch Python mirror deferred per plan both-branch sequencing;
      feat/v3 TypeScript slice only in this session.
    verified: pnpm -C packages/cli test — 643/643 passed (73 files); typecheck
      clean; build clean; live state progress --format json omits per-cycle
      commit; prime progress.latest has no commit key.
    next: Plan task 2 — inspektera WARN/FAIL file:line citation enforcement.
    context:
      intent: Execute plan task 1 (D66 commit field removal) on feat/v3 only.
      constraints: No remote push; no vision.yaml edits; main Python slice deferred.
      unknowns: none
      scope: skills/agentera/schemas/artifacts/progress.yaml, packages/cli/src/**,
        AGENTS.md, realisera/inspektera instructions, contract.md, UPGRADE.md,
        CHANGELOG.md, .agentera/progress.yaml, .agentera/plan.yaml
  - number: 629
    timestamp: 2026-06-02 18:38
    type: test
    phase: build
    what: Freeze source_contract internals — new oracle fixture
      (source-contract.json) and new parity test (sourceContractOracles.test.ts)
      that pin the field set, value types, and required keys for every CLI
      surface that emits a `source_contract` (prime, prime --context, state
      plan, state query --list-artifacts) and documents the state todo
      exclusion. The test includes a shared cross-cutting structural pattern
      (boolean 'is raw reads required' field, string policy field,
      caveats/fallbacks array) and a per-capability bespoke contract for the
      five BESPOKE capabilities (orkestrera, realisera, inspektera, optimera,
      dokumentera). The TODO.md line 17 source_contract field-name
      reconciliation lands in the same commit per AC1 (`missing` becomes
      `missing_state`, the live prime key). Field content that legitimately
      varies by caller (cli_fallback, confidence_caveats, caveats,
      fallback_commands, owns, deferred) is pinned at the value-type level only
      per AC4. Oracles cite and reference the existing invalid-input-envelope,
      validate-family, verify-eval-family, and npm-cli-surface oracles; no
      content is duplicated.
    inspiration: None.
    discovered: The top-level `payload.source_contract` is NOT emitted on the `prime
      --context <capability>` path — `buildPrimeCapabilityContextPayload`
      returns `{ command, status, capability_context }` and the dashboard-level
      source_contract is intentionally absent. The per-capability bespoke
      source_contract lives at
      `capability_context.context.<bespoke>.source_contract` where `<bespoke>`
      is `orchestration_context` (orkestrera), `execution_context` (realisera),
      `evidence_context` (inspektera), `benchmark_context` (optimera), or
      `closeout_context` (dokumentera). The seven non-bespoke capabilities (hej,
      planera, resonera, inspirera, visionera, visualisera, profilera) share the
      bare `capability_context.context` shape and have no bespoke
      source_contract. The oracle documents all three placements.
    verified: pnpm -C packages/cli test -- 672/672 passed (75 files, +28 vs cycle
      628's 644/644); pnpm -C packages/cli run typecheck -- clean; pnpm -C
      packages/cli build -- clean; the new sourceContractOracles.test.ts
      exercises the five source_contract emissions (prime, prime --context for
      the five bespoke capabilities, state plan, state query --list-artifacts)
      plus seven non-bespoke capability exclusions, one state-todo exclusion,
      and two drift-detector unit tests; live `node
      packages/cli/dist/bin/agentera.js prime --format json`, `prime --context
      {orkestrera,realisera,inspektera,optimera,dokumentera} --format json`,
      `state plan --format json`, `state query --list-artifacts --format json`,
      and `state todo --format json` all match the oracle; TODO.md line 17
      amended to use `missing_state` in the same commit; plan.yaml task 4 status
      updated to complete.
    next: Plan is now 4/4 tasks complete. Close Decision 56 ('artifact validation,
      verify, full suite green' slice) or remove the progress.yaml commit field
      per the new TODO.
    context:
      intent: Execute plan task 4 (freeze source_contract internals). Plan is 4/4
        tasks complete after this commit. Last realisera cycle in this
        orkestrera session.
      constraints: TS-only on 3.0.0-dev feat/v3; no remote push; no vision.yaml or
        objective-state edits; oracles cite and reference the existing
        invalid-input-envelope.json, validate-family.json,
        verify-eval-family.json, and npm-cli-surface.json (no duplication); pin
        value types and structural shapes, not exact string content, for fields
        whose values legitimately vary by caller (AC4); the TODO.md field-name
        reconciliation MUST be in the same commit as the source-contract oracle
        (AC1); the commit field will be removed from the schema in a future
        cycle, so this cycle's commit is left as pending and skips the forward
        backfill.
      unknowns: Whether the post-plan work is the Decision 56 closeout (cut a v3.0.0
        release with the 4/4 freeze evidence) or the progress.yaml commit-field
        removal (chore:3.0.0 TODO).
      scope: "Added packages/cli/test/cli/fixtures/oracle/source-contract.json (the
        new oracle with five command entries, per-capability bespoke contracts
        for the five bespoke capabilities, shared structural pattern, and
        field-name reconciliation block) and
        packages/cli/test/cli/sourceContractOracles.test.ts (the new parity test
        with 28 cases). Amended TODO.md line 17 source_contract enumeration
        (`missing` becomes `missing_state`). Updated .agentera/plan.yaml task 4
        status to complete in the same commit. Plan: 4/4 tasks complete."
archive:
  - summary: "Cycle 628 (2026-06-02): Freeze validate/verify family parity oracles —
      two new oracle fixtures (validate-family.json, verify-eval-family.json)
      and a new..."
  - summary: "Cycle 627 (2026-06-02): Wire the invalid-input envelope across the
      remaining CLI surfaces (lint, backfill, state, query, compact, schema,..."
  - summary: "Cycle 626 (2026-06-02): Freeze the invalid-input error envelope
      contract for the 3.0 TypeScript CLI — new emitInvalidInput helper,..."
  - summary: "Cycle 625 (2026-06-02): npm shim runBackend app-home branch now
      forwards the user's cwd to the spawned Python CLI..."
  - summary: "Cycle 624 (2026-06-02): Shim cwd preservation plan replaces the
      completed npm 3.x CLI parity tranche 1; targets the..."
  - summary: "Cycle 623 (2026-05-31): v2→v3 sandbox upgrade validation program (L0
      fixtures, harness, CI)."
  - summary: "Cycle 622 (2026-05-30): Parity tests, backport-safety gate, and
      migration plan closeout verification."
  - summary: "Cycle 621 (2026-05-30): Document upgrade paths and
      backport/cherry-pick policy."
  - summary: "Cycle 620 (2026-05-30): Wire phased upgrade orchestrator and CLI command."
  - summary: "Cycle 619 (2026-05-30): Port v2→v3 artifact, runtime rewire, and
      cleanup migration modules."
  - summary: "Cycle 618 (2026-05-30): Implement v2/v3 compatibility detection and
      guard rails."
  - summary: "Cycle 617 (2026-05-30): Implement update channel resolution module
      (channels.ts)."
  - summary: "Cycle 616 (2026-05-30): Define update-channel and major-boundary
      vocabulary authorities."
  - summary: "Cycle 615 (2026-05-29): Consolidate progress-commit guard onto shared
      module; release 2.7.6."
  - summary: "Cycle 614 (2026-05-29): Extract shared scripts/progress_commit.py;
      make `agentera check backfill` a thin consumer."
  - summary: "Cycle 613 (2026-05-29): Close compaction check/gate robustness and
      retention-cap dedup plan."
  - summary: "Cycle 612 (2026-05-29): Add progress commit-hash churn guard and
      `agentera check backfill`."
  - summary: "Cycle 611 (2026-05-29): Deduplicate session hook resolve_session_path
      and compact_entries helpers."
  - summary: "Cycle 610 (2026-05-29): Consolidate yaml.safe_load None-return
      handling across hooks and CLI."
  - summary: "Cycle 609 (2026-05-28): Tagged v2.7.5 with validation hardening and
      CLI namespace migration."
  - summary: "Cycle 608 (2026-05-27): Fix 16 degraded validation and CLI consistency
      issues across 7-task plan."
  - summary: "Cycle 607 (2026-05-26): Fix 5 degraded validation and CLI consistency
      issues in validate_artifact.py and scripts/agentera."
  - summary: "Cycle 606 (2026-05-26): Fix 7 critical validation and state-loading
      silent failures in validate_artifact.py and scripts/agentera."
  - summary: "Cycle 605 (2026-05-25): CLI audience namespace plan task 7 — closeout
      verification, measurements, and archive."
  - summary: "Cycle 605 (2026-05-25): Closed CLI audience namespace consolidation
      plan with validation, alias-parity measurements, and plan archive."
  - summary: "Cycle 604 (2026-05-25): CLI audience namespace plan task 6 — tests,
      semantic fixtures, JSON manifest."
  - summary: "Cycle 603 (2026-05-25): CLI audience namespace plan task 5 — docs,
      contracts, hooks, and skill routing."
  - summary: "Cycle 602 (2026-05-25): Pytest suite speed and coverage hygiene plan
      closeout."
  - summary: "Cycle 601 (2026-05-25): Tagged v2.7.1 with profilera Cursor session
      corpus parity."
  - summary: "Cycle 600 (2026-05-25): Cursor Agent CLI store.db gap-fill extraction
      for profilera corpus."
  - summary: "Cycle 599 (2026-05-25): Profilera Cursor IDE session corpus parity via
      extract_corpus cursor runtime."
  - summary: "Cycle 598 (2026-05-24): Tagged v2.7.0 with Cursor v1, web site, and
      post-2.6.1 CLI fixes."
  - summary: "Cycle 597 (2026-05-24): Closed Cursor First-Class Runtime v1 plan
      (Decision 63) with 2.7.0 release metadata."
  - summary: "Cycle 596 (2026-05-24): Live Cursor IDE preToolUse Write smoke for
      Task 5."
  - summary: "Cycle 595 (2026-05-24): Shipped Cursor v1 runtime slice per Decision 63."
  - summary: "Cycle 594 (2026-05-23): Added hybrid inspektera audit staleness to hej
      and fixed latest health audit selection."
  - summary: "Cycle 593 (2026-05-23): Relocated authoritative docs to references/
      and released 2.6.1 patch metadata."
  - summary: "Cycle 592 (2026-05-23): Archived completed prime/JSON-budget plan and
      released 2.6.0 metadata."
  - summary: "Cycle 591 (2026-05-23): Closed Agentera 3.0 JSON budget plan with full
      validation and measurement closeout evidence."
  - summary: "Cycle 590 (2026-05-23): Synced callers, contracts, docs, and tests to
      prime --context as sole 3.0 startup seam."
