cycles:
  - number: 711
    timestamp: 2026-07-11 22:16
    type: feat
    phase: build
    what: Integrated lifecycle repair and convergence into the upgrade namespace.
    inspiration: Active plan Task 7 and Decision 92.
    discovered: The existing app runtime phase would publish the same resources
      before ownership recording, and the npm bundle omitted declared repair
      sources; lifecycle-selected upgrades now make the shared engine the sole
      runtime mutator and package those sources.
    verified: pnpm -C packages/cli run typecheck and build passed. pnpm -C
      packages/cli run test --reporter=json
      --outputFile=/tmp/agentera-task7-final-tests.json passed 158 files/1461
      tests. lifecycle-adapters, capability build, capability-contract,
      cross-capability, app-home-contract, and vocabularyAuthority validation
      passed. schema, check compact, git diff --check, and lint passed; lint
      retained the 99-warning/0-error baseline. npm pack --dry-run --json
      --ignore-scripts reported 557 files with lifecycle code, contracts, and
      declared repair sources; production source/dist/bundle contained no
      AGENTERA_TEST token. Preview tree/byte snapshots were unchanged and
      native-command traps stayed untouched. Crash tests covered interruption
      before/after resource and ledger publication, restart, independent
      continuation, dependency skip, and noop convergence. Live prime and doctor
      agreed on blocked status for all four runtimes.
    next: Task 8 owns the broad release docs, changelog, TODO, version, and package
      synchronization sweep; Decision 92 satisfaction remains
      user-review-needed.
    context:
      intent: "Execute active plan Task 7: provide truthful read-only lifecycle
        preview, explicitly approved owned apply, crash recovery, independent
        failure continuation, and convergent reruns."
      constraints: Preserved user TODO, decisions, active plan status, vision, native
        trust/auth/package ownership, app-only upgrade behavior, and no-push
        boundary.
      unknowns: No Task 7 implementation unknowns remain; release-surface
        synchronization is intentionally deferred to Task 8.
      scope: Upgrade CLI/help/schema; lifecycle planner, journal, contracts, app
        integration, prime/doctor ledger reads; runtime
        fixtures/recovery/package tests and npm repair-source bundle inputs.
  - number: 710
    timestamp: 2026-07-11 21:26
    type: fix
    phase: build
    what: Removed production lifecycle evidence injection and split synthetic parity
      from real CLI coverage.
    inspiration: Final Task 6 audit after Cycle 709 overstated child-process fixture
      coverage.
    discovered: Synthetic host-owned trust and ownership cases remain useful only
      when clearly confined to imported observer and projection functions under
      packages/cli/test; production CLI coverage must rely on real filesystem
      observation.
    verified: "Actual built CLI coverage runs prime and doctor against real temp
      filesystem state. The production guard compares baseline calls with
      NODE_ENV=test plus fake AGENTERA_TEST variables and a valid spoof file:
      lifecycle output is identical, doctor remains exit 1, no target writes
      occur, and trapped native binaries are not invoked. Separate real CLI
      tests cover empty filesystem text output and capability startup. Seven
      synthetic cases run only in a test-only harness that directly calls
      exported observer and summary functions, not prime or doctor: unknown
      trust, denied trust, Cursor conditional IDE, shadowing, absent host and
      missing parent, unowned collision, and all-ready. Focused tests passed 2
      files/21 tests; full tests passed 155 files/1436 tests. Typecheck and
      build passed. Lifecycle-adapters, status, build, capability-contract,
      cross-capability, app-home-contract, vocabulary, and schema checks passed.
      Live built prime lifecycle was 2142 bytes; doctor JSON was 38780 bytes and
      exited 1. Lint reported 99 pre-existing warnings/0 errors. A 547-file npm
      tarball contained dist and bundle lifecycle authority surfaces and no
      lifecycle injection token; production source, rebuilt dist, and bundle
      also contained none."
    next: Re-run the independent Task 6 audit against Cycle 710; keep Task 6 in
      progress and leave Tasks 7 and 8 untouched.
    context:
      intent: Complete the final Task 6 retry by making runtime lifecycle evidence
        impossible to spoof through NODE_ENV or AGENTERA_TEST variables.
      constraints: Preserved support-floor, blocker, doctor-exit, shared-snapshot,
        prime-budget, Task 5 retirement, Cursor identity, user
        TODO/decisions/plan/.codex changes, vision, plan status, native package
        managers, remotes, and Tasks 7 and 8.
      unknowns: None in retry scope; live trust evidence remains host-owned and
        unknown where the host exposes no authoritative read-only signal.
      scope: Lifecycle snapshot production source; lifecycle diagnosis tests; npm
        packaging policy test; typed progress evidence only.
  - number: 709
    timestamp: 2026-07-11 21:10
    type: fix
    phase: build
    what: Made lifecycle support-floor evidence fail closed and aligned doctor
      release exits with prime status.
    inspiration: Task 6 retry audit, Decision 92, and Cycle 708 evidence correction.
    discovered: The legacy doctor app envelope can remain up_to_date while additive
      lifecycle readiness controls the process exit; the closed Python parity
      row therefore needs an explicit lifecycle exit override without changing
      payload parity.
    verified: "Seven child-process fixtures ran prime and doctor with identical
      inputs for unknown trust, denied trust, Cursor CLI-only and conditional
      IDE, shadowing, absent host and missing parent, unowned collision, and
      all-ready: 14 commands total; prime exits 0,0,0,0,0,0,0 and doctor exits
      1,1,0,0,1,0,0. Every pair compared authority, IDs, releaseBlocked, runtime
      and surface status, support floors, blockers/actions, and observed no
      target writes or native process invocation. Prime summaries stayed below
      4096 bytes; live built prime was 2142 lifecycle bytes versus 70745-byte
      doctor JSON, where app status remained up_to_date, lifecycle
      releaseBlocked was true, and doctor exited 1. Focused verification passed
      8 files/123 tests and final 4 files/61 tests; full pnpm -C packages/cli
      test passed 155 files/1435 tests. Typecheck and build passed. Built
      lifecycle-adapters, capability status, capability build,
      capability-contract, cross-capability, app-home-contract,
      vocabularyAuthority, schema, compact, lint, and git diff checks passed;
      lint reported 99 pre-existing warnings/0 errors."
    next: Re-run the independent Task 6 audit against Cycle 709; keep Task 7
      mutation UX and Task 8 release sweep untouched.
    context:
      intent: Retry Task 6 after audit by enforcing Decision 92 mandatory evidence and
        replacing overstated parity coverage with explicit CLI fixtures.
      constraints: Preserved Task 5 retirement, Task 4 adapters, Task 3 safety, user
        TODO/decisions/plan/.codex changes, vision, active plan status, native
        package managers, and remotes. No Task 7 or Task 8 work.
      unknowns: None in Task 6 scope; runtime trust remains host-owned evidence and
        unknown on the live host.
      scope: Lifecycle authority and adapter contracts, shared lifecycle
        state/snapshot projection, doctor exit and blocker detail, schema
        discovery, parity expectations, and runtime/CLI tests.
  - number: 708
    timestamp: 2026-07-11 20:47
    type: feat
    phase: build
    what: Integrated one authoritative read-only runtime lifecycle snapshot into
      bounded prime orientation and detailed doctor diagnosis.
    inspiration: Active plan Task 6 and Decision 92 lifecycle support-floor and
      trust policy.
    discovered: The legacy doctor app-status parity envelope needed to remain
      independently comparable after adding the additive runtime_lifecycle
      diagnosis; its oracle now excludes only that extension.
    verified: Focused pnpm -C packages/cli test
      test/cli/lifecycleStatusDiagnosis.test.ts
      test/runtime/lifecycleAdapters.test.ts test/cli/prime.test.ts
      test/cli/doctor.test.ts test/cli/schema.test.ts
      test/cli/sourceContractOracles.test.ts test/cli/npmParityMatrix.test.ts
      passed 7 files/101 tests. Full pnpm -C packages/cli test passed 155
      files/1,422 tests. pnpm -C packages/cli run typecheck and pnpm -C
      packages/cli build passed. pnpm -C packages/cli run lint returned 99
      pre-existing warnings/0 errors. Local built CLI check validate
      lifecycle-adapters, capability status, capability build,
      capability-contract, cross-capability, app-home-contract, and
      vocabularyAuthority passed. node packages/cli/dist/bin/agentera.js schema
      --format json parsed with lifecycle contracts; node
      packages/cli/dist/bin/agentera.js check compact passed 9 artifacts with
      zero over-limit/errors; git diff --check passed. Child-process fixtures
      observed no target writes or native invocation, retained complete doctor
      JSON above 65,536 bytes, and covered absent hosts, missing parents,
      collisions, shadowing, trust distinctions, unsupported categories, Cursor
      CLI/IDE conditions, and all-ready state. Live node
      packages/cli/dist/bin/agentera.js prime --format json --fields
      runtime_lifecycle was 2,890 bytes versus 59,502-byte doctor detail;
      authority, snapshot/status vocabulary, IDs, statuses, support floors,
      blockers, actions, and surfaces agreed.
    next: Task 7 may consume this snapshot for mutation UX without changing Task 6
      diagnosis semantics.
    context:
      intent: Complete active plan Task 6 with four-runtime status parity, truthful
        category evidence, and user-owned native steps.
      constraints: No Task 7 upgrade preview/apply, Task 8 release sweep,
        cleanup/import semantic change, vision edit, plan status edit,
        package-manager/native action, or remote push.
      unknowns: None; current-host degraded states reflect observed unknown trust and
        optional lifecycle gaps, not support-floor failure.
      scope: Lifecycle snapshot service; prime and doctor JSON/text projections;
        schema/help compatibility; integration, child-process, parity, and
        regression tests.
  - number: 707
    timestamp: 2026-07-11 17:25
    type: fix
    phase: build
    what: Removed the last active-style Claude question-tool claim and finalized
      canonical retirement surface enforcement.
    verified: Final focused pnpm -C packages/cli test
      test/runtime/retiredRuntimeSurfacePolicy.test.ts
      test/cli/profileRuntimePolicy.test.ts test/cli/schema.test.ts
      test/runtime/retiredRuntimeCleanup.test.ts passed 4 files/30 tests. Final
      full pnpm -C packages/cli test passed 154 files/1,417 tests. pnpm -C
      packages/cli run typecheck passed; standalone pnpm -C packages/cli build
      passed after one concurrent verification attempt raced dist cleanup with
      Directory not empty. pnpm -C packages/cli run lint returned 99 warnings/0
      errors. All 12 raw registered capability modules decoded identically to
      served CAPABILITY_INSTRUCTIONS; raw, served, registry-derived support, and
      public install scans found zero current Claude Task-tool, question-tool,
      supported-list, --no-claude, or install-command patterns. Rebuilt prime
      served registry-declared OpenCode, Codex, one Cursor identity including
      cursor-agent, and Copilot substrates; discuss used generic host-native
      question tooling; profile retained only explicit --import-source claude
      historical import. Schema active IDs were opencode,codex,cursor,copilot;
      cursor-agent was inactive and Claude retired. Capability
      discuss/orchestrate/profile/build, capability-contract,
      lifecycle-adapters, app-home-contract, vocabularyAuthority,
      cross-capability, compaction, and git diff checks passed. Cleanup and
      schema regressions preserved AC2 and AC3.
    next: Re-run Task 5 evidence audit against Cycle 707.
    context:
      intent: Supersede incomplete Cycle 706 pattern coverage with final raw and
        served evidence for Task 5 while preserving cleanup, historical import,
        and in-progress plan state.
  - number: 706
    timestamp: 2026-07-11 17:22
    type: fix
    phase: build
    what: Canonicalized Claude retirement policy in raw capability instructions and
      made registry-derived surface scans enforce it.
    verified: Focused pnpm -C packages/cli test
      test/runtime/retiredRuntimeSurfacePolicy.test.ts
      test/cli/profileRuntimePolicy.test.ts test/cli/schema.test.ts
      test/runtime/retiredRuntimeCleanup.test.ts passed 4 files/29 tests;
      policy-only rerun passed 1 file/11 tests. Full pnpm -C packages/cli test
      passed 154 files/1,416 tests. pnpm -C packages/cli run typecheck and pnpm
      -C packages/cli build passed; pnpm -C packages/cli run lint returned 99
      warnings/0 errors. All 12 raw registered capability modules decoded
      identically to served CAPABILITY_INSTRUCTIONS and raw plus served policy
      scans found zero current Claude patterns. Rebuilt prime served only
      registry-declared OpenCode, Codex, Cursor, and Copilot substrates; profile
      retained only explicit --import-source claude historical import. Schema
      active IDs were opencode,codex,cursor,copilot; cursor-agent was inactive
      and Claude retired. Capability orchestrate/profile/build,
      capability-contract, lifecycle-adapters, app-home-contract,
      vocabularyAuthority, cross-capability, compaction, and git diff checks
      passed. Cleanup and schema regressions preserved AC2 and AC3.
    next: Re-run Task 5 evidence audit against Cycle 706.
    context:
      intent: Close Task 5 AC1 canonical-source and evidence gaps while preserving AC2
        cleanup and AC3 historical import behavior; leave plan status unchanged.
  - number: 705
    timestamp: 2026-07-11 16:55
    type: fix
    phase: build
    what: "Retried Task 5 after audit: removed active Claude advertisements, drained
      large CLI output, and restricted retired cleanup to exact legacy
      ownership."
    inspiration: Task 5 retry 1 independent audit findings.
    discovered: The bin called process.exit before piped stdout drained; retired
      cleanup inherited active-resource semantics where managed and legacy
      ledger records both authorize removal.
    verified: "Focused: pnpm -C packages/cli test test/cli/schema.test.ts
      test/runtime/retiredRuntimeCleanup.test.ts
      test/runtime/retiredRuntimeSurfacePolicy.test.ts
      test/cli/profileRuntimePolicy.test.ts passed 4 files/21 tests. Full pnpm
      -C packages/cli test passed 154 files/1,408 tests; typecheck/build passed.
      Child schema JSON parsed at 103,242 bytes with active IDs
      opencode,codex,cursor,copilot; cursor-agent inactive and Claude retired.
      Seven current install surfaces had 0 Claude claims; served orchestrate had
      no Claude Task-tool claim. Cleanup tests observed managed
      blocked/unchanged, exact legacy success then noop, and
      missing/malformed/changed/identity-swapped/parent-swapped evidence blocked
      with data preserved. Default analytics performed no Claude read; opt-in
      records retained historical provenance and default exclusion.
      Lifecycle/build-capability/capability-contract/app-home/vocabulary
      validations, extraction parity, compaction, and diff checks passed. Lint:
      99 warnings/0 errors. vp run web:check hit known cache database version
      12; pnpm -C packages/web exec astro check passed 11 files/0 diagnostics."
    next: Keep Task 5 in_progress for independent re-audit; Task 8 owns changelog,
      version, TODO, and plan closeout.
    context:
      intent: Close AC1 and AC2 audit failures while preserving Decision 92
        historical-import analytics, active runtime authority, user-data
        boundaries, and the in-progress plan state.
      constraints: Do not alter plan status, Decision 92 analytics, active-resource
        managed semantics, user data, pre-existing TODO/decisions/plan changes,
        or release bookkeeping.
      unknowns: Independent retry audit remains pending.
      scope: CLI process lifecycle; served/install/support prose; retired cleanup
        contract and tests; active runtime vocabulary and web install docs.
  - number: 704
    timestamp: 2026-07-11 16:13
    type: feat
    phase: build
    what: Retired Claude Code from active runtime discovery, package manifests,
      hooks, doctor, upgrade, eval, and default analytics. Added consent-gated
      historical import provenance and ledger-owned retired-resource cleanup
      with pinned-parent identity and fingerprint checks.
    inspiration: Active plan Task 5, Decision 92, and the six-record adapter
      registry warning.
    discovered: The legacy adapter registry still fed doctor and upgrade, the
      packaged Claude hook and plugin manifests still claimed support, and
      default corpus extraction still read Claude transcripts.
    verified: "pnpm -C packages/cli test: 153 files and 1,401 tests passed; focused
      retirement suite: 18 files and 282 tests plus served-profile policy;
      typecheck and build passed; lifecycle-adapters, build capability,
      capability-contract, and app-home validation passed; extract-corpus parity
      was in sync; lint reported 99 warnings and 0 errors; check compact passed;
      git diff --check passed."
    next: Task 6 should integrate the four-runtime doctor projections. Task 7 should
      expose approved repair UX. Keep Task 5 in progress until the plan owner
      closes it.
    context:
      intent: Execute active-plan Task 5 under Decision 92 while preserving the
        four-runtime authority and prior lifecycle safety contracts.
      constraints: No Task 6 doctor detail, Task 7 upgrade repair UX, Task 8 broad
        docs/changelog/TODO/version sweep, plan status mutation, vision
        mutation, or remote push. User-owned Claude data and ambiguous resources
        remain untouchable.
      unknowns: Safe removal is intentionally available only on Linux systems with
        procfs pinned-parent support; other platforms return action_required
        pending a comparably strong primitive.
      scope: packages/cli runtime lifecycle, analytics, setup, upgrade, eval, hook and
        schema surfaces; references/adapters contracts and registries; focused
        fixtures/tests; generated extraction parity wrapper; typed progress
        cycle.
  - number: 703
    timestamp: 2026-07-11 15:31
    type: fix
    phase: build
    what: Corrected runtime adapter remediation projection so Copilot native actions
      stay in native_actions and missing destination parents remain truthful
      action-required work.
    inspiration: Task 4 retry 1 audit findings and Decision 92.
    discovered: Category remediation must derive from associated planned operations;
      probe absence cannot authorize repair when the shared engine has failed
      closed on an unsafe parent.
    verified: "Focused lifecycle adapter regression: 1 file and 19 tests passed.
      Full CLI suite: 151 files and 1,387 tests passed. Typecheck and build
      passed. Lifecycle-adapters, Build capability, and capability-contract
      validations passed. Compaction and built fresh-home smoke passed. Lint
      completed with 0 errors and the standing 108-warning baseline. Git diff
      checks passed."
    next: Keep Task 4 in_progress for orchestrator re-audit; Task 5 still owns
      Claude and cursor-agent retirement.
    context:
      intent: Close the Task 4 retry findings while preserving lifecycle ownership and
        publication boundaries.
      constraints: Preserved Task 3 publication safety, Decision 92, caches,
        collisions, trust, Cursor identity, command integration boundaries, and
        existing TODO, decisions, and plan changes; did not edit plan status.
      unknowns: Independent retry audit remains external; apply-time publication
        failures remain represented by lifecycle apply results, while preview
        category reports reflect planned blockers.
      scope: Adapter report and remediation projection, native-action contract
        validation, regression tests, and typed progress evidence.
  - number: 702
    timestamp: 2026-07-11 12:33
    type: feat
    phase: build
    what: Implemented authority-bound lifecycle adapters for OpenCode, Codex,
      Cursor, and Copilot with explicit eight-category surface reports, safe
      repair declarations, and fail-closed support floors.
    inspiration: Active plan Task 4 and Decision 92.
    discovered: Safe automatic repair requires an existing safe destination parent;
      user-owned parents, symlink replacement, and unverified host behavior
      remain explicit action_required or unavailable states.
    verified: "Focused adapter and lifecycle tests: 3 files and 34 tests passed.
      Full CLI suite: 151 files and 1,385 tests passed. Typecheck and build
      passed. Lifecycle-adapters, Build capability, and capability-contract
      validations passed. Compaction and built adapter smoke passed. Lint
      completed with 0 errors and the standing 108-warning baseline. Git diff
      checks passed."
    next: Keep Task 4 open. Task 5 may retire Claude support and analytics; Tasks
      6-7 may integrate lifecycle reports into commands; Task 8 owns release and
      documentation surfaces.
    context:
      intent: Deliver Task 4 runtime observation and repair adapters on the Task 2
        authority and Task 3 operation engine.
      constraints: Did not integrate prime, doctor, or upgrade; retire Claude or
        analytics; update release, docs, TODO, plan status, or versions; execute
        native actions; or change Task 3 publication behavior.
      unknowns: Interrupted existing-ledger rewrites remain the preserved Task 3
        fail-closed audit warning. Runtime-specific behavior remains unknown or
        unsupported wherever first-party evidence is absent.
      scope: Runtime lifecycle adapter contract, four concrete adapters, lifecycle
        validation, fixture matrix tests, and typed progress evidence.
archive:
  - summary: "Cycle 701 (2026-07-11): Hardened the shared lifecycle engine against
      publication-time target and parent substitution."
  - summary: "Cycle 700 (2026-07-11): Built the shared runtime-neutral lifecycle
      operation engine with declared manifests, ownership ledgers, safe
      filesystem actions,..."
  - summary: "Cycle 699 (2026-07-11): Established Decision 92 runtime lifecycle
      authority for four active identities with explicit Cursor CLI and..."
  - summary: "Cycle 698 (2026-07-11): Eliminated the CLI test suite's shared dist
      race by building package output once in Vitest..."
  - summary: "Cycle 697 (2026-07-11): Resolved state-writer review findings:
      preserved legacy scalar health archives, bounded plan archives and lock
      directories..."
  - summary: "Cycle 696 (2026-07-10): Implemented the standardized state artifact
      writer with typed progress, decisions, plan, and health operations;
      schema-derived..."
  - summary: "Cycle 695 (2026-07-10): Audited and rewrote profile capability
      instructions following six-section spine and direct-contract philosophy."
  - summary: "Cycle 694 (2026-07-10): Audited and rewrote optimize capability
      instructions following six-section spine and direct-contract philosophy."
  - summary: "Cycle 693 (2026-07-10): Audited and rewrote build capability
      instructions following six-section spine and direct-contract philosophy."
  - summary: "Cycle 692 (2026-07-08): Replaced cliDistributionMajor's terminal
      `return 2` fallback with a package.json#version secondary signal and a
      one-time stderr..."
  - summary: "Cycle 691 (2026-06-29): Plan task 5 (Implement trigger schema loader):
      added packages/cli/src/registries/triggerLoader.ts and
      packages/cli/test/registries/triggerLoader.test.ts. The loader reads
      all..."
  - summary: "Cycle 690 (2026-06-28): Plan task 5 (final state sync): closed the
      vitest 2→4 + Vite+ toolchain plan. Ran..."
  - summary: "Cycle 689 (2026-06-28): Plan task 4 of the vitest 2->4 toolchain
      modernization: align the pre-commit hook script with..."
  - summary: "Cycle 688 (2026-06-28): Plan task 3 of the vitest 2->4 toolchain
      modernization: wire `vp lint` and `vp fmt`..."
  - summary: "Cycle 687 (2026-06-28): Plan task 2 of the vitest 2->4 toolchain
      modernization: migrate the CLI test entry point..."
  - summary: "Cycle 686 (2026-06-28): Plan task 1 of the vitest 2->4 toolchain
      modernization: bump the `vitest` devDep `^2.1.8` ->..."
  - summary: "Cycle 685 (2026-06-28): Audit 25 of the feat/v3 CLI. Evaluated the C74
      Dict-consolidation refactor (commits 2715bd28..8e9f2447, 6 commits)..."
  - summary: "Cycle 684 (2026-06-27): Plan task 7 (final) of chore:3.0.0: final
      verification + plan archive + TODO closure. Ran..."
  - summary: "Cycle 683 (2026-06-27): Plan task 6 of chore:3.0.0 (cross-cutting cast
      audit + re-export deletion). Converted ~28 consumer files..."
  - summary: "Cycle 682 (2026-06-27): Plan task 5 of chore:3.0.0 (long-tail surface)
      + remediation. First build cycle eliminated aliases in..."
  - summary: "Cycle 681 (2026-06-27): Plan task 4 of chore:3.0.0: consolidate Dict
      aliases in registries & validate subsystems. Removed local..."
  - summary: "Cycle 680 (2026-06-27): Plan tasks 2+3 (coupled) of chore:3.0.0:
      consolidate Dict aliases in the CLI command/state-query surface (task..."
  - summary: "Cycle 679 (2026-06-27): chore:3.0.0 plan task 1 — consolidate `type
      Dict = Record<string, any>` aliases across the 12 hooks modules onto
      core/jsonValue's JsonObject. Strict union surfaced ~80 latent typecheck
      errors; resolved in-scope by keeping IO-boundary casts with `// cast:`
      comments and narrowing access via type guards. Typecheck 0 errors; suite
      1145/1145 green."
  - summary: "Cycle 678 (2026-06-27): v2/v3 coexistence B3 — bundle-status & channel
      resolution. Centralized channel resolution through
      resolveInvokedUpdateChannel so doctor..."
  - summary: "Cycle 677 (2026-06-27): v2/v3 coexistence B2 — env-var & path
      resolution remainder. Restored PROFILERA_PROFILE_DIR as a lower-precedence
      fallback..."
  - summary: "Cycle 676 (2026-06-26): D73 single Agentera agent plan closeout.
      Implementation shipped in a prior session (commit d1b4145d: replaced..."
  - summary: "Cycle 675 (2026-06-23): Rename published prerelease version suffix
      from -dev.N to -next.N across live source, tests, CI L2..."
  - summary: "Cycle 674 (2026-06-23): Remove dead `missingScriptCommands` from
      installRoot.ts; close three stale TODOs."
  - summary: "Cycle 673 (2026-06-23): Align parity oracle retryCommand pin with
      string|null contract."
  - summary: "Cycle 672 (2026-06-22): Backport fix: remove empty-severity-section
      rejection from validate_artifact.py on main."
  - summary: "Cycle 671 (2026-06-22): Fix stale YAML schema example, deprecated lint
      command, and archive reference in discuss instructions."
  - summary: "Cycle 670 (2026-06-18): Plan T6 — verification and closeout: archived
      PLAN-2026-06-18-decouple-vitest-live-repo.yaml; cleared active plan slot;
      confirmed vitest decoupled..."
  - summary: "Cycle 669 (2026-06-18): Plan T5 — decouple registry and evaluator TODO
      coupling: packageRegistry and artifactRegistry tests read fixture..."
  - summary: "Cycle 668 (2026-06-18): Plan T4 — decouple artifact validation and
      validate oracles: migrated validateArtifact.test.ts, validate.test.ts, and
      validateVerifyOracles.test.ts to..."
  - summary: "Cycle 667 (2026-06-18): Plan T2 — compaction tests use fixtures only:
      removed real-repo computeCompactionStatus/checkCompaction budget asserts
      from compaction.test.ts;..."
  - summary: "Cycle 666 (2026-06-18): Plan T3 — shared repo-state fixture harness:
      added packages/cli/test/fixtures/repo-state/ variants (ok,
      todo-resolved-over-limit, progress-at-cap, progress-over-limit,
      invalid-progress-yaml),..."
  - summary: "Cycle 665 (2026-06-18): Plan T1 — vitest/repo gate inventory:
      classified all REPO_ROOT couplings in packages/cli/test; documented
      mutable-state vs..."
  - summary: "Cycle 664 (2026-06-18): Plan T7 — state sync and plan closeout:
      archived PLAN-2026-06-18-profile-corpus-coverage-trust.yaml; cleared
      active plan slot; six..."
  - summary: "Cycle 663 (2026-06-18): Plan T6 — runtime metadata clarity and flag
      docs: renamed runtime_statuses[].candidate_count to file_count; documented
      --no-<runtime>..."
  - summary: "Cycle 662 (2026-06-18): Plan T5 — Py/TS extractor parity gate:
      extractCorpusParity manifest + opencodeParitySnapshot;
      generate-extract-corpus-parity.mjs syncs committed manifest..."
