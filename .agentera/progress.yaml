cycles:
  - number: 629
    timestamp: 2026-06-02 18:38
    type: test
    phase: build
    what: Freeze source_contract internals — new oracle fixture
      (source-contract.json) and new parity test (sourceContractOracles.test.ts)
      that pin the field set, value types, and required keys for every CLI
      surface that emits a `source_contract` (prime, prime --context, state
      plan, state query --list-artifacts) and documents the state todo
      exclusion. The test includes a shared cross-cutting structural pattern
      (boolean 'is raw reads required' field, string policy field,
      caveats/fallbacks array) and a per-capability bespoke contract for the
      five BESPOKE capabilities (orkestrera, realisera, inspektera, optimera,
      dokumentera). The TODO.md line 17 source_contract field-name
      reconciliation lands in the same commit per AC1 (`missing` becomes
      `missing_state`, the live prime key). Field content that legitimately
      varies by caller (cli_fallback, confidence_caveats, caveats,
      fallback_commands, owns, deferred) is pinned at the value-type level only
      per AC4. Oracles cite and reference the existing invalid-input-envelope,
      validate-family, verify-eval-family, and npm-cli-surface oracles; no
      content is duplicated.
    commit: pending
    inspiration: None.
    discovered: The top-level `payload.source_contract` is NOT emitted on the `prime
      --context <capability>` path — `buildPrimeCapabilityContextPayload`
      returns `{ command, status, capability_context }` and the dashboard-level
      source_contract is intentionally absent. The per-capability bespoke
      source_contract lives at
      `capability_context.context.<bespoke>.source_contract` where `<bespoke>`
      is `orchestration_context` (orkestrera), `execution_context` (realisera),
      `evidence_context` (inspektera), `benchmark_context` (optimera), or
      `closeout_context` (dokumentera). The seven non-bespoke capabilities (hej,
      planera, resonera, inspirera, visionera, visualisera, profilera) share the
      bare `capability_context.context` shape and have no bespoke
      source_contract. The oracle documents all three placements.
    verified: pnpm -C packages/cli test -- 672/672 passed (75 files, +28 vs cycle
      628's 644/644); pnpm -C packages/cli run typecheck -- clean; pnpm -C
      packages/cli build -- clean; the new sourceContractOracles.test.ts
      exercises the five source_contract emissions (prime, prime --context for
      the five bespoke capabilities, state plan, state query --list-artifacts)
      plus seven non-bespoke capability exclusions, one state-todo exclusion,
      and two drift-detector unit tests; live `node
      packages/cli/dist/bin/agentera.js prime --format json`, `prime --context
      {orkestrera,realisera,inspektera,optimera,dokumentera} --format json`,
      `state plan --format json`, `state query --list-artifacts --format json`,
      and `state todo --format json` all match the oracle; TODO.md line 17
      amended to use `missing_state` in the same commit; plan.yaml task 4 status
      updated to complete.
    next: Plan is now 4/4 tasks complete. Close Decision 56 ('artifact validation,
      verify, full suite green' slice) or remove the progress.yaml commit field
      per the new TODO.
    context:
      intent: Execute plan task 4 (freeze source_contract internals). Plan is 4/4
        tasks complete after this commit. Last realisera cycle in this
        orkestrera session.
      constraints: TS-only on 3.0.0-dev feat/v3; no remote push; no vision.yaml or
        objective-state edits; oracles cite and reference the existing
        invalid-input-envelope.json, validate-family.json,
        verify-eval-family.json, and npm-cli-surface.json (no duplication); pin
        value types and structural shapes, not exact string content, for fields
        whose values legitimately vary by caller (AC4); the TODO.md field-name
        reconciliation MUST be in the same commit as the source-contract oracle
        (AC1); the commit field will be removed from the schema in a future
        cycle, so this cycle's commit is left as pending and skips the forward
        backfill.
      unknowns: Whether the post-plan work is the Decision 56 closeout (cut a v3.0.0
        release with the 4/4 freeze evidence) or the progress.yaml commit-field
        removal (chore:3.0.0 TODO).
      scope: "Added packages/cli/test/cli/fixtures/oracle/source-contract.json (the
        new oracle with five command entries, per-capability bespoke contracts
        for the five bespoke capabilities, shared structural pattern, and
        field-name reconciliation block) and
        packages/cli/test/cli/sourceContractOracles.test.ts (the new parity test
        with 28 cases). Amended TODO.md line 17 source_contract enumeration
        (`missing` becomes `missing_state`). Updated .agentera/plan.yaml task 4
        status to complete in the same commit. Plan: 4/4 tasks complete."
  - number: 628
    timestamp: 2026-06-02 18:15
    type: test
    phase: build
    what: Freeze validate/verify family parity oracles — two new oracle fixtures
      (validate-family.json, verify-eval-family.json) and a new
      validateVerifyOracles.test.ts that pins the pass-envelope shape for all
      seven validate families and the two verify eval targets, with a strict
      drift detector (assertExactTopLevelKeys) that fails the test with a named
      diff when an undeclared top-level key is added to the live envelope, and
      two unit tests that self-verify the drift detector. Existing
      validate.test.ts and verify.test.ts are unchanged and still pass. Oracles
      cite and reference the existing invalid-input-envelope.json and
      npm-cli-surface.json; no content is duplicated.
    commit: pending
    inspiration: None.
    discovered: "cmdVerify's internal validateVerifyRequest catch writes `Error:
      ...` to stderr and returns rc 2 instead of routing through
      emitInvalidInput, so `check verify eval bogus --format json` does NOT emit
      the canonical invalid-input envelope. This is a pre-existing gap
      documented by verify.test.ts:63-67. The verify-eval-family oracle freezes
      the PASS envelope only; the text-mode error path is owned by
      verify.test.ts and is intentionally out of scope. Fixing the
      dispatch-level gap (route runVerify's catch through emitInvalidInput) is a
      follow-up cycle."
    verified: pnpm -C packages/cli test -- 644/644 passed (74 files, +27 vs cycle
      627's 617/617); pnpm -C packages/cli run typecheck -- clean; pnpm -C
      packages/cli build -- clean; new tests exercise all 7 validate families +
      2 verify eval targets against the oracles; drift detector unit tests
      verify the named-diff behavior on added/missing keys; live `node
      packages/cli/dist/bin/agentera.js check validate <family> --format json`
      for each of the 7 families and `check verify eval semantic
      fixtures/semantic/hej-bare-message.md --format json` / `check verify eval
      skills --dry-run --format json` all match the oracles.
    next: Plan task 4 (freeze source_contract internals) or close the cmdVerify
      dispatch-level text-mode-error gap as a task 2 follow-up, or remove the
      progress.yaml commit field per the new TODO.
    context:
      intent: "Execute plan task 3 (freeze validate/verify family parity oracles).
        Plan: 3/4 tasks complete after this commit."
      constraints: "TS-only on 3.0.0-dev feat/v3; no remote push; no vision.yaml or
        objective-state edits; oracles cite and reference the existing
        invalid-input-envelope.json and npm-cli-surface.json (no duplication);
        pin value types and structural shapes, not exact string content, for
        fields whose values legitimately vary by family/fixture; commit field
        will be removed from the schema in a future cycle, so this cycle
        intentionally leaves commit: pending and skips the forward backfill."
      unknowns: Whether task 4 fits in one cycle or splits the capability_context
        capsule and prime output into two sub-tasks.
      scope: Added packages/cli/test/cli/fixtures/oracle/validate-family.json (132
        lines, 7 families + alt-shape + fail-path reference) and
        packages/cli/test/cli/fixtures/oracle/verify-eval-family.json (61 lines,
        2 eval targets + safety/diagnostics/engine pinning + fail-path
        reference); added packages/cli/test/cli/validateVerifyOracles.test.ts
        (~600 lines, 27 tests covering pass envelopes for all 7 validate
        families, 2 verify eval targets, fail envelope for artifact, dispatch
        wiring for validate, and drift-detector unit tests). Plan task 3 status
        updated to complete in the same commit.
  - number: 627
    timestamp: 2026-06-02 17:52
    type: feat
    phase: build
    what: Wire the invalid-input envelope across the remaining CLI surfaces (lint,
      backfill, state, query, compact, schema, doctor, upgrade, report, verify,
      capability, help, usage, prime, gate, hook) in dispatch.ts —
      classifyParseError, asEnvelopeFormat, detectTopLevelFormat helpers; 21
      run* functions now route their parse-error and catch paths through
      emitInvalidInput; existing test assertions on plain-text error output
      updated to assert the four-question envelope template. Also fixes
      rewriteCycleCommits to always YAML-quote the commit value so all-digit and
      scientific-notation short hashes (e.g. 61e2490 -> Infinity) round- trip
      correctly through the parser.
    commit: pending
    inspiration: None.
    discovered: "Two follow-ups: (1) the parse-error path in non-validate surfaces
      hardcodes format: 'text' because the parse functions consume argv before
      the format flag is reliably known; runValidate inlines format detection
      and works correctly, but runLint/runBackfill/runState/runQuery/runCompact
      will emit text-mode envelope on --format json parse errors. Fix in a
      future cycle by either inlining the parse loops for each surface or
      extracting a detectFormat() pass that runs before parse. (2) The
      progress.yaml commit field will be removed entirely per user direction;
      this cycle's commit is left as pending with no forward backfill, and
      rewriteCycleCommits will be deleted alongside the field in the schema
      migration cycle."
    verified: pnpm -C packages/cli test -- 617/617 passed (no regression vs cycle
      626); pnpm -C packages/cli run typecheck -- clean; pnpm -C packages/cli
      build -- clean; live smoke `lint`, `state bogus`, `check backfill --mode
      bogus` all return the four-question template + rc 2; `schema --format
      text` (invalid choice) returns the canonical JSON envelope + rc 2; `lint
      --format json` returns text-mode template (known format-detection gap).
    next: Plan task 3 (validate/verify family parity oracles) or close the
      format-detection-in-error-paths gap as a task 2 follow-up, or remove the
      progress.yaml commit field per the new TODO.
    context:
      intent: Execute plan task 1b (wire emitInvalidInput across remaining CLI
        surfaces). The subagent dispatched by orkestrera returned an empty
        task_result, so the orchestrator took over verification, fix, and commit
        phases.
      constraints: "TS-only on 3.0.0-dev feat/v3; no remote push; no vision.yaml or
        objective-state edits; all wired surfaces return rc 2; commit field will
        be removed from the schema in a future cycle, so this cycle
        intentionally leaves commit: pending and skips the forward backfill."
      unknowns: Whether task 3 and the format-detection follow-up fit in one cycle or
        need to be split, and whether task 4 becomes the first schema-removal
        cycle for the progress commit field.
      scope: "Modified dispatch.ts (3 new helpers, 21 run* functions wired through
        emitInvalidInput), 4 CLI test files (assertions updated),
        progressCommit.ts (rewriteCycleCommits quotes commit value), 3
        progressCommit tests (expect quoted form). Plan: 2/4 tasks complete."
  - number: 626
    timestamp: 2026-06-02 15:48
    type: test
    phase: build
    what: Freeze the invalid-input error envelope contract for the 3.0 TypeScript
      CLI — new emitInvalidInput helper, oracle fixture, parity test, and check
      validate wired as the exemplar surface.
    commit: acc8db1
    inspiration: None.
    discovered: None.
    verified: pnpm -C packages/cli test — 617/617 passed (73 files, +12 vs cycle
      625's 605/605); pnpm -C packages/cli run typecheck — clean; pnpm -C
      packages/cli build — clean; live smoke `node
      packages/cli/dist/bin/agentera.js check validate --format json` returns
      the canonical envelope + rc 2, text mode returns the four-question
      template + rc 2, `check validate bogus --format json` returns an
      `unsupported_target` envelope + rc 2.
    next: Wire the invalid-input envelope across the remaining CLI surfaces (lint,
      backfill, state, schema, doctor, upgrade, report, verify, capability,
      help) per plan task 2, or freeze the validate/verify family parity oracles
      per plan task 3. Task 2 unblocks task 3.
    context:
      intent: "Execute plan task 1 (1a — envelope contract: oracle + helper + check
        validate exemplar). The 1a/1b split was needed because the original task
        1 was too large for one realisera cycle."
      constraints: TS-only on 3.0.0-dev feat/v3; no remote push; no vision.yaml or
        objective-state edits; existing text-mode assertions in validate.test.ts
        and verify.test.ts must continue to pass alongside the new json-mode
        envelope.
      unknowns: Whether task 2 will surface additional error classes beyond the seven
        declared in the oracle union (missing_argument, invalid_choice,
        unrecognized_argument, mutually_exclusive, invalid_int, invalid_format,
        unsupported_target).
      scope: Added packages/cli/src/cli/errors.ts (helper module),
        packages/cli/test/cli/fixtures/oracle/invalid-input-envelope.json
        (oracle contract), packages/cli/test/cli/invalidInputEnvelope.test.ts
        (12 parity tests); modified packages/cli/src/cli/dispatch.ts runValidate
        to route every error site through emitInvalidInput (six inline err()
        sites plus the catch block plus the not-yet-ported branch); other CLI
        surfaces unchanged — they migrate in task 2.
  - number: 625
    timestamp: 2026-06-02 09:20
    type: fix
    phase: build
    what: npm shim runBackend app-home branch now forwards the user's cwd to the
      spawned Python CLI instead of the installed app home's app/ subdir;
      re-added the shim source under packages/cli/shim/ (bin/agentera.mjs,
      lib/exec.mjs, lib/resolve.mjs, package.json, README.md, LICENSE) and added
      vitest regression coverage in packages/cli/test/shim/runBackend.test.ts.
    commit: 4534d90
    inspiration: None.
    discovered: Published npm shim 0.0.0 was the only npx -y agentera entry point
      until 3.0 ships; the shim's runBackend app-home branch set cwd to
      path.dirname(path.dirname(scriptPath)) = $AGENTERA_HOME/app, so the Python
      CLI's os.getcwd()-based state resolution read from the app home on every
      project invocation.
    verified: pnpm -C packages/cli test test/shim/ — 3/3 passed; pnpm -C
      packages/cli test — 605/605 passed (72 files); smoke check `cd
      /home/jgabor/git/agentera && node packages/cli/shim/bin/agentera.mjs state
      plan --format json` returns source.path =
      /home/jgabor/git/agentera/.agentera/plan.yaml (not under
      /home/jgabor/.local/share/agentera/app/).
    next: Republish agentera@0.0.0 (or 0.0.1) to npm with the fixed runBackend; once
      3.0 ships, retire the shim per the [Unreleased] changelog note.
    context:
      intent: Execute plan task 1 (npm shim app-home cwd preservation) with regression
        coverage and a transitional changelog note.
      constraints: No remote push; do not edit vision.yaml or objective state; the
        Python CLI's os.getcwd() semantics are unchanged; the repo backend keeps
        cwd equal to backend.repoRoot; shim version stays 0.0.0; match existing
        shim code style (ESM, JSDoc, node-style imports).
      unknowns: Whether to republish the shim from this repo as 0.0.0/0.0.1 or wait
        for the 3.0 cutover; npm pack artifacts live under packages/cli/*.tgz
        and are gitignored.
      scope: packages/cli/shim/{bin/agentera.mjs,lib/exec.mjs,lib/resolve.mjs,package.json,README.md,LICENSE},
        packages/cli/test/shim/runBackend.test.ts, CHANGELOG.md [Unreleased],
        .agentera/plan.yaml task 1 status, .agentera/progress.yaml cycle 625
        (and cycle 624 commit annotation).
  - number: 624
    timestamp: 2026-06-02 07:26
    type: chore
    phase: plan
    what: Shim cwd preservation plan replaces the completed npm 3.x CLI parity
      tranche 1; targets the broken npx -y agentera 0.0.0 shim whose runBackend
      spawns the Python CLI with cwd set to the installed app home.
    commit: "N/A: plan-only cycle, fix committed as cycle 625"
    inspiration: None.
    discovered: "npm shim delegates via 'uv run' with 'cwd: appRoot'; user working
      directory is never propagated. Affected commands: state
      plan/todo/docs/health/progress/objective/decisions, prime, hej, planera,
      resonera, inspektera. CLI reports 'fresh project' for any user with an
      installed app home."
    verified: Raw artifact fallback reads confirm project has complete plan 'npm 3.x
      CLI parity tranche 1' (all 6 tasks complete, header status complete) and 7
      open Normal TODO items; bug source located in the npm shim at
      packages/cli/lib/exec.mjs runBackend app-home branch.
    next: Route to realisera to fix the shim runBackend cwd, add regression
      coverage, and add a CHANGELOG note under [Unreleased].
    context:
      intent: Plan the next batch of TODO items after the user reported "planera next
        batch of todo items" and identified a CLI artifact resolution bug.
      constraints: Direct planera invocation permits implicit archive of the completed
        plan; no remote push; do not edit vision.yaml or objective state; do not
        modify Python CLI os.getcwd() semantics.
      unknowns: Regression test design is owned by realisera; behavioral coverage for
        the repo backend is regression-safe by inspection.
      scope: TODO.md Critical section, .agentera/plan.yaml,
        .agentera/archive/PLAN-2026-06-01-npm-3x-cli-parity-tranche-1.yaml,
        .agentera/progress.yaml cycle 624.
  - number: 623
    timestamp: 2026-05-31 00:25
    type: test
    phase: build
    what: v2→v3 sandbox upgrade validation program (L0 fixtures, harness, CI).
    commit: pending
    inspiration: None.
    discovered: partial-only-runtime must not assert app/ removal; OpenCode rewire
      and stale-surface cleanup remain gap-tracked expected-fail until migration
      ports land.
    verified: pnpm -C packages/cli test test/upgrade/ — 74/74 passed;
      scripts/sandbox P0 scenarios happy-path, stable, v1-md, noisy,
      codex-plugin, partial-only pass; gapRegistry.ts tracks three open gaps; CI
      adds sandbox-l1/l2 jobs on feat/v3.
    next: Flip gap tests to required pass when runtimeTargets and cleanup detection
      ports land; optional legacy-home-retirement harness scenario.
    context:
      intent: PLAN v2→v3 sandbox upgrade validation program tasks 1–8.
      scope: scripts/sandbox/, packages/cli/test/upgrade/fixtures/,
        packages/cli/test/upgrade/{dataPreservation,cleanupNoisyAppHome,migrateRuntimeMatrix,idempotency,leftoverScan,gapRegistry}.test.ts,
        packages/cli/test/upgrade/gapRegistry.ts, .github/workflows/ci.yml
  - number: 622
    timestamp: 2026-05-30 23:55
    type: test
    phase: build
    what: Parity tests, backport-safety gate, and migration plan closeout
      verification.
    commit: pending
    inspiration: None.
    discovered: Full CLI suite still has 2 unrelated repo-state failures (validate
      PLAN artifact, compaction over_limit on progress.yaml); upgrade path fixed
      to skip v2→v3 migration for v3 self-contained npm bundles.
    verified: "pnpm -C packages/cli test test/upgrade/ — 60/60 passed; pnpm -C
      packages/cli test — 540/543 passed (3 failures outside upgrade scope:
      validate.test PLAN artifact, compaction.test progress over_limit,
      progressCommit.test ancestor type); parity oracle fixture at
      test/upgrade/fixtures/oracle/python-main-contract.json; stable
      backport-safety dry-run emits zero cross-major migration operations;
      .agentera/plan.yaml header and task 8 marked complete."
    next: Fold migration branch work into release planning; continue Decision 56 CLI
      parity for non-upgrade surfaces.
    context:
      intent: PLAN task 8 for v2→v3 migration dual update channels.
      constraints: No commit; do not mark Decision 56 satisfied; user-only satisfaction.
      unknowns: Full vitest green blocked by progress.yaml compaction budget and
        active plan artifact validation.
      scope: packages/cli/test/upgrade/parityOracle.test.ts,
        packages/cli/test/upgrade/fixtures/oracle/python-main-contract.json,
        packages/cli/src/upgrade/upgradeOrchestrator.ts runMigration guard,
        packages/cli/test/cli/npxBundle.test.ts,
        packages/cli/test/upgrade/upgradeOrchestrator.test.ts, TODO.md Decision
        56 follow-up, .agentera/plan.yaml task 8 and header status.
  - number: 621
    timestamp: 2026-05-30 23:45
    type: docs
    phase: build
    what: Document upgrade paths and backport/cherry-pick policy.
    commit: pending
    inspiration: None.
    discovered: None.
    verified: UPGRADE.md and README.md reviewed against update-channels.yaml and
      upgrade orchestrator; lefthook markdownlint disables apply to repo doc
      style; check compact unchanged (progress.yaml over-limit pre-existing).
    next: Task 8 — parity tests, backport-safety gate, and closeout verification.
    context:
      intent: PLAN task 7 for v2→v3 migration dual update channels.
      constraints: No commit; factual tone; channel-aware commands; maintainer
        cherry-pick order.
      unknowns: Task 6 doctor/prime channel integration may land in parallel.
      scope: UPGRADE.md, README.md upgrade/troubleshooting sections,
        .agentera/plan.yaml task 7 status.
  - number: 620
    timestamp: 2026-05-30 23:36
    type: feat
    phase: build
    what: Wire phased upgrade orchestrator and CLI command.
    commit: pending
    inspiration: None.
    discovered: None.
    verified: pnpm -C packages/cli test test/upgrade/ — 48/48 passed; pnpm -C
      packages/cli run typecheck — clean.
    next: Task 6 — integrate channels and migration into doctor and prime.
    context:
      intent: PLAN task 5 for v2→v3 migration dual update channels.
      constraints: No commit; orchestrator emits agentera.upgrade.v2;
        preview-before-apply guard.
      unknowns: None for orchestrator wiring; doctor/prime channel integration lands
        in task 6.
      scope: packages/cli/src/upgrade/upgradeOrchestrator.ts,
        packages/cli/src/cli/commands/upgrade.ts,
        packages/cli/src/cli/dispatch.ts,
        packages/cli/src/cli/commands/schema.ts,
        packages/cli/test/upgrade/upgradeOrchestrator.test.ts,
        .agentera/plan.yaml task 5 status.
archive:
  - summary: "Cycle 619 (2026-05-30): Port v2→v3 artifact, runtime rewire, and
      cleanup migration modules."
  - summary: "Cycle 618 (2026-05-30): Implement v2/v3 compatibility detection and
      guard rails."
  - summary: "Cycle 617 (2026-05-30): Implement update channel resolution module
      (channels.ts)."
  - summary: "Cycle 616 (2026-05-30): Define update-channel and major-boundary
      vocabulary authorities."
  - summary: "Cycle 615 (2026-05-29): Consolidate progress-commit guard onto shared
      module; release 2.7.6."
  - summary: "Cycle 614 (2026-05-29): Extract shared scripts/progress_commit.py;
      make `agentera check backfill` a thin consumer."
  - summary: "Cycle 613 (2026-05-29): Close compaction check/gate robustness and
      retention-cap dedup plan."
  - summary: "Cycle 612 (2026-05-29): Add progress commit-hash churn guard and
      `agentera check backfill`."
  - summary: "Cycle 611 (2026-05-29): Deduplicate session hook resolve_session_path
      and compact_entries helpers."
  - summary: "Cycle 610 (2026-05-29): Consolidate yaml.safe_load None-return
      handling across hooks and CLI."
  - summary: "Cycle 609 (2026-05-28): Tagged v2.7.5 with validation hardening and
      CLI namespace migration."
  - summary: "Cycle 608 (2026-05-27): Fix 16 degraded validation and CLI consistency
      issues across 7-task plan."
  - summary: "Cycle 607 (2026-05-26): Fix 5 degraded validation and CLI consistency
      issues in validate_artifact.py and scripts/agentera."
  - summary: "Cycle 606 (2026-05-26): Fix 7 critical validation and state-loading
      silent failures in validate_artifact.py and scripts/agentera."
  - summary: "Cycle 605 (2026-05-25): CLI audience namespace plan task 7 — closeout
      verification, measurements, and archive."
  - summary: "Cycle 605 (2026-05-25): Closed CLI audience namespace consolidation
      plan with validation, alias-parity measurements, and plan archive."
  - summary: "Cycle 604 (2026-05-25): CLI audience namespace plan task 6 — tests,
      semantic fixtures, JSON manifest."
  - summary: "Cycle 603 (2026-05-25): CLI audience namespace plan task 5 — docs,
      contracts, hooks, and skill routing."
  - summary: "Cycle 602 (2026-05-25): Pytest suite speed and coverage hygiene plan
      closeout."
  - summary: "Cycle 601 (2026-05-25): Tagged v2.7.1 with profilera Cursor session
      corpus parity."
  - summary: "Cycle 600 (2026-05-25): Cursor Agent CLI store.db gap-fill extraction
      for profilera corpus."
  - summary: "Cycle 599 (2026-05-25): Profilera Cursor IDE session corpus parity via
      extract_corpus cursor runtime."
  - summary: "Cycle 598 (2026-05-24): Tagged v2.7.0 with Cursor v1, web site, and
      post-2.6.1 CLI fixes."
  - summary: "Cycle 597 (2026-05-24): Closed Cursor First-Class Runtime v1 plan
      (Decision 63) with 2.7.0 release metadata."
  - summary: "Cycle 596 (2026-05-24): Live Cursor IDE preToolUse Write smoke for
      Task 5."
  - summary: "Cycle 595 (2026-05-24): Shipped Cursor v1 runtime slice per Decision 63."
  - summary: "Cycle 594 (2026-05-23): Added hybrid inspektera audit staleness to hej
      and fixed latest health audit selection."
  - summary: "Cycle 593 (2026-05-23): Relocated authoritative docs to references/
      and released 2.6.1 patch metadata."
  - summary: "Cycle 592 (2026-05-23): Archived completed prime/JSON-budget plan and
      released 2.6.0 metadata."
  - summary: "Cycle 591 (2026-05-23): Closed Agentera 3.0 JSON budget plan with full
      validation and measurement closeout evidence."
  - summary: "Cycle 590 (2026-05-23): Synced callers, contracts, docs, and tests to
      prime --context as sole 3.0 startup seam."
  - summary: "Cycle 589 (2026-05-23): Introduced prime context control plane and
      removed hej capability-context compatibility."
  - summary: "Cycle 588 (2026-05-23): Wired D59 JSON byte/token budgets into
      manifest and measurement harness enforcement."
  - summary: "Cycle 587 (2026-05-23): Built whole-surface JSON measurement coverage
      for D58 inventory surfaces."
  - summary: "Cycle 586 (2026-05-23): Verified D58 JSON output surface inventory
      against Plan Task 1 acceptance criteria."
  - summary: "Cycle 585 (2026-05-21): Made Codex plugin-provided hooks primary when
      the Agentera Codex plugin is enabled."
  - summary: "Cycle 584 (2026-05-21): Synchronized slim capability-context contracts
      and validation evidence."
  - summary: "Cycle 583 (2026-05-21): Added capability-context byte and GPT-5 token
      budget regression coverage."
  - summary: "Cycle 582 (2026-05-21): Added generic slim capability-context builders."
