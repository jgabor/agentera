cycles:
  - number: 790
    timestamp: 2026-07-14 14:28
    type: fix
    phase: build
    what: Aligned the live migration and optional local-Git enrichment contract
      across authority, schema, help, output, upgrade guidance, migration
      handoff, bundle parity, and regression tests. The authority now derives
      bounded limits, consent, revalidation, failure recovery, and traceability
      without changing completed migration behavior.
    inspiration: Simplified Agentera plan Task 2 and the completed simple
      migration/enrichment behavior.
    discovered: The runtime already enforced the requested local behavior, but
      schema omitted the backfill command, output exposed only partial contract
      metadata, upgrade guidance omitted the optional handoff, and the authority
      retained stale task-ownership wording. Decision 53 and projection
      authorities already expressed non-inference and lossless capacity
      semantics; parity tests now protect those rules across source and bundle
      surfaces.
    verified: "Focused: pnpm -C packages/cli exec vp test run test/cli/help.test.ts
      test/cli/schema.test.ts test/state/archiveAuthority.test.ts
      test/state/simpleMigrationParity.test.ts test/state/gitBackfill.test.ts
      test/state/legacyMigration.test.ts test/state/migrationAuthority.test.ts
      test/state/projectionPolicy.test.ts (8 files, 98 tests passed).
      Non-packaging boundary: pnpm -C packages/cli exec vp test run --exclude
      test/packaging/prepack.test.ts (176 files, 1788 tests passed). Full
      boundary: pnpm -C packages/cli test (177 files, 1792 tests passed, 5 known
      npm 12 packaging JSON-parser failures; 1797 total). pnpm -C packages/cli
      run typecheck passed; pnpm -C packages/cli build passed; pnpm -C
      packages/cli run bundle:data passed. Capability build validation passed;
      capability-contract passed with 2/2 checks; PROGRESS.md, PLAN.md,
      DECISIONS.md, and HEALTH.md artifact validation passed. check compact
      --format json passed with error_count=0, over_limit_count=0,
      protected_overflow_count=0, changed_count=0; it preserved the existing
      five progress archive-recovery refusals. Schema smoke returned status=ok,
      gaps=0, state_git_backfill fields=18; backfill preview returned rc=0,
      read_only=true, status=degraded, remote_contact=false, limits
      100/500/16777216; missing-force apply returned rc=2 with invalid_request
      and no state changed; migration preview returned rc=1, read_only=true,
      mutation_performed=false, remote_contact=false, and
      separate_explicit_operator_invocation. Markdownlint for UPGRADE.md passed;
      stale-language parity passed with historical plan/progress/TODO evidence
      excluded."
    next: Leave simplified-plan Task 2 pending and do not evaluate or complete it.
      Preserve archived plans, prior progress, Decision 53 satisfaction state,
      vision, profile, completed migration behavior, version metadata, release
      state, known npm packaging failures, and existing archive-recovery
      caveats; do not begin Task 3 or add stronger authorization machinery.
    context:
      intent: Align current contracts and guidance with the completed simple migration
        and optional enrichment behavior.
      constraints: "Use the local-operator model: optional read-only preview, direct
        exact-selector --apply --force, apply-time revalidation, bounded
        100-result/500-history-unit/16-MiB local Git, HEAD/local heads/tags
        only, no remote or custom refs or operations, immutable equality, and
        traceable provenance. No receipt, HMAC, token, secret, expiry, ledger,
        daemon, service, or hostile-local-actor machinery. Historical evidence
        is exempt from live stale-language checks."
      unknowns: The five known npm 12 packaging JSON-parser failures and five existing
        progress archive-recovery refusals remain unchanged. Decision 53 is
        still represented by a legacy summary, so missing or unconfirmed
        satisfaction remains review-needed and is never inferred.
      scope: references/artifacts/state-storage-authority.yaml; UPGRADE.md;
        packages/cli/src/cli/commands/schema.ts; packages/cli/src/cli/help.ts;
        packages/cli/src/cli/commands/migrate.ts;
        packages/cli/src/state/gitBackfillAuthority.ts;
        packages/cli/src/state/gitBackfill.ts;
        packages/cli/src/state/gitBackfillOutput.ts;
        packages/cli/src/state/migrationEnrichment.ts;
        packages/cli/src/state/legacyMigration.ts; focused parity and contract
        tests; generated packages/cli/bundle data
  - number: 789
    timestamp: 2026-07-14 13:46
    type: fix
    phase: build
    what: "Fixed both bounded-history defects in simplified Agentera Task 1:
      inventory and preview now cap the final combined row list at the authority
      maximum of 100 with truthful omission metadata and exact-selector
      continuation guidance, while reachable and rewritten-history collection
      consumes one shared 500 commit/probe budget. Added adversarial combined
      reachable and rewritten fixtures, and corrected Cycle 788's help/schema
      evidence from 24 to the exact current count of 25 without changing prior
      cycle bytes."
    inspiration: Simplified Agentera plan Task 1 and independent audit
      simplified-task1-audit-1.
    discovered: A 100-target rewritten-history fixture produces 200 deterministic
      pre-bound rows when base and rewritten provenance rows are combined; the
      final authority cap must be applied after that expansion. A 499 reachable
      plus two reflog candidate fixture proves one shared budget processes only
      500 total commit/probe units, classifies the first rewritten occurrence,
      and does not probe the second.
    verified: "Focused command: pnpm -C packages/cli exec vp test run
      test/state/gitBackfill.test.ts test/state/legacyMigration.test.ts
      test/state/archiveAuthority.test.ts (3 files, 41 tests passed; includes
      direct apply, replay, and real-ref no-remote coverage; adversarial result
      bound is final entries=100, omitted_count=100,
      omission_reason=result_limit, deterministic repeat; history budget is 499
      reachable + 1 rewritten = commits_used 500 of 500, reflog --max-count 1,
      second rewritten hash not probed, no remote/custom operations).
      Help/schema: pnpm -C packages/cli exec vp test run test/cli/help.test.ts
      test/cli/schema.test.ts (2 files, 25 tests passed; this supersedes Cycle
      788's inaccurate 24 claim only). Non-packaging boundary: pnpm -C
      packages/cli exec vp test run --exclude test/packaging/prepack.test.ts
      (175 files, 1784 tests passed). Full boundary: pnpm -C packages/cli test
      (176 files, 1788 passed, 5 known npm 12 packaging JSON-parser failures;
      1793 total). pnpm -C packages/cli run typecheck passed; pnpm -C
      packages/cli build passed; pnpm -C packages/cli run bundle:data passed.
      node packages/cli/dist/bin/agentera.js check validate capability build
      passed; capability-contract --format json passed with 2/2 checks; artifact
      validation for progress, plan, decisions, and health passed. node
      packages/cli/dist/bin/agentera.js check compact --format json passed with
      error_count=0, over_limit_count=0, protected_overflow_count=0,
      changed_count=0 and 5 existing archive-recovery refusals."
    next: Leave simplified-plan Task 1 pending with its one recorded failure; do not
      evaluate or complete it and do not begin Task 2. Preserve immutable Cycles
      787 and 788, Decision 53, projection policy, startup caveats, malformed
      fixture caveat, known npm packaging failures, archive refusals, direct
      apply/replay, no remote/custom refs, and the no-receipt local-operator
      model. Do not add security machinery, audit, release, publication, tag,
      push, vision, profile, or satisfaction changes.
    context:
      intent: Retry simplified Agentera Task 1 after the bounded-history audit
        failure, preserving the simple optional local-Git enrichment model and
        immutable prior evidence.
      constraints: No receipt, HMAC, token, secret, expiry, ledger, daemon, service,
        remote access, hostile-local-actor defense, or speculative compatibility
        machinery. Fold this typed progress append and the existing one-failure
        .agentera/plan.yaml state into the substantive commit.
      unknowns: The five known npm 12 packaging parser failures, five archive-recovery
        refusals, malformed formatting fixture caveat, and stale or missing
        startup caveats remain unchanged.
      scope: packages/cli/src/state/gitBackfill.ts;
        packages/cli/src/state/gitBackfillHistory.ts;
        packages/cli/src/state/gitBackfillOutput.ts;
        packages/cli/test/state/gitBackfill.test.ts;
        packages/cli/test/state/archiveAuthority.test.ts;
        references/artifacts/state-storage-authority.yaml; .agentera/plan.yaml;
        .agentera/progress.yaml
  - number: 788
    timestamp: 2026-07-14 12:45
    type: fix
    phase: build
    what: "Removed speculative preview receipt security from optional Git
      enrichment: --dry-run remains read-only and optional, while exact --apply
      --force applies without a prior preview after independent project, HEAD,
      candidate provenance/content/reachability, and immutable-target
      revalidation. Preserved bounded local history, allowed refs, equality,
      idempotent retry, and local migration independence."
    inspiration: Simplified Agentera plan Task 1 and the explicit local-operator
      simplicity decision rejecting cryptographic prior-preview proof.
    discovered: Cycle 787's real temporary-repository 18-call assertion remains
      valid for allowed local heads/tags and forbidden remote/custom operations,
      but its receipt-integrity, token, expiry, and prior-preview claims are not
      part of the accepted model. Current evidence did not justify changing the
      separate open v1 custom-artifact-pair TODO.
    verified: "Focused command: pnpm -C packages/cli exec vp test run
      test/state/gitBackfill.test.ts test/state/legacyMigration.test.ts
      test/state/archiveAuthority.test.ts (3 files, 39 tests passed);
      help/schema: pnpm -C packages/cli exec vp test run test/cli/help.test.ts
      test/cli/schema.test.ts (2 files, 24 tests passed); non-packaging
      boundary: pnpm -C packages/cli exec vp test run --exclude
      test/packaging/prepack.test.ts (175 files, 1782 tests passed); full
      boundary: pnpm -C packages/cli test (176 files, 1786 passed, exactly 5
      known npm 12 packaging JSON-parser failures). pnpm -C packages/cli run
      typecheck, build, and bundle:data passed. Capability build/contract and
      progress/plan/decisions/health validations passed. check compact --format
      json passed with error_count=0, over_limit_count=0,
      protected_overflow_count=0, changed_count=0 and 5 existing
      archive-recovery refusals. Temp CLI smoke: direct apply without preview
      complete with applied=1, retry complete with replayed=1, optional preview
      read_only=true and unchanged=true, remote_contact=false, and reachable
      refs only HEAD/local heads/tags; the persisted real-ref fixture asserted
      18 Git calls and no remote/custom operations."
    next: Leave simplified-plan Task 1 pending; do not evaluate or complete it and
      do not begin Task 2. Preserve Decision 53, archive immutability,
      projection policy, stale/missing startup caveats, malformed formatting
      fixture caveat, known npm packaging failures, the separate open
      custom-artifact TODO, and the prior plan failure record. Do not add
      security machinery, audit, release, publication, tag, push, vision,
      profile, or satisfaction changes.
    context:
      intent: Correct Cycle 787 with immutable, truthful, reconstructable evidence
        while executing only simplified-plan Task 1.
      constraints: No receipt, HMAC, token, secret, expiry, ledger, daemon, service,
        remote access, or hostile-local-actor defense. Fold this typed progress
        append into the substantive implementation commit; leave Task 1 pending
        and preserve all prior progress bytes.
      unknowns: The separate v1 custom-artifact-pair migration TODO remains open
        because this task did not change that code. The five known npm 12
        packaging parser failures, five archive-recovery refusals, malformed
        formatting fixture caveat, and stale or missing startup caveats remain
        unchanged.
      scope: packages/cli/src/state/gitBackfillReceipt.ts (deleted);
        packages/cli/src/state/gitBackfill.ts;
        packages/cli/src/state/gitBackfillOutput.ts;
        packages/cli/src/state/migrationEnrichment.ts;
        packages/cli/src/cli/commands/backfill.ts; packages/cli/src/cli/help.ts;
        references/artifacts/state-storage-authority.yaml;
        packages/cli/test/state/gitBackfill.test.ts;
        packages/cli/test/state/legacyMigration.test.ts;
        packages/cli/test/state/archiveAuthority.test.ts;
        packages/cli/test/cli/help.test.ts; generated bundle parity; TODO.md;
        .agentera/progress.yaml
  - number: 787
    timestamp: 2026-07-14 11:33
    type: fix
    phase: build
    what: "Closed the optional Git handoff and guard gaps from the Task 3 audit:
      generated commands now carry exact project, artifact, and number
      selectors; exact dry-run output carries a bounded project-local receipt
      token; forced apply requires and validates that receipt before final HEAD
      and candidate revalidation; and real-ref Git evidence is persisted."
    inspiration: Retry continuation plan Task 3 after independent orchestration
      audit failure continuation-task3-audit-1.
    discovered: Cycle 786's 68-Git-call trace had no persisted assertion. A real
      temporary repository fixture now persists an 18-call preview assertion
      with command categories and forbidden-operation checks while local
      heads/tags plus remote-tracking and custom refs are present.
    verified: "Focused real-repository migration/backfill command: 5 files, 72 tests
      passed. Non-packaging boundary: 175 files, 1781 tests passed. Full suite:
      176 files, 1785 passed and exactly 5 known npm packaging JSON-parser tests
      failed. Typecheck, build, bundle:data, capability build validation,
      capability-contract validation, artifact progress/plan/decisions/health
      validation, and compact passed; compact reported error_count=0,
      over_limit_count=0, protected_overflow_count=0, changed_count=0 while
      preserving 5 existing archive-recovery refusals. Temp CLI smoke: preview
      complete with a 1135-byte token, apply complete with applied=1, retry
      complete with replayed=1, and remote_contact=false for all three."
    next: Leave Task 3 pending with failure_count 1 and do not record an evaluation
      or completion. Preserve migration zero-Git behavior, bounded history, HEAD
      revalidation, equality, archive idempotence, no custom refs, no remote,
      Decision 53, projection policy, startup caveats, known npm failures, and
      the prior plan failure record. Do not begin Task 4 or release/publication
      work.
    context:
      intent: Retry Agentera continuation Task 3 after the independent optional Git
        handoff, preview-guard, and evidence audit failure without evaluating or
        completing Task 3.
      constraints: Migration remains non-Git and non-backfill by default; Git
        enrichment is explicit, exact-selector, dry-run-first, local-only, and
        non-blocking when unavailable or declined. Apply requires --apply
        --force and the matching caller-carried preview token; no remote state,
        fetch, network, tag, push, or publication is allowed.
      unknowns: Task 3 remains pending for independent evaluation; failure_count
        remains 1. The 5 known npm packaging JSON-parser failures, 5 existing
        archive-recovery refusals, malformed formatting fixture caveat, and
        stale or missing startup caveats remain unchanged.
      scope: packages/cli/src/state/gitBackfillReceipt.ts;
        packages/cli/src/state/gitBackfill.ts;
        packages/cli/src/state/gitBackfillOutput.ts;
        packages/cli/src/state/migrationEnrichment.ts;
        packages/cli/src/state/legacyMigration.ts;
        packages/cli/src/cli/commands/backfill.ts; packages/cli/src/cli/help.ts;
        references/artifacts/state-storage-authority.yaml;
        packages/cli/test/state/gitBackfill.test.ts;
        packages/cli/test/state/legacyMigration.test.ts;
        packages/cli/test/state/archiveAuthority.test.ts;
        packages/cli/test/fixtures/git-backfill-real-repository-evidence.json;
        .agentera/plan.yaml
  - number: 786
    timestamp: 2026-07-14 10:53
    type: feat
    phase: build
    what: "Added explicit non-executing optional Git-enrichment handoff from
      completed local migration: exact stable artifact:number selectors now
      expose the existing backfill dry-run and forced-apply commands, while
      migration remains Git, subprocess, network, and backfill free. Added
      unavailable-Git, interrupted-apply, allowed-ref, selector-handoff, and
      zero-subprocess regression coverage without changing the guarded backfill
      engine."
    inspiration: "Active continuation plan Task 3: Chain optional guarded Git
      enrichment, with Task 1 and Task 2 lifecycle state folded into this
      substantive change."
    discovered: The existing backfill foundation already bounds traversal, captures
      only HEAD/heads/tags, detects shallow and rewritten history, revalidates
      HEAD and provenance, and converges identical archive retries. The
      repository-wide Git signing configuration required commit.gpgsign=false in
      the isolated smoke fixture; no remote access was attempted.
    verified: "Focused migration/backfill command: 2 files, 28 tests passed.
      Non-packaging CLI boundary: 175 files, 1779 tests passed. Full package
      boundary: 176 files, 1783 passed and exactly 5 known npm 12 packaging
      JSON-parser tests failed. Typecheck, build, bundle:data, build capability
      validation, capability-contract validation, progress/plan/decisions/health
      artifact validation, and check compact passed; compact reported
      error_count=0, over_limit_count=0, protected_overflow_count=0,
      changed_count=0 while preserving 5 existing progress archive-recovery
      refusals. Temp no-Git migration smoke reported remote_contact=false and
      progress:1 handoff; traced local Git smoke reported
      preview/applied/replayed=1/1/1 across 68 Git calls and zero remote,
      remote-tracking, custom-ref, fetch, pull, push, or ls-remote calls."
    next: Leave Task 3 pending and do not record an evaluation or completion.
      Preserve Decision 53, archive immutability, projection policy, bounded
      output, stale/missing startup caveats, malformed formatting fixture
      caveat, known npm packaging failures, and no remote access. Do not begin
      Task 4, broad docs/schema alignment, audit, release, profile/vision work,
      publication, tag, or push.
    context:
      intent: Execute continuation plan Task 3 by chaining completed non-Git local
        migration to the existing immutable local-Git backfill contract without
        making enrichment implicit or required.
      constraints: Only optional migration handoff, focused migration/backfill tests,
        typed progress, and the existing uncommitted Task 2 lifecycle transition
        are in scope. Enrichment remains separately operator-invoked through
        agentera state backfill --artifact A --number N --dry-run followed by
        --apply --force.
      unknowns: Task 3 remains pending for independent evaluation; the five known npm
        packaging failures, five existing archive-recovery refusals, malformed
        formatting fixture, and stale or missing startup caveats remain
        unchanged.
      scope: packages/cli/src/state/migrationEnrichment.ts;
        packages/cli/src/state/legacyMigration.ts;
        packages/cli/src/cli/commands/migrate.ts;
        packages/cli/test/state/legacyMigration.test.ts;
        packages/cli/test/state/gitBackfill.test.ts; .agentera/plan.yaml;
        .agentera/progress.yaml
  - number: 785
    timestamp: 2026-07-14 10:14
    type: fix
    phase: build
    what: "Corrected Task 2 migration safety and output evidence: Markdown and YAML
      identities are parsed before selector narrowing; ambiguous,
      shorthand-collision, corrupt, unsupported, boundary, changed, conflict,
      and projection refusals retain deterministic failure provenance; and
      migration responses byte-bound entries and diagnostics across text, JSON,
      and YAML."
    inspiration: Independent continuation-task2-audit-1 against the active migration
      authority and the immutable Cycle 784 evidence.
    discovered: Forced custom parsing could hide a secondary artifact identity.
      Removing entries alone could not fit 100-candidate JSON/YAML responses
      because diagnostics also consumed the authority budget, so
      omitted-candidate diagnostics are now deterministically aggregated by
      failure class. The existing five npm packaging JSON-parser failures remain
      unchanged.
    verified: "Focused command: pnpm -C packages/cli exec vp test run
      test/state/legacyMigration.test.ts test/state/migrationAuthority.test.ts;
      2 files, 30 tests passed, including 12 multi-identity apply attempts
      across MIXED.md and MIXED.yaml for progress/decisions/health numbers 1/2,
      shorthand/corrupt/unsupported/boundary provenance, changed source, backup
      conflict, immutable archive conflict, and exact ordering/retry coverage.
      Compatibility command: 11 files, 191 tests passed. Non-packaging boundary:
      175 files, 1776 tests passed. Full suite: 176 files, 1780 passed and 5
      known npm packaging JSON-parser tests failed. Typecheck, build, and
      bundle:data passed. Capability-contract and build capability validation
      plus progress/plan/decisions/health artifact validation passed. check
      compact passed with error_count=0, over_limit_count=0,
      protected_overflow_count=0, changed_count=0 and the five existing
      archive-recovery refusals. Byte smokes against the 32768-byte authority
      cap measured JSON 31791 bytes with 21 entries and omitted_count 79, YAML
      32499 bytes with 36 entries and omitted_count 64, and text 32351 bytes;
      all remained valid/bounded. Temp apply/retry smoke measured first/second
      JSON outputs 30124/29779 bytes, one 283-byte archive, one exact backup,
      134 source bytes, converged archive files, and git_contacted=false."
    next: Keep Task 2 pending with failure_count 1 and do not record a new
      evaluation or mark it complete. Cycle 784 remains immutable; this Cycle
      785 supersedes only its invalid claim that ambiguous records remain
      non-addressable, with the exact MIXED.md/MIXED.yaml regression evidence
      above. Preserve archive/backup/projection ordering, interruption recovery,
      exact bytes, idempotence, Decision 53, no Git/network/remote, projection
      policy, startup caveats, and the five known packaging failures. Do not
      begin Task 3, Git enrichment, profile/vision work, release, publish, tag,
      or push.
    context:
      intent: Retry Agentera continuation Task 2 after the independent migration
        safety/output/evidence audit without evaluating or completing Task 2.
      constraints: Only migration parser/classification/provenance/output-boundary
        tests, the authority projection, generated bundle parity, typed
        progress, and the pre-existing Task 2 evaluation state are in scope. Do
        not modify vision, Task 2 status/evaluation counts, Git/network
        behavior, profile, release metadata, or remote state.
      unknowns: Task 2 remains pending with failure_count 1; no new evaluation was
        recorded. The five known npm packaging JSON-parser failures and existing
        archive-recovery refusals remain open caveats.
      scope: packages/cli/src/state/legacyIdentity.ts;
        packages/cli/src/state/legacyMigrationParser.ts;
        packages/cli/src/state/legacyMigration.ts;
        packages/cli/src/state/migrationAuthority.ts;
        packages/cli/src/cli/commands/migrate.ts;
        packages/cli/test/state/legacyMigration.test.ts;
        packages/cli/test/state/migrationAuthority.test.ts;
        references/artifacts/state-storage-authority.yaml; .agentera/plan.yaml;
        .agentera/progress.yaml; generated bundle parity
  - number: 784
    timestamp: 2026-07-14 09:20
    type: feat
    phase: build
    what: Implemented bounded non-Git legacy migration with strict local
      YAML/Markdown parsing, exact immutable archive and backup publication,
      bounded projection application, conflict refusal, and interruption-safe
      idempotent retry.
    inspiration: Active migration rollout plan Task 2 and its confirmed Task 1
      authority/inventory contract.
    discovered: Task 2 needed a backup publication boundary and optimistic
      projection publication check in the existing mutation transaction. Custom
      candidates require exact path, artifact, and number pins; summary,
      ambiguous, corrupt, unsupported, unsafe, changed, and conflicting records
      remain non-addressable. The repository retains five known npm packaging
      JSON-parser failures and the existing malformed invalid-progress-yaml
      formatting fixture.
    verified: "Focused migration/state transaction command:
      test/state/legacyMigration.test.ts, test/state/migrationAuthority.test.ts,
      test/state/write/stateWrite.test.ts, and test/lint/monolithLint.test.ts; 4
      files, 89 tests passed. Lifecycle/read compatibility command:
      listRetrieval, directRetrieval, archiveDiscovery, archivePublication,
      projectionPolicy, and lifecycleAdapters; 6 files, 128 tests passed.
      Non-packaging boundary: 175 files, 1773 tests passed. Typecheck, build,
      bundle:data, targeted formatting, capability build, capability-contract,
      artifact progress/plan/decisions/health validation, compact, and
      temp-project dry-run/apply/retry smoke passed. Full suite retained exactly
      5 known npm packaging JSON-parser failures."
    next: Leave Task 2 pending and do not record evaluation; orchestrator owns
      audit/status. Preserve no Git or remote access, Decision 53 no-inference
      semantics, archive immutability, projection-failure recovery, startup
      caveats, and known packaging/formatting caveats. Do not begin Task 3,
      enrich Git, edit vision/profile, publish, tag, or push.
    context:
      intent: Execute continuation plan Task 2 without evaluating or completing it.
      constraints: Only Task 2 local migration application, its tests, the existing
        transaction boundary extension, typed progress, and the uncommitted Task
        1 lifecycle changes are in scope. No Git enrichment, broad docs
        alignment, health audit, version bump, publish, tag, push, profile
        refresh, vision edit, or satisfaction mutation.
      unknowns: Independent orchestration audit and final Task 2 status remain
        outstanding; historical startup caveats and the known packaging harness
        failures remain unchanged.
      scope: packages/cli/src/state/legacyMigration.ts;
        packages/cli/src/state/legacyMigrationParser.ts;
        packages/cli/src/state/write/mutation.ts;
        packages/cli/src/cli/commands/migrate.ts;
        packages/cli/test/state/legacyMigration.test.ts; .agentera/plan.yaml;
        .agentera/progress.yaml; generated dist and bundle parity
  - number: 783
    timestamp: 2026-07-14 08:11
    type: fix
    phase: build
    what: "Closed the continuation-task1-audit-1 gaps: bounded project-local
      read-only inventory now exercises authority roots, depth, exclusions,
      normalized ordering, symlink and boundary rejection, custom names,
      candidate/file/total-byte limits, and deterministic omission metadata;
      count rules and omission field sources are authority-driven; duplicate
      invalid-combination records fail closed; and every structured
      invalid-input envelope carries recovery guidance. Preview and apply still
      validate intent only with mutation_performed=false, no archives, backups,
      projections, or Git. Cycle 781 remains byte-unchanged and Cycle 782
      remains the prior superseding correction."
    inspiration: Active plan Task 1 continuation after independent orchestration
      evaluator attempt continuation-task1-audit-1.
    discovered: Inventory can exercise the byte bounds from lstat metadata without
      reading candidate contents or invoking Git. The full Vitest suite must run
      serially because concurrent global setup races copy-bundle. The existing
      malformed invalid-progress-yaml fixture still blocks repository
      formatting, and the five known npm 12 packaging JSON-parser failures
      remain outside this fix.
    verified: "Focused command: pnpm -C packages/cli exec vp test run
      test/state/migrationAuthority.test.ts
      test/cli/invalidInputEnvelope.test.ts test/cli/schema.test.ts; 3 files, 35
      tests passed. Adversarial coverage includes 257 candidates, 1-MiB-plus
      file, 17-MiB total, symlink and boundary escapes, normalized ordering,
      limits 1/100/out-of-range, duplicate authority combinations, and
      text/JSON/YAML omission and recovery parity. Non-packaging command: pnpm
      -C packages/cli exec vp test run --exclude test/packaging/prepack.test.ts;
      174 files, 1762 tests passed. Full command: pnpm -C packages/cli test; 174
      files passed, 1766 tests passed, 5 known npm 12 packaging JSON-parser
      tests failed. pnpm -C packages/cli run typecheck, pnpm -C packages/cli run
      build, and pnpm -C packages/cli run bundle:data passed. Capability status
      and capability-contract validation passed; artifact progress and plan
      validation passed; check compact --format json passed with error_count=0,
      over_limit_count=0, protected_overflow_count=0, changed_count=0 and the
      five existing archive-recovery refusals preserved. Source and bundle
      authority comparison passed. Bounded CLI smokes passed for migrate help,
      schema JSON, inventory JSON/YAML, dry-run, apply-force, and invalid apply
      without force; valid apply reported mutation_performed=false and
      remote_contact=false."
    next: Leave Task 1 pending with failure_count 1 for independent re-audit; do not
      record an evaluation or begin Task 2 mutation/application. Preserve
      Decision 53, archive immutability, projection policy, no remote, startup
      caveats, known npm failures, and the malformed fixture caveat.
    context:
      intent: Retry Task 1 after the independent executable migration authority and
        bounded inventory audit failure without evaluating or completing Task 1.
      constraints: Do not alter Cycle 781, do not append another correction solely to
        restate Cycle 782, do not evaluate or complete Task 1, do not implement
        Task 2 archive/backup/projection mutation, do not edit vision or
        profile, and do not publish, tag, or push.
      unknowns: Independent re-audit remains. Stale profile and missing decision,
        vision, and profile startup caveats remain unchanged.
      scope: references/artifacts/state-storage-authority.yaml;
        packages/cli/src/state/migrationAuthority.ts;
        packages/cli/src/cli/commands/migrate.ts;
        packages/cli/src/cli/errors.ts;
        packages/cli/test/state/migrationAuthority.test.ts;
        packages/cli/test/cli/invalidInputEnvelope.test.ts;
        packages/cli/test/cli/fixtures/oracle/invalid-input-envelope.json;
        .agentera/plan.yaml; generated dist and bundle parity
  - number: 782
    timestamp: 2026-07-14 07:35
    type: fix
    phase: build
    what: "Corrected Cycle 781 evidence and implemented Task 1 migration authority
      consumption: schema no longer keeps a migration filter registry; runtime
      derives selectors, invalid combinations, result/count/omission fields,
      bounds, and recovery from the canonical authority; deferred inventory and
      apply remain filesystem-free. Cycle 781 is superseded by this correction
      and remains byte-unchanged."
    inspiration: Active plan Task 1 and prior evaluator findings for Cycle 781
      authority duplication, unconsumed result metadata, missing truncation
      tests, and inaccurate count/path claims.
    discovered: The exact final focused command passed 61 tests across 4 files. The
      earlier 58/63 claims were not reused. The actual existing error helper
      path is packages/cli/src/cli/errors.ts, not
      packages/cli/src/cli/commands/errors.ts. Task 1 can validate symlink and
      exclusion paths read-only while Task 2 retains all candidate scanning and
      mutation. The repository formatter remains blocked by the existing
      intentionally malformed invalid-progress-yaml fixture.
    verified: "Exact focused rerun: pnpm -C packages/cli exec vp test run
      test/state/migrationAuthority.test.ts test/cli/state.test.ts
      test/cli/schema.test.ts test/cli/invalidInputEnvelope.test.ts -> 4 files,
      61 tests passed. Non-packaging: pnpm -C packages/cli exec vp test run
      --exclude test/packaging/prepack.test.ts -> 174 files, 1,757 tests passed.
      Full suite: pnpm -C packages/cli test -> 174 files passed, 1,761 tests
      passed, 5 known npm 12 packaging JSON-parser tests failed. Typecheck,
      build, bundle:data, capability/status, capability-contract,
      artifact/progress, compact, source scans, authority bundle parity, and
      text/JSON/YAML migration smokes passed; valid apply reported
      mutation_performed=false."
    next: Leave Task 1 pending and do not record an evaluation; the orchestrator
      owns independent audit and status. Keep Task 2 filesystem inventory,
      archive, backup, projection, and mutation deferred. Do not change Cycle
      781, run migration, enrich Git, alter vision/profile/decision
      satisfaction, publish, tag, or push.
    context:
      intent: "Execute Task 1: make migration authority executable, prove truthful
        bounded contracts, and publish corrected independent evidence without
        starting filesystem migration."
      constraints: Preserve archive immutability, Decision 53 no-inference,
        uniform_10_40_50, non-Git completion, no remote access, stale profile
        and missing startup caveats, known npm 12 failures, and the confirmed
        agentera state migrate namespace. Fold this progress correction into the
        substantive implementation commit.
      unknowns: Independent orchestration audit remains. Stale profile and missing
        decision, vision, and profile startup caveats remain unchanged.
      scope: references/artifacts/state-storage-authority.yaml;
        packages/cli/src/state/migrationAuthority.ts;
        packages/cli/src/cli/commands/migrate.ts;
        packages/cli/src/cli/commands/schema.ts; packages/cli/src/cli/help.ts;
        packages/cli/test/state/migrationAuthority.test.ts; TODO.md; corrected
        Cycle 781 actual path packages/cli/src/cli/errors.ts
  - number: 781
    timestamp: 2026-07-14 06:10
    type: fix
    phase: build
    what: Retried Task 2 after the independent authority audit by removing
      duplicated migration parser values, deriving parser/help/schema surfaces
      from the canonical authority, preserving structured YAML invalid-input
      envelopes, and adding bounded authority and dispatch coverage without
      implementing migration execution.
    inspiration: Plan Task 2 retry after independent migration authority and
      envelope audit failure; user-confirmed agentera state migrate namespace
      and bounded project-local custom-name inventory.
    discovered: The authority needed machine-readable selector metadata for the
      candidate filename pattern and format selector so parser and boundary
      checks could consume source-owned values without a second registry. The
      existing npm 12 packaging harness retains five known JSON parsing failures
      outside this scope.
    verified: "Focused migration/state/help/schema tests passed: 4 files, 63 tests.
      Non-packaging CLI suite passed: 174 files, 1,754 tests. Typecheck, build,
      and bundle:data passed. Migration source/bundle authority parity passed.
      Capability-contract and capability status validation passed. Plan,
      progress, decisions, and health artifact validation passed. check compact
      --format json passed with error_count=0, over_limit_count=0,
      protected_overflow_count=0, and changed_count=0; the five pre-existing
      progress archive-recovery refusals remained unchanged. CLI smoke checks
      passed for state migrate help, schema JSON, inventory JSON/YAML, dry-run,
      apply-force dispatch, invalid apply-without-force, and bare state
      guidance. JSON and YAML invalid-input envelopes parsed equivalently and
      text guidance retained syntax/example repair fields."
    next: Leave Task 2 pending with failure_count 1 for re-audit; do not record
      another evaluation or complete it. Task 3 remains deferred and may later
      implement bounded non-Git scanning and publication from this authority. Do
      not add migration execution, Git enrichment, release work, vision changes,
      or remote access.
    context:
      intent: Repair only the authority, help, schema, parser, error-envelope, and
        state-command self-correction defects identified by
        task2-independent-audit-1, then verify and publish the retry evidence
        while keeping Task 2 pending.
      constraints: Only authority/help/schema/parser/error-envelope/dispatch
        corrections and tests were in scope. Preserve Decision 53 no-inference
        rules, archive immutability, archive-before-projection and
        projection-failure policy, startup caveats, Git-independent publication,
        and known npm 12 failures. Do not modify the Task 2 evaluation state
        beyond committing its existing failure record; do not push.
      unknowns: Stale profile state and existing prime startup caveats for missing
        decision, vision, and profile state remain unchanged; independent
        re-audit remains required.
      scope: references/artifacts/state-storage-authority.yaml;
        packages/cli/src/state/migrationAuthority.ts;
        packages/cli/src/cli/commands/migrate.ts;
        packages/cli/src/cli/commands/errors.ts;
        packages/cli/src/cli/commands/schema.ts;
        packages/cli/src/cli/commands/help.ts;
        packages/cli/src/cli/dispatch/index.ts;
        packages/cli/test/state/migrationAuthority.test.ts;
        packages/cli/test/cli/state.test.ts; generated dist and bundle parity;
        .agentera/plan.yaml
  - number: 749
    timestamp: 2026-07-13 10:58
    type: feat
    phase: build
    what: Delivered read-only numbered archive discovery and entry validation for
      progress, decisions, and health.
    inspiration: Task 1 state-storage authority and its fixed project-local archive
      contract.
    discovered: The exact path template makes canonical numeric ordering and
      artifact attribution explicit; symlink checks must use lstat before
      realpath boundary checks so escaping links are classified as unsafe
      records.
    verified: "Focused test/state/archiveDiscovery.test.ts: 12 tests passed. pnpm -C
      packages/cli test: 1,599 tests passed across 164 files. pnpm -C
      packages/cli run typecheck passed. pnpm -C packages/cli build passed.
      Local capability-contract and build capability validation passed. node
      packages/cli/dist/bin/agentera.js check compact passed with zero
      over-limit artifacts."
    next: "Task 3: publish immutable archives with deterministic replay identity;
      keep Task 2 pending for orchestration evaluation."
    context:
      intent: Implement Task 2 from the active lossless archive-backed state storage
        plan without changing archive publication, projection, migration,
        retrieval, or plan and vision archive behavior.
      constraints: Read-only discovery only; no archive publication, transactions,
        overlays, projection, hydration, migration, Git scanning, get/list CLI
        behavior, satisfaction inference, profile refresh, or plan task
        completion.
      unknowns: Decision satisfaction remains subject to existing review pressure;
        historical lifecycle diagnostics remain non-executable; no satisfaction
        or profile state was inferred or updated.
      scope: packages/cli/src/state/archiveDiscovery.ts;
        packages/cli/test/state/archiveDiscovery.test.ts;
        .agentera/progress.yaml; existing uncommitted Task 1 status in
        .agentera/plan.yaml
  - number: 748
    timestamp: 2026-07-13 10:36
    type: docs
    phase: build
    what: Published the state-storage authority for numbered progress, decisions,
      and health archives, fixed project-root paths, decision overlays,
      projection roles, direct get/list API, compatibility classes, structured
      failures, and byte budgets; added contract tests and the authority
      inventory pointer.
    inspiration: Active plan Task 1 and the existing artifact registry, typed
      writer, state namespace, decision satisfaction schema, JSON output
      manifest, and archive conventions.
    discovered: The mutable decision surface is limited to satisfaction.state,
      satisfaction.evidence, and explicit user_confirmation fields;
      review_needed and caveats are derived. Existing plan and vision archives
      use separate filename conventions and remain outside numbered-entry
      discovery.
    verified: "Focused authority run and full pnpm -C packages/cli test: 1,587 tests
      passed across 163 files; pnpm -C packages/cli run typecheck; pnpm -C
      packages/cli build; check validate capability-contract --format json;
      check compact --format json; git diff --check."
    next: "Task 2: implement archive discovery and entry validation from this
      authority; do not change the active plan task status in this cycle."
    context:
      intent: Publish Task 1's storage, path, API, compatibility, projection, and
        output-budget authority before implementation.
      constraints: Task 1 contract only. No archive writes, hydration, migration, Git
        scanning, remote access, plan or vision archive changes, decision
        satisfaction changes, health writes, version bump, publish, tag, or
        push.
      unknowns: "Task 1 unknowns resolved by current writer and CLI evidence: decision
        overlay fields/transitions and exact state get/list command shape."
      scope: references/artifacts/state-storage-authority.yaml;
        packages/cli/test/state/archiveAuthority.test.ts;
        references/meta/documentation-inventory.md
  - number: 747
    timestamp: 2026-07-13 09:46
    type: fix
    phase: build
    what: Removed the fixed full-plan task-count ceiling and added coherent
      lifecycle-boundary guidance with nine-task publication coverage.
    discovered: The typed writer already accepts arbitrary sequential task counts;
      the former ceiling was declarative prose, schema, and startup metadata.
    verified: pnpm -C packages/cli build; pnpm -C packages/cli test (1579 passed).
    next: Run capability, contract, compact, and npm release gates; publish only the
      development next channel.
    context:
      intent: Allow future coherent plans to exceed eight tasks without weakening task
        identity, dependency, or publication validation.
      constraints: Do not modify historical archives, suite version surfaces, or @latest.
      scope: Plan instructions, validation schema, startup contract, tests, and
        changelog.
  - number: 746
    timestamp: 2026-07-13 00:27
    type: test
    phase: build
    what: Verified packages/cli/test/state/planLifecycleContract.test.ts and closed
      the plan lifecycle publication rollout.
    verified: 1,577 CLI tests; typecheck, build, compact, plan and contract
      validation, lifecycle adapter validation, npm pack/publish dry-runs, and
      68 lifecycle fixture tests passed.
    next: Orchestrator-owned post-evaluation archive only.
    context:
      intent: Complete Task 8 with behavioral lifecycle fixtures and release gates.
      constraints: No push, publish, tag, runtime refresh, or decision satisfaction changes.
      unknowns: Immutable historical archive diagnostics remain.
      scope: Plan lifecycle state, TODO, and state-writer documentation.
  - number: 745
    timestamp: 2026-07-13 00:10
    type: fix
    phase: build
    what: Updated packages/cli/src/capabilities/plan/instructions.ts so state plan
      create is the final publication gate and its YAML examples pass the typed
      writer.
    verified: "pnpm -C packages/cli test: 1,576 tests passed; typecheck, build, plan
      capability and capability-contract validation, compact, and writer-backed
      example publication passed."
    next: "Task 8: synchronize state and verify lifecycle behavior."
    context:
      intent: Complete active plan Task 7 without changing decisions or unrelated
        capabilities.
archive:
  - summary: "Cycle 780 (2026-07-14): Published the schema-backed local migration
      authority and the confirmed agentera state migrate discovery surface;
      valid..."
  - summary: "Cycle 779 (2026-07-14): Enforced bounded state discovery in all source
      capability instructions and the checked-in consumer matrix."
  - summary: "Cycle 778 (2026-07-14): Repaired bounded orchestration verification
      projection and completed the independent audit for Task 1."
  - summary: "Cycle 777 (2026-07-14): Validated the repaired Unicode surfaces and
      migrated the complete bounded state consumer matrix across all..."
  - summary: "Cycle 776 (2026-07-13): Made every bounded prime-path string
      truncation code-point safe and added adversarial emoji coverage across
      all..."
  - summary: "Cycle 775 (2026-07-13): Retried bounded prime startup projections:
      shared list classification now drives startup conflict, duplicate, mirror,
      ambiguity,..."
  - summary: "Cycle 774 (2026-07-13): Bound prime startup source work and output:
      incrementally scan current projection metadata, validate archives
      without..."
  - summary: "Cycle 773 (2026-07-13): Retried Task2 attempt1 by centralizing
      leading-only legacy identity parsing, making explicit Dnn decision
      shorthand rows..."
  - summary: "Cycle 772 (2026-07-13): Made legacy state listing identity-safe and
      physically truthful: explicit canonical numbers and decisions Dnn
      shorthand..."
  - summary: "Cycle 771 (2026-07-13): Repaired bounded snapshot cursor traversal:
      serializer-aware pages now retain an advancing row prefix or fail..."
  - summary: "Cycle 770 (2026-07-13): Allowed forced plan archive and replacement to
      preserve exact schema-valid predecessors whose only full-file overflow..."
  - summary: "Cycle 769 (2026-07-13): Retried Task14 Git legacy backfill: bounded
      all historical Git reads by the shared commit and..."
  - summary: "Cycle 768 (2026-07-13): Implemented Task14 Git legacy backfill as
      agentera state backfill: bounded read-only local-history inventory, exact
      unique..."
  - summary: "Cycle 767 (2026-07-13): Retried Task 12 after evaluator failure. Fixed
      durability Git probing so absent .git metadata skips..."
  - summary: "Cycle 766 (2026-07-13): Implemented Task 12 read-only durability
      diagnostics under agentera check durability. The authority now publishes
      the..."
  - summary: "Cycle 765 (2026-07-13): Retried Task 9 after evaluator attempt 1
      failed. Fixed bounded-list minimal fallback to preserve a..."
  - summary: "Cycle 764 (2026-07-13): Verified the final text list renderer includes
      explicit provenance and compatibility for every bounded row;..."
  - summary: "Cycle 763 (2026-07-13): Reverified Task 9 after the final overlay
      provenance assertion and state synchronization; the list
      implementation..."
  - summary: "Cycle 762 (2026-07-13): Implemented snapshot-stable bounded state
      listing for progress, decisions, and health with numeric ordering, opaque
      signed..."
  - summary: "Cycle 761 (2026-07-13): Retried Task 8 after evaluation failure:
      direct decision get now preserves absent outcome metadata, and..."
  - summary: "Cycle 760 (2026-07-13): Corrected Task 8 verification counts after
      adding unsupported-state archive fixtures; Cycle 759 evidence remains
      immutable..."
  - summary: "Cycle 759 (2026-07-13): Exposed stable-ID direct retrieval for
      progress, decisions, and health with exact archive lookup, provenance,
      overlay..."
  - summary: "Cycle 758 (2026-07-13): Fixed Task 7 attempt-1 audit findings for
      legacy summary bounds, byte-budget fallbacks, and retrieval provenance."
  - summary: "Cycle 757 (2026-07-13): Replaced numbered-state retention limits with
      lossless projection defaults and deterministic omission metadata."
  - summary: "Cycle 756 (2026-07-13): Corrected Cycle 755 Task 6 test counts through
      an append-only supersession record"
  - summary: "Cycle 755 (2026-07-13): Gated automatic and explicit projection
      summaries on verified numbered archives"
  - summary: "Cycle 754 (2026-07-13): Corrected Task 5 review-pressure consistency
      by hydrating decision overlays before append overflow calculations,
      compaction selection,..."
  - summary: "Cycle 753 (2026-07-13): Stored and composed current decision review
      state through an authority-derived ID-keyed overlay; decision updates
      preserve..."
  - summary: "Cycle 752 (2026-07-13): Unify state append and projection repair under
      one crash-consistent mutation transaction."
  - summary: "Cycle 751 (2026-07-13): Corrected Cycle 750's focused-test count
      through an append-only supersession record."
  - summary: "Cycle 750 (2026-07-13): Published immutable numbered progress,
      decisions, and health archives with deterministic replay and orphan
      recovery."
  - summary: "Cycle 741 (2026-07-12): Updated
      packages/cli/src/state/write/transaction.ts so plan create/archive
      publication validates final bytes and retries converge after
      interruptions."
  - summary: "Cycle 740 (2026-07-12): Separated plan lifecycle writes from task
      status writes and enforced canonical open or complete serialization."
  - summary: "Cycle 739 (2026-07-12): Added
      packages/cli/src/upgrade/migrateArtifactsV2ToV3.ts migration for legacy
      plan lifecycle values in docs-mapped current plans and archives."
  - summary: "Cycle 738 (2026-07-12): packages/cli/src/cli/planArtifacts.ts now
      reports legacy lifecycle input and malformed plan diagnostics to state and
      orientation consumers."
  - summary: "Cycle 737 (2026-07-12): Added behavioral lifecycle-scan safety
      coverage for every claimed traversal boundary."
  - summary: "Cycle 736 (2026-07-12): Repaired lifecycle inventory classification
      for OpenCode plan projections and generated-output pruning."
  - summary: "Cycle 735 (2026-07-12): Completed Task 1 lifecycle classification with
      a safely pruned repository-root scan in
      skills/agentera/schemas/artifacts/plan.yaml."
  - summary: "Cycle 734 (2026-07-12): Repaired Task 1 lifecycle inventory
      completeness with scan-enforced classification patterns in
      skills/agentera/schemas/artifacts/plan.yaml."
  - summary: "Cycle 733 (2026-07-12): Defined the canonical plan lifecycle contract
      in skills/agentera/schemas/artifacts/plan.yaml and enforced its
      seven-family inventory in planLifecycleContract.test.ts."
omitted: true
omitted_count: 28
omission_reason: projection_capacity
retrieval:
  available: true
  command: agentera state progress get --number N --format json
omission_provenance:
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
  - source: legacy_summary
    detail_availability: unavailable
    compatibility: degraded
    archive_verified: false
    omitted_count: 1
