{
  "description": "agentera v3 Codex plugin hooks: schema-backed apply_patch artifact validation using PLUGIN_ROOT",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^apply_patch$",
        "hooks": [
          {
            "type": "command",
            "command": "npx -y agentera@next hook validate-artifact",
            "timeout": 10,
            "statusMessage": "validating artifact"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "^apply_patch$",
        "hooks": [
          {
            "type": "command",
            "command": "npx -y agentera@next hook validate-artifact",
            "timeout": 10,
            "statusMessage": "validating artifact"
          }
        ]
      }
    ]
  }
}
