# TODO

## ⇶ Critical

- [chore] No open critical items.

## ⇉ Degraded

- [chore] No open degraded items.

## → Normal

- [fix:2.7.7] Orkestrera `task_queue.dependency_ready_tasks` and `blocked_tasks[*].blocked_reasons` are broken in `prime --context orkestrera --format json`: when task 1 is present and `complete`, all later tasks show `blocked_reasons: ["dependency 1 is not present in plan tasks"]` and `dependency_ready_tasks` is `[]`, but the same response's `task_summaries` correctly lists task 1 as complete and `selected_next_action` correctly identifies the next ready task. Root cause is almost certainly a string-vs-integer compare in the dependency lookup (plan tasks are serialized with `number: 1` as an int, but the dependency list uses the string form `"1"`). Orkestrera callers must currently trust `selected_next_action` (or `task_summaries`) over `task_queue.dependency_ready_tasks`, which contradicts the orchestration_context source contract. Fix the dependency-check serializer/lookup in `scripts/agentera` so the two agree, and add a regression test that asserts a complete task unblocks its dependents in the returned `task_queue`. (Branch: `main` — implement on `main` for the stable Python CLI (`scripts/agentera`, `tests/test_query_cli.py` or equivalent). Coordination ledger on `feat/v3` may track the npm `@next` slice separately; reconcile both TODO ledgers when closing.)

- [chore:3.0.0] Remove v1 legacy cruft after the 3.0 compatibility boundary (see also Tier C in [`references/meta/documentation-inventory.md`](references/meta/documentation-inventory.md)): delete obsolete migration paths, stale v1 runtime bridge behavior, v1 skill bundle/suite wording, legacy fixture assumptions, and compatibility-only docs/tests that no longer protect supported Agentera behavior. (Branch: `feat/v3` — the actual code lives on `feat/v3`; this entry remains on `main` for backport awareness of normative prose only.)
- [chore:3.0.0] Enforce Decision 58's single-name protocol boundary: remove every backwards-compatible, deprecated, parallel, display-only, or storage/protocol split name for artifacts, concepts, primitives, command surfaces, schema fields, metadata fields, runtime adapter surfaces, and protocol identities. Zero post-3.0 surfaces may retain legacy aliases or multiple names; old names must be removed or migrated, not supported alongside the canonical name. (Branch: `feat/v3` — the actual code lives on `feat/v3`; this entry remains on `main` for backport awareness of normative prose only.)
- [chore:3.0.0] Design the Bun/npm packaging plan for Agentera 3.0, including single-binary delivery, npm package layout, app-home migration from Python-managed installs, and preserved direct-helper compatibility boundaries where still needed. (Branch: `feat/v3` — the actual code lives on `feat/v3`; this entry remains on `main` for backport awareness of normative prose only.)
- [test:3.0.0] Freeze CLI JSON contract parity fixtures before the TypeScript rewrite: supported command output shapes, exit codes, invalid-input guidance, artifact path/source contracts, and no-raw-read completeness signals must match or intentionally version-break. (Branch: `feat/v3` — the actual code lives on `feat/v3`; this entry remains on `main` for backport awareness of normative prose only.)
- [test:3.0.0] Port artifact validation, compaction, doctor/upgrade safety, verify gates, usage/stats consent semantics, and runtime adapter hooks behind TypeScript parity tests before cutting over from Python. (Branch: `feat/v3` — the actual code lives on `feat/v3`; this entry remains on `main` for backport awareness of normative prose only.)
- [feat:3.0.0] Rewrite the supported Agentera CLI core in TypeScript for Bun/npm/single-binary distribution, gated by contract-first parity with the current Python `agentera` CLI rather than a clean-slate command redesign. (Branch: `feat/v3` — the actual code lives on `feat/v3`; this entry remains on `main` for backport awareness of normative prose only.)

## ⇢ Annoying

- [chore] No open annoying items.

## ✓ Resolved

- ~~[fix:2.7.7] Self-audit linter budget lookup misses schema-name CLI input (`plan`, `progress`) and falls back to the 1000-word default~~ · resolved by beb535c (4725657–e8714de): `_resolve_budget_artifact` normalizes artifact names, budget dicts use lowercase schema-name keys, CLI `--text`/`--file` paths wire the normalized lookup, and unit plus CLI end-to-end regression tests pin behavior; cross-surface D58 single-name vocabulary unification remains open on `feat/v3` under Decision 58.
- ~~[chore:2.7.7] Decision 66 remove the progress.yaml per-cycle `commit` field~~ · resolved by 41e0394: dropped schema/validation commit requirements, removed `scripts/progress_commit.py`, `agentera check backfill`, and the validate-artifact ancestry guard; stripped `commit` from `.agentera/progress.yaml` cycles and v1 migration extraction; CLI progress JSON/text omits `commit`. TS parity in `feat/v3` `6b8af92`.
- ~~[fix:2.7.7] Inspektera WARN findings must cite line numbers~~ · resolved by 41e0394: orkestrera V10 validation rule, `capability-instruction-contract.yaml#evaluator_handoff`, instruction prose updates, and `tests/test_evaluator_handoff_contract.py`. TS registry parity in `feat/v3` `6b8af92`.
- ~~[chore:2.7.6] Consolidate duplicated progress-commit and git-ancestry logic~~ · resolved by 6530f42 and 56fd8fe: one `scripts/progress_commit.py` module now serves `agentera check backfill` and the validate-artifact guard via `hooks/common.py`.
- ~~[chore:2.7.6] Extract progress-commit module from `scripts/agentera` monolith~~ · resolved by 6530f42: `cmd_backfill` is a thin wrapper importing `compute_backfill` and `rewrite_cycle_commits`.
- ~~[chore:2.7.6] Move progress commit validation out of oversized `hooks/validate_artifact.py`~~ · resolved by 56fd8fe: guard delegates to `validate_progress_commits` with no near-copy git/token logic left in the hook.
- ~~[fix:2.7.6] Align progress guard YAML loading~~ · resolved by 56fd8fe: guard uses `load_yaml_mapping()` and returns no commit violation on corrupt or non-mapping progress YAML.
- ~~[chore:2.7.6] Reduce hooks→scripts sys.path coupling~~ · resolved by 56fd8fe: `hooks/common.py` documents the single sanctioned scripts bridge; neutral package deferred to 3.0.
- ~~[test:2.7.6] Expand progress commit YAML rewrite regression coverage~~ · resolved by 56fd8fe: folded/flow scalar and comment-preservation cases in `tests/test_progress_commit_guard.py`.
- ~~[docs:2.7.6] Document unknown-hash and shallow-clone behavior for progress commits~~ · resolved by 56fd8fe: progress schema notes and `UPGRADE.md` describe intentional skip and reconciliation.
- ~~[test:2.7.6] DRY progress commit token tests after module extraction~~ · resolved by 6530f42 and 56fd8fe: one parametrized `commit_token` table on the shared module.
- ~~[test:2.7.6] De-flake the progress-commit guard `repo` fixture~~ · resolved by 56fd8fe: stale commit is created on a side branch and kept off main history.

- ~~[fix:2.7.6] Catch non-mapping YAML roots in compaction check/gate: `load_yaml_mapping()` raises on list/scalar roots in `hooks/compaction.py` `_yaml_counts`/`_yaml_lists`, so `agentera check compact`/gate can traceback instead of reporting a degraded status row; catch `YAMLError` and emit an error reason like validator degradation.~~ · resolved by 8be19c4: `compute_compaction_status` catches `yaml.YAMLError` and emits error-classified `CompactionStatus` rows so `agentera check compact` and gate exit 2 with guidance instead of tracebacks; regression coverage in `tests/test_compaction_status.py`.
- ~~[fix:2.7.6] Complete compaction dedup wiring: `apply_retention_caps`/`MAX_*` in `hooks/common.py` is used by session bookmarks, but `compaction.compact_entries` still has its own 10/40/50 loop with different ordering (`_entry_number`); wire through the shared helper or add a contract test documenting intentional divergence.~~ · resolved by cf0aa93 and 55a9df8: `compact_entries` and non-decision `compact_yaml_file` archive caps delegate to `apply_retention_caps`; `tests/test_compaction_retention_contract.py` pins shared 10/40/50 limits and intentional numeric-ID vs timestamp ordering divergence.
- ~~[chore:3.0.0] Consolidate duplicated hook session helpers (`compact_entries`, `resolve_session_path`).~~ · resolved by moving `resolve_session_path()`, retention caps, and `compact_session_bookmark_entries()` into `hooks/common.py`, keeping numeric-ID `compaction.compact_entries()` for artifacts, and adding `tests/test_hooks_common.py`.
- ~~[chore:3.0.0] Consolidate `yaml.safe_load()` `None`-return patterns across the codebase.~~ · resolved by `scripts/yaml_mapping.py` with `load_yaml_mapping()`, hook and CLI alignment, regression tests in `tests/test_yaml_mapping_loader.py`, and smoke bundle copy of `yaml_mapping.py`.
- ~~[fix] Agentera subagent descriptors define zero tool/permission configuration~~ · resolved: created capability-tool-classification.yaml authority, updated 12 OpenCode agents with frontmatter permissions, strengthened 12 Codex agent instructions, updated runtime adapter docs/parity tables/interface model, extended CLI validate descriptors command to check permissions/guidance, and added regression tests.
- ~~[test] No formal JSON schema validation for CLI `--format json` output~~ · resolved: added tests verifying all JSON surface outputs against formal schemas.
- ~~[fix] `cmd_lint` returns exit code 0 when `status=="fail"` unless `--strict` is passed~~ · resolved: return 1 on any failure, removing strict exit gate.
- ~~[fix] `_query_generic` always returns exit code 0 on empty results~~ · resolved: return 1 on empty filtered results in text mode.
- ~~[fix] Deprecated alias lists inconsistent across 4 docs~~ · resolved: reconciled to unified list in migration contract and updated all doc references.
- ~~[fix] Schema loading silently swallows all exceptions / Silent `except Exception` swallowers across 6+ locations~~ · resolved: warning diagnostic emitted to stderr in load paths.
- ~~[fix] `cmd_validate_capability` exit codes differ between single and multi-target validation~~ · resolved: normalized single-target validation exit codes to 0/1.
- ~~[fix] Original `satisfaction.caveats` in decisions.yaml overwritten by enriched caveats~~ · resolved: original caveats prepended before enriched caveats.
- ~~[fix] `agentera hej --format json` returns `"command": "prime"`~~ · resolved: command name correctly set to `"hej"` when hej is run.
- ~~[fix] `source_contract.fields` in hej JSON omits `source` and `source_contract`~~ · resolved: added fields to hej structured fields payload, variable renamed.
- ~~[fix] `progress` documented as deprecated but code has no deprecation handling~~ · resolved: added deprecation alias matching `plan`/`hej` pattern.
- ~~[fix] `capability_context` stale in `HEJ_STRUCTURED_FIELDS` and `agent-ready-state-contract.yaml`~~ · resolved: removed `capability_context` from field structures.
- ~~[chore] Replace live `SPEC.md` references and migrate off `contract.md`~~ · resolved: replaced SPEC.md and contract.md references outside of historical archives.
- ~~[fix] Fix 7 critical validation and state-loading silent failures: `validate_artifact.py` exit 0 on crash (now exit 2), empty schema files bypassing validation (now reports violation), objective `VALIDATION_RULES` group ignored (now iterated alongside `VALIDATION`), `closure_consistency` rule unenforced (now enforced), and three silent `except Exception` swallowers in `scripts/agentera` (`_load_schemas`, `_load_registry_for_schemas`, `_load_artifact`) now log warnings to stderr.~~ · resolved by targeted fixes in `hooks/validate_artifact.py` and `scripts/agentera`; 1285 tests pass.
- ~~[fix] Fix 5 degraded validation and CLI consistency issues: `validate artifact` returns exit code 2 on unparseable JSON, `query --format json` returns structured envelope, `query` emits deprecation warning, archive entries validate required fields, `user_confirmation` distinguishes missing from wrong type.~~ · resolved by targeted fixes in `hooks/validate_artifact.py` and `scripts/agentera`; 1287 tests pass.
- ~~[feat:2.7.0] Cursor first-class runtime v1 (Decision 63): ship cursor and cursor-agent registry identities, repo-native hooks and managed agents, upgrade/doctor/eval paths, live preToolUse Write smoke, parity docs, and plan closeout without release tag or publication.~~ · resolved in 2.7.0 by commit 151043d plus Task 8 closeout; live smoke passed 2026-05-24; release tagging and marketplace publication remain blocked pending explicit approval.

- ~~[fix:2.6.2] Update Codex setup/upgrade config generation for `multi_agent_v2`: stop writing legacy `[agents].max_threads`, translate the old default to `features.multi_agent_v2.max_concurrent_threads_per_session`, and add regression coverage for Codex 0.133.0 validation error `agents.max_threads cannot be set when multi_agent_v2 is enabled`.~~ · resolved by updating config generation to use `[features.multi_agent_v2]` and removing the legacy `max_threads` key from `[agents]`.
- ~~[fix:2.6.2] Retire stale file-based Codex hook trust entries during plugin-primary upgrades: when `[plugins."agentera@agentera"].enabled = true` and plugin hook trust is written, remove Agentera-owned `[hooks.state."...hooks.json:pre_tool_use:0:0"]` and `[hooks.state."...hooks.json:post_tool_use:0:0"]` entries along with copied `hooks.json`, while preserving mixed or user-owned hook state for manual review.~~ · resolved by unlinking copied `hooks.json` and removing its matching trust entries from the `[hooks.state]` configuration table when plugin hooks are enabled.

- ~~[chore:2.6.1] Relocate vocabulary and benchmark authorities from `docs/` to `references/` (vocabulary → `references/cli/`, benchmark → `references/analysis/`).~~ · resolved in 2.6.1 by moving tracked docs, updating delegation paths, README, docs.yaml index, and regression tests.
- ~~[chore:2.6.1] Add `references/cli/vocabulary-index.yaml` and migrate Decision 44 plus normalization rules off duplicated Markdown tables.~~ · resolved in 2.6.1 by `vocabulary-index.yaml`, slim `references/cli/vocabulary.md`, and `tests/test_decision44_vocabulary.py` loading the deprecated scan pattern from YAML.
- ~~[chore:2.6.1] Consolidate JSON surface docs into `scripts/json_output_surface_manifest.yaml` and archive d47/d58/d59 markdown under `.agentera/archive/`.~~ · resolved in 2.6.1 by manifest scope owning live surfaces, archived workpapers, and simplified `docs/` gitignore policy.
- ~~[fix:2.4.x] Qualify ambiguous `SKILL.md` prose across docs, runtime guidance, and tests so conceptual references say `Agentera skill dispatcher`, `skill entry file`, `v1 skill entry file`, or `legacy hej bridge` instead of using bare `SKILL.md` as a noun.~~ · resolved by qualifying current docs, runtime guidance, capability schemas/prose, scripts, and tests to use `Agentera skill dispatcher` or `skill entry file`, preserving literal path and compatibility references, and adding focused regression coverage that rejects the cleaned-up ambiguous current-prose forms while leaving historical archives, protected vision state, routing/execution vocabulary cleanup, Decision 57 implementation, broad vocabulary tests, and 3.0.0 work open.
- ~~[fix:2.5.x] Fix installed `hej` entrypoint command construction so app-home resolution happens before expanding the managed app script path; regression-cover the unset `AGENTERA_HOME`/`XDG_DATA_HOME` path so inline assignment ordering cannot collapse the invocation to `/app/scripts/agentera` with `Failed to spawn`.~~ · resolved by modeling the unset-env inline-assignment shell expansion failure, switching installed hej executable guidance to prior `RESOLVED_AGENTERA_HOME` script-path construction in the skill dispatcher, hej capability, and upgrade retry path, and adding focused regressions while preserving OpenCode app-home injection, compatibility identifiers, and unrelated vocabulary/Decision 57/3.0.0 work.
- ~~[fix] Repair the Full Suite and Review Fixes regressions from the Agentera App Files Wording Cleanup batch.~~ · resolved by bounding current-state wording in PLAN evidence, synchronizing dispatch-contract assertions to route/suggest/delegate/spawn/subagent-mechanism vocabulary, adding upgrade lifecycle metadata mapping coverage for build/apply/public paths and boundary statuses, adding app lifecycle docs-delegation metadata coverage, tightening stale-profile caveat wording, and verifying the full suite with 1142 passing tests without advancing unrelated vocabulary, Decision 57, or 3.0.0 work.
- ~~[fix:2.4.x] Replace user-facing and diagnostic `bundle` wording that means installed app files with `Agentera app files`, `app files directory`, or `installed app`, while preserving compatibility surfaces such as `.agentera-bundle.json`, `bundle.status`, `activeBundleRoot`, `--only bundle`, and historical records.~~ · resolved by rewording current installed-app diagnostics to `Agentera app files`/`app files` repair and update language, changing the approval phrase to `approve app files repair ...`, preserving shipped compatibility identifiers and historical records, and adding focused authority-backed regressions while leaving SKILL.md, routing/execution, Decision 57 implementation, broad regression, and 3.0.0 TODOs open.
- ~~[fix:2.4.x] Align `agentera doctor`, `agentera hej`, and `agentera upgrade` structured app lifecycle metadata with the canonical Decision 54 vocabulary: use `up_to_date`, `outdated`, `repair_needed`, `migration_needed`, `manual_review_needed`, `ready_to_apply`, `applied`, and `no_changes_needed`, with transitional aliases only for proven consumers.~~ · resolved by adding canonical upgrade `lifecycleStatus` metadata with preserved workflow `status`/`compatibilityStatus`, keeping doctor/hej app metadata inside the authority's consumer-allowed statuses, fixing hej capability contexts to consume `up_to_date` instead of deprecated `fresh`, and adding focused authority-synchronized regressions while leaving broader vocabulary cleanup TODOs open.
- ~~[docs:2.4.x] Document Decision 57's capability instruction contract: rename capability `prose.md` to `instructions.md`, define `first_invocation_read`, and state that initial capability invocation reads `instructions.md` in full unless a compact-startup exception is declared.~~ · resolved by `references/cli/capability-instruction-contract.yaml`, `docs/vocabulary.md` delegation, README capability-author guidance, `.agentera/docs.yaml` source-index wiring, and focused tests for future `instructions.md`, current `prose.md` compatibility, `first_invocation_read` values, full-read obligations, compact-startup exception boundaries, unsupported current implementation state, and preservation of later rename/metadata/validation TODOs.
- ~~[docs:2.4.x] Document the routing and execution vocabulary collapse: use `Agentera router` for request-to-capability routing, `suggest` for next-action recommendations, `delegate` for Orkestrera task assignment, `spawn` for Realisera/Optimera workers, `subagent mechanism` for runtime support, and `pre-spawn Git commit` for worker safety.~~ · resolved by `references/cli/routing-execution-vocabulary.yaml`, `docs/vocabulary.md` delegation to that authority, and focused regression coverage for canonical concepts, forbidden ambiguous shortcuts, compatibility identifiers, historical/code/fixture preservation, classification rules, docs delegation, and source-index wiring.
- ~~[docs:2.4.x] Extend the machine-readable vocabulary authority to classify `bundle` and `SKILL.md` usage into canonical concepts: Agentera app files, suite package, plugin-shipped hooks, removed `bundle-status` command, Agentera skill dispatcher, skill entry file, v1 skill entry file, and legacy hej bridge.~~ · resolved by `references/cli/bundle-skill-vocabulary.yaml`, `docs/vocabulary.md` delegation to that authority, and regression coverage for canonical concepts, allowed/forbidden terminology, compatibility identifiers, classification rules, and docs delegation.
- ~~[docs:2.4.x] Create a machine-readable app lifecycle vocabulary authority for Decision 54 that defines canonical statuses, operation verbs, deprecated aliases, and ownership boundaries for doctor, hej, upgrade, docs, and tests.~~ · resolved by `references/cli/app-lifecycle-vocabulary.yaml`, `docs/vocabulary.md` delegation to that authority, and regression coverage for canonical status order, deprecated aliases, scoped verbs, and closed consumer ownership boundaries.
- ~~[fix:2.4.x] Replace current prose that uses `master dispatcher`, `Agentera skill dispatcher`, bare `dispatch`, `runtime dispatch substrate`, `worktree dispatch`, `pre-dispatch commit gate`, or Orkestrera `conductor` metaphor with the canonical route/suggest/delegate/spawn/subagent-mechanism vocabulary, preserving qualified compatibility names only where shipped consumers require them.~~ · resolved by replacing legacy dispatch/conductor vocabulary with canonical route/suggest/delegate/spawn/subagent-mechanism terms across current SKILL.md, capability prose, schemas, scripts, tests, docs/vocabulary.md, and the bundle-skill vocabulary authority, while preserving compatibility identifiers (commit message formats, code identifiers, historical records, path references) and updating focused regression coverage.
- ~~[fix:2.4.x] Rename all `skills/agentera/capabilities/*/prose.md` files to `instructions.md` and update references across `skills/agentera/SKILL.md`, `capability_schema_contract.yaml`, runtime descriptors, scripts, docs, tests, package metadata, and compatibility fixtures.~~ · resolved by moving all real capability instruction files to `instructions.md`, updating validators/loaders/descriptors/docs/tests/package-facing references, preserving classified historical compatibility records, and adding regression coverage that rejects new current `prose.md` conventions.
- ~~[feat:2.4.x] Add `first_invocation_read` capability metadata and surface required instruction reads through `agentera hej --format json --capability-context <name>` so agents can discover startup read obligations without raw directory guessing.~~ · resolved by emitting `source_contract.capability_context.first_invocation_read` for capability contexts, defaulting non-Planera capabilities to `full`, reporting Planera as `compact_startup`, and preserving runtime enforcement as false.
- ~~[test:2.4.x] Add regression coverage that rejects `fresh`, `stale`, `refresh`, `fixed`, `ready`, and `noop` as canonical app lifecycle status values while preserving allowed plain-language prose and non-app uses such as `agentera stats refresh`.~~ · resolved by deriving rejected lifecycle status values from the Decision 54 authority, checking doctor/hej/upgrade lifecycle metadata surfaces against canonical statuses, and preserving compatibility/plain-language/non-app uses including `agentera stats refresh`, workflow `noop`, and `fixed`/`ready to fix` display prose.
- ~~[test:2.4.x] Add vocabulary regression coverage that classifies every surviving `bundle` and `SKILL.md` occurrence as canonical, compatibility-preserved, historical, fixture-only, or generic, and fails new ambiguous user-facing usage.~~ · resolved by adding focused scan classifications to `references/cli/bundle-skill-vocabulary.yaml` and regression coverage in `tests/test_bundle_skill_vocabulary_authority.py` that fails unclassified surviving `bundle`/`SKILL.md` occurrences while preserving canonical, compatibility-preserved, historical, fixture-only, and generic usage.
- ~~[test:2.4.x] Add vocabulary regression coverage that rejects new ambiguous dispatcher/dispatch/router/conductor usage in user-facing and protocol prose while allowing qualified compatibility names such as `subagent_dispatch` and historical records.~~ · resolved by adding focused occurrence classifications to `references/cli/routing-execution-vocabulary.yaml` and regression coverage in `tests/test_routing_execution_vocabulary_authority.py` that fails unclassified dispatcher/dispatch/router/conductor wording while preserving canonical, compatibility, historical, fixture, code-identifier, and generic usage.
- ~~[test:2.4.x] Add validation and regression coverage proving capability directories require `instructions.md`, reject new `prose.md` conventions, emit required-read capability context, and preserve explicit compact-startup exceptions where supported.~~ · resolved by commit 9f8071a (`test(schemas): add instruction contract regressions`), which added validator/contract-loader/contract-structure and hej capability-context regressions; targeted selection passed 235 tests, capability-contract validation passed, and representative capability validations passed.
- ~~[chore:2.5.x] Clean up all CLI-backed helper scripts by making `agentera` the canonical documented/tested entry point, retaining direct script compatibility only as thin implementation or backwards-compatible maintainer seams for validate, verify, usage, lint, doctor, upgrade, setup, smoke, eval, migration, and compaction flows.~~ · resolved by documenting `agentera` as the canonical user-facing entry point, updating README/UPGRADE/AGENTS/SKILL validation examples to canonical `agentera validate` commands, and preserving direct helper scripts as classified maintainer/backward-compatible/internal seams.
- ~~[docs:2.5.x] Document the helper-script classification policy so internal registry/support modules stay private, corpus generation stays internal behind `agentera stats refresh`, maintainer-only generators and startup analyzers stay explicit, and user-facing workflows route through `agentera` namespaces first.~~ · resolved by the README Helper Script Classification Policy, covering user-facing workflows, backward-compatible maintainer seams, internal support modules, corpus generation behind `agentera stats refresh`, and maintainer-only local analysis/generator surfaces with regression coverage; install-root semantics stay owned by `scripts/install_root.py`.
- ~~[feat:2.5.x] Add `agentera stats` as the canonical user-facing usage analytics workflow: plain `stats` must not read local runtime history, missing or stale data should show `agentera stats refresh --dry-run`, `stats refresh --consent local-history` should build the internal corpus at `$PROFILERA_PROFILE_DIR/intermediate/corpus.json`, and no top-level `agentera corpus` command should be exposed.~~ · resolved by adding `agentera stats` over the existing internal corpus, `agentera stats refresh --dry-run` privacy diagnostics, consent-gated `stats refresh --consent local-history` corpus extraction to `$PROFILERA_PROFILE_DIR/intermediate/corpus.json`, and no top-level `agentera corpus` command.
- ~~[test:2.5.x] Cover `agentera stats` privacy and consent semantics: no runtime-history reads or writes from plain `stats` or refresh dry-runs, explicit `--consent local-history` required for extraction, bounded runtime diagnostics, and preserved `agentera usage` compatibility.~~ · resolved by usage namespace tests covering missing/stale stats guidance, existing-corpus stats JSON, refresh dry-run no-write privacy payloads, explicit consent enforcement, consented corpus creation, preserved `agentera usage`/direct script behavior, and absence of `agentera corpus`.
- ~~[feat:2.5.x] Extend `agentera validate` to cover the remaining stable validators: `validate_cross_capability.py`, `validate_lifecycle_adapters.py`, `validate_app_home_contract.py`, and capability contract self/protocol checks, while preserving direct helper compatibility.~~ · resolved by adding canonical `agentera validate cross-capability`, `lifecycle-adapters`, `app-home-contract`, and `capability-contract` wrappers with text/JSON output while preserving the direct helper scripts.
- ~~[feat:2.5.x] Package Agentera's OpenCode adapter as an npm-loadable plugin while preserving project-local `.opencode/plugins/agentera.js` install behavior.~~ · resolved by exposing `.opencode/plugins/agentera.js` through `.opencode/package.json` `main` and `exports`, recording `npm-loadable-plugin` package metadata, preserving the local plugin path, and keeping `.opencode/package.json` non-version-bearing.
- ~~[feat:2.5.x] Add OpenCode compaction context support through `experimental.session.compacting` using CLI-first Agentera state summaries instead of raw artifact reads.~~ · resolved by adding the OpenCode compaction hook, formatting bounded context from `agentera hej --format json`, and covering the hook in smoke/runtime adapter tests without direct raw artifact reads for compaction context.
- ~~[test:2.5.x] Add source-backed OpenCode plugin hook contract coverage for `chat.message`, `shell.env`, `event`, tool hooks, and compaction hooks using `@opencode-ai/plugin` types/source when public docs omit specific keys.~~ · resolved by validating Agentera's OpenCode hooks against `.opencode/node_modules/@opencode-ai/plugin/dist/index.d.ts` for `event`, `shell.env`, `chat.message`, `tool.execute.before`, `tool.execute.after`, and `experimental.session.compacting`, while preserving direct `session.created`/`session.idle` hook rejection.
- ~~[feat:2.5.x] Add optional Codex 0.130+ plugin-bundled hook support: expose a `.codex-plugin/plugin.json` `hooks` entry and a plugin-root-safe hook config that uses `PLUGIN_ROOT` instead of `AGENTERA_HOME`.~~ · resolved by adding `.codex-plugin/plugin.json` `hooks: ./hooks/codex-plugin-hooks.json` plus a PLUGIN_ROOT-safe Codex apply_patch hook config that invokes `hooks/validate_artifact.py` without AGENTERA_HOME.
- ~~[fix:2.5.x] Fix Codex copied user-hook wiring so hook commands do not depend on hook-time `AGENTERA_HOME` expansion; generate the resolved app-root validator path (`$AGENTERA_HOME/app/hooks/validate_artifact.py` or absolute equivalent), refresh matching `[hooks.state]` trust hashes, and add regression coverage proving Codex hook subprocesses are not assumed to inherit `[shell_environment_policy].set`.~~ · resolved by generating copied `~/.codex/hooks.json` content during upgrade with an exact resolved validator command, computing `[hooks.state]` hashes from that command, and covering local-clone and installed-app paths without hook-time AGENTERA_HOME inheritance.
- ~~[docs:2.5.x] Document Codex plugin hook enablement and trust flow: users must set `[features].plugin_hooks = true`, review plugin hooks in `/hooks`, and understand that copied `~/.codex/hooks.json` remains the default reliable install path.~~ · resolved by README, UPGRADE, and runtime parity docs describing optional Codex 0.130+ plugin hooks, required `[features].plugin_hooks = true`, deliberate `/hooks` review, and copied user hooks as the default reliable path.
- ~~[test:2.5.x] Add Codex plugin hook packaging regression coverage proving bundled hook metadata is discoverable for plugin install surfaces while `setup_codex.py` and `agentera upgrade` still preserve the existing copied user-hook path.~~ · resolved by package/runtime tests for `.codex-plugin` hook metadata, PLUGIN_ROOT-safe hook commands, generated copied user-hook content, exact trust hashes, descriptor preservation, and upgrade/setup compatibility.
- ~~[fix:2.5.0] Ensure explicit Agentera capability invocations such as `resonera ...` actually trigger the capability workflow instead of receiving a generic analysis response; add guidance or regression coverage so missed Resonera deliberation routing is caught.~~ · resolved by canonical capability-prefix direct routing: schema-backed routing now treats `resonera <topic>`, `planera <topic>`, and `orkestrera <topic>` as direct canonical routes; dispatcher/vocabulary guidance says `resonera <topic>` invokes ❈ resonera instead of generic analysis or the unsupported `agentera resonera` CLI command; focused regression tests cover the missed route.
- ~~[fix:2.5.0] Clarify Planera confirmation boundaries when creating a new plan over a completed existing plan; explicitly state whether archiving the completed plan is implicit in direct Planera invocation or requires pre-write confirmation.~~ · resolved by Planera prose and compact startup-context guidance: archiving an already complete existing plan before writing a direct-Planera replacement is implicit and needs no separate pre-write confirmation, human-initiated replacement plan approval still applies, and replacing/archiving active or incomplete plans still requires explicit confirmation.
- ~~[fix:2.5.0] Runtime docs compatibility fixes for OpenCode hook evidence, OpenCode agent descriptors, Codex descriptor paths, OpenCode lifecycle classification, and focused regression coverage.~~ · resolved by source-backed exact bare-`hej` routing coverage, documented OpenCode `description`/`mode: subagent` agent descriptors with body ownership markers, Codex descriptor defaults limited to `~/.codex/agents` or project `.codex/agents`, RuntimeAdapter session event status through the generic `event` hook, focused setup/upgrade/runtime-adapter/lifecycle/smoke gates, and synchronized PLAN/PROGRESS closeout without release side effects or protected-state edits.
- ~~[fix:2.5.0] Reconcile OpenCode bare-message plugin interception evidence.~~ · resolved by keeping exact bare `hej` routing through the source-backed `chat.message` Hooks interface, updating OpenCode smoke/runtime-adapter coverage, and recording the public-docs omission as a known documentation gap.
- ~~[feat:2.5.0] Cross-runtime capability subagent dispatch support for Orkestrera, Realisera, Optimera, Codex, OpenCode, runtime adapter docs, and descriptor validation.~~ · resolved by managed Codex `skills/agentera/agents/*.toml` descriptors, managed OpenCode `.opencode/agents/*.md` descriptors, `setup_codex.py` and upgrade descriptor installation, OpenCode plugin descriptor bootstrap, RuntimeAdapter `subagent_dispatch`, `agentera validate descriptors`, concrete dispatch prose, and passing focused/full verification without adding unsupported capability-name CLI commands.
- ~~[fix] Session artifact not committed or cleaned by any capability after, e.g., an orkestrera run, leaving it dangling and untracked~~ · superseded: session bookmarks now live under the Agentera data directory as runtime-local hook cache instead of `.agentera/session.yaml`, and `session_stop.py` no longer stages them for project commits.
- ~~[chore] Add advisory CONVENTION section to `skills/agentera/schemas/artifacts/todo.yaml` covering type-tag presence, version-suffix scoping, title-format consistency, and resolved-summary markers; wire as non-blocking lint warnings so agents get "tighten this" nudges without hard failures.~~ · resolved by commit 8f4a025: added TC1–TC5 advisory rules to `todo.yaml` and wired `CONVENTION` into `_SKIP_META` in `hooks/validate_artifact.py`.
- ~~[fix] Investigate, review, and improve `agentera doctor` output so a ready/healthy status does not always show a `Next:` section that can be interpreted as required action; distinguish optional repair/upgrade guidance from actual required follow-up, and fix command formatting issues such as missing whitespace before `--dry-run`.~~ · resolved by the doctor app-status vocabulary and ready-state output fix: doctor JSON now uses canonical app metadata statuses `up_to_date`, `outdated`, `repair_needed`, `migration_needed`, and `manual_review_needed`; up-to-date text renders `No action needed` without a required-looking `Next:` block or repair commands, actionable update/repair/migration states retain preview/apply/retry guidance, manual-review states block automatic repair, and focused regressions cover canonical vocabulary plus correct `--dry-run` command spacing.
- ~~Analyze session transcripts to identify instances where linting thresholds unnecessarily compress or rewrite relevant information and determine if the thresholds should be increased.~~ · resolved by the transcript/retained-artifact threshold analysis: retained `.agentera/archive` evidence showed repeated `post-audit-flagged` PLAN.md full-plan verbosity pressure, so `agentera lint --artifact <ARTIFACT> --file <PATH>` now uses full-file budgets while `--text` and stdin keep per-entry budgets. Redacted scan/classification helpers preserve aggregate-only evidence and focused tests cover retained PLAN.md pressure, unflagged TODO.md inconclusive pressure, transcript scan privacy, and file-versus-text lint semantics.
- ~~[perf] Reduce AGENTERA_ARTIFACTS raw reads after CLI (4 reads): agents don't know where artifacts live — improve artifact path discovery through `agentera describe` or hej composite output.~~ · resolved by the artifact-location discovery contract: `agentera describe --format json` now exposes `artifact_locations` and schema-entry `location` records, while `agentera query --list-artifacts --format json` returns a structured v2 wrapper with mapped paths, normal read commands, raw-access boundaries, source contract, and caveats. Plain list output remains names-only; focused/relevant tests passed and the closeout records retained benchmark rows plus the no-live-benchmark caveat.
- ~~[perf] Reduce DECISIONS.md raw reads after CLI (2 reads, 24 redundant tokens): ensure `agentera decisions --format json` source_contract makes compacted/missing context boundaries unambiguous.~~ · resolved by the decision source-contract raw-read reduction: `agentera decisions --format json` now separates full-detail completeness from `complete_for_normal_deliberation_context`, exposes a decision context truth table, distinct compacted/missing/filter/satisfaction/missing-artifact boundaries, raw artifact access boundaries, and fallback policy; DECISIONS.md consumers now use returned entries plus `missing_fields`, `compacted`, `caveats`, and `satisfaction.review_needed` instead of raw-reading missing historical context. Focused and full query tests passed, direct source-contract proof showed current compacted decisions are normal-deliberation complete without full-detail overclaim, and refreshed `mage bench:startupState` reported zero raw-after-CLI and zero redundant raw artifact reads with degraded runtime coverage caveats recorded separately.
- ~~[perf] Reduce planera prose re-reads (5 reads in latest benchmark): improve planera routing/guidance discoverability so agents don't re-read instructions.md on every invocation.~~ · resolved by the Planera compact startup contract: `agentera hej --format json --capability-context planera` now emits `source_contract.capability_context.startup_contract` with bounded planning levels, step markers, CLI-first orientation, raw plan artifact boundaries, full-plan review/self-audit requirements, handoff expectations, prose authority exceptions, selected-seam rationale, and unsupported-command boundary. Focused tests prove the contract shape, `agentera planera` remains unsupported, and the startup context is available even when `planera/instructions.md` is absent from the installed app surface; refreshed `mage bench:startupState` evidence reports `capability_prose_read_counts.planera=0` with degraded runtime coverage caveats separated from product behavior.
- ~~[perf] Reduce PLAN.md raw reads after CLI (7 reads, 37 redundant tokens in latest benchmark): strengthen `agentera plan --format json` source_contract completeness signal so planera agents skip raw plan.yaml reads.~~ · resolved by the plan source-contract raw-read reduction: `agentera plan --format json` now exposes canonical/persisted plan identity, `complete_for_normal_startup_evaluation`, complete summary/entry field coverage, explicit raw-artifact boundaries, and fallback policy; Planera and master Agentera guidance now require complete CLI plan state before any defensive raw `.agentera/plan.yaml` read while preserving raw access for writes, archives, validation, corruption diagnostics, and incomplete CLI state. Focused tests passed and `mage bench:startupState` produced direct retrospective measurement (`PLAN.md` raw-after-CLI 11, redundant 10, recommendation `plan_cli_startup_envelope`) with degraded runtime caveats recorded separately.
- ~~[feat] Surface non-confirmed decisions (open, provisionally satisfied, review-needed) in `agentera hej` dashboard attention items so stale unreviewed decisions don't hide silently in the archive.~~ · resolved by the decision-review attention feature: `agentera hej` now emits one bounded decision satisfaction review attention item plus structured `decision_attention` JSON derived from the same decision satisfaction context as `agentera decisions --format json`, includes missing/open/provisional/unconfirmed/explicit review-needed decisions, excludes explicitly user-confirmed satisfied decisions, preserves `next_action` priority, and leaves `.agentera/decisions.yaml` unchanged; focused verification passed with hej/decision regression tests, guidance tests, package/version metadata tests, touched artifact validators, compaction check/fix as needed, and `git diff --check`, without changing decision satisfaction state, adding a new command, publishing, pushing, tagging, refreshing installed apps/profiles, or editing vision/objective state.
- ~~[feat] Implement `agentera corpus` or `agentera usage` CLI namespace for profile analytics (D48).~~ · resolved by the D48 usage namespace feature: `agentera usage` now exposes existing `scripts/usage_stats.py` analytics through the supported CLI with text report generation, JSON pass-through, `--corpus`, `--project`, missing-corpus guidance, invalid-format guidance, help discoverability, isolated write-path tests, and preserved direct script compatibility; focused verification passed with usage namespace tests, direct usage stats tests, explicit CLI surface tests, package/version metadata tests, touched artifact validators, and `git diff --check`, without implementing `agentera corpus`, auto-building corpus data, refreshing profiles or installed apps, publishing, pushing, tagging, editing vision/objective state, changing decision satisfaction, or doing unrelated D48 validate/verify work.
- ~~[feat] Implement `agentera smoke` or `agentera verify` CLI namespace for eval and smoke gates (D48).~~ · resolved by the D48 verify namespace feature: `agentera verify` now exposes bounded smoke and eval gate families with text/JSON results, safe offline/dry-run defaults, invalid-input guidance, and preserved direct smoke/eval script compatibility; focused verification passed with verify namespace tests, explicit CLI surface tests, package/version metadata tests, safe verify probes, direct smoke/eval probes, touched artifact validators, and `git diff --check`, while `node scripts/smoke_opencode_bootstrap.mjs` retained the expected bounded local failure for absent OpenCode skills, without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, package-manager update, unsupported capability-name CLI command, or D48 validate/corpus/usage work.
- ~~[feat] Implement `agentera validate` CLI namespace for capability/artifact validators (D48).~~ · resolved by the D48 validate namespace feature: `agentera validate capability` and `agentera validate artifact` now provide stable text/JSON validation wrappers over the existing direct validators while preserving artifact labels, capability names, direct script/hook behavior, and invalid-input guidance; focused verification passed with validate namespace tests, CLI/package metadata tests, direct capability and artifact validators, touched artifact validators, and `git diff --check`, without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, package-manager update, unsupported capability-name CLI command, or D48 smoke/verify/corpus/usage namespace work.
- ~~[fix] Investigate `agentera lint --artifact` false-positive advisory for Planera plans: `/tmp/opencode/aila-m13-plan.yaml` still reported `abstraction creep: no concrete anchor` after adding explicit anchors to `ROADMAP.md:523-542`, the queued-message fixture, semantic snapshots, PTY smoke, `mage check`, source docs, and concrete `internal/runtime`, `internal/app`, and `internal/tui` boundaries.~~ · resolved by the Planera lint anchor patch: `scripts/self_audit.py` now recognizes repo-style path anchors such as `internal/runtime`, `internal/app`, and `internal/tui`; `tests/test_self_audit.py` covers anchored PLAN.md pass and unanchored generic-prose advisory failure cases; focused verification passed with `uv run pytest tests/test_self_audit.py -q` (23 tests), lint contract probes for anchored/generic/strict/file/stdin/text modes, package/version metadata tests, artifact validators, and `git diff --check`, without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, or unsupported capability-name CLI command.
- ~~[chore] Audit codebase for leftover "bundle" terminology and legacy artifact names (e.g. DECISIONS.md) per D47 app-home migration — replace with app-home vocabulary.~~ · resolved by the D47 app-home vocabulary cleanup: inventory classified required terms and surfaces, live app-home vocabulary was updated across current docs/guidance/diagnostics/package text while preserving compatibility identifiers and historical records, focused verification reported 346 passing tests, and Task 4 closeout recorded metadata without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, or unsupported capability-name CLI command.
- ~~[fix] \`agentera state todo\` returns zero open entries despite TODO.md containing many open items across all severity levels. \`agentera state todo --format json\` reports \`open_count: 0\` and \`entries: []\` while TODO.md has 7 critical, 13 degraded, 27 normal, and 2 annoying open entries. The TODO.md parser in \`scripts/agentera\` fails to extract entries from the current file format (severity-sectioned Markdown with \`- [type]\` items). Fix: debug the parser to handle the actual TODO.md format and add regression coverage.~~ · resolved by 187e7b1
- ~~[fix] \`review_needed\` is stored inside satisfaction maps in \`.agentera/decisions.yaml\` despite the schema (\`decisions.yaml:229\`) declaring *"review_needed is derived from missing or stale protected state, not stored as a fourth state."* The validator never checks for unknown fields in nested satisfaction maps.~~ · resolved by bf9d83b
- ~~[fix] \`agentera validate\` subparser definition duplicated: \`_add_validate_family_parsers()\` at \`scripts/agentera:7602-7651\` defines the parser used by \`check validate\`, but the standalone \`agentera validate <family>\` at lines 8074-8140 has its own inline copy. Adding a family to one won't wire it into the other.~~ · resolved by 18598f3
- ~~[fix] \`agentera.js\` \`validateArtifactCandidate()\` parses stdout as JSON from a script that never writes JSON to stdout (\`.opencode/plugins/agentera.js:564-576\`). \`HookCliAdapter.run()\` writes violations to stderr only. The deny decision works accidentally via the \`err.status === 2\` exit-code check in the catch block. Fragile.~~ · resolved by d5192c1
- ~~[fix] Planera schema enforces deprecated checkpoint vocabulary (\`skills/agentera/capabilities/planera/schemas/validation.yaml:34-41\`), but \`references/cli/vocabulary-index.yaml:99-100\` explicitly deprecates it in favor of "final state sync" / "plan-level current-state check." Planera agents will perpetuate deprecated terminology in their output.~~ · resolved by d5192c1
- ~~[fix] Triple-duplicated artifact path mappings: \`DEFAULT_ARTIFACT_PATHS\` defined identically in \`validate_artifact.py:47-57\`, \`compaction.py:332-342\`, and \`common.py:24-34\`. Updating one diverges silently from the others. Consolidate into \`common.py\`.~~ · resolved by d5192c1
- ~~[fix] \`_SKIP_META\` groups differ between validator and CLI: \`hooks/validate_artifact.py:188\` skips \`CONVENTION\` (singular) but not \`ARCHIVE\`; \`scripts/agentera:332\` and \`:4270\` skip \`ARCHIVE\` but not \`CONVENTION\` (singular). Asymmetric skip lists mean some schema groups are processed by one code path but not the other. \`docs.yaml\` uses \`CONVENTIONS\` (plural) which matches neither. Consolidate into a single shared skip list.~~ · resolved by bf9d83b
- ~~[fix] \`doctor\` argument parser uses \`--json\` flag instead of the \`--format json\` convention every other structured-output command uses (\`scripts/agentera:8242-8246\`). README.md:312, internal help string (\`:244\`), and every other command use \`--format json\`; only \`doctor\` uses \`--json\` (store_true boolean). Fix: replace \`--json\` with \`--format\` choices matching \`prime\`/\`state\`/\`check\`.~~ · resolved by 18598f3

- ~~[fix] `_is_empty_required` (`hooks/validate_artifact.py:238-245`) treats `0` and `False` as non-empty (falls through to `return False`). For a required `number` field, `number: 0` passes even though the schema says "Must be a positive integer."~~ · resolved by rejecting boolean values and verifying type and constraint checks in validation.
- ~~[fix] Distinguish missing (`None`) from wrong-type in `user_confirmation` mapping at `hooks/validate_artifact.py:581-582` to emit detailed type error instead of saying it's required.~~ · resolved by checking exact types for validation fields.
- ~~[test] No coverage for `user_confirmation` typed as string/int/list/null in validation. All fixtures use proper dict form. The TODO.md string-vs-dict mismatch is untested (`hooks/validate_artifact.py:580-582`, `hooks/compaction.py:572`).~~ · resolved by adding negative type checks and wrong-type validation tests.
- ~~[test] No coverage for the `validate_artifact.py` exception handler `sys.exit(0)` on crash (line 910). A crashing hook produces exit code 0 silently; no regression test guards this.~~ · resolved by updating crash code to exit 2 and adding a regression test.
- ~~[test] No coverage for empty schema file bypassing all validation. No test creates an empty schema file and confirms violations are zero despite invalid artifact data.~~ · resolved by reporting violation when schema is empty.
- ~~[test] No negative type testing across validators: string-where-dict-expected, int-where-string-expected. No test verifies error messages differ from "missing field" errors.~~ · resolved by validating nested field types.
- ~~[test] No coverage for objective `VALIDATION_RULES` group being silently ignored. No test verifies that OVR1-OVR3 rules produce violations when objective data violates them.~~ · resolved by iterating rules group and validating `closure_consistency`.
