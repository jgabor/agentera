# TODO

## ⇶ Critical

## ⇉ Degraded

- [ ] [fix:3.0.0] **CLI `prime --context <capability>` returns deprecated fallback commands.** The `fallback_commands` array in the `prime --context resonera` JSON response includes `agentera query vision --format json`, `agentera objective --format json`, and `agentera todo --format json` — all of which emit stderr deprecation warnings (`use agentera state query`, `use agentera state objective`, `use agentera state todo`). The fallback_commands should use the non-deprecated `agentera state *` forms so agents don't emit deprecation noise on every fallback invocation.
- [ ] [fix:3.0.0] Audit capability prose for language that invites over-prescription. Words and phrases like "principles", "applicability", "key concepts", "design", "recommended" can drift into decisional content when an agent treats them as invitations to fill in. Audit each capability's instructions.md and tighten the scope-constraining language. The most prone to over-prescription: inspirera's "Key Concepts" and "Applicability Matrix" sections (scored recommendations drift in), visionera's `principles` field (implementation details drift in), planera's `design` field (architecture decisions drift in). (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [feat:3.0.0] Add a pre-write self-audit step to each capability that flags decisional language in descriptive artifacts. The current `agentera check lint` checks verbosity and abstraction-creep anchors but not the descriptive/decisional distinction. Extend the lint to detect prescriptive markers ("should", "must", "will", "adopt", "recommend") in artifacts that should be descriptive, and warn before the artifact is committed. (Branch: `feat/v3` — implement on `feat/v3`.)

## → Normal

- [ ] [feat:3.0.0] Extend planera's light/full mode to light/normal/full, then apply the same 3-tier scheme to the other 11 capabilities. Match the "Skip / Light / Full" plan-level taxonomy (PROFILE.md §3).
- [ ] [feat:3.0.0] Detect "You're right", "my mistake", "You're right to push back" responses during profiling, to add generalized rules to the profile to correct the model's behavior.
- [ ] [docs:3.0.0] Conduct a thorough QA audit and review of all capability instructions, SKILL.md, docs, etc.
- [ ] [feat:3.0.0] Add `statusline` command to show data such as current plan status, health, etc. that can be used by runtimes, similar to `https://github.com/ryoppippi/ccusage`, `https://github.com/avinashbest/cli-status-bar`, and `https://cursor.com/changelog/04-14-26`
- [ ] [feat:3.0.0] Decide extending visionera create/refine and `agentera prime --context visionera` to treat identity voice and emotional_register nuance as first-class attributes, or introduce a personality artifact ("SOUL.md") for agentera identity and personality, next to user's PROFILE.md.
- [ ] [chore:3.0.0] Implement Decision 70 capability rename on feat/v3 — rename capability dirs to English IDs; flip capability_schema_contract; update runtime descriptors, SKILL.md dispatch, tests, AGENTS.md scopes, coexistence probe naming line; promote CHANGELOG past tense when merged.
- [ ] [chore:3.0.0] Replace scattered `type Dict = Record<string, any>` aliases across `packages/cli` with a shared typed JSON/value module; narrow casts at IO boundaries only (T1 landed `core/jsonValue.ts` and prime/doctor/orientation contracts; repo-wide Dict elimination deferred).
- [ ] [chore:3.0.0] Add macOS and Windows GitHub Actions jobs for `pnpm -C packages/cli test` (and related gates); CLI tests are cross-platform-hardened on darwin and linux.
- [ ] [chore:3.0.0] Decide `@agentera/mobile` npm publish identity (private workspace vs npm vs deploy-only).
- [ ] [chore:3.0.0] Remove `packages/mobile` temporary `vite.config.ts` hook plumbing when SvelteKit scaffold lands (see `packages/mobile/TODO.md`; scaffold tracked there, not duplicated here).
- [ ] [docs:3.0.0] Decide long-term editor skill-bundle role (coexist vs eventual sunset) — horizon item, not blocking mobile v1.

## ⇢ Annoying

- [ ] [chore:3.0.0] Re-derive `first:` dates and confidence numbers from the current corpus on every profile write, instead of carrying them forward from the prior profile. The adversarial review in `ses_1406cbc95ffeZ8WRcSvUag387G` found 25+ entries where `first:` predated the corpus start (`first:2026-02-08` against a 2026-02-14 corpus) and several where the confidence number reflected a prior rebuild's evidence. The profilera writer must mark inherited entries explicitly (e.g., `first:2026-02-08 inherited:2026-05-30`) and demote confidence to the maximum of "re-evidenced in current corpus" and "carried forward" rather than silently blending them.

## ✓ Resolved

- [x] [chore:3.0.0] `validate_artifact.py:818` rejects every Edit/Write to TODO.md when `## ⇶ Critical` or `## ⇉ Degraded` are empty, preventing the fix from being applied. Move the check to commit-time, or populate those sections with real items. · resolved 2026-06-22 @ main: Removed the empty-severity-section check from `hooks/validate_artifact.py` on `main` (7-line removal). The v3 TS rewrite (`packages/cli/src/hooks/validateArtifact/markdown.ts`) already dropped this check; this backports the fix to the v2 Python hook. Test updated from "flags adjacent empty section" to "accepts empty severity section".
- [x] [fix:3.0.0] **resonera/instructions.md YAML schema example is stale and causes write failures.** The `decisions.yaml` example at line 43-62 shows `tradeoffs:` (should be `description:`), `chosen: true/false` (should be `status: chosen/rejected`), and `date: YYYY-MM-DD` unquoted (schema requires a string; YAML parses unquoted dates as `date` type, not `string`, causing `'date' must be a string, got date` validation error). An agent following the instructions.md example produces entries that fail schema validation on write. Fix: update the example to match the actual schema used by all existing decisions in the wild (`status: chosen`, `description:`, `date: "YYYY-MM-DD"` quoted). Observed in ktx D13 deliberation: first edit attempt to `.agentera/decisions.yaml` was rejected with `missing required field 'alternatives[N].status'` and `'date' must be a string, got date`. · resolved 2026-06-22: Updated YAML example in `packages/cli/src/capabilities/resonera/instructions.ts` to use `status: chosen/rejected` (DA3), `description:` (DA4), quoted `date: "YYYY-MM-DD"` (DC2); removed non-schema `tradeoffs` and `win_condition` fields; compatibility rule text updated to reference `name`, `status`, `description`.
- [x] [fix:3.0.0] **resonera/instructions.md references deprecated `agentera lint` command.** Line 235 says `agentera lint --artifact <ARTIFACT>` but the CLI emits `Deprecation: agentera lint is deprecated; use agentera check lint`. Update to `agentera check lint --artifact <ARTIFACT>`. · resolved 2026-06-22: Updated pre-write self-audit instruction to `agentera check lint --artifact <ARTIFACT>`.
- [x] [feat:3.0.0] Add a "Coverage Audit" pre-extraction step to the profilera capability. Before any extraction, enumerate *available* runtimes (codex, claude-code, cursor, cursor-agent, opencode, github-copilot) with each store's earliest/latest session timestamp, then name the runtimes being *skipped* and the reason. The first user-visible output of a Full-mode profilera run must be a coverage summary; if any available runtime is skipped, the run is `flagged` (EX2) until the user explicitly accepts the gap. Closes the data-loss path where the LLM in `ses_142098f9fffeE9sNvugn0l0Aku` ran `extract_corpus.py --no-claude --no-codex --no-copilot` and treated that as a "scoped" corpus instead of coverage loss against the 2026-05-30 prior rebuild. · resolved 2026-06-18: `coverageAudit.ts` emits pre-extraction summary; `extractCorpusMain` gates EX2 until `--accept-coverage-gap`; profilera instructions and exit.yaml updated.
- [x] [fix:3.0.0] Surface a coverage-loss warning in the corpus envelope and hej/prime consumers when an available runtime is skipped, capped, or truncated. Add `available_runtimes`, `selected_runtimes`, and `available_but_not_selected` arrays to the corpus envelope; require profilera Full-mode and the hej dashboard to surface a non-empty `available_but_not_selected` as `flagged` (EX2). · resolved 2026-06-18: `corpus.ts` envelope arrays; `corpusCoverage.ts` and `attention.ts` surface EX2 in hej/prime.
- [x] [fix:3.0.0] Lift the hardcoded `MAX_SQLITE_SESSIONS = 60` and `MAX_SQLITE_ROWS = 100_000` caps in extractCorpus. Expose `--max-sqlite-sessions N` / `--max-sqlite-rows N` CLI flags and `AGENTERA_EXTRACT_MAX_SQLITE_*` env-var overrides; emit `runtime_statuses[].truncated_at` from uncapped probe queries. · resolved 2026-06-18: `sqliteCaps.ts`, CLI/env overrides, stderr truncation warnings.
- [x] [chore:3.0.0] Eliminate the Python/TypeScript extractor drift. The bundle workflow must compile the TS extractor into the only Python-visible surface, or fail loudly; add a `parityCheck` test on a seeded opencode.db. · resolved 2026-06-18: `extractCorpusParity.ts`, `generate-extract-corpus-parity.mjs`, `copy-bundle.mjs` drift gate, `extractCorpusParity.test.ts` parityCheck.
- [x] [refactor:3.0.0] Rename `runtime_statuses[*].candidate_count` to `file_count` in extractCorpus discovery metadata; add regression test that SQLite stores expose `file_count` alongside `record_count`. · resolved 2026-06-18: `core.ts` and parity manifest use `file_count`; extractCorpus regression test.
- [x] [docs:3.0.0] Document the `extract --no-<runtime>` flags in `cli/help.ts` with deselection wording and Coverage Audit cross-reference. · resolved 2026-06-18: `help.ts` per-flag docs; `help.test.ts` regression.
- [x] [chore:3.0.0] Split `packages/cli/src/cli/commands/prime.ts` (~721 lines) — extract v1 artifact detection, app/doctor status, and orientation field assembly into focused modules · resolved 2026-06-10: `cli/commands/prime/{v1Migration,bundleStatus,collectOrientationState,orientationOutput,types}.ts`; `prime.ts` thin `cmdPrime` dispatch (78 lines).
- [x] [chore:3.0.0] Share TODO severity ordering (`TODO_SEVERITY_ORDER`) between `cli/commands/prime.ts` and `cli/orientation.ts`; align with `state/todo.ts` key list · resolved 2026-06-10: `cli/todoSeverity.ts` exports `TODO_SEVERITY_ORDER_KEYS` and `TODO_SEVERITY_ORDER`; prime, orientation, and `state/todo` import it.
- [x] [refactor:3.0.0] Refactor `summarizeProjectIntegration` upgrade-scenario booleans into an explicit decision table to simplify `artifactsOnly` / `runtimeOnly` / `crossMajor` branching · resolved 2026-06-10: `upgrade/projectIntegrationDecision.ts` typed scenario classifier and message table; `summarizeProjectIntegration` delegates; fixture coverage per scenario in `projectIntegrationWording.test.ts`.
- [x] [refactor:3.0.0] Unify prime upgrade attention — single source for `projectIntegrationAttention`, `appStatusAttention`, and `crossMajorAppHomeAttention` branching in `collectOrientationState` · resolved 2026-06-10: `collectOrientationState` emits upgrade attention only via `projectIntegrationAttention`; removed `appStatusAttention` and `crossMajorAppHomeAttention` forks; `primeAppWording.test.ts` asserts canonical integration narrative.
- [x] [chore:3.0.0] Extract shared npx platform status resolver — `prime.ts` `hejBundleStatus` and `projectIntegration.ts` `resolveIntegrationTargets` both call `buildDoctorStatus` on `resolvePlatformAppHome`; unify the pattern · resolved 2026-06-10: `upgrade/npxPlatformStatus.ts` exports `resolveNpxPlatformStatus`; prime and projectIntegration share it.
- [x] [refactor:3.0.0] Refactor `buildDoctorStatus` root-classification ladder into a typed classifier (`kind` → signals/status) to shrink the nested if-chain in `upgrade/doctor.ts` · resolved 2026-06-10: `upgrade/doctorClassifier.ts` typed `ROOT_CLASSIFIERS` map; `buildDoctorStatus` delegates to `classifyInstallRootStatus`.
- [x] [chore:3.0.0] Split `packages/cli/src/validate/lifecycleAdapters.ts` (~829 lines) by runtime adapter family before it re-hits the 1000-line monolith gate · resolved 2026-06-10: `lifecycleAdapters/shared.ts`, `nodeFormChecks.ts`, and `legacyPythonParity.ts`; barrel stays under monolith gate.
- [x] [chore:3.0.0] Split `packages/cli/src/validate/lifecycleAdapters.ts` node-form adapter checks from legacy Python parity (`validatePackagedPythonScripts`, `validateUvRuntime`); gate Python probes off the default npm v3 path · resolved 2026-06-10: default validate path skips probes; `--legacy-python-parity` or `AGENTERA_LEGACY_PYTHON_PARITY=1` enables maintainer parity.
- [x] [chore:3.0.0] Extract a shared dispatch argv parser from `cli/dispatch/{check,state,lifecycle}.ts` and retire duplicated hand-rolled flag loops · resolved 2026-06-10: `cli/dispatch/argvParser.ts` with `makeArgvValueReader`/`readArgvFlag`; check, state, lifecycle, and prime dispatch delegate.
- [x] [fix:3.0.0] Stop silently swallowing upgrade flags in `dispatch/lifecycle` `runUpgrade` (`--runtime`, `--opencode-config-dir`, `--update-packages`); reject unknown values or wire them through the orchestrator · resolved 2026-06-10: `rejectUnsupportedUpgradeFlag` returns explicit guidance for all three flags; regression in `hook.test.ts`.
- [x] [fix:3.0.0] Wire `doctor.ts` `defaultProbe` to the bundled TS CLI (or remove the stub) so `probeCli=true` does not always report "CLI probe not yet wired" · resolved 2026-06-10: `upgrade/cliProbe.ts` exports `inProcessProbe`; `buildDoctorStatus` defaults to it.
- [x] [chore:3.0.0] Consolidate npx-bundle detection — use `isNpxBundleRoot` from `core/sourceRoot.ts` everywhere; remove `isNpxBundle` duplicate in `cli/appContext.ts` · resolved 2026-06-10.
- [x] [chore:3.0.0] Consolidate Python-parity JSON helpers (`core/pyjson.ts`, `doctor.ts` `pyJsonIndentSorted`, `startupAnalysis/helpers.ts` `pyJsonDumps`, `startupAnalysis/report.ts` `pyJsonIndent`, `setup/doctor/report.ts` `pyJsonIndent`) into one module · resolved 2026-06-10: extended `core/pyjson.ts` with `pyJsonIndentSorted`; call sites updated; thin wrappers remain where scalar hooks differ.
- [x] [chore:3.0.0] Deduplicate v1 artifact detection — `prime.ts` imports `detectV1ArtifactPairs` from `migrateArtifactsV2ToV3.ts`; local `V1_ARTIFACT_PAIRS` removed · resolved 2026-06-10.
- [x] [chore:3.0.0] Deduplicate `installRoot` default app-home paths — `defaultAppHome` delegates to `linuxDefaultAppHome` / `macosDefaultAppHome` / `windowsDefaultAppHome` · resolved 2026-06-10.
- [x] [refactor:3.0.0] Introduce typed `BundleStatus` / `OrientationState` in `prime.ts` (replace `Dict` return shapes from `hejBundleStatus` and `collectOrientationState`) · resolved 2026-06-09: `packages/cli/src/core/jsonValue.ts`, `cli/contracts/bundleStatus.ts`, `cli/contracts/orientationState.ts`; `buildDoctorStatus`/`publicDoctorStatus`, `collectOrientationState`, and orientation summary exports use named types at module boundaries; prime/doctor/orientation test suites green.
- [x] [fix:3.0.0] Fix so that upgrades are not presented as repairs during `prime` or `doctor` · resolved 2026-06-09 @ 3.0.0-dev.6: `appLifecycleActionNoun` / `appLifecycleApprovalPhrase` in `upgrade/doctor.ts`; `managedStaleDiagnosticMessage` splits `version_mismatch`→update vs repair; `projectIntegration` and `prime.appStatusAttention` derive verbs from lifecycle status; `skills/agentera/SKILL.md` adds User-facing update wording; regression tests in `installRoot.test.ts`, `doctor.test.ts`, `projectIntegrationWording.test.ts`, and `primeAppWording.test.ts`; `check validate app-home-contract` pass.
- [x] [docs:3.0.0] Decide on Swedish -era verbs or standardizing on plain English (e.g., `agentera optimera` vs `agentera optimize`) · resolved 2026-06-09: Decision 70 — promote D43 English aliases to canonical v3 IDs; v2 `@latest` keeps Swedish; closeout deliberation pinned implementation details.
- [x] [fix:3.0.0] `agentera state todo --format json` ignores GitHub task-list `[x]`/`[ ]` checkboxes before Agentera `[type]` tags · resolved 2026-05-24 @ 2.7.9: shared markdown parser classifies `- [x] [type] …` as resolved and excludes those rows from open issue counts and hej `next_action`; `- [ ] [type] …` and type-only bullets stay open. Regression coverage in `packages/cli/test/cli/todoMarkdown.test.ts`, `state.test.ts`, `orientation.test.ts`, and `tests/test_query_cli.py::TestTodo::test_github_checkbox_before_type_tag_json`.
- [x] [fix:3.0.0] TODO.md severity-section body extraction is off-by-one in `validateMdItems` · resolved 2026-05-22 @ 2.7.8: slice severity bodies with `/\n##\s/` boundaries from the heading end, skip the trailing newline before list items; regression coverage in `packages/cli/test/hooks/validateArtifact.test.ts` and `tests/test_validate_md_items.py`.
- [x] [fix:3.0.0] npm 0.0.0 shim delegates to the Python CLI with cwd set to the installed app home, so every project-state read resolves to `$AGENTERA_HOME/app/...` instead of the user's project; preserve the user's working directory in the shim's runBackend app-home path. · resolved by packages/cli/shim/lib/exec.mjs runBackend app-home branch now passing the caller's cwd through dispatch → runBackend to spawnSync; regression coverage in packages/cli/test/shim/runBackend.test.ts pins user cwd vs. app home and asserts the repo backend is still anchored to its repo root; CHANGELOG [Unreleased] records the shim's transitional role against the 3.0 TypeScript CLI.
- [x] [feat:3.0.0] Fix so that TODO.md items use Markdown `- [ ]/- [x]` checkboxes instead of `~~` strikethrough formatting. · resolved on `feat/v3` @ e04f78fb: converted all TODO list items from `~~` strikethrough to GitHub task-list `- [ ]`/`- [x]` checkboxes; aligned todo.yaml TC5, contract.md, compaction dryRun, and packages/mobile/TODO.md to the same checkbox convention.
- [x] [docs:3.0.0] Document CLI ↔ mobile state integration options in `docs/consolidation/mobile-open-decisions.md` · resolved on `feat/v3` (branch cleanup pass): three options (CLI subprocess, embedded TS, hybrid) with tradeoffs documented; no integration choice recorded.
- [x] [test:3.0.0] v2→v3 migration test pollution guard · migration tests must use `sandboxMigrationEnv` / `migrationCtx` (no `process.env` spread) so `opencodeConfigDir` stays under the temp home; `applyRuntimeMigrationItem` `link-skill` must `lstat`+`unlink` dangling symlinks before `symlinkSync`.
- [x] [chore:3.0.0] Lefthook pre-commit bypass and brittle inspektera TODO.md oracle · resolved on `feat/v3` @ b3900f4 and `main` @ fc87e6e: citation-anchor fixture decouples evaluation-report WARN rows from the project ledger; `verifyWarnCitationAtLine` checks cited line text; `scripts/precommit-vitest.sh` / `scripts/precommit-pytest.sh` run staged-aware smoke tests; hooks verified green without `LEFTHOOK=0`.
- [x] [feat:3.0.0] Rewrite the supported Agentera CLI core in TypeScript for Bun/npm/single-binary distribution, gated by contract-first parity with the current Python `agentera` CLI rather than a clean-slate command redesign. (Branch: `feat/v3` — implement on `feat/v3`.) · resolved on `feat/v3` @ 618c8e7 (T1 merge): shipped the v3 Bun/npm/single-binary distribution contract — `docs/packaging/v3-packaging.md` design, `packages/cli/test/packaging/prepack.test.ts` regression, `scripts/single-binary.sh` build entrypoint, `.gitignore` lift for `docs/packaging/`, and release-metadata contract at `3.0.0-dev.5` for `@next`; npx @next parity holds.
- [x] [chore:3.0.0] Design the Bun/npm packaging plan for Agentera 3.0, including single-binary delivery, npm package layout, app-home migration from Python-managed installs, and preserved direct-helper compatibility boundaries where still needed. (Branch: `feat/v3` — implement on `feat/v3`.) · resolved on `feat/v3` @ 618c8e7 (T1 merge, same commit as the TS-rewrite fold above; the Bun/npm packaging plan is the implementation of this TODO).
- [x] [chore:3.0.0] Enforce Decision 58's single-name protocol boundary: remove every backwards-compatible, deprecated, parallel, display-only, or storage/protocol split name for artifacts, concepts, primitives, command surfaces, schema fields, metadata fields, runtime adapter surfaces, and protocol identities. Zero post-3.0 surfaces may retain legacy aliases or multiple names; old names must be removed or migrated, not supported alongside the canonical name. (Branch: `feat/v3` — implement on `feat/v3`.) · resolved on `feat/v3` @ 02a16e1 (T3): `packages/cli/src/registries/artifactProtocolIds.ts` and `references/cli/single-name-protocol.yaml` enforce the single-name protocol boundary; `references/cli/vocabulary.md` and `references/analysis/startup-measurement-contract.yaml` updated to canonical names; capability-contract validate and npmParityMatrix 14/14 green.
- [x] [chore:3.0.0] Remove v1 legacy cruft after the 3.0 compatibility boundary · resolved on `feat/t4-remove-v1-cruft` @ 2dcc2cc: deleted `skills/hej/` bridge, `references/v1-section-mapping.md`, `.opencode/commands/hej.md`, marketplace/codex hej plugin entries, and retired bundle-skill vocabulary bridge concepts; kept `migrateArtifactsV1ToV2.ts` for Markdown→YAML migration; `v1LegacyCruft.test.ts` (+10 cases); vitest 822/822; npmParityMatrix 14/14.
- [x] [chore:3.0.0] TS monolith hotspots persist after the v2→v3 rewrite.?lint gate when a source file exceeds the threshold Branch: `feat/v3` — implement on `feat/v3` · resolved on `feat/v3` by 8 splits: validateArtifact (1c6f5154), compaction (07ee505a), state (cca8d2a5), dispatch (15e4e2c4), capabilityContext (28ec13fb), extractCorpus (ef52335d), startupAnalysis (747cd279), doctor (4b88dc25), codex (c45a6b1e); plus `packages/cli/test/lint/monolithLint.test.ts` regression gates any new file over 1000 lines. Largest file after split: 528 lines.
- [x] [fix:3.0.0] npm `@next` version 3.0.0-dev.4 diverges from `suiteVersion` 2.7.6 in `packages/cli/package.json`; installed-app expectations, docs, and npm publish metadata can disagree until release alignment lands. Coordinate a release-metadata pass that pins `version`, `suiteVersion`, and `gitRef` to the same value for the active @next tag, keep stable channel on 2.x until explicit retirement, and add a release-metadata parity regression that fails the doctor/upgrade gate when channel versions disagree across package metadata, install-root manifests, and CLI surface reports. (Branch: `feat/v3` — implement on `feat/v3`.) · resolved on `feat/v3`: `packages/cli/src/release/releaseMetadata.ts` reads `registry.json` `skills[0].version` as the authoritative source of truth and asserts (a) `packages/cli/package.json` top-level `version` shares its major.minor.patch with the registry version, (b) `agentera.suiteVersion` equals the registry version exactly, (c) `agentera.gitRef` is a 40-character hex SHA, and (d) `references/cli/update-channels.yaml` `offline_defaults.development` equals the registry version; the local `packages/cli/bundle/.agentera-npx-bundle.json` is an advisory-only check (bundle is gitignored and regenerated at `prepack`); wired as `agentera check validate release-metadata` via `cli/commands/validate.ts` and `cli/dispatch.ts`; regression coverage in `packages/cli/test/release/releaseMetadata.test.ts` (18 cases: missing registry, malformed semver, pre-release on registry, npm-package drift, suiteVersion drift, gitRef shape across missing/short/branch/non-hex, channel-default drift, advisory bundle drift, in-sync bundle, missing package.json, missing channels, 2.x stable line, plus `releaseMetadataMain` rc=0/1 and live-repo green). `packages/cli/test` 85/85 files, 812/812 tests green; typecheck clean; build clean; `node packages/cli/dist/bin/agentera.js check validate release-metadata` returns `release-metadata ok` on the current tree and `release-metadata validation failed: …` (rc=1) when any surface diverges; stable channel on 2.x is allowed to retain its own offline_default until explicit retirement. CHANGELOG fold deferred to release per AGENTS.md.
