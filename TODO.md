# TODO

## ⇶ Critical

## ⇉ Degraded

## → Normal

- [ ] [chore:3.0.0] Decide `@agentera/mobile` npm publish identity (private workspace vs npm vs deploy-only).
- [ ] [docs:3.0.0] Decide long-term editor skill-bundle role (coexist vs eventual sunset) — horizon item, not blocking mobile v1.
- [ ] [feat:3.0.0] Extend visionera create/refine and `agentera prime --context visionera` to treat identity voice and emotional_register nuance as first-class attributes — surface the full identity block in capability context.
- [ ] [fix:3.0.0] Fix so that upgrades are not presented as repairs during `prime` or `doctor`. A version upgrade is distinct from a repair, even if they might overlap in their behavior.
- [ ] [feat:3.0.0] Detect "You're right", "my mistake", "You're right to push back" responses during profiling, to add generalized behavioral rules to the profile based on the times the human had to correct the model's behavior.
- [ ] [feat:3.0.0] Add or adapt context compression and memory techniques, such as `https://github.com/zdk/lowfat` and/or `https://github.com/yvgude/lean-ctx`, that seamlessly compresses context, stores insights and memories, etc., to enhance all capabilities' accuracy and token efficiency.
- [ ] [feat:3.0.0] Add `statusline` command to show data such as current plan status, health, etc. that can be used by runtimes, similar to `https://github.com/ryoppippi/ccusage`, `https://github.com/avinashbest/cli-status-bar`, and `https://cursor.com/changelog/04-14-26`

## ✓ Resolved

- [x] [fix:3.0.0] `agentera state todo --format json` ignores GitHub task-list `[x]`/`[ ]` checkboxes before Agentera `[type]` tags · resolved on `feat/v3` @ 8c15b32 and `main` @ b1150e4 (2.7.9): shared markdown parser classifies `- [x] [type] …` as resolved and excludes those rows from open issue counts and hej `next_action`; `- [ ] [type] …` and type-only bullets stay open. Regression coverage in `packages/cli/test/cli/todoMarkdown.test.ts`, `state.test.ts`, `orientation.test.ts`, and `tests/test_query_cli.py::TestTodo::test_github_checkbox_before_type_tag_json`.
- [x] [fix:3.0.0] TODO.md severity-section body extraction is off-by-one in `validateMdItems` · resolved on `feat/v3` @ 20f3837 and `main` @ d5bda6c (2.7.8): slice severity bodies with `/\n##\s/` boundaries from the heading end, skip the trailing newline before list items; regression coverage in `packages/cli/test/hooks/validateArtifact.test.ts` and `tests/test_validate_md_items.py`.
- [x] [fix:3.0.0] npm 0.0.0 shim delegates to the Python CLI with cwd set to the installed app home, so every project-state read resolves to `$AGENTERA_HOME/app/...` instead of the user's project; preserve the user's working directory in the shim's runBackend app-home path. · resolved by packages/cli/shim/lib/exec.mjs runBackend app-home branch now passing the caller's cwd through dispatch → runBackend to spawnSync; regression coverage in packages/cli/test/shim/runBackend.test.ts pins user cwd vs. app home and asserts the repo backend is still anchored to its repo root; CHANGELOG [Unreleased] records the shim's transitional role against the 3.0 TypeScript CLI.
- [x] [feat:3.0.0] Fix so that TODO.md items use Markdown `- [ ]/- [x]` checkboxes instead of `~~` strikethrough formatting. · resolved on `feat/v3` @ e04f78fb: converted all TODO list items from `~~` strikethrough to GitHub task-list `- [ ]`/`- [x]` checkboxes; aligned todo.yaml TC5, contract.md, compaction dryRun, and packages/mobile/TODO.md to the same checkbox convention.
- [x] [docs:3.0.0] Document CLI ↔ mobile state integration options in `docs/consolidation/mobile-open-decisions.md` · resolved on `feat/v3` (branch cleanup pass): three options (CLI subprocess, embedded TS, hybrid) with tradeoffs documented; no integration choice recorded.
- [x] [test:3.0.0] v2→v3 migration test pollution guard · migration tests must use `sandboxMigrationEnv` / `migrationCtx` (no `process.env` spread) so `opencodeConfigDir` stays under the temp home; `applyRuntimeMigrationItem` `link-skill` must `lstat`+`unlink` dangling symlinks before `symlinkSync`.
- [x] [chore:3.0.0] Lefthook pre-commit bypass and brittle inspektera TODO.md oracle · resolved on `feat/v3` @ b3900f4 and `main` @ fc87e6e: citation-anchor fixture decouples evaluation-report WARN rows from the project ledger; `verifyWarnCitationAtLine` checks cited line text; `scripts/precommit-vitest.sh` / `scripts/precommit-pytest.sh` run staged-aware smoke tests; hooks verified green without `LEFTHOOK=0`.
- [x] [feat:3.0.0] Rewrite the supported Agentera CLI core in TypeScript for Bun/npm/single-binary distribution, gated by contract-first parity with the current Python `agentera` CLI rather than a clean-slate command redesign. (Branch: `feat/v3` — implement on `feat/v3`.) · resolved on `feat/v3` @ 618c8e7 (T1 merge): shipped the v3 Bun/npm/single-binary distribution contract — `docs/packaging/v3-packaging.md` design, `packages/cli/test/packaging/prepack.test.ts` regression, `scripts/single-binary.sh` build entrypoint, `.gitignore` lift for `docs/packaging/`, and release-metadata contract at `3.0.0-dev.5` for `@next`; npx @next parity holds.
- [x] [chore:3.0.0] Design the Bun/npm packaging plan for Agentera 3.0, including single-binary delivery, npm package layout, app-home migration from Python-managed installs, and preserved direct-helper compatibility boundaries where still needed. (Branch: `feat/v3` — implement on `feat/v3`.) · resolved on `feat/v3` @ 618c8e7 (T1 merge, same commit as the TS-rewrite fold above; the Bun/npm packaging plan is the implementation of this TODO).
- [x] [chore:3.0.0] Enforce Decision 58's single-name protocol boundary: remove every backwards-compatible, deprecated, parallel, display-only, or storage/protocol split name for artifacts, concepts, primitives, command surfaces, schema fields, metadata fields, runtime adapter surfaces, and protocol identities. Zero post-3.0 surfaces may retain legacy aliases or multiple names; old names must be removed or migrated, not supported alongside the canonical name. (Branch: `feat/v3` — implement on `feat/v3`.) · resolved on `feat/v3` @ 02a16e1 (T3): `packages/cli/src/registries/artifactProtocolIds.ts` and `references/cli/single-name-protocol.yaml` enforce the single-name protocol boundary; `references/cli/vocabulary.md` and `references/analysis/startup-measurement-contract.yaml` updated to canonical names; capability-contract validate and npmParityMatrix 14/14 green.
- [x] [chore:3.0.0] Remove v1 legacy cruft after the 3.0 compatibility boundary · resolved on `feat/t4-remove-v1-cruft` @ 2dcc2cc: deleted `skills/hej/` bridge, `references/v1-section-mapping.md`, `.opencode/commands/hej.md`, marketplace/codex hej plugin entries, and retired bundle-skill vocabulary bridge concepts; kept `migrateArtifactsV1ToV2.ts` for Markdown→YAML migration; `v1LegacyCruft.test.ts` (+10 cases); vitest 822/822; npmParityMatrix 14/14.
- [x] [chore:3.0.0] TS monolith hotspots persist after the v2→v3 rewrite.?lint gate when a source file exceeds the threshold Branch: `feat/v3` — implement on `feat/v3` · resolved on `feat/v3` by 8 splits: validateArtifact (1c6f5154), compaction (07ee505a), state (cca8d2a5), dispatch (15e4e2c4), capabilityContext (28ec13fb), extractCorpus (ef52335d), startupAnalysis (747cd279), doctor (4b88dc25), codex (c45a6b1e); plus `packages/cli/test/lint/monolithLint.test.ts` regression gates any new file over 1000 lines. Largest file after split: 528 lines.
- [x] [fix:3.0.0] npm `@next` version 3.0.0-dev.4 diverges from `suiteVersion` 2.7.6 in `packages/cli/package.json`; installed-app expectations, docs, and npm publish metadata can disagree until release alignment lands. Coordinate a release-metadata pass that pins `version`, `suiteVersion`, and `gitRef` to the same value for the active @next tag, keep stable channel on 2.x until explicit retirement, and add a release-metadata parity regression that fails the doctor/upgrade gate when channel versions disagree across package metadata, install-root manifests, and CLI surface reports. (Branch: `feat/v3` — implement on `feat/v3`.) · resolved on `feat/v3`: `packages/cli/src/release/releaseMetadata.ts` reads `registry.json` `skills[0].version` as the authoritative source of truth and asserts (a) `packages/cli/package.json` top-level `version` shares its major.minor.patch with the registry version, (b) `agentera.suiteVersion` equals the registry version exactly, (c) `agentera.gitRef` is a 40-character hex SHA, and (d) `references/cli/update-channels.yaml` `offline_defaults.development` equals the registry version; the local `packages/cli/bundle/.agentera-npx-bundle.json` is an advisory-only check (bundle is gitignored and regenerated at `prepack`); wired as `agentera check validate release-metadata` via `cli/commands/validate.ts` and `cli/dispatch.ts`; regression coverage in `packages/cli/test/release/releaseMetadata.test.ts` (18 cases: missing registry, malformed semver, pre-release on registry, npm-package drift, suiteVersion drift, gitRef shape across missing/short/branch/non-hex, channel-default drift, advisory bundle drift, in-sync bundle, missing package.json, missing channels, 2.x stable line, plus `releaseMetadataMain` rc=0/1 and live-repo green). `packages/cli/test` 85/85 files, 812/812 tests green; typecheck clean; build clean; `node packages/cli/dist/bin/agentera.js check validate release-metadata` returns `release-metadata ok` on the current tree and `release-metadata validation failed: …` (rc=1) when any surface diverges; stable channel on 2.x is allowed to retain its own offline_default until explicit retirement. CHANGELOG fold deferred to release per AGENTS.md.
- [x] [chore:3.0.0] v2 state manifest for v3 consumption.?preflight reads the manifest in <100ms · resolved on `feat/v3` @ 53e842a6 (v3 reader) and `main` @ 8a3a7da9 (v2 writer): `references/cli/v3-handoff-manifest.schema.yaml` is the shared contract; v3 reader at `packages/cli/src/migrate/v2HandoffManifest.ts` reads in <100ms with directory-scan fallback; v2 writer at `scripts/v3_handoff_manifest.py` emits on install/upgrade/doctor; round-trip test passes both sides.
- [x] [chore:3.0.0] v2/v3 coexistence detection.?a v3 install alongside v2 install shows the symmetric warning · resolved on `feat/v3` @ 54bf5a20 and `main` @ 54d5b22b: shared contract at `references/cli/coexistence-probe.yaml`; v3 probe in `packages/cli/src/upgrade/coexistenceProbe.ts` detects the v2 managed app home; v2 probe in `scripts/coexistence_probe.py` scans the npm npx cache and `npm ls -g agentera`; both warn with the same five-line Coexistence block ("v3 detected alongside v2; pick one line" + three resolutions).
- [x] [fix:3.0.0] Python `agentera upgrade` regresses the v3 cursor agent runtime surface.?rejects the legacy `Read \.\.\./capabilities/<name>/instructions\.md` substring Branch: `feat/v3` — implement on `feat/v3` · resolved on `feat/v3` @ cafe6ef: `packages/cli/src/upgrade/v3CapabilitySurface.ts` probe; `runtimeMigration.ts` skip-wired when all 12 `packages/cli/src/capabilities/<name>/instructions.ts` modules exist; `packages/cli/test/upgrade/cursorAgentSurface.test.ts` regression (29 cases) asserts the D65 `Run agentera prime --context <name> --format json` form and rejects the legacy substring; Python stable backport lives on `fix/t7-cursor-agent-python-main`.
- [x] [test:3.0.0] Decision 56 upgrade/migration parity on `feat/v3` · v2→v3 orchestrator, dual update channels, compatibility guards, backport-safety gate, and Python-oracle fixture tests pass in `packages/cli/test/upgrade/` (60 tests); `UPGRADE.md` documents channels and backport order. Decision 56 remains **open** until remaining CLI surfaces (artifact validation, compaction, verify, usage/stats, full suite green) meet contract-first parity per feeds_into. · resolved on `feat/d56-parity-closeout` @ b9afb55: six v3-remaining CLI surface families ported to TypeScript with npmParityMatrix drift_direction equal for artifact_validation, compaction, verify_eval, doctor_upgrade_safety, usage_stats_consent, and runtime_adapter_hooks; gapRegistry marks all six D56 families closed; 794/794 vitest green. Decision 56 implementation track closed — v3 cutover remains a separate planera cycle per feeds_into.
- [x] [test:3.0.0] Port artifact validation, compaction, doctor/upgrade safety, verify gates, usage/stats consent semantics, and runtime adapter hooks behind TypeScript parity tests before cutting over from Python. · resolved on `feat/d56-parity-closeout` @ b9afb55: parity oracle at parity-remaining-families.json pins all six families; T2–T7 parity tests (validateParity, compactParity, doctorUpgradeParity, verifyEvalParity, usage stats consent, runtimeAdapterHooksParity) green against the Python commit pin; rebase_oracle.sh documents oracle rebase policy.
- [x] [chore:3.0.0] Decision 65 per-capability content surface · resolved on `feat/d65-capability-ts-surface`: 12 capability prose files relocated from `skills/agentera/capabilities/<name>/instructions.md` to `packages/cli/src/capabilities/<name>/instructions.ts` as default-exported string constants (round-trip byte-for-byte); the slim `capability_context` capsule gains a `prose` field and `first_invocation_read: prime_context` with `runtime_enforcement: true`; 36 runtime descriptors (12 codex toml + 12 cursor md + 12 opencode md) now invoke `agentera prime --context <name> --format json` instead of reading the on-disk prose; `capability_schema_contract.yaml` renames `instruction_file` → `instruction_module`; the validate command adds a regression sentinel rejecting the legacy `capabilities/<name>/instructions.md` substring in descriptor bodies. `references/cli/capability-instruction-contract.yaml` collapses `first_invocation_read.allowed_values` to the single `prime_context` value. `skills/agentera/SKILL.md` frontmatter gains an explicit `capabilities` list and the body drops `instructions.md` references; the published `npx skills` surface (SKILL.md only) keeps the discoverability table and validation commands, so the ecosystem stays functionally equivalent. 692/692 vitest green; capability-contract validate pass; per-name capability validate pass for all 12 (V1 error string now `packages/cli/src/capabilities/<name>/instructions.ts not found`); `check validate descriptors` reports 24/24 passed. CHANGELOG fold deferred to release per AGENTS.md. `.agentera/plan.yaml` status: complete; this is the implementation commit for that plan.
- [x] [test:3.0.0] v2→v3 migration test pollution guard · migration tests must use `sandboxMigrationEnv` / `migrationCtx` (no `process.env` spread) so `opencodeConfigDir` stays under the temp home; `applyRuntimeMigrationItem` `link-skill` must `lstat`+`unlink` dangling symlinks before `symlinkSync`. · resolved on `feat/v3`: new `packages/cli/test/upgrade/helpers/migrationCtx.ts` with `sandboxMigrationEnv` (`HOME`, `XDG_CONFIG_HOME=home/xdg`, `AGENTERA_BOOTSTRAP_SOURCE_ROOT`) — no `process.env` spread, so developer's `XDG_CONFIG_HOME` no longer routes opencode writes to real `~/.config/opencode`; `applyRuntimeMigrationItem` `link-skill` switched from `rmSync` to `lstat`+`unlink` so pre-existing dangling symlinks are removed safely. Regression coverage in `packages/cli/test/upgrade/migrateRuntimeMatrix.test.ts` for pre-existing regular file, pre-existing dangling symlink, and default-env XDG isolation.
- [x] [fix:3.0.0] Orkestrera `task_queue.dependency_ready_tasks` and `blocked_tasks[*].blocked_reasons` are broken in `prime --context orkestrera --format json` · resolved on `feat/v3`: normalize plan task numbers and `depends_on` refs to comparable lookup keys in `orchestrationContext` and `dependencyReadyTasks` (int/string/object forms); regression coverage in `packages/cli/test/cli/prime.test.ts` for a complete task 1 unblocking task 2 when `depends_on` uses string `"1"`.
- [x] [chore:3.0.0] TODO.md empty severity sections · resolved on `feat/v3` (this commit): `validateMdItems` in `packages/cli/src/hooks/validateArtifact.ts` now enforces the hybrid policy: only Critical (⇶) requires ≥1 `- [type]` item; Degraded (⇉), Normal (→), and Annoying (⇢) are allowed to be header-only without `- Placeholder` decoys. The policy is documented in `skills/agentera/schemas/artifacts/todo.yaml` CONVENTION TC6 (level: required, asymmetric) and discoverability-indexed in `references/cli/vocabulary.md`. Regression coverage: retired the placeholder-decoy test and added 4 new cases in `packages/cli/test/hooks/validateArtifact.test.ts` (header-only Degraded, all-four header-only, empty Critical flagged, boundary-scanner Critical-body-collapse). 686 vitest green; capability-contract unchanged; TS-only fix (no Python port, no main-branch backport). `.agentera/plan.yaml` status: complete; this is the implementation commit for that plan.
- [x] [feat:3.0.0] Next-major doctor section · resolved on `feat/v3` @ 51feef4 and `main` @ fc87e6e: per-channel `next_major` in `references/cli/update-channels.yaml`, TypeScript `nextMajorDoctor.ts` and Python `scripts/next_major_doctor.py` head-of-output doctor rendering, vitest 12/12 and pytest 5/5 on main; CHANGELOG `[3.0.0]` and feat/v3 version_files at 3.0.0.
- [x] [chore:2.7.7] `progress.yaml` exceeds the 10/40/50 compaction retention caps on `feat/v3` · resolved on `feat/v3` @ fc87e6e: added `--apply` alias for `agentera check compact --mode fix`, regression tests that fail the gate when progress.yaml exceeds 50 cycles, archive preservation on compact, and live progress.yaml pinned at 10/40/50; `agentera check compact` passes without `over_limit`. Python stable mirror on `main` deferred per both-branch sequencing.
- [x] [chore:2.7.7] Decision 66 remove the progress.yaml per-cycle `commit` field · resolved on `feat/v3` @ fc87e6e: dropped PR6/PV2 commit requirements from the progress schema; deleted `progressCommit.ts`, `backfill.ts`, and related tests; removed `agentera check backfill` from dispatch/help/doctor and the validate-artifact ancestry guard; stripped `commit` from progress output and v1 migration; updated AGENTS.md, realisera/inspektera instructions, contract.md, UPGRADE.md, and CHANGELOG. Python stable mirror on `main` deferred per both-branch sequencing.
- [x] [fix:2.7.7] Inspektera WARN findings must cite line numbers · resolved on `feat/v3` @ fc87e6e: tightened orkestrera Step 3 Surface 2 delegation template, `capability-instruction-contract.yaml` evaluator-handoff schema, `evaluatorHandoffContract.ts` report validation with `verify_command` line reproduction, and inspektera citation prose; regression coverage in `evaluatorHandoffContract.test.ts` and `inspekteraEvaluationReport.test.ts`. Python stable mirror on `main` deferred per both-branch sequencing.
- [x] [docs:3.0.0] Align update-channel authority with TS-only v3: remove or mark unsupported the development git/uvx resolution (`uvx ...@feat/v3`) in `references/cli/update-channels.yaml` and related docs; feat/v3 is npm-only, v2→v3 is one-way via `npx -y agentera@next`, and uvx/git remains the stable Python 2.x maintainer path only. · development channel git resolution marked `supported: false` in `references/cli/update-channels.yaml`; `channels.ts` returns empty git fields for development; UPGRADE.md and vocabulary.md state npm-only v3 and @main Python stable path; channel tests updated.
- [x] [fix:3.0.0] npm CLI help surface: `npx -y agentera@next --help`, `-h`, and subcommand help such as `agentera upgrade --help` return "unknown or not-yet-ported command"; the self-contained 3.x CLI still needs discoverable help without relying on the Python entry point. · resolved by `packages/cli/src/cli/help.ts` and dispatch wiring: top-level `--help`/`-h`, subcommand help for upgrade, doctor, state, check, and report; regression tests in `packages/cli/test/cli/help.test.ts`.
- [x] [fix:3.0.0] Port or formally retire `check verify smoke` in the npm CLI: stable Python still runs `check verify smoke installed-skills`; npm `@next` exposes only the `eval` verify family; update contracts, CI, and maintainer docs to match the TS-only distribution boundary. · resolved by retired smoke family messaging in `verify.ts`, `agent-ready-state-contract.yaml` eval-only npm guidance, and verify regression tests.
- [x] [fix:3.0.0] `report`/`usage`/`stats` must tolerate large corpus files: npm `@next` crashes reading oversized `corpus.json` (`Cannot create a string longer than 0x1fffffe8 characters`); stable Python `report` succeeds on the same data. · resolved by size-bounded corpus reads in `usageStats.ts` and `report.ts` with degraded stale status and repair guidance; regression tests cover oversized corpus paths.
- [x] [fix:3.0.0] Port `doctor --smoke` in the npm CLI: npm `@next` throws "setup doctor --smoke is not yet ported"; stable Python doctor still supports the smoke path. · resolved by `runNpmSmokeChecks` in setup and user doctor paths; bounded offline smoke without live model calls by default; doctor regression tests added.
- [x] [test:3.0.0] Extend contract-first parity tests (Python oracle → npm `@next`) for the 3.0 namespace surface (`prime`, `state`*, `check *`, `report`, `upgrade --channel development`, hooks); do not treat uvx/git as a v3 entry point or require cross-entry-point `--channel`/`--json` flag parity with Python stable. · resolved by `packages/cli/test/cli/npmParityMatrix.test.ts` and oracle fixture comparing prime, state plan/todo, check compact, report refresh dry-run, and upgrade development dry-run envelopes without uvx @feat/v3 assertions.
- [x] [fix:2.7.7] Self-audit linter budget lookup misses schema-name CLI input (`plan`, `progress`) and falls back to the 1000-word default · resolved on `main` @ 2.7.7 by beb535c (4725657–e8714de): `_resolve_budget_artifact` normalizes artifact names, budget dicts use lowercase schema-name keys, CLI `--text`/`--file` paths wire the normalized lookup, and unit plus CLI end-to-end regression tests pin behavior; cross-surface D58 single-name vocabulary unification remains open under Decision 58 below.
- [x] [chore:2.7.6] Consolidate duplicated progress-commit and git-ancestry logic · resolved by 6530f42 and 56fd8fe: one `scripts/progress_commit.py` module now serves `agentera check backfill` and the validate-artifact guard via `hooks/common.py`.
- [x] [chore:2.7.6] Extract progress-commit module from `scripts/agentera` monolith · resolved by 6530f42: `cmd_backfill` is a thin wrapper importing `compute_backfill` and `rewrite_cycle_commits`.
- [x] [chore:2.7.6] Move progress commit validation out of oversized `hooks/validate_artifact.py` · resolved by 56fd8fe: guard delegates to `validate_progress_commits` with no near-copy git/token logic left in the hook.
- [x] [fix:2.7.6] Align progress guard YAML loading · resolved by 56fd8fe: guard uses `load_yaml_mapping()` and returns no commit violation on corrupt or non-mapping progress YAML.
- [x] [chore:2.7.6] Reduce hooks→scripts sys.path coupling · resolved by 56fd8fe: `hooks/common.py` documents the single sanctioned scripts bridge; neutral package deferred to 3.0.
- [x] [test:2.7.6] Expand progress commit YAML rewrite regression coverage · resolved by 56fd8fe: folded/flow scalar and comment-preservation cases in `tests/test_progress_commit_guard.py`.
- [x] [docs:2.7.6] Document unknown-hash and shallow-clone behavior for progress commits · resolved by 56fd8fe: progress schema notes and `UPGRADE.md` describe intentional skip and reconciliation.
