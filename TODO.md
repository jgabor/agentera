# TODO

## ⇶ Critical

- [ ] [fix:3.0.0] Make every documented and machine-returned v3 bootstrap one safe, channel-correct call. Until stable promotion, `agentera route receipt` provenance, `prime` startup and fallback commands, shared-skill examples, and package smokes must use `npx -y agentera@next`, not authorize bare `agentera`. Verify clean, v2, partial, and v3 projects end with `prime` and `doctor` passing without redirecting `@latest`. The matrix must assert the unified startup outcome owned by TODO `rsplcfvosn` and prove healthy projects require no second dashboard or fallback call.
- [ ] [fix:3.0.0] Make `agentera route request --input - --format json` self-describing after `semantic_required`. Return or reference the exact `agentera.route_receipt.v1` nullable schema, valid outcomes, nullability, compound and span rules, and one runnable stdin example. `agentera route receipt --help` must expose the same contract so hosts never inspect `hybridRouteReceipt.ts` or `hybrid-route-contract.yaml` to proceed.
- [ ] [fix:3.0.0] Make `agentera prime --context <capability> --format json` one bounded, unambiguous startup contract. Replace overlapping `included`, `missing`, `missing_state`, and `fallback_only` with one per-family availability projection; stop duplicating writer contracts and copied prose; emit exact detail commands. Keep the status payload at or below 22,500 bytes and deprecate `prime --dashboard`. Emit one aggregate `ok`, `degraded`, or `blocked` outcome; require top-level status, `brief.status`, and the status-context outcome to agree. Add boundary regressions at 22,500 and 25,000 bytes and prove a healthy startup never needs a second dashboard call.
- [ ] [fix:3.0.0] Prevent new out-of-order replacement evaluation deadlocks. Require every task selected as a superseded_by replacement to be complete with a latest persisted PASS before supersession succeeds, and reject new plan completion when a superseded task points to a replacement without PASS provenance. Preserve historical records and the narrow first-PASS recovery for already-persisted open-plan state. Add regressions for unevaluated replacements, failed replacements, exact replay, closeout validation, and actionable correction guidance.
- [ ] [fix:3.0.0] Use one profile validity result in `agentera prime --context discuss --format json` and `agentera report profile-grounding --format json`. Prime currently reports `profile.status: loaded` and `stale` while grounding rejects the same `PROFILE.md` as malformed. Report `invalid` or `repair_needed` with exact recovery and add malformed-profile parity coverage.
- [ ] [fix:3.0.0] Make bounded state list limits predictable. `agentera state decisions list --limit 20 --format json` returned 9 records because full entries exhausted the byte budget; TODO listing similarly stopped early. Add a summary/detail projection or define the limit as requested candidates, and always return exact `get --id` pointers plus deterministic omission metadata.
- [ ] [fix:3.0.0] Generate artifact-specific `state ... list --help` from the retrieval authority. `agentera state decisions list --help` omitted the supported `--topic` filter, forcing source inspection. Show each artifact's valid filters, bounds, formats, and runnable examples, and test help/runtime parity.
- [ ] [fix:3.0.0] Align `packages/cli/src/state/write/operations.ts` dependency discovery with entity-mode bare task IDs. `agentera state plan explain --verb append` reports `integer_list` and task numbers, but runtime rejects `3` and requires a ten-letter task ID. Make discovery, parsing, examples, and command regressions agree.
- [ ] [fix:3.0.0] Make `agentera doctor --format json` diagnose retired Agentera-native resources. It returned `up_to_date` with no signals while `~/.config/opencode` still contained the restoring plugin, managed commands and agents, broken skill links, and `~/.codex/agents` held 24 managed descriptors. Emit bounded cleanup previews without treating names or matching content as ownership.
- [ ] [fix:3.0.0] Complete `references/adapters/runtime-retired-resources.yaml` and cleanup discovery for every retired Agentera-native surface. Include proven OpenCode plugin, `agentera` and `hej` commands, primary and Swedish agents, stale skill links, and Codex English and Swedish descriptors plus Agentera plugin, marketplace, skill, and hook registrations. Remove restorers before leaf resources and preserve user-owned state.
- [ ] [fix:3.0.0] Migrate lifecycle ownership IDs so declared cleanup matches installed records. `agentera upgrade --legacy-cleanup codex.agent-descriptor.status --dry-run` looked up `codex.agent-descriptor.status`, but the journal records the exact file as `codex.agents.status`; the CLI falsely classified `~/.codex/agents/status.toml` as unowned. Add ID alias or migration coverage and exact replay tests for every descriptor.
- [ ] [fix:3.0.0] Make `agentera upgrade --legacy-cleanup RESOURCE_ID --dry-run --format json` a targeted operation. The status-descriptor preview ran unrelated app, artifact, runtime, and cleanup phases and embedded the full 29KB OpenCode plugin `newText`. Return only the selected resource, bounded configuration actions, hashes or diff summaries, and blockers that affect that resource.
- [ ] [fix:3.0.0] Do not let preserved user-owned collisions block independent legacy cleanup. `agentera upgrade --only cleanup --dry-run` found 12 marker-owned Swedish OpenCode agents ready for removal but marked the phase blocked because `~/.config/opencode/agents/audit.md` lacked Agentera ownership. Report that file as preserved/manual review and allow proven removals to converge.
- [ ] [fix:3.0.0] Remove every tracked and installed reference to the retired top-level `agentera hook` command. `.codex/hooks.json` and `~/.config/opencode/plugins/agentera.js` invoke `hook validate-artifact`, while `agentera@next` rejects `hook` as unknown. Extend retirement and package tests to scan host-native config and ensure shipped hooks invoke only current commands or are absent.
- [ ] [fix:3.0.0] Make `agentera prime --context build --format json` ready for explicit no-plan work. It reported `mode: no_plan` but `complete_for_execution_context: false`, then prescribed `agentera state plan --format json`, which failed `not_found`. Represent no open plan as valid `no_plan`, accept user-scoped acceptance, and avoid non-advancing fallback loops.
- [ ] [fix:3.0.0] Make `agentera state query changelog --format json` expose the same changelog boundary used by Build prime. The fallback returned `status: ok` and `entries: []` for an existing `CHANGELOG.md`, while `execution_context.changelog_boundary` found `## [Unreleased]` and the latest release. Use one scanner and return headings, boundary, and provenance consistently.
- [ ] [fix:3.0.0] Make `execution_context.verification_expectations.expected_commands` project-aware and deduplicated. In this TypeScript and Vitest repository, Build prime prescribed `focused pytest targets` and both `agentera check compact` and `compaction check`. Derive runnable commands from `AGENTS.md` and package scripts, preserve provenance, and reject placeholders or duplicate aliases.
- [ ] [fix:3.0.0] Put Build artifact logging before its single commit. `packages/cli/src/capabilities/build/instructions.ts` currently orders Step 7 Commit before Step 8 Log, which writes TODO, progress, and changelog after the commit and contradicts the same-commit rule. Remove stale claims that entity progress assigns numbers or compacts aggregate files, and test served instructions against commit policy.
- [ ] [fix:3.0.0] Establish one TODO mutation and projection authority. `agentera state todo create` writes `.agentera/entities/...` but does not update root `TODO.md`, forcing agents to duplicate issue text manually; entity listing sorts critical items by opaque ID, so callers cannot request top insertion. Generate or update the Markdown projection atomically or retire it, and support deterministic priority order.

## ⇉ Degraded

- [ ] [fix:3.0.0] Normalize npm registry JSON before publication convergence checks. Accept scalar, object, and singleton-array shapes from supported npm versions for exact-version integrity and dist-tags; retain string-safe prerelease ordering; add npm 12 fixtures; and prove matching exact package and tag state replays as non-publishing success rather than an integrity conflict.
- [ ] [fix:3.0.0] Keep initial status orientation informational until the user expresses execution intent. Render `next_action` and wait for free-form acceptance; do not open a native Proceed/Cancel question merely because the suggestion is state-changing. After explicit acceptance, apply the ordinary confirmation boundary before dispatch. Add host-contract regressions for bare status, explicit build requests, accepted suggestions, cancellation, and autonomous orchestration.
- [ ] [test:3.0.0] Add an executable activation-conjunction release gate for every active CLI and capability surface. Derive checks from owning authorities and require schema or contract, CLI discovery and help, runtime acceptance, bounded privacy-safe diagnostics, package projection, served instructions, and adversarial tests to agree before a feature is declared active. Seed coverage with route receipts, state list/help and plan identity, profile validity, changelog, verification expectations, Build ordering, and TODO projection; report the exact owner and correction for each mismatch.

## → Normal

- [ ] [refactor:3.0.0] Refactor plan levels from skip/light/full to light/normal/full. Remove skip mode so every direct plan invocation produces at least a light plan: light for trivial or localized work, normal for clear single-cycle work, and full for multi-cycle or architecture-spanning work. Update the runtime startup contract, plan instructions, schemas, validation, and tests to use the new three-level taxonomy consistently.
- [ ] [feat:3.0.0] Add light/normal/full levels to research. Light handles a focused lookup with bounded evidence, normal compares multiple relevant sources, and full performs broad synthesis across competing approaches. Levels control evidence breadth, analysis depth, and verification—not prose length. Update the startup contract, instructions, schemas, and tests.
- [ ] [feat:3.0.0] Add light/normal/full levels to audit. Light assesses one named finding or dimension, normal assesses the relevant selected dimensions, and full performs a repository-wide multi-dimensional audit. Levels control inspected scope and evidence depth while preserving finding-confidence and citation requirements. Update the startup contract, instructions, schemas, and tests.
- [ ] [feat:3.0.0] Add light/normal/full levels to document. Light handles one localized documentation correction, normal covers a feature or coherent document set, and full performs repository-wide generation or drift auditing. Levels control document scope and verification breadth while preserving approval gates. Update the startup contract, instructions, schemas, and tests.
- [ ] [feat:3.0.0] Add light/normal/full levels to profile. Light incrementally incorporates recent high-signal evidence, normal performs the standard synthesis workflow, and full rebuilds from the available corpus with complete validation. Levels control corpus breadth, synthesis depth, and verification—not output verbosity. Update the startup contract, instructions, schemas, and tests.
- [ ] [refactor:3.0.0] Audit and retire `skills/agentera/references/contract.md` (79KB, 1048 lines, Sections 1-23). Survey found ~60% of sections are duplicated into `protocol.yaml` (Sections 1, 2, 3, 11, 13, 18), `SKILL.md` + `AGENTS.md` (Section 5), or are stale v2-era content using Swedish capability names and `$AGENTERA_PROFILE_DIR` (Section 6). ~40% (Sections 4 partial, 19, 20, 22, 23) may still be uniquely authoritative but use v2-era naming. Cut duplicated/stale sections; migrate unique sections (Staleness Detection, Behavioral Verification Gate, Session Corpus Contract, Pre-spawn Git Commit) into `references/cli/` with v3 naming; retire contract.md when empty. Surfaced during the D81 vision audit — the D79/D81 direct-contract philosophy removed the contract.md pointer from status and vision; the 10 remaining unaudited capabilities still carry the boilerplate pointer and will lose it during their respective audits. (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [feat:3.0.0] Add progress cycles tracking to more capabilities, not just `build`.
- [ ] [fix:3.0.0] Harden `loadSuiteVersion` (`appModel.ts:72-95`) to surface WHY it returned null on a corrupted/missing `registry.json` version-authority file. Current swallow-all catch at line 81 returns null silently for JSON parse failures, missing `skills` array, wrong `authority.selector`, and missing file — none of which reach the operator. Add structured diagnostic strings (e.g., `version_authority_file_missing`, `version_authority_parse_failed`, `skills_array_empty`, `selector_mismatch`) so callers like `cliDistributionMajor`'s secondary-signal fallback can distinguish "v2 with no package.json" from "v3 with corrupted registry.json." Surfaced during the `cliDistributionMajor` plan (Step 2, scope-boundary decision: kept out of plan scope; tracks as separate concern). (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [feat:3.0.0] Add `statusline` command to show data such as current plan status, health, etc. that can be used by runtimes, similar to `https://github.com/ryoppippi/ccusage`, `https://github.com/avinashbest/cli-status-bar`, and `https://cursor.com/changelog/04-14-26`
- [ ] [refactor:3.0.0] Rename `agentera app-home` command to `agentera path`
- [ ] [feat:3.0.0] Add macOS and Windows GitHub Actions jobs for `pnpm -C packages/cli test` (and related gates); CLI tests are cross-platform-hardened on darwin and linux.
- [ ] [test:3.0.0] Make contributor verification terminable and discoverable. Reproduce the 300-second timeout with process-tree evidence, assign cleanup to the actual owner, and, if reproduced, verify that termination stops all descendants without residue. Document `pnpm -C packages/cli run test:source -- test/path.test.ts`, including exact-path and source-owner constraints.
- [ ] [docs:3.0.0] Surface the four-question update/repair wording template (What happened / What the preview did / What the recommended fix will do / What it will not do) in `agentera prime --format json` when `app.status` is `outdated` or `repair_needed`, or document in the collapsed SKILL.md that the host must read `agentera prime --context status --format json` `prose` for the framing. The JSON provides `app.approval` (one-line) and `app.signals[].message` — not the structured four-question template or recommendation-first label convention the old SKILL.md specified. (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [chore:3.0.0] Remove deprecated `PROFILERA_PROFILE_DIR` alias fallback from `setProfileDir()` in `.opencode/plugins/agentera.js` before v3 stable release. Task 12 of the D70 cleanup plan added the fallback as a migration bridge; the upgrade migration step (Task 13) rewires config files automatically. Before v3 stable, verify no v2 users still depend on the old name, then remove the fallback code and the `profileraDeprecation` flag. The v2→v3 upgrade window is the only period where the alias is needed.
- [ ] [chore:3.0.0] Address pre-existing lint findings surfaced by the `vp lint` migration in `packages/cli`. The toolchain-cutover plan (Task 3) wired `scripts.lint` → `vp lint` and recorded a baseline of 101 warnings / 0 errors over 190 files (exit 0) at `packages/cli/.lint-baseline.txt`; `vp fmt --check src/` reports 151 files would be reformatted. Non-regression is the gate held here — address the findings (unused imports, regex-preference, control-char, etc.) in a separate cleanup pass, not as part of the toolchain migration.
- [ ] [fix:3.0.0] Remove deprecated `issues` alias from prime JSON output. The resolved item (2026-06-27) kept `issues` as a deprecated alias with stderr warning; the stable-cut removal was deferred but never tracked as an open item. A public beta should not ship a deprecated field alias with no removal date.
- [ ] [refactor:3.0.0] After the unified status capsule and reserve gate ship, measure whether repeated capability instructions remain a material startup cost. Only if they do, generate one concise, validated, versioned execution contract from the authoritative capability prose so callers can omit an unchanged instruction body without creating a second hand-maintained contract.
- [ ] [feat:3.0.0] Use git `user.name` and `user.email` for `confirmed_by` fields in `decisions.yaml`
- [ ] [feat:3.0.0] Detect "You're right", "my mistake", "You're right to push back" responses during profiling, to add generalized rules to the profile to correct the model's behavior.
- [ ] [feat:3.0.0] Add a glossary synthesis pass to profilera (Full mode, after Step 4) that persists a `## Glossary` section to the user-level `PROFILE.md`. **Prerequisite: tiered extraction (see Degraded).** Source: the bounded signal stream (tier 2) — not the raw `corpus.json` dump. Glossary-worthy terms are surfaced from two signals: (1) correction/clarification events in `conversation_turn` records where the agent named a term and the user corrected or disambiguated it (e.g., "you said 'account' — Customer or User?"); these records are already classified `signal_type: "correction"` by the extractor. (2) Stable recurring terms appearing across `instruction_document` and `project_config_signal` source families in the signal stream. Each entry records the term, the user's definition/usage, one evidence anchor (record id + date), and a confidence along the existing CS1–CS5 scale. Skip the pass entirely when the project being profiled has `.agentera/glossary.yaml` registered in `.agentera/docs.yaml` — that signals an established convention and the user-level glossary should defer to it. The glossary section is independent of the decision-pattern entries and does not affect confidence metadata elsewhere in `PROFILE.md`; it is consumed read-only by downstream capabilities the same way the decision profile is. Schema change to profilera's output contract — add a regression test that a signal-stream fixture with a known correction event produces the expected glossary entry.
- [ ] [feat:3.0.0] Add terminology-drift detection to audit (alongside the existing finding families) and emit findings that propose canonical terms; persist the resolved canonical set as `.agentera/glossary.yaml`. No conversation history needed — this is a structural codebase scan, independent of corpus.json and the tiered-extraction work. Audit detects drift when the same concept carries multiple names across the codebase (e.g., `Dict` vs `JsonValue` vs `Record<string, any>`), or when shipped code uses a term that diverges from any glossary the user carries in their `PROFILE.md`. Audit emits findings with proposed canonical term + list of drift variants + evidence anchors (file:line), using the existing severity/confidence vocabulary; the canonical set lands in `.agentera/glossary.yaml` only after `discuss` confirms each proposal, mirroring how audit findings flow through discuss before becoming decisions. Register `.agentera/glossary.yaml` in `.agentera/docs.yaml` artifact mapping so profilera's skip rule and other capabilities resolve it through the standard path. Add a `v1LegacyCruft` scan guard so a repo cannot reintroduce drift variants once `discuss` has confirmed the canonical term.
- [ ] [feat:3.0.0] Add real-time term disambiguation to the consuming capabilities (discuss, plan, build) as a pre-processing step that cross-references incoming user terms against both glossaries and flags ambiguity before proceeding. **Prerequisites: profilera glossary and audit glossary.** This is the consumer that makes the two glossary sources actionable — neither profilera nor audit can answer "you said 'account' — Customer or User?" on their own; profilera captures how the user talks, audit establishes how the codebase talks, but neither detects when a user's term is ambiguous against the project's concept set. The check runs when a capability receives user input containing a domain term: (1) look up the term in the project glossary (`.agentera/glossary.yaml`); (2) if absent or ambiguous, fall back to the user's glossary (`PROFILE.md` `## Glossary`); (3) if the term maps to multiple distinct concepts, emit an advisory asking the user to clarify before proceeding — "You said 'account' — do you mean Customer or User? Those are different things." Precedence: project glossary is authoritative for project-scoped work; user glossary fills gaps when the project glossary is absent or doesn't cover the term; conflicts between the two (user says `account = User`, project says `account = Customer`) surface as a tension flagged to the user, not silently resolved. Scope: a shared glossary-lookup helper (resolved through `.agentera/docs.yaml` mapping) consumed by the three capabilities' startup context; no new artifact written. The advisory is non-blocking for capabilities that cannot pause for user input mid-cycle (e.g., autonomous build) — in those cases the ambiguity is logged as a caveat in the cycle evidence and surfaced in the next prime attention item.
- [ ] [chore:3.0.0] Re-derive `first:` dates and confidence numbers from the current corpus on every profile write, instead of carrying them forward from the prior profile. The adversarial review in `ses_1406cbc95ffeZ8WRcSvUag387G` found 25+ entries where `first:` predated the corpus start (`first:2026-02-08` against a 2026-02-14 corpus) and several where the confidence number reflected a prior rebuild's evidence. The profilera writer must mark inherited entries explicitly (e.g., `first:2026-02-08 inherited:2026-05-30`) and demote confidence to the maximum of "re-evidenced in current corpus" and "carried forward" rather than silently blending them.
- [ ] [release:3.0.0] At the approved stable cutover, promote the TypeScript package to npm `@latest`, update the channel authority and bundled bootstrap from `agentera@next` to bare `agentera`, replace the v2 Python delivery path with an explicit deprecation stub, remove retired Python and shim delegation sources, migrate remaining runtime hooks, and execute the clean, v2, partially migrated, and v3 bootstrap matrix against `@latest`. Do not change stable-channel resolution before this gate.

## ⇢ Annoying

## ✓ Resolved

- [x] [fix:3.0.0] Make development and stable package publication one bounded, verified transaction. Load credentials through a temporary restricted npm configuration; report package identity, file count, sizes, integrity, and warnings by default while retaining full manifests behind explicit JSON or verbose output; publish; poll the exact version and expected dist-tag with bounded retries; and run an exact-version bootstrap smoke test before reporting success.
- [x] ~~[refactor:3.0.0] Superseded 2026-07-25 by TODO rsplcfvosn, which consolidates structured attention into the sole bounded status dashboard contract.~~
- [x] ~~[refactor:3.0.0] Superseded 2026-07-25 by TODO rsplcfvosn, which consolidates canonical entity references and exact detail recovery into the sole bounded status dashboard contract.~~
- [x] ~~[test:3.0.0] Superseded 2026-07-25 by TODO vptlelnadp, which incorporates the representative v2 migration proof into the exact documented bootstrap matrix.~~
- [x] [fix:3.0.0] Fix entity-state publication validation against secure pinned paths. Resolved 2026-07-25: logical migration-provenance validation now uses the validated real-project-root identity while atomic publication and ownership checks remain descriptor-pinned. Integration coverage verifies plan and TODO creation beside migrated decision/progress summaries, complete graph rollback, root-substitution rejection, predecessor restoration, and successful archive-then-create retry.
- [x] [docs:3.0.0] Decide long-term editor skill-bundle role (coexist vs eventual sunset). Resolved 2026-07-23: retain `skills/agentera/SKILL.md` and the CLI as the portable integration contract; native descriptor coexistence is retired.
- [x] [fix:3.0.0] Resolve valid v2.7.11 compacted progress, decision, and health migration. Resolved 2026-07-22: summaries now migrate as source-bound immutable degraded entities, preserve inherited confidence during migration, use ordinary immutable readers and startup, report root/dependent diagnostics, and pass the Git-backed lifecycle proof. Read-only KTX preview reports 249 identities and 248 targets because of a pre-existing Decision 25 conflict; KTX was not migrated and its conflict remains owner work.
- [x] [test:3.0.0] Prove verification ownership and source-run performance. Resolved 2026-07-21: all four independent owners and all seven policy compositions pass with 200 files assigned exactly once; three retained eight-worker source runs have a 42.908-second median, 47.330% below the four-worker baseline, with raw reports and recomputation provenance retained under `evidence/ymkdnqmroi/`. Historical `ymkdnqmroi` audit-2 provenance remains blocked; successor `qgkxggjber` restored all-capability retired-vocabulary runtime reconciliation without another prime projection.
- [x] [fix:3.0.0] Finish generated-output recovery. Resolved 2026-07-21: mutation ownership uses atomic owner records and fails closed for stale canonical locks; legacy reclaim claims recover only when ownership is provable, while conflicting live claims remain preserved with actionable diagnostics. Repeated concurrent publication, cleanup, and reader tests passed without residue.
- [x] [test:3.0.0] Correct serialized consumed-policy fixture coverage. Resolved 2026-07-21: adversarial fixtures now parse the canonical policy, mutate data, and serialize valid YAML; they prove the bounded consumed-schema diagnostic rather than a parser failure or stack trace, cover all consumed policy sections plus unknown owner routing, and retain focused, source, package, performance, build, and generated-overlap evidence.
- [x] [fix:3.0.0] Report complete open-TODO severity totals in status. Resolved 2026-07-20: `agentera prime --context status` now separates complete aggregate counts from its bounded 20-item detail projection and exposes total, returned, omitted, and exact continuation recovery without dropping routing state at the startup byte budget.
- [x] [fix:3.0.0] Exclude resolved entity TODOs from status orientation. Resolved 2026-07-20: `agentera prime --context status` now filters to open TODOs before bounded pagination, preventing closed critical or degraded history from contaminating counts, attention, and next-action routing.
- [x] ~~[fix:3.0.0] Make the typed decisions writer support safe decision-content amendments without rewriting unrelated legacy semantics. Resolved 2026-07-16: `agentera state decisions amend --number N [--question ... --context ... --alternative-chosen ... --alternative-rejected ... --choice ... --reasoning ... --confidence firm|provisional|exploratory --feeds-into ...] [--dry-run] --format json` composes a record-local revision under `.agentera/revisions/decisions.yaml` over the immutable numbered archive; the decisions projection is never rewritten, unrelated records keep their bytes (comments, block scalars, quoting, inherited legacy confidence labels), amend refuses a broken revision document or unsafe record-local boundary without side effects, retries converge on a stable revision identity, and append/update/amend tolerate untouched inherited labels as explicit legacy caveats while keeping new/amended content on the current vocabulary. The transaction candidate-builder was split into `candidateMutation.ts` to restore file headroom. Plan and progress state records the verified contract synchronization.~~
- [x] ~~[refactor:3.0.0] Split `lifecycleAdapters.ts` barrel — resolved 2026-07-16: moved lifecycle validation orchestration and parity checks into a dedicated module, kept the historical public re-export surface stable, removed the three obsolete lint-baseline entries, and passed the full CLI tests and quality gates.~~
- [x] ~~[refactor:3.0.0] Replace the full-materialized `corpus.json` dump with tiered extraction. Resolved 2026-07-15: bounded evidence tiers (full-evidence shards + signal tier with evidence anchors) are the only canonical publication output; the monolithic `corpus.json` write is eliminated from the extract CLI. All consumers — usage analytics, report status, prime coverage, startup analysis, and profile synthesis — read from bounded tiers. Profile synthesis reads the signal tier and assesses per-family retention against the contract's `minimum_family_retention=0.5` threshold, resolving planning Unknown 2.~~
- [x] ~~[fix:3.0.0] No diagnostic when AGENTERA_HOME is set but its managed script is unavailable. Resolved 2026-07-15: `packages/cli/shim/lib/resolve.mjs` now reports the resolved missing-script path on stderr, preserves repo and uvx fallback precedence, and stays silent when app-home resolution is unset or explicitly excluded.~~
- [x] ~~[fix:3.0.0] Make lint consume authoritative verbosity budgets. Resolved 2026-07-15: `selfAudit.ts` now uses owning schema declarations for full-file and per-entry word findings; explicit no-limit and token-only dimensions skip word findings; malformed, unavailable, and unsupported authority returns an actionable error without fallback constants.~~
- [x] ~~[feat:3.0.0] Define authoritative artifact verbosity budgets. Resolved 2026-07-15: every supported artifact resolves to one owning schema; word, no-limit, and token-only declarations are machine-classified; malformed, unreadable, ambiguous, and unsupported authority fails without fallback limits.~~
- [x] ~~[fix:3.0.0] Independently audit the bounded-retrieval alias-parity repair. Resolved 2026-07-15: schema, help, prime contexts, and structured runtime output no longer advertise retired `gate`; Build uses `agentera check compact`; canonical and transitional compaction report `command: "check compact"`; retired `gate` returns a structured runnable correction; exhaustive alias parity and independent audit passed while Cycles 814–816 remained immutable.~~
- [x] ~~[fix:3.0.0] Synchronize public bounded-retrieval contracts. Resolved 2026-07-15: runtime authority, help, schema, optimize startup/prose, capability schemas, operator docs, bundle parity, output bounds, archive ownership, and runnable structured errors now agree; focused 217/217 and full 2051/2051 tests passed.~~
- [x] ~~[test:3.0.0] Repair blocked bounded-retrieval Plan Task 4 after two failed evaluations. Resolved 2026-07-15: committed deterministic retrieval evidence serializes to 30,407 YAML bytes with stable SHA-256 `059edafd…de75`, verifies list/get identity, lifecycle, omission, cursor, recovery, and compatibility behavior, and uses only a live `<32768` assertion. Independent audit passed; Cycle 803 remains immutable.~~
- [x] ~~[fix:3.0.0] Only six v1 artifact pairs recognized in the legacy Markdown detector. Superseded 2026-07-14: the closed six-pair v1 Markdown→v2 parser remains a compatibility path, while current v3 `agentera state migrate` inventories arbitrary project-local YAML candidates within bounded file and byte limits, preserves custom names in normalized order, and requires exact `--path`, `--artifact`, and `--number` selectors for custom application. `packages/cli/test/state/migrationAuthority.test.ts` covers the published custom-name rule, 256-file/1 MiB/16 MiB bounds, normalized ordering, symlink rejection, and bounded output behavior.~~
- [x] [fix:3.0.0] Repair blocked archive-rollout Plan Task 3 after two failed evaluations. Resolved 2026-07-14: removed speculative...
- [x] [fix:3.0.0] Repair blocked archive-rollout Plan Task 2 after two failed evaluations. Resolved 2026-07-14: Task 1...
- [x] [fix:3.0.0] Repair blocked lossless-storage Plan Task 9 after two failed evaluations. Resolved 2026-07-14: list/compact totals...
- [x] [fix:3.0.0] Repair blocked archive-storage continuation Plan Task 3. Resolved 2026-07-13: prime now shares code-point-safe truncation...
- [x] [fix:3.0.0] Align the plan capability's published YAML format with the typed writer schema. Resolved 2026-07-13:...
- [x] [test:3.0.0] Verify Plan Lifecycle Clarity and Validated Publication closeout. Resolved 2026-07-13: `packages/cli/test/state/planLifecycleContract.test.ts`, lifecycle fixtures, and...
- [x] [fix:3.0.0] Make plan next-task output dependency-aware. Resolved 2026-07-12: status and typed plan writes now return...
- [x] [fix:3.0.0] Persist orchestration retry state through the typed plan interface. Resolved 2026-07-12: `record-evaluation` persists CLI-owned...
- [x] [refactor:3.0.0] Separate plan-level and task-level status mutation contracts. Resolved 2026-07-12: `agentera state plan set-status --task`...
- [x] [fix:3.0.0] Provide deterministic plan lifecycle migration. Resolved 2026-07-12: `agentera upgrade --only artifacts --dry-run|--yes` migrates legacy...
- [x] [fix:3.0.0] Complete the plan lifecycle inventory classifier before resuming "Plan Lifecycle Clarity and Validated Publication"....
- [x] [fix:3.0.0] Reconcile the canonical OpenCode/shared skill topology. Resolved 2026-07-12: the lifecycle authority makes `~/.agents/skills/agentera` canonical...
- [x] [docs:3.0.0] Update root README.md for v3. Resolved 2026-07-12: README, CLI README, upgrade guide, web lifecycle...
- [x] [release:3.0.0] Prepare the final 3.0.0 release surfaces. Resolved 2026-07-12: tracked package and runtime manifests, skill,...
- [x] [docs:3.0.0] Investigate how the "Audit" column in `references/cli/capability-instruction-structure.md` is managed or maintained. Resolved 2026-07-11: stale...
- [x] [refactor:3.0.0] Move `## Personality` sections out of capability instructions.ts and into the vision artifact or...
- [x] [docs:3.0.0] Reconcile bundled SKILL.md with the v3 TypeScript CLI. Resolved 2026-07-11: stale TODO — source...
- [x] [feat:3.0.0] Treat identity voice and emotional-register nuance as first-class vision attributes or introduce a personality...
- [x] [docs:3.0.0] Audit and review ⎈ orchestrate capability instructions from the ground up. Prose lives in...
- [x] [docs:3.0.0] Audit and review ◰ design capability instructions from the ground up. Prose lives in...
- [x] [docs:3.0.0] Audit and review ♾ profile capability instructions from the ground up. Prose lives in...
- [x] [docs:3.0.0] Audit and review ▤ document capability instructions from the ground up. Prose lives in...
- [x] [docs:3.0.0] Audit and review ⛶ audit capability instructions from the ground up. Prose lives in...
- [x] [docs:3.0.0] Audit and review ⧉ build capability instructions from the ground up. Prose lives in...
- [x] [docs:3.0.0] Audit and review ⎘ optimize capability instructions from the ground up. Resolved 2026-07-10: D86....
- [x] [docs:3.0.0] Audit and review ⬚ research capability instructions from the ground up. Resolved 2026-07-09: D83....
- [x] [docs:3.0.0] Audit and review ❈ discuss capability instructions from the ground up. Resolved 2026-07-09: discuss...
- [x] [docs:3.0.0] Backfill the D82 schema-validator rule to status (D79) and vision (D81). Resolved 2026-07-09: scrubbed...
- [x] [fix:3.0.0] `cliDistributionMajor` defaults to 2 when version is missing. Resolved 2026-07-08: replaced the terminal `return...
- [x] [fix:3.0.0] Legacy Python parity checks gated behind opt-in `--legacy-python-parity` flag. `lifecycleAdapters.ts:95-96` — default `validate lifecycle-adapters`...
- [x] [docs:3.0.0] Audit and review ⌂ status capability instructions from the ground up. Resolved 2026-07-06: status...
- [x] [refactor:3.0.0] Rename `prose` to `instructions` and use it consistently. Resolved 2026-07-06: folded into the status...
- [x] [docs:3.0.0] Audit and review ⛥ vision capability instructions from the ground up. Resolved 2026-07-06 per...
- [x] [fix:3.0.0] `extractCorpusParity.test.ts` flaked on its 30000ms `testTimeout`; a single `it` ran both the TypeScript and...
- [x] [fix:3.0.0] Runtime migration regex detection misses partial hand-edits. `runtimeMigration.ts:87-92` `textUsesPythonManagedEntrypoint()` only detects exact v2 patterns...
- [x] [fix:3.0.0] Add `--version` / `version` command to the CLI so consumers can check the installed...
- [x] [fix:3.0.0] Bespoke capability contexts return error instead of degraded output. `prime.ts:43-61` — capability contexts not...
- [x] [fix:3.0.0] Doctor and prime health data sources are independent and can disagree. Resolved 2026-06-30: Decision...
- [x] [fix:3.0.0] `agentera check compact --apply` produces archive entry ordering that `agentera check validate artifact --artifact...
- [x] [fix:3.0.0] Tune trigger patterns and thresholds so common natural-language inputs route correctly under the scoring...
