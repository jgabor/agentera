# TODO

## ⇶ Critical

## ⇉ Degraded

## → Normal

## ⇢ Annoying

- [ ] [fix] Investigate, review, and improve `agentera doctor` output so a ready/healthy status does not always show a `Next:` section that can be interpreted as required action; distinguish optional repair/upgrade guidance from actual required follow-up, and fix command formatting issues such as missing whitespace before `--dry-run`.
- [ ] We should consider removing support for Claude Code: <https://xcancel.com/ClaudeDevs/status/2054610152817619388>

## Resolved

- [x] ~~Analyze session transcripts to identify instances where linting thresholds unnecessarily compress or rewrite relevant information and determine if the thresholds should be increased.~~ · resolved by the transcript/retained-artifact threshold analysis: retained `.agentera/archive` evidence showed repeated `post-audit-flagged` PLAN.md full-plan verbosity pressure, so `agentera lint --artifact <ARTIFACT> --file <PATH>` now uses full-file budgets while `--text` and stdin keep per-entry budgets. Redacted scan/classification helpers preserve aggregate-only evidence and focused tests cover retained PLAN.md pressure, unflagged TODO.md inconclusive pressure, transcript scan privacy, and file-versus-text lint semantics.
- [x] ~~[perf] Reduce AGENTERA_ARTIFACTS raw reads after CLI (4 reads): agents don't know where artifacts live — improve artifact path discovery through `agentera describe` or hej composite output.~~ · resolved by the artifact-location discovery contract: `agentera describe --format json` now exposes `artifact_locations` and schema-entry `location` records, while `agentera query --list-artifacts --format json` returns a structured v2 wrapper with mapped paths, normal read commands, raw-access boundaries, source contract, and caveats. Plain list output remains names-only; focused/relevant tests passed and the closeout records retained benchmark rows plus the no-live-benchmark caveat.
- [x] ~~[perf] Reduce DECISIONS.md raw reads after CLI (2 reads, 24 redundant tokens): ensure `agentera decisions --format json` source_contract makes compacted/missing context boundaries unambiguous.~~ · resolved by the decision source-contract raw-read reduction: `agentera decisions --format json` now separates full-detail completeness from `complete_for_normal_deliberation_context`, exposes a decision context truth table, distinct compacted/missing/filter/satisfaction/missing-artifact boundaries, raw artifact access boundaries, and fallback policy; DECISIONS.md consumers now use returned entries plus `missing_fields`, `compacted`, `caveats`, and `satisfaction.review_needed` instead of raw-reading missing historical context. Focused and full query tests passed, direct source-contract proof showed current compacted decisions are normal-deliberation complete without full-detail overclaim, and refreshed `mage bench:startupState` reported zero raw-after-CLI and zero redundant raw artifact reads with degraded runtime coverage caveats recorded separately.
- [x] ~~[perf] Reduce planera prose re-reads (5 reads in latest benchmark): improve planera routing/guidance discoverability so agents don't re-read prose.md on every invocation.~~ · resolved by the Planera compact startup contract: `agentera hej --format json --capability-context planera` now emits `source_contract.capability_context.startup_contract` with bounded planning levels, step markers, CLI-first orientation, raw plan artifact boundaries, full-plan review/self-audit requirements, handoff expectations, prose authority exceptions, selected-seam rationale, and unsupported-command boundary. Focused tests prove the contract shape, `agentera planera` remains unsupported, and the startup context is available even when `planera/prose.md` is absent from the installed app surface; refreshed `mage bench:startupState` evidence reports `capability_prose_read_counts.planera=0` with degraded runtime coverage caveats separated from product behavior.
- [x] ~~[perf] Reduce PLAN.md raw reads after CLI (7 reads, 37 redundant tokens in latest benchmark): strengthen `agentera plan --format json` source_contract completeness signal so planera agents skip raw plan.yaml reads.~~ · resolved by the plan source-contract raw-read reduction: `agentera plan --format json` now exposes canonical/persisted plan identity, `complete_for_normal_startup_evaluation`, complete summary/entry field coverage, explicit raw-artifact boundaries, and fallback policy; Planera and master Agentera guidance now require complete CLI plan state before any defensive raw `.agentera/plan.yaml` read while preserving raw access for writes, archives, validation, corruption diagnostics, and incomplete CLI state. Focused tests passed and `mage bench:startupState` produced direct retrospective measurement (`PLAN.md` raw-after-CLI 11, redundant 10, recommendation `plan_cli_startup_envelope`) with degraded runtime caveats recorded separately.
- [x] ~~[feat] Surface non-confirmed decisions (open, provisionally satisfied, review-needed) in `agentera hej` dashboard attention items so stale unreviewed decisions don't hide silently in the archive.~~ · resolved by the decision-review attention feature: `agentera hej` now emits one bounded decision satisfaction review attention item plus structured `decision_attention` JSON derived from the same decision satisfaction context as `agentera decisions --format json`, includes missing/open/provisional/unconfirmed/explicit review-needed decisions, excludes explicitly user-confirmed satisfied decisions, preserves `next_action` priority, and leaves `.agentera/decisions.yaml` unchanged; focused verification passed with hej/decision regression tests, guidance tests, package/version metadata tests, touched artifact validators, compaction check/fix as needed, and `git diff --check`, without changing decision satisfaction state, adding a new command, publishing, pushing, tagging, refreshing installed apps/profiles, or editing vision/objective state.
- [x] ~~[feat] Implement `agentera corpus` or `agentera usage` CLI namespace for profile analytics (D48).~~ · resolved by the D48 usage namespace feature: `agentera usage` now exposes existing `scripts/usage_stats.py` analytics through the supported CLI with text report generation, JSON pass-through, `--corpus`, `--project`, missing-corpus guidance, invalid-format guidance, help discoverability, isolated write-path tests, and preserved direct script compatibility; focused verification passed with usage namespace tests, direct usage stats tests, explicit CLI surface tests, package/version metadata tests, touched artifact validators, and `git diff --check`, without implementing `agentera corpus`, auto-building corpus data, refreshing profiles or installed apps, publishing, pushing, tagging, editing vision/objective state, changing decision satisfaction, or doing unrelated D48 validate/verify work.
- [x] ~~[feat] Implement `agentera smoke` or `agentera verify` CLI namespace for eval and smoke gates (D48).~~ · resolved by the D48 verify namespace feature: `agentera verify` now exposes bounded smoke and eval gate families with text/JSON results, safe offline/dry-run defaults, invalid-input guidance, and preserved direct smoke/eval script compatibility; focused verification passed with verify namespace tests, explicit CLI surface tests, package/version metadata tests, safe verify probes, direct smoke/eval probes, touched artifact validators, and `git diff --check`, while `node scripts/smoke_opencode_bootstrap.mjs` retained the expected bounded local failure for absent OpenCode skills, without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, package-manager update, unsupported capability-name CLI command, or D48 validate/corpus/usage work.
- [x] ~~[feat] Implement `agentera validate` CLI namespace for capability/artifact validators (D48).~~ · resolved by the D48 validate namespace feature: `agentera validate capability` and `agentera validate artifact` now provide stable text/JSON validation wrappers over the existing direct validators while preserving artifact labels, capability names, direct script/hook behavior, and invalid-input guidance; focused verification passed with validate namespace tests, CLI/package metadata tests, direct capability and artifact validators, touched artifact validators, and `git diff --check`, without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, package-manager update, unsupported capability-name CLI command, or D48 smoke/verify/corpus/usage namespace work.
- [x] ~~[fix] Investigate `agentera lint --artifact` false-positive advisory for Planera plans: `/tmp/opencode/aila-m13-plan.yaml` still reported `abstraction creep: no concrete anchor` after adding explicit anchors to `ROADMAP.md:523-542`, the queued-message fixture, semantic snapshots, PTY smoke, `mage check`, source docs, and concrete `internal/runtime`, `internal/app`, and `internal/tui` boundaries.~~ · resolved by the Planera lint anchor patch: `scripts/self_audit.py` now recognizes repo-style path anchors such as `internal/runtime`, `internal/app`, and `internal/tui`; `tests/test_self_audit.py` covers anchored PLAN.md pass and unanchored generic-prose advisory failure cases; focused verification passed with `uv run pytest tests/test_self_audit.py -q` (23 tests), lint contract probes for anchored/generic/strict/file/stdin/text modes, package/version metadata tests, artifact validators, and `git diff --check`, without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, or unsupported capability-name CLI command.
- [x] ~~[chore] Audit codebase for leftover "bundle" terminology and legacy artifact names (e.g. DECISIONS.md) per D47 app-home migration — replace with app-home vocabulary.~~ · resolved by the D47 app-home vocabulary cleanup: inventory classified required terms and surfaces, live app-home vocabulary was updated across current docs/guidance/diagnostics/package text while preserving compatibility identifiers and historical records, focused verification reported 346 passing tests, and Task 4 closeout recorded metadata without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective edit, decision satisfaction change, or unsupported capability-name CLI command.
- [x] ~~[fix] D46 handoff wording implementation doesn't appear to trigger in practice — review whether the handoff guidance in SKILL.md is being followed by runtime agents or if the prose needs a different enforcement mechanism.~~ · resolved by the D46 handoff wording enforcement patch: semantic fixture defaults, fixture documentation, and hej routing tests now require glyph plus canonical capability names such as `⧉ realisera`, reject standalone slash-capability handoff labels like `/realisera`, preserve `/agentera <alias>` for explicit route documentation, and record focused regression/gate evidence without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective-state edit, or unsupported capability-name CLI command.
- [x] ~~[fix] Implement Decision 53 in `.agentera/decisions.yaml`, `scripts/agentera`, and `hooks/compaction.py`: add decision-entry satisfaction state, provisional evidence in `agentera decisions --format json`, user confirmation, compaction protection for open/provisional decisions, and review triggers before compaction, in health audits, and at release closeout.~~ · resolved by the Decision satisfaction tracking patch: decisions now carry explicit satisfaction state and review-needed semantics, decisions JSON exposes non-inferential satisfaction context, shared compaction protects unresolved decisions and reports protected overflow, Inspektera and Dokumentera surface stale protected-decision review pressure, capability guidance preserves satisfaction authority boundaries, and focused regression/gate evidence is recorded without publication, push, tag, installed-app refresh, profile refresh, vision edit, objective-state edit, or unsupported capability-name CLI command.
- [x] ~~[feat:2.3.12] Add a Realisera execution-context source contract that exposes the current task's acceptance criteria, constraints, expected verification, allowed files/scope, and progress evidence requirements so implementation agents can start from CLI state instead of raw plan/progress reads.~~ · resolved by the 2.3.12 Realisera execution-context source-contract patch: `agentera hej --format json --capability-context realisera` now exposes selected work, task details, acceptance criteria, constraints, verification expectations, artifact update requirements, progress logging requirements, changelog boundary, conservative scope boundary, fallbacks, caveats, and raw-read-last-resort policy without adding a capability-name CLI command.
- [x] ~~[feat:2.3.11] Add an Optimera benchmark-context source contract or equivalent CLI summary for startup benchmark history/latest results so optimization work can inspect rates, token estimates, runtime caveats, and recommendation triggers without reading user-local `latest-report.json` directly.~~ · resolved by the 2.3.11 Optimera benchmark-context source-contract patch: `agentera hej --format json --capability-context optimera` now exposes retained startup benchmark source status, latest report summary, aggregate history, runtime coverage caveats, state-access rates, token-impact estimates, comparison status, recommendation action, manual refresh guidance, privacy boundary, fallbacks, and raw-read-last-resort policy; the 2.3.12 Realisera execution-context source-contract work is now closed separately.
- [x] ~~[feat:2.3.10] Add an Inspektera evidence-context source contract that packages plan criteria, progress verification, docs/health findings, protected-state/version checks, and residual risks so evaluations can stay CLI-first and avoid defensive raw artifact inspection.~~ · resolved by the 2.3.10 Inspektera evidence-context source-contract patch: `agentera hej --format json --capability-context inspektera` now exposes evaluation target, plan criteria, progress verification, docs and health state, TODO state, protected/version checks, compacted decision caveats, attributed residual risks, fallback commands, and raw-read-last-resort policy while the 2.3.12+ source-contract train remains open.
- [x] ~~[feat:2.3.9] Add Dokumentera closeout-context source contracts for TODO, docs, changelog, and progress state so final synchronization can verify mappings, benchmark evidence, remaining blockers, and no-release boundaries through CLI output before any raw artifact fallback.~~ · resolved by the 2.3.9 Dokumentera closeout-context source-contract patch: `agentera hej --format json --capability-context dokumentera` now exposes artifact mappings, version policy, TODO blockers, changelog and release boundaries, progress and benchmark evidence, fallback commands, caveats, non-empty evidence flags, and raw-read-last-resort policy while the 2.3.10+ source-contract train remains open.
- [x] ~~[feat:2.3.8] Add an Orkestrera orchestration-context source contract that combines dependency-ready plan tasks, latest progress verification, health/TODO/decision caveats, retry state, and evaluation requirements so task selection and Inspektera handoff do not need raw artifact reads.~~ · resolved by the 2.3.8 orchestration-context source-contract patch: `agentera hej --format json --capability-context orkestrera` now exposes dependency-ready tasks, blocked reasons, selected task, progress verification, retry-state provenance, evaluator handoff fields, state-family caveats, fallback commands, and raw-read-last-resort policy while the 2.3.9+ source-contract train remains open.
- [x] ~~[feat:2.3.7] Add a Resonera decision-context source contract so `agentera decisions --format json` exposes outcome, reasoning, alternatives, feeds-into, confidence, and downstream consequences without requiring raw `.agentera/decisions.yaml` reads or git-history fallback during deliberation.~~ · resolved by the 2.3.7 decision-context source-contract patch: decisions JSON now exposes stable outcome, reasoning, alternatives, confidence, feeds-into, downstream consequence references, source-contract metadata, compacted-entry missing-field caveats, and Resonera startup guidance while the 2.3.8+ source-contract train remains open.
- [x] ~~[fix:2.3.6] Make artifact compaction a repo-level invariant instead of a runtime-hook side effect. Add an explicit `agentera compact --check|--fix --json` or equivalent maintenance command that resolves artifact paths through `.agentera/docs.yaml`, supports current v2 YAML artifacts plus `TODO.md` Resolved, exposes real explicit validator CLI behavior for `--artifact/--file`, and wires a pre-commit or pre-push check so `apply_patch`, scripts, generated edits, and direct file changes cannot bypass 10/40/50 enforcement.~~ · resolved by the 2.3.6 artifact compaction enforcement patch: shared check/fix compaction, explicit artifact validation, runtime hook convergence, current artifact remediation, repository gate automation, focused regressions, and local gate/compact verification now enforce the 10/40/50 retained-entry contract while the 2.3.7+ source-contract train remains open.
- [x] ~~[fix:2.3.5] Demote `AGENTERA_HOME` to a validated override in `scripts/install_root.py` instead of the normal app-home source of truth. Normal installed execution should resolve directly to the platform app directory (`XDG_DATA_HOME`/macOS Application Support/Windows AppData) unless an explicit valid override is supplied; runtime adapters such as OpenCode must not blindly propagate stale legacy values like `~/.agents/agentera`, and doctor/upgrade/tests should preserve custom-install and hermetic-test support without making stale parent-process environments authoritative.~~ · resolved by the 2.3.5 install-state reliability patch: normal installed execution now treats stale inherited deprecated-default `AGENTERA_HOME` values as non-authoritative residue, preserves explicit valid custom app homes and hermetic tests, keeps doctor read-only and upgrade as the repair writer, and verifies default/custom/unsafe app-home behavior with focused regressions.
- [x] ~~[fix:2.3.5] Add logic to semantically differentiate between Agentera app upgrades and broken or stale state. Currently, a new version is interpreted as a "stale" installation", causing the agent to recommend a "safe fix" instead of an "upgrade" (e.g., `2.3.2` -> `2.3.4`).~~ · resolved by the 2.3.5 upgrade-versus-repair semantics: valid older app files now report update-needed guidance, broken or unverifiable app files keep repair/blocking language, structured hej/doctor output distinguishes update-needed from repair states, and release metadata is aligned to 2.3.5 without publication, tag, push, installed app refresh, vision edit, or objective-state edit.
- [x] ~~[feat] Eliminate the remaining `PLAN.md` redundant raw startup reads after the CLI startup state envelope and runtime schema/token-estimate work.~~ · resolved by making `agentera plan --format json` complete for normal `PLAN.md` startup/evaluation context: the command now exposes full plan metadata in `summary`, task acceptance/evidence in `entries`, and a selectable `source_contract` with `complete_for_plan_artifact=true` and `raw_artifact_reads_required=false`. Focused CLI contract tests passed with 88 selected tests, the mixed-window benchmark recorded `245` estimated redundant tokens saved versus the previous row, and the clean post-fix probe produced zero raw/redundant artifact reads in a tiny weak-evidence window.
- [x] ~~[test] Add a manual startup state benchmark suite for Decision 52.~~ · resolved by the manual `mage bench:startupState` benchmark suite: explicit runtime/path approval is required before local history access, fixture-backed tests cover consent refusal, successful synthetic measurement, missing-store degradation, aggregate JSONL schema, privacy exclusions, and no repository-local generated outputs, and durable retained evidence is limited to user-local `runs.jsonl`, `latest-report.json`, and `latest-report.md`; the separate CLI startup state-envelope follow-up remains open.
- [x] ~~[fix] Remove shell startup mutation from Agentera runtime setup and repair.~~ · resolved by the shell-rc removal plan: setup, doctor, upgrade preview/apply, repair guidance, managed runtime refresh, stale-remnant cleanup, and default non-live smoke now keep shell startup files user-owned and unchanged; stale exact deprecated-default `AGENTERA_HOME` is treated as recoverable residue, custom unknown app homes remain blocked, package-manager updates stay opt-in, and final sync records no release-target selection or version-bearing file changes.
- [x] ~~[feat:2.3.x] Add Profilera corpus mining parity for OpenCode and GitHub Copilot.~~ · resolved by the Profilera corpus runtime parity plan: extraction now reads OpenCode `opencode.db` and GitHub Copilot `session-store.db` stores read-only, preserves bounded status/reason diagnostics for missing, locked, sparse, and schema-divergent stores, proves Claude/Codex/OpenCode/Copilot fixture parity in focused tests and default smoke, and records no release-target selection or version-bearing file changes.
- [x] ~~[fix:2.3.1] Recover from runtime-injected stale old-default `AGENTERA_HOME` without asking users to unset environment variables.~~ · resolved by the 2.3.1 runtime-injected app-home recovery work: deterministic tests cover the exact deprecated default, upgrade previews platform app-home recovery when no explicit `--install-root` was supplied, doctor remains read-only, explicit install-root values stay authoritative, safe user-data-only app homes can receive managed code under `app/`, OpenCode/runtime defaults no longer prefer the old root layout, and skill guidance presents app-home recovery instead of stale-env unsetting.
- [x] ~~[fix:2.3.0] Retire the deprecated default `~/.agents/agentera` location when the platform app home is selected, and confirm supported v1 Markdown project artifacts are migrated automatically.~~ · resolved by the 2.3.0 legacy default retirement follow-up: default bundle upgrade previews and applies `retire-legacy-default-app-home`, moves known user state into the selected app home before removing managed legacy files, leaves preview mode no-write, and regression coverage proves canonical v1 Markdown artifacts are handled by the artifacts phase.
- [x] ~~[feat:2.3.0] Implement Decision 47 app-home migration: replace live bundle terminology with app terminology, redefine `AGENTERA_HOME` as the durable platform data home, place the managed Agentera app under `AGENTERA_HOME/app`, keep profile/usage/history/corpus data at the home root, make `agentera doctor` detect legacy bundle-root installs as `migration_required` with exact dry-run/apply commands, and make `agentera upgrade` own idempotent migration writes.~~ · resolved by 2.3.0 Tasks 1-3: app-home public output, doctor legacy-root diagnostics, and upgrade-owned idempotent migration now agree on `AGENTERA_HOME` as app home, `$AGENTERA_HOME/app` as managed app root, and home-root user data; no package update, installed app refresh, publication, tag, push, vision edit, or objective-state edit occurred.
- [x] ~~[fix:2.3.x] Make artifact schema discovery explicit for installed capabilities. During `visionera`, the vision artifact schema had to be found by manually searching multiple roots before locating `schemas/artifacts/vision.yaml`; the capability prose references `skills/agentera/schemas/artifacts/vision.yaml` without giving an installed-root-independent lookup path or CLI command. Add a routine `agentera` command or capability-local reference that resolves the schema path from the same root used for the active invocation.~~ · resolved by Task 5: producing capability prose points agents at `agentera describe --format json` artifact schema entries, and missing schemas are reported against the active app-home/managed-app/skill-root/runtime-root model instead of requiring manual root searches.
- [x] ~~[fix:2.3.x] Add a doctor check for split Agentera install roots used by capability invocation. One session involved the skill runtime under `~/.config/opencode/skills/agentera/`, the durable skill copy under `~/.agents/skills/agentera/`, and the CLI bundle under `~/.agents/agentera/`; this made it unclear which prose, schemas, and helper scripts were authoritative. `agentera doctor` should report these roots, version/marker status, and missing expected helper/schema surfaces before a capability asks agents to use them.~~ · resolved by Tasks 2 and 5: doctor reports app, managed app, skill, runtime, and source roots for install/runtime diagnostics, while helper and schema discovery failures point at the same active app model without broadening doctor beyond Decision 48's boundary.
- [x] ~~[fix:2.3.x] Ship or remove the referenced `scripts/self_audit.py` capability helper.~~ · resolved by exposing the self-audit checks through `agentera lint --artifact <ARTIFACT>` with stdin/file/text input, bounded diagnostics, JSON output, and strict mode; producing capability prose now points at the CLI instead of a hidden installed skill-root helper path.
- [x] ~~[fix:2.3.x] Finish the app-home public JSON surface migration before 2.3.0.~~ · resolved by removing public `installRoot` and `installRootSource` from `agentera doctor --json` and nested `agentera hej --format json` app status, updating CLI-visible vocabulary labels to app-home wording, and extending `scripts/validate_app_home_contract.py` to fail on stale structured output.
- [x] ~~[feat:2.2.3] Suggest profilera run when stale: track time or cycles since last profilera run and nudge when it's due.~~ · resolved by `agentera hej` profile staleness output: stale profiles now emit a direct "suggest running profilera to refresh PROFILE.md" attention item, structured `profile.suggested_action`, configurable threshold evidence, deterministic date tests, and invalid threshold fallback to the default.
- [x] ~~[test:2.2.3] Release-readiness gate: run opt-in all-runtime live install proof for Claude Code, OpenCode, Copilot CLI, and Codex CLI.~~ · resolved by adding `run_claude_live_section` to `scripts/smoke_live_hosts.py` (the other three runtimes were already covered); `--live --yes` now exercises all four runtimes with isolated temp state, explicit consent via cost_gate, SKIP for auth/network failures, and Codex 0.129.0 trusted hook-state setup for the temporary apply_patch hook wrapper. Passing evidence does not require a release; only detected product/doc defects feed a patch or minor.
- [x] ~~[test] Add an explicit opt-in Claude Code live model-host smoke when Claude Pro/Max or API access is available.~~ · resolved by the 2.2.3 live-smoke harness work: `scripts/smoke_live_hosts.py --live --yes` includes a Claude Code section behind explicit consent, with temporary `HOME`, `XDG_*`, `CLAUDE_CONFIG_DIR`, `AGENTERA_HOME`, and `CLAUDE_PLUGIN_ROOT` state plus SKIP handling for missing auth/network access.
- [x] ~~[fix:2.2.2] Stop repeated fresh-project hej from routing back to visionera.~~ · resolved by saved-context routing: first-run empty projects still route to visionera, saved context without vision routes to resonera, and vision-only state avoids the hej-to-visionera loop.
- [x] ~~[fix:2.2.2] Wire TODO Resolved compaction into artifact writes.~~ · resolved by invoking todo-resolved compaction after valid TODO writes, preserving open sections and type-prefixed summaries while avoiding generated IDs.
- [x] ~~[fix] Fix Copilot current-checkout plugin smoke blocked by escaping skills path.~~ · fixed in Cycle 153; root `plugin.json` loads shared `skills/` through `copilot --plugin-dir`
- [x] ~~[test] Add live Copilot and Codex host behavior smoke tests.~~ · narrowed by Cycle 152; Codex `$hej` and Copilot installed skills passed, current-checkout Copilot plugin loading remains blocked
