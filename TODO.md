# TODO

## ⇉ Degraded

- [ ] [fix:3.0.0] Align the plan capability's published YAML format with the typed writer schema. `agentera prime --context plan --format json` currently teaches `overall_acceptance` as a list and `depends_on` as integer lists, while `agentera state plan create` requires `overall_acceptance` to be a string and dependencies to be strings; it also rejects `rejected: []` without explaining that an empty section must be omitted. An approved plan produced directly from the capability example therefore fails its first publication attempt. Generate the example from the schema or add a contract test that submits the documented shape through the writer.
- [ ] [fix:3.0.0] Make plan next-task output dependency-aware. After Task 1 was marked `blocked`, `agentera state plan set-status --task 1 --status blocked --format json` still returned dependency-blocked Task 2 as `state.next_pending_task`. Consumers must independently consult orchestration's dependency-ready queue to avoid treating it as executable. Either return the first dependency-ready pending task, return null when none is eligible, or rename the field to `first_pending_task` so it does not imply readiness.
- [ ] [fix:3.0.0] Persist orchestration retry state through the typed plan interface. The orchestrate contract requires incrementing evaluation retries and blocking a task after two failures, but plan task state exposes no CLI-owned attempt count, last verdict, or last failure. Audit context therefore reports retry state as unavailable, and enforcement depends on conversation memory that can disappear after restart or compaction. Add retry-safe typed mutations and expose provenance in orchestration context.
- [ ] [fix:3.0.0] Only 6 v1 artifact pairs recognized in migration. `migrateArtifactsV2ToV3.ts:33-40` `V1_ARTIFACT_PAIRS` only recognizes 6 specific MD→YAML pairs. Custom v1 artifact names are invisible to migration detection. `listV2YamlArtifacts()` (line 137-155) only recognizes 5 YAML names plus `vision.yaml`. Fix: either enumerate from a directory scan or document the closed set as intentional. (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [fix:3.0.0] No diagnostic when AGENTERA_HOME is set but managed script is missing or corrupt. `resolve.mjs:82-88` silently skips to the next resolution strategy when `findAppHomeScript` returns null. The user might expect the app-home to work. Fix: emit a stderr diagnostic ("AGENTERA_HOME is set but no managed script at $AGENTERA_HOME/app/scripts/agentera") before falling through. (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [refactor:3.0.0] Replace the full-materialized `corpus.json` dump with tiered extraction. The current design writes a single 500+ MB JSON file (`extractCorpus/cli.ts:159` `fs.writeFileSync`) that every CLI reader path rejects via the 64 MB `MAX_CORPUS_READ_BYTES` cap (`usageStats.ts:350`), leaving `agentera usage`, `agentera prime` coverage, and `agentera report` status dead on real corpora; only the profilera agent reads the raw file directly, and it already self-imposes a 523 KB `synthesis_digest.jsonl` subset to work around the size. Replace with three tiers: (1) **Metadata envelope** — coverage audit, family counts, runtime statuses, extraction timestamp; produced by every `report refresh`; sized in KB; serves `prime` coverage, `report` status, and `usage` aggregations. (2) **Bounded signal stream** — scored jsonl of high-signal records only (`signal_type` ∈ {`correction`, `question`, `decision`}, already classified by `signalType()` at `extractCorpus/core.ts:336-341`); capped at ~500–1000 records matching the profilera instructions' existing prioritization rule ("if total records exceed 500, prioritize high-signal records"); serves profilera synthesis and the glossary extraction pass. (3) **Raw dump** — opt-in via explicit `--emit raw` to a caller-specified path; not the default, not read by any normal code path. Compute `usage` skill-invocation aggregations (counts, exit statuses, trigger types) during extraction and store them in the metadata envelope so `usage` reads the envelope, not the raw corpus. Formalize `synthesis_digest.jsonl` as a first-class CLI output of tier 2 rather than an agent-produced intermediate. Update `extractCorpus.test.ts`, `report.test.ts`, `usageStats.test.ts`, `orientationCorpusCoverage.test.ts`, and `compactParity.test.ts` for the new output shape. Update profilera `instructions.ts` Step 1–3 prose to reference the metadata envelope and signal stream instead of `corpus.json`. (Branch: `feat/v3` — implement on `feat/v3`.)

## → Normal

- [ ] [refactor:3.0.0] Separate plan-level and task-level status mutation contracts. `agentera state plan set-status` currently accepts plan lifecycle values and task execution values through one `--status` field; `state plan explain --verb set-status` lists `active`, `complete`, `in_progress`, `pending`, and `blocked` together, with applicability inferred only from the optional `--task`. Introduce distinct operations or context-specific explain output before migrating plan lifecycle to `open|complete`, so agents cannot apply task statuses to plans or lifecycle statuses to tasks.
- [ ] [release:3.0.0] Replace `scripts/agentera` on `main` with a thin Python deprecation stub (~15 lines) that prints the v3 migration message and exits 2. **Gate: only execute when 3.0.0-stable is ready to promote and `@latest` retags to the TS bundle.** Must happen as the final commit before the v3→main merge. The full 8,528-line v2 Python CLI and all `scripts/*.py` modules get deleted; the stub preserves `uvx --from git+... agentera` and `uv run scripts/agentera` users from hard crashes, directing them to `npx -y agentera`. Also delete `src/agentera/`, `pyproject.toml`, `uv.lock`, and the 0.0.1 npm shim's Python delegation path. Migrate `.codex/hooks.json` from `uv run .../validate_artifact.py` to `npx -y agentera@next hook validate-artifact` (or `@latest` post-promotion) as part of the same commit.
- [ ] [refactor:3.0.0] Split `lifecycleAdapters.ts` barrel (107 lines) — extract `lifecycleMain()` orchestrator (line 61-107) into its own module. The barrel mixes re-export responsibility with procedural orchestration logic, flagged as structural debt in Audit 24. The barrel should be a pure re-export surface; `lifecycleMain` should live in a dedicated orchestrator module. (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [refactor:3.0.0] Refactor plan levels from skip/light/full to light/normal/full. Remove skip mode so every direct plan invocation produces at least a light plan: light for trivial or localized work, normal for clear single-cycle work, and full for multi-cycle or architecture-spanning work. Update the runtime startup contract, plan instructions, schemas, validation, and tests to use the new three-level taxonomy consistently.
- [ ] [feat:3.0.0] Add light/normal/full levels to research. Light handles a focused lookup with bounded evidence, normal compares multiple relevant sources, and full performs broad synthesis across competing approaches. Levels control evidence breadth, analysis depth, and verification—not prose length. Update the startup contract, instructions, schemas, and tests.
- [ ] [feat:3.0.0] Add light/normal/full levels to audit. Light assesses one named finding or dimension, normal assesses the relevant selected dimensions, and full performs a repository-wide multi-dimensional audit. Levels control inspected scope and evidence depth while preserving finding-confidence and citation requirements. Update the startup contract, instructions, schemas, and tests.
- [ ] [feat:3.0.0] Add light/normal/full levels to document. Light handles one localized documentation correction, normal covers a feature or coherent document set, and full performs repository-wide generation or drift auditing. Levels control document scope and verification breadth while preserving approval gates. Update the startup contract, instructions, schemas, and tests.
- [ ] [feat:3.0.0] Add light/normal/full levels to profile. Light incrementally incorporates recent high-signal evidence, normal performs the standard synthesis workflow, and full rebuilds from the available corpus with complete validation. Levels control corpus breadth, synthesis depth, and verification—not output verbosity. Update the startup contract, instructions, schemas, and tests.
- [ ] [refactor:3.0.0] Audit and retire `skills/agentera/references/contract.md` (79KB, 1048 lines, Sections 1-23). Survey found ~60% of sections are duplicated into `protocol.yaml` (Sections 1, 2, 3, 11, 13, 18), `SKILL.md` + `AGENTS.md` (Section 5), or are stale v2-era content using Swedish capability names and `$AGENTERA_PROFILE_DIR` (Section 6). ~40% (Sections 4 partial, 19, 20, 22, 23) may still be uniquely authoritative but use v2-era naming. Cut duplicated/stale sections; migrate unique sections (Staleness Detection, Behavioral Verification Gate, Session Corpus Contract, Pre-spawn Git Commit) into `references/cli/` with v3 naming; retire contract.md when empty. Surfaced during the D81 vision audit — the D79/D81 direct-contract philosophy removed the contract.md pointer from status and vision; the 10 remaining unaudited capabilities still carry the boilerplate pointer and will lose it during their respective audits. (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [feat:3.0.0] Add progress cycles tracking to more capabilities, not just `build`.
- [ ] [fix:3.0.0] Harden `loadSuiteVersion` (`appModel.ts:72-95`) to surface WHY it returned null on a corrupted/missing `registry.json` version-authority file. Current swallow-all catch at line 81 returns null silently for JSON parse failures, missing `skills` array, wrong `authority.selector`, and missing file — none of which reach the operator. Add structured diagnostic strings (e.g., `version_authority_file_missing`, `version_authority_parse_failed`, `skills_array_empty`, `selector_mismatch`) so callers like `cliDistributionMajor`'s secondary-signal fallback can distinguish "v2 with no package.json" from "v3 with corrupted registry.json." Surfaced during the `cliDistributionMajor` plan (Step 2, scope-boundary decision: kept out of plan scope; tracks as separate concern). (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [feat:3.0.0] Add `statusline` command to show data such as current plan status, health, etc. that can be used by runtimes, similar to `https://github.com/ryoppippi/ccusage`, `https://github.com/avinashbest/cli-status-bar`, and `https://cursor.com/changelog/04-14-26`
- [ ] [refactor:3.0.0] Rename `agentera app-home` command to `agentera path`
- [ ] [feat:3.0.0] Add macOS and Windows GitHub Actions jobs for `pnpm -C packages/cli test` (and related gates); CLI tests are cross-platform-hardened on darwin and linux.
- [ ] [feat:3.0.0] Decide `@agentera/mobile` npm publish identity (private workspace vs npm vs deploy-only).
- [ ] [chore:3.0.0] Remove `packages/mobile` temporary `vite.config.ts` hook plumbing when SvelteKit scaffold lands (see `packages/mobile/TODO.md`; scaffold tracked there, not duplicated here).
- [ ] [docs:3.0.0] Decide long-term editor skill-bundle role (coexist vs eventual sunset) — horizon item, not blocking mobile v1.
- [ ] [docs:3.0.0] Surface the four-question update/repair wording template (What happened / What the preview did / What the recommended fix will do / What it will not do) in `agentera prime --format json` when `app.status` is `outdated` or `repair_needed`, or document in the collapsed SKILL.md that the host must read `agentera prime --context status --format json` `prose` for the framing. The JSON provides `app.approval` (one-line) and `app.signals[].message` — not the structured four-question template or recommendation-first label convention the old SKILL.md specified. (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [chore:3.0.0] Remove deprecated `PROFILERA_PROFILE_DIR` alias fallback from `setProfileDir()` in `.opencode/plugins/agentera.js` before v3 stable release. Task 12 of the D70 cleanup plan added the fallback as a migration bridge; the upgrade migration step (Task 13) rewires config files automatically. Before v3 stable, verify no v2 users still depend on the old name, then remove the fallback code and the `profileraDeprecation` flag. The v2→v3 upgrade window is the only period where the alias is needed.
- [ ] [chore:3.0.0] Address pre-existing lint findings surfaced by the `vp lint` migration in `packages/cli`. The toolchain-cutover plan (Task 3) wired `scripts.lint` → `vp lint` and recorded a baseline of 101 warnings / 0 errors over 190 files (exit 0) at `packages/cli/.lint-baseline.txt`; `vp fmt --check src/` reports 151 files would be reformatted. Non-regression is the gate held here — address the findings (unused imports, regex-preference, control-char, etc.) in a separate cleanup pass, not as part of the toolchain migration.
- [ ] [fix:3.0.0] Remove deprecated `issues` alias from prime JSON output. The resolved item (2026-06-27) kept `issues` as a deprecated alias with stderr warning; the stable-cut removal was deferred but never tracked as an open item. A public beta should not ship a deprecated field alias with no removal date.
- [ ] [test:3.0.0] Add an end-to-end v2→v3 migration integration test that validates a representative v2 project upgrades cleanly: artifacts migrate (MD→YAML with backups), runtime hooks rewire to the npm entrypoint, no data loss, and `agentera doctor` passes post-migration. The five Degraded migration fixes address edge cases individually but no test proves the full upgrade path works for a real v2 user. (Branch: `feat/v3` — implement on `feat/v3`.)
- [ ] [feat:3.0.0] Use git `user.name` and `user.email` for `confirmed_by` fields in `decisions.yaml`
- [ ] [feat:3.0.0] Detect "You're right", "my mistake", "You're right to push back" responses during profiling, to add generalized rules to the profile to correct the model's behavior.
- [ ] [feat:3.0.0] Add a glossary synthesis pass to profilera (Full mode, after Step 4) that persists a `## Glossary` section to the user-level `PROFILE.md`. **Prerequisite: tiered extraction (see Degraded).** Source: the bounded signal stream (tier 2) — not the raw `corpus.json` dump. Glossary-worthy terms are surfaced from two signals: (1) correction/clarification events in `conversation_turn` records where the agent named a term and the user corrected or disambiguated it (e.g., "you said 'account' — Customer or User?"); these records are already classified `signal_type: "correction"` by the extractor. (2) Stable recurring terms appearing across `instruction_document` and `project_config_signal` source families in the signal stream. Each entry records the term, the user's definition/usage, one evidence anchor (record id + date), and a confidence along the existing CS1–CS5 scale. Skip the pass entirely when the project being profiled has `.agentera/glossary.yaml` registered in `.agentera/docs.yaml` — that signals an established convention and the user-level glossary should defer to it. The glossary section is independent of the decision-pattern entries and does not affect confidence metadata elsewhere in `PROFILE.md`; it is consumed read-only by downstream capabilities the same way the decision profile is. Schema change to profilera's output contract — add a regression test that a signal-stream fixture with a known correction event produces the expected glossary entry.
- [ ] [feat:3.0.0] Add terminology-drift detection to audit (alongside the existing finding families) and emit findings that propose canonical terms; persist the resolved canonical set as `.agentera/glossary.yaml`. No conversation history needed — this is a structural codebase scan, independent of corpus.json and the tiered-extraction work. Audit detects drift when the same concept carries multiple names across the codebase (e.g., `Dict` vs `JsonValue` vs `Record<string, any>`), or when shipped code uses a term that diverges from any glossary the user carries in their `PROFILE.md`. Audit emits findings with proposed canonical term + list of drift variants + evidence anchors (file:line), using the existing severity/confidence vocabulary; the canonical set lands in `.agentera/glossary.yaml` only after `discuss` confirms each proposal, mirroring how audit findings flow through discuss before becoming decisions. Register `.agentera/glossary.yaml` in `.agentera/docs.yaml` artifact mapping so profilera's skip rule and other capabilities resolve it through the standard path. Add a `v1LegacyCruft` scan guard so a repo cannot reintroduce drift variants once `discuss` has confirmed the canonical term.
- [ ] [feat:3.0.0] Add real-time term disambiguation to the consuming capabilities (discuss, plan, build) as a pre-processing step that cross-references incoming user terms against both glossaries and flags ambiguity before proceeding. **Prerequisites: profilera glossary and audit glossary.** This is the consumer that makes the two glossary sources actionable — neither profilera nor audit can answer "you said 'account' — Customer or User?" on their own; profilera captures how the user talks, audit establishes how the codebase talks, but neither detects when a user's term is ambiguous against the project's concept set. The check runs when a capability receives user input containing a domain term: (1) look up the term in the project glossary (`.agentera/glossary.yaml`); (2) if absent or ambiguous, fall back to the user's glossary (`PROFILE.md` `## Glossary`); (3) if the term maps to multiple distinct concepts, emit an advisory asking the user to clarify before proceeding — "You said 'account' — do you mean Customer or User? Those are different things." Precedence: project glossary is authoritative for project-scoped work; user glossary fills gaps when the project glossary is absent or doesn't cover the term; conflicts between the two (user says `account = User`, project says `account = Customer`) surface as a tension flagged to the user, not silently resolved. Scope: a shared glossary-lookup helper (resolved through `.agentera/docs.yaml` mapping) consumed by the three capabilities' startup context; no new artifact written. The advisory is non-blocking for capabilities that cannot pause for user input mid-cycle (e.g., autonomous build) — in those cases the ambiguity is logged as a caveat in the cycle evidence and surfaced in the next prime attention item.
- [ ] [refactor:3.0.0] Investigate what scenarios that can lead to "blocked by manual review" that are overly-conservative or -defensive, and could be automated or simplified to improve user experience during upgrades, migrations, etc.

## ✓ Resolved

- [x] ~~[fix:3.0.0] Complete the plan lifecycle inventory classifier before resuming "Plan Lifecycle Clarity and Validated Publication". Resolved 2026-07-12: `packages/cli/test/state/planLifecycleContract.test.ts` selects `.opencode/plugins/agentera.js`, prunes `.wrangler` and `*.tgz`, and reports 908 scanned files, 200 candidates, and zero unclassified.~~
- [x] ~~[fix:3.0.0] Reconcile the canonical OpenCode/shared skill topology. Resolved 2026-07-12: the lifecycle authority makes `~/.agents/skills/agentera` canonical for all four runtimes, treats runtime-specific locations as additional discovery paths, and requires complete diagnosis before the support floor can pass.~~
- [x] ~~[docs:3.0.0] Update root README.md for v3. Resolved 2026-07-12: README, CLI README, upgrade guide, web lifecycle docs, and mobile capability vocabulary now describe the npm command tree, one Cursor identity, canonical skill, bounded prime/detailed doctor split, and ownership-safe upgrade flow.~~
- [x] ~~[release:3.0.0] Prepare the final 3.0.0 release surfaces. Resolved 2026-07-12: tracked package and runtime manifests, skill, registry, changelog, docs mappings, bundle sources, and validation expectations keep suite surfaces at 3.0.0 while the npm development package advances to 3.0.0-dev.17; publication, tagging, and dist-tag promotion remain separate.~~
- [x] ~~[docs:3.0.0] Investigate how the "Audit" column in `references/cli/capability-instruction-structure.md` is managed or maintained. Resolved 2026-07-11: stale TODO — the document defines the table as incrementally maintained as capability audits complete, and all 12 capability rows now record their completed audit decisions.~~
- [x] ~~[refactor:3.0.0] Move `## Personality` sections out of capability instructions.ts and into the vision artifact or a personality artifact ("SOUL.md"). Resolved 2026-07-11: stale TODO — the discuss and orchestrate rewrites removed their standalone Personality sections and delegate conversational voice to the vision artifact's `identity.voice`; no separate SOUL.md is needed for this cleanup.~~
- [x] ~~[docs:3.0.0] Reconcile bundled SKILL.md with the v3 TypeScript CLI. Resolved 2026-07-11: stale TODO — source and bundled SKILL.md are synchronized, use the v3 `prime`, `state`, `schema`, and `check` surfaces, and contain none of the cited Python managed-app commands.~~
- [x] ~~[feat:3.0.0] Treat identity voice and emotional-register nuance as first-class vision attributes or introduce a personality artifact ("SOUL.md"). Resolved 2026-07-11: stale TODO — the vision schema and create/refine instructions already require first-class `identity.personality`, `identity.voice`, and `identity.emotional_register` fields; capability voice delegates to that identity.~~
- [x] ~~[docs:3.0.0] Audit and review ⎈ orchestrate capability instructions from the ground up. Prose lives in `packages/cli/src/capabilities/orchestrate/instructions.ts` (renamed from `orkestrera`); verify multi-cycle orchestration dispatch, chain/handoff/suggest verbs, and plan/experiment state integration; reconcile drift against `agentera prime --context orchestrate --format json` output. Resolved 2026-07-10: D91. Rewrote instructions following six-section spine, direct-contract delegation, voice delegation. 28,544→18,497 chars (-35%). v2 drift cut: standalone Visual identity (fold into Purpose); skill introduction line; `agentera decisions`→`agentera state decisions`; `agentera progress`→`agentera state progress`; `agentera describe`→`agentera schema`; `agentera query`→`agentera state query`; canonical filenames (PLAN.md/PROGRESS.md/HEALTH.md/DECISIONS.md/VISION.md/DOCS.md)→artifact-id; contract.md pointer; Contract values token enumeration→protocol.yaml ref; artifact-path-resolution subsection (SKILL.md owns); orchestration context 9-field enumeration+4-step→2-sentence source-contract delegation; `complete_for_normal_deliberation_context`→`complete_for_orchestration_context`; Personality section "the sharp colleague" (D81); Narration voice 3× (D81); "Keeping the orchestrator lean" table→folded into Safety rails; runtime subagent table compressed (D85 propagation); "Agentera v2 never writes"→dropped v2 ref; NEVER→MUST NOT (D71); `// Source: D65` comment header cleaned; Getting started→folded into §6; `/loop`→explicit user request. Substance: (S1) WARN eliminated from evaluation (PASS/FAIL only, state machine determinism); (S2) test-suppression directive removed from delegation prompt (respects delegated capability autonomy); (S3) staleness check self-contained 6→3 steps (removes last contract.md pointer across all 12 capabilities). Sibling files: validation.yaml "prose"→"instructions", canonical filenames→artifact-ids, "inspektera_citation_required"→"audit_citation_required", WARN→FAIL in V10; exit.yaml canonical filenames→artifact-ids; deviation table Complete (D91). typecheck 0 errors; build clean; capability/contract validation PASS; check compact within limits; vitest 1278/1278 green.~~

- [x] ~~[docs:3.0.0] Audit and review ◰ design capability instructions from the ground up. Prose lives in `packages/cli/src/capabilities/design/instructions.ts` (renamed from `visualisera`); verify visual identity system flow, design artifact contract, and token/glyph vocabulary alignment with `protocol.yaml`; reconcile drift against `agentera prime --context design --format json` output. Resolved 2026-07-10: D90. Rewrote instructions following six-section spine, direct-contract delegation, voice delegation. 20,596→16,383 chars (-20%). v2 drift cut: standalone Visual identity (fold into Purpose); `agentera describe`→`agentera schema`; `agentera lint`→`agentera check lint`; contract Section 24 ghost (2×, contract tops at 23); `$AGENTERA_PROFILE_DIR`/`$XDG_DATA_HOME` resolver (v3 serves `design_context.profile.path`); `CLAUDE.md`→`AGENTS.md`; canonical filenames (VISION.md/PROGRESS.md)→artifact-id; "One file in `.agentera/`"→project root; contract.md pointer (2×); Contract values token enumeration→protocol.yaml ref; artifact-path-resolution subsection (SKILL.md owns); `AskUserQuestion`→runtime's question tool; "the sharp colleague" personality (D81); "Narration voice" (2×, D81); NEVER→MUST NOT (D71); D65 comment header marker; Getting started→folded into §6; "Agentera v2 does not ship..."→`agentera check lint`. Substance: (S1) pre-write self-audit + post-write validation folded into Write/Update (Create 7→5, Refine 4→3); Audit Step 1 uses `agentera check lint`. D85 compacted to archive. Sibling files: exit.yaml "validation script"→lint, VISION.md→vision; validation.yaml V2 VISION.md→vision; deviation table Complete (D90).~~

- [x] ~~[docs:3.0.0] Audit and review ♾ profile capability instructions from the ground up. Prose lives in `packages/cli/src/capabilities/profile/instructions.ts` (renamed from `profilera`); verify decision profiling flow, PROFILE.md artifact contract, confidence inheritance rules, and coverage audit step; reconcile drift against `agentera prime --context profile --format json` output. Resolved 2026-07-10: D89. Rewrote instructions following six-section spine, direct-contract delegation, voice delegation. 24,219→19,567 chars (-19%). v2 drift cut: standalone Visual identity (fold into Purpose); Skill introduction line; Contract values token enumeration; contract.md pointer; artifact-path-resolution subsection (SKILL.md owns); `crystallized.json` ghost; `effective profile script` ghost (4×, v3 uses `status.profile`); `$AGENTERA_PROFILE_DIR` hardcoded 6× (v3 serves `profile_context.profile.path`); `$XDG_DATA_HOME` resolver flow; `~/.claude/profile/` migration claim (no v3 code); `agentera decisions`→`agentera state decisions`; `CLAUDE.md`→`AGENTS.md`; `DECISIONS.md`→`decisions` artifact-id (D82); `Python is unavailable` in EX3 (v3 is TypeScript); `npx -y agentera`→bare `agentera`; NEVER→MUST NOT (D71); "sharp colleague"/"narration voice"/"colleague voice" personality leaks (D81); D65 comment header marker; Getting started→folded into §6; Notes on depth vs speed→cut (crystallized.json + subagent references); Step 0→folded into opening. Substance: (S1) coverage audit + extraction merged (6→4 Full steps, preserves multi-select + EX2 gating per user pushback to value each step on importance); (S2) prediction validation folded into Generate step + EX2 flagged for <3/5 accuracy (D85-D88 self-audit fold pattern); (S3) Validate V3+V4 merged (4→3 steps). D78 compacted to archive for D89 entry; D3+D4 staging entries merged for archive limit. Sibling files: exit.yaml Python ghost removed; deviation table row Complete (D89). typecheck 0 errors; build clean; capability/contract validation PASS; check compact within limits.~~

- [x] ~~[docs:3.0.0] Audit and review ▤ document capability instructions from the ground up. Prose lives in `packages/cli/src/capabilities/document/instructions.ts` (renamed from `dokumentera`); verify docs.yaml artifact mapping, path override resolution, and documentation sync flow; reconcile drift against `agentera state docs --format json` output. Resolved 2026-07-10: D88. Rewrote instructions following six-section spine, direct-contract delegation, voice delegation. 21,779→14,886 chars (-32%). v2 drift cut: standalone Visual identity (fold into Purpose); Contract values token enumeration; contract.md pointer; artifact-path-resolution subsection (SKILL.md owns); ghost contract Section 24 (contract tops at 23); `agentera describe`→`agentera schema`; `agentera query`→`agentera state query`; `agentera lint`→`agentera check lint` (4×); `agentera decisions`→`agentera state decisions`; `agentera docs/progress/todo`→closeout_context delegation; `CLAUDE.md`→`AGENTS.md` (3×); canonical filenames (VISION.md/PROGRESS.md/DECISIONS.md/HEALTH.md)→artifact-id + `.agentera/*.yaml`; Closeout context 40-line section→3-line pointer (D79); Decision satisfaction authority→Safety rail one-liner (D87); Step 0 Detect context→opening paragraph; Doc-prose enforcement→lint+report in Verify step; narration voice personality leaks (D81); NEVER→MUST NOT (D71); comment header D65 marker; Getting started→folded into §6. Substance: (S1) self-audit folded into write steps (4 standalone steps removed, D85/D86/D87 propagation); (S2) closeout context compressed to brief pointer; (S3) decision satisfaction to Safety rails; (S4) Step 0 folded into opening; (S5) doc-prose enforcement compressed to lint+report. D77 compacted to archive for D88 entry. Sibling files: triggers.yaml CLAUDE.md→AGENTS.md; deviation table row Complete (D88).~~

- [x] ~~[docs:3.0.0] Audit and review ⛶ audit capability instructions from the ground up. Prose lives in `packages/cli/src/capabilities/audit/instructions.ts` (renamed from `inspektera`); verify codebase health audit flow, health.yaml write contract, severity/confidence vocabulary, and finding taxonomy; reconcile drift against `agentera state health --format json` output. Resolved 2026-07-10: D87. Rewrote instructions following six-section spine, direct-contract delegation, voice delegation. 31,313→18,401 chars (-41%). v2 drift cut: standalone Visual identity (fold into Purpose); Skill introduction line; `agentera describe`→`agentera schema`; `agentera lint`→`agentera check lint`; `agentera decisions`→`agentera state decisions`; CLAUDE.md→AGENTS.md; canonical filenames (HEALTH.md/DECISIONS.md/PROGRESS.md/PLAN.md/VISION.md/DOCS.md)→`.agentera/*.yaml`; artifact-path-resolution subsection (SKILL.md owns); contract.md pointer; ghost `references/audit-commands.md`; per-dimension methodology for 7 standard SE dimensions (retain 3 agentera-specific: Version health, Artifact freshness, Prose health self-audit rules); subagent spawning template→brief pointer (D85/D86 propagation); quick checklist ghost reference; personality leaks ("the colleague", narration voice); NEVER→MUST NOT (D71); step numbering bloat (5b/5c); comment header D65 marker. Substance: (S1) compress per-dimension methodology; (S2) self-audit folded into Report (7→6 steps); (S3) subagent spawning compressed + citation standard relocated to State artifacts; (S4) quick checklist cut. D76 compacted to archive for D87 entry. Sibling files: validation.yaml V3 DECISIONS.md→`.agentera/decisions.yaml`; exit.yaml E1 HEALTH.md→health artifact; deviation table row Complete (D87). typecheck 0 errors; build clean; capability/contract validation PASS; check compact within limits.~~

- [x] ~~[docs:3.0.0] Audit and review ⧉ build capability instructions from the ground up. Prose lives in `packages/cli/src/capabilities/build/instructions.ts` (renamed from `realisera`); verify autonomous development execution flow, plan-task ownership, artifact fold-in, and progress evidence contract; reconcile drift against `agentera prime --context build --format json` output. Resolved 2026-07-10: D85. Rewrote instructions following six-section spine, direct-contract delegation, voice delegation. 20,662→15,081 chars (-27%). v2 drift cut: standalone Visual identity (fold into Purpose); Skill introduction line; Contract values token enumeration; contract Section 24 ghost (contract tops at 23); contract.md pointer; `agentera lint`→`agentera check lint`; `agentera decisions`→`agentera state decisions`; `agentera describe` (not a real alias); `agentera verify eval skills`→`agentera check verify eval skills`; CLAUDE.md→AGENTS.md; canonical filenames (PLAN.md/PROGRESS.md/DECISIONS.md/HEALTH.md/DOCS.md)→artifact-id + `.agentera/*.yaml`; artifact-path-resolution subsection (SKILL.md owns); full brainstorm with personality (D81 violation)→minimal 5-line vision fallback; runtime subagent spawning table→brief pointer; NEVER→MUST NOT (D71); `/loop`→explicit user request; triple-stated cycle boundary→stated once. Substance: (S1) minimal vision fallback; (S2) no pre-dispatch fog-check (rejected); (S3) plan unknowns consumption from D84; (S4) subagent spawning cut to brief pointer (propagates to optimize); (S5) self-audit folded into Log (9→8 steps). Clean comment header (no decision IDs in shipped code). typecheck 0 errors; build clean; capability/contract validation PASS; check compact within limits; vitest 1278/1278 green.~~ Prose lives in `packages/cli/src/capabilities/plan/instructions.ts` (renamed from `planera`); verify planning_context startup contract, light/normal/full mode taxonomy, acceptance criteria enforcement, and plan archive flow; reconcile drift against `agentera state plan --format json` source contract. Resolved 2026-07-09: D84. Rewrote instructions following D80 six-section spine, D79 direct-contract delegation, D81 voice delegation. 14,800→14,515 chars (-2% — substance additions balanced drift cuts). v2 ghosts cut: standalone Visual identity + Skill introduction + Contract values token enumeration + contract.md pointer (D79 pattern); `$AGENTERA_PROFILE_DIR/PROFILE.md` direct reads → served `planning_context.profile.path`; full artifact-path-resolution subsection (SKILL.md owns per D79); standalone Getting-started → folded into §6; `## How build reads PLAN.md` section (scope leak: build consumption belongs in build audit); hardcoded "sharp colleague" personality → `identity.voice` delegation (D81); re-encodings of `planning_context.startup_contract` rules (planning levels, step markers, max-tasks, cli-first orientation, artifact access boundaries, handoff expectations — runtime owns); canonical-filename references (VISION.md/DECISIONS.md/TODO.md/HEALTH.md/PROGRESS.md → artifact-id + CLI mediation); CLAUDE.md → AGENTS.md; `agentera describe`/`agentera query`/`agentera decisions`/`agentera lint` ghosts → `agentera schema`/`agentera state query`/`agentera state decisions`/`agentera check lint`; `agentera plan --format json` for state → `agentera state plan --format json`; `contract Section 24 conventions` ghost cut (contract.md tops at Section 23); `complete_for_normal_deliberation_context` → `complete_for_plan_artifact`; `agentera lint --artifact` command shape in Step 4 → delegated to capability-instruction contract. Schema version `agentera.planeraStartup.v1` Swedish residue flagged out-of-scope for prose audit. Safety rails converted from NEVER to MUST NOT (D71); added consent-on-handoff rule. Substance design: (S1) skip-criteria drops 50-LOC threshold in favor of shape-only signals (single-file/localized defect/config tweak AND touches-no-shared-abstraction), since LOC ignores shared-export ripples; (S2) effort-bias check cut entirely — plan picks ONE shape per request, no multi-option comparisons to bias (rule was misplaced from discuss); (S3) adversarial-review dismissed findings now persist in artifact `rejected:` field (issue + rationale) instead of ephemera — downstream consumers inherit adjudication; (S4) plan-level `unknowns:` field added: every full plan surfaces at least one planning-time known-unknown (task X outcome determines task Y need) with question/affects_task/resolve_by (mirrors D82 fog-aware readiness check); (S5) replan trigger defined qualitatively — re-invoke when surprises on one task alter acceptance criteria of downstream tasks, not a numeric count; (S6) `## How build reads PLAN.md` section cut, plan declares only artifact read-contract in §2 (status enum + archive lifecycle + surprises+unknowns logging); (S7) full-plan YAML formats reordered intent-first (what/why/overall_acceptance precede tasks/scope/design). Added validation rules V7 full_plan_unknowns_required and V8 adversarial_review_dismissals_persisted to enforce S3/S4. references/cli/capability-instruction-structure.md plan row Complete (D84). vitest 1278/1278 green; typecheck 0 errors; capability + contract validation PASS; check compact within limits.~~
- [x] ~~[docs:3.0.0] Audit and review ⬚ research capability instructions from the ground up. Resolved 2026-07-09: D83. Rewrote instructions following D80 six-section spine, D79 direct-contract, D81 voice delegation. Cut v2-era drift: Visual identity section, Skill introduction, Contract values token enumeration, contract.md pointer, $AGENTERA_PROFILE_DIR resolver flows, full artifact-path-resolution paragraph, standalone Getting-started, Notes on depth section, "sharp colleague" personality. Added: user motivation extraction (Step 1), transferable concepts checkpoint (named boundary between Steps 2-3), adaptation-framed Applicability Matrix, source-type broadening (URLs + local files + concept names + verbal descriptions), runtime-agnostic tool references (no MCP/tool-specific names). Safety rails converted to D71 MUST NOT. Sibling files: artifacts.yaml effective-profile-script→prime-context, research.toml prose→instructions, deviation table row filled. D71 satisfaction confirmed (modal vocabulary convention adopted across 4 audited capabilities). typecheck 0 errors; capability/contract validation PASS; check compact within limits.~~
- [x] ~~[fix:3.0.0] `cliDistributionMajor` defaults to 2 when version is missing. Resolved 2026-07-08: replaced the terminal `return 2` fallback in `cliDistributionMajor` (`compatibility.ts:146-190`) with a `package.json#version` secondary signal. When `registry.json` version-authority is missing or corrupted, the function reads `package.json#version` and returns the parsed major. When both signals miss, returns 2 and emits a one-time stderr diagnostic naming the unresolved sourceRoot. 7 regression tests added in `compatibility.test.ts` covering missing-version→3, corrupted-registry→3, both-missing→2+warn, v2-valid-registry→2 (no warn), no-version-in-pkg→2+warn, malformed-pkg→2+warn, one-shot guard. vitest 1278/1278 green; typecheck 0 errors; check compact within limits.~~
- [x] ~~[refactor:3.0.0] Rename prime JSON output field `issues` to `todo` (singular) to match the source artifact (`TODO.md`), the CLI command (`agentera state todo`), and the singular field-name convention (`plan:`, `health:`, `objective:`). Resolved 2026-06-27: `todo` is the primary field in prime JSON; `issues` remains as a deprecated alias with stderr warning; status capability dashboard template and prose updated to reference `todo:`. Stable-cut removal of the `issues` alias tracked separately as a release task.~~
- [x] ~~[fix:3.0.0] Runtime migration regex detection misses partial hand-edits. `runtimeMigration.ts:87-92` `textUsesPythonManagedEntrypoint()` only detects exact v2 patterns (`uv run`, `hooks/validate_artifact.py`, `scripts/agentera`). A user who partially hand-edited hook configs to remove Python references but did not switch to the npm entrypoint would be invisible — `pushRewireItem` reports `"noop"` even though hooks still call stale v2 scripts. Fix: add a positive check for the v3 npm entrypoint (`npx -y agentera`) and flag if neither v2 nor v3 pattern matches. Resolved 2026-07-01: added `textUsesV3NpmEntrypoint()` positive check; `pushRewireItem` now pushes a `blocked` item with diagnostic when neither v2 nor v3 entrypoint is detected instead of silently returning. 5 regression tests in `pushRewireItemGap.test.ts`; vitest 1175/1175 green; typecheck 0 errors.~~
- [x] ~~[fix:3.0.0] Add `--version` / `version` command to the CLI so consumers can check the installed Agentera version. Resolved 2026-07-01: `--version` and `version` both print the package version from `package.json`; `--format json` produces `{"version":"..."}`; `--help` for version shows usage; top-level help lists `--version` in usage and options. 11 regression tests in `version.test.ts`; vitest 1186/1186 green; typecheck 0 errors.~~
- [x] ~~[fix:3.0.0] `extractCorpusParity.test.ts` flaked on its 30000ms `testTimeout`; a single `it` ran both the TypeScript and generated Python extractor probes on a seeded `opencode.db` (30-50s combined), tipping over the default 30s limit under parallel load (observed failing during the dev.9 release and the v3 managed-content cleanup fix). Resolved 2026-07-04: split the combined `it` into two — `matches TypeScript extractor probe on seeded opencode.db` and `matches generated Python extractor probe on seeded opencode.db` — each asserting against a TS reference snapshot computed once in a shared `beforeAll` (30s `hookTimeout` for the setup). Each probe now runs ~9.4s and a Python flake fails only its own block; vitest 1266/1266 green.~~
- [x] ~~[chore:3.0.0] Replace scattered `type Dict = Record<string, any>` aliases across `packages/cli` with a shared typed JSON/value module; narrow casts at IO boundaries only (T1 landed `core/jsonValue.ts` and prime/doctor/orientation contracts; repo-wide Dict elimination deferred). Resolved 2026-06-27: all ~44 local `type Dict` aliases + 9 `Record<string, unknown>` variants + 6 stray re-export aliases eliminated; every site now imports `JsonObject` from `core/jsonValue` directly; `as` casts narrowed to IO boundaries (file/YAML/JSON.parse/stdin/registry parse) with `// cast:` comments; 5+1 sanctioned re-export passthroughs deleted; canonical deprecated `Dict = JsonObject` alias retained in `core/jsonValue.ts` (removal deferred until zero consumers). Plan C74 (7 tasks) complete; typecheck 0 errors; 1145/1145 tests green; 0 fixture drift.~~

## ⇢ Annoying

- [ ] [chore:3.0.0] Re-derive `first:` dates and confidence numbers from the current corpus on every profile write, instead of carrying them forward from the prior profile. The adversarial review in `ses_1406cbc95ffeZ8WRcSvUag387G` found 25+ entries where `first:` predated the corpus start (`first:2026-02-08` against a 2026-02-14 corpus) and several where the confidence number reflected a prior rebuild's evidence. The profilera writer must mark inherited entries explicitly (e.g., `first:2026-02-08 inherited:2026-05-30`) and demote confidence to the maximum of "re-evidenced in current corpus" and "carried forward" rather than silently blending them.

## ✓ Resolved

- [x] ~~[docs:3.0.0] Audit and review ⎘ optimize capability instructions from the ground up. Resolved 2026-07-10: D86. Rewrote instructions following six-section spine, direct-contract delegation, voice delegation. 25,990→20,622 chars (-21%). v2 drift cut: standalone Visual identity (fold into Purpose); Skill introduction line; Contract pointer + contract values token enumeration (D79); artifact-path-resolution subsection (SKILL.md owns); Experiment history analyzer contract ghost section; full brainstorm with personality (D81)→4 questions + write; runtime subagent spawning table→brief pointer (D85 propagation); `agentera lint`→`agentera check lint`; `npx -y agentera experiments`→`agentera state experiments`; `agentera decisions`→`agentera state decisions`; CLAUDE.md→AGENTS.md; `$AGENTERA_PROFILE_DIR`→`status.profile`; PROGRESS.md→`.agentera/progress.yaml`; OBJECTIVE.md/EXPERIMENTS.md in schemas→objective.yaml/experiments.yaml; `optimera_harness`→`optimize_harness` (D72); NEVER→MUST NOT (D71); `/loop`→explicit user request; benchmark context→lean seam; narration voice personality leak cut; comment header D65 marker cleaned. Substance: (S1) self-audit folded into Log (8→7 steps); (S2) subagent spawning table compressed (D85 propagation); (S3) benchmark context 30-line section→lean seam; (S4) experiments.yaml gets context block; (S5) brainstorm stripped to 4 questions.~~
- [x] ~~[fix:3.0.0] Bespoke capability contexts return error instead of degraded output. `prime.ts:43-61` — capability contexts not yet ported return an error instead of providing degraded v2-compatible output. Resolved 2026-06-30: stale TODO — the error gate (`BESPOKE_CONTEXT_CAPABILITIES` set + prime.ts:54-56 check) was dead code; the set was empty so the gate never fired. All 12 capabilities already produce valid `prime --context` output via `buildPrimeCapabilityContextPayload`. Removed the dead gate and the empty set.~~
- [x] ~~[fix:3.0.0] Doctor and prime health data sources are independent and can disagree. Resolved 2026-06-30: Decision 77 separated the concerns — doctor (app install health) and health audit (codebase quality) measure different things and should remain independent. Health staleness is already surfaced via `healthSummary`'s `stale`/`days_since_audit` fields. The `probeCli` flag (prime uses `false`, doctor uses `true`) is v2-era debt; the in-process probe was tautological in v3 (if prime is running, the dispatcher works). Decision 77 retires the `probeCli` flag, the `cliProbe.ts` module, and the probeRunner concept. Implementation deferred to realisera.~~
- [x] ~~[fix:3.0.0] `agentera check compact --apply` produces archive entry ordering that `agentera check validate artifact --artifact DECISIONS.md` rejects. Resolved 2026-06-30: `selectDecisionArchiveEntries` (`retention.ts:224`) used `yamlArchiveEntries` (descending/newest-first) for the final archive return, while `selectDecisionActiveEntries` correctly used `yamlSortEntries(..., "decisions")` (ascending). The asymmetry meant every compaction write produced a descending archive that the validator's default "ascending" rule rejected. Fixed by using `yamlSortEntries(..., "decisions")` for the archive return so both active and archive entries are emitted ascending, matching validate-artifact's expectation. Regression test added in `compaction.test.ts`.~~
- [x] ~~[feat:3.0.0] Add CLI natural-language capability routing (`agentera prime --route "<text>"`) that loads trigger schemas and returns matched capability + confidence + disambiguation candidates. Resolved 2026-06-29: `prime --route` implemented in commit e1ea812b; Layer 3-4 routing engine in commit b35abeb1; trigger schema loader in commit 6c304d89. The engine is a pure function: `(input_text, trigger_schemas) -> RouteResult` with confidence scoring (regex > substring weighting), borderline-band disambiguation, and fallback to status. Mutually exclusive with state-derived `next_action`. 100% corpus accuracy on 36 test inputs. Follow-up: trigger tuning (see open TODO above).~~
- [x] ~~[feat:3.0.0] Add confidence thresholds and disambiguation metadata to `skills/agentera/capabilities/*/schemas/triggers.yaml`. Resolved 2026-06-29: all twelve triggers.yaml files enriched with `confidence_threshold`, `disambiguates_against`, `patterns_regex`, and `borderline_band` fields per Decision 75. Contract enforcement added as V7 validator in commit 8f23b9c1. Cross-capability conflicts encoded (vision↔build on "refine the vision", optimize↔audit, design↔audit). Cross-cutting priority hints applied to status, research, profile. Backward compatible — triggers without enriched fields still validate.~~
- [x] ~~[fix:3.0.0] Tune trigger patterns and thresholds so common natural-language inputs route correctly under the scoring algorithm. The routing engine weights substring matches at 0.6 and regex matches at 1.0, so a single substring hit on a medium-priority trigger scores 42 (`0.6 * 0.7 * 100`) — below the default `confidence_threshold` of 50. For example, `prime --route "help me decide"` falls back to status instead of routing to discuss because discuss T2 has no regex pattern for that phrase. Fix: add `patterns_regex` entries to triggers for common natural-language phrasings (e.g., `\\bhelp\\s+me\\s+(decide|think|figure)\\b` for discuss T2) so regex weighting clears the threshold, or lower `confidence_threshold` for triggers whose substring-only score is too low. Apply across all twelve capabilities. (Branch: `feat/v3` — implement on `feat/v3`.) Retired by Decision 76 (2026-06-30): `prime --route` retired as a router; the LLM host routes natural language natively via trigger descriptions plus a ranked state-readiness hint (evolved `next_action`), so threshold/regex tuning of a request-derived engine is no longer applicable. Supersedes Decision 75.~~
- [x] ~~[feat:3.0.0] Evaluate state-augmented disambiguation for `prime --route`. The v1 routing engine is pure request-derived (no project state), which means it cannot disambiguate "plan the next step" (could be plan capability or status next_action) without knowing whether an active plan exists. If text-only disambiguation proves insufficient after trigger tuning, add an optional `--with-state` flag that passes limited project state (active plan, health grade, objective) to the routing engine so it can resolve state-dependent ambiguities. Do not make this the default — it breaks the pure-function contract. (Branch: `feat/v3` — implement on `feat/v3`.) Retired by Decision 76 (2026-06-30): instinct validated — state IS needed for disambiguation (e.g. 'plan the next step' needs to know whether an active plan exists), and D76 makes state-readiness core via the evolved `next_action` hint the LLM consults, rather than an opt-in `--with-state` flag on the (now-retired) `prime --route` engine. The state-dependent disambiguation cases anticipated here are resolved by the LLM owning routing with the readiness hint as foundation. Validation deferred to planera as an acceptance criterion for D76's implementation.~~
- [x] ~~[chore:3.0.0] Add a v2→v3 upgrade cleanup step to remove stale Swedish-verb-named agents and legacy skill bundles. Resolved 2026-06-27: `legacyAgentCleanup.ts` with closed set of 12 agents, `planCleanupPhase` integration, `v1LegacyCruft`-style reintroduction guard (B7-T1).~~
- [x] ~~[feat:3.0.0] Implement Decision 72 install_track consolidation. Resolved 2026-06-27: `install_track` field in prime JSON, `rootStatus` retired from public output, `bundle`→`app` rename, v2/v3 coexistence attention item (commits `992f7842`, `eb0a84fb`).~~
- [x] ~~[chore:3.0.0] Implement Decision 70 capability rename on feat/v3 — rename capability dirs to English IDs; flip capability_schema_contract; update runtime descriptors, SKILL.md dispatch, tests, AGENTS.md scopes, coexistence probe naming line; promote CHANGELOG past tense when merged. Resolved 2026-06-24: English capability IDs renamed and promoted as canonical on v3 surface; CHANGELOG entry promoted to past tense.~~
- [x] ~~[fix:3.0.0] `--force` does not bypass the major-boundary gate. Resolved 2026-06-23: `isStableSuccessorAnnounced` is now channel-aware (`nextMajorDoctor.ts:104-113`),...~~
- [x] ~~[feat:3.0.0] Add CI publish jobs for both npm channels. `.github/workflows/ci.yml` runs build/typecheck/test/sandbox/compact gates but had...~~
- [x] ~~[fix:3.0.0] validate-artifact hook rejects TODO.md edits when any severity section is empty. Resolved 2026-06-23: The...~~
- [x] ~~[fix:3.0.0] `isStableSuccessorAnnounced` hardcoded `"stable"` channel, ignoring `--channel development`. Resolved 2026-06-23: `nextMajorDoctor.ts:104-113` accepts the resolved channel...~~
- [x] ~~[fix:3.0.0] Shim `isRunnableFile` had no shebang/runtime validation or fallback. Resolved 2026-06-23: `resolve.mjs` adds `detectShebangContentMismatch` (shebang...~~
- [x] ~~[chore:3.0.0] `missingScriptCommands` dead code at `installRoot.ts:304-315` removed; the function was never called. Its intended failure...~~
- [x] ~~[fix:3.0.0] Audit capability prose for language that invites over-prescription. Resolved by Decision 71: adopted RFC...~~
- [x] ~~[fix:3.0.0] Investigate corrupted managed app script write path. Root cause was a test leak in...~~
- [x] ~~[fix:3.0.0] Prime/upgrade messaging conflict: prime recommends upgrade while upgrade blocks. Added `major_boundary_block` field to `ProjectIntegrationSummary`...~~
- [x] ~~[fix:3.0.0] Prime suggests wrong npm channel on feat/v3 checkout. Made `resolveInvokedUpdateChannel` derive channel from `cliDistributionMajor(sourceRoot)...~~
- [x] ~~[fix:3.0.0] Bundle marker version detection: `.agentera-bundle.json` can be written with a stale or placeholder version...~~
- [x] ~~[fix:3.0.0] **CLI `prime --context <capability>` returns deprecated fallback commands.** The `fallback_commands` array in the `prime...~~
- [x] ~~[chore:3.0.0] `validate_artifact.py:818` rejects every Edit/Write to TODO.md when `## ⇶ Critical` or `## ⇉ Degraded`...~~
- [x] ~~[fix:3.0.0] **resonera/instructions.md YAML schema example is stale and causes write failures.** The `decisions.yaml` example at...~~
- [x] ~~[fix:3.0.0] **resonera/instructions.md references deprecated `agentera lint` command.** Line 235 says `agentera lint --artifact <ARTIFACT>` but...~~
- [x] ~~[feat:3.0.0] Add a "Coverage Audit" pre-extraction step to the profilera capability. Before any extraction, enumerate...~~
- [x] ~~[fix:3.0.0] Surface a coverage-loss warning in the corpus envelope and hej/prime consumers when an available...~~
- [x] ~~[fix:3.0.0] Lift the hardcoded `MAX_SQLITE_SESSIONS = 60` and `MAX_SQLITE_ROWS = 100_000` caps in extractCorpus. Expose...~~
- [x] ~~[chore:3.0.0] Eliminate the Python/TypeScript extractor drift. The bundle workflow must compile the TS extractor into...~~
- [x] ~~[refactor:3.0.0] Rename `runtime_statuses[*].candidate_count` to `file_count` in extractCorpus discovery metadata; add regression test that SQLite stores...~~
- [x] ~~[docs:3.0.0] Document the `extract --no-<runtime>` flags in `cli/help.ts` with deselection wording and Coverage Audit cross-reference.~~
- [x] ~~[chore:3.0.0] Split `packages/cli/src/cli/commands/prime.ts` (~721 lines) — extract v1 artifact detection, app/doctor status, and orientation field...~~
- [x] ~~[chore:3.0.0] Share TODO severity ordering (`TODO_SEVERITY_ORDER`) between `cli/commands/prime.ts` and `cli/orientation.ts`; align with `state/todo.ts` key list~~
- [x] ~~[refactor:3.0.0] Refactor `summarizeProjectIntegration` upgrade-scenario booleans into an explicit decision table to simplify `artifactsOnly` / `runtimeOnly`...~~
- [x] ~~[refactor:3.0.0] Unify prime upgrade attention — single source for `projectIntegrationAttention`, `appStatusAttention`, and `crossMajorAppHomeAttention` branching in...~~
- [x] ~~[chore:3.0.0] Extract shared npx platform status resolver — `prime.ts` `hejBundleStatus` and `projectIntegration.ts` `resolveIntegrationTargets` both call...~~
- [x] ~~[refactor:3.0.0] Refactor `buildDoctorStatus` root-classification ladder into a typed classifier (`kind` → signals/status) to shrink the...~~
- [x] ~~[chore:3.0.0] Split `packages/cli/src/validate/lifecycleAdapters.ts` (~829 lines) by runtime adapter family before it re-hits the 1000-line monolith...~~
- [x] ~~[chore:3.0.0] Split `packages/cli/src/validate/lifecycleAdapters.ts` node-form adapter checks from legacy Python parity (`validatePackagedPythonScripts`, `validateUvRuntime`); gate Python probes...~~
- [x] ~~[chore:3.0.0] Extract a shared dispatch argv parser from `cli/dispatch/{check,state,lifecycle}.ts` and retire duplicated hand-rolled flag loops~~
- [x] ~~[fix:3.0.0] Stop silently swallowing upgrade flags in `dispatch/lifecycle` `runUpgrade` (`--runtime`, `--opencode-config-dir`, `--update-packages`); reject unknown values...~~
- [x] ~~[fix:3.0.0] Wire `doctor.ts` `defaultProbe` to the bundled TS CLI (or remove the stub) so `probeCli=true`...~~
- [x] ~~[chore:3.0.0] Consolidate npx-bundle detection — use `isNpxBundleRoot` from `core/sourceRoot.ts` everywhere; remove `isNpxBundle` duplicate in `cli/appContext.ts`~~
- [x] ~~[chore:3.0.0] Consolidate Python-parity JSON helpers (`core/pyjson.ts`, `doctor.ts` `pyJsonIndentSorted`, `startupAnalysis/helpers.ts` `pyJsonDumps`, `startupAnalysis/report.ts` `pyJsonIndent`, `setup/doctor/report.ts` `pyJsonIndent`) into...~~
- [x] ~~[chore:3.0.0] Deduplicate v1 artifact detection — `prime.ts` imports `detectV1ArtifactPairs` from `migrateArtifactsV2ToV3.ts`; local `V1_ARTIFACT_PAIRS` removed~~
- [x] ~~[chore:3.0.0] Deduplicate `installRoot` default app-home paths — `defaultAppHome` delegates to `linuxDefaultAppHome` / `macosDefaultAppHome` / `windowsDefaultAppHome`~~
- [x] ~~[docs:3.0.0] Audit and review ⌂ status capability instructions from the ground up. Resolved 2026-07-06: status prose rewritten per Decision 79 (direct contract philosophy, status owns dashboard, three-phase structure). 18K→10.4K chars (-42%). D71 RFC 2119 modal vocab applied; D72 bundle→app terminology applied; v2 residue cut (AGENTERA_HOME broken-copy recovery, 1.x managed markers, `npx skills update` cross-runtime leak, `bundle.status` gate, Step 0.5 CLI-owned checks, triple-stated do-not-read-raw-yaml rules, duplicate forbidden-command list with v2 command names, contract-values token enumeration, artifact-path-resolution subsection, `agentera query` reference, PROGRESS.md recency mention, PROFILE.md global path detail). SKILL.md dashboard rendering block delegated to status prose. triggers.yaml T2 dropped `hello`/`hi` greeting patterns and `Greetings` description (relics from when status was named `hej`). routing-model.md Layer 1 `hej` mention and Layer 5 `greetings` mention cleaned up. vitest 1271/1271 green; typecheck 0 errors; capability + contract validation PASS.~~
- [x] ~~[fix:3.0.0] Legacy Python parity checks gated behind opt-in `--legacy-python-parity` flag. `lifecycleAdapters.ts:95-96` — default `validate lifecycle-adapters` never checks Python script conventions (`validatePackagedPythonScripts`, `validateUvRuntime`). A v2 user running validate gets a clean pass without knowing their v2 Python scripts need attention. During the cutover window, Python parity should run by default when a v2 managed app is detected, or at minimum emit an advisory. (Branch: `feat/v3` — implement on `feat/v3`.) Resolved 2026-07-08: invalid premise. `feat/v3` has exactly one `.py` file — `scripts/extract_corpus.py`, a generated wrapper whose header states "TypeScript is authoritative" (full TS replacement at `packages/cli/src/analytics/extractCorpus/`). The full v2 Python surface (8,528-line CLI, 40+ scripts) exists only on `main` and is absent from `feat/v3`. The v3 `validate` command resolves root to `resolveSourceRoot()` (v3 repo/bundle), never to a v2 managed app home — no v2 user can run v3 validate against v2 Python scripts. The `--legacy-python-parity` gate is a correct maintainer seam for the one generated wrapper; both it and `legacyPythonParity.ts` get deleted when v3 promotes to `main` and `@latest` retags to the TS bundle.~~
- [x] ~~[refactor:3.0.0] Rename `prose` to `instructions` and use it consistently. Resolved 2026-07-06: folded into the status capability audit (Decision 79). The `instructions.ts` body uses "instructions" terminology throughout; "prose" keyword was checked and not present in the body. Surrounding artifacts already use "instructions" per D57/D65.~~
- [x] ~~[docs:3.0.0] Audit and review ⛥ vision capability instructions from the ground up. Resolved 2026-07-06 per Decision 81: vision instructions rewritten to D79 direct-contract + D80 six-section spine (17,076→16,667 chars (-2.4% from original; subagent-driven prose-tightening pass trimmed redundant restatements, speculative commentary, and duplicate mode-header boilerplate; structural content preserved).). v2 residue retired: `describe`/`lint`/`decisions`/`query` → v3 `schema`/`check lint`/`state decisions`/`state query`; `$AGENTERA_PROFILE_DIR` retired (vision_startup_context.profile.path serves); CLAUDE.md dropped; contract Section 24 ghost cut; Artifact-path-resolution + Contract-values subsections delegated to SKILL.md + protocol.yaml. Pre-write self-audit hoisted (DRY); Visual identity folded into Purpose; Getting started folded into §6; mode detection CLI-driven via `state query vision source.exists`; profile-stale handling added as MUST NOT. Principle brevity rule cut (linter never enforced; live artifact violates). New D80-rule: capability instructions MUST NOT hardcode personality when owning artifact (vision.yaml identity.voice) defines it — applies to all 10 remaining audits. references/cli/capability-instruction-structure.md vision row Complete (D81). vitest green; typecheck 0; validation PASS.~~
- [x] ~~[docs:3.0.0] Audit and review ❈ discuss capability instructions from the ground up. Resolved 2026-07-09: discuss instructions rewritten per Decision 82 (D80 six-section spine, D79 direct-contract delegation, D81 voice delegation). 13,816→12,045 chars (-13%). v2 ghosts cut: `agentera describe` and `agentera decisions` commands → v3 `agentera schema` and `agentera state decisions`; `$AGENTERA_PROFILE_DIR` → served `deliberation_context.profile.path`; contract Section 24 ghost cut (contract.md tops at Section 23); hardcoded "sharp colleague" personality → `identity.voice` delegation (D81); re-encoded source_contract truth tables → D79 delegation to `deliberation_context` + `raw_artifact_read_policy`; canonical-filename reads (DECISIONS.md/VISION.md/OBJECTIVE.md/TODO.md) → artifact-id + CLI mediation; contract.md pointer + Artifact path resolution + Contract values token enumeration → SKILL.md/protocol.yaml; standalone Getting-started folded into §6. Verified-real v3 retained: `agentera check lint --artifact decisions`, runtime question-tool names, per-decision satisfaction fields. Wayfinder-inspired additions: destination-fixes-scope startup, Ruled-out scratchpad field, fog-aware readiness check at Done, breadth-before-depth steering, ask-user-to-sketch, edge-signal reframing of the implementation rail. Schema validators (artifacts/validation/exit .yaml) updated to artifact-id labels. references/cli/capability-instruction-structure.md discuss row Complete (D82). New D82 rule for the 9 remaining audits: schema validator files use artifact-id labels, not canonical filenames. vitest 1278/1278 green; typecheck 0 errors; capability + contract validation PASS; check compact within limits.~~
- [x] ~~[docs:3.0.0] Backfill the D82 schema-validator rule to status (D79) and vision (D81). Resolved 2026-07-09: scrubbed all state-artifact canonical-filename mentions from status and vision instruction prose AND schema validator files. Status: 2 instruction refs (TODO.md, DECISIONS.md in routing suggestions) → artifact-id form. Vision: 6 instruction refs (VISION.md in State artifacts table, TODO.md direct-read instruction, DECISIONS.md ×3 in cross-capability, HEALTH.md in audit-informed-by) + 9 schema refs across validation/exit/triggers. Trigger patterns "write VISION.md"/"update VISION.md" → "write the vision"/"update the vision" (natural v3 phrasing, routing coverage maintained). DESIGN.md refs preserved (legitimate project-root file, not a CLI-served state artifact). vitest 1278/1278; typecheck 0; capability + contract validation PASS; check compact within limits.~~
