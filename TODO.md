# TODO

## ⇶ Critical

- [ ] [fix] Decisions are compacted into single lines only containing the question, but not the outcome, losing important context. The outcomes still live in git history, but there is currently no mechanism to retrieve them efficiently.
- [ ] [fix] Outcomes based on decisions are not tracked, which can lead to a decision being forgotten or lost due to compaction, or if several decisions has been made that puts them outside the typical query range.
- [ ] Analyze session transcripts to identify instances where linting thresholds unnecessarily compress or rewrite relevant information and determine if the thresholds should be increased.

## ⇉ Degraded

## → Normal

- [ ] [fix:2.3.x] Demote `AGENTERA_HOME` to a validated override instead of the normal app-home source of truth. Normal installed execution should resolve directly to the platform app directory (`XDG_DATA_HOME`/macOS Application Support/Windows AppData) unless an explicit valid override is supplied; runtime adapters such as OpenCode must not blindly propagate stale legacy values like `~/.agents/agentera`, and doctor/upgrade/tests should preserve custom-install and hermetic-test support without making stale parent-process environments authoritative.
- [ ] [feat] Plan a CLI startup state envelope from the Decision 51 state-access metric. Direct OpenCode analysis found Agentera CLI state calls followed by raw artifact reads/greps/globs before implementation work, replacing an uncommitted route/intro startup-window draft that had found zero qualifying windows; follow-up planning should design a capability-ready state envelope or equivalent CLI output that prevents redundant `.agentera/*.yaml` and root artifact reads after `agentera plan/progress/docs/todo/decisions`.

## ⇢ Annoying

- [ ] [fix] Investigate, review, and improve `agentera doctor` output so a ready/healthy status does not always show a `Next:` section that can be interpreted as required action; distinguish optional repair/upgrade guidance from actual required follow-up, and fix command formatting issues such as missing whitespace before `--dry-run`.

## Resolved

- [x] ~~[test] Add a manual startup state benchmark suite for Decision 52.~~ · resolved by the manual `mage bench:startupState` benchmark suite: explicit runtime/path approval is required before local history access, fixture-backed tests cover consent refusal, successful synthetic measurement, missing-store degradation, aggregate JSONL schema, privacy exclusions, and no repository-local generated outputs, and durable retained evidence is limited to user-local `runs.jsonl`, `latest-report.json`, and `latest-report.md`; the separate CLI startup state-envelope follow-up remains open.
- [x] ~~[fix] Remove shell startup mutation from Agentera runtime setup and repair.~~ · resolved by the shell-rc removal plan: setup, doctor, upgrade preview/apply, repair guidance, managed runtime refresh, stale-remnant cleanup, and default non-live smoke now keep shell startup files user-owned and unchanged; stale exact deprecated-default `AGENTERA_HOME` is treated as recoverable residue, custom unknown app homes remain blocked, package-manager updates stay opt-in, and final sync records no release-target selection or version-bearing file changes.
- [x] ~~[feat:2.3.x] Add Profilera corpus mining parity for OpenCode and GitHub Copilot.~~ · resolved by the Profilera corpus runtime parity plan: extraction now reads OpenCode `opencode.db` and GitHub Copilot `session-store.db` stores read-only, preserves bounded status/reason diagnostics for missing, locked, sparse, and schema-divergent stores, proves Claude/Codex/OpenCode/Copilot fixture parity in focused tests and default smoke, and records no release-target selection or version-bearing file changes.
- [x] ~~[fix:2.3.1] Recover from runtime-injected stale old-default `AGENTERA_HOME` without asking users to unset environment variables.~~ · resolved by the 2.3.1 runtime-injected app-home recovery work: deterministic tests cover the exact deprecated default, upgrade previews platform app-home recovery when no explicit `--install-root` was supplied, doctor remains read-only, explicit install-root values stay authoritative, safe user-data-only app homes can receive managed code under `app/`, OpenCode/runtime defaults no longer prefer the old root layout, and skill guidance presents app-home recovery instead of stale-env unsetting.
- [x] ~~[fix:2.3.0] Retire the deprecated default `~/.agents/agentera` location when the platform app home is selected, and confirm supported v1 Markdown project artifacts are migrated automatically.~~ · resolved by the 2.3.0 legacy default retirement follow-up: default bundle upgrade previews and applies `retire-legacy-default-app-home`, moves known user state into the selected app home before removing managed legacy files, leaves preview mode no-write, and regression coverage proves canonical v1 Markdown artifacts are handled by the artifacts phase.
- [x] ~~[feat:2.3.0] Implement Decision 47 app-home migration: replace live bundle terminology with app terminology, redefine `AGENTERA_HOME` as the durable platform data home, place the managed Agentera app under `AGENTERA_HOME/app`, keep profile/usage/history/corpus data at the home root, make `agentera doctor` detect legacy bundle-root installs as `migration_required` with exact dry-run/apply commands, and make `agentera upgrade` own idempotent migration writes.~~ · resolved by 2.3.0 Tasks 1-3: app-home public output, doctor legacy-root diagnostics, and upgrade-owned idempotent migration now agree on `AGENTERA_HOME` as app home, `$AGENTERA_HOME/app` as managed app root, and home-root user data; no package update, installed app refresh, publication, tag, push, vision edit, or objective-state edit occurred.
- [x] ~~[fix:2.3.x] Make artifact schema discovery explicit for installed capabilities. During `visionera`, the vision artifact schema had to be found by manually searching multiple roots before locating `schemas/artifacts/vision.yaml`; the capability prose references `skills/agentera/schemas/artifacts/vision.yaml` without giving an installed-root-independent lookup path or CLI command. Add a routine `agentera` command or capability-local reference that resolves the schema path from the same root used for the active invocation.~~ · resolved by Task 5: producing capability prose points agents at `agentera describe --format json` artifact schema entries, and missing schemas are reported against the active app-home/managed-app/skill-root/runtime-root model instead of requiring manual root searches.
- [x] ~~[fix:2.3.x] Add a doctor check for split Agentera install roots used by capability invocation. One session involved the skill runtime under `~/.config/opencode/skills/agentera/`, the durable skill copy under `~/.agents/skills/agentera/`, and the CLI bundle under `~/.agents/agentera/`; this made it unclear which prose, schemas, and helper scripts were authoritative. `agentera doctor` should report these roots, version/marker status, and missing expected helper/schema surfaces before a capability asks agents to use them.~~ · resolved by Tasks 2 and 5: doctor reports app, managed app, skill, runtime, and source roots for install/runtime diagnostics, while helper and schema discovery failures point at the same active app model without broadening doctor beyond Decision 48's boundary.
- [x] ~~[fix:2.3.x] Ship or remove the referenced `scripts/self_audit.py` capability helper.~~ · resolved by exposing the self-audit checks through `agentera lint --artifact <ARTIFACT>` with stdin/file/text input, bounded diagnostics, JSON output, and strict mode; producing capability prose now points at the CLI instead of a hidden installed skill-root helper path.
- [x] ~~[fix:2.3.x] Finish the app-home public JSON surface migration before 2.3.0.~~ · resolved by removing public `installRoot` and `installRootSource` from `agentera doctor --json` and nested `agentera hej --format json` app status, updating CLI-visible vocabulary labels to app-home wording, and extending `scripts/validate_app_home_contract.py` to fail on stale structured output.
- [x] ~~[feat:2.2.3] Suggest profilera run when stale: track time or cycles since last profilera run and nudge when it's due.~~ · resolved by `agentera hej` profile staleness output: stale profiles now emit a direct "suggest running profilera to refresh PROFILE.md" attention item, structured `profile.suggested_action`, configurable threshold evidence, deterministic date tests, and invalid threshold fallback to the default.
- [x] ~~[test:2.2.3] Release-readiness gate: run opt-in all-runtime live install proof for Claude Code, OpenCode, Copilot CLI, and Codex CLI.~~ · resolved by adding `run_claude_live_section` to `scripts/smoke_live_hosts.py` (the other three runtimes were already covered); `--live --yes` now exercises all four runtimes with isolated temp state, explicit consent via cost_gate, SKIP for auth/network failures, and Codex 0.129.0 trusted hook-state setup for the temporary apply_patch hook wrapper. Passing evidence does not require a release; only detected product/doc defects feed a patch or minor.
- [x] ~~[test] Add an explicit opt-in Claude Code live model-host smoke when Claude Pro/Max or API access is available.~~ · resolved by the 2.2.3 live-smoke harness work: `scripts/smoke_live_hosts.py --live --yes` includes a Claude Code section behind explicit consent, with temporary `HOME`, `XDG_*`, `CLAUDE_CONFIG_DIR`, `AGENTERA_HOME`, and `CLAUDE_PLUGIN_ROOT` state plus SKIP handling for missing auth/network access.
- [x] ~~[fix:2.2.2] Stop repeated fresh-project hej from routing back to visionera.~~ · resolved by saved-context routing: first-run empty projects still route to visionera, saved context without vision routes to resonera, and vision-only state avoids the hej-to-visionera loop.
- [x] ~~[fix:2.2.2] Wire TODO Resolved compaction into artifact writes.~~ · resolved by invoking todo-resolved compaction after valid TODO writes, preserving open sections and type-prefixed summaries while avoiding generated IDs.
- [x] ~~[fix] Fix Copilot current-checkout plugin smoke blocked by escaping skills path.~~ · fixed in Cycle 153; root `plugin.json` loads shared `skills/` through `copilot --plugin-dir`
- [x] ~~[test] Add live Copilot and Codex host behavior smoke tests.~~ · narrowed by Cycle 152; Codex `$hej` and Copilot installed skills passed, current-checkout Copilot plugin loading remains blocked
- [x] ~~[feat] Add native Copilot and Codex packaging metadata.~~ · resolved without issue ID; Task 2 lacked a dedicated PROGRESS entry, now covered by Cycle 134 rollup
- [x] ~~[chore:1.17.0] Bump native runtime support release metadata to 1.17.0.~~ · resolved without issue ID; Cycle 133 added missing evidence and preserved marketplace `profilera` at `2.8.0`
- [x] ~~[feat:2.2.0] Implement Decision 45 agent-ready state interface: preserve routine state commands, hard-rename `bundle-status` to `doctor` with no compatibility alias, add structured routine output, sparse field selection, runtime describe introspection, input hardening, compatibility/release-readiness proof, and final pre-closure state sync.~~ · resolved by the Implement Decision 45 Agent-Ready State Interface For 2.2.0 plan: Tasks 1-7 are complete, Task 8 final sync records TODO/changelog/progress/docs/plan pre-closure evidence, `agentera doctor` is the current self-check command, `bundle-status` remains intentionally unavailable, Decision 43 aliases remain slash-only routes, and no tag, push, package publication, package update, installed bundle refresh, version beyond 2.2.0, vision edit, or objective edit was performed. Optional Claude live-smoke and generated install-root contract work remain separate open follow-ups.
- [x] ~~[refactor:2.2.0] Apply Decision 44 plain-English vocabulary: remove `freshness`, non-Git `checkpoint`, most `guard`, `Reality Verification Gate`, `conductor protocol`, `memory layer`, `operating record`, prose/label `drift`, and `DTC` from docs/vocabulary, capability prose, safe labels/tests, and active plan wording; preserve stable IDs/enums unless a deliberate migration is planned.~~ · resolved by the Apply Decision 44 Plain-English Vocabulary For 2.2.0 plan: replacement boundary, user-facing docs, capability/protocol instructions, maintained diagnostics/tests/safe labels, executable vocabulary coverage, 2.2.0 release-inclusion evidence, and final pre-closure current-state check are recorded without a later version, publication, tag, push, package update, or installed bundle refresh. Protected compatibility exceptions remain documented as deliberate exceptions, not unfinished cleanup; optional Claude live-smoke and install-root/generated-contract follow-ups remain separate.
- [x] ~~[feat:2.2.0] Implement one primary plain `/agentera <alias>` per capability while preserving canonical Swedish capability names: `status`→hej, `vision`→visionera, `discuss`→resonera, `research`→inspirera, `plan`→planera, `build`→realisera, `optimize`→optimera, `audit`→inspektera, `document`→dokumentera, `profile`→profilera, `design`→visualisera, `orchestrate`→orkestrera. Keep secondary phrases in trigger schemas and update README, docs/vocabulary, and tests.~~ · resolved by the Implement Capability Route Aliases plan: Decision 43 alias contract, schema-backed routing, secondary wording preservation, README/vocabulary documentation, full alias behavior proof, 2.2.0 version alignment, and this final state sync. No tag, push, package publication, package-manager update, or installed bundle refresh was performed; optional live-smoke and install-root/generated-contract follow-ups remain separate.
- [x] ~~[feat:1.19.0] Build `scripts/usage_stats.py` per Decision 32: detect skill invocations via workflow markers across the Claude Code + OpenCode session corpus, pair with exit signals to count completed workflows, tag slash-vs-NL triggers, default cross-project with a `--project` flag, write markdown to `~/.local/share/agentera/USAGE.md` plus a brief stdout summary, and accept `--json` for machine output.~~ · fixed by Suite Usage Analytics PLAN Tasks 1-3 (commits 7f536a0, dcfb872, 4c4b907), documented by Task 4 (e12c628), released in 1.19.0 by Task 5 (cc1eacc)
- [x] ~~[feat:1.12.0] Implement optimera multi-objective support (Decision 31).~~ · fixed in bd88f63..845a387 (per-objective subdirs under .agentera/optimera/, active-objective inference, SPEC.md/SKILL.md/hooks/DOCS.md updates, version bump 1.12.0)
- [x] ~~[fix] Fix profile path mismatch after Decision 28.~~ · fixed — all SKILL.md, SPEC.md, DOCS.md, DOCS template, README.md, opencode adapter doc, and 12 contract.md files updated to `$PROFILERA_PROFILE_DIR/PROFILE.md` with platform-appropriate defaults; 292 tests pass, validate_spec.py 0 errors
- [x] ~~[feat] Move profile path to XDG-standard agentera data directory (Decision 28).~~ · fixed in 32cd8c7..4870d80 (XDG default with platform detection, auto-migration from ~/.claude/profile/, OpenCode plugin sets PROFILERA_PROFILE_DIR at init)
- [x] ~~[feat:1.10.0] Implement Section 21 session corpus contract (Decision 27).~~ · fixed in 74e539c..277e559 (spec envelope format, multi-runtime corpus builder, self-validation, SKILL.md Steps 1-2 update, 14 new tests, version bump 1.10.0)
- [x] ~~[feat] Implement plan-relative staleness detection (Decision 22).~~ · fixed in cd519b0..7e3255b
- [x] ~~[feat] Deliver the phase tracking spec.~~ · spec delivered in 2caa9cb, SKILL.md enforcement dropped per Decision 22 (replaced by plan-relative staleness detection)
- [x] ~~[docs] Add test proportionality convention (Decision 21).~~ · fixed in 0c67553..61e7f7d
- [x] ~~[fix] Make `scripts/agentera health` select the newest health entry consistently with `hej`, and add a newest-first regression test.~~ · fixed by aligning `_query_health` with newest-first health audits, adding a direct regression for Audit 20 before Audit 10, and repairing one malformed health YAML scalar so the current project smoke reads Audit 20.
- [x] ~~[feat] Build orkestrera capability (Decision 20).~~ · fixed in 1858de0..3decb87
- [x] ~~[feat] Add retry caps in the spec.~~ · superseded by orkestrera (Decision 20)
- [x] ~~[feat] Add structured acceptance-criteria verification for planera → realisera handoff.~~ · superseded by orkestrera (Decision 20)
- [x] ~~[feat] Add headless runner script.~~ · superseded by orkestrera (Decision 20)
- [x] ~~[feat] Add separated evaluator for realisera.~~ · superseded by orkestrera (Decision 20)
- [x] ~~[feat] Add narration voice principle and warm up skill narration (Decision 17).~~ · fixed in 2ee4e99..de45a45
- [x] ~~[refactor] Refine skill voice to match "sharp colleague" standard (Decision 16).~~ · fixed in e17d588..067a251
- [x] ~~[feat] Implement formatting decisions (Decision 14).~~ · fixed in 8dfb6fe..e73d31e
- [x] ~~[docs] Refresh DOCS Plan index after the completed runtime portability plan.~~ · resolved without commit by explicit user request; Task 8 set DOCS and PLAN to one current completed Audit 11 state
- [x] ~~[docs] Treat Audit 11 as the resolution marker for stale historical collector-unavailable wording.~~ · resolved without commit by explicit user request; Task 8 records collector cleanup complete while keeping live host behavior deferred
- [x] ~~[fix:2.2.1] Close the completed-plan lifecycle gap.~~ · resolved by the Agentera 2.2.1 Contract And Entrypoint Correctness Patch plan: complete plans now require `header.status: complete` plus every task complete before closure, blocked or incomplete plans remain visible, realisera/orkestrera prose requires sweep/archive/remove ordering, and `agentera hej` no longer surfaces stale completed-plan context as active work.
- [x] ~~[fix:2.2.1] Enforce the full-plan adversarial review contract.~~ · resolved by hard validation in `hooks/validate_artifact.py`: full plans now require `header.reviewed`, parseable non-zero `header.critic_issues`, `design`, and task acceptance criteria, while light plans may still omit full-review fields.
- [x] ~~[fix:2.2.1] Make a bare user message `hej` trigger the Agentera brief path.~~ · resolved by dispatcher/hej guidance, schema-backed routing coverage, and `fixtures/semantic/hej-bare-message.md`, which requires `agentera hej`, the dashboard markers, and `⌂ hej` rather than generic greeting behavior.
- [x] ~~[fix:2.2.1] Refine Agentera question-tool gating in `skills/agentera/SKILL.md` and capability handoff prose.~~ · resolved by gating initial Agentera/hej briefs to free-form continuation unless bounded choices are requested, limiting mid-conversation native question tools to real non-terminal alternatives or consequential Proceed/Cancel, and preserving glyph-forward downstream labels.
- [x] ~~[refactor] Deepen artifact write validation into explicit hook Modules.~~ · resolved by splitting `hooks/validate_artifact.py` into explicit `RuntimeEventParser`, `ArtifactSchemaValidator`, and `HookCliAdapter` interfaces while keeping the executable hook path stable. `tests/test_hook_v2.py` now targets named interfaces and full hook behavior instead of private runtime parser functions; runtime adapter coverage and full pytest passed. Health command freshness, optional Claude live smoke, and generated install-root follow-up remain separate open findings.
- [x] ~~[chore:2.2.3] Generate the JavaScript runtime install-root contract from the shared Python/interface fixture.~~ · resolved by creating `scripts/generate_js_install_root_contract.py` and embedding `INSTALL_ROOT_CONTRACT` in `.opencode/plugins/agentera.js`; generator correctly reads `.agentera/install_root_interface_model.yaml` and the `--write`/`--verify` flow reports OK; 88 tests pass
- [x] ~~[refactor] Deepen the artifact registry Module.~~ · resolved by the Deepen Artifact Registry Interface plan; `scripts/artifact_registry.py` projects canonical artifact schema metadata plus `references/artifacts/artifact-registry-interface-model.yaml`, capability-local schemas reference `artifact_id` plus `local_role`, `scripts/validate_cross_capability.py` and bounded CLI artifact-resolution reads consume registry facts, and `tests/test_validate_cross_capability.py` now verifies relationships and special cases through registry-backed fixtures. Artifact write validation, health command freshness, optional Claude live smoke, and generated install-root follow-up remain separate open findings.
- [x] ~~[refactor] Deepen capability routing and exit vocabulary.~~ · resolved by the Deepen Capability Routing And Exit Vocabulary plan; `skills/agentera/SKILL.md` now stays thin, schema-backed routing tests exercise trigger-schema priorities, thresholds, disambiguation, and hej fallback through `tests/routing_test_interface.py`, and `protocol.yaml` remains the authority for complete, flagged, stuck, and waiting exit signals. Artifact registry, artifact write validation, health command freshness, optional Claude live smoke, and generated install-root follow-up remain separate open findings.
- [x] ~~[refactor] Make `skills/agentera/capability_schema_contract.yaml` the real capability contract.~~ · resolved by the Deepen Capability Schema Contract plan; `skills/agentera/capability_schema_contract.yaml` and `scripts/capability_contract.py` now own the executable capability schema Interface for directory requirements, required groups, entry fields, trigger priorities, deprecation behavior, group prefixes, and capability primitive-reference field mappings. `scripts/validate_capability.py` consumes the loaded model, while `protocol.yaml` remains the authority for protocol primitive groups and values. Routing/exit vocabulary, artifact registry, artifact write validation, health command freshness, and optional Claude live smoke remain separate open findings.
- [x] ~~[refactor] Deepen package manifest knowledge into one registry separate from install-root classification.~~ · resolved by the Deepen PackageManifest Registry plan; `references/adapters/package-registry.yaml` and `scripts/package_registry.py` now own version authority, version surfaces, runtime package manifests, bundle surfaces, package commands, docs targets, and release policy. Validators, upgrade, docs, and tests consume registry facts through consumer views. Install-root classification, RuntimeAdapter ownership, artifact write validation, and release publication remain separate open findings.
- [x] ~~[refactor] Deepen runtime adapter knowledge into one registry.~~ · resolved by the Deepen RuntimeAdapter Registry plan; `scripts/runtime_adapter_registry.py` and `references/adapters/runtime-adapter-registry.yaml` now own runtime identity, host detection, lifecycle events, artifact-validation support, config targets, diagnostics, and documentation claims for Claude Code, OpenCode, Copilot CLI, and Codex CLI. Lifecycle validation, setup doctor, upgrade planning, runtime tests, and adapter docs consume or validate registry facts. Package manifest registry, install-root classification semantics, and artifact write validation remain separate open findings.
- [x] ~~[refactor] Deepen Agentera install-root identity, discovery, classification, diagnostics, and write-safety into one shared Module.~~ · resolved by the Deepen Agentera Install Root Module plan; `scripts/install_root.py` now owns Python setup, doctor, upgrade, and installed-bundle classification, runtime fixture tests validate the OpenCode contract, and remaining RuntimeAdapter registry, package manifest registry, and optional generated cross-language contract work are tracked separately
- [x] ~~[fix] Align recent `phase: verify` progress entries with the protocol phase model, or deliberately add `verify` to `protocol.yaml`, progress schema validation, and consumers.~~ · fixed by mapping recent realisera cycles and semantic fixtures to `phase: build`, adding hook enum enforcement, and adding a regression that rejects `phase: verify`
- [x] ~~[test] Run `uv run scripts/smoke_live_hosts.py --live --yes` after explicit approval for live model calls. Required before claiming cutover-ready model-host behavior across Claude/Codex/Copilot/OpenCode.~~ · passed on 2026-05-05 for Codex AGENTERA_HOME/query, Codex apply_patch hooks, Copilot AGENTERA_HOME/query, and OpenCode AGENTERA_HOME/query via `opencode run --pure`; Claude Code live smoke remains deferred separately
