#!/usr/bin/env -S uv run --script
# /// script
# requires-python = ">=3.10"
# dependencies = ["pyyaml"]
# ///
"""Deprecated: use scripts/migrate_artifacts_v1_to_v2 instead."""
import sys
import os
import subprocess
print("WARNING: scripts/migrate_v1_to_v2 is deprecated. Use scripts/migrate_artifacts_v1_to_v2.", file=sys.stderr)
script_dir = os.path.dirname(os.path.abspath(sys.argv[0])) if len(sys.argv) > 1 else os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else os.getcwd()
target = os.path.join(script_dir, "migrate_artifacts_v1_to_v2")
raise SystemExit(subprocess.run([sys.executable, target] + sys.argv[1:]).returncode)
