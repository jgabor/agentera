{
  "schemaVersion": "agentera.packagePublication.v2",
  "owner": "repository/package scripts",
  "invariants": {
    "preparation": "Preparation is pure and registry-independent. It accepts an explicit target version and source commit, changes only package version and agentera.gitRef, and leaves the diff for review and commit.",
    "sourceQualification": "Source qualification never repairs source. It issues a content-addressed receipt only after every declared source gate succeeds against a clean committed tree. A matching receipt is a deterministic local cache, not an authorization boundary.",
    "candidateQualification": "Candidate qualification validates a current source receipt, constructs one retained immutable tarball with the contract-required retained mode, proves dry-pack observation equivalence, and runs a local exact-artifact smoke in an empty isolated npm state before any registry effect.",
    "publication": "Registry publication consumes the retained candidate bytes through one measured stage, independent exact-version L2, and promote envelope. Stage and promotion include their required registry convergence and observational checks. The envelope never reconstructs an artifact, reruns source qualification, or rolls a tag back.",
    "approval": "Registry mutation requires an explicit approval bound to the candidate receipt digest, target version, integrity, package, registry, and tag. A branch push is never approval.",
    "replay": "A matching exact version, integrity, and required tag state is a successful no-upload replay. Conflicting integrity, an escaped artifact, a stale tag, or any unrecognized registry state fails before mutation.",
    "credentials": "Registry inspection runs before credentials are required. Only the npm mutation child receives a mode-0600 temporary user config. Tool probes and every npm, pnpm, and npx child, including receipt validation during stage and promotion, use isolated user/global config, home, cache, and sanitized npm/pnpm environments.",
    "output": "Every transaction phase emits a bounded start and end result with package, version, phase, outcome, elapsedMs, executed, reused, and nextAction. The qualified-publication coordinator emits one content-bound timing receipt in JSON or equivalent human output with measured component and total durations, strict budget status, first failure, reconciliation, and replay state. Diagnostics redact credentials and private absolute paths."
  },
  "packages": {
    "development": {
      "label": "development TypeScript package",
      "packagePath": "packages/cli",
      "manifestPath": "packages/cli/package.json",
      "expectedTag": "next",
      "preparation": "incrementDevPrerelease",
      "construction": "isolatedTypeScriptPackage",
      "smoke": ["npx", "-y", "agentera@{version}", "--version"],
      "localSmoke": ["agentera", "--version"]
    },
    "stable": {
      "label": "transitional stable shim",
      "packagePath": "packages/cli/shim",
      "manifestPath": "packages/cli/shim/package.json",
      "expectedTag": "latest",
      "preparation": "incrementPatch",
      "construction": "stableShim",
      "smoke": ["npx", "-y", "agentera@{version}", "--version"],
      "localSmoke": ["agentera", "--version"],
      "sourceProvenance": {
        "rule": "agentera.gitRef must name a commit with exactly the current shim packaged inputs. Only package.json version and agentera.gitRef may differ because preparation owns those fields.",
        "packagedInputs": [
          "packages/cli/shim/bin",
          "packages/cli/shim/lib",
          "packages/cli/shim/README.md",
          "packages/cli/shim/LICENSE"
        ],
        "permittedManifestDifferences": ["version", "agentera.gitRef"]
      }
    }
  },
  "ci": {
    "repository": "jgabor/agentera",
    "qualificationWorkflow": {
      "name": "Qualify release candidate",
      "path": ".github/workflows/qualify-candidate.yml",
      "ref": "refs/heads/feat/v3"
    },
    "publicationRunBinding": {
      "beforeArtifactDownload": [
        "run.id",
        "run.repository.full_name",
        "run.head_repository.full_name",
        "run.name",
        "run.path",
        "run.head_branch",
        "run.head_sha",
        "run.event",
        "run.conclusion"
      ],
      "refRule": "qualificationWorkflow.ref is a full refs/heads branch ref; run.head_branch must equal its exact branch name",
      "beforeEnvironmentApproval": "The content-validated candidate receipt metadataCommit must equal the API-backed run.head_sha"
    }
  },
  "qualification": {
    "source": {
      "component": "release-source",
      "inputs": [
        "tracked_tree_except_package_version_gitRef",
        "verification_policy",
        "lockfile",
        "node",
        "npm",
        "pnpm",
        "gate_set"
      ],
      "gates": [
        { "name": "source", "command": ["pnpm", "-C", "packages/cli", "run", "test:source"] },
        { "name": "stress", "command": ["pnpm", "-C", "packages/cli", "run", "test:stress"] },
        {
          "name": "performance",
          "command": ["pnpm", "-C", "packages/cli", "run", "test:performance"]
        },
        { "name": "package", "command": ["pnpm", "-C", "packages/cli", "run", "verify:package"] },
        {
          "name": "generated-overlap",
          "command": ["pnpm", "-C", "packages/cli", "run", "verify:generated-overlap"]
        },
        { "name": "typecheck", "command": ["pnpm", "-C", "packages/cli", "run", "typecheck"] },
        { "name": "build", "command": ["pnpm", "-C", "packages/cli", "build"] },
        {
          "name": "compact",
          "command": ["node", "packages/cli/dist/bin/agentera.js", "check", "compact"]
        },
        {
          "name": "capability-contract",
          "command": [
            "node",
            "packages/cli/dist/bin/agentera.js",
            "check",
            "validate",
            "capability-contract",
            "--format",
            "json"
          ]
        }
      ],
      "performanceEvidenceSchema": "agentera.entityAuthorityPerformanceEvidence.v1",
      "overlapEvidenceSchema": "agentera.generatedOverlapEvidence.v1",
      "gateEvidence": "A source receipt contains the exact nine governed gates in contract order. Every gate has its governed origin and phase, outcome passed, a finite non-negative duration, the exact executed/reused shape, and gate-relevant observations. Digest validation precedes semantic validation; either kind of one-field tampering fails closed.",
      "dag": {
        "batchA": ["generated-overlap", "stress", "performance", "typecheck"],
        "generatedOverlapOrigins": ["source", "package", "build", "generated-overlap"],
        "barrierB": ["compact", "capability-contract"],
        "generatedOutputWriter": "generated-overlap",
        "overlapCleanupMarginMs": 10000,
        "overlapParentReconciliationMarginMs": 4000,
        "failure": "The coordinator passes generated-overlap the actual source deadline. Overlap stops owned work 10000ms before that deadline, settles and cleans its generated state, and returns at least 4000ms before the deadline for parent reconciliation. The first observed owner failure cancels only cancellable process groups. generated-overlap is cooperatively stopped and never force-killed; barrier B and receipt issuance remain blocked after any batch failure.",
        "isolation": "Every batch and barrier child receives a distinct HOME, cache, user config, global config, and report output."
      },
      "parallelism": "Batch A runs generated-overlap, stress, performance, and typecheck concurrently. generated-overlap is the sole checkout generated-output writer and sole execution origin for source, package, build, and overlap evidence. After it and all other batch owners pass, compact and capability-contract run concurrently as readers of the settled generation."
    },
    "candidate": {
      "component": "release-candidate",
      "inputs": [
        "source_receipt",
        "metadata_commit",
        "target_version",
        "source_commit",
        "adapter",
        "package",
        "registry",
        "expected_tag",
        "artifact_bytes",
        "artifact_mode"
      ],
      "retainedArtifactMode": "0444",
      "gates": [
        "release-metadata",
        "dry-pack-observation-equivalence",
        "local-exact-artifact-smoke"
      ],
      "artifact": {
        "receipt": "candidate-receipt.json",
        "sourceReceipt": "source-receipt.json",
        "mode": "0444",
        "mustRemainWithinCandidateDirectory": true
      }
    }
  },
  "trust": {
    "local": "A same-principal source receipt is a deterministic cache only. It cannot authorize registry mutation.",
    "ci": "CI registry mutation requires the transferred exact artifact and receipts, a CI attestation bound to the candidate receipt and source run, and an explicit candidate-bound approval. OIDC provenance is deferred."
  },
  "registryStateTable": [
    {
      "state": "absent",
      "exactVersion": "absent",
      "candidateTag": "absent",
      "expectedTag": "older-or-absent",
      "stage": "upload exact artifact once",
      "promote": "blocked"
    },
    {
      "state": "staged",
      "exactVersion": "matching integrity",
      "candidateTag": "target version",
      "expectedTag": "older",
      "stage": "replay without upload and run observational smoke",
      "promote": "move expected tag forward after exact-version qualification"
    },
    {
      "state": "promoted",
      "exactVersion": "matching integrity",
      "candidateTag": "target version or retained history",
      "expectedTag": "target version",
      "stage": "replay without upload",
      "promote": "replay without tag mutation"
    },
    {
      "state": "conflict",
      "exactVersion": "missing, changed, conflicting, or tag ahead",
      "candidateTag": "any",
      "expectedTag": "any",
      "stage": "fail before mutation",
      "promote": "fail before mutation"
    }
  ],
  "benchmark": {
    "environment": "Ubuntu runner or clean local Node 22, npm 10, and pnpm 10.30.3; record exact tool versions in receipts.",
    "fixture": "clean committed checkout and an external empty candidate directory; no tracked or checkout-local generated candidate files.",
    "cacheNetwork": "Preflight uses no registry call. Every qualification DAG child and each smoke use a distinct empty HOME, npm cache, user config, global config, and report output; public npm dependency network access is allowed and recorded as cold-cache. Publication reads the registry before credentials.",
    "repetitions": 3,
    "statistic": "Qualification reports median wall milliseconds over exactly three cold-cache repetitions. Concurrent owner-duration totals are reported separately and reconcile through measured DAG wall time rather than being summed as sequential time. Qualified publication reports one actual approved transaction because registry mutation is not a benchmark repetition; replays run the same measured envelope and report reused registry phases.",
    "coordinator": "packages/cli/scripts/release-benchmark.mjs has separate qualification and publication commands. qualification runs only preflight and source-plus-candidate qualification and never receives npm credentials. publication runs only the approved stage, independent exact-version L2, and promote commands, measures them with one monotonic deadline, and never runs source qualification.",
    "qualifiedPublication": {
      "command": "node packages/cli/scripts/release-benchmark.mjs publication --adapter ADAPTER --candidate-dir DIR --receipt-file FILE",
      "sequence": [
        "stage retained bytes on the candidate tag, wait for exact integrity convergence, and observe an isolated exact-version registry smoke",
        "run an independent exact-version L2 consumer check while the public tag is unchanged",
        "promote the expected tag forward, wait for tag and integrity convergence, and observe the final exact-version registry smoke"
      ],
      "receiptBindings": [
        "candidate_receipt_sha256",
        "metadata_commit",
        "package",
        "version",
        "artifact_sha256",
        "artifact_integrity",
        "source_run_id"
      ],
      "timing": "The coordinator measures commands with a monotonic clock. Caller-supplied elapsed data is not accepted. Component floors plus unattributed elapsed time must equal the measured total, and total elapsed time must be strictly less than qualifiedPublicationMs.",
      "failure": "The first child failure label or timeout owner is retained, later phases do not start, and no rollback is attempted. A retry uses the same exact candidate; stage and promote replay matching registry state without upload or backward tag movement."
    },
    "timeouts": {
      "preflightMs": 30000,
      "sourceQualificationMs": 300000,
      "qualifiedPublicationMs": 120000
    },
    "failureRule": "The first observed failing owner remains the reported label. Cancellable peers are stopped, generated-overlap is allowed to settle without forced termination, barrier B and receipt issuance are blocked, and completed/cancelled failures are reported. No timeout or failure authorizes repair, candidate work, or registry mutation."
  },
  "bounds": {
    "registryAttempts": 6,
    "registryDelayMs": 5000,
    "commandTimeoutMs": 120000,
    "diagnosticCharacters": 1000
  }
}
